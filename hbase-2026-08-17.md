# HBase 面试题（2026-08-17）

> 本文精选 5 道高频 HBase 面试题，涵盖架构、RowKey 设计、存储结构、MemStore/HFile、协处理器等核心知识点。

---

## 题目一：HBase 的整体架构是怎样的？请说明 HMaster、RegionServer、Region、ZooKeeper 的作用及协作关系。

### 参考答案

HBase 是一个面向列族的分布式 NoSQL 数据库，构建于 HDFS 之上，采用类 BigTable 的架构设计。其核心组件及协作关系如下：

#### 1. HMaster（主节点）
- **职责**：负责监控和管理整个集群中的所有 RegionServer，具体包括：
  - 为 RegionServer 分配 regions（region 负载均衡）
  - 检测 RegionServer 的存活状态
  - 处理表的创建、删除、修改（DDL 操作）等元数据管理
  - 管理 namespace 和 schema 定义
- **高可用**：HBase 支持部署多个 HMaster，通过 ZooKeeper 进行 leader 选举，主 Master 宕机后备用 Master 会自动接管。

#### 2. RegionServer（数据节点）
- **职责**：负责直接处理客户端的读写请求，是 HBase 中实际存储数据的服务进程：
  - 一个 RegionServer 管理多个 Region
  - 每个 Region 对应一张表的一个数据分片
  - 负责 MemStore 写缓存、HFile 文件管理、 Compaction 合并操作
  - 与 HDFS DataNode 部署在一起，实现数据本地性（data locality）

#### 3. Region（数据分片）
- **职责**：是 HBase 表的水平分片单位：
  - 表创建后只有一个 Region，随着数据增长自动 split（拆分成两个）
  - 每个 Region 包含该表一部分行键范围的数据
  - 一个 Region 包含多个 Column Family，每个 Column Family 对应独立的 MemStore 和 HFile 集合

#### 4. ZooKeeper（协调服务）
- **职责**：作为 HBase 集群的协调中心：
  - 维护集群元数据（Master 地址、RegionServer 列表及状态）
  - 负责 Master 的选举（确保只有一个 active Master）
  - 实时监控 RegionServer 的心跳，检测节点故障并触发恢复
  - 客户端通过 ZooKeeper 获取 meta 表位置，再访问具体数据

#### 整体协作流程

```
客户端 ──→ ZooKeeper ──→ 获取 META 表位置 / Master 地址
       └──→ 直接访问目标 RegionServer（数据读写）
                    └──→ MemStore（写缓存）→ HFile（持久化存储）
```

> **来源**：[Guru99 - Top 30 HBase Interview Questions](https://www.guru99.com/hbase-interview-questions.html)、[Edureka - Hadoop Interview Questions On HBase](https://www.edureka.co/blog/interview-questions/hbase-interview-questions/)

---

## 题目二：HBase 的 RowKey 应该如何设计？有哪些原则和最佳实践？

### 参考答案

RowKey 是 HBase 中数据的唯一主键，决定了数据的存储位置和访问效率。优秀的 RowKey 设计是 HBase 应用性能的关键。

#### RowKey 设计核心原则

**1. 散列性（Hash Distribution）原则**
- RowKey 应尽量打散，避免热点（hot-spotting）问题
- 避免使用单调递增/递减的序列（如时间戳作为前缀）作为 RowKey，否则所有写入会集中到单个 RegionServer
- 常用方法：对 RowKey 做 MD5 哈希、或在开头拼接哈希前缀

**2. 长度原则**
- RowKey 长度应尽量短（建议 ≤ 16 字节，最多 64 字节）
- RowKey 会被存储多次（索引、memstore、HFile），过长的 RowKey 会极大浪费存储空间和内存

**3. 唯一性原则**
- RowKey 必须唯一标识一行数据，类似关系数据库的主键

**4. 查询模式匹配原则**
- RowKey 设计要匹配业务的主要查询场景：
  - **点查**：RowKey = 明确标识（如用户ID、订单号）
  - **范围查**：RowKey 前缀 = 查询条件（如时间范围、设备ID+时间戳组合）
  - **多条件查询**：考虑使用二级索引（Coprocessor 实现）或在 RowKey 中组合多个查询字段

#### 常见 RowKey 设计模式

| 场景 | RowKey 设计方式 | 示例 |
|------|----------------|------|
| 用户行为记录 | `userId + reverse(timestamp)` | `1001_9999999999 - timestamp` |
| 订单查询 | `merchantId + orderId` | `M001_20260817001` |
| 时间序列数据 | `deviceId + timestamp`（加盐打散）| `sensor_A_20260817`（加随机前缀） |
| 消息队列 | `topic + timestamp` + 哈希前缀 | `msg_topic_abc_20260817` |

#### 加盐（Salting）技巧
如果写入确实存在热点，可以对 RowKey 头部追加随机字节，使写入分散到不同 Region，但会损失点查能力。

> **来源**：[Intellipaat - HBase Interview Questions](https://intellipaat.com/blog/interview-question/hbase-interview-questions/)、[Guru99 - Top 30 HBase Interview Questions](https://www.guru99.com/hbase-interview-questions.html)

---

## 题目三：HBase 的存储结构是什么？请详细说明 MemStore 和 HFile 的工作原理，以及它们之间的关系。

### 参考答案

HBase 采用 LSM-Tree（Log-Structured Merge-Tree）架构，MemStore 和 HFile 是其核心存储结构。

#### 1. MemStore（写缓存）

- **位置**：每个 Region 的每个 Column Family 对应一个 MemStore 实例
- **作用**：接收所有写请求（put/delete），先将数据写入 MemStore（内存），而不是直接落盘
- **为什么需要 MemStore**：
  - HDFS 只支持顺序写入，不支持随机更新，MemStore 作为写缓冲批量合并写入
  - 按 RowKey 排序后批量写入 HFile，减小 I/O 开销
- **触发刷写（Flush）的条件**：
  - MemStore 大小达到 `hbase.hregion.memstore.flush.size`（默认 128MB）
  - Region 内存使用率超过阈值
  - WAL 文件累积超过一定大小或时间
  - 手动执行 flush 命令
- **MSLAB（MemStore-Local Allocation Buffer）**：HBase 使用 MSLAB 减少内存碎片，避免频繁 GC

#### 2. HFile（HBase 的存储文件）

- **位置**：存储在 HDFS 上，每个 Column Family 的数据对应多个 HFile
- **结构**：HFile 是一种支持快速随机读取的索引文件格式，核心结构如下：

```
┌─────────────────────────────────┐
│  Block Index（块索引）           │  存储 Data Block 的起始 RowKey
├─────────────────────────────────┤
│  Data Blocks（数据块）           │  实际 KV 数据，HFile 可配置块大小
│  - Bloom Filter（布隆过滤器）  │  快速判断某 RowKey 是否存在于此文件
│  - KeyValue 格式               │  [RowKey Len][RowKey][Column Family]
│                                   [Qualifier][Timestamp][Value]
├─────────────────────────────────┤
│  File Info（元信息）            │  包含 Max SequenceId、bloom key 等
├─────────────────────────────────┤
│  Trailer（尾部信息）            │  指向各区域的偏移量
└─────────────────────────────────┘
```

- **特点**：
  - 数据按 RowKey 排序（与 MemStore 排序一致）
  - 支持 Bloom Filter 加速查找
  - 支持数据块级压缩

#### 3. MemStore → HFile 的生命周期

```
写请求 → WAL（Write Ahead Log） → MemStore（内存排序）
       ↓（满足 Flush 条件）
MemStore 刷写 → 生成新的 HFile（临时）
       ↓（minor compaction 合并小文件）
       ↓（major compaction 合并重写，去除 tombstone 标记）
更大的 HFile
```

#### 4. Compaction（压缩合并）机制

| 类型 | 说明 |
|------|------|
| Minor Compaction | 自动将多个小 HFile 合并成较大的 HFile，不删除过期/删除数据 |
| Major Compaction | 将一个 Region 内所有 HFile 合并为一个，**删除标记的单元格会被真正移除**，生成新的 HFile 并删除旧文件 |

#### 5. 数据读取流程（Read Path）

读取数据时，HBase 按以下顺序查找（先读新后读旧）：
1. **Block Cache**：最近读取的 HFile 数据块缓存
2. **MemStore**：还未刷写到磁盘的最新数据
3. **HFile**：从磁盘加载，通过 Bloom Filter 定位目标块再读取

> **来源**：[Edureka - Hadoop Interview Questions On HBase](https://www.edureka.co/blog/interview-questions/hbase-interview-questions/)、[Guru99 - HBase Interview Questions](https://www.guru99.com/hbase-interview-questions.html)

---

## 题目四：HBase 的协处理器（Coprocessor）是什么？它有哪几种类型？各自的使用场景是什么？

### 参考答案

HBase 协处理器是一种运行在 RegionServer 上的扩展机制，允许用户将自定义代码注入到 HBase 的数据处理流程中，无需修改 HBase 源码。

#### 协处理器的两种类型

**1. Observer Coprocessor（观察者型）**

- **原理**：类似数据库触发器，在特定事件发生时自动执行用户代码
- **使用场景**：
  - 在数据写入前进行权限验证（Security）
  - 在 GET/PUT 前动态添加列或修改值（Column Value Manipulation）
  - 在数据删除时记录审计日志
  - 实现二级索引（数据变更时同步更新索引表）
  - 收集访问统计信息

- **三种 Observer 接口**：
  | 接口 | 触发时机 |
  |------|---------|
  | `RegionObserver` | GET/PUT/DELETE/SCAN 等数据操作前后 |
  | `MasterObserver` | DDL 操作（创建表、删除列族等）前后 |
  | `WALObserver` | WAL 写入前后 |

**2. Endpoint Coprocessor（端点型）**

- **原理**：类似存储过程，允许在 RegionServer 上执行自定义计算逻辑
- **使用场景**：
  - 在服务端执行聚合计算（COUNT、SUM、AVG 等），减少网络传输
  - 批量数据转换或计算
  - 服务端自定义过滤逻辑
- **工作方式**：客户端调用 `CoprocessorRpcChannel` 与 RegionServer 通信，执行完返回结果

#### 协处理器的加载方式

| 方式 | 配置位置 | 适用场景 |
|------|---------|---------|
| 静态加载（Table Coprocessor）| hbase-site.xml 或表描述 | 加载到所有表或所有 Region |
| 动态加载（Shell/API） | 通过 HBase Shell 或 Java API | 测试、临时场景 |
| 系统级 Coprocessor | hbase-site.xml | ZooKeeper、Master 等系统组件 |

#### 注意事项
- 协处理器代码 bug 可能导致 RegionServer 崩溃
- 高并发场景下注意性能影响
- 生产环境建议做好隔离和监控

> **来源**：[Guru99 - Top 30 HBase Interview Questions](https://www.guru99.com/hbase-interview-questions.html)、[Intellipaat - HBase Interview Questions](https://intellipaat.com/blog/interview-question/hbase-interview-questions/)

---

## 题目五：HBase 的删除机制是怎样的？为什么删除的数据还能被读到？Tombstone 标记是什么？

### 参考答案

HBase 的删除机制比较特殊，理解其原理对排查数据一致性问题至关重要。

#### HBase 的"假删除"机制

在 HBase 中执行 `delete` 命令时，数据**并不会立即从磁盘删除**，而是经历了以下过程：

```
DELETE 命令 → 写入 Tombstone 标记 → 数据在 Compaction 时被真正删除
```

#### Tombstone（墓碑标记）的三种类型

| 类型 | 作用范围 | 说明 |
|------|---------|------|
| **Version Delete Marker** | 单个版本 | 仅标记某一特定版本（timestamp）的单元格 |
| **Column Delete Marker** | 所有版本 | 标记某个 Column Qualifier 的所有版本 |
| **Family Delete Marker** | 列族级别 | 标记某个 Column Family 下所有列的所有版本 |

#### 删除流程详解

**1. 删除操作写入 WAL**
- 删除请求先写入 WAL（Write Ahead Log），确保数据可恢复

**2. 写入 Tombstone 标记**
- 删除操作会在 MemStore 中写入一个特殊的 KeyValue（类型为 Delete/DeleteColumn/DeleteFamily），即 Tombstone
- 该 Tombstone 包含被删除数据的 RowKey、Column Family、Qualifier 和时间戳

**3. 查询时如何过滤被删除数据**
- 当执行 GET/SCAN 时，HBase 会检查 Cell 的时间戳和删除标记：
  - 如果发现 tombstone 标记的单元格比数据版本更新，则该数据对客户端不可见
  - 读取时会跳过被标记删除的数据，仿佛数据已被删除
- 因此**删除后立即查询仍可能看到旧数据**（如果旧版本的时间戳更新）

**4. 真正删除——Major Compaction**
- Minor Compaction：只合并 HFile，不处理 Tombstone
- Major Compaction：合并所有 HFile 为一个，此时：
  - 带有 Delete 标记的 KeyValue 会覆盖所有匹配的历史数据
  - 被删除的单元格被真正从 HFile 中移除
  - 释放磁盘空间
- 这是 HBase 实现"假删除"的核心原因——避免每次删除都立即触发大量磁盘 I/O

#### 典型问题场景

**问题**：某行数据被删除后，由于写入新数据的时间戳早于 tombstone 的时间戳，新数据被遮蔽（masked）。

**原因**：如果写入的数据使用旧时间戳（`put 't', 'r1', 'cf:c', 'v1', ts=100`，而 delete 发生在 ts=200），major compaction 之前新写入的数据会被 tombstone 遮蔽。

**解决方案**：major compaction 执行后，问题自动解除；或者写入时使用 `setTimestamp` 确保时间戳足够新。

> **来源**：[Guru99 - Top 30 HBase Interview Questions](https://www.guru99.com/hbase-interview-questions.html)、[Edureka - Hadoop Interview Questions On HBase](https://www.edureka.co/blog/interview-questions/hbase-interview-questions/)

---

## 📚 参考来源

1. [Guru99 - Top 30 HBase Interview Questions (2026)](https://www.guru99.com/hbase-interview-questions.html)
2. [Edureka - Hadoop Interview Questions On HBase](https://www.edureka.co/blog/interview-questions/hbase-interview-questions/)
3. [Intellipaat - HBase Interview Questions and Answers](https://intellipaat.com/blog/interview-question/hbase-interview-questions/)
4. [Apache HBase Official Documentation](https://hbase.apache.org/book.html)

---

*本文件由自动化脚本生成，每日定时更新*
