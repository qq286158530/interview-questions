# Redis 面试题精选（2026-08-16）

> 每道题均附有详细答案与知识点解析，涵盖 Redis 核心知识体系。

---

## 题目一：Redis 有哪几种数据类型？各自的使用场景是什么？

### 答案

Redis 共支持 **8 种数据类型**（Redis 3.2 之后增加了 Geo 类型，5.0 之后增加了 Stream 类型）：

| 数据类型 | 底层实现 | 典型使用场景 |
|----------|----------|--------------|
| **String** | SDS（简单动态字符串） | 缓存、计数器、分布式锁、限流、Session 存储 |
| **Hash** | 压缩列表 / 哈希表 | 存储对象（如用户信息、商品详情），比 String 节省空间 |
| **List** | 压缩列表 / 双向链表 | 消息队列（lpush + brpop）、最新消息列表、关注列表 |
| **Set** | 整数集合 / 哈希表 | 去重（UV 统计）、标签系统、共同好友、抽奖 |
| **Sorted Set（ZSet）** | 压缩列表 / 跳表 | 排行榜、延时队列、有序通知、权重队列 |
| **Bitmap** | 位图（String 类型扩展） | 签到统计、在线用户、布隆过滤器实现 |
| **HyperLogLog** | 概率算法（String 类型扩展） | UV 统计（海量数据去重），内存仅 12KB |
| **Stream** | Radix Tree + 消息链 | 消息队列（功能最全，支持消费组、ACK） |

**高频追问：**
- ZSet 为什么用跳表而不是红黑树？—— 跳表范围查询更简单，区间遍历效率高；红黑树插入/删除需要旋转维护平衡。
- SDS 与 C 字符串的区别？—— SDS 有长度记录（O(1) 获取）、空间预分配、惰性释放，比 C 字符串更安全高效。

**来源：** [Redis 设计与实现](http://redisbook.com/) / [Redis 面试题全集（博客园）](https://www.cnblogs.com/javazhouqi/p/13412190.html)

---

## 题目二：Redis 的持久化机制有哪些？各有何优缺点？如何选择？

### 答案

Redis 有两种持久化机制：

#### 1. RDB（Redis Database）—— 快照方式

- **原理**：在指定时间间隔内，将内存中的**全量数据**生成快照，保存为 `dump.rdb` 文件。
- **配置示例**：
  ```
  save 900 1   # 900秒内 ≥1次写操作则触发
  save 300 10  # 300秒内 ≥10次写操作则触发
  save 60 10000
  ```
- **优点**：恢复速度快（fork 子进程完成，对主进程影响小），适合备份/灾难恢复。
- **缺点**：可能丢失最后一次快照后的数据；fork 进程有性能开销；数据量大时 fork 时间长。

#### 2. AOF（Append Only File）—— 日志方式

- **原理**：每个写命令追加到 `appendonly.aof` 文件，以文本协议记录。
- **配置示例**：
  ```
  appendonly yes
  appendfsync everysec  # 每秒同步（默认推荐）
  # appendfsync always  # 每次写都同步，最安全但最慢
  # appendfsync no      # 由 OS 决定同步，性能最好但可能丢数据
  ```
- **优点**：数据安全性更高（everysec 最多丢1秒数据），AOF 日志可修复。
- **缺点**：文件体积通常比 RDB 大，恢复速度慢。

#### 3. 混合持久化（Redis 4.0+）

- 开启 `aof-use-rdb-preamble yes`，AOF 重写时先写 RDB 快照，再追加增量 AOF 日志。
- 兼具快速恢复与较少数据丢失。

#### 如何选择？

- **纯缓存场景**：可关闭持久化。
- **能容忍分钟级丢失**：RDB。
- **数据安全性要求高**：AOF everysec 或混合持久化。
- **最优方案**：RDB + AOF 混合使用。

**来源：** [Redis 官方文档 - Persistence](https://redis.io/docs/management/persistence/) / [Redis 持久化详解（CSDN）](https://blog.csdn.net/qq_34827674/article/details/121137882)

---

## 题目三：Redis 的过期键删除策略是什么？

### 答案

Redis 有 **三种过期键删除策略**：

#### 1. 定时删除（主动删除）
- 为每个设置了过期时间的 key 创建一个定时器，到期立即删除。
- **优点**：对内存最友好（过期键及时释放）。
- **缺点**：CPU 开销大（大量定时器会影响性能），不适合生产环境。

#### 2. 惰性删除（被动删除）
- 每次访问 key 时检查是否过期，过期则删除。
- **优点**：对 CPU 友好，只在必要时检查。
- **缺点**：过期键长期占用内存（即使过期但从未被访问则永不过期）。
- **Redis 的默认策略**，即"用的时候再删"。

#### 3. 定期删除（折中策略）
- 每隔一段时间（默认 100ms），随机检查一批设置了过期时间的 key，删除其中过期的。
- 配置：`hz` 参数控制每秒检查次数（默认 10，即每秒 10 次抽样检查）。
- **优点**：折中 CPU 与内存压力。
- **缺点**：仍可能有漏网之鱼（过期键在检查窗口外未被扫到）。

#### 内存淘汰策略（6种，当内存不够时触发）

当 Redis 内存达到 `maxmemory` 上限，触发 **内存淘汰策略**：

| 策略 | 说明 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 在设置了过期时间的 key 中淘汰最少使用的 |
| `allkeys-lru` | 在所有 key 中淘汰最少使用的（**最常用**） |
| `volatile-lfu` | 在设置了过期时间的 key 中淘汰使用频率最低的 |
| `allkeys-lfu` | 在所有 key 中淘汰使用频率最低的 |
| `volatile-random` | 在设置了过期时间的 key 中随机淘汰 |
| `allkeys-random` | 在所有 key 中随机淘汰 |
| `volatile-ttl` | 淘汰 TTL 最短的（即最快过期的） |

**生产建议**：使用 `allkeys-lru` 或 `volatile-lru` + 合理设置 `maxmemory`。

**来源：** [Redis 过期键详解（掘金）](https://juejin.cn/post/6844903989945565198) / [Redis 内存淘汰策略（思否）](https://segmentfault.com/a/1190000021841215)

---

## 题目四：Redis 主从复制原理是什么？哨兵模式解决了什么问题？

### 答案

#### 主从复制原理（Master-Slave Replication）

**全量同步（SYNC）**：
1. 从服务器向主服务器发送 `SYNC` 命令。
2. 主服务器执行 `BGSAVE`，在后台生成 RDB 文件，同时将此后收到的写命令写入缓冲区。
3. 主服务器将 RDB 文件发送给从服务器，从服务器加载。
4. 主服务器再将缓冲区中的写命令发送给从服务器（命令传播阶段）。

**增量同步（PSYNC，Redis 2.8+）**：
- 引入 **复制偏移量**、**主服务器 ID**、**复制积压缓冲区**。
- 从库断线重连后，主库根据 `run_id` 判断是否可以用增量数据补发，避免全量同步。

**主从复制工作流程**：
```
从库发送 PSYNC → 主库返回 +CONTINUE → 从库加载 RDB → 主库发送积压缓冲区命令
```

#### 哨兵模式（Sentinel）

**解决的问题**：
- 主从模式下，如果主库宕机，需要手动切换（修改配置、重新启动从库），**无法自动故障转移**。
- 哨兵是一个独立进程，负责：**监控（Monitoring）**、**自动故障转移（Automatic Failover）**、**通知（Notification）**、**配置提供者（Configuration Provider）**。

**哨兵核心机制**：
- **主观下线（SDown）**：单个哨兵认为主库不可达。
- **客观下线（ODown）**：多个哨兵（quorum 配置）都认为主库不可达，触发故障转移。
- **选举领头哨兵**：raft 协议选出负责故障转移的哨兵。
- **新主选举**：优先级（`replica-priority`）→ 复制偏移量 → run_id 最小。

**配置示例**（`sentinel.conf`）：
```conf
sentinel monitor mymaster 127.0.0.1 6379 2  # 2个哨兵认为ODown就故障转移
sentinel down-after-milliseconds mymaster 5000  # 5秒不可达则判定SDown
```

**集群模式 vs 哨兵模式**：
- 哨兵模式：主从复制，读写分离，**不支持自动分片**（数据在多个主库间不分区）。
- 集群模式：支持**数据分片**（16384 槽位）、**自动故障转移**，但不支持跨槽位事务。

**来源：** [Redis 主从复制原理（腾讯云）](https://cloud.tencent.com/developer/article/1595985) / [Redis Sentinel 官方文档](https://redis.io/docs/management/sentinel/)

---

## 题目五：什么是缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 答案

这是高并发系统中 Redis 缓存的 **三大经典问题**：

#### 1. 缓存穿透

**问题**：查询一个**不存在的数据**（数据既不在缓存也不在 DB），每次都打到 DB，导致 DB 压力过大。
**常见场景**：恶意请求、爬虫、故意用不存在的大量 ID 查询。

**解决方案**：
- **布隆过滤器（BloomFilter）**：将所有存在的 key 存入布隆过滤器，查询前先判断是否存在，不存在直接返回。
- **缓存空值**：将查询结果为空的 key 也缓存起来（设置较短的 TTL，如 30 秒）。
- **参数校验**：对请求参数做合法性校验，过滤明显非法的 ID。

#### 2. 缓存击穿

**问题**：某个**热点 key 过期**，瞬间大量请求同时打到 DB。
**特点**：跟穿透不同，key 实际存在，只是过期了。

**解决方案**：
- **互斥锁（分布式锁）**：对热点 key 的访问加锁，只允许一个线程从 DB 加载数据并写回缓存，其他线程等待。
  ```
  SETNX key lock → 成功则从 DB 加载并 SET 到缓存 → 解锁
  ```
- **热点数据永不过期**：对热点 key 不设 TTL，通过后台异步更新。
- **逻辑过期**：不真正设置过期时间，只在 Value 中记录"逻辑过期时间"，访问时判断并异步更新。

#### 3. 缓存雪崩

**问题**：大量缓存 key 在**同一时间过期**，导致大量请求同时打到 DB。
**场景**：双十一零点大量缓存同时过期。

**解决方案**：
- **过期时间随机化**：给缓存 TTL 加一个随机值（`TTL + random(0, 3600)`），避免同时过期。
- **多级缓存**：本地缓存 + Redis 缓存 + DB，即使 Redis 过期，本地缓存还能挡一下。
- **Redis 高可用**：主从 + 哨兵 / 集群，保证缓存服务的高可用。
- **服务熔级 / 限流**：在 DB 被打垮前，限流或熔级保护。
- **预热**：在大促前，提前将热点数据加载到缓存。

#### 总结对比

| 问题 | 原因 | 解决思路 |
|------|------|----------|
| 缓存穿透 | 查询不存在的数据 | 布隆过滤器 + 缓存空值 + 参数校验 |
| 缓存击穿 | 热点 key 过期 | 互斥锁 / 永不过期 / 逻辑过期 |
| 缓存雪崩 | 大量 key 同时过期 | 过期时间随机化 + 多级缓存 + 限流 |

**来源：** [Redis 缓存三大问题（掘金）](https://juejin.cn/post/6895925007823100942) / [缓存穿透、击穿、雪崩（知乎）](https://zhuanlan.zhihu.com/p/75597070)

---

> 📌 本题库由 AI 自动整理，每日更新。更多题目请访问：[GitHub - interview-questions](https://github.com/qq286158530/interview-questions)
