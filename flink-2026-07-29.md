# Flink面试题（2026-07-29）

> 本文档整理了 5 道高质量的 Flink 大数据实时计算面试题，涵盖 Flink 架构、Watermark、Checkpoint、窗口函数、状态管理等核心知识点。题目来源见各题附注。

---

## 题目一：Flink 的架构是怎样的？为什么说它是真正的流计算框架？

### 参考答案

**Flink 架构核心组件**

Flink 采用 **Master-Slave 架构**，主要包含以下组件：

```
┌─────────────────────────────────────────────────────────┐
│                     Flink Cluster                         │
│  ┌─────────────┐                    ┌─────────────────┐  │
│  │   JobManager │◄────---RPC------►│  TaskManager(s)  │  │
│  │  (Master)    │                    │   (Worker)       │  │
│  │  - Dispatcher │                    │  - Slot(s)        │  │
│  │  - ResourceMgr│                   │  - Task(s)         │  │
│  │  - JobMaster │                    │  - Network Mgr    │  │
│  └─────────────┘                    └─────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

- **JobManager**：负责调度 Task、协调 Checkpoint、处理故障恢复
- **TaskManager**：执行具体的 Task，每个 TaskManager 有多个 Slot
- **Slot**：资源分配单元，限定了 Task 的并发执行能力

**Flink vs 其他框架（批流统一）**

| 特性 | Flink | Spark Streaming | Storm |
|------|-------|-----------------|-------|
| 计算模型 | 真正的流计算 | 微批（Mini-Batch） | 逐条处理 |
| 延迟 | 低（毫秒级） | 高（秒级） | 低（毫秒级） |
| 背压机制 | 自然背压 | Wny WAL | 无 |
| 批流统一 | Flink DataStream + Flink Table 统一 | Spark SQL 批流统一 | 不支持 |
| 状态管理 | 原生状态后端 | checkpoint | 不支持 |

**Flink 是"true streaming"的原因：**

1. **事件时间驱动**：基于真实事件时间处理，支持 Watermark 处理乱序
2. **无界流处理**：原生支持无限数据流，无需人为划分批次
3. **低延迟**：一条一条处理记录，而非等待微批窗口
4. **状态快照（Checkpoint）**：精确一次（Exactly-Once）语义

> **来源：** [Apache Flink 官方文档](https://flink.apache.org/)  
> **来源：** [InfoQ - Flink入门及实战（2024修订版）](https://xie.infoq.cn/article/f7b39eb313b054732ccd23b0)

---

## 题目二：Flink 的 Watermark 机制是什么？如何处理乱序和迟到数据？

### 参考答案

**什么是 Watermark（水位线）**

Watermark 是 Flink 用于处理**事件时间（Event Time）**乱序问题的机制，本质是一个**时间戳**，表示"所有时间戳小于该水位线的数据都已经到达"。

```
Event Time Timeline:
t=1 ──► t=2 ──► t=5 ──► t=3 ──► t=4 ──► t=6 ──► ...
         ↑
    Watermark(t=4) 表示：时间戳 <= 4 的数据都到齐了
    t=3 和 t=4 在 Watermark 之后到达，属于乱序/迟到数据
```

**两种 Watermark 策略**

```java
// 1. 固定延迟 Watermark（AssumedParty）
WatermarkStrategy.forMonotonousTimestamps();  // 单调递增

// 2. 乱序 Watermark（允许一定延迟）
WatermarkStrategy
    .<Tuple2<String, Long>>forBoundedOutOfOrderness(Duration.ofSeconds(10))
    .withTimestampAssigner((event, timestamp) -> event.f1);
```

**核心概念：窗口 + Watermark 触发**

| 机制 | 作用 |
|------|------|
| **Event Time** | 事件实际发生的时间 |
| **Watermark** | 告诉 Flink"这个时间点之前的数据都到齐了" |
| **窗口** | 等待 Watermark 触发后才计算 |
| **allowedLateness** | 窗口触发后，额外等待一段时间接收迟到数据 |
| **sideOutputTag** | 将迟到太久（超过 allowedLateness）的数据输出到侧输出流 |

**处理迟到数据的三层保障**

```java
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofSeconds(10))  // Watermark 延迟
    .withTimestampAssigner(...)
    
// 在窗口层面：
.window(SlidingEventTimeWindows.of(Time.minutes(5)))
.allowedLateness(Time.minutes(2))  // 窗口触发后，额外等2分钟
.sideOutputLateData(lateOutputTag)  // 超过2分钟的数据进入侧输出
```

**常见面试追问**

- **Watermark 传播**：下游算子会接收所有上游分区中最小的 Watermark
- **Watermark 跳跃**：当 Watermark 越过窗口结束时间时，触发窗口计算
- **空窗口**：如果 Watermark 从未达到窗口结束时间，窗口永远不触发

> **来源：** [Apache Flink 官方博客 - Watermark](https://flink.apache.org/features/2017/07/24/flink-watermarks.html)  
> **来源：** [掘金 - Flink Watermark 详解](https://juejin.cn/post/6844904173359800334)

---

## 题目三：Flink 的 Checkpoint 和 Savepoint 有什么区别？如何实现 Exactly-Once 语义？

### 参考答案

**Checkpoint vs Savepoint 对比**

| 特性 | Checkpoint | Savepoint |
|------|------------|-----------|
| 触发方式 | Flink 自动周期性触发 | 手动触发（用户命令） |
| 用途 | 故障恢复 | 计划性停止/升级 |
| 存储位置 | State Backend（HDFS/RocksDB） | 指定目录 |
| 是否阻塞 | 非阻塞（异步） | 暂停作业后触发 |
| 删除策略 | Job 删除时自动清理 | 手动删除 |
| 兼容性 | 可能不兼容（JobGraph 变化） | 兼容（通过 JobID 追踪） |

**Checkpoint 原理（Chandy-Lamport 分布式快照）**

```
Barrier（屏障）传播机制：

Source ──► Operator1 ──► Operator2 ──► Sink
  │           │            │
  ▼           ▼            ▼
Barrier    Barrier      Barrier
(对齐)      (等待对齐)    (等待对齐)
```

1. **JobManager** 定期向 Source 发送 Checkpoint 触发消息
2. **Source** 发送 **Checkpoint Barrier**，将状态写入 State Backend
3. **Barrier** 向下游传播，算子等待所有输入分区的 Barrier **对齐**
4. 对齐完成后，算子做**本地快照**，继续处理缓冲数据
5. 全部完成上报给 **JobManager**，Checkpoin t完成

**Exactly-Once 语义实现**

Flink 通过以下三层保证 Exactly-Once：

1. **输入源**：Kafka Consumer 支持 Exactly-Once 语义（事务提交偏移量）
2. **Flink 内部**：Checkpoint 快照 + 两阶段提交（TwoPhaseCommitSink）
3. **输出端**：两阶段提交协议（预提交 → 确认/回滚）

```java
// 两阶段提交 Sink 示例（Kafka Sink）
KafkaSink.<String>builder()
    .setBootstrapServers("localhost:9092")
    .setRecordSerializer(new SimpleStringSerializer())
    .setDeliveryGuarantee(DeliveryGuarantee.EXACTLY_ONCE)
    .setTransactionalIdPrefix("flink-")
    .build();
```

**状态后端选择**

| 后端 | 适用场景 | 优点 |
|------|----------|------|
| **HashMapStateBackend** | 小状态、低延迟 | 内存速度，无需序列化 |
| **EmbeddedRocksDBStateBackend** | 大状态、超大窗口 | 支持增量 Checkpoint，内存+本地磁盘 |
| **远程 RocksDB** | 超大规模集群 | 状态卸载到 HDFS/S3 |

> **来源：** [Apache Flink Checkpoint 官方文档](https://nightlies.apache.org/flink/flink-docs-release-1.17/docs/ops/state/checkpoints/)  
> **来源：** [Flink中文社区 - Checkpoint详解](https://flinkchina.net)

---

## 题目四：Flink 窗口函数有哪些？滚动窗口、滑动窗口、会话窗口的区别是什么？

### 参考答案

**窗口类型总览**

```
无界数据流
    │
    ▼
┌───────────────────────────────────────────┐
│               Window Assigner              │
├─────────────┬─────────────┬──────────────┤
│ 滚动窗口     │ 滑动窗口      │ 会话窗口       │
│ (Tumbling)  │ (Sliding)   │ (Session)     │
└─────────────┴─────────────┴──────────────┘
```

**1. 滚动窗口（Tumbling Windows）**

每个元素只属于一个窗口，窗口不重叠：

```java
// 每5分钟一个窗口，互不重叠
DataStream<Tuple2<String, Long>> counts = stream
    .keyBy(0)
    .window(TumblingEventTimeWindows.of(Time.minutes(5)))
    .sum(1);

// TumblingProcessingTimeWindows：基于处理时间
// TumblingEventTimeWindows：基于事件时间
```

```
时间线: |---0-5---|---5-10---|---10-15---|---15-20---|
数据:      a b c       d e          f g
窗口1              窗口2           窗口3
```

**2. 滑动窗口（Sliding Windows）**

窗口重叠，支持重叠区域计算：

```java
// 窗口大小10分钟，滑动步长5分钟
.window(SlidingEventTimeWindows.of(Time.minutes(10), Time.minutes(5)))
```

```
时间线: |--0-10--|---5-15---|--10-20--|---15-25---|
窗口1:   ■■■■■■■■■■
窗口2:              ■■■■■■■■■■
窗口3:                         ■■■■■■■■■■
```

**3. 会话窗口（Session Windows）**

根据不活动间隙动态创建窗口：

```java
// 会话超时 gap=5分钟，5分钟内无数据则关闭窗口
.window(SessionWindows.withGap(Time.minutes(5)))
```

```
数据: a b c    d e    f g h      i
          ↑ gap=5min     ↑ gap=5min
窗口1: ■■■■■
窗口2:            ■■■■■
窗口3:                   ■■■■■■■
```

**窗口函数类型**

| 函数类型 | 说明 | 示例 |
|----------|------|------|
| **reduce()** | 增量聚合，元素来一条处理一条 | `sum()`、`min()`、`max()` |
| **aggregate()** | 更通用的聚合，可输出不同类型 | 带中间状态的复杂聚合 |
| **apply()** | 全量窗口函数，先收集再计算 | 窗口内排序、取 TopN |
| **process()** | 最强大，支持 Context，可输出侧输出 | 自定义逻辑、复杂事件处理 |

```java
// 推荐：增量聚合 process 方式（性能更好）
DataStream<SensorReading> input = ...

input.keyBy(x -> x.id)
     .window(SlidingEventTimeWindows.of(Time.seconds(10), Time.seconds(5)))
     .process(new DeduplicateProcessWindowFunction())
```

**GlobalWindow**：全局窗口，所有相同 key 的数据进入同一窗口（需自定义触发器，很少用）

> **来源：** [Apache Flink 窗口官方文档](https://nightlies.apache.org/flink/flink-docs-release-1.17/docs/dev/datastream/operators/windows/)  
> **来源：** [CSDN - Flink窗口函数详解（2024版）](https://blog.csdn.net/qq286158530/article/details/126120301)

---

## 题目五：Flink 的状态后端是什么？如何选择合适的状态后端？如何处理大状态？

### 参考答案

**Flink 状态后端分类**

```
State Backend
    │
    ├── HashMapStateBackend（HashMap）
    │       └── 状态存储在 JVM 堆内存
    │
    ├── EmbeddedRocksDBStateBackend（RocksDB）
    │       └── 状态存储在本地 RocksDB + 可选远程存储
    │
    └── （旧版）FsStateBackend / MemoryStateBackend
            └── 已废弃，1.10后合并到上述两类
```

**各状态后端对比**

| 特性 | HashMapStateBackend | EmbeddedRocksDBStateBackend |
|------|---------------------|------------------------------|
| 存储介质 | JVM 堆内存 | 本地 RocksDB（磁盘） |
| 读写性能 | 极快（内存速度） | 较慢（需序列化/反序列化） |
| 状态大小 | 受限于 JVM 堆大小 | 可存储 TB 级状态 |
| Checkpoint | 全量快照 | 增量 Checkpoint |
| 维护成本 | 低 | 高（RocksDB 调优） |
| 推荐场景 | 小状态、低延迟 | 大状态、长窗口、超大规模 |

**配置示例**

```java
// 代码中配置
StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();

// HashMapStateBackend（默认）
env.setStateBackend(new HashMapStateBackend());

// EmbeddedRocksDBStateBackend（生产环境大状态推荐）
env.setStateBackend(new EmbeddedRocksDBStateBackend());

// 开启增量 Checkpoint（RocksDB）
EmbeddedRocksDBStateBackend rocksDB = new EmbeddedRocksDBStateBackend(true); // true = 增量
env.setStateBackend(rocksDB);
```

**生产环境大状态优化策略**

```java
// 1. 开启增量 Checkpoint（避免每次全量快照）
EmbeddedRocksDBStateBackend rocksDB = new EmbeddedRocksDBStateBackend(true);
env.enableCheckpointing(60_000, CheckpointingMode.EXACTLY_ONCE);
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(Time.of(30, TimeUnit.SECONDS));

// 2. 配置 RocksDB 内存
Configuration config = new Configuration();
config.setString("state.backend.rocksdb.memory.managed", "true");  // 内存托管
config.setString("state.backend.rocksdb.memory.write-buffer-size", "128mb");
config.setString("state.backend.rocksdb.memory.high-water-mark", "64mb");

// 3. 开启本地恢复（failover时从本地恢复，不读远程）
env.getCheckpointConfig().enableUnalignedCheckpoints();

// 4. 状态过期清理（TTL）
StateTtlConfig ttlConfig = StateTtlConfig.newBuilder(Time.days(7))
    .setUpdateType(StateTtlConfig.UpdateType.OnReadAndWrite)
    .cleanupInRocksdbCompactFilter(1000)
    .build();
```

**常见面试追问**

- **Flink 状态存储在哪里？** 算子本地（TaskManager 内存或 RocksDB），通过 Chandy-Lamport 快照同步到远程存储（State Backend）
- **状态可以共享吗？** 同一 KeyBy 内的算子可以通过 `broadcast state` 共享状态
- **状态大小怎么估算？** 状态大小 ≈ key 数量 × 每条状态大小，注意防止 OOM

> **来源：** [Apache Flink State Backend 官方文档](https://nightlies.apache.org/flink/flink-docs-release-1.17/docs/ops/state/state_backends/)  
> **来源：** [Apache Flink RocksDB 调优指南](https://flink.apache.org/features/2021/05/06/flink-rocksdb.html)  
> **来源：** [美团技术团队 - Flink 状态管理详解](https://tech.meituan.com/2020/03/01/flink-state-backends-and-checkpoints.html)
