# Redis 面试题（2026-07-29）

> 涵盖数据类型、持久化、缓存、集群等核心知识点

---

## 1. Redis 有哪些主要的数据类型？分别用在什么场景？

**参考答案：**

Redis 支持五种基础数据类型：

| 类型 | 底层实现 | 典型场景 |
|------|----------|----------|
| **String** | SDS（简单动态字符串） | 缓存、计数器、分布式锁、session |
| **Hash** | 压缩列表 或 哈希表 | 存储对象（用户信息、商品详情） |
| **List** | 压缩列表 或 快速列表 | 消息队列、排行榜、最新动态 |
| **Set** | 整数集合 或 哈希表 | 去重、标签系统、共同好友 |
| **Zset** | 压缩列表 或 跳表+哈希表 | 有序排行榜、带权重的任务队列 |

**进阶：**
- **BitMap**（String 类型位操作）：签到、活跃用户统计
- **HyperLogLog**：海量数据去重计数（误差约 0.81%）
- **GEO**：附近的人、LBS 服务
- **Stream**：消息队列（Redis 5.0+），支持消费者组

**来源：** [Redis 官方文档 - Data Types](https://redis.io/docs/data-types/)

---

## 2. Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？如何选择？

**参考答案：**

### RDB（Redis Database）

- **原理**：定时将内存数据快照写入二进制文件 `dump.rdb`
- **触发方式**：
  - `SAVE`（阻塞）或 `BGSAVE`（后台异步）
  - 自动按配置 `save <seconds> <changes>` 触发
  - `FLUSHALL`（清空）后自动触发
- **优点**：恢复速度快，适合大规模数据备份
- **缺点**：可能丢失最近一次快照后的数据

### AOF（Append Only File）

- **原理**：记录每个写操作命令，追加到 `appendonly.aof`
- **同步策略**：
  - `always`：每条命令都 fsync（最安全，性能差）
  - `everysec`（默认）：每秒同步，最多丢 1 秒数据
  - `no`：由操作系统决定何时同步
- **重写机制**：`BGREWRITEAOF` 压缩 AOF 文件，去除冗余命令
- **优点**：数据安全性更高，支持多种同步频率
- **缺点**：文件体积大，恢复速度慢

### 如何选择？

| 场景 | 推荐策略 |
|------|----------|
| 完全追求性能，允许少量数据丢失 | RDB |
| 对数据安全性要求高 | AOF（everysec 或 always） |
| 最佳方案 | **RDB + AOF 混合持久化**（Redis 7.0+ 默认开启） |

**来源：** [Redis 官方文档 - Persistence](https://redis.io/docs/management/persistence/)

---

## 3. Redis 的过期 key 删除策略有哪些？

**参考答案：**

### 三种删除策略

1. **定时删除**（主动）：
   - 为每个设置过期时间的 key 创建定时器，到期立即删除
   - **优点**：内存友好，key 一过期就能释放
   - **缺点**：CPU 消耗高，key 多时会影响性能

2. **惰性删除**（被动）：
   - 访问 key 时才检查是否过期，过期则删除
   - **优点**：对 CPU 友好，只有被访问时才检查
   - **缺点**：过期 key 长期占用内存（内存泄漏风险）

3. **定期删除**（折中）：
   - 周期性扫描 key space，删除过期的 key
   - `hz` 参数控制扫描频率（默认 10，即每秒 10 次）
   - **优点**：平衡 CPU 和内存
   - **缺点**：如果执行时间过长，仍会阻塞 Redis

### 生产环境建议

- **同时使用惰性删除 + 定期删除**（Redis 默认策略）
- 使用 `MEMORY USAGE` + `UNLINK`（异步删除大 key）
- 监控 `expired_keys` 统计指标，防止内存膨胀

**来源：** [Redis 官方文档 - expire](https://redis.io/commands/expire/)

---

## 4. Redis 主从复制原理是什么？哨兵（Sentinel）和集群（Cluster）有什么区别？

**参考答案：**

### 主从复制原理

```
Master → 全量同步（RDB）→ Slave
Master → 增量同步（命令流）→ Slave（同步偏移量机制）
```

- **全量同步**：Slave 首次连接或断线重连时，Master 生成 RDB 发送给 Slave
- **增量同步**：Master 将写命令追加到 `repl_backlog` 缓冲区，Slave 持续同步
- **Psync2**：支持断点续传，无需全量同步

### 哨兵（Sentinel）vs 集群（Cluster）

| 维度 | 哨兵模式 | 集群模式 |
|------|----------|----------|
| **定位** | 高可用（HA）+ 故障转移 | 数据分片（Sharding） |
| **数据存储** | 每节点完整数据 | 16384 个槽位，数据分布存储 |
| **节点关系** | Master-Slave 一主多从 | 多主多从，每个 Master 有多个 Slave |
| **故障转移** | Sentinel 自动选主 | Cluster 自动迁移槽位 |
| **客户端路由** | 需要 Sentinel 感知拓扑 | 客户端直连任意节点，MOVED 重定向 |
| **适用场景** | 数据量小（< 10GB），需要 HA | 数据量大，需要水平扩展 |
| **写能力** | 仅有 Master 可写 | 每个 Master 分片可写 |
| **跨机房** | 支持，但需要额外配置 | 原生支持跨槽位分布 |

**来源：** [Redis 官方文档 - Replication](https://redis.io/docs/management/replication/) | [Sentinel](https://redis.io/docs/management/sentinel/) | [Cluster](https://redis.io/docs/management/scaling/)

---

## 5. 什么是缓存穿透、缓存击穿、缓存雪崩？如何解决？

**参考答案：**

### 缓存穿透

- **定义**：查询一个不存在的数据，缓存没有，数据库也没有，每次请求都打到数据库
- **危害**：恶意请求或爬虫攻击导致数据库崩溃

**解决方案：**
1. **布隆过滤器（BloomFilter）**：将所有存在的 key 存入布隆过滤器，请求先过滤
2. **缓存空值**：将查询结果为空的 key 也缓存（设置短过期时间，如 60s）
3. **参数校验**：接口层做基础合法性校验

### 缓存击穿

- **定义**：某个热点 key 过期瞬间，大量并发请求涌入数据库
- **危害**：瞬间数据库压力剧增

**解决方案：**
1. **互斥锁（SETNX）**：只有一个请求去加载数据，其他等待
2. **逻辑过期**：不设置 TTL，而是业务层判断是否过期，过期则异步重建
3. **永不过期**：更新数据时异步更新缓存

### 缓存雪崩

- **定义**：大量 key 同时过期，或 Redis 宕机，导致大量请求打到数据库
- **危害**：数据库瞬间承受不住压力而崩溃

**解决方案：**
1. **过期时间随机化**：` TTL = baseTTL + random(0, 600)`
2. **多级缓存**：本地缓存 + Redis + 数据库
3. **Redis 高可用**：哨兵 / 集群部署，避免单机宕机
4. **熔断限流**：Hystrix / Sentinel 保护下游依赖
5. **预热**：系统启动时主动加载热点数据到缓存

### 总结对比

| 问题 | 原因 | 关键词 |
|------|------|--------|
| 穿透 | 查询不存在的数据 | 布隆过滤 + 空值缓存 |
| 击穿 | 热点 key 过期瞬间 | 互斥锁 + 逻辑过期 |
| 雪崩 | 大量 key 同时过期 / Redis 宕机 | 随机 TTL + 高可用 + 限流 |

**来源：** [Redis 实战 - 缓存问题详解](https://github.com/doocs/redis#%E7%BC%93%E5%AD%98%E5%A4%84%E7%90%86)
