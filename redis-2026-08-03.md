# Redis 面试题精讲

> 📅 日期：2026-08-03  
> 🔖 来源：图解Redis（小林coding）、Redis 官方文档

---

## 题目一：Redis 有哪些数据类型？底层分别是如何实现的？

### 参考答案

Redis 提供了丰富的数据类型，常见的有 **5 种基础数据类型**和**4 种扩展数据类型**：

### 5 种基础数据类型

| 类型 | 应用场景 |
|------|----------|
| **String** | 缓存对象、常规计数、分布式锁、共享 session 信息 |
| **Hash** | 缓存对象、购物车 |
| **List** | 消息队列、链表、评论列表 |
| **Set** | 聚合计算（并集/交集/差集），如点赞、共同关注、抽奖 |
| **ZSet** | 排序场景，如排行榜、电话姓名排序 |

### 4 种扩展数据类型

| 类型 | 版本 | 应用场景 |
|------|------|----------|
| **BitMap** | 2.2+ | 二值状态统计（签到、判断用户登录、连续签到总数） |
| **HyperLogLog** | 2.8+ | 海量数据基数统计（百万级网页 UV 计数） |
| **GEO** | 3.2+ | 存储地理位置（滴滴叫车） |
| **Stream** | 5.0+ | 消息队列（支持消费组、自动生成全局唯一 ID） |

### 底层数据结构实现

- **String → SDS（简单动态字符串）**：SDS 使用 `len` 属性记录长度，获取长度 O(1)；支持二进制数据；拼接字符串不会缓冲区溢出。
- **List → quicklist**（3.2+ 版本）：由双向链表 + 压缩列表实现，元素 < 512 个且每个 < 64 字节时用压缩列表，否则用双向链表。
- **Hash → 哈希表 / 压缩列表**：元素 < 512 个且值 < 64 字节用压缩列表，否则用哈希表。Redis 7.0 压缩列表已废弃，改用 listpack。
- **Set → 哈希表 / 整数集合**：全是整数且 < 512 个用整数集合，否则用哈希表。
- **ZSet → 跳表 / 压缩列表**：元素 < 128 个且值 < 64 字节用压缩列表，否则用跳表。Redis 7.0 压缩列表已废弃。

> 📎 来源：[图解Redis · 常见面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目二：Redis 为什么用单线程？单线程为什么这么快？

### 参考答案

#### Redis 确实是单线程的吗？

Redis 单线程指的是**「接收客户端请求 → 解析请求 → 执行命令 → 返回结果」**这一过程由单线程完成，但 Redis 实际上还有**后台线程（BIO）**：

- **2.6 版本**：2 个后台线程（关闭文件、AOF 刷盘）
- **4.0+ 版本**：新增 lazyfree 线程，异步释放内存

后台线程处理的耗时任务不会阻塞主线程，例如 `unlink key` 删除大 key 就是异步的。

#### 为什么单线程还这么快？

1. **瓶颈不在 CPU**：Redis 数据存在内存，读写操作极快，瓶颈是内存或网络带宽，而非 CPU，因此单线程反而避免了锁竞争和上下文切换的开销。
2. **高效数据结构**：SDS、跳表、哈希表等都是为 Redis 量身定制的高效数据结构。
3. **I/O 多路复用**：使用 `epoll` 机制，单线程可以同时处理多个 Socket 请求，一个线程管理多个 IO 流，极大提升了并发能力。
4. **无锁设计**：避免了死锁、线程切换、竞态条件等性能损耗。

#### Redis 6.0 引入多线程

Redis 6.0 开始用多线程处理**网络 I/O**（写客户端 socket），但**命令执行仍是单线程**，整体性能提升至少一倍。

> 📎 来源：[图解Redis · 常见面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目三：Redis 有哪几种持久化方式？各有何优缺点？

### 参考答案

Redis 有 **3 种**数据持久化方式：

### 1. AOF（Append Only File）

每执行一条写操作命令，就将该命令追加写入 AOF 文件，恢复时逐一执行命令来重建数据。

**写回策略（appendfsync）**：

| 策略 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **Always** | 同步写回，每个命令执行完即写盘 | 数据基本不丢失 | 占用大量 I/O，性能差 |
| **Everysec** | 每秒写回（默认） | 性能适中 | 最多丢 1 秒数据 |
| **No** | 操作系统控制写回时机 | 性能最好 | 数据丢失不可控 |

**AOF 重写机制**：当 AOF 文件过大时，后台子进程 `bgrewriteaof` 会读取数据库中所有键值对，用一条命令记录（去重压缩），避免文件膨胀。重写期间主进程的新命令同时写入「AOF 缓冲区和 AOF 重写缓冲区」，保证数据一致。

### 2. RDB（Redis Database）

将某一时刻的内存数据以**二进制格式**快照写入磁盘。

- **save**（主线程执行，会阻塞）
- **bgsave**（子进程执行，不阻塞）

可通过配置自动触发：
```
save 900 1   # 900秒内至少1次修改
save 300 10  # 300秒内至少10次修改  
save 60 10000 # 60秒内至少10000次修改
```

快照过程中使用 **Copy-On-Write（写时复制）**：fork 出子进程后，父子进程共享物理内存，主线程执行写操作时复制出副本写入 RDB，主线程本身继续处理请求。

### 3. 混合持久化（Redis 4.0+）

重写 AOF 时，先将以 RDB 格式写入 AOF 文件前半部分，再将增量命令以 AOF 格式写入后半部分。兼具 RDB 快速加载和 AOF 少丢数据的优点。

**缺点**：可读性差，Redis 4.0 之前版本不兼容。

### 对比总结

| 特性 | RDB | AOF | 混合持久化 |
|------|-----|-----|-----------|
| 文件体积 | 小（二进制） | 大（命令文本） | 中等 |
| 恢复速度 | 快 | 慢 | 快 |
| 数据安全性 | 可能有丢失 | 可配置（always~no） | 最多丢 1 秒 |
| 兼容性 | 通用 | 通用 | 仅 4.0+ |

> 📎 来源：[图解Redis · 常见面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目四：Redis 主从复制是如何工作的？哨兵模式解决了什么问题？

### 参考答案

### 主从复制

Redis 主从复制实现**一主多从**架构，采用**读写分离**模式：
- 主服务器处理写操作，同步给从服务器
- 从服务器只读，接受主服务器同步的命令

**同步过程**：
1. 从服务器启动后，向主服务器发送 `SYNC` 命令
2. 主服务器执行 `bgsave`，将快照数据发送给从服务器
3. 主服务器再将缓冲区中的写命令传播给从服务器

**注意**：命令复制是**异步**的——主服务器本地执行完命令即返回客户端，不会等到从服务器实际执行完再返回。因此主从数据**无法保证强一致性**，存在短暂不一致是正常现象。

### 哨兵模式（Sentinel）

主从复制模式下，如果主服务器宕机，需要手动恢复。**哨兵模式**解决了这一问题：

- **监控**：哨兵进程监控主从服务器的运行状态（心跳检测）
- **自动故障转移**：主服务器故障时，哨兵自动选举一个从服务器升级为新的主服务器
- **通知**：故障转移后，通过发布/订阅机制通知应用方新的主服务器地址

### 集群模式（切片集群）

当数据量非常大时，哨兵模式仍只有一台主节点承担写压力。Redis 提供了**切片集群**（Cluster 模式）：
- 将数据划分为 **16384 个槽**（slot），分布到多个主节点
- 每个主节点可配置多个从节点，实现**读写分离 + 高可用**
- 客户端请求自动路由到正确的节点

> 📎 来源：[图解Redis · 常见面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目五：Redis 缓存策略有哪些？如何解决缓存穿透、缓存雪崩、缓存击穿？

### 参考答案

### 缓存读写策略

#### 1. Cache-Aside（旁路缓存，最常用）

- **读**：先查缓存，缓存命中直接返回；未命中查数据库，加载到缓存再返回
- **写**：先更新数据库，再删除缓存（而非更新缓存，避免并发导致数据不一致）

#### 2. Read-Through / Write-Through

- Read-Through：缓存负责加载数据，应用程序只和缓存打交道
- Write-Through：写操作同时更新数据库和缓存

#### 3. Write-Behind（Write-Back）

更新数据库后异步更新缓存，适合高并发写入场景。

---

### 三大缓存问题

#### ❄️ 缓存穿透（数据既不在缓存也不在数据库）

**问题**：恶意请求或业务故障，查询一个根本不存在的数据，每次都打到数据库。

**解决方案**：
- **布隆过滤器**：将所有合法数据哈希到一个 bitmap，请求先过布隆过滤器
- **缓存空值**：查询结果为空时，在缓存中存入一个空值（设置短过期时间）
- **接口参数校验**：拦截非法参数请求

#### ❄️ 缓存雪崩（大量缓存同时过期）

**问题**：大量缓存 key 同一时间集中过期，导致大量请求直接打到数据库。

**解决方案**：
- **过期时间随机化**：给 key 的 TTL 加一个随机偏移量，避免集中过期
- **永不过期**：对热点数据使用主动刷新，而非依赖 TTL
- **Redis 高可用**：搭建主从 + 哨兵/集群，避免 Redis 本身故障
- **服务降级/限流**：数据库压力过大时，返回兜底数据

#### 🔥 缓存击穿（单个热点 key 过期）

**问题**：某个热点 key 过期瞬间，大量并发请求同时击穿缓存直接打数据库。

**解决方案**：
- **互斥锁（分布式锁）**：只允许一个请求重建缓存，其他请求等待
- **热点数据永不过期**：物理不过期 + 逻辑过期（后台异步刷新）
- **接口限流**：限制并发量，保护数据库

> 📎 来源：[图解Redis · 常见面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

*更多面试题持续更新中，欢迎 Star ⭐ [qq286158530/interview-questions](https://github.com/qq286158530/interview-questions)*
