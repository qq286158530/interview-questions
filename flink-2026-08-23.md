# Flink 面试题整理

> 📅 日期：2026-08-23
> 🐼 来源：面试题助手整理

---

## 题目一：Flink 的架构是怎样的？请详细说明各组件及其作用

### 参考答案

Apache Flink 是一个分布式流处理框架，其核心架构采用 **Master-Slave** 模式，主要包含以下组件：

### 1. **JobManager（Master）**
- **核心组件**，负责整个集群的调度和协调
- 包含三个核心组件：
  - **Dispatcher**：负责接收用户提交的 Job，为每个 Job 创建一个 JobMaster，并提供 REST 接口
  - **ResourceManager**：负责集群资源的管理，与 YARN/K8s 等资源管理框架交互
  - **JobMaster**：负责单个 Job 的执行协调，包括 Task 调度、Checkpoint 协调、故障恢复等

### 2. **TaskManager（Slave）**
- **执行节点**，负责执行具体的 Task
- 每个 TaskManager 是一个 JVM 进程，启动多个 **TaskSlot**
- TaskSlot 是资源分配的基本单位，每个 TaskSlot 有固定的资源（内存、CPU）
- 多个算子可以在一个 TaskSlot 中并发执行

### 3. **Slot 与资源分配**
- Flink 默认使用 **FIFO** 调度策略
- 集群启动时，TaskManager 向 ResourceManager 注册自己的 Slot 资源
- JobMaster 向 ResourceManager 申请 Slot，ResourceManager 再向 TaskManager 发起分配请求

### 4. **数据流执行**
```
DataSource → Transformation → DataSink
     ↓            ↓              ↓
  Operator     Operator       Operator
     ↓            ↓              ↓
  Task(并发)   Task(并发)    Task(并发)
```

### 5. **执行模式**
- **Streaming 模式**：默认，持续处理无限数据流
- **Batch 模式**：有限数据集，有界流处理

### 来源
- Apache Flink 官方文档：https://nightlies.apache.org/flink/flink-docs-stable/docs/concepts/flink-architecture/
- 《Flink原理与实战》

---

## 题目二：什么是 Watermark（水位线）？Flink 如何处理乱序数据？

### 参考答案

### 1. **Watermark 的概念**
Watermark 是 Flink 用于处理**乱序事件**和**延迟数据**的核心机制。它是一个时间戳标记，表示"所有时间戳小于该 Watermark 的数据都已经到达"。

### 2. **为什么需要 Watermark**
在真实场景中，数据会因网络延迟、系统故障等原因产生乱序。Watermark 机制让 Flink 能够在**事件时间（Event Time）**模式下合理地触发窗口计算。

### 3. **Watermark 的生成方式**

#### (1) Ascending Timestamp（水印单调递增）
```java
WatermarkStrategy
    .forMonotonousTimestamps()
```
适用于数据基本有序的场景。

#### (2) Bounded Out-of-Orderness（带延迟上限的水印）
```java
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofSeconds(10))
```
允许最多 `delay` 秒的乱序。

#### (3) 自定义 Watermark
```java
WatermarkStrategy
    .forGenerator((context) -> new PeriodicWatermarkGenerator<>())
    .withTimestampAssigner((event, timestamp) -> event.getTimestamp())
```

### 4. **Watermark 的传播**
Watermark 沿数据流向下游传递，下游算子会取所有上游输入的 **最小 Watermark**，确保不提前触发窗口。

### 5. **窗口触发机制**
- 当 `Watermark > 窗口结束时间` 时，窗口被触发计算
- `allowedLateness` 设置额外等待时间，允许迟到数据更新结果
- `sideOutputLateData` 将迟到严重的数据输出到侧输出流

### 6. **乱序处理流程图**
```
数据流 → Timestamp Assigner → Watermark Generator → Window Operator
                                          ↓
                              取所有上游最小 Watermark
                                          ↓
                              当 watermark >= 窗口 end_time 时触发
```

### 来源
- Flink官方文档：https://nightlies.apache.org/flink/flink-docs-stable/docs/concepts/time-processing/
- Flink CDC：https://ververica.cn/

---

## 题目三：Flink Checkpoint 和 Savepoint 的区别是什么？如何实现故障恢复？

### 参考答案

### 1. **Checkpoint（检查点）**
- **自动触发**，由 Flink 引擎定期执行
- 用于**故障恢复**，当作业失败时从最近的 Checkpoint 恢复
- 依赖 **Chandy-Lamport 算法**的变体实现分布式快照
- 默认存储在 HDFS/RocksDB 中
- 可启用/禁用，作业停止后默认清除

### 2. **Savepoint（保存点）**
- **手动触发**，由用户显式创建
- 用于**作业升级、集群迁移、A/B测试**等场景
- 作业拓扑不变的情况下，可以从 Savepoint 恢复
- 存储在用户指定目录，不会自动清除

### 3. **核心区别**

| 特性 | Checkpoint | Savepoint |
|------|-----------|-----------|
| 触发方式 | 自动 | 手动 |
| 用途 | 故障恢复 | 维护/升级/迁移 |
| 清理策略 | 作业停止后删除 | 需手动删除 |
| 状态存储 | 临时 | 永久 |
| 性能优化 | 更激进 | 更保守 |

### 4. **Checkpoint 配置**
```java
StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
// 每 10 秒执行一次 Checkpoint
env.enableCheckpointing(10000);
// Checkpoint 模式：exactly-once（精确一次）
env.getCheckpointConfig().setCheckpointingMode(CheckpointingMode.EXACTLY_ONCE);
// Checkpoint 超时时间
env.getCheckpointConfig().setCheckpointTimeout(60000);
// 两次 Checkpoint 最小间隔
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(5000);
// 最大并发 Checkpoint 数
env.getCheckpointConfig().setMaxConcurrentCheckpoints(1)
// 开启 Checkpoint 外部持久化
env.getCheckpointConfig().setExternalizedCheckpointCleanup(ExternalizedCheckpointCleanup.RETAIN_ON_CANCELLATION);
```

### 5. **故障恢复流程**
```
JobManager 定期触发 Checkpoint
        ↓
各 TaskManager 执行 State Backend 快照
        ↓
快照完成，Checkpoint 完成屏障（Barrier）对齐
        ↓
状态元数据写入持久化存储
        ↓
故障时 → JobManager 重启作业
        ↓
从最近 Checkpoint 恢复状态
        ↓
重新消费 Kafka 等数据源
```

### 6. **常用 State Backend**
- **HashMapStateBackend**：状态小、访问频繁的场景，元数据存在 JVM 堆内存
- **EmbeddedRocksDBStateBackend**：大状态、增量 Checkpoint 的场景，状态存在 RocksDB

### 来源
- Flink官方文档：https://nightlies.apache.org/flink/flink-docs-stable/docs/ops/state/checkpoints/
- Flink官方文档：https://nightlies.apache.org/flink/flink-docs-stable/docs/ops/state/savepoints/

---

## 题目四：Flink 的窗口函数有哪些？滚窗、滑动窗、会话窗口的区别是什么？

### 参考答案

### 1. **窗口类型概览**

```
┌─────────────────────────────────────────────────────────────┐
│                      Window Functions                        │
├─────────────────┬─────────────────┬─────────────────────────┤
│  Time Window    │  Count Window   │  Custom Window          │
├─────────────────┼─────────────────┼─────────────────────────┤
│ Tumbling        │ Tumbling        │ GlobalWindow            │
│ Sliding         │ Sliding         │ WindowAssigner          │
│ Session         │ Session         │                         │
│ Count           │                 │                         │
└─────────────────┴─────────────────┴─────────────────────────┘
```

### 2. **滚动窗口（Tumbling Window）**
- 无重叠，等长切分
- 数据不重复，每个数据只属于一个窗口
```java
// 每 5 分钟一个窗口，不重叠
stream.keyBy(x -> x.key)
      .window(TumblingProcessingTimeWindows.of(Time.minutes(5)))
      .sum(1);
```

### 3. **滑动窗口（Sliding Window）**
- 有重叠，通过 `size` 和 `slide` 两个参数控制
- 窗口长度 > 滑动步长时重叠，< 时跳跃
```java
// 窗口长度 10 分钟，滑动步长 5 分钟
stream.keyBy(x -> x.key)
      .window(SlidingProcessingTimeWindows.of(Time.minutes(10), Time.minutes(5)))
      .sum(1);
```

### 4. **会话窗口（Session Window）**
- 根据数据之间的**空闲间隔**动态创建窗口
- 没有固定大小，窗口在 inactivity gap 之后关闭
```java
// 会话间隔 5 分钟
stream.keyBy(x -> x.key)
      .window(ProcessingTimeSessionWindows.withGap(Time.minutes(5)))
      .sum(1);
```

### 5. **计数窗口（Count Window）**
- 按元素数量触发，而非时间
```java
// 每 100 条数据触发一次
stream.keyBy(x -> x.key)
      .countWindow(100)
      .sum(1);

// 每 100 条触发，滑动 50 条
stream.keyBy(x -> x.key)
      .countWindow(100, 50)
      .sum(1);
```

### 6. **窗口函数（Window Functions）**

| 类型 | 说明 | 示例 |
|------|------|------|
| **ReduceFunction** | 增量聚合，性能最优 | `reduce((v1, v2) -> v1 + v2)` |
| **AggregateFunction** | 更灵活的聚合接口 | `aggregate(new MyAggregateFunction())` |
| **FoldFunction** | 旧版接口，已废弃 | - |
| **ProcessWindowFunction** | 全量窗口数据处理 | `process(new MyProcessWindowFunction())` |
| **WindowFunction** | 旧版全量接口 | `apply(new MyWindowFunction())` |

### 7. **增量聚合 vs 全量窗口**
- **增量聚合**（ReduceFunction/AggregateFunction）：来一条处理一条，结果直接更新，状态小、延迟低
- **全量窗口**（ProcessWindowFunction）：等窗口关闭后处理所有数据，可以访问完整上下文

### 8. **混合使用**
```java
stream.keyBy(x -> x.key)
      .window(TumblingProcessingTimeWindows.of(Time.minutes(5)))
      .aggregate(
          new AverageAggregate(),    // 增量聚合
          new ProcessWindowFunction<>() // 全量处理，输出侧输出流
      );
```

### 来源
- Flink官方文档：https://nightlies.apache.org/flink/flink-docs-stable/docs/dev/datastream/operators/windows/
- 阿里云实时计算Flink：https://help.aliyun.com/document_detail/62503.html

---

## 题目五：Flink 的状态管理机制是什么？Keyed State 和 Operator State 有何区别？

### 参考答案

### 1. **Flink 状态管理概述**
Flink 是有状态的流处理框架，状态（State）是指算子在处理过程中需要记住的信息，如聚合结果、缓存数据等。Flink 通过 **State Backend** 实现状态的分布式存储和故障恢复。

### 2. **状态类型**

```
┌────────────────────────────────────────────────────────────┐
│                        State                                │
├─────────────────────────┬──────────────────────────────────┤
│     Keyed State         │        Operator State             │
├─────────────────────────┼──────────────────────────────────┤
│ 与 Key 绑定              │ 与算子实例绑定，不依赖 Key         │
│ 仅 KeyedStream 上可用    │ Source、Sink 等算子常用           │
│ 自动按 Key分区           │ 每个算子实例独立维护               │
└─────────────────────────┴──────────────────────────────────┘
```

### 3. **Keyed State（按键分区状态）**
必须先 `keyBy`，状态按 Key 隔离，自动保证线程安全。

| 状态类型 | 说明 | 示例 |
|---------|------|------|
| **ValueState\<T\>** | 单值状态，存储单个值 | `state.update(newValue)` |
| **ListState\<T\>** | 列表状态 | `state.add(item)` |
| **MapState\<K, V\>** | 键值对状态 | `state.put(key, value)` |
| **ReducingState\<T\>** | 自动聚合的列表 | `add` 时自动调用 reduce |
| **AggregatingState\<I, O\>** | 自定义聚合 | `add` 时调用 aggregate 函数 |

**使用示例：**
```java
public class CountWindowAverage 
    extends KeyedProcessFunction<String, Integer, Double> {
    
    private ValueState<Integer> count;
    private ValueState<Integer> sum;

    @Override
    public void open(Configuration parameters) {
        count = getRuntimeContext()
            .getState(new ValueStateDescriptor<>("count", Integer.class));
        sum = getRuntimeContext()
            .getState(new ValueStateDescriptor<>("sum", Integer.class));
    }

    @Override
    public void processElement(Integer value, Context ctx, Collector<Double> out) {
        Integer c = count.value() == null ? 0 : count.value();
        Integer s = sum.value() == null ? 0 : sum.value();
        count.update(c + 1);
        sum.update(s + value);
        out.collect((double) s / c);
    }
}
```

### 4. **Operator State（算子状态）**
不依赖 Key，通常用于 Source（如 Kafka Consumer 的分区偏移量）。

| 状态类型 | 说明 | 使用场景 |
|---------|------|---------|
| **ListState\<T\>** | 列表状态，各实例状态独立 | Kafka 分区状态 |
| **UnionListState\<T\>** | 广播列表状态，故障恢复时可合并 | 故障恢复 |
| **BroadcastState\<K, V\>** | 广播状态，所有实例共享 | 动态规则更新 |

**使用示例：**
```java
public class SourceFunctionWithState implements SourceFunction<String> {
    
    private ListState<Long> offsetState;

    @Override
    public void initializeState(StateInitializationContext context) {
        offsetState = context.getOperatorStateStore()
            .getListState(new ListStateDescriptor<>("offset", Long.class));
    }

    @Override
    public void run(SourceContext<String> ctx) {
        for (Long offset : offsetState.get()) {
            // 从指定 offset 开始消费
        }
    }
}
```

### 5. **State Backend**

| Backend | 存储介质 | 适用场景 |
|---------|---------|---------|
| **HashMapStateBackend** | JVM Heap | 小状态、低延迟 |
| **EmbeddedRocksDBStateBackend** | RocksDB（磁盘） | 大状态、支持增量 Checkpoint |
| **jbhm** | — | — |

### 6. **状态过期与清理**
```java
StateTtlConfig ttlConfig = StateTtlConfig.newBuilder(Time.days(7))
    .setUpdateType(StateTtlConfig.UpdateType.OnCreateAndWrite)  // 创建或写入时更新
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired)  // 不返回过期数据
    .build();

ValueStateDescriptor<String> descriptor = new ValueStateDescriptor<>("state", String.class);
descriptor.enableTimeToLive(ttlConfig);
```

### 来源
- Flink官方文档：https://nightlies.apache.org/flink/flink-docs-stable/docs/concepts/stateful-stream-processing/
- Flink官方文档：https://nightlies.apache.org/flink/flink-docs-stable/docs/dev/datastream/fault-tolerance/state/

---

> 📚 以上题目涵盖 Flink 核心知识点，建议配合源码和实战理解。如有疑问，欢迎交流！
