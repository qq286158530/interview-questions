# 程序员面试题 - 2026-08-19

## 🔥 HBase 大数据面试题

---

### 题目1：HBase 的 RowKey 设计原则是什么？如何避免热点问题？

【答案】

**RowKey 设计核心原则**：

1. **长度原则**：RowKey 长度建议控制在 16-100 字节之间，越短越好
   - 原因：HBase 数据按 RowKey 排序存储，过长的 RowKey 会占用更多 MemStore/HFile 空间
   - 经验值：业务中常使用 MD5、UUID 或业务 ID 的 hash 值作为 RowKey 前缀

2. **散列原则**：避免使用单调递增或递减的 RowKey
   - 坏处：会导致所有数据写入集中在某个 RegionServer，造成热点
   - 解决：使用 hash 打散、随机数前缀、字典反转等技巧

3. **唯一原则**：RowKey 必须唯一标识一行数据，类似主键

**热点问题（HotSpotting）解决方案**：

| 方案 | 原理 | 适用场景 |
|------|------|----------|
| Hash 打散 | MD5(RowKey) 前缀 + 业务ID | 适合有唯一标识的实体 |
| 盐值分区 | Salting + RowKey | 写入分散，但读取需扫描多 Region |
| 反转手机号 | 手机号反转 | 手机号登录场景 |
| 时间戳打散 | Long.MAX_VALUE - timestamp | 时序数据，避免新数据热点 |

**代码示例**：
```java
// 方式一：Hash打散
String rowKey = MD5(orderId).substring(0, 6) + orderId;

// 方式二：盐值前缀（随机字母）
String salt = String.valueOf((char)('a' + Math.random() * 26));
String rowKey = salt + orderId;

// 方式三：时间戳反转（适合时序查询）
String rowKey = Long.MAX_VALUE - System.currentTimeMillis() + userId;
```

**热点监控指标**：
- `hbase.regionserver.requests` - 各 RegionServer 请求量
- `hbase.region.server.queueLength` - 队列长度
- Region 拆分后检查 `hfile.STRESS_HINTS`

---

### 题目2：HBase 的 MemStore 刷写机制是什么？为什么需要先写 MemStore 而不是直接写 HFile？

【答案】

**刷写触发条件**：

1. **内存阈值**：当 MemStore 大小超过 `hbase.hregion.memstore.flush.size`（默认 128MB）时触发刷写
2. **全局内存限制**：当 RegionServer 上所有 MemStore 总大小超过 `hbase.regionserver.global.memstore.size`（默认 0.4，即 40% JVM 堆内存）时，会按 region 倒序刷写，直到降到安全线以下
3. **时间限制**：超过 `hbase.regionserver.global.memstore.size.lower.limit` * `hbase.regionserver.global.memstore.size` 仍不下降，强制刷写
4. **WAL 故障**：如果 WAL 文件数量超过 `hbase.regionserver.max.logs`（默认 32），强制刷写防止日志堆积

**为什么不直接写 HFile**：

1. **顺序写入优化**：HDFS 顺序写入远快于随机写入，MemStore 作为内存缓冲，批量写入 HFile 可以减少 HDFS 的随机 IO
2. **合并写入（Minor Compaction）**：多个 MemStore 刷写后的 HFile 可以合并，减少小文件数量
3. **写放大问题**：每次 put/delete 都会生成 KVO（Key-Value-Output），直接写 HFile 会产生大量小文件
4. **MVCC 支持**：内存中完成多版本管理，批量刷写时统一生成 HFile，保证数据一致性

**刷写流程**：
```
Client Put → WAL(Write-Ahead-Log) → MemStore → (触发条件) → HFile + StoreFile
```

**相关配置参数**：
```xml
<property>
  <name>hbase.hregion.memstore.flush.size</name>
  <value>134217728</value>  <!-- 128MB -->
</property>
<property>
  <name>hbase.regionserver.global.memstore.size</name>
  <value>0.4</value>  <!-- 40% 堆内存 -->
</property>
<property>
  <name>hbase.regionserver.max.logs</name>
  <value>32</value>
</property>
```

---

### 题目3：HBase 的 HFile 存储结构是怎样的？为什么 HFile 能支持高效随机读取？

【答案】

**HFile 物理结构**：

HFile 是基于 SSTable（Sorted String Table）的实现，结构如下：

```
┌──────────────────────────────────────────────────────────┐
│  HFile (Data Block)                                      │
├──────────────────────────────────────────────────────────┤
│  Block1 (Data Block): [KeyValue][KeyValue][KeyValue]... │
│  Block2 (Data Block): [KeyValue][KeyValue][KeyValue]... │
│  ...                                                     │
│  BlockN (Data Block): [KeyValue][KeyValue][KeyValue]... │
├──────────────────────────────────────────────────────────┤
│  BlockN+1 (Leaf Index Block):                           │
│    BlockOffset0|BlockKey0                                │
│    BlockOffset1|BlockKey1                               │
│    ...                                                   │
├──────────────────────────────────────────────────────────┤
│  Meta Block (BloomFilter):                               │
│    用于快速判断 Key 是否存在于当前 HFile                  │
├──────────────────────────────────────────────────────────┤
│  Meta Index Block                                        │
├──────────────────────────────────────────────────────────┤
│  FileInfo: AvgKeyLen, LastKey, MaxSeqID...               │
├──────────────────────────────────────────────────────────┤
│  Trailer: 记录各区域的起始偏移量                           │
└──────────────────────────────────────────────────────────┘
```

**核心组成部分详解**：

| 组成部分 | 作用 | 大小 |
|----------|------|------|
| Data Block | 存储实际的 KeyValue 数据 | 可配置，默认 64KB |
| Leaf Index Block | 索引 Data Block，支持二分查找 | - |
| Bloom Filter | 快速判断 Key 是否存在，减少不必要的 IO | - |
| Trailer | 存储各区域的偏移量，用于快速定位 | 固定大小 |

**KeyValue 结构**：
```
[KeyLength (4B)][ValueLength (4B)][Key][Value]
  Key = RowKey + ColumnFamily + Column + Timestamp + KeyType
```

**高效随机读取原理**：

1. **Block 索引**：通过 Leaf Index Block 可以快速定位到目标 Key 所在的 Data Block
2. **BloomFilter**：在读取前先查 BloomFilter，如果不存在直接跳过该 HFile
3. **稀疏索引**：每个 Data Block 第一个 Key 会被记录到索引中，支持快速跳转
4. **块缓存**：热点 Data Block 会被缓存到 BlockCache（LRU 缓存）中，二次读取直接命中缓存

**三级查询流程**：
```
1. BloomFilter 过滤 → 不存在则直接跳过此 HFile
2. Index Block 二分查找 → 定位到目标 Data Block
3. Data Block 内顺序扫描 → 找到目标 KeyValue
```

---

### 题目4：HBase 的协处理器（Coprocessor）有哪两类？它们的使用场景和加载方式有什么区别？

【答案】

**两类协处理器对比**：

| 类型 | 加载方式 | 作用对象 | 触发时机 |
|------|----------|----------|----------|
| Observer | 配置文件 / HBase Shell | 表级别 | 数据的 GET/PUT/DELETE 操作前后 |
| Endpoint | 配置文件 / HBase API | 列簇级别 | 需要自定义 RPC 协议 |

**一、Observer 协处理器**

类似数据库触发器，在指定操作前后执行钩子函数：

```java
// 继承 BaseRegionObserver 并重写钩子方法
public class AuditCoprocessor extends BaseRegionObserver {
    
    @Override
    public void prePut(ObserverContext<RegionCoprocessorEnvironment> e, 
                       Put put, WALEdit edit, boolean writeToWAL) {
        // 在 Put 之前执行：数据脱敏、权限校验
        String rowKey = Bytes.toString(put.getRow());
        if (rowKey.startsWith("sensitive_")) {
            // 移除敏感列
            put.getFamilyMap().values().forEach(list -> {
                // 数据脱敏处理
            });
        }
    }
    
    @Override
    public void postPut(ObserverContext<RegionCoprocessorEnvironment> e,
                        Put put, WALEdit edit, boolean writeToWAL) {
        // 在 Put 之后执行：异步记录操作日志
        logAudit(put);
    }
    
    @Override
    public void preGet(ObserverContext<RegionCoprocessorEnvironment> e, Get get) {
        // GET 之前：权限校验
        if (!hasPermission(get)) {
            throw new AccessDeniedException("No permission to access");
        }
    }
}
```

**常见使用场景**：
1. **二级索引**：在 prePut 时异步写入索引表
2. **数据审计**：记录所有数据变更操作
3. **权限控制**：基于行级别的访问控制
4. **数据脱敏**：自动过滤敏感列

**加载方式**：
```xml
<!-- 方式一：hbase-site.xml 全局加载 -->
<property>
  <name>hbase.coprocessor.user.region.classes</name>
  <value>com.example.AuditCoprocessor</value>
</property>

<!-- 方式二：HBase Shell 表级别加载 -->
alter 'myTable', METHOD => 'table_att', 
'Coprocessor'=>'hdfs:///path/to/coprocessor.jar|com.example.AuditCoprocessor|1001|arg1=val1'
```

**二、Endpoint 协处理器**

类似存储过程，在 RegionServer 上执行自定义计算逻辑：

```java
// 定义协议接口
public interface ColumnSumProtocol extends CoprocessorProtocol {
    long getColumnSum(byte[] family, byte[] qualifier) throws IOException;
}

// 实现类
public class ColumnSumEndpoint extends BaseEndpointCoprocessor 
    implements ColumnSumProtocol {
    
    @Override
    public long getColumnSum(byte[] family, byte[] qualifier) throws IOException {
        long sum = 0;
        Scanner scanner = getEnvironment().getRegion().getScanner(
            new Scan().addColumn(family, qualifier));
        try {
            Result result = new Result();
            while (scanner.next(result)) {
                sum += Bytes.toLong(result.getValue(family, qualifier));
            }
        } finally {
            scanner.close();
        }
        return sum;
    }
}

// 客户端调用
Table table = connection.getTable(TableName.valueOf("myTable"));
CoprocessorRpcChannel channel = table.coprocessorService(rowKey);
ColumnSumProtocol protocol = 
    RPC.BlockingProxy(channel, ColumnSumProtocol.class, 
        RPC.getProtocolVersion(ColumnSumProtocol.class));
long sum = protocol.getColumnSum(Bytes.toBytes("cf"), Bytes.toBytes("amount"));
```

**常见使用场景**：
1. **聚合计算**：求和、计数、平均值等
2. **复杂过滤**：正则匹配、模糊查询
3. **跨行事务**：多行原子性操作

**两类协处理器对比总结**：

| 维度 | Observer | Endpoint |
|------|----------|----------|
| 执行位置 | RegionServer | RegionServer |
| 通信方式 | 被动触发 | RPC 主动调用 |
| 返回值 | 无（Side Effect） | 有（自定义协议） |
| 典型场景 | 审计、脱敏、二级索引 | 聚合计算、自定义过滤 |

---

### 题目5：HBase 的 Minor Compaction 和 Major Compaction 有什么区别？它们分别什么时候触发？

【答案】

**核心区别**：

| 维度 | Minor Compaction | Major Compaction |
|------|------------------|------------------|
| 合并范围 | 多个小 HFile | 一个列簇下所有 HFile |
| 执行频率 | 较高（几分钟到几小时） | 较低（默认 7 天） |
| 删除处理 | 不删除标记删除的数据 | 删除过期版本和标记删除的数据 |
| IO 影响 | 较小 | 较大，通常放在业务低峰期 |
| 结果 | 生成少量中等大小的 HFile | 生成一个大的 HFile |

**Minor Compaction 详解**：

又称 StoreFile Compaction，过程如下：
1. 挑选出多个小的 HFile（满足 `hbase.store.compaction.min` 默认 3 个）
2. 合并排序后写入一个较大的 HFile
3. 保留原有 HFile 直到合并完成

**触发条件**：
- HFile 数量超过 `hbase.store.compaction.min`（默认 3）
- HFile 大小小于 `hbase.hstore.compaction.max.size`（默认 HRegion 大小）
- 需要排除正在 Compact 的 HFile

**Major Compaction 详解**：

又称 Normal Compaction，将一个 Store（列簇）下所有 HFile 合并为一个：
1. 读取所有 HFile 的 KeyValue
2. 按照 RowKey、ColumnFamily、Column、Timestamp 排序
3. 根据 MVCC 版本号过滤：只保留最新版本
4. 删除标记（KeyType=Delete/ColumnDelete）的数据永久删除
5. 删除 TTL 过期（Version > maxVersion）的数据
6. 生成一个全新的 HFile

**触发方式**：

| 触发方式 | 配置参数 | 说明 |
|----------|----------|------|
| 自动触发 | `hbase.hregion.majorcompaction` | 默认 7 天，建议设置为 0 关闭自动 major |
| 手动触发 | `major_compact` HBase Shell 命令 | 对表或列簇执行 |
| API 触发 | HBaseAdmin.majorCompact() | 代码调用 |

**Compaction 相关配置**：

```xml
<!-- Minor Compaction 配置 -->
<property>
  <name>hbase.hstore.compaction.min</name>
  <value>3</value>  <!-- 最少 HFile 数量 -->
</property>
<property>
  <name>hbase.hstore.compaction.max</name>
  <value>10</value>  <!-- 单次 Minor Compaction 最大 HFile 数量 -->
</property>

<!-- Major Compaction 配置 -->
<property>
  <name>hbase.hregion.majorcompaction</name>
  <value>604800000</value>  <!-- 7 天，单位毫秒 -->
</property>
<property>
  <name>hbase.hstore.compaction.max.size</name>
  <value>10737418240</value>  <!-- 10GB，超过此大小不参与 Minor Compaction -->
</property>

<!-- IO 控制 -->
<property>
  <name>hbase.regionserver.thread.compaction throttle</name>
  <value>2</value>  <!-- Compaction 并发线程数 -->
</property>
```

**最佳实践建议**：

1. **关闭自动 Major Compaction**：在业务高峰期 major compaction 可能导致 IO 飙升
2. **使用脚本在低峰期手动触发**：使用 Crontab 定时任务在凌晨 2-6 点执行
3. **监控 HFile 数量**：如果 store 下 HFile 数量持续增长，说明 minor compaction 不及，需要调参
4. **SSD 场景**：可以适当增加 compaction 频率，因为 IO 开销较小

---

## 📚 参考来源

1. [HBase Architecture - Apache HBase Documentation](https://hbase.apache.org/book.html#arch.regionserver.store)
2. [HBase RowKey Design - Cloudera Blog](https://blog.cloudera.com/understanding-hbase-and-big-table/)
3. [HBase Coprocessor - Hortonworks](https://docs.cloudera.com/HDPDocuments/HDP3/HDP-3.1.0/hbase-overview/content/hbase_coprocessors.html)
4. [HBase Compaction - Data Engineering with Hadoop](https://docs.cloudera.com/runtime/7.2.2/managing-hbase/content/managing_hbase_compactions.html)
5. [HFile: A File Format for HBase - Apache Wiki](https://wiki.apache.org/hadoop/Hbase/Hfile)
