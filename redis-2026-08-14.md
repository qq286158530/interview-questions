# Redis 面试题 - 2026-08-14

> 本文精选5道高质量Redis面试题，涵盖数据类型、持久化、集群、缓存等核心知识点。

---

## 题目一：Redis 和 Memcached 有什么区别？为什么 Redis 比 Memcached 更适合作为 MySQL 的缓存？

### 参考答案

**Redis 与 Memcached 的区别：**

| 区别点 | Redis | Memcached |
|--------|-------|-----------|
| 数据类型 | 支持5种基本类型 + 4种扩展类型 | 仅支持简单的 key-value |
| 持久化 | 支持 RDB 和 AOF 两种持久化方式 | 无持久化功能，数据全存内存 |
| 集群 | 原生支持主从复制、哨兵、Cluster 模式 | 无原生集群，需客户端实现分片 |
| 高级特性 | 支持发布订阅、Lua脚本、事务 | 不支持 |
| 数据结构 | 底层实现丰富（SDS、跳表、压缩列表等） | 纯内存 kv |

**为什么 Redis 更适合作为 MySQL 的缓存？**

1. **高性能**：数据在内存中读写，速度极快。第一次访问MySQL的数据会被缓存到Redis，下一次直接从缓存获取，避免硬盘IO。

2. **高并发**：单台Redis的QPS可达10W+，而MySQL单机很难破1W。可以承担大部分读请求，降低数据库压力。

3. **丰富的数据类型**：Redis支持String、Hash、List、Set、ZSet等多种数据类型，能满足更复杂的业务场景。

4. **数据持久化**：Redis支持RDB和AOF两种持久化方式，重启后可恢复数据，而Memcached数据完全丢失。

**来源：** [图解Redis - 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 各自有什么优缺点？

### 参考答案

**Redis 三种持久化方式：**

1. **RDB（Redis Database）**：记录某一时刻的内存数据，以二进制格式写入磁盘
2. **AOF（Append Only File）**：每执行一条写操作命令，就将该命令追加到文件末尾
3. **混合持久化**：Redis 4.0 新增，结合 RDB 和 AOF 的优点

**RDB 优缺点：**

- ✅ 优点：恢复速度快（直接读取二进制文件加载到内存）；适合做备份
- ❌ 缺点：无法实时持久化，可能丢失数据；fork子进程时有性能开销

**AOF 优缺点：**

- ✅ 优点：数据安全性高（可选 always/everysec/no 三种策略）；日志格式易理解
- ❌ 缺点：文件体积通常比RDB大；恢复速度慢（需逐条执行命令）

**AOF 三种写回策略：**

| 策略 | 说明 | 安全性 | 性能 |
|------|------|--------|------|
| Always | 每次写命令执行完后同步写回硬盘 | 最高 | 最差 |
| Everysec | 每秒将缓冲区内容写回硬盘 | 中等 | 中等 |
| No | 由操作系统决定何时写回 | 最低 | 最好 |

**混合持久化**：AOF文件前半部分是RDB格式的全量数据，后半部分是AOF格式的增量数据。结合了两者优点——启动快 + 丢失数据少。

**来源：** [图解Redis - 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目三：Redis 是单线程吗？为什么单线程的 Redis 还能保持高并发？

### 参考答案

**Redis 的线程模型：**

Redis **单线程**指的是接收客户端请求→解析请求→读写数据→发送响应这个过程由**主线程**完成。

但Redis程序**并非完全单线程**，它会启动后台线程（BIO）：
- **2.6版本**：2个后台线程，处理关闭文件、AOF刷盘
- **4.0+版本**：新增lazyfree线程，异步释放内存

**为什么单线程的 Redis 还能保持高并发？**

1. **内存操作**：Redis大部分操作在内存中完成，且采用了高效数据结构（SDS、跳表等），性能瓶颈是内存或网络带宽，而非CPU

2. **避免了多线程开销**：无线程切换、锁竞争、死锁等问题

3. **I/O多路复用**：使用epoll机制，一个线程处理多个Socket请求。内核同时监听多个Socket，有请求到达时才交给Redis线程处理

4. **Redis 6.0+ 多线程I/O**：对于网络I/O采用多线程处理（默认3个I/O线程），命令执行仍是单线程

**Redis 6.0 后的线程分工：**
- 主线程：执行命令
- bio_close_file：异步关闭文件
- bio_aof_fsync：异步刷盘
- bio_lazy_free：异步释放内存
- io_thd_1/2/3：处理网络I/O

**来源：** [图解Redis - 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目四：Redis 如何实现服务高可用？哨兵模式和Cluster模式有什么区别？

### 参考答案

**Redis 高可用方案：**

| 方案 | 原理 | 适用场景 |
|------|------|----------|
| 主从复制 | 一主多从，读写分离，数据同步 | 数据备份 + 读写分离 |
| 哨兵模式 | 监控主从、自动故障转移 | 自动 Failover |
| Cluster 模式 | 16384个哈希槽，数据分片 | 海量数据 + 高可用 |

**主从复制：**
- 主服务器处理写操作，异步同步给从服务器
- 从服务器只读，接受主服务器同步的命令
- 命令复制是异步的，无法保证强一致性

**哨兵模式：**
- 监控主从服务器状态（心跳检测）
- 主服务器下线时，自动从从服务器选举新主节点
- 解决了手动故障恢复的问题

**Cluster 切片集群：**
- 16384个哈希槽（Hash Slot）
- 根据key的CRC16值对16384取模，决定键值对归属
- 数据分布在不同节点，解决单节点内存瓶颈

**脑裂问题：**
网络分区导致多个主节点，客户端写入旧主节点的数据可能丢失。解决方案：设置 `min-slaves-to-write` 和 `min-slaves-max-lag`，确保主节点必须有足够从节点连接才接受写请求。

**来源：** [图解Redis - 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目五：Redis 的过期删除策略是什么？内存淘汰机制有哪些？

### 参考答案

**过期删除策略：惰性删除 + 定期删除**

1. **惰性删除**：每次访问key时检查是否过期，过期则删除
   - ✅ CPU友好（很少系统资源）
   - ❌ 内存不友好（过期key占空间）

2. **定期删除**：每隔一段时间随机抽取20个key检查，删除已过期的
   - 若过期key超过5个（25%），继续抽取删除
   - 循环时间上限25ms，避免阻塞
   - ✅ 平衡CPU和内存

**持久化对过期键的处理：**

| 阶段 | 处理方式 |
|------|----------|
| RDB生成 | 过期键不会被写入新RDB文件 |
| RDB加载 | 主服务器不载入过期键；从服务器载入但会被主从同步清空 |
| AOF写入 | 过期键保留，删除时追加DEL命令 |
| AOF重写 | 已过期键不会被保存 |

**主从模式**：从库不主动删除过期键，由主库在AOF中追加DEL指令同步给从库。

**内存淘汰机制（6种）：**

| 策略 | 说明 |
|------|------|
| noeviction | 不淘汰，返回错误（默认） |
| volatile-lru | 从已设置过期时间的key中淘汰最近最少使用的 |
| volatile-lfu | 从已设置过期时间的key中淘汰使用频率最低的 |
| volatile-ttl | 从已设置过期时间的key中淘汰剩余TTL最短的 |
| volatile-random | 从已设置过期时间的key中随机淘汰 |
| allkeys-lru | 从所有key中淘汰最近最少使用的 |
| allkeys-lfu | 从所有key中淘汰使用频率最低的 |
| allkeys-random | 从所有key中随机淘汰 |

**来源：** [图解Redis - 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

*面试题整理自网络，仅供学习参考*
