# Spark 面试题（2026-08-13）

> 本文档整理 5 道高频 Spark 面试题，涵盖 Spark 架构、RDD、DataFrame/Shuffle、广播变量、内存管理等核心知识点。

---

## 题目一：Spark 的架构与核心组件

### 问题
请描述 Spark 的整体架构，以及 Driver、Executor、Master、Worker 的作用与相互关系。

### 答案

**Spark 整体架构**

Spark 采用 Master-Slave 架构，核心组件如下：

| 组件 | 角色 | 职责 |
|------|------|------|
| **Driver（驱动器）** | 主节点 | 运行用户代码，创建 SparkContext，将任务（DAG）拆分为多个 Stage，调度 Task 到 Executor |
| **Executor（执行器）** | 从节点 | 负责执行 Driver 分发的 Task，将数据缓存在内存/磁盘中 |
| **Master** | 资源管理主节点 | 协调整个集群的资源分配，管理 Worker 节点 |
| **Worker** | 资源管理从节点 | 向 Master 注册自身资源（CPU、内存），启动 Executor 进程 |

**工作流程**

1. 用户提交 Spark 应用程序，通过 `spark-submit` 发送到集群；
2. Driver 向 Master 申请资源，Master 通知 Worker 启动 Executor；
3. Executor 向 Driver 注册，Driver 将应用代码分发给各 Executor；
4. SparkContext 将用户代码转化为 DAG，根据 shuffle 边界划分 Stage，再将 Stage 调度为一批批 Task；
5. Task 被发送到各 Executor 的线程池中并行执行；
6. 结果写回 SparkContext（最终汇聚到 Driver）。

**关键理解**
- **Driver 是逻辑中心**：负责任务调度、结果汇总，即使在 Standalone 模式下 Driver 也会参与计算；
- **Executor 是物理执行单元**：每个 Application 独占自己的 Executor 进程，进程内所有 Task 共享内存；
- **Master 与 Worker 不参与具体计算**：它们只负责集群资源管理，类似 YARN 的 ResourceManager / NodeManager。

---

## 题目二：RDD 的特性与 lineage（血统）机制

### 问题
RDD（Resilient Distributed Dataset）有哪些核心特性？血统（Lineage）机制是如何实现容错的？

### 答案

**RDD 五大核心特性**

1. **分区列表（Partitions）**：RDD 由多个 Partition 组成，每个 Partition 可并行处理；
2. **计算函数（Compute Function）**：每个 Partition 上执行相同的计算函数（如 `map`、`filter`）；
3. **依赖关系（Dependencies）**：RDD 之间存在依赖（宽依赖或窄依赖）；
4. **分区器（Partitioner，可选）**：Key-Value 类型的 RDD 可指定分区器（如 HashPartitioner、RangePartitioner）；
5. **最优位置（Preferred Locations，可选）**：每个 Partition 的最优执行位置（数据局部性）。

**Lineage（血统）机制**

Lineage（血统链）是 Spark 实现容错的核心思想：**不依赖数据副本复制，而是记录 RDD 的转换操作（Transformation）序列**。

当某个 Partition 数据丢失时：
1. Spark 根据 Lineage 从父 RDD 重新计算该 Partition（只需重跑丢失部分）；
2. 窄依赖（Narrow Dependency）：父 RDD 一个 Partition 只对应子 RDD 一个 Partition，可流水线式重算，效率高；
3. 宽依赖（Wide Dependency）：父 RDD 一个 Partition 对应子 RDD 多个 Partition（如 `groupByKey`、`join`），需要 shuffle，重算代价大。

**与 MapReduce 的对比**

| | Spark RDD | Hadoop MapReduce |
|---|---|---|
| 容错方式 | Lineage（血统）重算 | Checkpoint（检查点）+ 数据副本 |
| 中间结果 | 内存缓存 | 写磁盘（HDFS） |
| 错误恢复 | 仅重算丢失 Partition | 重跑整个 Job/Task |

**典型容错示例**

```
rdd1 = sc.textFile("hdfs://...")          // RDD1
rdd2 = rdd1.map(...).filter(...)          // RDD2 窄依赖
rdd3 = rdd2.groupByKey()                  // RDD3 宽依赖
```

若 RDD3 的某个 Partition 丢失，只需从 RDD2 对应的 Partition 重算，不需要重算整个 Lineage。

---

## 题目三：DataFrame 与 DataSet 的区别，以及 Spark SQL 的执行原理

### 问题
DataFrame 和 DataSet 有何区别？Spark SQL 是如何将 SQL / DSL 转换为可执行任务的？

### 答案

**DataFrame vs DataSet**

| 特性 | DataFrame | DataSet（强类型） |
|------|-----------|------------------|
| **数据类型** | Row 对象（弱类型，类似数据库表） | JVM 对象（强类型，Case Class） |
| **API 风格** | DSL 链式调用 / SQL | 强类型 lambda 函数 |
| **编译时检查** | 仅运行时检查 | 编译时类型检查 |
| **性能优化** | Catalyst 优化器自动优化 | 同 DataFrame（统一优化） |
| **Encoder** | 内置 `RowEncoder` | 需手动 `encoders.product[T]` |
| **使用场景** | 结构化/半结构化数据，动态字段 | 明确类型的业务逻辑代码 |

> 在 Spark 2.x 之后，`DataFrame = DataSet[Row]`，两者在内部共享同一套优化引擎。

**Spark SQL 执行原理（Catalyst 优化器）**

Spark SQL 的执行分为 4 个阶段：

```
SQL / DSL → Unresolved Logical Plan → Analyzed Logical Plan 
→ Optimized Logical Plan → Physical Plan → RDDs
```

1. **Parsing（解析）**：将 SQL 解析为 AST，或将 DSL 转换为 Unresolved Logical Plan；
2. **Analysis（分析）**：通过 Catalog（元数据）解析表名、列名，生成 Analyzed Logical Plan；
3. **Optimization（优化）**：Catalyst 执行规则优化，包括：
   - 列裁剪（Column Pruning）：只读取需要的列；
   - 谓词下推（Predicate Pushdown）：先 filter 再 join；
   - 常量替换、布尔表达式简化；
   - **Spark 3.x 新增 AQE（Adaptive Query Execution）**：运行时动态调整执行计划（动态 Shuffle 分区、Join 策略切换）；
4. **Physical Planning（物理计划）**：将优化后的逻辑计划转为物理执行计划（选择 HashJoin / SortMergeJoin 等），生成 RDD 操作。

---

## 题目四：Shuffle 的原理、常见调优参数与常见问题

### 问题
什么是 Shuffle？为什么 Shuffle 是 Spark 性能的关键瓶颈？请列出主要调优参数。

### 答案

**Shuffle 的定义**

Shuffle 是**重新分区数据的过程**，将所有相同的 Key 汇聚到同一个 Partition 中，以便后续聚合或 Join。触发 Shuffle 的操作包括：`groupByKey`、`reduceByKey`、`join`、`cogroup`、`sortByKey`、`repartition`、`collect` 等。

**Shuffle 的工作原理**

```
写入端（Map端）                          读取端（Reduce端）
   |                                          |
分区器决定 Key → 哪个 bucket              Reducer 去拉取对应 bucket
   |                                          |
数据写入本地磁盘文件（多个 bucket）       fetch 远程文件块到本地
   |                                          |
   └── FileConsolidation（可合并小文件）  ──┘
```

- **Map 端**：每个 Task 将数据按 Key hash 到 `M` 个 bucket（文件），写入本地磁盘；
- **Reduce 端**：每个 Task 需要从所有 Map 端节点拉取自己负责的 bucket（网络 IO）；
- **bucket 数量**：由 `spark.sql.shuffle.partitions`（默认 200）或 `spark.default.parallelism` 控制。

**为什么是瓶颈？**

1. **磁盘 IO**：Shuffle 数据先落盘再网络传输，HDFS 风格下更慢；
2. **网络 IO**：跨节点数据传输量大；
3. **内存压力**：Shuffle 数据可能使用堆外内存，且并发 fetch 消耗大量内存；
4. **磁盘小文件爆炸**：大量 Task 产生海量小文件，增加磁盘寻道开销。

**主要调优参数**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `spark.sql.shuffle.partitions` | 200 | Shuffle 分区数（过大增加调度开销，过小数据倾斜） |
| `spark.shuffle.file.buffer` | 32KB | Map 端写磁盘的缓冲区大小 |
| `spark.reducer.maxSizeInFlight` | 48MB | Reduce 端单次从 Map 端拉取的最大数据量 |
| `spark.shuffle.service.enabled` | false | 开启 External Shuffle Service（YARN 模式），避免 Executor 失败影响 shuffle |
| `spark.sql.adaptive.enabled` | false | 开启 AQE（Spark 3.0+），动态调整分区数 |
| `spark.sql.adaptive.skewJoin.enabled` | true | 开启倾斜 Join 优化 |

**最佳实践**

- Shuffle 分区数 ≈ `集群 CPU 核数 × 2~4`，避免过多小分区；
- 避免单次 Shuffle 数据量过大（大表 JOIN 前先过滤）；
- 使用 `coalesce(1)` 谨慎，只有在确定数据量小时才用（会聚集所有数据到一个 Partition）。

---

## 题目五：广播变量（Broadcast Variable）原理与使用场景

### 问题
什么是广播变量？它的工作原理是什么？适用于哪些场景？有何注意事项？

### 答案

**广播变量的定义**

广播变量是只读的、会被分发到集群每个节点的高效数据结构。Driver 端将数据序列化后一次性发送到各 Executor，所有 Task 共享同一份内存副本，**避免每个 Task 各自持有一份副本造成内存浪费**。

**工作原理**

```
1. Driver 端：broadcast(x) 将变量 x 序列化并缓存
2. Driver → Executor：高效 P2P 分发（BitTorrent / HTTP Server），广播到所有节点
3. Executor 端：数据存储在 BlockManager 中，Task 通过 broadcastId 查询获取
4. Task 使用：本地读取，无需跨网络传输
```

**典型使用场景**

```scala
// 场景1：小表广播（广播 JOIN）
val smallDF = spark.table("dim_product")  // 维度表，数据量小
broadcast(smallDF).join(factDF, "product_id")

// 场景2：配置信息/字典表广播
val dict = sc.broadcast(Map("A" -> "内科", "B" -> "外科"))
val result = rdd.map(row => dict.value.getOrElse(row._1, "未知"))

// 场景3：跨 Stage 共享只读数据
val sharedData = sc.broadcast(Array(1, 2, 3, 4, 5))
rdd.map(x => x * sharedData.value.sum)
```

**适用条件（黄金法则）**

广播变量适用于满足以下**任一条件**的场景：

| 条件 | 说明 | 示例 |
|------|------|------|
| 小表数据 | 可以放入 Executor 内存 | 维度表 < 20MB |
| 常量/配置数据 | 整个 Job 生命周期不变 | 城市映射、分类字典 |
| 跨 Task 共享 | 多个 Task 需要相同数据 | 全局配置参数 |

> **注意**：若广播数据过大，会导致 Executor OOM（因为所有 Task 共享同一份，存储在 BlockManager 堆内/堆外内存中）。

**不适用场景**

- 大表 JOIN（大表不适合广播，否则内存爆炸）；
- 数据频繁变更（广播变量只读，更新需要重新广播整个对象）。

**相关参数**

| 参数 | 说明 |
|------|------|
| `spark.sql.broadcastTimeout` | 广播超时时间（默认 5 分钟） |
| `spark.sql.autoBroadcastJoinThreshold` | 自动广播阈值（默认 10MB），`-1` 禁用 |

---

> 以上题目均基于 Spark 3.x 最新特性整理，来源包括 Spark 官方文档及业界经典博客。持续更新中，欢迎 Star ⭐。
