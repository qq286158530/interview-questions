# Redis 面试题（2026-08-17）

> 以下题目由 AI 助手基于 Redis 核心知识点整理，涵盖数据类型、持久化、集群、缓存等高频面试内容。

---

## 题目一：Redis 有哪些数据类型？适用场景分别是什么？

### 答案

Redis 共支持 **8 种数据类型**：

| 数据类型 |底层实现| 典型使用场景 |
|----------|--------|-------------|
| **String** | SDS（简单动态字符串） | 缓存、计数器、分布式锁（SETNX）、限流、Session 存储 |
| **Hash** | 压缩列表（元素少时）或 HashMap | 存储对象（用户信息、商品信息）、购物车 |
| **List** | 压缩列表或链表 | 消息队列（LPUSH + BRPOP）、文章/评论列表、最新消息流 |
| **Set** | 整数集合或 HashMap | 标签（Tag）聚合、好友关系、共同关注、去重集合 |
| **Zset（Sorted Set）** | 压缩列表或跳表 | 排行榜（带分数排序）、延时队列、权重队列 |
| **Bitmap** | String 位图 | 签到统计、用户在线状态、布隆过滤器辅助 |
| **HyperLogLog** | string（pfadd/pfcount） | 大规模独立用户数统计（UV），内存极小（12KB） |
| **Geospatial** | 有序集合（zset） | 附近的人/商家、LBS 位置服务 |
| **Stream** | RadixTree | 消息队列（替代 List，支持消费组、消息 ID） |

**高频追问：String 最长能存多大？**
> 默认最大 512MB，建议存小于 100KB 的数据，大文件建议存 URL 或使用 OSS。

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？如何选择？

### 答案

Redis 提供了两种持久化机制：

### RDB（Redis Database）
- **原理**：定时将内存中的**全量数据**快照（Snapshot）写入二进制文件 `dump.rdb`
- **触发方式**：
  - 手动：`SAVE`（阻塞）或 `BGSAVE`（后台异步）
  - 自动：`save m n`（m 秒内有 n 次写操作时触发）
- **优点**：恢复速度快（全量快照文件小）
- **缺点**：可能丢失最近一次快照之后的数据

### AOF（Append Only File）
- **原理**：将每一次**写操作**追加到文件 `appendonly.aof`
- **同步策略**（`appendfsync`）：
  - `always`：每条命令都同步（最安全，性能最差）
  - `everysec`：每秒同步（默认，推荐）
  - `no`：由 OS 决定何时同步（最快，可能丢失较多数据）
- **优点**：数据安全性更高
- **缺点**：AOF 文件比 RDB 大，恢复速度慢

### 混合持久化（Redis 4.0+）
- 开启 `aof-use-rdb-preamble yes`
- AOF 重写时，先写 RDB 快照，再追加增量 AOF 命令
- 兼具两者优点：恢复快 + 丢数据少

### 如何选择？
| 场景 | 推荐策略 |
|------|---------|
| 数据重要、可接受一定丢失 | RDB + AOF everysec |
| 追求最高数据安全 | AOF always + 混合持久化 |
| 只做缓存、可以丢失 | 不开持久化 |
| 大数据量、关注性能 | 混合持久化 |

---

## 题目三：Redis 的过期 key 删除策略是什么？内存淘汰机制有哪些？

### 答案

### 过期 key 删除策略

Redis 同时使用**两种策略**删除过期 key：

1. **惰性删除**（Lazy Expire）
   - 访问 key 时才检查是否过期，过期则删除
   - 优点：对 CPU 友好；缺点：过期 key 长期占用内存

2. **定期删除**（Active Expire）
   - 每隔一段时间（默认 100ms），随机抽取设置了过期时间的 key 检查删除
   - 避免全量扫描造成阻塞，同时防止内存过度膨胀

### 内存淘汰机制（当内存达到 `maxmemory` 时）

| 策略 | 行为 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 从已设置过期时间的 key 中淘汰 **LRU** 最少使用的 |
| `allkeys-lru` | 从所有 key 中淘汰 LRU 最少使用的 **（最常用）** |
| `volatile-lfu` | 从已设置过期时间的 key 中淘汰 LFU 最少使用的 |
| `allkeys-lfu` | 从所有 key 中淘汰 LFU 最少使用的 |
| `volatile-random` | 随机淘汰已设置过期时间的 key |
| `allkeys-random` | 随机淘汰所有 key |
| `volatile-ttl` | 淘汰最近要过期的 key（TTL 最小） |

**高频追问：LRU 和 LFU 的区别？**
> - LRU（Least Recently Used）：淘汰最长时间未被访问的
> - LFU（Least Frequently Used）：淘汰访问频率最低的（更精准，但实现复杂度更高）

---

## 题目四：Redis 主从复制原理是什么？哨兵（Sentinel）和集群（Cluster）有什么区别？

### 答案

### 主从复制原理

```
主节点（Master）                  从节点（Slave）
    │                                │
    │◄────── PSYNC / SYNC ──────────│  ① 首次全量同步
    │                                │
    │─────── RDB 文件（bgsave）──────►  ② 传输快照
    │                                │
    │─────── 增量命令流 ─────────────►  ③ 命令传播
    │                                │
    │◄─────── 心跳检测 ──────────────│  ④ 保持同步
```

- **首次同步**：从节点发送 `PSYNC ? -1` → 主节点 `BGSAVE` 生成 RDB → 传输给从节点 → 从节点加载
- **增量复制**：主节点将写命令维护在 **replication buffer** 和 **replication backlog** 中，从节点定期上报 offset，主节点推送缺失的命令
- **断线重连**：从节点发送 `PSYNC <runid> <offset>` 尝试增量同步

### 哨兵（Sentinel）vs 集群（Cluster）

| 对比维度 | 哨兵模式 | 集群模式 |
|---------|---------|---------|
| **定位** | 高可用（HA）+ 主从自动故障转移 | 数据分片（Sharding）+ 高可用 |
| **数据分布** | 主从复制（全量数据每节点一份） | 16384 个槽位分片（每节点存部分数据） |
| **写能力** | 仅主节点可写 | 仅主节点可写（有 replica 作为热备） |
| **节点数量** | 至少 3 哨兵 + 1 主 2 从 | 至少 6 节点（3 主 3 从） |
| **故障转移** | 哨兵监控并投票选出新主 | 集群自动进行槽位迁移和故障转移 |
| **适用场景** | 小型项目、单机多实例 | 大型项目、数据量大、需要水平扩展 |
| **跨分片查询** | 支持 | 不支持（需客户端支持跨槽查询） |

### 槽位分配举例（Cluster）
> 假设 3 主 3 从：节点 A 负责槽 0-5460，节点 B 负责 5461-10922，节点 C 负责 10923-16383。客户端根据 `CRC16(key) % 16384` 定位槽位并请求对应节点。

---

## 题目五：什么是缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 答案

这是 Redis 和后端面试中最经典的高频问题之一。

### 缓存穿透
**问题**：查询一个**不存在**的数据，缓存没有命中，请求直接打到数据库，导致数据库压力过大。

**常见原因**：恶意刷接口、数据库本就没有该数据。

**解决方案**：
- **布隆过滤器（BloomFilter）**：将所有存在的 key 存入 BitMap，请求进来先查布隆过滤器，命中再去查 Redis/DB
- **空值缓存**：查询结果为空时，在 Redis 中缓存一个 `NULL` 值（如 `"NULL"`），并设置较短 TTL（如 30s）
- **接口层校验**：参数合法性检查、用户鉴权等

### 缓存击穿
**问题**：某个**热点 key 过期**，大量并发请求同时涌入直接打DB。

**解决方案**：
- **互斥锁（SETNX）**：第一个请求去查 DB 并重建缓存，其他请求等待锁释放后从缓存读
- **永不过期**：不给热点 key 设置过期时间，由后台异步或逻辑控制更新
- **热点数据预热**：提前将热点数据加载到缓存

### 缓存雪崩
**问题**：大量 key **同时过期** 或 **Redis 宕机**，导致大量请求同时打到数据库。

**解决方案**：
- **过期时间随机化**：给 key 的 TTL 加随机偏移量（如 `baseTTL + random(0, 600)`），避免集中过期
- **多级缓存**：本地缓存（Guava/Caffeine）+ Redis + DB 多层防护
- **Redis 高可用**：使用哨兵或集群模式，保证 Redis 自身高可用
- **服务降级/限流**：前端限流、后端熔断，保护数据库
- **提前演练**：提前压测，识别风险点

### 总结对比

| 场景 | 核心原因 | 关键词 | 典型解决 |
|------|---------|--------|---------|
| 穿透 | 查询不存在的数据 | 布隆过滤器 / 空值缓存 | 挡在缓存层之前 |
| 击穿 | 热点 key 过期瞬间 | 互斥锁 / 永不过期 | 单点保护 |
| 雪崩 | 大量 key 同时过期或宕机 | 过期随机 / 高可用 | 兜底保护 |

---

> 📚 更多面试题欢迎 Star 本仓库并持续关注更新！
> 
> 来源整理：本文件由 AI 助手基于 Redis 官方文档及业界经典面试题整理，参考来源包括 Redis 官方文档、CSDN、掘金等社区技术文章。
