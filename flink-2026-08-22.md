# Flink 面试题 — 2026-08-22

## 题目一：Flink 与 Spark Streaming 的核心区别

### 📌 问题
Apache Flink 和 Spark Streaming 都是常用的大数据流处理框架，请从架构模型、处理语义、时间机制、窗口支持、状态管理等多个维度详细对比两者的核心差异，并说明各自的优势场景。

### ✅ 答案

**核心架构对比：**

| 维度 | Spark Streaming | Flink |
|------|----------------|-------|
| **处理模型** | 微批处理（Micro-Batch） | 流原生（Native Stream） |
| **架构理念** | 批处理思想延伸，将流拆成小批量 | 流优先，批是流的一种特例 |
| **延迟** | 最低百毫秒级 | 毫秒级（Event-Driven） |
| **吞吐** | 高（批量优化） | 高（Pipeline 并行） |
| **背压机制** | Receiver → BlockGenerator → RateController | TaskManager 之间直接 TCP 反压 |

**处理语义对比：**

| 语义 | Spark Streaming | Flink |
|------|----------------|-------|
| **At-Least-Once** | ✅ 支持 | ✅ 支持 |
| **Exactly-Once** | ✅ 支持（checkpoint） | ✅ 支持（分布式快照） |
| **精确一次端到端** | 需要外部系统配合（如 Write-Ahead Log） | 通过 TwoPhaseCommitSink 天然支持 |

**时间机制对比：**

| 时间 | Spark Streaming | Flink |
|------|----------------|-------|
| **Event Time** | 支持（1.6+），需手动开启 Watermark | 原生支持，Watermark 完善 |
| **Processing Time** | 默认 | 支持 |
| **Ingestion Time** | 不支持 | 支持 |
| **乱序处理** | 有限支持 | Watermark + allowedLateness 完善方案 |

**窗口支持对比：**

| 窗口类型 | Spark Streaming | Flink |
|----------|----------------|-------|
| **滚动窗口** | ✅ 支持 | ✅ 支持 |
| **滑动窗口** | ✅ 支持 | ✅ 支持 |
| **会话窗口** | ❌ 不支持（只能用 updateStateByKey 模拟） | ✅ 原生支持会话窗口 |
| **全局窗口** | ❌ | ✅ 支持 |
| **增量窗口** | ✅ reduceByKeyAndWindow（逆运算优化） | ✅ AggregateFunction |

**状态管理对比：**

| 状态 | Spark Streaming | Flink |
|------|----------------|-------|
| **状态后端** | Checkpoint 到 HDFS（状态粗粒度） | HashMap / RocksDB 多选择 |
| **KeyedState** | updateStateByKey / mapWithState | 完整 keyedState 体系 |
| **OperatorState** | 有限（Kafka Direct API） | 完整支持（ListState / UnionState） |
| **增量 Checkpoint** | 支持 | 支持（RocksDB 增量） |

**各自优势场景：**

**Spark Streaming 优势场景：**
- 已有 Spark 生态（如 Spark SQL、MLlib）的团队，复用资源
- 批流一体场景（离线和实时用同一套框架）
- 吞吐量优先、延迟要求在 500ms+ 可接受
- 团队熟悉 Spark，生态工具链成熟（Spark UI 完善）

**Flink 优势场景：**
- 低延迟需求（毫秒级实时分析）
- 复杂事件处理（CEP）、实时风控、实时大屏
- 无界流上有精确事件时间窗口需求
- 超大规模状态管理（TB 级状态）
- 需要 exactly-once 且端到端简单实现
- 动态缩容/扩容、任意时刻 Savepoint

**背压（Back Pressure）处理：**

Spark Streaming 的背压：Receiver → BlockGenerator（令牌桶限速）→ RateController 动态调整接收速率，延迟秒级响应。

Flink 的背压：TaskManager 之间通过 TCP 握手直接传递背压信号，Source 立即降低生产速率，毫秒级响应，更精确。

**来源**：https://spark.apache.org/docs/latest/streaming-programming-guide.html
**来源**：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/overview/

---

## 题目二：Flink JobManager HA 高可用配置与故障恢复

### 📌 问题
Flink 集群的 JobManager 是单点瓶颈，如何配置高可用（HA）方案？在 JobManager 故障后，Flink 如何实现快速故障恢复？请详细说明基于 Zookeeper 和基于 Kubernetes 的 HA 配置方式。

### ✅ 答案

**为什么需要 JobManager HA：**

JobManager 负责作业调度、Checkpoint 协调、资源管理。一旦 JobManager 宕机：
- 所有运行中的 Job 失败
- 无法提交新 Job
- 正在进行的 Checkpoint 信息丢失

因此生产环境必须配置 HA。

**基于 Zookeeper 的 HA 配置：**

Flink 使用 Zookeeper 进行 Leader 选举和元数据存储，配置方式如下：

```yaml
# flink-conf.yaml
high-availability: zookeeper
high-availability.storageDir: hdfs:///flink/ha/
high-availability.zookeeper.quorum: zk1:2181,zk2:2181,zk3:2181
high-availability.zookeeper.path.root: /flink
high-availability.cluster-id: /flink-cluster-1  # 区分多个集群
```

**Zookeeper HA 工作原理：**

1. **Leader 选举**：所有 JobManager 启动后向 Zookeeper 注册，Zookeeper 选举出一个 Leader（Live），其他为 Standby
2. **元数据存储**：JobGraph、Checkpoint 元数据、UserCode 类等存储在 HDFS/NFS，Zookeeper 只存储指向这些数据的指针
3. **故障检测**：Zookeeper 通过心跳监控 JobManager 存活，Leader 宕落后触发重新选举
4. **故障恢复**：新 Leader 从 HDFS 读取 JobGraph 和 Checkpoint 元数据，恢复所有运行中的 Job

**Kubernetes HA 配置（K8s）：**

```yaml
# 高可用模式下，JobManager 作为 Kubernetes Deployment 部署
env:
  - name: HIGH_AVAILABILITY
    value: kubernetes
  - name: HIGH_AVAILABILITY_STORAGE_DIR
    value: flink-ha-data
```

Kubernetes HA 使用 Kubernetes ConfigMap + Leader Election（Lease 资源）实现，不需要 Zookeeper：

1. **Leader Election**：K8s LeaseLock 确保只有一个 JobManager 为 Leader
2. **元数据存储**：存储在 Kubernetes ConfigMap 或持久化存储（如 NFS、Ceph）
3. **Pod 故障恢复**：K8s 自动重启 JobManager Pod，新 Pod 获取 Lease 后恢复状态

**独立模式（Standalone）HA：**

```yaml
high-availability: standalone
high-availability.storageDir: hdfs:///flink/ha/
```
适用于测试环境，仅支持单 JobManager 故障后从共享存储恢复。

**TaskManager 容错：**

TaskManager 故障不影响 Job 整体：JobManager 将失败 Task 调度到其他 TaskManager Slot 重新执行，状态从最近 Checkpoint 恢复。

**故障恢复流程详解：**

1. **故障检测**：TaskManager 向 JobManager 发送心跳超时 → JobManager 标记该 TM 失效
2. **Task 重调度**：JobManager 将失败 Task 调度到其他可用 Slot
3. **状态恢复**：Task 从 Checkpoint 元数据读取状态，放置到 State Backend（RocksDB/HashMap）
4. **数据源重消费**：Source 算子从 Checkpoint 记录的 offset 重新消费（如 Kafka 重新 seek 到保存的 offset）
5. **增量恢复**：Flink 1.13+ 支持增量 Checkpoint，只恢复变更的 RocksDB SST 文件

**恢复策略配置：**

```java
// 设置重启策略
env.setRestartStrategy(RestartStrategies.failureRateRestart(
    3,                      // 最大失败次数
    Time.of(10, TimeUnit.MINUTES),  // 时间窗口
    Time.of(10, TimeUnit.SECONDS)   // 每次重启间隔
));

// 或者固定延迟重启
env.setRestartStrategy(RestartStrategies.fixedDelayRestart(
    3,                      // 重试次数
    Time.of(30, TimeUnit.SECONDS)  // 重试间隔
));
```

**HA 配置最佳实践：**

- **生产环境**：推荐 Kubernetes HA 或 Zookeeper HA
- **Checkpoints 必须开启**：没有 Checkpoint，HA 无法恢复状态
- **检查点存储**：生产环境推荐 HDFS/S3/OSS 等分布式存储
- **TM 多租户**：避免单个 TM 宕机导致大量 Task 同时失效，分配到不同机器/机架
- **监控**：配置 JobManager 存活监控 + Zookeeper/Kubernetes 事件告警

**来源**：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/deployment/ha/zookeeper_ha/

---

## 题目三：Flink 数据交换策略与 Task 链合并机制

### 📌 问题
Flink 在运行时如何管理 Task 之间的数据交换（Data Exchange）？什么是 Task 链（Task Chaining）？在什么条件下会触发链合并？请详细说明 Flink 的物理执行图转换过程以及数据交换的Shuffle 类型。

### ✅ 答案

**Flink 执行图的转换过程：**

用户编写的 StreamGraph（逻辑 DAG）到物理执行图，经历三层转换：

```
StreamGraph（用户代码）
    ↓ JobGraphGenerator（用户代码 DAG）
JobGraph（提交给集群的 Job）
    ↓ ExecutionGraphBuilder（JobManager 转换）
ExecutionGraph（可调度的物理 DAG）
    ↓ SchedulerNG（调度到 TaskManager）
物理执行图（Task 实例在 TM 上运行）
```

**StreamGraph → JobGraph：**
- 将算子链化（Chain）减少数据传输开销
- 算子间不跨 TaskManager 时优先合并为同一 Task
- 嵌入用户代码的序列化（Class 注入到 JobGraph）

**JobGraph → ExecutionGraph：**
- 按并行度展开所有算子子任务（SubTask）
- 建立上下游 SubTask 之间的连接关系
- 构建中间结果（IntermediateResult）和分区（Partition）

**Task 链合并（Task Chaining）：**

Task 链是指将多个相邻算子合并到同一个 Task 中执行，减少线程切换和网络传输。

**链合并条件：**

| 条件 | 说明 |
|------|------|
| **上下游算子并行度相同** | chain 上下游算子并行度必须一致 |
| **无 Rebalance/Rescale/Union 等 shuffle** | shuffle 会打断链 |
| **未禁用链** | `disableChaining()` / `env.disableOperatorChaining()` |
| **算子间无数据分区策略变更** | 如 keyBy 会打断链 |
| **算子足够简单** | UDF 不能有外部状态或复杂异步操作 |

**链合并示例：**

```java
env.socketTextStream("localhost", 9999)
    .flatMap(...)          // 算子1
    .filter(...)           // 算子2
    .keyBy(...)            // shuffle，打断链
    .map(...)              // 算子3，keyBy 之后，新链开始
    .print();              // 算子4，可链合并
```

上面例子中 flatMap + filter 可链，map + print 可链，keyBy 中间的 shuffle 打断链。

**手动控制链：**

```java
// 强制链合并
stream.filter(...).map(...).startNewChain().keyBy(...);

// 禁用某个算子的链
stream.map(...).disableChaining();

// 全局禁用链
env.disableOperatorChaining();
```

**数据交换 Shuffle 类型：**

| Shuffle 类型 | 说明 | 使用场景 |
|-------------|------|----------|
| **Forward** | 上游 1 个分区 → 下游 1 个分区（同 Slot 内或同 mach） | chain 内数据传输，无网络开销 |
| **Hash Partition** | 按 Key hash 分区 | keyBy / groupBy |
| **ReBalance** | 轮询分发到所有下游分区 | 解决数据倾斜 |
| **Broadcast** | 数据复制到所有下游分区 | 维表Join、配置下发 |
| **Rescale** | 按比例分发（上游分区数/n = 下游分区数） | 局部 shuffle，省网络 |
| **Custom** | 用户自定义分区策略 | 特殊业务需求 |

**网络传输与序列化：**

- Flink 使用自定义的 `Pipelined Data Exchange`（流水线数据交换）
- 数据通过 Netty 网络传输，使用 NIO + Zero-Copy 优化
- 算子间数据以 StreamRecord 为单位，每个 Record 包含序列化后的字节数据
- TypeSerializer 序列化类型信息，Flink 支持 Kryo/Java/Avro 等序列化框架
- **对象重用模式**：`env.getConfig().enableObjectReuse()` 可减少 GC，但需保证 UDF 不修改输入对象

**Pipelined vs Blocking 交换：**

- **Pipelined**（流式）：数据一旦产生即向下游传递，无需等待全部完成。延迟低，但上游失败会导致下游部分输出无效。
- **Blocking**（批式/Hash shuffle）：等待上游全部完成后再 Shuffle。适合大数据量批处理，但延迟高。

Flink 流处理默认使用 Pipelined 交换。

**来源**：https://nightlies.apache.org/fllink/flink-docs-release-1.18/docs/internals/stream_checkpoint/

---

## 题目四：Flink CEP（复杂事件处理）详解

### 📌 问题
请详细解释 Flink CEP（Complex Event Processing）的使用场景和核心概念。什么是模式（Pattern）API？如何定义单例模式、量词模式、条件模式？请结合实际案例说明如何用 Flink CEP 实现实时欺诈检测。

### ✅ 答案

**CEP 核心概念：**

Flink CEP 是 Flink 提供的复杂事件处理库，用于在无界流中识别符合特定规则的事件序列。

**典型应用场景：**

- **实时风控/欺诈检测**：用户短时间内连续失败登录 → 账户冻结
- **异常行为检测**：服务器 CPU/内存异常飙升模式
- **用户行为分析**：用户浏览 → 加购 → 下单的转化漏斗
- **IoT 异常告警**：传感器数据异常模式识别
- **金融交易监控**：大额快进快出、异常高频交易

**Pattern API 核心概念：**

Pattern 是事件序列的匹配规则，由多个模式单元组成：

```java
Pattern<T, T> pattern = Pattern.<T>begin("start")   // 第一个匹配事件
    .where(...)      // 条件1
    .next("middle")  // 第二个事件（严格连续）
    .where(...)      // 条件2
    .quantifier()    // 量词
    .within(Time.seconds(10)); // 时间窗口
```

**单例模式（Singleton）：**

每个事件只匹配一次：

```java
Pattern.<LoginEvent>begin("first-login")
    .where(e -> e.getFailedAttempts() >= 3);
```

**量词模式（Quantifier）：**

| 量词 | 说明 | 示例 |
|------|------|------|
| `.oneOrMore()` | 事件出现 1 次或多次 | 连续 3 次失败登录 |
| `.times(n)` | 事件精确出现 n 次 | 恰好 2 次 |
| `.times(n, m)` | 事件出现 n 到 m 次 | 2 到 5 次 |
| `.greedy()` | 贪婪匹配（尽可能多） | 连续失败越多越危险 |
| `.optional()` | 可选（匹配 0 次也算成功） | |

```java
// 连续 1-5 次失败后成功登录
Pattern.<LoginEvent>begin("failures")
    .where(e -> e.isFailed())
    .oneOrMore()
    .quantifier().times(1, 5)
    .next("success")
    .where(e -> e.isSuccess());
```

**条件模式（Conditions）：**

- **简单条件**：`.where(clause)` — 基于事件属性
- **迭代条件**：`.iterativeCondition(clause)` — 基于已匹配事件序列
- **组合条件**：`.or()` / `.and()`

```java
// 迭代条件：当前登录失败次数超过之前所有失败次数的总和（异常行为）
Pattern.<LoginEvent>begin("first")
    .where(e -> e.isFailed())
    .next("second")
    .where(e -> e.isFailed())
    .iterativeCondition(
        ctx -> ctx.getEventsForPattern("first").stream()
            .mapToInt(LoginEvent::getFailedAttempts)
            .sum() < ctx.getEvent().getFailedAttempts()
    );
```

**近邻模式（Contiguity）：**

| 模式 | 说明 |
|------|------|
| `.next()` | 严格连续，中间无其他事件 |
| `.followedBy()` | 非严格连续，中间可以有其他事件 |
| `.followedByAny()` | 允许忽略已匹配事件 |
| `.notNext()` | 严格连续地不希望匹配 |
| `.notFollowedBy()` | 非严格不希望匹配 |

```java
// 快速连续登录（非严格，1分钟内）
Pattern.<LoginEvent>begin("first")
    .followedBy("second")
    .within(Time.minutes(1));
```

**实时欺诈检测案例：**

**场景**：某金融 APP，用户登录后 5 分钟内，若同一设备触发了 3 次以上不同密码的尝试，且最后一次成功登录的 IP 与前两次不在同一地区，则触发风控告警。

```java
// 1. 定义输入事件类型
public class SecurityEvent {
    private String userId;
    private String deviceId;
    private String ip;
    private String region;
    private String eventType; // LOGIN_FAIL, LOGIN_SUCCESS
    private long eventTime;
}

// 2. 定义欺诈检测 Pattern
Pattern<SecurityEvent, SecurityEvent> fraudPattern = Pattern
    .<SecurityEvent>begin("firstFail")  // 第1次失败
    .where(e -> "LOGIN_FAIL".equals(e.getEventType()))
    .next("secondFail")                  // 第2次失败（严格连续）
    .where(e -> "LOGIN_FAIL".equals(e.getEventType()))
    .next("thirdFail")                   // 第3次失败（严格连续）
    .where(e -> "LOGIN_FAIL".equals(e.getEventType()))
    .next("successLogin")               // 最后成功登录
    .where(e -> "LOGIN_SUCCESS".equals(e.getEventType()))
    .within(Time.minutes(5));           // 5分钟内

// 3. 应用 Pattern
PatternStream<SecurityEvent> patternStream = CEP.pattern(
    securityEventStream.keyBy(SecurityEvent::getUserId),
    fraudPattern);

// 4. 提取匹配结果并告警
DataStream<Alert> alerts = patternStream.select(
    "firstFail", "secondFail", "thirdFail", "successLogin",
    (Map<String, SecurityEvent> events) -> {
        SecurityEvent first = events.get("firstFail");
        SecurityEvent success = events.get("successLogin");
        
        // 检查 IP 地区异常
        if (!isSameRegion(first.getRegion(), success.getRegion())) {
            return new Alert(
                success.getUserId(),
                "Fraud detected: 5min内连续3次失败后异地登录",
                events.values().stream()
                    .map(SecurityEvent::toString)
                    .collect(Collectors.joining("; "))
            );
        }
        return null; // 非欺诈，忽略
    }
).filter(Objects::nonNull);
```

**超时事件处理（Pattern 超时）：**

```java
// 匹配超时未完成的事件
OutputTag<TimeoutEvent> timeoutTag = new OutputTag<TimeoutEvent>("timeout"){};

PatternStream<TradeEvent> patternStream = CEP.pattern(
    tradeStream.keyBy(TradeEvent::getUserId),
    tradePattern.within(Time.minutes(10));

// 处理超时
DataStream<TimeoutEvent> timeouts = patternStream.select(
    timeoutTag,
    (Map<String, TradeEvent> events, Timeout timeout) ->
        new TimeoutEvent(timeout.getPatternName(), events.values())
);
```

**Flink CEP 原理：**

Flink CEP 底层使用 **NFA（非确定性有限自动机）** 实现：
- 状态机状态包括：初始状态、中间状态、最终状态
- 每个状态有对应的匹配条件（Condition）
- 匹配过程维护一个 Map<String, List<T>> 记录每个模式名匹配的事件
- 使用 WaterMark 机制处理乱序，超时由 TimerService 管理

**来源**：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/libs/cep/

---

## 题目五：Flink SQL 实时计算与时间语义配置

### 📌 问题
Flink SQL 是 Flink 1.13+ 主推的统一批流处理接口。请详细说明 Flink SQL 的表环境（TableEnvironment）创建、动态表与持续查询概念、如何配置事件时间（Event Time）和处理时间（Processing Time）语义，以及 Flink SQL 与 DataStream API 的混合编程方式。

### ✅ 答案

**TableEnvironment 创建与版本差异：**

Flink 1.15+ 使用全新的 Table API：

```java
// 1.15+ 统一 API（推荐）
EnvironmentSettings settings = EnvironmentSettings.newInstance()
    .inStreamingMode()   // 流模式
    // .inBatchMode()    // 批模式
    .build();

TableEnvironment tEnv = TableEnvironment.create(settings);

// 旧版 1.13-1.14 Blink Planner
TableEnvironment tEnv = TableEnvironment.create(
    EnvironmentSettings.newInstance().useBlinkPlanner().inStreamingMode().build());
```

**动态表（Dynamic Tables）与持续查询（Continuous Query）：**

动态表是 Flink SQL 对流数据的抽象——表 schema 定义不变，但数据持续追加/更新。

```
DataStream (流动的数据)  ←→  Dynamic Table (关系语义)  ←→  DataStream (输出)
```

**持续查询流程**：
1. 输入流 → 持续追加到动态表（Append Mode）
2. 对动态表执行 SQL 查询
3. 查询结果持续输出到下游动态表
4. 下游可输出为 Append Mode（只追加）或 Retract Mode（撤回更新）

```java
// 示例：从 Kafka 读取订单流，按产品聚合统计金额
Table orders = tEnv.from("kafka_orders"); // 从 Kafka 表读取

Table result = tEnv.sqlQuery(
    "SELECT product, SUM(amount) AS total_amount, TUMBLE_END(event_time, INTERVAL '1' MINUTE) AS window_end "
    + "FROM orders "
    + "WHERE status = 'PAID' "
    + "GROUP BY product, TUMBLE(event_time, INTERVAL '1' MINUTE)"
);

// 输出到 MySQL（需要 JDBC Connector）
tEnv.executeSql(
    "CREATE TABLE mysql_sink_products ("
    + "  product VARCHAR, "
    + "  total_amount DECIMAL(18,2), "
    + "  window_end TIMESTAMP(3), "
    + "  PRIMARY KEY (product, window_end) NOT ENFORCED"
    + ") WITH ('connector' = 'jdbc', "
    + "        'url' = 'jdbc:mysql://localhost:3306/orders', "
    + "        'table-name' = 'products_report')"
);

result.executeInsert("mysql_sink_products");
```

**时间语义配置（Event Time vs Processing Time）：**

**Event Time 配置（推荐）：**

```java
// 方式1：DataStream 转型时指定
DataStream<Tuple2<String, Long>> stream = ...
Table table = tEnv.fromDataStream(
    stream,
    $("user"), 
    $("event_time").rowtime()  // 指定为 Event Time
);

// 方式2：DDL 定义（推荐，声明式）
tEnv.executeSql(
    "CREATE TABLE orders ("
    + "  order_id STRING, "
    + "  user_id STRING, "
    + "  amount DECIMAL(18,2), "
    + "  event_time TIMESTAMP(3), "
    + "  WATERMARK FOR event_time AS event_time - INTERVAL '5' SECOND"  // 5秒乱序容忍
    + ") WITH ('connector' = 'kafka', ...)"
);
```

**Processing Time 配置：**

```java
// DDL 定义
tEnv.executeSql(
    "CREATE TABLE orders ("
    + "  order_id STRING, "
    + "  user_id STRING, "
    + "  amount DECIMAL(18,2), "
    + "  proc_time AS PROCTIME()  -- 处理时间系统列
    + ") WITH ('connector' = 'kafka', ...)"
);

// 或者 DataStream 转型
Table table = tEnv.fromDataStream(
    stream,
    $("user"),
    $("proc_time").proctime()
);
```

**Watermark 在 Flink SQL 中的使用：**

```java
// WATERMARK 语法
WATERMARK FOR <rowtime_column> AS <rowtime_column> - <offset>

// 完整示例
CREATE TABLE sensor_readings (
    sensor_id STRING,
    temperature DOUBLE,
    event_time TIMESTAMP(3),
    WATERMARK FOR event_time AS event_time - INTERVAL '3' SECOND
);
```

**Flink SQL 窗口函数：**

| 窗口类型 | 语法 | 说明 |
|----------|------|------|
| **滚动窗口** | `TUMBLE(event_time, INTERVAL '5' MINUTE)` | 固定大小，不重叠 |
| **滑动窗口** | `HOP(event_time, INTERVAL '5' MINUTE, INTERVAL '1' MINUTE)` | 固定大小，滑动步长 |
| **会话窗口** | `SESSION(event_time, INTERVAL '5' MINUTE)` | 按 gap 分隔 |
| **累计窗口** | `CUMULATE(event_time, INTERVAL '5' MINUTE, INTERVAL '1' MINUTE)` | 从起点累计 |

```java
// 示例：每5分钟统计一次，过去1分钟滑动平均
Table result = tEnv.sqlQuery(
    "SELECT sensor_id, "
    + "  AVG(temperature) AS avg_temp, "
    + "  HOP_END(event_time, INTERVAL '1' MINUTE, INTERVAL '5' MINUTE) AS window_end "
    + "FROM sensor_readings "
    + "GROUP BY sensor_id, HOP(event_time, INTERVAL '1' MINUTE, INTERVAL '5' MINUTE)"
);
```

**Flink SQL 与 DataStream 混合编程：**

```java
// 场景：Flink SQL 处理聚合，但需要 DataStream 做复杂 CEP

// Step 1: 基础表通过 SQL 预处理
Table aggTable = tEnv.sqlQuery(
    "SELECT user_id, SUM(amount) AS total, TUMBLE_END(event_time, INTERVAL '5' MINUTE) AS window_end "
    + "FROM orders GROUP BY user_id, TUMBLE(event_time, INTERVAL '5' MINUTE)"
);

// Step 2: 转换为 DataStream 继续处理（Retract Mode 获取更新）
DataStream<Tuple2<Boolean, Row>> retractStream = 
    tEnv.toRetractStream(aggTable, Row.class);

// Boolean=true 表示最新结果，false 表示撤回旧结果
DataStream<Alert> alerts = retractStream.filter(t -> t.f0)
    .map(t -> t.f1)
    .keyBy(r -> r.getField(0))
    .process(new ComplexAlertProcessFunction());

// Step 3: 注册新表继续 SQL 处理
tEnv.createTemporaryView("user_alerts", alerts, Schema.newBuilder()
    .field("user_id", DataTypes.STRING())
    .field("total", DataTypes.DECIMAL(18,2()))
    .field("window_end", DataTypes.TIMESTAMP(3))
    .build());

// 继续 SQL 查询
Table finalResult = tEnv.sqlQuery(
    "SELECT * FROM user_alerts WHERE total > 10000"
);
```

**Changelog Compaction（变更日志）支持：**

Flink SQL 支持 Kafka 的 Debezium CDC（Change Data Capture）格式，可以直接 Debezium 格式消费 MySQL binlog：

```java
tEnv.executeSql(
    "CREATE TABLE mysql_orders_cdc ("
    + "  order_id STRING, "
    + "  amount DECIMAL(18,2), "
    + "  status STRING, "
    + "  event_time TIMESTAMP(3), "
    + "  WATERMARK FOR event_time AS event_time - INTERVAL '5' SECOND"
    + ") WITH ("
    + "  'connector' = 'kafka', "
    + "  'topic' = 'mysql.orders', "
    + "  'format' = 'debezium-json', "  // CDC 格式
    + "  'debezium-json.schema-include' = 'true'"
    + ")"
);
```

**性能优化建议：**

- **MiniBatch**：开启微批处理减少状态访问频率：`table.exec.mini-batch.enabled=true`
- **状态 TTL**：聚合状态设置 TTL 防止状态膨胀
- **分区裁剪**：善用 `partitioned` hint 减少 shuffle
- **Filter 下推**：WHERE 条件尽可能在 Source 端过滤

**来源**：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/dev/table/overview/

---

*📅 本文件由定时任务自动生成 | 2026-08-22*
