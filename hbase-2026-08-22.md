# HBase 面试题精选（2026-08-22）

> 本文件收录 5 道高质量 HBase 面试题，涵盖架构、存储结构、RowKey 设计、协处理器等核心知识点。

---

## 题目一：HBase 的整体架构是怎样的？请描述各组件的作用。

### 参考答案

HBase 是一个基于 Google BigTable 设计的分布式列族存储系统，整体架构分为以下几层：

**1. ZooKeeper 协调层**
- 负责 HBase 集群的协调管理，存储 `META` 表位置、根目录等元信息。
- 通过 Raft 协议实现高可用，ZooKeeper 检测 RegionServer 宕机并触发恢复流程。

**2. HMaster（主节点）**
- 负责表的 Schema 管理（创建、删除、修改列族等）。
- 调度 Region 分配：将 Region 分配给 RegionServer，支持负载均衡和故障恢复。
- 监控 RegionServer 状态，检测到宕机时重新分配其上的 Region。
- 在 HBase 2.x 中，HMaster 还负责分配系统表的 Region。

**3. RegionServer（数据节点）**
- 负责处理读写请求，是实际数据存储和查询的执行节点。
- 每个 RegionServer 管理多个 Region，每个 Region 对应一个表的连续行区间。
- 包含以下核心组件：
  - **MemStore**：写缓冲区，按列族组织，先写入内存并排序，刷新后生成 HFile。
  - **HFile**：HDFS 上的数据存储文件格式，Immutable 的 LSM-Tree 结构。
  - **BlockCache**：读缓存热点数据，使用 LRU 策略。
  - **WAL（Write-Ahead Log）**：预写日志，保证写入持久性，先写 WAL 再写 MemStore。

**4. HDFS（底层存储）**
- HBase 并不自己管理数据副本，而是依赖 HDFS 实现数据冗余（默认 3 副本）。
- HFile 直接存储在 HDFS 上，HBase 利用 HDFS 的块平衡和恢复机制。

```
Client → ZooKeeper → HMaster/HRegionServer → HDFS
```

> **面试加分点**：可以对比 HBase 和 HDFS 的关系——HDFS 擅长顺序大文件读写，HBase 在其上实现随机读写能力。

---

## 题目二：HBase 的 RowKey 设计有哪些最佳实践？为什么 RowKey 设计如此重要？

### 参考答案

RowKey 是 HBase 数据访问的核心，设计的优劣直接影响读写性能和集群稳定性。

**RowKey 设计原则：**

**1. 长度原则**
- RowKey 越短越好（建议 ≤ 16 字节，最长 64KB）。
- 原因：HBase 的 KeyValue 对象中 RowKey 会被反复存储和传输，过长的 RowKey 会浪费大量存储空间和内存。

**2. 散列原则**
- 避免热点 Region（Region 是按 RowKey 字典序分片的）。
- 如果写入的 RowKey 有明显的时间顺序特征（如时间戳），会导致所有新数据集中在同一个 Region，形成写热点。
- 解决方案：
  - 在 RowKey 前加盐（Salting）：在 RowKey 头部加随机前缀，使数据分散到多个 Region。
  - 哈希打散：用 MD5 或其他哈希算法取前几位作为前缀。
  - 反转（Reverse）：反转时间戳等有规律的部分，打破单调性。

**3. 查询多样性**
- 支持多种查询模式：点查（Get）、范围扫描（Scan）、顺序扫描。
- 常见的 RowKey 设计：
  - **用户中心型**：`userId + action + timestamp`，适合用户行为分析场景。
  - **消息系统型**：`receiverId + timestamp`，查询某用户的全部消息。
  - **物化视图型**：将需要聚合统计的维度编码进 RowKey，支持快速 Scan。

**4. 唯一性原则**
- RowKey 必须全局唯一，重复 RowKey 的 Put 操作会被当作同一行数据更新（而非插入）。

**热点场景示例：**
```
# 错误设计（写入热点）
RowKey = userId + timestamp（所有新用户集中在末尾Region）

# 优化设计（散列打散）
RowKey = MD5(userId).substring(0,4) + userId + timestamp
```

> **面试加分点**：提到热点监控方法——HBase Web UI 中的 Region 负载分布，以及 `split` 手动拆分的触发条件。

---

## 题目三：简述 HBase 的 MemStore 刷新机制和 HFile 的合并流程。

### 参考答案

**MemStore 刷新机制（Flush）**

MemStore 是 RegionServer 内存中的写缓冲区，按列族组织。当写请求到达时：

1. 数据先写入 **WAL（预写日志）**，确保宕机可恢复。
2. 数据写入 MemStore，按 RowKey + Column + Version 排序。
3. 当满足以下任一条件时，MemStore 会刷新（Flush）为 HFile：
   - MemStore 大小达到 `hbase.hregion.memstore.flush.size`（默认 128MB）。
   - RegionServer 整体 MemStore 大小超过 `hbase.regionserver.global.memstore.size.lower.limit`（默认堆内存 40%）。
   - WAL 文件数量超过 `hbase.regionserver.max.logs`（默认 32），强制刷新以回收 WAL。
   - 定时刷新：HBase 后台线程定期检查，最长等待 `hbase.regionserver.flush.interval`（默认 1 小时）。

**HFile 合并流程（Compaction）**

LSM-Tree（Log-Structured Merge-Tree）结构的核心优化机制：

**Minor Compaction（小合并）**：
- 将多个较小的 HFile 合并为少量较大的 HFile。
- 触发条件：HFile 数量超过 `hbase.hstore.compaction.min`（默认 3）。
- 只合并同一列族下的 HFile，不清理已删除和过期数据。

**Major Compaction（大合并）**：
- 将一个列族下所有 HFile 合并为一个。
- 触发条件：手动触发或配置 `hbase.hregion.majorcompaction`（默认 7 天周期）。
- 会清理已删除的单元格（墓碑标记）和超过版本数的数据。

**HFile 读取流程**：
- 先查 BlockCache（读缓存），未命中则从 HFile 加载到 BlockCache。
- 多版本时按时间戳倒序返回最新数据。

> **面试加分点**：LSM-Tree 的核心思想——用写放大（Write Amplification）换取读性能的提升。解释 `flush` 和 `compact` 的权衡关系。

---

## 题目四：什么是 HBase 协处理器（Coprocessor）？它有哪两种类型？分别用于什么场景？

### 参考答案

HBase 协处理器是运行在 RegionServer 上的扩展机制，允许用户将自定义逻辑嵌入到 HBase 的数据处理流程中，无需修改 HBase 源码。

**两种协处理器类型：**

**1. Observer Coprocessor（观察者型）**

原理：类似数据库触发器，在特定事件前后插入自定义钩子。

常见钩子：
- `RegionObserver`：行级操作前后，如 `preGet`、`postGet`、`prePut`、`postPut`、`preDelete`、`postDelete`。
- `MasterObserver`：DDL 操作前后，如 `preCreateTable`、`postCreateTable`、`preModifyTable`。
- `WALObserver`：WAL 写入前后。

使用场景：
- **二级索引**：在 `postPut` 时将索引数据写入另一张索引表，实现近似实时的二级索引。
- **数据验证**：在 `prePut` 中校验数据合法性，拒绝不合规数据写入。
- **审计日志**：记录所有写操作到独立日志表。
- **权限控制**：在 `preGet`/`prePut` 中实现列级或列族级权限检查。

示例（RegionObserver 伪代码）：
```java
@Override
public void postPut(final ObserverContext<RegionCoprocessorEnvironment> e,
                    final Put put, final WALEdit edit, final Durability durability) {
    // 在写入后触发自定义逻辑，如更新二级索引
    updateSecondaryIndex(put);
}
```

**2. Endpoint Coprocessor（端点型）**

原理：类似数据库存储过程，将计算逻辑下沉到数据所在节点执行。

使用场景：
- **聚合计算**：在每个 Region 并行计算 Sum/Count/Max/Min，最后在 HMaster 端汇总，避免全表扫描数据传输。
- **自定义过滤**：在 RegionServer 端过滤数据，只返回符合条件的结果。
- **批量计算**：实现自定义的批量处理逻辑。

调用方式：使用 `CoprocessorRpcChannel` 通过 RPC 调用，HBase 2.x 推荐使用 `AsyncCoprocessorService`。

**协处理器的局限性**：
- 协处理器崩溃会连带 RegionServer 崩溃，需要做好异常捕获。
- 不支持分布式执行，所有数据处理在单个节点完成（Endpoint 除外）。
- HBase 2.x 移除了部分早期 API，需要使用新的 Endpoint API。

> **面试加分点**：协处理器 vs. 触发器的对比，以及 HBase 2.x 中 Coprocessor 的改进（Phoenix 对协处理器的封装使用）。

---

## 题目五：HBase 的数据一致性模型是什么？如何保证读写一致性？

### 参考答案

**HBase 的一致性模型：行级强一致性**

- 在同一个 Region 内，HBase 提供的是行级别的强一致性保证。
- 对同一行的读写操作是严格串行的，不存在脏读。

**读写流程与一致性保证：**

**写入一致性**：
1. 客户端将数据写入目标 RegionServer 的 MemStore。
2. 数据同时写入 WAL（预写日志），保证宕机可恢复。
3. MemStore 刷新为 HFile 后，数据对读可见。

> **关键点**：HBase 不保证跨行或跨表的强一致性，因为不同 Region 位于不同 RegionServer，Region 之间的操作是相互独立的。

**读取一致性**：
- HBase 默认是"最终一致性"读取，但通过以下机制实现强一致性读取：
  - **Flush 和 Compact 的屏障作用**：MemStore 刷新为 HFile 后，之前写入的数据才对读请求可见。
  - **MVCC（Multi-Version Concurrency Control）**：HBase 在读请求时通过 MVCC 确保读取到的一致性快照，避免读到部分写入的数据。
  - **Get vs Scan**：`Get` 是单行读取，保证返回该行最新版本；`Scan` 在开启 `CONSISTENCY=SYNC_STRONG` 时保证所有行都是同一时刻的快照。

**写入后立即读取（Read-your-writes）**：
- 默认情况下，HBase **不保证**写入后立即能读到（因为写入 MemStore 后还未 Flush，数据可能还在内存中）。
- 解决方案：
  1. 写入时使用 `setDurability(Durability.SYNC_WAL)` + 手动 `flush`：
     ```java
     put.setDurability(Durability.SYNC_WAL);
     table.put(put);
     table.flush(new Get(put.getRow())); // 强制刷新
     ```
  2. 使用 `org.apache.hadoop.hbase.client.Consistency` 控制读取一致性：
     ```java
     Get get = new Get(row);
     get.setConsistency(Consistency.TIMELINE);
     ```
  3. Region 切分/迁移时锁等待会导致短暂不可读。

**HBase 一致性 vs CAP 定理**：
- HBase 选择了 **CP（一致性+分区容忍性）**，牺牲了部分可用性（Region 宕机时该 Region 短暂不可用）。
- 相比之下，Cassandra 选择了 AP（可用性+分区容忍性），提供最终一致性。

> **面试加分点**：可以结合 ZooKeeper 选举机制说明 HBase 如何在 Master 宕机时保持可用，以及 HBase 与 Redis 缓存一致性策略的对比（Cache-Aside 模式在 HBase + Hive/HDFS 场景的应用）。

---

## 来源参考

1. **HBase 官方文档 - Architecture**：https://hbase.apache.org/book.html#architecture
2. **HBase 官方文档 - Coprocessors**：https://hbase.apache.org/book.html#cp
3. **HBase 官方文档 - Region Server Sizing and Tuning**：https://hbase.apache.org/book.html#ops.sizing
4. **HBase 官方文档 - LSN, SequenceID, and MVCC**：https://hbase.apache.org/book.html#acls_summary
5. **Cloudera HBase Reference Guide - RowKey Design**：https://docs.cloudera.com/runtime/7.2.2/hbase-reference/topics/hbase_rowkey_design.html

---

*本文件由自动化脚本生成，每日更新 | GitHub: qq286158530/interview-questions*
