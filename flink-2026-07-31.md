# Flink 面试题 — 2026年7月31日

> 本文档整理5道高质量Flink面试题，涵盖架构、Watermark、Checkpoint、窗口函数、状态管理等核心知识点。

---

## 题目一：Flink 的架构是怎样的？JobManager 和 TaskManager 分别负责什么？

### 参考答案

Flink 采用 **Master-Slave 架构**，核心组件包括 JobManager、TaskManager 和 ResourceManager（YARN/Kubernetes 等）。

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     Flink 集群                          │
│  ┌─────────────────┐    ┌─────────────────────────┐    │
│  │   JobManager    │    │      TaskManager        │    │
│  │  (Master Node)   │◄──►│    (Worker Node)        │    │
│  │                 │    │  ┌─────┐ ┌─────┐ ┌─────┐ │    │
│  │  - Dispatcher   │    │  │Slot1│ │Slot2│ │Slot3│ │    │
│  │  - ResourceMgr  │    │  └─────┘ └─────┘ └─────┘ │    │
│  │  - JobMaster    │    │  ┌─────┐ ┌─────┐ ┌─────┐ │    │
│  └─────────────────┘    │  │Slot4│ │Slot5│ │Slot6│ │    │
│         │               │  └─────┘ └─────┘ └─────┘ │    │
│         │               └─────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

### JobManager（Master）

负责整个集群的协调和作业管理，包含三个核心组件：

| 组件 | 职责 |
|------|------|
| **Dispatcher** | 提供 REST 接口，接收用户提交的任务，启动 JobMaster |
| **ResourceManager** | 管理 TaskManager 的 Slot资源，与外部框架（YARN/K8s）交互分配资源 |
| **JobMaster** | 负责单个 Job 的执行，包括生成执行图（ExecutionGraph）、调度 Task 到 TaskManager |

### TaskManager（Worker）

负责执行具体的算子任务：

- **Slot**：TaskManager 的资源抽象，每个 Slot 有固定的内存资源（不绑 CPU）
- **并行度**：一个 Task 可以并行多个 Subtask，每个 Subtask 占用一个 Slot
- **数据交换**：Subtask 之间通过数据流进行数据传递

### Slot 与并行度

```java
// 设置作业级别并行度
env.setParallelism(3);

// 设置算子级别并行度
stream.map(new MyMap()).setParallelism(5);
```

**Slot 数量计算**：
- 默认：一个 TaskManager 的 Slot 数 = CPU 核心数
- Slot 共享：同一作业的不同算子可以共享 Slot（前提是 Slot 足够）

### 作业执行流程

1. 用户提交 JAR → Dispatcher 接收
2. Dispatcher 启动 JobMaster
3. JobMaster 生成 **ExecutionGraph**（并行化后的执行图）
4. JobMaster 向 ResourceManager 申请 Slot
5. ResourceManager 向 TaskManager 申请资源，分配 Slot
6. JobMaster 将 Subtask 调度到对应 Slot 执行
7. 数据流动：Source → Transformation → Sink

### 高可用（HA）机制

JobManager 支持 **Leader-Follower** 模式的高可用：
- ZooKeeper/Kubernetes election 选举 Leader
- Leader 故障时，Follower 接管，不影响作业运行

> **来源说明：** 本题参考 Apache Flink 官方文档 https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/flink-architecture/

---

## 题目二：Flink 的 Watermark（水位线）是什么？如何解决乱序和延迟数据问题？

### 参考答案

Watermark 是 Flink 处理**事件时间（Event Time）**乱序和延迟数据的核心机制。

### 什么是 Watermark

Watermark 是一个**时间戳**，本质是一个承诺：

> "所有事件时间小于此 Watermark 的数据都已经到达，之后不会再有早于这个时间的数据。"

```
Event Time
  │
  │  ╭─ 窗口 [10:00, 10:05)
  │  │
  │  ├─ 数据 10:01 ──┼──→ 已触发
  │  ├─ 数据 10:03 ──┼──→ 已触发
  │  ├─ 数据 10:02 ──┼──→ 乱序，到达较晚
  │  │
  │  ├───── Watermark 10:04 ──┼──→ 窗口 [10:00,10:05) 被触发
  │  │
  │  └─ 数据 10:01 ──┼──→ 丢弃（Watermark 已过）
```

### 两种 Watermark 策略

#### 1. 固定延迟策略（Fixed Amount of Lateness）

```java
WatermarkStrategy
    .<MyEvent>forBoundedOutOfOrderness(Duration.ofMinutes(5))
    .withTimestampAssigner((event, timestamp) -> event.getTimestamp());
```

- 允许最多 **5 分钟**的乱序
- 简单粗暴，适用于延迟分布稳定的场景

#### 2. 单调递增策略（Monotonously Increasing Timestamps）

```java
WatermarkStrategy
    .forMonotonousTimestamps()
    .withTimestampAssigner((event, timestamp) -> event.getTimestamp());
```

- 假设时间戳单调递增，永不乱序
- 性能最好，适用于数据严格有序的场景（如埋点数据）

### 窗口触发时机

```
Watermark 到达 → 窗口关闭 → 触发计算
```

对于窗口 `[window_start, window_end)`：
- 当 `watermark_time >= window_end` 时，窗口被触发
- `watermark_time = window_end - 1ms` 时，窗口真正关闭

### 迟到数据处理

即使窗口已触发，Flink 仍提供三种处理策略：

| 策略 | 说明 | 代码 |
|------|------|------|
| **默认（丢弃）** | 迟到数据直接丢弃 | — |
| **allowedLateness** | 允许一定时间内的迟到数据，触发窗口重新计算 | `.allowedLateness(Duration.ofMinutes(2))` |
| **sideOutputLateData** | 将迟到数据输出到侧输出流，供后续处理 | `.sideOutputLateData(lateTag)` |

```java
OutputTag<MyEvent> lateTag = new OutputTag<MyEvent>("late-data"){};

stream.keyBy(...)
      .window(TumblingEventTimeWindows.of(Time.minutes(5)))
      .allowedLateness(Time.minutes(2))
      .sideOutputLateData(lateTag)
      .sum(1);
```

### Watermark 传播机制

Watermark 在算子间传播规则：

- **多输入算子（Union/Co-process）**：取所有输入的最小 Watermark（最慢的那个）
- **多输出算子**：向下游广播当前 Watermark
- **并行度变化**：每个并行实例维护自己的 Watermark

### 常见问题

**1. 没有数据进入，Watermark 不推进**
- 使用 **Idle Timeout** 检测空闲源并推进 Watermark：
```java
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofMinutes(5))
    .withIdleness(Duration.ofMinutes(1));
```

**2. Watermark 设置过大**
- 窗口等待时间长，延迟增加
- 设置过小 → 过多数据被丢弃

> **来源说明：** 本题参考 Apache Flink 官方文档 https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/temporal-domains/

---

## 题目三：Flink 的状态管理机制是什么？Keyed State 和 Operator State 有何区别？

### 参考答案

Flink 的状态管理是其实现精确一次语义和复杂业务逻辑的基础。

### 状态的定义

**状态（State）**：流处理过程中，算子记住之前处理过的信息的机制。

```
状态实例：
┌──────────────────────────────────────────────┐
│  算子实例（Task Subtask）                     │
│  ┌────────────────────────────────────────┐  │
│  │  状态变量（State）                      │  │
│  │  - 计数器：已处理 10000 条              │  │
│  │  - 缓存：最近5分钟的用户Session          │  │
│  │  - 窗口缓冲：当前窗口的聚合结果           │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### 两类状态

#### 1. Keyed State（键控状态）

**必须**在 `keyBy` 之后使用，状态作用于相同 Key 的数据。

| 类型 | 说明 | 示例 |
|------|------|------|
| **ValueState** | 存储单个值 | `State.update(100)` |
| **ListState** | 存储列表 | `State.add(item)` |
| **MapState** | 存储键值对 | `State.put(key, value)` |
| **ReducingState** | 自动聚合的列表 | `State.add(item)` |
| **AggregatingState** | 自定义聚合 | `State.add(item)` |

**使用示例**：

```java
public class CountWindowAverage extends KeyedProcessFunction<String, Long, String> {
    
    private ValueState<Long> countState;
    private ValueState<Long> sumState;
    
    @Override
    public void open(Configuration parameters) {
        countState = getRuntimeContext().getState(
            new ValueStateDescriptor<>("count", Long.class));
        sumState = getRuntimeContext().getState(
            new ValueStateDescriptor<>("sum", Long.class));
    }
    
    @Override
    public void processElement(Long value, Context ctx, Collector<String> out) {
        Long count = countState.value() == null ? 0L : countState.value();
        Long sum = sumState.value() == null ? 0L : sumState.value();
        
        count++;
        sum += value;
        
        countState.update(count);
        sumState.update(sum);
        
        out.collect("Key: " + ctx.getCurrentKey() + ", Avg: " + (sum / count));
    }
}
```

#### 2. Operator State（算子状态）

算子状态与 Key 无关，整个算子实例共享一份状态。

| 类型 | 说明 |
|------|------|
| **ListState** | 状态为列表，各算子实例状态可重新分配 |
| **UnionListState** | 广播状态，所有算子实例收到完整状态 |
| **BroadcastState** | 广播状态，用于动态规则/配置变更 |

**使用示例**：

```java
public class SourceFunctionWithState implements SourceFunction<String> {
    
    private ListState<Long> offsetState;
    private long offset = 0;
    
    @Override
    public void initializeState(FunctionInitializationContext ctx) {
        offsetState = ctx.getOperatorStateStore().getListState(
            new ListStateDescriptor<>("offset", Long.class));
        
        // 从Checkpoint恢复
        for (Long l : offsetState.get()) {
            offset = l;
        }
    }
    
    @Override
    public void snapshotState(FunctionSnapshotContext ctx) {
        offsetState.clear();
        offsetState.add(offset);
    }
}
```

### Keyed State vs Operator State

| 维度 | Keyed State | Operator State |
|------|-------------|----------------|
| **作用域** | 按 Key 分区 | 整个算子实例 |
| **是否需要 keyBy** | 必须 keyBy | 不需要 |
| **状态访问** | 只读当前 Key 的状态 | 读写全局状态 |
| **重新分发** | 按 Key 哈希自动分发 | 需自定义重分发策略 |
| **典型使用** | 聚合计算、CEP 模式匹配 | Source 偏移量、算子本地缓存 |

### 状态 TTL（过期策略）

```java
StateTtlConfig ttlConfig = StateTtlConfig.newBuilder(Time.minutes(10))
    .setUpdateType(StateTtlConfig.UpdateType.OnReadAndWrite)
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired)
    .build();

ValueStateDescriptor<String> descriptor = new ValueStateDescriptor<>(
    "cached-data", String.class);
descriptor.enableTimeToLive(ttlConfig);
```

### 状态后端选择

- **小状态**：HashMapStateBackend（内存，高性能）
- **大状态**：EmbeddedRocksDBStateBackend（支持增量 Checkpoint）

> **来源说明：** 本题参考 Apache Flink 官方文档 https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/stateful-stream-processing/

---

## 题目四：Flink 的 CEP（复杂事件处理）是什么？如何实现订单风控实时检测？

### 参考答案

CEP（Complex Event Processing）是 Flink 用于**模式匹配**的高级功能，适合从有序事件流中检测复杂模式。

### CEP 核心概念

```
事件流 ──→ [模式匹配引擎] ──→ 检测到复合事件 ──→ 触发报警/处理

示例：用户行为异常检测
登录 → 改密 → 支付的正常路径
登录 → 改密 → 改密 → 改密 → 支付  ← 异常模式（可能被盗号）
```

### 模式（Pattern）API

```java
Pattern<Event, Event> pattern = Pattern.<Event>begin("first")
    .where(new SimpleCondition<Event>() {
        @Override
        public boolean filter(Event value) {
            return value.getType().equals("login");
        }
    })
    .next("second")
    .where(new SimpleCondition<Event>() {
        @Override
        public boolean filter(Event value) {
            return value.getType().equals("change_password");
        }
    })
    .within(Time.minutes(5));
```

### 贪婪模式和总量模式

```java
// 贪婪模式（greedy）：匹配尽可能多的事件
Pattern.<Event>begin("start").where(...).oneOrMore().greedy()

// 总和模式（count）：匹配固定数量的事件
Pattern.<Event>begin("start").where(...).times(3)
```

### 订单风控实时检测示例

**场景**：检测以下异常模式
1. **短时间多笔订单**：同一用户 5 分钟内下单超过 5 笔
2. **大额订单**：单笔订单金额超过 10000 元
3. **异地登录后大额下单**：登录地点与下单地点不同，且金额 > 5000

```java
public class OrderRiskDetection {
    
    public static void main(String[] args) throws Exception {
        StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
        env.setParallelism(1);
        
        DataStream<OrderEvent> orderStream = env.addSource(new OrderEventSource());
        
        // 模式1：同一用户5分钟内下单超过5次
        Pattern<OrderEvent, OrderEvent> frequentPattern = Pattern
            .<OrderEvent>begin("order")
            .where(e -> e.getType().equals("create"))
            .times(5, 10)  // 5到10次
            .consecutive()  // 连续发生（无其他事件插入）
            .within(Time.minutes(5));
        
        // 模式2：异地登录 + 大额下单
        Pattern<LoginEvent, LoginEvent> remoteLargePattern = Pattern
            .<LoginEvent>begin("login")
            .where(e -> e.getLocation().equals("abnormal_location"))
            .next("order")
            .where(e -> e.getAmount() > 5000 && e.getOrderType().equals("create"))
            .within(Time.minutes(30));
        
        // 匹配结果输出
        PatternStream<OrderEvent> frequentStream = CEP.pattern(
            orderStream.keyBy(OrderEvent::getUserId), 
            frequentPattern
        );
        
        DataStream<Alert> alertStream = frequentStream.select(
            (Map<String, List<OrderEvent>> pattern) -> {
                List<OrderEvent> orders = pattern.get("order");
                return new Alert(
                    orders.get(0).getUserId(),
                    "FREQ_ORDER",
                    "5分钟内下单超过5次",
                    orders
                );
            }
        );
        
        alertStream.addSink(new AlertSink());
    }
}
```

### 常见应用场景

| 场景 | 模式描述 |
|------|----------|
| **信用卡欺诈** | 短时间内多地刷卡、短时间内频繁小额交易 |
| **网站攻击** | 同一IP短时间内大量请求 |
| **设备故障预警** | 温度持续上升超过阈值 |
| **用户行为分析** | 浏览 → 加购 → 下单 转化漏斗 |

### 性能优化

1. **选择合适的匹配策略**：严格连续（consecutive）比宽松连续（allowLoose）性能好
2. **减少状态大小**：只保存匹配需要的字段
3. **合理设置 within**：避免过期数据占用状态

> **来源说明：** 本题参考 Apache Flink CEP 官方文档 https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/libs/cep/

---

## 题目五：Flink 与 Kafka 如何集成实现端到端精确一次？Kafka Source 如何保证数据不丢失？

### 参考答案

Flink + Kafka 是实时流处理的黄金组合，实现端到端精确一次（Exactly-Once）需要两端共同保证。

### 端到端精确一次架构

```
                                    Checkpoint Barrier
                                           │
┌─────────────────────────────────────────────────────────┐
│                    Flink 作业                            │
│                                                         │
│  ┌──────────────┐    ┌──────────────┐    ┌────────────┐ │
│  │ Kafka Source │───►│  Processing  │───►│Kafka Sink  │ │
│  │              │    │              │    │            │ │
│  │  ✓ 读取偏移量  │    │  ✓ 状态快照   │    │  ✓ 事务写入 │ │
│  │  ✓ Checkpoint│    │  ✓ Checkpoint│    │  ✓ 两阶段提交│ │
│  └──────────────┘    └──────────────┘    └────────────┘ │
│         ↑                                       │        │
└─────────┼───────────────────────────────────────┼────────┘
          │                                       │
     从Checkpoint                              预提交/正式提交
     恢复偏移量                                Kafka事务
```

### Kafka Source 配置

```java
KafkaSource<String> source = KafkaSource.<String>builder()
    .setBootstrapServers("localhost:9092")
    .setTopics("input-topic")
    .setGroupId("my-group")
    .setStartingOffsets(OffsetsInitializer.committedOffsets(OffsetsInitializer.earliest()))
    .setValueOnlyDeserializer(new SimpleStringSchema())
    .setProperty("isolation.level", "read_committed")  // 消费已提交事务的消息
    .build();

DataStream<String> stream = env.fromSource(source, WatermarkStrategy.noWatermarks(), "Kafka Source");
```

### Checkpoint 与 Kafka 偏移量提交

Flink **不依赖** Kafka 的自动提交机制，而是通过 Checkpoint 管理偏移量：

```java
// 开启 Checkpoint（必须！）
env.enableCheckpointing(10000);  // 每10秒做一次 Checkpoint

CheckpointConfig config = env.getCheckpointConfig();
config.setCheckpointingMode(CheckpointingMode.EXACTLY_ONCE);
config.setMinPauseBetweenCheckpoints(500);
config.setCheckpointTimeout(60000);
config.setTolerableCheckpointFailureNumber(3);
```

**偏移量保存时机**：
- Checkpoint 完成时，Kafka Consumer 的读取偏移量作为算子状态的一部分被快照
- 如果 Checkpoint 成功，则该偏移量之前的消息都已被处理

### Kafka Sink 两阶段提交

FlinkKafkaProducer 基于 **TwoPhaseCommitSinkFunction** 实现两阶段提交：

```java
FlinkKafkaProducer<String> sink = new FlinkKafkaProducer<>(
    "output-topic",
    new SimpleStringSchema(),
    properties,
    FlinkKafkaProducer.Semantic.EXACTLY_ONCE  // 关键：开启精确一次
);
```

**两阶段提交流程**：

| 阶段 | 操作 | 说明 |
|------|------|------|
| **预提交** | Checkpoint 开始时，启动 Kafka 事务但不提交 | 数据写入 Kafka 缓冲区 |
| **正式提交** | Checkpoint 完成时，提交 Kafka 事务 | 数据对消费者可见 |
| **回滚** | Checkpoint 失败时，放弃 Kafka 事务 | 数据不写入 |

```
时间轴：
───► Checkpoint 1 开始 ──► 预提交事务 ──► 数据写入 ──► Checkpoint 1 完成 ──► 提交事务 ──►
         │
         ├── 如果 Checkpoint 1 失败 → Kafka 事务回滚 → 不丢不重复
         │
         └── 如果提交后才崩溃 → Kafka 事务已提交 → 不丢（已提交事务不会重复消费）
```

### Kafka 事务隔离级别

消费者端需要配置读取已提交事务的消息：

```properties
isolation.level = read_committed
# 或
isolation.level = read_uncommitted  # 读取所有消息（包括未提交事务）
```

### 常见问题

**1. Kafka 端到端只能保证一条消息被处理一次，不能保证业务只执行一次**
- 例：向数据库写入，如果写入成功但 Checkpoint 失败，可能重复写入
- 解决：业务层实现幂等（如数据库唯一键）

**2. Kafka Topic 的事务特性需要 Broker 配置支持**
```properties
transaction.state.log.replication.factor = 3
transaction.state.log.min.isr = 2
```

**3. Exactly-Once 性能开销**
- 延迟增加（等待 Checkpoint 和事务提交）
- Throughput 降低（约 20-30%）
- 生产环境需要评估是否必需

> **来源说明：** 本题参考 Apache Flink 官方文档 https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/connectors/datastream/kafka/
