# Spark 面试题精选（2026-08-21）

> 涵盖 Spark 架构、RDD、DataFrame/DataSet、Shuffle、内存管理、广播变量等核心知识点。

---

## 题目一：Spark 与 Hadoop 的核心区别是什么？为什么 Spark 比 MapReduce 快？

**参考答案：**

| 对比维度 | Hadoop MapReduce | Spark |
|---------|----------------|-------|
| **编程模型** | Map → Shuffle → Reduce，纯批处理 | DAG + 多 stage，支持迭代计算 |
| **中间结果** | 写入 HDFS，每次落盘 | 基于内存（RDD）或磁盘（checkpoint），无需频繁 IO |
| **任务调度** | 每次 Map/Reduce 均需启动 JVM | Executor 内任务在线程池复用，启动开销小 |
| **Shuffle** | 基于磁盘，文件排序合并 | 基于磁盘但做了聚合缓存，支持 Map 端combine |
| **数据存储** | HDFS / 本地磁盘 | 内存优先，内存不足时溢写磁盘 |

**快的根本原因：**
1. **内存计算** — 迭代型作业（如机器学习、图计算）避免了大量磁盘 IO
2. **DAG 优化** — 一个 job 内多个 stage 可在同一个 Executor 内高效传递数据
3. **线程池复用** — Task 在 Executor 内以线程方式复用，不像 MR 每次都 fork 进程

> 📎 来源：[Apache Spark 官方文档 - Overview](https://spark.apache.org/docs/latest/)

---

## 题目二：RDD 的五大特性是什么？Lazy Execution 在哪里体现？

**参考答案：**

RDD（Resilient Distributed Dataset）具有以下五大特性：

1. **`partitions`** — 数据集的分区列表，每个分区对应一个 Task 并行计算
2. **`compute(spark Partition) => Iterator[T]`** — 对每个分区的计算函数
3. **`dependencies: Seq[Dependency[_]]`** — RDD 依赖关系（宽依赖/窄依赖），决定 DAG 的划分
4. **`partitioner: Option[Partitioner]`** — 可选分区器（HashPartitioner / RangePartitioner），决定 Shuffle 类型
5. **`preferredLocations(spark Partition) => Seq[String]`** — 数据局部性偏好，最佳计算位置

### Lazy Execution（惰性执行）

RDD 的转换操作（`map`、`filter`、`groupByKey` 等）只是**构建 DAG**，并不会立即执行。只有遇到**行动操作**（`action`：如 `collect`、`count`、`saveAsTextFile`）时，才会：
- 触发 DAGScheduler 将 DAG 划分为 Stage
- 生成 TaskSet 提交给 TaskScheduler
- Executor 开始真正计算

```scala
// Lazy — 只记录 lineage，不会立即执行
val rdd = sc.textFile("hdfs://path/to/data").filter(_.nonEmpty).map(_.split(","))

// Action 触发真正执行
val result = rdd.collect()  // ← 这里是执行点
```

> 📎 来源：《Learning Spark》Lightning-Fast Data Analytics, 2nd Edition

---

## 题目三：解释 Spark Shuffle 的过程，为什么 Shuffle 是性能瓶颈？

**参考答案：**

### Shuffle 流程（以 `groupByKey` 为例）

```
Shuffle Write 阶段（Map Task）：
  → 对每个分区数据按 key 打标签（hash）
  → 将数据写入本地磁盘，生成 index + data 文件
  → 向 Driver 注册 Shuffle 元数据

Shuffle Read 阶段（Reduce Task）：
  → Reduce Task 向 Driver 获取 Shuffle Map 输出位置
  → 通过 BlockStore / HTTP 拉取对应分区的数据文件
  → 按 key 聚合（sort/aggregate）
```

### 为什么是性能瓶颈？

| 问题 | 原因 |
|------|------|
| **磁盘 IO** | Shuffle 数据必须落盘，无法纯内存交换 |
| **网络 IO** | 数据跨节点传输，带宽成为瓶颈 |
| **序列化开销** | 数据需序列化成字节数组传输（常见 Kryo） |
| **磁盘随机写** | Map 端输出到磁盘为大量小文件，HDFS 写入慢 |
| **内存压力** | 聚合时需在内存缓存数据，易导致 OOM 或 spill |
| **HDFS 冗余** | Shuffle 数据通常走 HDFS 副本，3倍网络开销 |

### 优化方向
- **Map 端 Combine**（`reduceByKey` 优于 `groupByKey`）
- **合理设置分区数**（避免过多小分区或过少大分区）
- **使用广播变量**减少 Shuffle 数据量
- **开启 Sort Shuffle**（Spark 1.2+ 默认）减少文件数

> 📎 来源：[Apache Spark 官方文档 - Shuffle Behavior](https://spark.apache.org/docs/latest/rdd-programming-guide.html#shuffle-operations)

---

## 题目四：Spark 内存管理模型是怎样的？Executor 内存如何划分？

**参考答案：**

Spark 2.x 采用 **Unified Memory Management**（统一内存管理），将堆内内存统一管理，不再硬性划分。

### 内存区域划分

```
┌─────────────────────────────────────────┐
│           Execution Memory               │
│   (Shuffle、sort、hash join 等计算)      │
│         ↕ 可借用 Storage Memory          │
├─────────────────────────────────────────┤
│           Storage Memory                 │
│   (RDD Cache、Broadcast、Task 结果缓存)   │
├───────────────┬─────────────────────────┤
│  User Memory  │   Reserved Memory       │
│ (用户数据结构、  │  (约 300MB，系统预留)   │
│  内部元数据)    │                        │
└───────────────┴─────────────────────────┘
```

### 核心规则

| 区域 | 用途 | 特点 |
|------|------|------|
| **Execution Memory** | Shuffle 聚合、排序、Join 计算 | 可借用 Storage（ eviction 机制） |
| **Storage Memory** | RDD Cache、Broadcast 变量 | 可借用 Execution（仅在 Execution 空闲时） |
| **User Memory** | 用户自定义数据结构、Task 内部对象 | 完全用户控制，Spark 不管理 |
| **Reserved Memory** | Spark 内部系统使用，固定约 300MB | 不可更改 |

### Spark 1.6 之前 vs 之后
- **1.6 之前**：静态内存管理（硬编码比例，难以充分利用资源）
- **1.6+（默认）**：统一内存管理，动态借用，资源利用率更高

### 配置参数
```bash
--conf spark.memory.fraction=0.6       # 统一内存占 Executor 比例（不含 Reserved）
--conf spark.memory.storageFraction=0.5  # Storage 最低保障比例
--conf spark.executor.memory=4g        # 总 Executor 内存
```

> 📎 来源：[Apache Spark 官方文档 - Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management-overview)

---

## 题目五：广播变量（Broadcast Variable）的原理、用途及注意事项？

**参考答案：**

### 什么是广播变量？

广播变量是**只读的、缓存在每个 Executor 节点内存中的变量**，Task 无需重复拉取，只在 Executor 第一次访问时加载，之后同节点所有 Task 共享。

```scala
// 普通变量：每个 Task 都会序列化一份副本
val lookup = Map(1 -> "北京", 2 -> "上海", ...)  // N 个 Task = N 份副本 ❌

// 广播变量：每个 Executor 只存一份
val broadcastLookup = sc.broadcast(Map(1 -> "北京", 2 -> "上海", ...))  // ✅
val result = rdd.map(row => broadcastLookup.value(row.cityId))
```

### 广播原理

1. **Driver 端**：调用 `sparkContext.broadcast(v)` 创建 Broadcast 对象，将变量序列化并注册到 Driver 元数据
2. **Task 调度时**：Spark 自动将 Broadcast 变量追加到 Task 依赖（作为闭包的一部分）
3. **Executor 端**：首次访问时通过 BlockManager 获取并缓存到本地，后续 Task 直接从内存读取

### 适用场景

| 场景 | 效果 |
|------|------|
| **大表 Join 小表**（小表作为广播） | 避免 Shuffle，全量数据在本地完成 Join |
| **配置表 / 维度表** | 每个 Task 直接查询内存中的广播数据 |
| **机器学习特征映射** | 广播特征权重或字典 |
| **IP地址库 / 地名映射** | 避免大表 Shuffle |

### 注意事项

| 问题 | 说明 |
|------|------|
| **数据量不宜过大** | 广播数据存储在 Executor 堆内，过大导致 OOM |
| **只读不变** | 广播后不可修改，否则 Task 间可见性不可预期 |
| **不适合高频更新场景** | 广播更新需要重新创建 Broadcast 对象 |
| **合理设置分区数** | `spark.sql.shuffle.partitions` 影响聚合效率 |

### 高级配置
```bash
--conf spark.broadcast.blockSize=4m   # 广播块大小，过大影响传输效率
--conf spark.broadcast.compress=true  # 开启广播压缩（默认开启）
```

> 📎 来源：[Apache Spark 官方文档 - Broadcast Variables](https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables)

---

*整理自互联网公开资料，仅供学习参考。*
