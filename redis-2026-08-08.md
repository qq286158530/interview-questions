# Redis 面试题精选 | 2026-08-08

> 📚 来源：[小林图解Redis · xiaolincoding.com](https://xiaolincoding.com/redis/)
> 涵盖数据类型、持久化、集群、缓存等核心知识点

---

## 题目一：Redis 有哪些数据类型？底层是如何实现的？

### 参考答案

Redis 提供了丰富的数据类型，常见的有 **5 种基础数据类型**：String（字符串）、Hash（哈希）、List（列表）、Set（集合）、Zset（有序集合）。随着版本更新，又支持了 BitMap、HyperLogLog、GEO、Stream 四种高级数据类型。

#### 五种基础数据类型的应用场景

| 类型 | 应用场景 |
|------|----------|
| **String** | 缓存对象、常规计数、分布式锁、共享 session 信息 |
| **Hash** | 缓存对象、购物车 |
| **List** | 消息队列（需自行实现全局唯一 ID） |
| **Set** | 聚合计算（并集/交集/差集），如点赞、共同关注、抽奖 |
| **Zset** | 排序场景，如排行榜、电话姓名排序 |

#### 底层数据结构实现

Redis 3.0 ~ 7.0 各版本的底层实现有所演进：

- **String**：底层采用 **SDS（简单动态字符串）**。SDS 不仅可以保存文本数据，还可以保存二进制数据；获取字符串长度的时间复杂度为 O(1)；拼接字符串不会造成缓冲区溢出。

- **List**：Redis 3.2 之前由双向链表或压缩列表实现；之后统一由 **quicklist** 实现，替代了双向链表和压缩列表。

- **Hash**：由压缩列表或哈希表实现。当元素个数小于 512 且每个值小于 64 字节时使用压缩列表，否则使用哈希表。（Redis 7.0 压缩列表已废弃，改为 listpack）

- **Set**：由哈希表或整数集合实现。当集合元素都是整数且个数小于 512 时使用整数集合，否则使用哈希表。

- **Zset**：由压缩列表或跳表实现。当元素个数小于 128 且每个值小于 64 字节时使用压缩列表，否则使用跳表。（Redis 7.0 压缩列表已废弃）

---

## 题目二：Redis 持久化机制是怎么实现的？RDB 和 AOF 各有什么优缺点？

### 参考答案

Redis 共有三种数据持久化方式：**AOF 日志、RDB 快照、混合持久化**。

#### AOF 日志（Append Only File）

Redis 在执行完写操作命令后，将该命令以追加的方式写入一个文件。Redis 重启时，通过读取 AOF 文件中的命令来恢复数据。

**AOF 写回策略（appendfsync 配置项）：**

| 策略 | 含义 | 优点 | 缺点 |
|------|------|------|------|
| **Always** | 每次写命令执行完，同步写回硬盘 | 数据几乎不丢失 | 开销大，阻塞主线程 |
| **Everysec**（默认） | 每次写命令执行完，先写入内核缓冲区，每秒刷盘 | 性能适中 | 最多丢失 1 秒数据 |
| **No** | 交由操作系统决定何时刷盘 | 性能最好 | 依赖操作系统，数据丢失不可控 |

**AOF 重写机制**：当 AOF 文件过大时，后台子进程 `bgrewriteaof` 会读取数据库中所有键值对，用一条命令记录到新的 AOF 文件，从而压缩文件体积。

**AOF 优点**：数据丢失少；**缺点**：恢复速度慢（需要逐一执行命令）。

#### RDB 快照（Redis Database）

记录某一瞬间的内存数据，以二进制方式写入磁盘。

- `save` 命令：在主线程生成 RDB 文件，会阻塞主线程。
- `bgsave` 命令：创建子进程生成 RDB 文件，主线程不受影响。
- Redis 默认配置满足以下任一条件自动触发 `bgsave`：
  - 900 秒内至少 1 次修改
  - 300 秒内至少 10 次修改
  - 60 秒内至少 10000 次修改

**RDB 优点**：恢复速度快（全量数据直接读入内存）；**缺点**：快照频率难以把控，频率太低丢数据多，频率太高影响性能。

**RDB 执行期间数据可修改**：得益于 **写时复制（COW）** 技术，fork 创建子进程时父子进程共享物理内存，子进程只读，主线程修改数据时复制副本给子进程写入 RDB。

#### 混合持久化（Redis 4.0+）

结合 RDB 和 AOF 的优点。AOF 重写时，先以 RDB 格式写入全量数据，再以 AOF 格式追加增量命令。这样**既保证了启动速度，又降低数据丢失风险**。

| 特性 | AOF | RDB | 混合持久化 |
|------|-----|-----|-----------|
| 启动速度 | 慢 | 快 | 快 |
| 数据丢失 | 少 | 多 | 少 |
| 文件可读性 | 好 | 好 | 差（RDBAOF混合） |

---

## 题目三：什么是缓存雪崩、击穿、穿透？各有何解决方案？

### 参考答案

引入缓存层后，常见的三个缓存异常问题：

#### 缓存雪崩

大量缓存数据**在同一时间过期**或 **Redis 故障宕机**，导致大量请求直接访问数据库，数据库压力骤增甚至宕机。

**解决方案：**

- **均匀设置过期时间**：给过期时间加上随机数，避免大量数据同时过期。
- **互斥锁**：只允许一个请求构建缓存，其他请求等待或返回默认值。
- **后台更新缓存**：缓存不设有效期，由后台线程定时更新。
- **Redis 高可靠集群**：主从节点切换，保证故障时从节点接管。
- **服务熔断/请求限流**：缓存故障时暂停或限制对数据库的访问。

#### 缓存击穿

某个**热点数据过期**，大量请求并发访问该数据，无法命中缓存，直接冲垮数据库。

**解决方案：**

- **互斥锁**：同雪崩，保证同一时间只有一个请求构建缓存。
- **不设置过期时间**：后台异步更新，或在热点数据过期前主动刷新。

#### 缓存穿透

数据**既不在缓存也不在数据库**中（如恶意攻击或误删数据），大量请求直接访问数据库。

**常见原因：**

- 业务误操作，缓存和数据库数据都被删除
- 黑客恶意攻击大量访问不存在的数据

**解决方案：**

- **非法请求限制**：API 入口校验参数合法性
- **缓存空值/默认值**：数据库查不到时写入空值，减少数据库压力
- **布隆过滤器**：写入数据时在布隆过滤器做标记，查询时先判断数据是否存在，存在再查 DB。**注意**：布隆过滤器说数据存在不一定在 DB 中，说不存在则一定不在。

---

## 题目四：Redis 主从复制、哨兵模式、Cluster 集群是如何工作的？

### 参考答案

#### 主从复制

一主多从模式，主节点负责写操作，从节点只读并同步主节点数据。**命令复制是异步的**，主节点不会等从节点执行完命令再返回客户端。

**优点**：读写分离提升并发能力，数据冗余备份。
**缺点**：主从数据存在延迟，无法保证强一致性。

#### 哨兵模式（Sentinel）

监控主从节点健康状态，自动完成**故障转移**。

核心功能：

- **监控**：周期检查主从服务器是否宕机
- **自动故障转移**：主节点故障时，选举从节点升级为新主节点
- **通知**：向客户端发送主节点变更通知

#### Cluster 切片集群

当数据量大到单台 Redis 无法缓存时，使用 **Hash Slot（哈希槽）** 方案将数据分布在多个节点。

- 共有 **16384 个哈希槽**
- key 通过 `CRC16(key) % 16384` 映射到对应哈希槽
- 哈希槽可手动分配或自动平均分配
- 所有哈希槽须分配完集群才能正常工作

**高可用保证**：每个主节点可配置多个从节点，主节点故障时从节点自动切换。

**脑裂问题**：网络分区导致主节点失联但仍在服务，哨兵重新选举新主节点，网络恢复后旧主降级为从导致数据丢失。可通过配置 `min-slaves-to-write` 和 `min-slaves-max-lag` 限制主节点在从节点不达标时禁止写入。

---

## 题目五：Redis 过期删除策略和内存淘汰策略有什么区别？

### 参考答案

#### 过期删除策略

Redis 对 key 设置过期时间，过期数据存储在**过期字典**中。Redis 采用 **惰性删除 + 定期删除** 配合使用：

**惰性删除**：每次访问 key 时才检查是否过期。优点是对 CPU 友好，缺点是过期 key 长期占用内存。

**定期删除**：每隔一段时间随机从数据库中抽取 20 个 key 检查，删除已过期的 key。如果已过期 key 超过 5 个（1/4），继续抽取检查。为防止卡死，单次执行上限为 25ms。

#### 内存淘汰策略（maxmemory-policy）

当 Redis 内存达到 `maxmemory` 配置值时，触发内存淘汰机制：

| 策略 | 含义 |
|------|------|
| **noeviction** | 不淘汰，返回错误（默认） |
| **volatile-lru** | 在设置了过期时间的 key 中淘汰最少使用的（LRU） |
| **volatile-ttl** | 在设置了过期时间的 key 中淘汰剩余 TTL 最少的 |
| **volatile-random** | 在设置了过期时间的 key 中随机淘汰 |
| **allkeys-lru** | 在所有 key 中淘汰最少使用的 |
| **allkeys-random** | 在所有 key 中随机淘汰 |
| **volatile-lfu** | 在设置了过期时间的 key 中淘汰最不经常使用的（LFU） |
| **allkeys-lfu** | 在所有 key 中淘汰最不经常使用的 |

#### 两者核心区别

- **过期删除**：针对已设置 TTL 的 key，由惰性删除和定期删除两个机制配合执行，是主动的*时间维度的清理*。
- **内存淘汰**：当内存达到上限时，被动触发，是内存紧张时的*空间维度的自我保护*。

#### 持久化时对过期键的处理

- **RDB 生成阶段**：已过期键不会被写入 RDB 文件
- **RDB 加载阶段**：主服务器不载入过期键；从服务器不论是否过期都载入（主从同步时会清空）
- **AOF 写入阶段**：过期键未被删除前会保留，删除时追加一条 DEL 命令
- **AOF 重写阶段**：已过期键不会被保存

---

> 📖 参考来源：[小林图解Redis · xiaolincoding.com](https://xiaolincoding.com/redis/)
