# Redis 面试题 - 2026-08-08

## 题目一：Redis 的过期删除策略与内存淘汰策略有什么区别？

**参考答案：**

Redis 的过期删除策略和内存淘汰策略是两个不同的概念，解决的问题也不同。

### 过期删除策略

过期删除策略是针对**已设置过期时间的 key**，用于删除过期的键值对。Redis 采用的是「惰性删除 + 定期删除」组合策略：

**惰性删除**：每次访问 key 时才检查是否过期，如果过期则删除。优点是对 CPU 友好，缺点是如果过期 key 长期不被访问，会占用内存。

**定期删除**：每隔一段时间随机从过期字典中抽取 20 个 key 检查，删除过期的 key。如果已过期 key 超过 25%，则继续抽取，直到比例低于 25% 为止。为了防止循环过度，单次定期删除执行时间不超过 25ms。

### 内存淘汰策略

内存淘汰策略是当 Redis 运行内存达到 `maxmemory` 阈值时，触发淘汰机制。Redis 3.0 后默认策略是 `noeviction`（不淘汰，返回错误）。

**八种淘汰策略**：

| 分类 | 策略 | 说明 |
|------|------|------|
| 不淘汰 | noeviction | 内存满时拒绝写入 |
| \ | volatile-random | 随机淘汰带过期时间的 key |
| \ | volatile-ttl | 优先淘汰最早过期的 key |
| \ | volatile-lru | 淘汰最久未使用的带过期时间的 key |
| \ | volatile-lfu | 淘汰访问频率最低的带过期时间的 key |
| \ | allkeys-random | 随机淘汰任意 key |
| \ | allkeys-lru | 淘汰整个库中最久未使用的 key |
| \ | allkeys-lfu | 淘汰整个库中访问频率最低的 key |

**LRU vs LFU**：
- LRU（Least Recently Used）：淘汰最近最少使用的数据，基于访问时间
- LFU（Least Frequently Used）：淘汰访问频率最低的数据，基于访问次数，Redis 4.0 新增，能更好解决缓存污染问题

---

**来源**：[图解 Redis | 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目二：Redis 持久化机制有哪些？AOF 和 RDB 有什么区别？

**参考答案：**

Redis 提供了三种持久化方式：RDB、AOF 和混合持久化。

### RDB（Redis Database）

RDB 是将某一时刻的内存数据以**二进制格式**写入磁盘，是全量快照。

**触发方式**：
- 手动触发：`save`（主线程执行，会阻塞）和 `bgsave`（fork 子进程执行，不阻塞）
- 自动触发：通过 `save m n` 配置，如 `save 900 1` 表示 900 秒内至少 1 次修改则触发 bgsave

**优点**：恢复速度快（全量数据直接加载）
**缺点**：可能丢失最后一次快照后的数据；fork 子进程时有内存膨胀问题

### AOF（Append Only File）

AOF 是将每条写操作命令以追加方式写入日志文件，是增量持久化。

**写回策略**（`appendfsync` 配置项）：

| 策略 | 行为 | 安全性 | 性能 |
|------|------|--------|------|
| Always | 同步写回，每个命令执行完即写盘 | 高 | 低 |
| Everysec | 每秒写回，命令执行后先写缓冲区 | 中 | 中 |
| No | 由操作系统决定何时写回 | 低 | 高 |

**AOF 重写**：当 AOF 文件过大时，后台子进程 `bgrewriteaof` 会读取数据库所有键值对，用一条命令记录到新的 AOF 文件，实现压缩。重写过程中主进程的新命令会写入「AOF 重写缓冲区」，重写完成后合并到新 AOF 文件。

### 混合持久化

Redis 4.0 引入，结合 RDB 和 AOF 的优点。

重写时，fork 出的子进程先将以 RDB 格式写入 AOF 文件，然后主进程处理的增量命令以 AOF 格式追加到文件末尾。重启恢复时先加载 RDB 部分加速，再加载 AOF 增量数据。

**优点**：启动快 + 数据丢失少
**缺点**：AOF 文件可读性差；不兼容 4.0 之前版本

### 对比总结

| 特性 | RDB | AOF |
|------|-----|-----|
| 文件格式 | 二进制 | 文本（命令） |
| 恢复速度 | 快 | 慢 |
| 数据完整性 | 可能丢失数据 | 可配置（everysec/always） |
| 文件大小 | 小 | 大 |
| 性能影响 | fork 消耗 | 持续写开销 |

---

**来源**：[图解 Redis | 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目三：Redis 主从复制原理是什么？哨兵模式解决了什么问题？

**参考答案：**

### 主从复制

主从复制是 Redis 高可用架构的基础，采用「一主多从」模式，实现读写分离。

**复制原理**：
1. 从服务器启动后，向主服务器发送 `PSYNC` 命令（部分同步）或 `SYNC` 命令（全量同步）
2. 主服务器收到命令后，执行 `bgsave` 生成 RDB 文件，同时将后续写命令写入缓冲区
3. 主服务器将 RDB 文件发送给从服务器，从服务器加载 RDB 文件
4. 主服务器将缓冲区中的写命令发送给从服务器执行

**复制方式**：
- **全量同步**：适用于首次复制或从服务器丢失大部分数据时
- **部分同步**：主服务器维护复制积压缓冲区，如果从服务器偏移量在缓冲区范围内，则发送增量命令

**异步复制**：主从复制是异步的，主服务器执行完命令后即返回客户端，不等待从服务器执行完成。因此主从数据可能存在短暂不一致。

### 哨兵模式

主从架构中，如果主节点故障，需要手动恢复。哨兵模式（Sentinel）解决了这个问题，实现了**自动故障转移**。

**哨兵职责**：
1. **监控**：定期检查主从服务器是否存活
2. **通知**：故障时通知其他哨兵和应用程序
3. **自动故障转移**：从从服务器中选举一个新的主服务器，并让其他从服务器复制新的主服务器

**故障转移过程**：
1. 主观下线：单个哨兵检测到主服务器心跳超时
2. 客观下线：多个哨兵投票，超过 quorum 数量认为主服务器真的下线
3. 选举领头哨兵：由所有哨兵投票选出一个领头的负责执行故障转移
4. 选举新主服务器：领头哨兵从从服务器中根据优先级、复制偏移量等选出新的主服务器
5. 通知其他从服务器连接新主服务器，并通知客户端

---

**来源**：[图解 Redis | 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目四：什么是缓存雪崩、缓存击穿、缓存穿透？如何应对？

**参考答案：**

这三个问题都是缓存失效带来的风险，但场景不同。

### 缓存雪崩

大量缓存数据在同一时间过期，导致大量请求直接访问数据库。

**解决方案**：
- **过期时间随机化**：在原有过期时间基础上加上随机值（如 1~10 分钟），避免集体失效
- **设置缓存永不过期**：通过后台服务更新缓存数据
- **使用多级缓存**：建立多级缓存架构
- **熔断/限流**：数据库压力过大时拒绝部分请求

### 缓存击穿

某个热点数据过期，但该数据被大量并发访问，导致瞬间大量请求直接打到数据库。

**解决方案**：
- **互斥锁**：使用 `SETNX` 实现分布式锁，保证只有一个请求去查询数据库并更新缓存
- **热点数据不过期**：由后台异步更新缓存数据
- **提前更新**：在热点数据即将过期前，主动更新缓存

### 缓存穿透

请求访问的数据既不在缓存中也不在数据库中，导致请求直接打到数据库。

**常见原因**：
- 业务误操作删除了缓存和数据库数据
- 恶意攻击大量请求不存在的数据

**解决方案**：
- **参数校验**：在 API 入口处校验请求参数，拦截非法请求
- **设置空值/默认值**：查询结果为空时，在缓存中设置空值或默认值
- **布隆过滤器**：在写入数据库时将数据标记到布隆过滤器，查询时先检查布隆过滤器判断数据是否存在
- **增强数据库安全**：做好数据库的权限管理，防止恶意攻击

### 区别与联系

| 问题 | 原因 | 影响 | 共同点 |
|------|------|------|--------|
| 缓存雪崩 | 大量缓存同时过期 | 数据库压力骤增 | \ |
| 缓存击穿 | 热点缓存过期 | 数据库压力骤增 | 都可能造成数据库宕机 |
| 缓存穿透 | 数据本就不存在 | 数据库压力骤增 | \ |

---

**来源**：[图解 Redis | 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目五：Redis Cluster 是如何实现数据分片的？集群脑裂问题如何处理？

**参考答案：**

### Redis Cluster 数据分片

Redis Cluster 采用**哈希槽（Hash Slot）** 机制实现数据分片，共有 16384 个槽。

**数据映射过程**：
1. 根据 key 使用 CRC16 算法计算 16 位哈希值
2. 用哈希值对 16384 取模，得到 0~16383 的槽编号
3. 根据槽编号找到对应的 Redis 节点

**槽分配方式**：
- **平均分配**：`cluster create` 命令会自动将 16384 个槽平均分配到各节点
- **手动分配**：使用 `cluster meet` 建立节点连接，再用 `cluster addslots` 手动指定每个节点的槽

**特点**：
- 客户端可以连接任意节点，节点会自动重定向到正确节点
- 支持在线扩容，可以通过 `cluster resharding` 迁移槽和数据
- 所有 16384 个槽必须分配完集群才能正常工作

### 集群脑裂问题

**什么是脑裂**：主节点网络故障，与从节点失联，但与客户端连接正常。客户端继续向主节点写入数据，同时哨兵误以为主节点宕机，选举出新主节点，形成两个主节点同时存在的局面。

**数据丢失原因**：网络恢复后，旧主节点降级为从节点，从节点会执行全量同步清空本地数据，导致之前写入的数据丢失。

**解决方案**：通过配置 `min-slaves-to-write` 和 `min-slaves-max-lag` 两个参数：

```conf
min-slaves-to-write 1    # 主节点必须至少有1个从节点连接
min-slaves-max-lag 10    # 从节点复制延迟不能超过10秒
```

**工作原理**：
- 当主节点发现从节点数量不足或复制延迟超过阈值时，**禁止主节点接收写请求**
- 即使原主节点是"假故障"（网络问题），它无法响应从节点的心跳和 ACK 确认
- 新主库接管后，只有新主库能处理写请求，避免脑裂发生

这两个参数组合使用，确保主库只有在有足够数量的从节点正常同步时才能接收写请求。

---

**来源**：[图解 Redis | 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)
