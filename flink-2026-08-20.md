# Flink 面试题 — 2026-08-20

> 涵盖 Flink 架构、Watermark、Checkpoint、窗口函数、状态管理等核心知识点

---

## 题目 1：Flink 的架构是怎样的？为什么 Flink 能做到流处理最强？

### 参考答案

Flink 采用**流优先（Stream-first）**架构，数据从始至终都是通过**无界数据流**进行处理，这一点与批处理优先的框架（如 Spark）有本质区别。

Flink 架构主要分为以下几层：

1. **API Layer（API 层）**
   - **DataStream API**：用于无界数据流处理（Real-time Processing）
   - **DataSet API**：用于有界批处理（Batch Processing）
   - 之上还有 **Table API / SQL** 等高级抽象

2. **Runtime Core（运行时核心）**
   - **JobManager**：协调整个作业的执行，负责任务调度、资源管理、Checkpoint 协调
   - **TaskManager**：真正执行 Task 的工作节点，每个 TM 包含多个 **TaskSlot**
   - **TaskSlot**：TM 的资源单位，规定了每个任务的并行度资源配额

3. **Physical Layer（物理层）**
   - Flink 程序被编译为 **JobGraph** → **ExecutionGraph**（物理执行图）
   - ExecutionGraph 才是真正在集群上调度的执行计划，描述了算子间的数据分区和并行关系

4. **Connectors（连接层）**
   - Kafka、Hive、HDFS、S3 等外部系统的 connector

**为什么 Flink 流处理最强？**

| 特性 | Flink | Spark Streaming |
|------|-------|-----------------|
| 处理模型 | 真正的流（一条一条处理） | 微批（Mini-batch，延迟秒级） |
| 事件时间支持 | ✅ 原生支持 | ✅ 有限支持 |
| 状态管理 | 强大的 Keyed State | 仅 RLock |
| Watermark | 原生支持 | 不支持 |
| 窗口语义 | 灵活（滚动/滑动/会话） | 固定窗口 |
| 容错 | Chandy-Lamport 分布式快照 |Checkpoint |

**核心优势总结：**
- **流原生架构**：真正的 Streaming，不像 Spark 是"Batch化的 Streaming"
- **精确一次（Exactly-Once）语义**：通过分布式快照（Chandy-Lamport）实现
- **强大的 Time Serise 处理**：Watermark + 窗口机制完美支持乱序数据
- **灵活的 State Backend**：支持 RocksDB（增量 checkpoint）和 Heap（内存）

---

## 题目 2：什么是 Watermark？Flink 如何处理乱序数据和迟到事件？

### 参考答案

### Watermark 的概念

**Watermark（水位线）**是一种**时间戳标记**，用于告诉 Flink："**时间戳 ≤ Watermark 的数据已经全部到达，后续不会再有了**"。

本质上是 Flink 实现**事件时间（Event Time）**处理的核心机制，用来平衡**数据完整性**和**处理延迟**。

### 为什么需要 Watermark？

在现实场景中，数据会因为网络延迟、分布式系统等原因产生**乱序**甚至**迟到**。如果 Flink 严格按照数据到达顺序（Processing Time）处理，会导致结果不准确。

### Watermark 的工作原理

```
Watermark(t) = max(当前最大事件时间) - 最大乱序容忍度
```

当 Watermark 通过窗口的**窗口结束时间**时，窗口就触发计算。

**示例：**

```
窗口 [10:00, 10:10)，允许乱序 3 秒
- 当前最大事件时间 = 10:08
- Watermark = 10:08 - 3 = 10:05
- 当 Watermark >= 10:10 时，窗口触发
```

### Flink 处理迟到事件的三种策略

| 策略 | 说明 |
|------|------|
| **默认丢弃** | 事件时间 < Watermark 的数据直接丢弃 |
| **allowedLateness()** | 额外等待一段时间，迟到事件仍可触发窗口计算 |
| **sideOutputTag()** | 将迟到事件单独收集到侧输出流，供后续处理 |

### 代码示例

```java
DataStream<MyEvent> stream = env.addSource(new MySource());

WatermarkStrategy<MyEvent> wms = WatermarkStrategy
    .<MyEvent>forBoundedOutOfOrderness(Duration.ofSeconds(10)) // 最大乱序容忍 10 秒
    .withTimestampAssigner((event, timestamp) -> event.getTimestamp()) // 从数据中提取时间戳
    .withIdleness(Duration.ofMinutes(1)); // 处理空闲数据源

DataStream<MyEvent> withWatermarks = stream.assignTimestampsAndWatermarks(wms);

DataStream<result> result = withWatermarks
    .keyBy(MyEvent::getKey)
    .window(TumblingEventTimeWindows.of(Time.minutes(5)))
    .allowedLateness(Time.minutes(1))  // 允许额外迟到 1 分钟
    .process(new MyWindowFunction());

// 收集迟到超过 1 分钟的事件
OutputTag<MyEvent> lateTag = new OutputTag<MyEvent>("late-data"){};
DataStream<MyEvent> lateStream = result.getSideOutput(lateTag);
```

### 关键点总结

- Watermark 的生成方式：**周期性生成**（默认 200ms 周期）和**标点生成**
- 常用 Watermark 策略：`forBoundedOutOfOrderness`（有界乱序）和 `forMonotonousTimestamps`（单调递增）
- **乱序度（lateness）设置原则**：根据数据特点和网络延迟情况，设置合理的乱序容忍时间

---

## 题目 3：Flink 的 Checkpoint 和 Savepoint 有什么区别？如何实现精确一次语义？

### 参考答案

### Checkpoint vs Savepoint 对比

| 维度 | Checkpoint | Savepoint |
|------|------------|-----------|
| **触发方式** | Flink 自动周期性触发 | 手动触发（用户命令） |
| **用途** | 故障恢复（自动） | 作业计划性停止/恢复 |
| **清理策略** | Job 删除时自动清理 | 需手动删除 |
| **数据格式** | 由 State Backend 决定 | 统一的 Savepoint 格式 |
| **依赖** | 依赖 CheckpointingConfiguration | 不依赖 |
| **路径** | 由 Flink 管理 | 用户指定路径 |

### Checkpoint 的实现原理——Chandy-Lamport 分布式快照

Flink 使用**改进的 Chandy-Lamport 算法**实现分布式快照，核心思想：

1. **JobManager** 周期性地向所有 Source 算子发送 **Checkpoint Barrier（检查点屏障）**
2. **Barrier 替代数据流中的普通数据**，随数据流向下游传播
3. 每个算子收到 Barrier 时：
   - 对齐所有输入 Channel 的 Barrier（**Barrier 对齐**）
   - 暂停该 Channel 的数据处理
   - 将当前状态快照到 State Backend
   - 继续处理其他 Channel 的数据
4. 当所有 Sink 算子确认快照完成，Checkpoint 才算成功

```
[Source] ---Barrier A---> [Map] ---> [KeyBy] ---> [Window] ---> [Sink]
                                ↑
                           快照状态存储到 RocksDB/Heap
```

### Exactly-Once 语义实现条件

Flink 的 **Exactly-Once** 需要同时满足以下条件：

1. **Source 端**：支持数据回退（如 Kafka Consumer 的 offset 提交）
2. **State 端**：状态可重放（通过 Checkpoint 恢复）
3. **Sink 端**：支持幂等写入或事务写入（如 Kafka Sink 的两阶段提交）

### 两阶段提交 Sink（TwoPhaseCommitSink）

对于需要 Exactly-Once 的 Sink（如 Kafka），Flink 使用**两阶段提交协议**：

```
阶段 1：预提交（Pre-commit）
  - 算子处理完数据后，先将数据写入并"冻结"
  - 等待 Checkpoint 完成

阶段 2：正式提交（Commit）
  - Checkpoint 完成后，触发 commit 操作
  - 如果 Checkpoint 失败，abort 预提交的数据
```

常见的两阶段提交 Sink 实现：
- **FlinkKafkaProducer**（旧版）
- **FlinkKafkaSink**（新版，支持 Exactly-Once）
- **File Sink**（支持 Exactly-Once）
- **Hive Sink**（支持 Exactly-Once）

### Checkpoint 配置示例

```java
StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();

// 开启 Checkpoint，间隔 10 秒
env.enableCheckpointing(10_000);

// 高级选项
CheckpointConfig checkpointConfig = env.getCheckpointConfig();
checkpointConfig.setCheckpointingMode(CheckpointingMode.EXACTLY_ONCE);
checkpointConfig.setMinPauseBetweenCheckpoints(5000); // 两个 Checkpoint 间隔至少 5 秒
checkpointConfig.setCheckpointTimeout(60000); // Checkpoint 超时 1 分钟
checkpointConfig.setMaxConcurrentCheckpoints(1); // 最大并发 Checkpoint 数
checkpointConfig.setTolerableCheckpointFailureNumber(3); // 容忍 3 次失败

// 开启非对齐 Checkpoint（加快恢复速度）
checkpointConfig.enableUnalignedCheckpoints();
```

---

## 题目 4：Flink 的窗口函数有哪些？滚窗、滑动窗、会话窗口的区别是什么？

### 参考答案

### 窗口分类

Flink 支持的窗口主要分为以下几类：

#### 按时间划分

| 窗口类型 | 说明 | 适用场景 |
|----------|------|----------|
| **Tumbling Window（滚动窗口）** | 大小固定，不重叠 | 每天/每小时统计 |
| **Sliding Window（滑动窗口）** | 大小固定，可重叠 | 需要平滑数据的场景 |
| **Session Window（会话窗口）** | 由间隔 gap 划分，活性不固定 | 用户行为分析 |

#### 按划分方式划分

| 窗口类型 | 说明 |
|----------|------|
| **Count Window（计数窗口）** | 按数据条数触发（如每 100 条计算一次） |
| **Global Window（全局窗口）** | 所有数据进入同一窗口，需配合 Trigger 使用 |

### 滚动窗口 vs 滑动窗口 vs 会话窗口

#### 1. 滚动窗口（Tumbling Window）

```
窗口大小：5 分钟
[00:00-05:00) | [05:00-10:00) | [10:00-15:00) | ...
特点：首尾相接，不重叠
```

```java
// 滚动事件时间窗口
data.keyBy(...)
    .window(TumblingEventTimeWindows.of(Time.minutes(5)));

// 滚动处理时间窗口
data.keyBy(...)
    .window(TumblingProcessingTimeWindows.of(Time.minutes(5)));

// 滚动计数窗口（每 100 条触发一次）
data.keyBy(...)
    .countWindow(100);
```

#### 2. 滑动窗口（Sliding Window）

```
窗口大小：10 分钟，滑动步长：5 分钟
[00:00-10:00)  |  [05:00-15:00)  |  [10:00-20:00) | ...
特点：窗口重叠，可以平滑数据
```

```java
// 滑动事件时间窗口：窗口大小 10 分钟，滑动步长 5 分钟
data.keyBy(...)
    .window(SlidingEventTimeWindows.of(Time.minutes(10), Time.minutes(5)));

// 滑动计数窗口（每 10 条计算一次，窗口包含 20 条数据）
data.keyBy(...)
    .countWindow(20, 10);
```

#### 3. 会话窗口（Session Window）

```
gap = 3 分钟
[session A: 00:00-00:02-00:05-gap-] [session B: 00:08-gap-]
特点：没有固定边界，由数据之间的间隔 gap 决定窗口边界
```

```java
// 会话窗口，间隔 gap 为 3 分钟
data.keyBy(...)
    .window(EventTimeSessionWindows.withGap(Time.minutes(3)));

// 动态 gap（基于数据属性）
data.keyBy(...)
    .window(EventTimeSessionWindows.withDynamicGap(new SessionWindowTimeGapSelector() {
        @Override
        public long select(T element) {
            return element.getGapTime();
        }
    }));
```

### 窗口函数（ProcessFunction）

窗口内的数据处理有三种方式：

| 函数类型 | 说明 | 适用场景 |
|----------|------|----------|
| **reduce()** | 增量聚合，内存占用低 | SUM、MAX、MIN 等可提前聚合的操作 |
| **aggregate()** | 更灵活的增量聚合，可输出多类型 | 需要复杂聚合逻辑 |
| **process()** | 全量窗口函数，获取窗口所有数据 | 需要遍历全部数据、自定义复杂逻辑 |

```java
// 增量聚合：reduce
data.keyBy(...)
    .window(TumblingEventTimeWindows.of(Time.minutes(5)))
    .reduce((v1, v2) -> new MyAggResult(v1.getSum() + v2.getSum()));

// 全量窗口：process
data.keyBy(...)
    .window(TumblingEventTimeWindows.of(Time.minutes(5)))
    .process(new ProcessWindowFunction<Type, OutType, Key, TimeWindow>() {
        @Override
        public void process(Key key, Context ctx, Iterable<Type> elements, Collector<OutType> out) {
            // elements 包含窗口内所有数据
            for (Type e : elements) {
                // 遍历处理
            }
            // 可获取窗口信息
            TimeWindow window = ctx.window();
            long start = window.getStart();
            long end = window.getEnd();
        }
    });
```

---

## 题目 5：Flink 的状态管理机制是什么？Keyed State 和 Operator State 有何区别？

### 参考答案

### Flink 状态管理概述

Flink 是**有状态的流处理框架**，状态（State）是指算子在处理过程中需要记住的信息，如聚合结果、缓存数据等。Flink 的状态管理是其实现**精确一次语义**和高性能流处理的基础。

### 状态的分类

#### 1. Keyed State（键控状态）

**前提**：必须先调用 `keyBy()`，按 Key 分区后的状态。

| 状态类型 | 说明 | 示例 |
|----------|------|------|
| **ValueState\<T\>** | 单值状态 | 存储当前最大值 |
| **ListState\<T\>** | 列表状态 | 存储最近的 N 条数据 |
| **ReducingState\<T\>** | 自动聚合状态 | 自动求和 |
| **AggregatingState\<IN, OUT\>** | 聚合状态 | 自定义聚合逻辑 |
| **MapState\<K, V\>** | Map 状态 | 存储键值对映射 |

```java
public class MyStatefulFunction extends KeyedProcessFunction<String, Long, String> {
    
    // 声明状态
    private ValueState<Long> countState;
    private ListState<String> historyState;
    
    @Override
    public void open(Configuration parameters) {
        // 初始化状态
        countState = getRuntimeContext().getState(
            new ValueStateDescriptor<>("count", Long.class));
        historyState = getRuntimeContext().getListState(
            new ListStateDescriptor<>("history", String.class));
    }
    
    @Override
    public void processElement(Long value, Context ctx, Collector<String> out) {
        // 使用状态
        Long currentCount = countState.value() == null ? 0L : countState.value();
        countState.update(currentCount + 1);
        
        historyState.add("event_" + value);
        
        out.collect("Key: " + ctx.getCurrentKey() + ", Count: " + countState.value());
    }
}
```

#### 2. Operator State（算子状态）

**特点**：不依赖 Key，整个算子实例共享同一份状态。

| 状态类型 | 说明 | 常见场景 |
|----------|------|----------|
| **ListState** | 列表状态 | Kafka Connector 的 Partition 分区状态 |
| **UnionListState** | 联合列表状态 | 故障恢复时各算子获取全部状态 |
| **BroadcastState** | 广播状态 | 配置流广播，实时更新算子行为 |

```java
public class SourceFunctionWithState implements SourceFunction<String> {
    
    private ListState<Long> offsetState;
    
    @Override
    public void initializeState(StateInitializationContext context) {
        offsetState = context.getOperatorStateStore().getListState(
            new ListStateDescriptor<>("offset", Long.class));
    }
    
    @Override
    public void run(SourceContext<String> ctx) {
        for (Long offset : offsetState.get()) {
            // 从上次的 offset 继续消费
            String record = kafkaConsumer.getRecord(offset);
            ctx.collect(record);
        }
    }
}
```

### Keyed State vs Operator State 对比

| 维度 | Keyed State | Operator State |
|------|-------------|----------------|
| **前提** | 必须先 keyBy | 不需要 keyBy |
| **作用范围** | 按 Key 隔离，每个 Key 独立状态 | 整个算子实例共享 |
| **并行度扩缩容** | 自动重新分配（按 Key 路由） | 需实现 CheckpointListener 手动处理 |
| **状态描述符** | ValueStateDescriptor、ListStateDescriptor 等 | ListStateDescriptor、UnionListStateDescriptor |
| **典型使用** | 聚合计算、CEP 模式匹配 | Kafka Source 偏移量、Spark Streaming 监听器 |

### State Backend（状态后端）

Flink 支持多种状态存储后端：

| Backend | 存储介质 | 适用场景 | 性能 |
|---------|----------|----------|------|
| **HashMapStateBackend** | JVM Heap（内存） | 状态小、低延迟 | 最快 |
| **EmbeddedRocksDBStateBackend** | RocksDB（本地磁盘 + 内存缓存） | 大状态、增量 Checkpoint | 支持大状态 |
| **AdvancedRocksDBStateBackend** | 远程 RocksDB（如 HDFS） | 超大规模分布式 | 支持 TB 级状态 |

```java
// 配置 State Backend
env.setStateBackend(new HashMapStateBackend());
// 或使用 RocksDB（需引入依赖）
env.setStateBackend(new EmbeddedRocksDBStateBackend());

// 开启增量 Checkpoint（RocksDB）
env.getCheckpointConfig().enableIncrementalCheckpointing();
```

### 状态过期与清理

```java
// 状态 TTL（过期时间）
StateTtlConfig ttlConfig = StateTtlConfig.newBuilder(Time.minutes(30))
    .setUpdateType(StateTtlConfig.UpdateType.OnReadAndWrite) // 读/写时更新 TTL
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired) // 不返回过期数据
    .cleanupFullSnapshot() // 全量快照时清理过期数据
    .build();

ValueStateDescriptor<String> stateDescriptor = new ValueStateDescriptor<>("myState", String.class);
stateDescriptor.enableTimeToLive(ttlConfig);
```

---

> 📌 **面试重点提示**
> - Flink 架构题要突出"流优先"和"真正流处理"的优势
> - Watermark 题要理解乱序处理的本质——平衡延迟与完整性
> - Checkpoint 题要掌握 Chandy-Lamport 核心流程和两阶段提交
> - 窗口函数题要能用图示说明三种窗口的区别
> - 状态管理题要区分 Keyed State 和 Operator State 的使用场景
