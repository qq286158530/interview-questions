# Spark 面试题 - 2026年8月24日

> 每日5道高质量Spark面试题，涵盖核心知识点

---

## 1. Spark的架构是怎样的？Driver和Executor分别承担什么角色？

**参考来源：** [Apache Spark官方文档 - Cluster Mode Overview](https://spark.apache.org/docs/latest/cluster-overview.html)

**答案：**

Spark采用主从架构（Master-Slave），核心组件包括：

**Driver（驱动器）：**
- 是用户程序的入口点，运行用户的`main()`函数
- 负责创建`SparkContext`（或`SparkSession`），与集群管理器（Cluster Manager）通信
- 将用户程序转化为Task，分发到Executor上执行
- 维护DAG（有向无环图），进行Stage划分和Task调度
- 监控Executor的执行状态，处理异常和失败重试

**Executor（执行器）：**
- 运行在Worker节点上的JVM进程，负责执行具体的Task
- 每个Executor拥有独立的内存和CPU资源
- 将执行结果返回给Driver，或写入外部存储
- 通过Block Manager管理数据缓存（RDD Cache）
- 一个Executor可以同时执行多个Task（由`spark.executor.cores / spark.task.cpus`决定）

**Cluster Manager（集群管理器）：**
- 负责资源的分配和调度，支持Standalone、YARN、Mesos、Kubernetes等模式

整体流程：Driver → 申请资源 → Cluster Manager分配Executor → Driver发送Task → Executor执行 → 结果返回。

---

## 2. RDD是什么？RDD的宽依赖和窄依赖有什么区别？

**参考来源：** [Spark论文 - Resilient Distributed Datasets: A Fault-Tolerant Abstraction for In-Memory Cluster Computing](https://www.usenix.org/system/files/conference/nsdi12/nsdi12-final138.pdf)

**答案：**

**RDD（Resilient Distributed Dataset，弹性分布式数据集）** 是Spark中最基本的数据抽象，具有以下特性：
- **Resilient（弹性）：** 支持容错，可通过血缘（Lineage）信息重新计算丢失的分区
- **Distributed（分布式）：** 数据分布在集群多个节点上
- **Dataset（数据集）：** 是一组只读的、可分区的数据集合

RDD支持两类操作：**Transformation**（惰性求值，如map、filter、groupByKey）和 **Action**（触发计算，如count、collect、saveAsTextFile）。

**窄依赖（Narrow Dependency）：**
- 父RDD的每个分区最多被子RDD的一个分区使用
- 例如：map、filter、union操作
- 优势：可以在同一个Stage内进行流水线（Pipeline）计算，无需Shuffle，数据局部性好

**宽依赖（Wide Dependency）：**
- 父RDD的每个分区可能被子RDD的多个分区使用（涉及Shuffle）
- 例如：groupByKey、reduceByKey、repartition
- 特点：需要跨节点传输数据，产生Shuffle，是Stage划分的边界

Spark的DAG调度器（DAGScheduler）根据宽依赖将Job划分为多个Stage，窄依赖的操作在同一个Stage内流水线执行。

---

## 3. DataFrame和DataSet有什么区别？分别适用于什么场景？

**参考来源：** [Spark官方文档 - Spark SQL, DataFrames and Datasets Guide](https://spark.apache.org/docs/latest/sql-programming-guide.html)

**答案：**

| 特性 | DataFrame | DataSet |
|------|-----------|---------|
| 类型安全 | 编译时无类型检查（弱类型） | 编译时有类型检查（强类型） |
| API风格 | 类似Pandas/SQL的操作 | 函数式编程风格 |
| 序列化 | 使用Tungsten + Encoders | 使用Encoders（Kryo/Java） |
| 语言支持 | Scala、Java、Python、R | 仅Scala、Java |
| 优化 | Catalyst优化器 + Tungsten引擎 | 同DataFrame，支持Catalyst优化 |
| Schema | 有（结构化） | 有（强类型Schema） |

**核心区别：**
- `DataFrame = Dataset[Row]`，即DataFrame是DataSet的一种特例，元素类型为`Row`
- DataFrame是弱类型的，编译时无法检查列名和类型，运行时才会报错
- DataSet是强类型的，编译时就能发现类型错误，IDE支持更好（自动补全、重构）

**使用场景：**
- **DataFrame：** 适合SQL风格的数据分析、ETL处理、Python/R用户、需要Catalyst优化的场景
- **DataSet：** 适合Scala/Java开发者、需要类型安全的复杂业务逻辑、函数式数据处理
- **RDD：** 适合需要精细控制底层逻辑、非结构化数据处理、已有RDD代码的场景

**最佳实践：** 优先使用DataFrame/DataSet，享受Catalyst优化和Tungsten内存管理带来的性能优势。只有在需要底层控制时才使用RDD。

---

## 4. Spark Shuffle是什么？有哪些优化策略？

**参考来源：** [Spark官方文档 - Shuffle Operations](https://spark.apache.org/docs/latest/tuning.html)

**答案：**

**Shuffle** 是Spark中将数据重新分区和分布的过程，通常发生在宽依赖操作（如`groupByKey`、`reduceByKey`、`join`、`repartition`）时。Shuffle涉及大量磁盘I/O、网络I/O和序列化/反序列化，是Spark作业性能的关键瓶颈。

**Shuffle的完整流程：**
1. **Map端：** 每个Map Task将结果写入内存缓冲区，溢写（Spill）到磁盘时按Reducer分区排序
2. **Reduce端：** 通过HTTP从各个Map端拉取（Fetch）对应分区的数据，进行合并排序

**主要优化策略：**

1. **选择合适的算子：**
   - `reduceByKey` 优于 `groupByKey`：reduceByKey在Map端进行预聚合（Combine），减少Shuffle数据量
   - 使用 `aggregateByKey`、`combineByKey` 实现自定义聚合逻辑

2. **调整Shuffle并行度：**
   - 设置 `spark.sql.shuffle.partitions`（默认200）和 `spark.default.parallelism`
   - 并行度过小 → 单个Task数据量大、OOM；并行度过大 → 小文件多、调度开销大
   - 经验法则：每个分区128MB左右

3. **使用广播Join（Broadcast Join）：**
   - 当一侧表较小时（< `spark.sql.autoBroadcastJoinThreshold`，默认10MB），使用广播变量避免Shuffle
   - `df1.join(broadcast(df2), "key")`

4. **启用Tungsten Shuffle：**
   - Spark 2.x+ 默认使用基于排序的Shuffle Manager（SortShuffleManager）
   - 支持堆外内存管理，减少GC开销

5. **AQE（Adaptive Query Execution，自适应查询执行）：**
   - Spark 3.x 引入，运行时动态调整Shuffle分区数
   - `spark.sql.adaptive.enabled=true`

6. **数据倾斜处理：**
   - 使用Salting（加盐）技术打散倾斜的Key
   - 两阶段聚合：先局部聚合，再全局聚合

---

## 5. Spark的内存管理机制是怎样的？Storage内存和Execution内存如何共享？

**参考来源：** [Spark官方文档 - Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management-overview)

**答案：**

Spark 1.6+ 引入了**统一内存管理（Unified Memory Management）** 机制，取代了早期的静态内存管理。

**内存区域划分：**

```
系统预留内存 (300MB)
├── User Memory（用户内存）  ← 用户代码、数据结构
└── Spark Memory（Spark统一管理内存）
    ├── Storage Memory（存储内存）  ← RDD Cache、Broadcast变量、Unroll数据
    └── Execution Memory（执行内存）  ← Shuffle、Join、Sort、Aggregation
```

**关键参数：**
- `spark.memory.fraction`（默认0.6）：Spark Memory占堆内存的比例
- `spark.memory.storageFraction`（默认0.5）：Storage Memory占Spark Memory的初始比例

**共享机制（Unified Memory）的核心思想：**

1. **Storage和Execution共享同一块内存区域**，但各有"保底"份额
2. **Execution可以借用Storage的空闲内存**，但当Storage需要时可以将Execution驱逐的数据写回磁盘
3. **Storage不能驱逐Execution正在使用的内存**
4. 空闲内存的使用权遵循"先到先得"原则

**内存不足时的处理：**
- **Storage内存不足：** 按LRU策略驱逐缓存的RDD分区，被驱逐的分区需要时会重新计算
- **Execution内存不足：** 溢写（Spill）到磁盘，然后合并排序

**GC优化建议：**
- 使用G1GC垃圾收集器（`-XX:+UseG1GC`）
- 合理设置堆内存大小（建议4-8GB，避免过大导致Full GC停顿）
- 使用堆外内存（`spark.memory.offHeap.enabled=true`）减少GC压力
- 监控GC时间，如果GC时间占比超过10%则需要调优

---

> 📌 **学习建议：** 理解Spark的核心原理比记忆API更重要。面试中，结合实际项目经验说明调优过程会更有说服力。

*生成日期：2026-08-24*
