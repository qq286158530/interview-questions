# Redis 面试题 — 2026-08-10

> 本文档整理了 5 道高质量 Redis 面试题，涵盖数据类型、持久化、线程模型、缓存等核心知识点。

---

## 题目一：Redis 和 Memcached 的区别是什么？为什么生产环境优先选择 Redis？

### 参考答案

**共同点：**
- 都是基于内存的数据库，常用于缓存场景
- 都有过期策略
- 两者性能都很高

**核心区别：**

| 维度 | Redis | Memcached |
|------|-------|-----------|
| 数据类型 | 支持 String、Hash、List、Set、ZSet 等丰富类型 | 只支持简单的 key-value 类型 |
| 持久化 | 支持 RDB、AOF、混合持久化，重启后可恢复 | 无持久化功能，数据全在内存，重启即丢失 |
| 集群 | 原生支持主从复制、哨兵、切片集群模式 | 无原生集群，需客户端实现分片 |
| 高级特性 | 支持发布/订阅、Lua 脚本、事务、慢查询分析等 | 不支持 |
| 存储结构 | 使用 SDS（简单动态字符串）存储 value | 使用 prealloc 内存块 |

**为什么选 Redis：**
- Redis 数据类型丰富，能覆盖更多业务场景（如排行榜用 ZSet、计数器用 String）
- 支持数据持久化，生产环境数据安全性更高
- 原生集群支持，分片、故障转移开箱即用
- 社区活跃，生态成熟，是缓存事实标准

> 📎 来源：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目二：Redis 持久化机制有哪些？RDB 和 AOF 的区别是什么？如何选择？

### 参考答案

Redis 共有三种持久化方式：

### 1. RDB（Redis Database）—— 快照方式
- 记录某一时刻的内存数据，以二进制格式写入磁盘
- 使用 `bgsave` 命令时通过 fork 子进程生成 RDB 文件，主线程不被阻塞（利用 COW 写时复制）
- 可配置自动触发：`save 900 1`（900秒内≥1次修改）等条件

### 2. AOF（Append Only File）—— 日志方式
- 每执行一条写操作命令，以追加方式写入文件
- 写回策略（`appendfsync`）有三种：
  - `Always`：每次写命令执行完后同步写盘（最安全，性能最差）
  - `Everysec`（默认）：每秒将缓冲区内容写盘（折中方案）
  - `No`：由操作系统控制何时写盘（性能最好，丢失数据最多）

### 3. 混合持久化（Redis 4.0+）
- AOF 重写时，前半部分以 RDB 格式写入，后半部分以 AOF 增量命令写入
- 兼顾**快速启动**（RDB 部分）和**低数据丢失**（AOF 部分）

### RDB vs AOF 对比

| 特性 | RDB | AOF |
|------|-----|-----|
| 文件体积 | 小（二进制） | 大（命令文本） |
| 恢复速度 | 快（直接读入内存） | 慢（逐条执行命令） |
| 数据完整性 | 可能丢失较多数据 | 取决于写回策略 |
| 快照频率 | 难以把控（太频繁影响性能/太稀疏丢数据） | 可配置 |
| 是否阻塞 | bgsave 不阻塞主线程 | Always 模式会阻塞 |

**如何选择：**
- 一般生产环境建议 **同时开启 RDB + AOF**，用混合持久化效果最佳
- 如果只在意性能且可容忍数据丢失，可仅用 RDB

> 📎 来源：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目三：Redis 是单线程吗？为什么单线程的 Redis 还能保持这么高的性能？

### 参考答案

**Redis 确实是单线程的吗？**

这里要分两个层面理解：

- ✅ **Redis 的主要工作（接收客户端请求、解析请求、执行命令、返回结果）是单线程**，这是我们常说"Redis 单线程"的原因
- ❌ **Redis 程序并不完全是单线程**，它还有后台线程：
  - Redis 2.6：2 个后台线程（关闭文件、AOF 刷盘）
  - Redis 4.0+：新增 lazyfree 线程，异步释放大 key 内存
  - Redis 6.0+：3 个 I/O 线程分担网络 I/O（但命令执行仍是单线程）

**Redis 单线程为什么这么快？**

1. **内存操作 + 高效数据结构**：Redis 数据都在内存中，操作瓶颈是内存或网络带宽而非 CPU
2. **无锁竞争**：单线程避免了多线程间的加解锁、死锁、线程切换开销
3. **I/O 多路复用**：使用 epoll/select/kqueue 机制，一个线程同时监听处理多个 Socket 请求
4. **高效数据结构**：SDS（动态字符串）、跳表、压缩列表、quicklist 等内部结构经过精心设计

**Redis 6.0 引入多线程 I/O 的原因：**
随着网络硬件性能提升，网络 I/O 处理成为瓶颈，因此将网络 I/O 读写分给多个线程并行处理（命令执行仍为单线程），性能提升至少一倍以上。

> 📎 来源：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目四：Redis 的过期键删除策略有哪些？内存淘汰机制是什么？

### 参考答案

### 一、过期键删除策略（针对已设置过期时间的 key）

Redis 有三种过期键删除策略：

| 策略 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **定时删除** | 设置过期时间时创建定时器，到期立即删除 | 内存及时释放，CPU 友好 | 创建大量定时器，占用 CPU 资源 |
| **惰性删除** | 访问 key 时检查是否过期，过期则删除 | 对 CPU 友好，不额外消耗资源 | 过期键可能长期占用内存 |
| **定期删除** | 每隔一段时间扫描数据库，删除过期键 | 折中方案，定时清理 | 执行频率难以把控 |

Redis 采用的是 **惰性删除 + 定期删除** 组合策略，兼顾内存和CPU。

### 二、内存淘汰机制（内存达到 maxmemory 时的策略）

Redis 8 种淘汰策略（`maxmemory-policy` 配置项）：

| 策略 | 说明 |
|------|------|
| `noeviction` | 不淘汰，返回写操作错误（默认） |
| `volatile-lru` | 从已设置过期时间的 key 中淘汰最近最少使用的 |
| `volatile-lfu` | 从已设置过期时间的 key 中淘汰使用频率最低的 |
| `volatile-ttl` | 从已设置过期时间的 key 中淘汰剩余 TTL 最短的 |
| `volatile-random` | 从已设置过期时间的 key 中随机淘汰 |
| `allkeys-lru` | 从所有 key 中淘汰最近最少使用的 |
| `allkeys-lfu` | 从所有 key 中淘汰使用频率最低的 |
| `allkeys-random` | 从所有 key 中随机淘汰 |

**如何选择：**
- 如果数据访问均匀分布 → `allkeys-lru` 或 `allkeys-lfu`
- 如果有明确热点数据 → `volatile-lru`
- 如果所有 key 都有 TTL → 优先用 volatile 策略
- 写多读少且内存紧张 → `allkeys-lru`

> 📎 来源：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目五：Redis 主从复制原理是什么？哨兵模式解决了什么问题？

### 参考答案

### 一、主从复制原理

Redis 主从复制采用 **一主多从** 架构，主节点负责写，从节点负责读（读写分离）。

**复制过程：**
1. 从节点保存主节点地址（`replicaof masterip port`）
2. 从节点向主节点发起 socket 连接
3. 从节点发送 `PING` 确认主节点可用
4. 主节点授权后，进行数据同步（全量复制或增量复制）
5. 后续主节点写操作通过 **命令传播** 异步同步给从节点

**全量复制 vs 增量复制：**
- **全量复制**：首次同步时，主节点将所有数据通过 bgsave 生成 RDB，发送给从节点
- **增量复制**：主节点维护一个 **复制积压缓冲区（repl_backlog）**，记录最近执行的写命令，从节点断线重连后从这里补数据

> ⚠️ 注意：主从复制是**异步**的，主节点不等从节点执行完就返回客户端，因此无法保证强一致性。

### 二、哨兵模式（Sentinel）

**解决了什么问题：**
主从模式下，主节点故障时需要人工手动切换。哨兵模式实现了**自动化故障转移（Failover）**。

**核心功能：**
1. **监控**：哨兵进程监控主从节点的运行状态
2. **自动故障转移**：主节点不可达时，哨兵自动选举从节点晋升为新主节点，并通知其他从节点切换
3. **通知**：向客户端推送故障转移事件
4. **配置提供者**：客户端可通过哨兵获取当前主节点地址

**哨兵集群**：生产环境至少部署 3 个哨兵实例，遵循 **少数服从多数** 原则进行故障判定，避免单点故障。

**故障转移过程：**
1. 主观下线（单个哨兵认为主节点不可达）
2. 客观下线（哨兵集群投票达成共识）
3. 哨兵集群选举出一个 leader 负责故障转移
4. 从节点投票选出新主节点
5. 旧主节点恢复后降级为从节点

> 📎 来源：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

*本文由 AI 面试题助手自动整理 | 来源：小林coding图解Redis系列*
