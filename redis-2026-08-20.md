# Redis 面试题（2026-08-20）

> 本系列面试题由 AI 助手整理，每日定时推送，已归档至 GitHub：[qq286158530/interview-questions](https://github.com/qq286158530/interview-questions)

---

## 题目一：Redis 的数据类型有哪些？分别适用于哪些场景？

### 参考答案

Redis 支持 5 种基础数据类型和 3 种高级数据结构：

**基础类型：**

| 类型 | 数据结构 | 典型场景 |
|------|---------|---------|
| `String` | 简单字符串、整数、浮点数 | 缓存、计数器、分布式锁（SETNX） |
| `Hash` |field-value 映射 | 存储对象，如用户信息、商品详情 |
| `List` | 双向链表 | 消息队列（LPUSH+BRPOP）、最新列表、关注列表 |
| `Set` | 无序不重复集合 | 标签系统、好友关系、共同关注、去重 |
| `Zset`（Sorted Set）| 有序不重复集合（score 排序） | 排行榜、延时队列、带权重的优先级队列 |

**高级类型（由以上类型扩展）：**

- **Bitmap**：位图，适合签到、在线状态布隆过滤计数（`SETBIT`/`GETBIT`）
- **HyperLogLog`：基数统计，如 UV 统计，内存极小（`PFADD`/`PFCOUNT`）
- **Geospatial**：地理位置附近的人（`GEOADD`/`GEORADIUS`）

**实战 tip：** 如果不确定选什么，用 `String` 存 JSON 是最保险的，但会失去原子操作能力；需要频繁修改对象字段时用 `Hash`。

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？如何选择？

### 参考答案

Redis 提供了两种持久化机制：

**RDB（Redis Database）：**
- 定时将内存中的**全量数据快照** dump 成 `.rdb` 文件
- 可手动触发（`SAVE`/`BGSAVE`）或自动触发（配置 `save` 秒数和写次数）
- 恢复快，但可能丢失最后一次快照之后的数据

**AOF（Append Only File）：**
- 将每个写命令追加到 `.aof` 文件
- 支持三种同步策略：`always`（每条都写）/ `everysec`（每秒一次）/ `no`（由 OS 决定）
- 数据完整性高，但文件体积大，恢复慢

**核心区别对比：**

| 维度 | RDB | AOF |
|------|-----|-----|
| 文件体积 | 小（紧凑快照） | 大（累积所有写命令） |
| 写入方式 | 阻塞 fork 子进程写 | 逐条追加，fork bgrewriteaof 压缩 |
| 丢失数据 | 可能丢失一个周期 | 可配置丢失秒数 |
| 恢复速度 | 快（二进制直接加载） | 慢（重放命令） |
| 资源消耗 | fork 时短暂阻塞 | 持续占用磁盘 IO |

**生产环境建议：**
- 小型项目或能接受分钟级数据丢失 → 仅 RDB
- 对数据完整性要求高 → **同时开启 RDB + AOF**（AOF 优先），这是生产标配
- 开启 AOF 时建议用 `everysec` 策略，平衡性能与安全

---

## 题目三：Redis 的过期 key 删除策略有哪几种？什么是缓存淘汰（eviction）策略？

### 参考答案

**过期 key 删除策略（主动/被动）：**

1. **惰性删除（Lazy Expire）**：访问 key 时才检查是否过期，过期则删除。对 CPU 友好，但过期 key 可能长期占用内存。
2. **定期删除（Active Expire）**：Redis 每隔一段时间（默认 100ms）随机抽取设置了过期时间的 key 检查，删除过期的。平衡 CPU 与内存。
3. **定时删除（TTL 定时器）**：为每个 key 创建定时器，到期立即删除。对 CPU 最不友好，一般不推荐。

> Redis 采用的是「惰性删除 + 定期删除」组合策略。

**内存淘汰策略（Eviction Policies）：**

当 Redis 内存达到 `maxmemory` 上限时，会根据配置的淘汰策略腾出空间：

| 策略 | 含义 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `allkeys-lru` | 在所有 key 中淘汰最近最少使用的 |
| `allkeys-random` | 在所有 key 中随机淘汰 |
| `volatile-lru` | 只在设置了过期时间的 key 中淘汰 LRU |
| `volatile-random` | 只在设置了过期时间的 key 中随机淘汰 |
| `volatile-ttl` | 淘汰最快过期的（TTL 最短的） |
| `volatile-lfu` | 只在设置了过期时间的 key 中淘汰最不常用的 |
| `allkeys-lfu` | 在所有 key 中淘汰最不常用的 |

**实战 tip：** 缓存场景通常用 `allkeys-lru`（热点数据自动保留）；需要保证session不过期用 `volatile-lru`；不确定时用 `allkeys-lru`。

---

## 题目四：Redis 主从复制原理是什么？哨兵（Sentinel）和集群（Cluster）有什么区别？

### 参考答案

**主从复制原理：**

1. 从节点（replica）发送 `PSYNC ? -1` 请求全量同步
2. 主节点执行 `BGSAVE` 生成 RDB，同时将新写命令写入 **replication buffer** 和 **replication backlog**
3. RDB 快照发送给从节点，从节点清空本地数据后加载 RDB
4. 主节点将复制缓冲区中的增量命令发给从节点（命令传播阶段）
5. 之后主节点每执行一条写命令，就同步推送给从节点（**长连接命令传播**）

若从节点断开后重连，支持 **部分重同步**（`PSYNC <runid> <offset>`），利用 backlog 环形缓冲区只同步断连期间的增量。

**哨兵（Sentinel）vs 集群（Cluster）：**

| 维度 | 哨兵模式 | 集群模式 |
|------|---------|---------|
| 定位 | **高可用**：主从自动故障转移 | **分布式存储**：数据分片 + 高可用 |
| 数据分片 | 无，每节点存全量数据 | 有，16384 个 slot，自动分配到 6+ 节点 |
| 写能力 | 主节点单点写 | 每个分片的主节点可写（写操作只在对应主节点） |
| 故障转移 | 哨兵投票选新主 | 分片内主节点故障，该分片从节点自动晋升 |
| 节点数量 | 1 主 + N 从 | 至少 6 节点（3 主 3 从）|
| 跨分片查询 | 支持（客户端聚合） | 需要 `MGET`/`MSET` 跨 slot 时用 hash tag |
| 适用场景 | 数据量小，需要读写分离 | 数据量大，需要水平扩展 |

**生产建议：** 数据量 < 10GB 用哨兵即可；数据量大或需要高并发写入用 Cluster。

---

## 题目五：什么是缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 参考答案

这三个问题是缓存失效引发的典型线上故障：

**① 缓存穿透（Cache Penetration）**

- **定义**：查询一个**既不存在于缓存也不存在于数据库**的数据，每次都打到 DB。
- **危害**：大量恶意/无效请求击穿缓存，直接压垮数据库。
- **解决方案：**
  - **布隆过滤器（BloomFilter）**：将所有合法 key 存入 BloomFilter，查询前先过滤，不存在直接返回。
  - **缓存空值**：对查不到的结果也在缓存中存一个 `null`（或特定值），并设置短过期时间。
  - **参数校验**：对非法参数（如负数 ID）直接拦截。

**② 缓存击穿（Cache Break-in）**

- **定义**：某个**热点 key 过期**的瞬间，大量并发请求同时涌入，直接打穿到 DB。
- **特点**：只有1个热点 key，问题集中。
- **解决方案：**
  - **互斥锁（SETNX）**：只允许1个请求去查 DB 并回填缓存，其他等待。
  - **逻辑过期**：不给热点 key 设置真正过期时间，而是存一个"逻辑过期时间"字段，后台异步更新。
  - **热点数据永不过期**：用后台异步刷新替代主动过期。

**③ 缓存雪崩（Cache Avalanche）**

- **定义**：大量 key **在同一时间过期**，导致大量请求同时击穿缓存打到 DB。
- **解决方案：**
  - **过期时间随机化**：给 key 的 TTL 加一个随机偏移量（如 `baseTTL + random(0, 60s)`）。
  - **多级缓存**：L1（本地 Caffeine/Guava） + L2（Redis），避免同时失效。
  - **Redis 高可用**：主从 + 哨兵/集群，避免 Redis 宕机导致所有缓存失效。
  - **服务降级/熔断**：DB 压力大时返回兜底数据（如"系统繁忙"）。

**三者对比总结：**

| 问题 | 根因 | 范围 | 核心解法 |
|------|------|------|---------|
| 穿透 | 查询不存在的数据 | 全量无效 key | 布隆过滤器 + 缓存空值 |
| 击穿 | 热点 key 过期 | 单个热点 key | 互斥锁 / 逻辑过期 |
| 雪崩 | 大量 key 同时过期 | 大量 key | 随机 TTL + 多级缓存 + 高可用 |

---

> 📌 本题库由 AI 定时整理推送，欢迎 Star！
> GitHub: https://github.com/qq286158530/interview-questions
> 提交日期：2026-08-20
