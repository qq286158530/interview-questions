# Redis 面试题整理（2026-08-04）

> 本文档整理了 5 道高质量 Redis 面试题，涵盖数据类型、持久化、集群、缓存等核心知识点。题目参考了业界经典面试题库，内容经过校验，确保准确可靠。

---

## 题目一：Redis 有哪些数据类型？各自的使用场景是什么？

### 参考答案

Redis 支持以下五种基础数据类型：

| 数据类型 | 底层实现 | 典型使用场景 |
|----------|----------|-------------|
| **String** | SDS（简单动态字符串） | 缓存简单值、计数器、限流、分布式锁（SET NX） |
| **List** | QuickList（压缩列表+双向链表） | 消息队列（LPUSH+BRPOP）、最新列表、评论系统 |
| **Hash** | 压缩列表 / 哈希索引 | 存储对象（如用户信息）、购物车数据 |
| **Set** | 整数集合 / 哈希表 | 去重、标签系统、共同好友、抽奖 |
| **Sorted Set（ZSet）** | 压缩列表 / 跳表 | 排行榜、延迟队列、有序通知 |

此外还有三种高级类型：

- **Bitmaps**：位图，适合做签到、活跃用户统计
- **HyperLogLog**：基数估算，适合 UV 统计（误差约 0.81%）
- **Geospatial**：地理空间，适合附近的人、位置服务

**加分项**：Redis 7.0 引入了 **Multi-part AOF** 和 **Config set** 等特性，以及 **ACL** 权限管理。面试时可以提到 Redis 6.x 的多线程 IO 模型（只是辅助提升网络读写效率，核心命令执行仍是单线程）。

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？如何选择？

### 参考答案

Redis 提供两种持久化机制：

#### RDB（Redis Database）

- **原理**：定时将内存中的全部数据快照（point-in-time snapshot）写入磁盘，生成 `.rdb` 文件。
- **触发方式**：
  - 手动：`SAVE`（阻塞）或 `BGSAVE`（后台异步）
  - 自动：按配置规则（如 `save 900 1`、`save 300 10`）
- **优点**：恢复速度快，适合做冷备份
- **缺点**：可能丢失最后一次快照之后的数据

#### AOF（Append Only File）

- **原理**：每个写命令追加到 `appendonly.aof` 文件，支持三种同步策略：
  - `always`：每次写都同步（最安全，性能最差）
  - `everysec`（默认）：每秒同步一次
  - `no`：由操作系统决定何时同步（性能最好，可能丢失较多数据）
- **重写（Rewrite）**：AOF 文件过大时，Redis 会执行 `BGREWRITEAOF`，压缩冗余命令
- **优点**：数据安全性更高，支持多种同步策略
- **缺点**：文件体积通常比 RDB 大，恢复速度较慢

#### 混合持久化（Redis 4.0+）

- 开启 `aof-use-rdb-preamble yes` 后，AOF 重写时先用 RDB 格式写入数据头，后续增量用 AOF 格式。既能快速恢复，又能保证数据安全。

#### 如何选择？

- 如果能接受分钟级别的数据丢失 → 仅用 RDB
- 如果对数据安全要求高 → 优先 AOF（everysec 策略）
- 最佳实践 → 同时开启 RDB + AOF，优先用 AOF 恢复

> 📎 参考来源：Redis 官方文档 - Persistence（https://redis.io/docs/management/persistence/）

---

## 题目三：Redis 的过期 key 删除策略有哪些？什么是内存淘汰机制？

### 参考答案

#### 过期 key 删除策略

Redis 有两种删除策略：

1. **惰性删除（Lazy Expires）**
   - 访问 key 时检查是否过期，过期则删除
   - 优点：对 CPU 友好，不额外消耗资源
   - 缺点：过期 key 长期不被访问会一直占用内存

2. **定时删除（Scheduled Expires）**
   - Redis 每秒随机抽取一定数量的 key 检查过期并删除
   - 由 `redis.conf` 中的 `hz` 参数控制扫描频率（默认 10，即每秒 10 次）
   - 优点：及时释放内存
   - 缺点：对 CPU 有一定开销

> 注意：Redis 并不保证所有过期的 key 都会被立即删除，上述两种策略配合使用。

#### 内存淘汰机制（Maxmemory Policy）

当 Redis 内存达到 `maxmemory` 上限时（默认 `0` 即不限制），通过以下策略淘汰 key：

| 策略 | 行为 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 从已设置过期时间的 key 中淘汰最近最少使用的 |
| `volatile-ttl` | 从已设置过期时间的 key 中淘汰剩余 TTL 最短的 |
| `volatile-random` | 从已设置过期时间的 key 中随机淘汰 |
| `volatile-lfu` | 从已设置过期时间的 key 中淘汰使用频率最低的（Redis 4.0+） |
| `allkeys-lru` | 从所有 key 中淘汰最近最少使用的 |
| `allkeys-random` | 从所有 key 中随机淘汰 |
| `allkeys-lfu` | 从所有 key 中淘汰使用频率最低的（Redis 4.0+） |

**实战建议**：
- 生产环境务必设置 `maxmemory`，并根据业务选择合适的淘汰策略
- 如果缓存数据有明确冷热区分，推荐 `allkeys-lru` 或 `volatile-lru`
- 避免使用 `noeviction`（除非明确知道数据不会超限）

---

## 题目四：Redis 主从复制原理是什么？哨兵模式解决了什么问题？

### 参考答案

#### 主从复制（Replication）

Redis 主从复制采用 **PSYNC** 协议（Redis 2.8+ 替代了老的 SYNC），支持全量同步和增量同步：

**全量同步流程**：
1. 从节点（replica）向主节点发送 `PSYNC ? -1`
2. 主节点执行 `BGSAVE`，生成 RDB 快照，同时将后续写命令写入缓冲区
3. 主节点将 RDB 文件发送给从节点
4. 从节点加载 RDB 文件，同步数据
5. 主节点将缓冲区中的增量命令发送给从节点（Master -> PSYNC runid offset）

**增量同步（命令传播）**：
- 主节点维护一个 ** replication backlog** 环形缓冲区（默认 1MB）
- 从节点记录自身 `master_repl_offset`，断线重连后发送 `PSYNC <runid> <offset>`
- 主节点对比 offset，将差距的增量命令发送给从节点

**重要参数**：
- `repl-diskless-sync`：无盘复制，网络效率更高
- `replica-read-only`：从节点默认只读（可通过 `config set replica-read-only no` 改）

#### 哨兵模式（Sentinel）

哨兵是 Redis 的高可用解决方案，核心解决以下问题：

1. **故障自动检测**：哨兵通过心跳机制（PING）检测主节点是否宕机
2. **自动故障转移（failover）**：当主节点不可用时，哨兵自动将从节点升级为新的主节点
3. **配置中心**：客户端通过哨兵获取当前的主节点地址，实现透明切换

哨兵集群通常部署 **3 台及以上**（奇数台），通过 **Raft 协议** 投票选出领导者执行故障转移，避免脑裂。

**配置示例**（`sentinel.conf`）：
```
sentinel monitor mymaster 127.0.0.1 6379 2
sentinel down-after-milliseconds mymaster 30000
sentinel failover-timeout mymaster 180000
```

> 📎 参考来源：Redis 官方文档 - Replication（https://redis.io/docs/management/replication/）

---

## 题目五：什么是缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 参考答案

这是 Redis 和缓存系统中最经典的高频面试题，合称"缓存三连"。

#### 1. 缓存穿透

**定义**：查询一个根本不存在的数据，缓存没有，数据库也没有。每次请求都打到数据库。

**危害**：恶意攻击者大量请求不存在的数据，直接打垮数据库。

**解决方案**：
- **布隆过滤器（BloomFilter）**：将所有存在的数据 hash 到 BitMap，请求过来先查布隆过滤器，存在才查 Redis/数据库
- **缓存空值**：即使数据不存在，也将 `null` 写入 Redis（设置较短 TTL，如 30s）
- **参数校验**：接口层对 id 等参数做基础校验，过滤明显非法请求

#### 2. 缓存击穿

**定义**：某个热点 key 过期或失效的瞬间，大量并发请求涌入，直接打垮数据库（同一个 key）。

**解决方案**：
- **互斥锁（分布式锁）**：SETNX + SETEX，只允许一个线程从数据库加载数据，其他线程等待并从缓存读
- **热点数据永不过期**：物理不过期 + 逻辑过期（后台异步更新）
- **提前重建**：异步检测热点 key 即将过期，主动续期或提前重建

#### 3. 缓存雪崩

**定义**：大量 key 在同一时间集中过期，或者 Redis 突然宕机，导致大量请求直接打到数据库。

**解决方案**：
- **过期时间随机化**：给 key 的 TTL 加随机偏移量，避免集中过期
- **多级缓存**：本地缓存（如 Caffeine/Guava）+ Redis + 数据库
- **Redis 高可用**：部署集群（主从 + 哨兵 / Cluster），避免单点故障
- **服务熔丝 / 限流**：Hystrix、Sentinel 等，数据库承受不住时快速拒绝部分请求
- **预热**：系统启动时主动加载热点数据到缓存

---

## 总结

| 知识点 | 掌握程度 |
|--------|---------|
| Redis 五种基础数据类型 | 必须精通 |
| RDB / AOF 持久化原理 | 理解差异与选型 |
| 过期删除 + 内存淘汰策略 | 理解惰性删除 + LRU/LFU |
| 主从复制原理 + 哨兵模式 | 理解 PSYNC + failover |
| 缓存三连（穿透/击穿/雪崩） | 理解原因 + 对应解决方案 |

> 📎 更多面试题可在 GitHub 仓库 `qq286158530/interview-questions` 中持续更新。
