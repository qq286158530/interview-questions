# Spark 面试题 — 2026-08-02

> 每日精选5道高质量Spark面试题，涵盖核心知识点。  
> 来源：[DataFlair - Top 100 Apache Spark Interview Questions](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目一：Spark RDD 的 transformation 为什么要设计成懒加载（Lazy Evaluation）？

### 参考答案

RDD 的 transformation 操作（如 `map`、`filter`、`flatMap` 等）并不会立即执行计算，而是只记录血缘关系（Lineage），构建 DAG（有向无环图）。只有当遇到 Action 操作（如 `collect`、`count`、`saveAsTextFile`）时，DAG 才会被提交给 DAGScheduler 进行调度执行。

**懒加载的好处：**

1. **减少中间结果物化**：避免不必要的Shuffle和数据落盘，节省内存和磁盘 I/O。
2. **优化执行计划**：Spark 在 Action 触发时，可以基于整个 DAG 做整体优化（如 pipeline、代码生成），而不是逐个算子执行。
3. **容错机制更高效**：如果某个分区丢失，只需根据 Lineage 重新计算该分区，而不需要重算整个链路。
4. **减少数据跨网络传输**：懒加载让 Spark 有机会把可以合并的 shuffle 合并，减少网络传输。

**简单示例：**
```scala
val rdd = sc.textFile("hdfs://data.log")  // Lazy
  .map(_.toUpperCase)                      // Lazy
  .filter(_.contains("ERROR"))             // Lazy
// 此时没有任何计算发生
val result = rdd.count()                    // Action — 触发真正计算
```

> **来源：** [DataFlair - What is lazy evaluation in Spark?](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目二：reduceByKey 和 groupByKey 的区别是什么？为什么优先使用 reduceByKey？

### 参考答案

| 特性 | `reduceByKey` | `groupByKey` |
|------|--------------|-------------|
| **计算位置** | 在 Map 端先做局部聚合（combiner），再 shuffle | 不做任何局部聚合，直接 shuffle 所有数据 |
| **网络传输量** | 小（聚合后传输） | 大（原始数据全部 shuffle） |
| **内存消耗** | 低（combiner 减少数据量） | 高（相同 key 的所有 value 存在一个集合） |
| **时间复杂度** | O(n) | O(n log n)（需要排序/哈希） |

**为什么优先用 reduceByKey：**

`reduceByKey` 会在 shuffle 之前先在本地分区进行局部 reduce（combiner），相当于做了一次预聚合。这样：
- **显著减少网络 I/O**：shuffle 的数据量大幅下降。
- **降低内存压力**：combiner 阶段已经合并了数据，不需要把所有相同 key 的 value 都加载到内存。

**反面例子（不推荐）：**
```scala
// ❌ 大量数据 shuffle，极易 OOM
rdd.groupByKey().mapValues(_.sum)
```

**推荐写法：**
```scala
// ✅ 先局部聚合再 shuffle，性能好
rdd.reduceByKey(_ + _)
```

> **来源：** [DataFlair - groupByKey vs reduceByKey](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目三：Spark 的广播变量（Broadcast Variable）是什么？如何使用？有什么注意事项？

### 参考答案

**什么是广播变量：**

广播变量是 Spark 提供的共享变量机制，用于将一个**只读的、小型的**数据集缓存到每个 worker 节点的内存中，而不是每个 task 都需要从 driver 拉取一份副本。

**如何使用：**
```scala
// 定义广播变量
val broadcastVar = sc.broadcast(Array(1, 2, 3, 4, 5))

// 在算子中使用
val result = rdd.map(x => x * broadcastVar.value.sum)
```

**核心原理：**
- Driver 端调用 `broadcast()` 将数据序列化并分发到所有 executor（每个节点一份）。
- Executor 上的所有 task 共享这份数据，无需重复传输。
- 使用 `broadcastVar.value` 读取。

**注意事项：**

1. **只适合小数据集**：数据太大会导致 executor 内存不足。
2. **只读不变**：广播后不能修改，否则可能造成数据不一致。
3. **选择合适的序列化方式**：广播变量默认使用 Java/Kryo 序列化，大数据量时建议使用 `spark.serializer=org.apache.spark.serializer.KryoSerializer`。
4. **自动广播优化**：Spark SQL 中，join 小表时自动使用广播 join（BroadcastHashJoin），无需手动 broadcast。

**典型使用场景：**
- 维度表 join（事实表很大，维度表很小）。
- 配置参数表。
- 机器学习中的特征向量/模型参数。

> **来源：** [DataFlair - What are shared variables in Apache Spark?](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目四：Spark 的 Shuffle 机制是什么？为什么 Shuffle 是性能瓶颈？

### 参考答案

**什么是 Shuffle：**

Shuffle 是 Spark 在 **需要跨分区重新分布数据** 时发生的操作。当一个算子需要读取其他分区的数据才能完成计算时（比如 `groupByKey`、`join`、`reduceByKey`），Spark 不得不将数据按照 key 进行重新分区，这个跨节点/跨分区传输数据的过程就是 Shuffle。

**执行流程（以 ReduceByKey 为例）：**
1. **Map 阶段**：每个 partition 计算出自己的 (key, value) 中间结果。
2. **Write 阶段**：按照 key 的 hash 值写入本地磁盘文件（同时为每个下游 partition 生成单独的文件）。
3. **Fetch 阶段**：每个 reduce task 去上游所有 map task 的输出文件中拉取属于自己的那部分数据。
4. **Reduce 阶段**：对拉取到的数据进行聚合。

**为什么是性能瓶颈：**

1. **磁盘 I/O**：Shuffle 中间数据先写磁盘再读磁盘，涉及大量磁盘读写。
2. **网络 I/O**：数据需要在网络中传输，占用带宽。
3. **内存压力**：Shuffle 数据可能超过内存，需要 spill 到磁盘（spill 过程本身也很慢）。
4. **高基数 key 问题**：某些 key 数据量特别大，导致数据倾斜（data skew），拖慢整个 job。
5. **启动开销**：每次 shuffle 都涉及大量小文件的创建和网络连接建立。

**优化建议：**
- 使用 `reduceByKey`/`aggregateByKey` 替代 `groupByKey`，减少 shuffle 数据量。
- 合理设置 `spark.shuffle.file.buffer`（默认 32KB）和 `spark.reducer.maxSizeInFlight`。
- 避免数据倾斜：加盐（salting）、自定义 Partitioner。
- 升级到 Spark 3.x 使用 AQE（Adaptive Query Execution）动态优化 shuffle。

> **来源：** [DataFlair - How can data transfer be minimized when working with Apache Spark?](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目五：Spark 的内存管理模型是怎样的？Executor 内存是如何划分的？

### 参考答案

Spark 采用**统一内存管理**（Unified Memory Management，从 Spark 1.6 开始引入），将堆内内存分为两大类：**存储内存（Storage Memory）**和**执行内存（Execution Memory）**，二者可以动态借用。

**内存区域划分（spark.executor.memory）：**

```
┌─────────────────────────────────────────────────┐
│                   堆内内存                        │
│  ┌───────────────────┬───────────────────────┐  │
│  │    存储内存         │     执行内存           │  │
│  │  (Storage)        │   (Execution)         │  │
│  │  - RDD Cache      │  - Shuffle buffer     │  │
│  │  - Broadcast data │  - Hash table         │  │
│  │  - Internal meta  │  - Sort buffer        │  │
│  └───────────────────┴───────────────────────┘  │
│  ┌──────────────────────────────────────────┐   │
│  │         用户内存 (User Memory)             │   │
│  │  - 用户数据结构 & 引用                      │   │
│  └──────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────┐   │
│  │         预留内存 (Reserved Memory)         │   │
│  │  300MB — 系统内部使用                      │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

**各区域说明：**

| 区域 | 默认占比 | 用途 |
|------|---------|------|
| **存储内存** | 约 50%（可动态扩展至 75%） | RDD 缓存（`cache()`/`persist()`）、广播变量 |
| **执行内存** | 约 50%（可动态扩展至 75%） | Shuffle 数据聚合、排序、HashJoin 哈希表 |
| **用户内存** | 约 25% | 用户自定义数据结构、RDD 算子内部对象 |
| **预留内存** | 300MB | Spark 内部元数据，不能被用户使用 |

**动态借用机制：**
- 当执行内存不足时，可以借用存储内存，反之亦然。
- 但存储内存被占用时，**不会强制回收**正在使用的缓存数据（只能等待被驱逐）。
- 执行内存有更强的抢占优先级。

**Spark 3.x 的变化（统一内存管理增强）：**
- 引入 AQE（Adaptive Query Execution），动态调整 shuffle 分区数。
- 新增 `spark.memory.storageFraction` 控制存储内存的保留下限。
- Off-heap 内存管理（`spark.memory.offHeap.enabled`）允许使用堆外内存。

**常见 OOM 原因：**
- 缓存数据过多（`Storage.MEMORY_AND_DISK` 满后按 LRU 淘汰）。
- 执行内存超限（如大 key 导致 HashMap 膨胀）。
- 用户内存被耗尽（大量创建大对象）。

> **来源：** [DataFlair - Explain RDD properties](https://data-flair.training/blogs/spark-interview-questions/)

---

*每日 Spark 面试题推送 — 由 OpenClaw 自动生成*
