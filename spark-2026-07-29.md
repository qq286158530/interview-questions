# Spark 核心面试题 · 2026-07-29

> 本文档收录 5 道高质量 Spark 面试题，涵盖 RDD、DataFrame、Shuffle、内存管理、广播变量等核心知识点。

---

## 题目一：RDD 为什么是惰性求值的？Action 操作的执行流程是怎样的？

### 参考答案

**RDD 采用惰性求值（Lazy Evaluation）的原因：**

RDD（Resilient Distributed Dataset）通过血统（Lineage）机制记录血统链（依赖关系），只有在遇到 **Action** 操作时才会真正触发计算。这种设计有两方面好处：

1. **减少不必要的计算**：Transformations 只是记录计算逻辑，不立即执行，可以避免中间结果的无意义落地。比如对同一 RDD 做多次 `filter`，Spark 会自动将它们合并成一条链，只扫描一遍数据。
2. **优化执行计划**：Spark 在 Action 触发前有机会对 DAG 进行优化（如 pipeline、减少 Shuffle），形成更优的执行策略。

**Action 操作的完整执行流程：**

1. 用户调用 Action（如 `collect`、`count`、`saveAsTextFile`）
2. Spark 从后向前根据 RDD 的血统链（Lineage）构建 **DAG（Directed Acyclic Graph）**
3. DAG 被提交给 **DAGScheduler**，DAGScheduler 根据 Shuffle 边界将 DAG 划分为多个 **Stage**
4. 每个 Stage 包含若干可以流水线执行的 Task，Stage 之间的边界就是 Shuffle 的位置
5. TaskScheduler 将 Task 调度到集群的 Executor 上并行执行
6. 结果最终返回给 Driver

> **来源说明**：本题为 Spark 基础知识经典题，参考 Apache Spark 官方文档 https://spark.apache.org/docs/latest/rdd-programming-guide.html

---

## 题目二：DataFrame 和 RDD 的区别是什么？为什么企业开发中更推荐使用 DataFrame？

### 参考答案

| 维度 | RDD | DataFrame |
|------|-----|-----------|
| **数据结构** | 底层抽象，无结构信息 | 类似二维表，有 schema（列名+类型） |
| **优化机制** | 无自动优化，依赖用户写法 | Catalyst 优化器自动优化执行计划 |
| **类型安全** | 编译时类型安全（Scala/Java） | 运行时才检查类型（Row 类型） |
| **API 风格** | 函数式，底层灵活 | SQL-like，上层友好 |
| **序列化** | JVM 对象，低效 | Tungsten 二进制序列化，高效 |
| **外部数据源** | 需要手写输入输出 | 内置支持 Parquet、ORC、JSON、JDBC 等 |

**为什么更推荐 DataFrame：**

1. **性能更好**：Spark SQL 的 Catalyst 优化器会对 DataFrame/Dataset 的执行计划进行优化，包括列剪裁、谓词下推、常量折叠、代码生成等，在无需用户干预的情况下显著提升性能。
2. **开发效率高**：可以用 SQL、DSL 两种方式操作数据，学习成本低。
3. **生态兼容好**：与 Spark Streaming、Spark MLlib、Spark GraphFrame 等无缝衔接。
4. **多语言统一**：Python、 Scala、Java、R、SQL 均可使用同一套 DataFrame API。

> **来源说明**：本题为 Spark 核心概念对比题，参考 Databricks 博客 https://databricks.com/blog/2015/02/17/introducing-dataframes-in-spark-for-large-scale-data-science.html

---

## 题目三：Spark Shuffle 的原理是什么？为什么 Shuffle 是性能瓶颈？如何优化 Shuffle？

### 参考答案

**Shuffle 的原理：**

Shuffle 是 Spark 中将数据按照某种规则重新分区的过程，本质是将 Map 端的结果（按 Reducer 划分）写入磁盘文件，然后由 Reduce 端拉取。当 RDD 依赖链中遇到 `groupByKey`、`reduceByKey`、`join` 等需要跨分区聚合的操作时，就会产生 Shuffle。

执行流程：
1. **Map 端**：ShuffleWriter 将数据写入本地磁盘，分为多个 bucket（每个 Reducer 一个）
2. **Shuffle 文件写入**：数据先写入缓冲区，满了后排序溢写（spill）到磁盘
3. **Reduce 端**：Executor 通过 BlockStore 或其他 ShuffleService 拉取属于本分区的数据
4. **聚合**：拉取的数据会进行 sort-merge 聚合

**Shuffle 是性能瓶颈的原因：**

- **磁盘 I/O 开销大**：大量数据需要落盘，不能纯内存操作
- **网络 I/O**：跨节点的数据传输
- **高基数风险**：分区不均匀时某些 Reducer 压力过大
- **对象创建开销**：Key-Value 对象频繁创建和 GC

**常见优化手段：**

1. **减少 Shuffle 数据量**：尽量在 Map 端做预聚合（如 `reduceByKey` 而非 `groupByKey`）
2. **合理设置分区数**：`spark.sql.shuffle.partitions`（默认 200）应与集群资源匹配，避免过多小任务或过少大任务
3. **使用 Coalesce/Repartition**：`coalesce(numPartitions)` 可减少分区避免 Shuffle；`repartition` 会触发 Shuffle 但更均匀
4. **开启 Shuffle 压缩**：`spark.shuffle.compress=true`（默认开启）
5. **使用 SortShuffleWriter** 替代 HashShuffleWriter：`spark.shuffle.manager=sort`
6. **避免 groupByKey**：优先使用 `reduceByKey`、`aggregateByKey`，它们会在 Map 端做预聚合

> **来源说明**：本题为 Spark 核心调优题，参考 Spark 官方 Shuffle 文档 https://spark.apache.org/docs/latest/rdd-programming-guide.html#shuffle-operations 及美团技术博客 https://tech.meituan.com/2016/05/12/spark-tuning.html

---

## 题目四：Spark 的内存管理模型是怎样的？Executor 内存是如何划分的？

### 参考答案

Spark 采用统一的内存管理模型（Spark 1.6+ 引入 Unified Memory Management），将堆内存分为三大区域：

**内存区域划分：**

```
┌──────────────────────────────────────┐
│           Executor 堆内存             │
│  ┌────────────┬────────────┬──────┐ │
│  │  Execution │   Storage  │ User │ │
│  │   Memory   │   Memory   │      │ │
│  │  (执行内存) │ (存储内存)  │  内存 │ │
│  └────────────┴────────────┴──────┘ │
└──────────────────────────────────────┘
```

| 区域 | 用途 | 占比 | 说明 |
|------|------|------|------|
| **Execution Memory** | Shuffle 排序、Hash Join 哈希表、缓存 RDD 物理执行计划等 | 默认 30%（`spark.memory.fraction=0.6` 的 50%） | 用于计算过程中的中间数据 |
| **Storage Memory** | 缓存 RDD/Broadcast 变量、Unroll 内存 | 默认 30% | 存储持久化的数据 |
| **User Memory** | 用户数据结构、自定义类、元数据等 | 约 40% | 不可压缩，由 JVM 管理 |

**动态占用机制：**

- Execution 和 Storage 内存可以互相借用（`spark.memory.storageFraction=0.5` 控制 Storage 的可借用下限）
- 当 Execution 内存不足时，可以驱逐 Storage 内存中的数据（但正在被使用的 Block 不可驱逐）
- 当 Storage 内存不足时，只能丢弃尚未使用的缓存数据

**相关配置参数：**
```scala
spark.executor.memory           // Executor 总堆内存
spark.memory.fraction           // 统一内存占 Executor 内存的比例（默认 0.6）
spark.memory.storageFraction    // Storage 内存占统一内存的比例（默认 0.5）
```

> **来源说明**：本题为 Spark 内存管理核心题，参考 Spark 官方内存管理文档 https://spark.apache.org/docs/latest/memory-management.html

---

## 题目五：广播变量（Broadcast）的原理是什么？它能解决什么问题？使用注意事项有哪些？

### 参考答案

**广播变量的原理：**

广播变量是一种只读变量，Driver 端将数据序列化后发送到每个 Executor，Executor 将其缓存在本地，后续 Task 在同一 Executor 内共享这份数据，避免了每个 Task 都携带一份数据副本导致的内存浪费。

**执行流程：**
1. Driver 端调用 `sc.broadcast(value)` 创建广播变量
2. 广播变量被序列化为二进制后，按需（首次遇到 Action 时）发送到各 Executor
3. Executor 通过 `BroadcastManager` 和 `BlockManager` 管理广播数据的存储
4. Task 访问时通过 `broadcast.value` 获取本地缓存的数据

**能解决的问题：**

- **降低内存消耗**：避免每个 Task 都复制一份数据（如大维度表、小表广播）
- **减少网络传输**：大表 JOIN 时，将小表广播到各节点，只需一次网络传输

**典型使用场景：**
```scala
// 小表广播到所有节点进行 JOIN
val smallTable = sc.broadcast(broadcastData)

// 在 Map 端使用
rdd.map { row =>
  val lookup = smallTable.value  // 直接从本地获取，无网络开销
  // ...
}
```

**使用注意事项：**

1. **广播的表不宜过大**：广播变量存在 Executor 堆内，过大可能导致 OOM；一般超过 20MB 要谨慎
2. **广播变量不可修改**：修改 `broadcast.value` 不会影响其他 Executor 上的副本
3. **广播后不要在 Shuffle 中使用**：广播变量不适合作为 Shuffle 的 Key
4. **及时取消广播**：使用完后调用 `broadcast.unpersist()` 释放 Executor 上的缓存
5. **序列化方式影响性能**：默认使用 Java Serialize，可通过 `spark.serializer=org.apache.spark.serializer.KryoSerializer` 提升性能

> **来源说明**：本题为 Spark 广播变量核心题，参考 Spark 官方编程指南 https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables 及 Databricks 博客 https://databricks.com/blog/2016/05/27/apache-spark-structured-streaming.html

---

*整理 by Spark Interview Bot · 2026-07-29*
