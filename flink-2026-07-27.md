# Flink 面试题 - 2026-07-27

## 题目一：Flink 的架构是怎样的？请详细解释 JobManager 和 TaskManager 的职责。

### 参考答案

**Flink 架构采用主从架构**，由 JobManager（主节点）和 TaskManager（从节点）组成。

**JobManager 核心职责：**
- **ResourceManager**：负责管理任务槽（Task Slot），协调资源分配
- **Dispatcher**：提供 REST 接口，提交应用并为每个作业启动一个新的 JobMaster
- **JobMaster**：负责执行单个 JobGraph，包括故障恢复

**TaskManager 核心职责：**
- 执行 DataStream 的算子子任务
- 管理内存、CPU、磁盘等资源
- 通过心跳机制向 JobManager 汇报状态
- 每个 TaskManager 包含多个 Task Slot，每个 slot 可运行一个算子并行实例

**作业执行流程：**
1. 用户编写的 Flink 程序转换为 StreamGraph（用户代码）
2. StreamGraph 优化为 JobGraph
3. JobGraph 提交给 JobManager
4. JobManager 根据 JobGraph 生成 ExecutionGraph（执行图）
5. TaskManager 向 ResourceManager 申请 Slot 资源
6. 资源分配完成后，JobManager 部署执行计划

**来源参考：**
- Apache Flink 官方文档：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/flink-architecture/

---

## 题目二：什么是 Watermark（水位线）？Flink 如何处理乱序数据和迟到事件？

### 参考答案

**Watermark 定义：**
Watermark 是 Flink 用于处理乱序事件的一种机制，本质是一个时间戳。当 Watermark 达到某个窗口的结束时间时，表示所有早于该时间戳的事件都已经到达。

**核心思想：**
```
Event Time - Watermark >= Window End Time
```
当 Watermark >= 窗口结束时间时，窗口才会被触发计算。

**处理乱序数据：**
Flink 提供了三种处理迟到事件的策略：

1. **Allowed Lateness（允许延迟）**
   - 窗口触发后，不会立即删除，而是等待一段时间
   - 配置方式：`window.assignWithAllowedLateness(Time.minutes(5))`
   - 迟到事件仍然可以触发窗口重新计算

2. **Side Output（侧输出流）**
   - 将迟到事件输出到单独的流中进行处理
   - 配置方式：
   ```java
   OutputTag<String> lateTag = new OutputTag<>("late-data");
   windowedStream.sideOutputLateData(lateTag);
   ```

3. **直接丢弃**
   - 默认策略，超过 Watermark 的事件直接丢弃

**Watermark 传播机制：**
Watermark 从上游算子传播到下游算子时，会取 min 值（取最小值确保下游所有算子都能正确触发）

**来源参考：**
- Apache Flink 官方文档：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/time-processing/#event-time-and-watermarks

---

## 题目三：Flink Checkpoint 和 Savepoint 的区别是什么？如何选择合适的 Checkpoint 策略？

### 参考答案

**核心区别：**

| 特性 | Checkpoint | Savepoint |
|------|------------|-----------|
| **触发方式** | Flink 自动周期性触发 | 用户手动触发 |
| **用途** | 故障恢复 | 计划性停机、版本升级 |
| **存储位置** | JobManager 内存（可配置） | 外部文件系统 |
| **删除机制** | Job 取消后自动删除 | 需手动删除 |
| **兼容性** | 依赖 JobGraph 结构 | 跨 Job 版本兼容 |

**Checkpoint 执行原理：**
1. JobManager 定期向所有 Source 算子发送 Checkpoint 触发消息
2. Source 算子保存状态，生成 CheckpointBarrier，向下游发送 Barrier
3. 中间算子收到所有输入的 Barrier 后完成自身 Checkpoint
4. Sink 算子完成 Checkpoint 后向 JobManager 确认
5. Checkpoint 完成，存储到状态后端

**Checkpoint 配置策略：**

```java
StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
// 间隔 10 秒执行一次 Checkpoint
env.enableCheckpointing(10000);
// 语义选择：EXACTLY_ONCE（精确一次）或 AT_LEAST_ONCE（至少一次）
env.getCheckpointConfig().setCheckpointingMode(CheckpointingMode.EXACTLY_ONCE);
// Checkpoint 超时时间
env.getCheckpointConfig().setCheckpointTimeout(60000);
// 两次 Checkpoint 最小间隔
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(5000);
// 允许的最大并发 Checkpoint 数
env.getCheckpointConfig().setMaxConcurrentCheckpoints(1);
```

**状态后端选择：**
- **HashMapStateBackend**：状态存储在 TaskManager JVM 堆内存，适合小状态
- **EmbeddedRocksDBStateBackend**：状态存储在 RocksDB，适合大状态，支持增量 Checkpoint

**来源参考：**
- Apache Flink 官方文档：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/ops/state/checkpoints/

---

## 题目四：Flink 窗口函数有哪些类型？Session Window 和 Tumbling Window 的区别是什么？

### 参考答案

**窗口函数分类：**

**1. 按时间划分：**
- **Tumbling Window（滚动窗口）**：固定大小，不重叠
  ```java
  .window(TumblingEventTimeWindows.of(Time.seconds(10)))
  ```
- **Sliding Window（滑动窗口）**：固定大小，可重叠
  ```java
  .window(SlidingEventTimeWindows.of(Time.seconds(10), Time.seconds(5)))
  ```
- **Session Window（会话窗口）**：基于活动间隙划分
  ```java
  .window(EventTimeSessionWindows.withGap(Time.seconds(10)))
  ```
- **Global Window（全局窗口）**：所有相同 key 的数据进入同一个窗口

**2. 按语义划分：**
- **Count Window（计数窗口）**：按元素数量触发
- **Time Window（时间窗口）**：按时间触发

**滚动窗口 vs 会话窗口对比：**

| 特性 | Tumbling Window | Session Window |
|------|-----------------|----------------|
| **大小** | 固定 | 不固定，动态形成 |
| **重叠** | 不重叠 | 不重叠 |
| **触发时机** | 窗口结束时固定触发 | 最后一条数据到来后等待 gap 时间内无新数据才触发 |
| **适用场景** | 周期性统计 | 用户行为分析、会话分析 |
| **示例** | 每分钟统计pv | 用户浏览商品会话分析 |

**窗口函数示例：**
```java
// 增量聚合函数
input
    .keyBy(x -> x.key)
    .window(TumblingEventTimeWindows.of(Time.minutes(1)))
    .reduce((a, b) -> a + b);

// 全量窗口函数
input
    .keyBy(x -> x.key)
    .window(TumblingEventTimeWindows.of(Time.minutes(1)))
    .apply(new WindowFunction<...>());
```

**来源参考：**
- Apache Flink 官方文档：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/dev/datastream/operators/windows/

---

## 题目五：Flink 状态管理机制是怎样的？Keyed State 和 Operator State 的区别是什么？

### 参考答案

**Flink 状态分类：**

**1. Keyed State（键控状态）**
- 只能在 KeyedStream 上使用
- 状态按 Key 进行隔离和管理
- 每个 Key 维护独立的状态实例

**支持的数据结构：**
```java
// ValueState：单个值
ValueState<Long> countState = getRuntimeContext().getState(
    new ValueStateDescriptor<>("count", Long.class));

// ListState：列表
ListState<String> listState = getRuntimeContext().getListState(
    new ListStateDescriptor<>("list", String.class));

// ReducingState：聚合状态
ReducingState<UserEvent> reducingState = getRuntimeContext().getReducingState(
    new ReducingStateDescriptor<>(...));

// AggregatingState：聚合状态
AggregatingState<UserEvent, Long> aggState = getRuntimeContext().getAggregatingState(
    new AggregatingStateDescriptor<>(...));

// MapState：映射状态
MapState<String, Integer> mapState = getRuntimeContext().getMapState(
    new MapStateDescriptor<>("map", String.class, Integer.class));
```

**2. Operator State（算子状态）**
- 作用于整个算子实例，不区分 Key
- 所有并行实例共享状态

**状态广播特性（BroadcastState）：**
```java
// 广播状态，用于动态规则更新场景
BroadcastState<String, Rule> broadcastState = ctx.getBroadcastState(
    new MapStateDescriptor<>("rules", String.class, Rule.class));
```

**Keyed State vs Operator State 对比：**

| 特性 | Keyed State | Operator State |
|------|-------------|----------------|
| **作用范围** | 绑定到 Key | 绑定到算子实例 |
| **使用场景** | KeyedStream 算子 | Source 算子（如 Kafka Consumer Offset） |
| **状态划分** | 按 Key 自动分区 | 需要手动处理状态重分布 |
| **并行度变化** | 自动重新分配 | 支持 UnionState、ListState、SingletonState |

**状态一致性保证：**
- Flink 通过 Checkpoint 机制保证 EXACTLY_ONCE 语义
- 状态后端负责存储和读取状态数据
- 推荐使用 RocksDBStateBackend 配合增量 Checkpoint 处理大状态

**来源参考：**
- Apache Flink 官方文档：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/stateful-stream-processing/

---

*本面试题由 FlinkInterview Bot 自动生成 | GitHub: qq286158530/interview-questions*
