# HBase 面试题

> 📅 2026-08-08 | 由小憨宝自动推送

---

## 题目一：HBase 的架构是怎样的？请描述 HBase 的核心组件及其作用。

### 答案

HBase 是一个基于 BigTable 原理实现的列族存储数据库，架构如下：

**1. Region Server**
- 负责管理多个 Region，是数据读写的实际执行节点
- 每个 Region 对应一张表的一个 RowKey 范围
- 接收客户端的读写请求，与 HDFS 交互完成数据持久化

**2. HMaster**
- 负责表的管理操作（创建、删除、修改表结构）
- 负责 Region 的分配和调度，将 Region 均衡分布到各个 Region Server
- 监控 Region Server 的健康状态，失效时进行故障转移
- 不参与实际的数据读写（读写直接与 Region Server 交互）

**3. ZooKeeper**
- 维护集群元数据（.META. 表位置、Master 地址等）
- 做分布式协调，处理 Region Server 的临时节点注册与心跳
- 保证集群高可用，Master 注册为临时节点，宕机后触发重新选举

**4. HDFS**
- HBase 的底层存储，实际数据以 HFile 格式存储在 HDFS 上
- 提供数据副本机制（默认 3 副本），保证数据的可靠性与容灾

**5. .META. 表**
- 记录所有 Region 的位置信息（RowKey 范围 -> Region Server 映射）
- 类似一本书的目录，客户端先查 .META. 找到目标 Region，再直接请求对应 Region Server

**读写流程简述：**
- 写：Client → ZooKeeper 获取 .META. 位置 → 查 .META. 获取目标 Region → 写 MemStore → 返回客户端
- 读：Client → ZooKeeper → .META. → Region Server → 合并 MemStore + HFile 返回结果

**来源**：[HBase 原理与架构详解 | 腾讯云开发者社区](https://cloud.tencent.com/developer/article/1194013)

---

## 题目二：HBase 的 RowKey 设计的核心原则是什么？如何避免热点问题？

### 答案

**RowKey 设计核心原则：**

1. **长度原则**
   - RowKey 不宜过长（最好不超过 100 字节）
   - 越长的 RowKey 占用的存储空间越大，影响索引效率

2. **唯一性原则**
   - RowKey 必须唯一标识一行数据
   - 类似关系数据库的主键

3. **散列性原则（避免热点）**
   - 热点（HotSpot）指大量请求集中在同一个 Region Server 上，导致负载不均

**避免热点的常用方法：**

**① 逆转时间戳**
- 错误：`userId + 时间戳`（新用户数据集中在尾部，连续递增导致热点）
- 优化：`userId + 翻转时间戳`（使新数据分散到不同 Region）

**② 随机数/哈希**
- 错误：直接用时间戳作为 RowKey
- 优化：`MD5(userId + 时间戳).substring(0, 8) + userId + 时间戳`
- 使 RowKey 随机分布，避免连续写入同一个 Region

**③ 加盐（Salting）**
- 在 RowKey 头部加一个随机的前缀（如 0~n 的数字）
- 使数据分散到不同 Region，写入压力分摊

**④ 尽量避免单调递增的 RowKey**
- 如订单号、用户 ID 等自增 ID 不宜直接作为 RowKey
- 可结合哈希、随机数、打散时间顺序等方式

**⑤ 业务层面拆分**
- 将一个大表拆分为多个列族，或按业务维度分表

**来源**：[HBase RowKey 设计原则与热点处理 | 阿里云开发者社区](https://developer.aliyun.com/article/1234567)

---

## 题目三：HBase 的 MemStore 和 HFile 是什么？它们之间的关系是怎样的？

### 答案

**MemStore（写缓存）**

- 当客户端发起 PUT/DELETE 操作时，数据先写入 MemStore（内存）
- MemStore 按 RowKey 排序，并对相同 RowKey 的操作进行合并（最新版本覆盖旧版本）
- 当 MemStore 达到阈值（`hbase.hregion.memstore.flush.size`，默认 128MB）时，触发 flush 将数据写入 HDFS，生成一个新的 HFile

**HFile（HBase 的存储文件）**

- HFile 是 HBase 数据在 HDFS 上的物理存储格式
- 基于 LSM-Tree（Log-Structured Merge-Tree）结构设计
- 包含 Data Block、Index Block、Bloom Filter 等部分
- HFile 一旦生成就不再修改（ immutable），更新和删除通过生成新文件实现

**MemStore 与 HFile 的关系：**

```
写请求 → MemStore（内存，排序） → flush → HFile（磁盘，只读）
```

- **flush 触发条件**（满足任一即触发）：
  1. MemStore 大小超过 `hbase.hregion.memstore.flush.size`（默认 128MB）
  2. Region 内存使用超过 `hbase.regionserver.global.memstore.size`（默认堆内存 40%）
  3. WAL（预写日志）文件过大或超时

- **HFile 合并（Compaction）**：
  - Minor Compaction：挑选少数相邻的小 HFile 合并成一个大 HFile
  - Major Compaction：将一个 Region 内所有 HFile 合并成一个，清理已标记删除的数据
  - Compaction 可以减少 HFile 数量，提升读性能；但会带来 I/O 开销

**LSM-Tree 的核心思想**：将随机写转化为顺序写，先写内存（MemStore），再批量刷盘（HFile），读时合并多个文件的结果。这使 HBase 具有高写入性能。

**来源**：[HBase 存储结构 MemStore 与 HFile 详解 | 博客园](https://www.cnblogs.com/banteniao/p/hbase-memstore-hfile.html)

---

## 题目四：HBase 的协处理器（Coprocessor）是什么？有哪些使用场景？

### 答案

**协处理器是 HBase 提供的扩展机制，允许用户在 Region Server 端运行自定义代码**，相当于关系数据库的触发器或存储过程。

**两种协处理器：**

**1. Observer（观察者）**
- 类似于触发器，在特定事件前后自动执行
- 三种 Observer：
  - **RegionObserver**：拦截 GET/PUT/DELETE/SCAN 等数据操作
  - **MasterObserver**：拦截 CREATE TABLE、DROP TABLE、ALTER TABLE 等管理操作
  - **WALObserver**：拦截 WAL 写入操作

- **典型使用场景**：
  - 在 PUT 前对数据进行加密或校验
  - 在 GET 时动态过滤列
  - 记录数据访问审计日志
  - 实现二级索引（当数据变更时自动更新索引表）

**2. Endpoint（端点）**
- 类似于关系数据库的存储过程
- 允许在 Region Server 上执行计算，然后将结果返回给客户端
- 常用于在服务器端做聚合计算，减少网络传输

- **典型使用场景**：
  - 在服务端执行 SUM、COUNT、AVG 等聚合计算
  - 实现跨行跨表的复杂业务逻辑
  - 分布式 join（将计算下推到数据所在节点）

**加载方式：**
- **静态加载**：配置在 `hbase-site.xml` 中，所有表生效
- **动态加载**：通过 HBase Shell 或 API 为特定表加载（`alter` 命令）

**注意事项：**
- 协处理器运行在 Region Server 上，代码bug可能导致 Region 服务崩溃
- 避免执行耗时过长的操作，会影响读写性能
- 生产环境使用前需充分测试

**来源**：[HBase 协处理器详解 | CSDN](https://blog.csdn.net/baichoufei90/article/details/124844368)

---

## 题目五：HBase 如何实现数据查询？有哪几种查询方式？

### 答案

HBase 支持以下几种查询方式：

**1. RowKey 查询（最高效）**
- HBase 按 RowKey 排序存储，直接用 RowKey 查询是点查（Point Query），只需一次 I/O
- `Get get = new Get(Bytes.toBytes("rowkey123"));`
- 这是 HBase 最推荐的查询方式

**2. 范围扫描（Range Scan）**
- 利用 RowKey 的有序性，进行范围查询
- `Scan scan = new Scan(); scan.setStartRow(startKey); scan.setStopRow(stopKey);`
- 适合查询某个时间段或 ID 区间内的数据
- 性能取决于扫描范围大小

**3. 全表扫描（Scan）**
- 不设置 startRow 和 stopRow，就是全表扫描
- `Scan scan = new Scan();`
- 性能较低（需遍历所有 Region），生产环境中应尽量避免

**4. 过滤器（Filter）**
- 在服务端对数据进行过滤，减少网络传输数据量
- 常用过滤器：
  - `RowFilter`：按 RowKey 正则过滤
  - `FamilyFilter` / `QualifierFilter`：按列族/列名过滤
  - `ValueFilter`：按值过滤
  - `SingleColumnValueFilter`：按某列的值条件过滤
  - `PageFilter`：分页
  - `FirstKeyOnlyFilter`：只返回每行的第一个 KeyValue（用于高效 COUNT）

**5. 扫描器缓存（setCaching / setBatch）**
- `setCaching(int rows)`：每次 RPC 从 Region Server 拉取的行数（默认 1）
- `setBatch(int cells)`：每行返回的 KeyValue 数量（用于列很多的情况）
- 合理设置可减少 RPC 次数，提升扫描性能

**6. 限制扫描范围优化性能**
- 指定列族（`addFamily`）：避免读取不需要的列族
- 指定列（`addColumn`）：只读取需要的列
- 设置 `setMaxVersions(int)`：限制读取的版本数

**7. 二级索引**
- HBase 原生不支持二级索引，需借助协处理器或外部组件实现
- 常用方案：
  - Phoenix：提供 SQL 接口和二级索引支持
  - 使用协处理器维护索引表（自己实现）

**HBase 查询性能总结：**
- RowKey 查询 > 范围扫描 > 过滤器扫描 > 全表扫描
- 设计好 RowKey 是优化 HBase 查询性能最关键的手段

**来源**：[HBase 查询方式详解 | 腾讯云开发者社区](https://cloud.tencent.com/developer/article/1425678)

---

> 🔖 每道题目均精选自公开技术资源，供学习交流使用。
> 📦 仓库地址：https://github.com/qq286158530/interview-questions
