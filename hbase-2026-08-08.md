# 程序员面试题 - HBase专题 - 2026-08-08

## 🔥 HBase 核心面试题

---

### 题目1：HBase 的架构原理是什么？请描述数据写入和读取的流程。

【答案】

**HBase 架构核心组件**：

| 组件 | 作用 |
|------|------|
| Zookeeper | 元数据管理、Master 选举、故障检测 |
| HMaster | 负责表管理、Region 分配、负载均衡 |
| RegionServer | 负责数据读写、MemStore、HFile 管理 |
| Region | 表的数据分片，一个 Region 对应一个列族 |
| Store | 一个列族对应一个 Store，包含 MemStore 和 HFiles |
| MemStore | 写缓存，先写入内存，满了再刷写成 HFile |
| HFile | HBase 的数据存储文件，存储在 HDFS 上 |

**数据写入流程（Write-Ahead Log）**：
1. 客户端先写入 WAL（Write-Ahead Log，保存在 HDFS 上）
2. 数据写入 MemStore（内存中）
3. 更新操作成功返回客户端
4. 当 MemStore 达到阈值（约 128MB），触发 flush 成 HFile
5. HFile 异步刷写到 HDFS

**数据读取流程**：
1. 客户端请求 Zookeeper 获取 meta 表位置
2. 从 meta 表获取目标 Region 的位置
3. 请求 RegionServer，先查 MemStore（读内存）
4. 再查 BlockCache（读缓存）
5. 最后查 HFile（读磁盘）
6. 合并结果返回客户端

**特点**：
- 强一致性：写操作必须写入 WAL + MemStore 才返回成功
- 高写入：LSM-Tree 结构，适合高并发写入
- 自动分区：Region 自动 split 和负载均衡

📖 来源：https://hbase.apache.org/book.html

---分割线---

### 题目2：RowKey 如何设计？有哪些原则和技巧？

【答案】

**RowKey 设计原则**：

1. **唯一性**：RowKey 必须唯一标识一行数据
2. **长度控制**：越短越好，建议不超过 16 字节（100 字节内）
3. **散列性**：避免热点，数据均匀分布在所有 Region 上
4. **业务相关性**：将查询条件放在 RowKey 前缀

**常见 RowKey 设计技巧**：

**① 加盐（Salt）**
```
原 RowKey：userId_订单ID
加盐后：0_userId_订单ID, 1_userId_订单ID ...
方法：对 userId hash 后取模 n，拼接在前面
优点：打散热点，避免所有数据写到一个 Region
```

**② 哈希（Hash）**
```
原 RowKey：2026-08-08_13812345678
哈希后：MD5(13812345678)_2026-08-08_13812345678
优点：保证数据均匀分布
```

**③ 反转（Reverse）**
```
原 RowKey：13812345678
反转后：87654321831
适用：手机号、身份证等有规律的前缀
```

**④ 组合列式（Composite RowKey）**
```
设计：时间戳_用户ID_设备ID
查询：按时间范围 + 用户ID 快速定位
```

**热点场景解决方案**：
- 时间戳反转：e.g., `Long.MAX_VALUE - timestamp` + 业务字段
- 随机数打散：在 RowKey 前加随机前缀

📖 来源：https://hbase.apache.org/book.html#rowkey.design

---分割线---

### 题目3：MemStore 和 HFile 的工作原理是什么？为什么需要 MemStore？

【答案】

**HFile 结构（LSM-Tree）**：

HBase 底层采用 LSM-Tree（Log-Structured Merge-Tree）数据结构：

```
HFile 构成：
├── BlockIndex（索引块）：存储 Data Block 的起始key
├── Data Block（数据块）：默认 64KB，存储 KeyValue 对
├── Meta Block（元数据块）
├── FileInfo（文件信息）
└── Trailer（文件尾）：指向各块的偏移量
```

**MemStore 的作用**：
1. **写缓存**：将随机写转换为顺序写，提高写入性能
2. **排序准备**：写入 MemStore 前会按 RowKey 排序，为后续 flush 成 HFile 做准备
3. **批量合并**：多个小 HFile 会通过 Major/Minor Compaction 合并成大 HFile

**MemStore 刷写（Flush）时机**：
1. MemStore 大小达到 `hbase.hregion.memstore.flush.size`（默认 128MB）
2. RegionServer 内存超过阈值 `hbase.regionserver.global.memstore.size`（默认 40%）
3. WAL 文件数量超过 `hbase.regionserver.max.logs`（默认 32）
4. 定期刷新：定时任务定期检查并刷新

**HFile 合并（Compaction）**：
- **Minor Compaction**：合并少量小 HFile，删除标记过的数据
- **Major Compaction**：合并一个 Region 下的所有 HFile，删除所有过期/删除数据

**为什么需要 MemStore 而不是直接写 HFile**：
- HDFS 顺序写效率高，但随机写效率低
- MemStore 内存排序后顺序刷写，充分利用 HDFS 顺序写的优势
- 减少 HDFS 的小文件问题，提高读写效率

📖 来源：https://hbase.apache.org/book.html#regionserver.arch

---分割线---

### 题目4：HBase 的协处理器（Coprocessor）是什么？有哪些使用场景？

【答案】

**协处理器概述**：

HBase 协处理器是一种扩展机制，允许用户在 RegionServer 上运行自定义代码，实现对数据的拦截、处理和计算。

**两种协处理器类型**：

**① Observer 协处理器（观察者）**
类似 AOP 拦截器，Hook 在特定操作前后执行：

| 钩子 | 触发时机 |
|------|----------|
| RegionObserver | Get/Put/Delete/Scan 等数据操作前后 |
| MasterObserver | 创建/删除表等 DDL 操作前后 |
| WALObserver | WAL 写入前后 |

**应用场景**：
- **二级索引**：Put 时自动写入索引表
- **数据校验**：写入前校验数据合法性
- **审计日志**：记录所有数据变更操作
- **权限控制**：基于列级的访问控制

**② Endpoint 协处理器（端点）**
类似存储过程，在 RegionServer 上执行计算：

**应用场景**：
- **聚合计算**：在 RegionServer 上执行 Count/Sum/Avg，减少数据传输
- **自定义过滤**：在服务端执行复杂过滤条件
- **批量处理**：在 RegionServer 上批量处理数据

**使用示例（二级索引）**：
```java
public class IndexObserver extends RegionObserver {
    @Override
    public void postPut(ObserverContext<RegionCoprocessorEnvironment> e,
                        Put put, WALKey key, WALEdit edit) {
        // 解析主表数据，构造索引表的 Put
        // 写入索引表
    }
}
```

**加载方式**：
1. **静态加载**：配置 `hbase-site.xml`（全局生效）
2. **动态加载**：DDL 语句 `alter table 't1' COPROCESSOR => '...'`

**注意事项**：
- 协处理器在 RegionServer 上执行，代码bug会影响集群稳定性
- 避免执行耗时操作，会阻塞读写请求
- 做好异常处理，防止数据不一致

📖 来源：https://hbase.apache.org/book.html#cp

---分割线---

### 题目5：HBase 如何实现高可用？故障恢复机制是怎样的？

【答案】

**HBase 高可用架构**：

```
                 Zookeeper Cluster
                      │
          ┌───────────┼───────────┐
          ▼           ▼           ▼
      HMaster     HMaster      HMaster
   (Active)    (Standby)    (Standby)
          │           │           │
          └───────────┼───────────┘
                      ▼
         ┌────────────────────────┐
         │     RegionServer       │
         │  ┌─────┐ ┌─────┐      │
         │  │Region│ │Region│ ... │
         │  └─────┘ └─────┘      │
         └────────────────────────┘
```

**高可用保障**：

1. **Master 高可用**
   - 多个 Master 通过 Zookeeper 选举主节点
   - Active Master 挂了，Standby Master 自动接管
   - 负责表管理、Region 分配、负载均衡

2. **RegionServer 高可用**
   - 数据副本存储在 HDFS 上（默认 3 副本）
   - RegionServer 挂了，Master 将其上的 Region 迁移到其他节点
   - WAL 保存在 HDFS，MemStore 数据可恢复

3. **Zookeeper 高可用**
   - 存储元数据（-ROOT- 表位置、表结构等）
     - 负责 Master 选举和故障检测

**故障恢复流程**：

**RegionServer 故障**：
1. Zookeeper 检测到心跳超时（默认 3 分钟）
2. HMaster 将该 RS 上的所有 Region 标记为待分配
3. 从 HDFS 读取 WAL，恢复未 flush 的数据
4. 将 Region 分配到其他健康的 RegionServer
5. 客户端通过 meta 表感知新位置，继续服务

**Master 故障**：
1. Zookeeper 重新选举新的 Active Master
2. 新 Master 接管元数据管理
3. 恢复 Region 分配状态
4. 处理Pending的建表/修改请求

**数据恢复（核心）**：
- 未刷写的 MemStore 数据：通过 WAL 重放恢复
- HFile 数据：依赖 HDFS 多副本，自动恢复

**读写可用性**：
- 写入：强一致性，需要 WAL 写入成功才返回
- 读取：可以配置读取本地副本，降低延迟
- 故障时：短暂不可用（Master 切换约 30s，RS 恢复约 1-2 分钟）

📖 来源：https://hbase.apache.org/book.html#arch overview

---分割线---

## 📚 更多资源

- 官方文档：https://hbase.apache.org/book.html
- GitHub 仓库：https://github.com/qq286158530/interview-questions

---

> 本题库由 GitHub Action 自动推送 | 每日定时更新
