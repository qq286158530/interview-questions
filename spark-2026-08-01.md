# Spark 核心面试题 — 2026-08-01

> 涵盖 Spark 架构、RDD、DataFrame/DataSet、Shuffle、内存管理、广播变量等核心知识点

---

## 题目一：Spark Shuffle 机制原理是什么？为什么 Shuffle 是性能瓶颈？

### 参考答案

**Shuffle 是 Spark 中将数据按 Key 进行重新分布的过程**，发生在 `groupByKey`、`reduceByKey`、`join`、`distinct` 等需要跨分区聚合或关联的操作时。

**工作原理：**

1. **Map 阶段**：每个 Task 将数据写入本地磁盘文件（spill 文件），并按目标 Partitioner 计算出数据应发送到的下游分区编号。
2. **Shuffle Write**：Map Task 完成后，将产生的文件合并为一个 Shuffle Block 文件，同时生成索引文件（`.index`），记录每个下游分区在该文件中的偏移量。
3. **Shuffle Read**：Reduce Task 启动时，根据上游 Map 的索引文件，通过 HTTP/Fetch 请求并发拉取属于自己分区的数据块。
4. **Sort**：如果 Reduce 端需要排序（如 `groupByKey`），会在内存或磁盘中对拉取的数据进行排序合并。

**为什么是性能瓶颈：**

- **磁盘 I/O**：Shuffle 数据先写入磁盘再读出，涉及大量磁盘读写，HDFS 或 NVMe 还好，普通 HDD 影响显著。
- **网络 I/O**：数据在节点间传输，网络带宽和延迟直接影响性能。
- **内存压力**：Shuffle 过程中需要缓存数据，内存不足时频繁 Spill 到磁盘。
- **短柄短板效应**：个别慢节点（Straggler）拖慢整个 Stage。

**优化手段：**
- 使用 `reduceByKey` 而非 `groupByKey`（预聚合减少数据量）
- 合理设置 `spark.sql.shuffle.partitions`（默认 200）
- 开启 `spark.shuffle.compress` 压缩
- 使用 ` coalesce`/`repartition` 减少分区数
- Spark 3.0+ 使用 `SortMergeFormat` 替代 Hash Shuffle，减少文件数

**来源：** [DataFlair — Apache Spark Interview Questions](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目二：RDD 的血统（Lineage）机制是如何实现容错的？与 Hadoop 的副本机制有何区别？

### 参考答案

**RDD 容错机制 — Lineage（血统）**

RDD 通过**血统链（Lineage Graph）** 实现容错。每个 RDD 都知道自己是由哪个父 RDD 经过何种 transformation 得到的。当某个 Partition 丢失时，Spark 按照血统链重新计算该 Partition，而不需要复制整个数据集。

```
例如：
rdd2 = rdd1.map(...).filter(...).reduceByKey(...)
         ──────── lineage ────────>
rdd2 知道自己的数据来自 rdd1.map().filter()，丢失时直接回溯重算
```

**与 Hadoop 副本机制的区别：**

| 特性 | Spark RDD (Lineage) | Hadoop (副本机制) |
|---|---|---|
| 容错方式 | 血统重算 | 数据块副本（默认 3 副本） |
| 存储开销 | 无额外存储（仅记录血统） | 3 倍存储空间 |
| 恢复速度 | 需重算（可能较慢） | 直接读副本（快） |
| 适用场景 | 宽依赖重算代价可控 | 大数据量、低重算成本 |

**注意**：如果血统链过长（如多轮窄依赖+宽依赖交替），重算代价会很高，此时可以将中间结果 `cache()` 或 `persist()` 起来。

**来源：** [DataFlair — What is Lineage Graph in Apache Spark](https://data-flair.training/blogs/topic/what-is-lineage-graph-in-apache-spark/)

---

## 题目三：Spark 内存管理机制是怎样的？堆内内存（Heap）和堆外内存（Off-Heap）有什么区别？

### 参考答案

**Spark 统一内存模型（Spark 2.x+）**

Spark 将内存划分为两大类：**执行内存（Execution Memory）** 和**存储内存（Storage Memory）**，二者可以动态借用。

```
┌──────────────────────────────────────────┐
│           Spark 统一内存（Unified Memory）│
├────────────────┬─────────────────────────┤
│   Execution    │       Storage          │
│   Memory       │       Memory           │
│  (Shuffle/排序 │  (cache/RDD/Broadcast) │
│   哈希聚合等)   │                        │
├────────────────┴─────────────────────────┤
│         Other Memory (系统预留 ~300MB)    │
└──────────────────────────────────────────┘
```

**堆内内存（Heap Memory）**
- 由 JVM 统一管理，大小由 `spark.executor.memory` 指定
- 通过 `spark.memory.fraction`（默认 0.6）控制 Spark 可用比例
- 对象在 JVM Heap 中创建，有 GC 压力

**堆外内存（Off-Heap Memory）**
- 通过 `spark.memory.offHeap.enabled=true` 开启
- 使用 `spark.memory.offHeap.size` 设置大小（需要 OS 层面授权 ` --extraJavaOptions="-XX:MaxDirectMemorySize=xxg"`）
- 优点：避免 GC 开销，适合大内存块的高效管理
- 存储序列化后的数据（Tungsten 优化）

**内存分配策略：**
- `spark.memory.storageFraction`（默认 0.5）决定 Storage 初始预留比例
- Execution 和 Storage 空间不足时，可互相借用已用空间
- 借用后若一方需要回收，另一方有空闲则触发 spilling

**来源：** [DataFlair — Apache Spark Interview Questions](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目四：广播变量（Broadcast Variables）的作用是什么？使用广播变量有哪些注意事项？

### 参考答案

**广播变量的作用**

广播变量是只读的、缓存在每个 Executor 节点内存中的共享变量。Driver 端将数据只发送一次到各 Executor，后续 Task 无需重复传输，从而**大幅降低网络开销**。

典型使用场景：
- 大表 join 时将小表广播出去（`BroadcastHashJoin`）
- 配置表、维度表广播
- 机器学习中的特征元数据等

```scala
// Scala 示例
val broadcastVar = sc.broadcast(Array(1, 2, 3, 4, 5))
val result = rdd.map(x => x * broadcastVar.value.sum)
```

**注意事项：**

1. **数据大小**：广播变量存储在 Executor 堆内内存中，过大的数据会导致 OOM。经验阈值：广播数据 < 20MB（可调 `spark.sql.autoBroadcastJoinThreshold`，默认 10MB）。
2. **只读性**：广播变量在 Executor 端不可修改，修改无效且不安全。
3. **序列化**：广播数据需支持 Kryo 或 Java 序列化。
4. **广播阈值**：Spark SQL 中 `spark.sql.autoBroadcastJoinThreshold` 控制何时自动广播（默认 10MB）。
5. **广播顺序**：广播变量在使用前必须 `broadcast()`，且尽量提前广播，避免重复创建。
6. **删除/更新**：广播变量一旦创建不可更新，修改需要重新广播。
7. **多 Executor 共享**：同一个 Executor 上的多个 Task 共享同一个广播变量副本，节省内存。

**来源：** [DataFlair — What are shared variables in Apache Spark](https://data-flair.training/blogs/topic/what-are-shared-variables-in-apache-spark/)

---

## 题目五：DataFrame 和 Dataset 的区别是什么？什么时候该用 Dataset 而不是 DataFrame？

### 参考答案

**DataFrame vs Dataset 核心区别**

| 维度 | DataFrame | Dataset |
|---|---|---|
| **语言** | 纯 Python/SQL/R API（无类型安全） | 强类型语言（Scala/Java，RDD-like） |
| **类型安全** | 无编译时类型检查 | 有编译时类型检查 |
| **数据结构** | `Row` 类型的 `RDD[Row]` | `JVM T` 类型的 `RDD[T]` |
| **操作方式** | DSL 表达式 / SQL | Lambda 函数 + DSL |
| **Encoder | 自动 | 需显式提供 Encoders |

**DataFrame 实际上是 `Dataset[Row]` 的别名**（在 Scala/Java 中）。所以：
```scala
// 这两行等价：
val df: DataFrame = spark.read.json("people.json")
val ds: Dataset[Row] = spark.read.json("people.json")
```

**什么时候用 Dataset（T）而不是 DataFrame（Dataset[Row]）？**

1. **需要编译时类型安全**：在 Scala/Java 中处理业务逻辑时，Dataset 提供编译期检查，减少运行时错误。
2. **业务逻辑复杂**：需要使用 `map`、`filter`、`flatMap` 等强类型操作时，Dataset 更自然。
3. **需要操作自定义对象**：例如 `case class Person(name: String, age: Int)`，Dataset 可以直接使用。
4. **性能敏感场景**：Dataset 使用 Encoders 序列化（Tungsten），比 Java Serialization 高效很多，且有编译时优化。
5. **迁移自 RDD 代码**：已有 RDD 风格代码迁移到 DataFrame/Dataset 时，Dataset 更容易过渡。

**什么时候用 DataFrame 更合适？**

- 使用 Python 或 R 语言时（Dataset 仅支持 JVM 语言）
- 纯 SQL 查询和交互式分析
- 数据结构不固定（动态 Schema）

**来源：** [DataFlair — What is Dataset in Spark / Benefits of DataFrame](https://data-flair.training/blogs/spark-interview-questions/)

---

*📅 自动生成于 2026-08-01 | 来源整理自 DataFlair Apache Spark Interview Questions*
