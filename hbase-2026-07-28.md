# 程序员面试题 - 2026-07-28

## 🔥 HBase 核心知识点

---

### 题目1：HBase 的 Bloom Filter 是什么？它在读性能优化中起到了什么作用？

【答案】

**Bloom Filter 概念**：
Bloom Filter 是 HBase 用于快速判断某个 Key 是否存在于 HFile 中的数据结构，由布隆过滤器（Burton Bloom）于1970年提出。它利用多个独立的 Hash 函数，将数据映射到一个位数组中，可以高效判断"**一定不存在**"或"**可能存在**"。

**在 HBase 中的作用**：
HBase 在读取数据时，会依次检查 MemStore 和 BlockCache，如果都未命中，则需要扫描 HFile。Bloom Filter 在这个过程中起到**快速过滤**作用：

| 场景 | 无 Bloom Filter | 有 Bloom Filter |
|------|----------------|----------------|
| 查找不存在的 Key | 需扫描所有 HFile 的所有 Block | 通过 Bloom Filter 直接判断不存在，跳过该 HFile |
| 查找存在的 Key | 扫描所有 HFile | 定位到可能的 HFile，读取并精确查找 |

**Bloom Filter 类型**：
- **RowBloomFilter**：对 RowKey 级别过滤（只记录 RowKey）
- **RowColBloomFilter**：对 RowKey + Column Family + Qualifier 组合过滤，更精确但更占空间

**工作原理**：
```
写入数据时：
Key1 → Hash1()=2, Hash2()=5, Hash3()=7 → 位数组第2、5、7位设为1
Key2 → Hash1()=3, Hash2()=6, Hash3()=9 → 位数组第3、6、9位设为1

查询 Key3：
Hash1()=4, Hash2()=7, Hash3()=10
检查位数组：第4位=0 → Key3 一定不存在！跳过该 HFile
```

**实际效果**：
- 对于随机读取（Get）场景，Bloom Filter 可以跳过大量不包含目标数据的 HFile
- 可以减少 50%~90% 的 HFile IO 扫描，提升读取性能
- Bloom Filter 数据存储在 HFile 的 Meta Block 中，随 HFile 一起存储

**注意事项**：
- Bloom Filter 有一定的误报率（false positive），可能将不存在的 Key 误判为存在
- 误报率与位数组大小、Hash 函数数量有关，可通过 `BloomFilterBlockSize`、`hfile.block.bloom.max.fold` 等参数调优
- 适合等值查询（Get）场景，对范围扫描（Scan）效果有限

📖 来源：https://hbase.apache.org/book.html#bloom.filters

---分割线---

### 题目2：HBase 的 Region 分裂（Split）策略有哪些？不同分裂策略适用于什么场景？

【答案】

**为什么要分裂**：
当一个 Region 内的数据量增长到一定阈值时，HBase 会自动将该 Region 分裂成两个子 Region，以实现数据的负载均衡和并行处理。分裂策略决定了"何时分裂"以及"在哪里分裂"。

**常见分裂策略**：

**1. ConstantSizeRegionSplitPolicy（常量大小策略，已废弃）**
- 当 Region 大小超过 `hbase.hregion.max.filesize`（默认 10GB）时触发分裂
- 简单粗暴，不考虑实际数据分布

**2. IncreasingToUpperBoundRegionSplitPolicy（递增上限策略，HBase 1.x 默认）**
- 分裂阈值 = `Math.min(maxFileSize, minFileSize * numRegions * numRegions * numRegions)`
  - 初始阈值较小（128M），随着 Region 数量增长而递增
  - 前几个 Region 分裂较快，后期趋近于 maxFileSize
- **优点**：避免小表产生过多 Region

**3. KeyPrefixRegionSplitPolicy（Key前缀分裂策略）**
- 在 IncreasingToUpperBound 基础上，保证具有相同前缀的 RowKey 不会被分裂到不同的 Region
- 通过 `KeyPrefixRegionSplitPolicy` 的 `splitEncodings` 参数指定前缀长度
- **适用场景**：需要保证同一业务实体的数据在同一 Region（如加盐后的 RowKey）

**4. DelimitedKeyPrefixRegionSplitPolicy（分隔符前缀分裂策略）**
- 根据 RowKey 中指定分隔符（如 `_`）之前的内容来决定分裂点
- 同样是保证相同前缀的 RowKey 在同一 Region

**5. BusyRegionPolicy（繁忙 Region 策略）**
- 根据 Region 的写入/读取频率判断是否"繁忙"
- 如果某个 Region 持续繁忙（热 Region），则优先分裂它
- 需要配合 `hbase.busy.policy.block.duration` 等参数使用
- **适用场景**：存在明显热点 Region 的业务

**6. DisabledRegionPolicy（禁止分裂策略）**
- 完全禁止自动分裂
- 手动管理 Region 分裂，适用于需要精确控制数据分布的场景

**分裂流程**：
```
1. RegionServer 发起分裂请求 → Master 审核
2. 在 HDFS 上创建临时目录，存放子 Region 的数据
3. 将原 Region 下线，标记为 splitting 状态
4. 元数据更新（ZK + .META. 表）
5. 两个子 Region 上线，开始服务
```

**手动分裂**：
```bash
hbase> split 'tableName', 'splitKey'
hbase> split 'regionName'
```

**最佳实践**：
- 小表（GB 级以下）：可以用 DisabledRegionPolicy，避免频繁分裂
- 大表：使用 IncreasingToUpperBound，让分裂自动适配
- 有明显 RowKey 热点：使用 BusyRegionPolicy + 预分裂

📖 来源：https://hbase.apache.org/book.html#ops.regionmgt.split

---分割线---

### 题目3：HBase 的命名空间（Namespace）是什么？它与表的管理有什么关系？

【答案】

**Namespace 概念**：
Namespace 是 HBase 中的逻辑命名空间，用于对表进行分组和管理。从 HBase 0.98.0 开始引入，类似于关系型数据库中的"数据库（Database）"概念。

**系统预定义的 Namespace**：
| Namespace | 用途 | 示例 |
|-----------|------|------|
| `default` | 默认命名空间，未指定时使用 | 创建表时不指定 namespace |
| `hbase` | 系统表所在命名空间 | `hbase:meta`, `hbase:namespace`, `hbase:acl` |

**Namespace 操作**：
```bash
# 创建命名空间
hbase> create_namespace 'ns1'

# 在命名空间中创建表
hbase> create 'ns1:table1', 'cf1'

# 列出所有命名空间
hbase> list_namespace

# 查看命名空间中的表
hbase> list_namespace_tables 'ns1'

# 删除命名空间（需先删除所有表）
hbase> drop_namespace 'ns1'
```

**Namespace 的作用**：

**1. 资源隔离与配额管理**：
```bash
# 设置命名空间配额：最多 100 张表
hbase> set_namespace_quota 'ns1', 'TABLE => 100'

# 设置命名空间内存配额（所有 MemStore 总和上限）
hbase> set_namespace_quota 'ns1', 'MEMSTORE => 2G'

# 设置命名空间 Region 数量配额
hbase> set_namespace_quota 'ns1', 'REGION => 200'
```

**2. 表的分组管理**：
- 不同业务或环境的表放在不同 Namespace：`ecommerce:orders`、`warehouse:inventory`
- 便于分类统计和权限管理

**3. 权限控制（ACL）**：
```bash
# 给用户分配命名空间级别的权限
hbase> grant 'user1', 'RW', '@ns1'
# @ 前缀表示命名空间权限
```

**4. 快照管理**：
```bash
# 按命名空间列出快照
hbase> list_snapshots 'ns1'
```

**Namespace 与表的关系**：
```
HBase 集群
├── hbase:meta（系统表，记录所有表和 Region 的映射关系）
├── hbase:namespace（系统表，记录命名空间信息）
├── hbase:acl（系统表，记录权限信息）
│
├── default（默认命名空间）
│   ├── user_table_1
│   └── user_table_2
│
└── production（业务命名空间）
    ├── orders
    ├── users
    └── logs
```

**使用场景**：
- **多租户系统**：不同租户使用不同的 Namespace，配合配额和 ACL 实现隔离
- **环境隔离**：dev、test、prod 三个命名空间分别管理不同环境的表
- **业务分组**：按业务线划分，如 `finance:account`、`crm:customer`

📖 来源：https://hbase.apache.org/book.html#namespaces

---分割线---

### 题目4：HBase 的 Scan 操作如何优化？请从缓存、区间扫描、并行化等方面说明。

【答案】

**Scan 是 HBase 中最常用的范围查询操作，优化 Scan 对性能至关重要。**

**1. 合理设置 Cache（缓存批次大小）**：
```java
Scan scan = new Scan();
scan.withStartRow(startKey);
scan.withStopRow(stopKey);
scan.setBatch(10);           // 每行最多返回的列数
scan.setCacheBlocks(true);   // 是否缓存结果块（默认true）
scan.setCaching(1000);       // 每次 RPC 最多返回的行数（重要！）
```
- `setCaching(1000)`：每次从 RegionServer 取 1000 行，而不是 1 行，减少 RPC 次数
- 但不要设置过大（10000+），会导致一次 RPC 超时，且占用大量内存

**2. 只获取需要的列（Select 列）**：
```java
scan.addColumn(Bytes.toBytes("cf1"), Bytes.toBytes("qualifier1"));
scan.addColumn(Bytes.toBytes("cf1"), Bytes.toBytes("qualifier2"));
// 只返回指定的列，减少网络传输和数据解析开销
```
- 不使用 `addColumn` 会返回所有列，增加 IO

**3. 利用 RowKey 有序特性，减少扫描范围**：
```java
// ✅ 正确：利用 RowKey 前缀精确限定范围
Scan scan = new Scan("user_1001_".getBytes(), "user_1001~".getBytes());

// ❌ 错误：全表扫描，即使只查一个用户
Scan scan = new Scan();
scan.setFilter(new SingleColumnValueFilter(...));
```

**4. 设置最大版本数和时间范围**：
```java
scan.setMaxVersions(1);                    // 只返回最新版本
scan.setTimeRange(1625100000000L, 1625200000000L);  // 只查指定时间范围
```
- 减少需要处理的数据量

**5. 使用 BloomFilter 加速**：
```java
// 在建表时启用 BloomFilter
create 'table', {NAME => 'cf', BLOOMFILTER => 'ROWCOL'}
// ROWCOL：对 RowKey+Column 级别过滤，比 ROW 更精确
```

**6. 并行化 Scan（Multi-Threaded Scanning）**：
```java
TableDescriptor tableDesc = TableDescriptorBuilder
    .newBuilder(TableName.valueOf("t"))
    .setValue("hbase.client.scanner.timeout.period", "60000")
    .build();

// 使用线程池并发扫描多个 Region
ExecutorService pool = Executors.newFixedThreadPool(10);
List<Future<Result[]>> results = new ArrayList<>();

for (String regionName : regionNames) {
    Scan scan = new Scan();
    scan.withStartRow(regionStartKey);
    scan.withStopRow(regionEndKey);
    scan.setCaching(500);
    results.add(pool.submit(() -> table.getScanner(scan)));
}
```

**7. 避免 Scan 进入热点 Region**：
- 如果 RowKey 设计不当，Scan 可能集中在少数热点 Region
- 结合 RowKey 散列原则，避免热点

**8. 关闭 BlockCache（针对只读扫描）**：
```java
Scan scan = new Scan();
scan.setCacheBlocks(false);  // 不缓存到 BlockCache，节省缓存空间
```
- 全表扫描、历史数据分析等场景适用，避免污染热点数据的缓存

**9. 使用 Filter 的注意事项**：
```java
// ❌ 错误：使用 Filter 导致无法利用 RowKey 有序性优化
Filter filter = new RowFilter(CompareOperator.GREATER_OR_EQUAL, 
    new BinaryComparator(startKey.getBytes()));
scan.setFilter(filter);  // 必须在服务端扫描所有行再过滤

// ✅ 正确：直接用 StartRow/StopRow 限定范围
scan.withStartRow(startKey);
scan.withStopRow(stopKey);
```
- **Filter 会导致全 Region 扫描**，无法利用 RowKey 索引优化
- 优先使用 StartRow/StopRow 限定范围

📖 来源：https://hbase.apache.org/book.html#scan

---分割线---

### 题目5：HBase 的 WAL（预写日志）机制是什么？如果 RegionServer 宕机，数据如何恢复？

【答案】

**WAL（Write-Ahead Log）概念**：
WAL 是 HBase 实现高可靠性的核心机制。在将数据写入 MemStore 之前，先将操作记录追加写入到 WAL 日志中。如果系统宕机，可以通过回放 WAL 来恢复未持久化到 HFile 的数据。

**为什么需要 WAL**：
| 存储层 | 位置 | 可靠性 | 速度 |
|--------|------|--------|------|
| MemStore | 内存 | 断电丢失 | 极快 |
| WAL | HDFS（3副本） | 高可靠 | 快 |
| HFile | HDFS | 高可靠 | 慢（顺序写入） |

**写入流程**：
```
客户端 PUT 请求
       ↓
  写入 WAL（HDFS，三副本）← 关键：保证数据不丢失
       ↓
  写入 MemStore（内存）
       ↓
  返回客户端 ACK（写入成功）
       ↓
  异步：MemStore 刷写成 HFile
```

**WAL 存储结构**：
```
WAL 文件（HDFS）
├── WAL 文件头（Header）
├── 数据块（Data Block）
│   ├── Entry 1: RowKey-A, Column, Value, SequenceId
│   ├── Entry 2: RowKey-B, Column, Value, SequenceId
│   └── ...
└── 同步标记（Sync）
```

- 每个 Entry 都有唯一的 `SequenceId`（递增），用于恢复时的顺序保证
- WAL 是追加写的顺序日志，支持高效写入

**WAL 滚动（Rolling）**：
```bash
# WAL 文件按大小或时间滚动，生成新文件
# 触发条件：
# 1. 当前 WAL 大小超过 hbase.regionserver.maxlogs（默认 32）个
# 2. 每隔 hbase.regionserver.logroll.period（默认 3600000ms = 1小时）
```
- 旧的 WAL 文件在所有 MemStore 刷写完成后会被删除

**RegionServer 宕机恢复流程**：

```
1. 检测阶段：
   ZooKeeper 心跳超时（默认 3分钟）→ 判定 RegionServer 宕机
   Master 收到通知，开始故障恢复

2. 日志分配阶段：
   Master 从 ZK 获取 WAL 日志位置
   将宕机 Region 的 WAL 日志分配给新的 RegionServer

3. 回放（Replay）阶段：
   新 RegionServer 读取 WAL 文件
   按 SequenceId 顺序回放：KeyValue → 写入 MemStore
   完成后清空该 WAL 日志

4. Region 重分配阶段：
   Master 将宕机的 Region 重新分配到其他 RegionServer
   客户端通过 .META. 表感知新的 Region 位置
```

**恢复原理**：
```
RegionServer 宕机前：
  WAL: [Entry-Seq:100, Entry-Seq:101, Entry-Seq:102]  (3条未刷写)
  MemStore: [Entry-Seq:103]  (已有1条刷写)

恢复后：
  从 Entry-Seq:100 开始回放
  将 100、101、102 重新写入 MemStore
  最终状态与宕机前一致（不丢失数据）
```

**WAL 相关配置参数**：
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `hbase.wal.provider` | `filesystem` | WAL 存储方式（filesystem/asyncfs） |
| `hbase.regionserver.maxlogs` | 32 | 最大 WAL 文件数，超过触发强制刷写 |
| `hbase.regionserver.logroll.period` | 3600000ms | WAL 滚动周期 |
| `hbase.wal.replication` | true | 是否启用 WAL 复制（跨集群容灾） |

**可靠性保证**：
- WAL 写入 HDFS（三副本），即使整个服务器损坏，HDFS 仍有完整日志
- 借助 HDFS 的强一致性保证，WAL 写入成功即保证数据不丢失
- 每个 Cell 的修改都有对应的 WAL Entry，通过 SequenceId 追踪

**性能影响**：
- WAL 是写路径上的必经过渡，会影响写入性能
- 但由于 WAL 是顺序写 HDFS（高吞吐），且写完即返回，性能损耗可控
- 异步刷写机制使 HBase 写入速度远高于随机写入磁盘的传统数据库

📖 来源：https://hbase.apache.org/book.html#wal

---分割线---

> 📌 本题库每日更新，欢迎 Star：https://github.com/qq286158530/interview-questions
