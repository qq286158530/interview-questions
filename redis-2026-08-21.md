# Redis 面试题 — 2026-08-21

> 来源：小林coding《图解 Redis》https://xiaolincoding.com/redis/base/redis_interview.html

---

## 题目一：Redis 和 Memcached 有什么区别？为什么 Redis 用作缓存？

### 参考答案

**共同点：**
- 都是基于内存的数据库，常用作缓存
-都有过期策略
- 性能都非常高

**区别：**
- **数据类型**：Redis 支持 String、Hash、List、Set、ZSet 等丰富数据类型；Memcached 仅支持简单的 key-value
- **持久化**：Redis 支持 RDB + AOF 持久化，宕机可恢复；Memcached 无持久化，数据全在内存，挂了就没了
- **集群**：Redis 原生支持主从、哨兵、Cluster 模式；Memcached 无原生集群，需依赖客户端分片
- **功能**：Redis 支持发布订阅、Lua 脚本、事务等；Memcached 不支持

**为什么选 Redis 作 MySQL 缓存？**
- **高性能**：Redis 直接操作内存，QPS 可达 10W+，是 MySQL 的 10 倍
- **高并发**：绕过数据库直接命中缓存，减少数据库压力

---

## 题目二：Redis 的过期删除策略是什么？如何避免内存浪费？

### 参考答案

Redis 采用「**惰性删除 + 定期删除**」组合策略：

**惰性删除**：每次访问 key 时才检查是否过期，过期则删除
- 优点：对 CPU 友好，几乎不占系统资源
- 缺点：过期 key 长期不被访问会占用内存（内存不友好）

**定期删除**：每隔一段时间随机从过期字典中抽取 20 个 key 检查，删除已过期的 key；如果已过期比例超过 25%，继续抽取；循环时间上限 25ms
- 优点：定期清理部分过期键，平衡 CPU 与内存
- 缺点：难以精确控制频率

两者结合——惰性删除负责"守株待兔"，定期删除负责"主动出击"，在 CPU 时间和内存占用之间取得平衡。

---

## 题目三：Redis 持久化有哪几种方式？RDB 和 AOF 各自优缺点是什么？

### 参考答案

Redis 有三种持久化方式：

**1. RDB（快照）**
将某一时刻内存数据以二进制格式写入磁盘，是全量快照。
- `save` 在主线程执行，会阻塞；`bgsave` 创建子进程执行，不阻塞
- 恢复速度快，但可能丢失最近一次快照后的数据
- 利用 COW（写时复制）技术，bgsave 期间主线程可继续处理命令

**2. AOF（日志）**
每执行一条写命令就追加到文件末尾，是增量日志。
- 写回策略有三种：`always`（同步写回）、`everysec`（每秒写回，默认）、`no`（由操作系统决定）
- 数据丢失少，但恢复速度慢（需逐条执行命令）
- AOF 文件过大会触发重写（bgrewriteaof 子进程），合并重复命令压缩文件

**3. 混合持久化（Redis 4.0+）**
AOF 重写时，先以 RDB 格式写入全量数据，再以 AOF 格式追加增量命令。
- 兼具 RDB 快速恢复和 AOF 少丢失的优点
- 缺点：AOF 可读性差，不兼容旧版本

---

## 题目四：什么是缓存雪崩、击穿、穿透？如何应对？

### 参考答案

**缓存雪崩**：大量缓存数据在同一时间过期，导致大量请求直接打数据库
- 解决：给过期时间加随机偏移量（1~10 分钟），避免集体失效；或设置缓存不过期，由后台更新

**缓存击穿**：某个热点 key 过期，瞬间大量并发请求直接打数据库
- 解决：互斥锁（setNX）保证只有一个线程重建缓存；或热点数据不过期，由后台异步更新

**缓存穿透**：查询既不在缓存也不在数据库的数据（如恶意攻击），导致数据库压力骤增
- 解决：
  - 合法请求限制：API 入口校验参数合理性
  - 空值/默认值：缓存一个空值或默认值挡住后续请求
  - 布隆过滤器：在写入数据库时标记，数据到达时先查布隆过滤器快速判断是否存在

---

## 题目五：Redis 主从复制原理是什么？哨兵模式解决了什么问题？

### 参考答案

**主从复制（Replication）**
- 采用一主多从架构，主库可读写，从库只读
- 主库执行写命令后，异步同步给从库（不保证强一致性）
- 同步方式：首次全量同步（RDB 文件传输），后续增量同步（命令传播）

**哨兵模式（Sentinel）**
- 解决了主从架构中"故障需人工恢复"的问题
- 核心功能：**监控**（检测主从节点是否宕机）、**自动故障转移**（选举新主节点）、**通知**（告知客户端新的主节点地址）
- 哨兵本身也是集群部署，避免单点故障

**切片集群（Redis Cluster）**
- 当数据量单节点无法承载时使用
- 采用 **16384 个哈希槽**，key 通过 CRC16 取模映射到槽，槽分配到不同节点
- 支持手动分配槽，也支持自动平均分配

**脑裂问题**：主节点网络断开但与客户端连通，哨兵选举出新主节点后出现两个主节点，网络恢复后旧主降为从节点会清空本地数据导致之前写入的数据丢失。解决：配置 `min-slaves-to-write` 和 `min-slaves-max-lag`，限制从库数量不足或复制延迟过大时主库拒绝写入。

---

> 📚 以上题目整理自小林coding《图解 Redis》系列
> 🔗 原文链接：https://xiaolincoding.com/redis/base/redis_interview.html
