# Spark 核心面试题 · 2026-08-20

> 涵盖 Spark 架构、RDD、DataFrame/DataSet、Shuffle、内存管理等核心知识点

---

## 题目 1：Spark 与 Hadoop 的核心区别是什么？为什么 Spark 比 Hadoop 快？

**参考答案：**

Spark 与 Hadoop 的核心区别在于**计算模型和数据处理方式**：

| 对比维度 | Hadoop MapReduce | Spark |
|---------|-----------------|-------|
| **计算模型** | 每次 Map + Reduce 后将结果写磁盘 | 算子链在内存中完成，迭代不出盘 |
| **数据共享** | 各阶段通过 HDFS 共享，重复读写 | 基于 RDD/Dataset 抽象，内存中共享 |
| **延迟** | 高延迟（批处理，非实时） | 低延迟（支持流处理、微批） |
| **容错** | 通过 Replicate 实现，代价高 | 通过 Lineage + Checkpoint 实现，轻量 |
| **资源开销** | 每阶段启动 JVM，overhead 大 | Executor 长期运行，复用 JVM |

**Spark 更快的本质原因：**

1. **内存计算**：Spark 将数据保留在内存中（前提是内存充足），避免频繁磁盘 I/O
2. **DAG 优化**：一个 job 内的多个 Stage 可在同一个 Executor 内 pipeline 执行，减少 shuffle
3. **惰性求值**：Transformation 阶段只记录 lineage，Action 时才真正触发计算，Spark 可在整个 DAG 层面做优化
4. ** Tungsten & Unsafe Row**：自定义内存管理、off-heap 存储、CPU cache 友好，大幅降低 GC 开销

> **来源：** [DataFlair - Apache Spark Interview Questions](https://data-flair.training/blogs/spark-interview-questions/)  
> **来源：** [InterviewBit - Apache Spark Interview Questions](https://www.interviewbit.com/apache-spark-interview-questions/)

---

## 题目 2：RDD 的惰性求值（Lazy Evaluation）是如何工作的？惰性求值带来了哪些优化机会？

**参考答案：**

**惰性求值的原理：**

- RDD 的 **Transformation**（如 `map`、`filter`、`flatMap`）只是**记录计算逻辑**（构建 DAG），并不会立即执行
- 只有遇到 **Action**（如 `collect`、`count`、`saveAsTextFile`）时，Spark 才会：
  1. 根据 DAG 生成物理执行计划
  2. 调度 Task 到 Executor 执行
  3. 返回结果给 Driver

**DAG 的构建过程：**

```
textFile("/path")          ← RDD[String]（不读取数据）
  .flatMap(_.split(" "))   ← Transformations（只记录）
  .map((_, 1))             ← Transformations（只记录）
  .reduceByKey(_ + _)      ← Transformation（触发 shuffle 规划）
  .collect()               ← ACTION → 触发真正执行
```

**惰性求值带来的优化机会：**

1. **Pipeline 融合**：连续的窄依赖（如 map→filter→map）会在同一 Task 内 pipeline 执行，无需物化中间结果
2. **全链路重算优化**：Spark 可看到完整的计算图，选择最优策略（如谓词下推）
3. **减少数据物化**：无需在每个 Stage 后将数据写磁盘或网络传输，仅在 Shuffle 时才物化
4. **Code Generation（钨丝计划）**：Catalyst 在执行阶段将整个表达式树编译为 JVM 字节码，减少虚函数调用

```scala
// 示例：filter 后下推投影
rdd.filter(_.age > 18).map(_.name)
// 优化后可先投影 name 再过滤，减少序列化/反序列化开销
```

> **来源：** [DataFlair - Lazy Evaluation in Spark](https://data-flair.training/blogs/spark-interview-questions/)  
> **来源：** [Apache Spark Official Documentation - RDD Lineage](https://spark.apache.org/docs/latest/rdd-programming-guide.html)

---

## 题目 3：Spark Shuffle 的原理是什么？为什么 Shuffle 是性能瓶颈？如何优化？

**参考答案：**

**Shuffle 的原理：**

Shuffle 是 **Spark 将数据按 Key 重新分布**的过程，发生在 `reduceByKey`、`groupByKey`、`join`、`repartition` 等需要跨节点数据重分布的算子中。

执行流程：

```
Stage 1 (Map Stage)
  ↓  mapPartitions：每个分区对数据做本地聚合
  ↓  shuffle write：将分区数据按 partitioner 写入本地磁盘，生成 shuffle 文件
       ────────────────────────────────────────── network I/O
Stage 2 (Reduce Stage)
  ↓  shuffle read：每个 Reduce Task 去所有 Map 节点的 shuffle 文件拉取属于自己的数据
  ↓  按 Key 聚合/排序
```

**Shuffle 是瓶颈的原因：**

1. **磁盘 I/O**：Shuffle 数据必须落盘（Hadoop MapReduce 习惯），虽然 Spark 也做，但内存缓存可缓解
2. **网络 I/O**：跨节点数据传输是主要开销
3. **序列化开销**：数据需要序列化（Java/Kryo）才能网络传输
4. **内存压力**：Reduce 端需要在内存中聚合数据，容易 OOM
5. **磁盘空间**：Shuffle 文件默认会落盘，占用大量磁盘

**优化策略：**

| 优化方向 | 具体措施 |
|---------|---------|
| **减少 Shuffle 数据量** | 使用 `mapSideCombine`（等价于在 Map 端先做预聚合） |
| **合理设置分区数** | 分区数 ≈ 总 CPU cores * 2~4；太少任务不均衡，太多小文件 |
| **使用广播变量** | 小表广播可避免大表 Shuffle（如 `broadcast(fact.join(broadcast(dim)))`） |
| **启用 SortShuffle** | Spark 2.x+ 默认 SortShuffle，减少文件数（`spark.shuffle.sort.bypassMergeThreshold: 200`） |
| **调整缓冲** | `spark.shuffle.file.buffer.size: 1MB`（默认 32KB）减少随机写 |
| **使用 Kryo 序列化** | `spark.serializer=org.apache.spark.serializer.KryoSerializer` |
| **避免 groupByKey** | groupByKey 先 Shuffle 再聚合，先在 Map 端 combine 的 reduceByKey 更优 |

```scala
// 优化示例：用 reduceByKey 替代 groupByKey
// 差的写法（数据量大）：
rdd.groupByKey().mapValues(_.sum)

// 好的写法（Map 端预聚合）：
rdd.reduceByKey(_ + _)  // Shuffle 前后各做一次局部聚合，网络传输数据量大幅减少
```

> **来源：** [DataFlair - Spark Shuffle Optimization](https://data-flair.training/blogs/spark-interview-questions/)  
> **来源：** [Spark Official - Tuning Guide](https://spark.apache.org/docs/latest/tuning.html)

---

## 题目 4：Spark 内存管理模型是怎样的？Executor 内存如何划分？

**参考答案：**

Spark 使用 **Unified Memory Management**（统一内存管理，Spark 1.6+），将 Executor 堆内内存划分为两大区域：**存储内存** 和 **执行内存**，二者可动态借用。

**内存区域划分（`spark.memory.fraction=0.6`，`spark.memory.storageFraction=0.5`）：**

```
┌─────────────────────────────────────────┐
│            Executor 堆内内存             │
│  ┌───────────┬───────────────────────┐ │
│  │ 用户内存   │     统一内存            │ │
│  │ 40%       │      60%               │ │
│  │           │ ┌─────────┬─────────┐ │ │
│  │           │ │ 存储内存 │ 执行内存 │ │ │
│  │           │ │ (动态)   │ (动态)   │ │ │
│  │           │ └─────────┴─────────┘ │ │
│  └───────────┴───────────────────────┘ │
└─────────────────────────────────────────┘
```

**各区域详解：**

| 内存区域 | 占比 | 用途 |
|---------|------|------|
| **系统预留** | ~300MB | JVM 自身开销 |
| **用户内存** | 40% | 用户代码（SparkContext 初始化、RDD 衍生物等） |
| **统一内存** | 60% | 存储 + 执行，可动态借用 |
| ↳ **存储内存** | 动态 | RDD Cache、Broadcast、Unroll |
| ↳ **执行内存** | 动态 | Shuffle 排序、Hash 聚合、代码缓存 |
| **外部存储** | 堆外 | Off-heap 内存（`spark.memory.offHeap.enabled`），Tungsten 使用 |

**动态借用规则：**

- 当存储内存空闲时，可被执行内存借用（用于大排序等）
- 当执行内存空闲时，可被存储内存借用（用于缓存）
- 存储内存占用的数据**不可被驱逐**（`storage.memoryFraction` 以下的可以）
- 执行内存可以被存储内存抢占，抢占后不会导致执行失败，只是变慢

**常见 OOM 原因：**
- `reduceByKey` 等聚合操作超出执行内存 → 降低分区数或开启 mapSideCombine
- `collect` 将数据拉回 Driver 超出用户内存 → 避免大结果 collect，用 sample 或 filter 先过滤
- `groupByKey` + 大数据量 → 改用 `reduceByKey` 或调整 `spark.memory.fraction`

```scala
// 查看内存使用
sc.getConf.get("spark.executor.memory")        // 总内存
sc.getConf.get("spark.memory.fraction")        // 统一内存比例
```

> **来源：** [DataFlair - Spark Memory Management](https://data-flair.training/blogs/spark-interview-questions/)  
> **来源：** [Apache Spark Official - Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management)

---

## 题目 5：广播变量（Broadcast）和累加器（Accumulator）的作用是什么？有哪些使用注意事项？

**参考答案：**

**广播变量（Broadcast）：**

广播变量是 **只读的、分布式共享的变量**，将一份小数据发送到每个 Executor（只发一次），避免在每个 Task 中重复传输。

```scala
// 将 lookup 表广播到所有节点
val lookupTable: Map[String, String] = ...  // 小表
val broadcastVar = sc.broadcast(lookupTable)

rdd.map(record => {
  val info = broadcastVar.value.getOrElse(record.key, "default")
  // ...
})
```

**典型使用场景：**
- 大表 join 小表（小表广播，避免 Shuffle）
- 维度表（用户信息、商品信息等）缓存
- 静态配置、阈值等只读数据

**注意事项：**
- 只适合**小数据集**（太大导致 Executor OOM）
- 广播后不可修改（只读）
- 使用后需主动 `unpersist()` 释放缓存：`broadcastVar.unpersist()`
- Spark 2.x 后 Broadcast Hash Join 会自动应用（`autoBroadcastJoinThreshold`，默认 10MB）

---

**累加器（Accumulator）：**

累加器是 **只写的共享变量**，用于在 Task 间累积计数（如统计总数、错误数），只保证**最终一致性**，中间过程可能有重复计数。

```scala
// 统计空行数量
val emptyLines = sc.longAccumulator("EmptyLines")

rdd.foreach { line =>
  if (line.isEmpty) {
    emptyLines.add(1)
  }
}

println(s"空行数: ${emptyLines.value}")  // Action 后才能获取值
```

**注意事项：**

| 问题 | 说明 |
|-----|------|
| **只保证最终一致** | Task 重试时可能重复计数，多次 Task 并行时不能做精确除法 |
| **只应在 Action 中使用** | 在 Transformation 中使用累加器，结果不可预测（因为 Transformation 可能执行多次） |
| **自定义类型累加器** | 需继承 `AccumulatorV2` 并实现 `reset/add/isZero/merge` 等方法 |
| **Task 内多次使用** | 同一个 Task 内对同一累加器多次 add 会累加 |

```scala
// 自定义累加器示例
class WordCountAccumulator extends AccumulatorV2[String, mutable.HashMap[String, Int]] {
  private val counter = mutable.HashMap[String, Int]()

  override def reset(): Unit = counter.clear()
  override def add(word: String): Unit = counter(word) = counter.getOrElse(word, 0) + 1
  override def merge(other: AccumulatorV2[String, mutable.HashMap[String, Int]]): Unit = {
    other.value.foreach { case (word, count) =>
      counter(word) = counter.getOrElse(word, 0) + count
    }
  }
  override def value: mutable.HashMap[String, Int] = counter
  override def copy(): AccumulatorV2[String, mutable.HashMap[String, Int]] = new WordCountAccumulator
}
```

**Broadcast vs Accumulator 对比：**

| 特性 | Broadcast | Accumulator |
|-----|-----------|-------------|
| 读写 | 只读 | 只写 |
| 用途 | 分发只读数据 | 累积计数 |
| 一致性 | 无问题 | 仅最终一致 |
| 使用限制 | 小数据 | 简单数值类型或自定义 V2 |

> **来源：** [DataFlair - Shared Variables in Spark](https://data-flair.training/blogs/spark-interview-questions/)  
> **来源：** [Apache Spark Official - Broadcast Variable](https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables)  
> **来源：** [Apache Spark Official - Accumulators](https://spark.apache.org/docs/latest/rdd-programming-guide.html#accumulators)

---

*整理自互联网公开面试题资源，仅供学习参考*
