# HBase 面试题（2026-08-16）

> 涵盖 HBase 架构、RowKey 设计、存储结构、MemStore/HFile、协处理器等核心知识点

---

## 题目 1：HBase 的整体架构是怎样的？Client/ZooKeeper/RegionServer/HMaster 分别承担什么职责？

### 参考答案

HBase 采用 **Master-Slave 架构**，核心组件及职责如下：

| 组件 | 职责 |
|------|------|
| **Client** | 访问 HBase 的入口，使用 RPC 与 RegionServer 通信，支持缓存 Meta 路由信息 |
| **ZooKeeper** | 实现分布式协调，负责选举 Master、存储 Meta 表位置、检测 RegionServer 存活状态 |
| **HMaster** | 负责表（Table）的创建/删除/修改，分配 Region 到 RegionServer，监控集群状态，负载均衡 |
| **RegionServer** | 负责数据的读写操作，一个 RegionServer 管理多个 Region；与 HDFS DataNode 交互完成实际存储 |

**工作流程简述：**
1. Client 连接 ZooKeeper，获取 Meta 表所在的 RegionServer 地址
2. Client 查询 Meta 表，找到目标数据所在的 RegionServer
3. Client 直接与目标 RegionServer 通信，进行数据读写
4. HMaster 负责 Region 的分配和调度，不参与实际数据操作

**来源：** [CSDN - HBase 面试题整理](https://blog.csdn.net/qq_36482751/article/details/107097886)

---

## 题目 2：HBase 的 RowKey 设计应该遵循哪些原则？为什么？

### 参考答案

RowKey 是 HBase 数据访问的核心，设计原则如下：

### 1. **RowKey 长度原则**
- RowKey 应尽可能短（建议不超过 16 字节）
- 原因：MemStore 内存存储、KeyValue 对象、HFile 索引都会占用内存；且每次 RPC 传输都会携带 RowKey

### 2. **RowKey 唯一性原则**
- RowKey 必须唯一标识一行数据，否则会发生数据覆盖

### 3. **散列/打散原则**
- 避免热点问题（HotSpot）：不要使用时间戳开头、连续递增的数字作为 RowKey
- 常用方法：MD5 散列、盐值（Salt）前缀、哈希反转

### 4. **业务查询导向原则**
- 将查询条件放在 RowKey 前缀位置，充分利用 HBase 按 RowKey 字典序排序的特性
- 支持范围查询时，RowKey 前缀需有序

### 热点场景举例
```
# 错误设计（热点）
RowKey: 20260816_001, 20260816_002, ...  # 连续递增，写入集中在同一 RegionServer

# 正确设计（打散）
RowKey: MD5(设备ID)_20260816_001  # 哈希打散后分布到不同 Region
```

**来源：** [阿里云开发者社区 - HBase RowKey 设计指南](https://developer.aliyun.com/article/762525)

---

## 题目 3：HBase 的 MemStore 和 HFile 是什么？它们之间的关系是怎样的？

### 参考答案

### MemStore（写缓存）
- 位于 **RegionServer 内存**中，每个 Column Family 对应一个 MemStore
- 数据写入顺序：先写 WAL（Write-Ahead Log）→ 再写入 MemStore
- MemStore 达到阈值（默认 `hbase.hregion.memstore.flush.size=128MB`）时，触发 **flush** 操作

### HFile（HBase 的存储文件）
- HBase 数据以 HFile 格式存储在 **HDFS** 上
- HFile 结构（从旧版到新版 BlockEncoding）：
  ```
  Data Block（数据块）→ Meta Block（可选）→ File Info → Index Block → Bloom Filter → Trailer
  ```
- Data Block 默认 64KB，用于存储 KeyValue 键值对
- 支持 **Bloom Filter** 快速判断某 Key 是否存在于文件中

### MemStore → HFile 的流程
```
1. 数据写入 MemStore（同时顺序写入 WAL）
2. MemStore 写满 → flush 成 HFile（临时文件）
3. HFile 合并（Minor Compaction / Major Compaction）
4. HFile 最终落盘到 HDFS
```

### 为什么要先写 MemStore？
- 内存写入速度远快于磁盘 IO，直接写 HDFS 会严重拖累性能
- MemStore 批量 flush 成大文件，减少 HDFS 小文件问题

**来源：** [HBase 权威指南](https://hbase.apache.org/book.html#arch.regionserver.flushing)

---

## 题目 4：HBase 的存储结构（KeyValue、MemStore flush、Compaction）是怎样的？

### 参考答案

### KeyValue 结构
HBase 中每行数据的每个列版本存储为一个 **KeyValue**，结构如下：
```
[Key Length (4B)] [Value Length (4B)] 
[RowKey] [Column Family] [Qualifier] [Timestamp] [Type] 
[Value]
```
- Type：指明该 KeyValue 是 Put、Delete、DeleteColumn 等操作类型

### 写入流程
```
Client → WAL（RegionServer 本地 HDFS）→ MemStore → flush → HFile
```

### HFile 合并机制（Compaction）

| 类型 | 触发条件 | 行为 |
|------|----------|------|
| **Minor Compaction** | HFile 数量超过阈值（默认 3） | 合并多个小 HFile 为一个，不删除过期数据 |
| **Major Compaction** | 自动触发（默认 7 天）或手动 | 合并所有 HFile，**删除过期数据和墓碑标记** |

### 墓碑标记（Tombstone Marker）
- 当删除一行或一列时，不会立即删除数据，而是写入一个 **墓碑标记**
- Major Compaction 时才真正清理被删除的数据

**来源：** [HBase 官方文档 - Compaction](https://hbase.apache.org/book.html#compaction)

---

## 题目 5：HBase 协处理器（Coprocessor）是什么？有哪几种类型？各自的使用场景是什么？

### 参考答案

### 什么是协处理器
协处理器是运行在 **RegionServer** 上的插件代码，允许用户在数据读写时自动执行自定义逻辑，无需修改 HBase 核心代码。

### 两种协处理器类型

#### 1. Observer（观察者）—— 类似触发器
- 挂载到特定事件上，事件触发时执行回调
- **类型及 Hook 列表：**
  - `RegionObserver`：表扫描、GET、PUT、DELETE、Batch 操作前后
  - `MasterObserver`：表创建、删除、修改等 DDL 操作前后
  - `WALObserver`：WAL 写入前后

- **典型应用场景：**
  - 数据写入前校验（如 value 合法性检查）
  - 自动构建二级索引（写入主表时同步更新索引表）
  - 权限控制（基于列级权限过滤）

#### 2. Endpoint（端点）—— 类似存储过程
- 允许在 RegionServer 上执行自定义计算，并将结果返回给 Client
- 类似关系型数据库的 **存储过程**
- **典型应用场景：**
  - 在服务端聚合统计（如 SUM、COUNT）
  - 自定义过滤逻辑下推到 RegionServer 执行，减少网络传输
  - 批量数据转换

### 使用示例（RegionObserver 二级索引）
```
# 伪代码示例
onPostPut(Put put) {
    // 写入主表后，自动构建二级索引
    Table indexTable = connection.getTable("index_table");
    Put indexPut = new Put(put.getRow()); // 用主表 RowKey 作为索引
    indexTable.put(indexPut);
}
```

### 加载方式
- **静态加载**：通过 `hbase-site.xml` 配置，需重启集群（用于系统级协处理器）
- **动态加载**：通过 HBase Shell `alter` 命令在线加载（用于表级协处理器）

**来源：** [HBase 官方文档 - Coprocessor](https://hbase.apache.org/book.html#cp)

---

> 📌 以上题目均基于 HBase 核心原理整理，适合 1-5 年经验开发者面试准备
> 持续更新，欢迎 Star ⭐：[github.com/qq286158530/interview-questions](https://github.com/qq286158530/interview-questions)
