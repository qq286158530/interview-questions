# Flink 面试题 — 2026-08-18

> 本文档整理了5道高质量Flink面试题，涵盖核心知识点，适合面试准备。

---

## 题目一：Flink 的架构是怎样的？请描述 JobManager 和 TaskManager 的作用

### 参考答案

Apache Flink 采用**主从架构**，由 JobManager（主节点）和 TaskManager（工作节点）组成。

**JobManager 的职责：**
- 负责接收 Flink 作业（Job），将 Job 翻译成 ExecutionGraph（执行图）
- 协调作业调度，包括 task 的并行部署到 TaskManager
- 协调 Checkpoint 机制
- 协调故障恢复（Failover）
- 管理 TaskManager 的注册与心跳

**TaskManager 的职责：**
- 是 Flink 的工作进程，负责启动和管理 Task slot
- 每个 TaskManager 会将自己所在的服务器资源（内存、网络、CPU）划分为多个 TaskSlot
- 实际执行 DataStream API 或 DataSet API 生成的算子子任务（SubTask）
- 与 JobManager 保持心跳，汇报自身状态

**简单流程：**
1. 客户端（Client）提交 JobGraph 到 JobManager
2. JobManager 分析 JobGraph，生成 ExecutionGraph，按拓扑顺序调度到 TaskManager 执行
3. TaskManager 通过 Slot 机制共享资源并执行任务

> 📎 来源：[Apache Flink 官方文档 - Concepts Overview](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/overview/)

---

## 题目二：什么是 Watermark？Flink 如何利用 Watermark 处理乱序数据？

### 参考答案

**Watermark（水位线）** 是 Flink 中用于处理**乱序事件**和**事件时间（Event Time）**的机制。

**核心概念：**
- Watermark 本质上是一个时间戳（Timestamp），表示"**所有时间戳小于该 watermark 的数据都已经到达**"
- 当 Watermark >= 某个窗口的结束时间时，该窗口触发计算

**处理乱序的原理：**
- 数据在分布式传输中可能乱序，Watermark 用来"等待"迟到的数据
- 用户可以定义 Watermark 的生成策略，常见的有：
  - **固定延迟策略（FixedWatermarkStrategy）**：固定延迟多少毫秒
  - **单调递增策略（MonotonousWatermarkStrategy）**：保证 watermark 单调递增

**代码示例：**
```java
WatermarkStrategy<MyEvent> wms = WatermarkStrategy
    .<MyEvent>forBoundedOutOfOrderness(Duration.ofSeconds(10))
    .withTimestampAssigner((event, timestamp) -> event.getTimestamp());

DataStream<MyEvent> stream = env.fromSource(
    source, WatermarkStrategy.noWatermarks(), "MySource");
```

**关键点：**
- Watermark 解决的是"窗口何时触发"的问题
- 如果设置了 `allowedLateness`，窗口会在触发后额外等待一段时间接收迟到数据
- 迟到太久的数据可以通过 `sideOutputLateData` 侧输出流处理

> 📎 来源：[Apache Flink 官方文档 - Event Time & Watermarks](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/time_model/)

---

## 题目三：Flink 的 Checkpoint 和 Savepoint 有什么区别？如何实现 exactly-once 语义？

### 参考答案

**Checkpoint vs Savepoint：**

| 特性 | Checkpoint | Savepoint |
|------|-----------|-----------|
| 触发方式 | Flink 自动周期性触发 | 手动触发 |
| 用途 | 故障恢复 | 计划性停止/重启/升级 |
| 存储 | 作业失败自动清理 | 手动清理 |
| 依赖 | 状态后端（如 RocksDB） | 状态后端 |
| ID | 自动递增数字 | 用户指定名称/ID |

**Checkpoint 原理：**
- 基于 **Chandy-Lamport 分布式快照算法**
- JobManager 定期向所有 Source 算子注入 Barrier（屏障）
- Barrier 随数据流向下游传递，形成 Checkpoint 快照
- 快照包含：数据源位置（offset）+ 算子状态（State）

**实现 exactly-once 语义的条件：**
1. **数据源支持回放**（如 Kafka 的 offset 重置）
2. **开启 Checkpoint**：`env.enableCheckpointing(interval)`
3. **选择合适的 Checkpoint 模式**：
   - `EXACTLY_ONCE`：保证每条数据只被处理一次（开销较大）
   - `AT_LEAST_ONCE`：保证每条数据至少被处理一次（性能更好）
4. **状态后端选择**：RocksDBStateBackend（生产推荐）或 FsStateBackend（开发测试）

**代码示例：**
```java
env.enableCheckpointing(60_000); // 每60秒一次
env.getCheckpointConfig().setCheckpointingMode(CheckpointingMode.EXACTLY_ONCE);
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(500);
env.getCheckpointConfig().setCheckpointTimeout(600_000);
env.getCheckpointConfig().setMaxConcurrentCheckpoints(1);
```

> 📎 来源：[Apache Flink 官方文档 - Checkpointing](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/ops/state/checkpoints/)

---

## 题目四：Flink 的窗口函数有哪些类型？Session Window 和 Tumbling Window 的区别是什么？

### 参考答案

**窗口函数的主要类型：**

1. **时间窗口（Time Windows）**
   - **Tumbling Window（滚动窗口）**：固定大小、无重叠
     - 例如：每5分钟一个窗口，互不重叠
     - `TumblingEventTimeWindows.of(Time.minutes(5))`
   - **Sliding Window（滑动窗口）**：固定大小、可重叠
     - 例如：每5分钟计算一次，但窗口长度为10分钟
     - `SlidingEventTimeWindows.of(Time.minutes(10), Time.minutes(5))`
   - **Session Window（会话窗口）**：根据活跃间隔划分
     - 动态大小，没有固定开始/结束
     - 两条数据之间的间隔超过会话超时时间则生成新窗口
     - `SessionWindow.withGap(Time.minutes(10))`

2. **计数窗口（Count Windows）**
   - 当元素数量达到阈值时触发

3. **全局窗口（Global Window）**
   - 所有数据分配到同一个窗口，需配合触发器使用

**Session Window vs Tumbling Window 核心区别：**

| 特性 | Tumbling Window | Session Window |
|------|----------------|----------------|
| 大小 | 固定 | 动态 |
| 边界 | 固定对齐 | 不固定 |
| 触发时机 | 时间到达或数量达到 | 间隔超时 |
| 适用场景 | 固定周期的聚合统计 | 用户行为分析、会话统计 |
| 数据不遗漏 | 可能包含未来数据 | 只包含真正的会话数据 |

**示例场景：**
- Tumbling Window：每天的销售额统计、每分钟的PV统计
- Session Window：用户浏览商品的会话分析，用户下单流程分析

> 📎 来源：[Apache Flink 官方文档 - Windows](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/dev/datastream/operators/windows/)

---

## 题目五：Flink 的状态管理机制是什么？Keyed State 和 Operator State 有何区别？

### 参考答案

**Flink 的状态管理：**

Flink 是**有状态流处理**框架，状态（State）是指算子在处理过程中需要记住的信息。

**状态的类型：**

### 1. Keyed State（键控状态）
- **只能在 KeyedStream 上使用**（即 `keyBy()` 之后）
- 状态按 Key 分组，每个 Key 有独立的状态副本
- Flink 自动保证线程安全
- 支持的数据结构：
  - `ValueState<T>`：单一值
  - `ListState<T>`：列表
  - `MapState<K, V>`：键值对
  - `ReducingState<T>`：自动聚合
  - `AggregatingState<I, O>`：自定义聚合

**使用示例：**
```java
public class CountWindowAverage implements MapFunction<SensorReading, Tuple2<Integer, Double>> {
    private ValueState<Integer> countState;
    private ValueState<Double> sumState;

    @Override
    public void open(Configuration parameters) {
        countState = getRuntimeContext().getState(
            new ValueStateDescriptor<>("count", Integer.class));
        sumState = getRuntimeContext().getState(
            new ValueStateDescriptor<>("sum", Double.class));
    }

    @Override
    public Tuple2<Integer, Double> map(SensorReading value) {
        Integer count = countState.value() == null ? 0 : countState.value();
        Double sum = sumState.value() == null ? 0.0 : sumState.value();
        count++;
        sum += value.getTemperature();
        countState.update(count);
        sumState.update(sum);
        return new Tuple2<>(value.getId(), sum / count);
    }
}
```

### 2. Operator State（算子状态）
- 整个算子实例共享同一份状态
- 不需要 keyBy，可用于 Source算子（如 Kafka Consumer 的 offset）
- 支持三种重分布策略：
  - **ListState**：算子实例状态平均分配
  - **UnionListState**：故障恢复时所有算子都获取完整状态
  - **BroadcastState**：广播状态，所有实例持有相同副本

**Keyed State vs Operator State 对比：**

| 特性 | Keyed State | Operator State |
|------|------------|----------------|
| 作用域 | 按 Key 分组 | 算子级别 |
| 使用前提 | 必须 keyBy | 无需 keyBy |
| 状态隔离 | 每个 Key 独立 | 所有 subtask 共享 |
| 典型场景 | 聚合计算、CEP | Kafka offset、连接后广播数据 |
| 并行度变化 | 自动重新分配 | 需指定重分布策略 |

**状态后端：**
- `MemoryStateBackend`：开发测试用，状态存储在 JVM 堆内存
- `FsStateBackend`：生产小规模使用，状态存储在文件系统
- `RocksDBStateBackend`：生产大规模使用，增量 Checkpoint， LSM 树结构

> 📎 来源：[Apache Flink 官方文档 - State & Fault Tolerance](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/stateful-stream-processing/)

---

*整理日期：2026-08-18 | 面试题来源：Apache Flink 官方文档 & 社区经典面试题*
