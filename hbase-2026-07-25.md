# HBase 面试题（2026-07-25）

> 本文档整理了 5 道高质量的 HBase 面试题，涵盖架构、存储结构、RowKey 设计、协处理器等核心知识点。

---

## 题目一：HBase 架构组成及核心组件职责

### 题目
请描述 HBase 的整体架构，包含哪些核心组件？Zookeeper 在 HBase 中扮演什么角色？

### 答案

HBase 是一个基于列式存储的分布式 NoSQL 数据库，构建在 HDFS 之上。其核心架构分为客户端、Zookeeper协调层和 HBase 服务层。

**核心组件：**

1. **Zookeeper**
   - 提供分布式一致性服务，负责 HBase 的元数据管理和集群协调
   - 维护 `.META.` 表的地址和当前集群的 Master 地址
   - 监听 RegionServer 的上下线，当 RegionServer 宕机时触发恢复流程
   - 避免 HMaster 的单点故障，实现 Master 高可用

2. **HMaster**
   - 是 HBase 的主节点，负责管理元数据（表结构、Region 分布）
   - 分配和调度 Region 到各个 RegionServer
   - 监控 RegionServer 的健康状态，宕机时触发 Region 迁移
   - 处理建表、删表等 DDL 操作
   - 在有多个 HMaster 时，通过 Zookeeper 选举实现高可用

3. **RegionServer**
   - 是 HBase 的工作节点，负责数据的读写操作
   - 接收客户端请求，与 HDFS DataNode 交互完成数据读写
   - 管理 MemStore 和 HFile，实现数据的持久化存储
   - 定期执行 Compaction 合并操作，清理过期数据

4. **Region**
   - 是 HBase 水平扩展的基本单位，每个 Region 对应表中一段连续的行键范围
   - 一个表初始只有一个 Region，随着数据增长自动分裂（Split）

5. **Store**
   - 每个 Region 由一个或多个 Store 组成，每个 Store 对应一个列族
   - 包含 MemStore 和 StoreFile（HFile）

**数据读写流程简述：**
- 写数据：Client → Zookeeper → HMaster/HRegionServer → MemStore → Flush 到 HFile
- 读数据：Client → Zookeeper → .META.表定位Region → HRegionServer → MemStore/HFile 合并读取

**来源参考：**
- Apache HBase Official Documentation: https://hbase.apache.org/book.html
- 美团技术团队《HBase 存储架构与原理》: https://tech.meituan.com/2016/01/13/hbase-arch.html

---

## 题目二：HBase 的存储结构（MemStore + HFile）

### 题目
HBase 如何实现数据的持久化？MemStore 和 HFile 的关系是什么？Flush 的触发条件有哪些？

### 答案

**数据写入流程：**

1. 客户端请求先写入 WAL（Write-Ahead Log），保证数据可靠性
2. 数据随后写入 MemStore（内存缓冲区），按 RowKey 排序
3. 当 MemStore 达到阈值时，触发 Flush 操作，将数据刷写到磁盘生成 HFile

**MemStore 的特点：**
- 是一个基于 LSM-Tree（Log-Structured Merge-Tree）思想的内存结构
- 数据在 MemStore 中按 RowKey、Column Family、Column、TimeStamp 排序
- 支持快速写入，数据先入内存，后续再异步刷写到磁盘

**HFile 的特点：**
- HBase 自定义的存储格式，构建在 HDFS 之上
- HFile 是有序的 KeyValue 结构，包含 Data Block、Meta Block、Index Block、Bloom Filter 等
- 每个 HFile 都有对应的 Bloom Filter，用于快速判断某个 RowKey 是否存在
- 支持数据压缩（Data Block 级别）

**Flush 触发条件（满足任一即触发）：**
1. MemStore 大小达到 `hbase.hregion.memstore.flush.size`（默认 128MB）
2. Region 内所有 MemStore 总大小超过 `hbase.regionserver.global.memstore.size`（默认堆内存 40%）
3. WAL 文件数量超过 `hbase.regionserver.max.logs`（默认 32）
4. 定期刷新：RegionServer 每隔 `hbase.regionserver.optionalcacheflushinterval`（默认 1 小时）检查
5. 手动执行 flush 命令

**重要机制 — Compaction：**
- Minor Compaction：合并少量相邻的 HFile，保留所有版本数据
- Major Compaction：合并一个 Store 下所有的 HFile，清理已删除和过期数据，默认 7 天执行一次

**来源参考：**
- Apache HBase Reference Guide - Storage: https://hbase.apache.org/book.html#arch.store
- 华为云技术专栏《深入理解 HBase MemStore》: https://www.huaweicloud.com/

---

## 题目三：HBase RowKey 设计原则与最佳实践

### 题目
HBase 中 RowKey 设计的核心原则是什么？如何设计 RowKey 以避免热点问题？请举例说明。

### 答案

**RowKey 是 HBase 数据访问的核心，RowKey 设计直接影响读写性能和负载均衡。**

**核心原则：**

**1. RowKey 长度原则**
- RowKey 应尽可能短，建议不超过 16 字节
- 原因：每次读写都要传输 RowKey，过长的 RowKey 会浪费 I/O 和内存
- HBase 存储时以字节数组形式存储，过长的 RowKey 还会占用更多 HFile 空间

**2. RowKey 唯一性原则**
- RowKey 必须唯一标识一行数据，类似主键
- 如果多条数据使用相同 RowKey，后写入的数据会覆盖先前的版本

**3. 散列性原则（避免热点）**
- 热点问题：大量请求集中到少数节点上，导致负载不均
- 常见热点场景：使用时间戳或单调递增/递减的序列作为 RowKey 前缀

**热点解决方案：**

| 方案 | 实现方式 | 适用场景 |
|------|----------|----------|
| 盐值（Salting）| 在 RowKey 前加随机前缀（如 0/1/2/3_原key）| 读请求随机分布场景 |
| 哈希（Hashing）| 对业务 ID 做 MD5/Hash，取前几位做前缀 | 需要均匀分布的场景 |
| 反转（Reversing）| 反转固定格式的电话号码、时间戳 | 固定前缀导致热点 |
| 字符串拼接+MD5 | `md5(id).substring(0,6) + id` | 兼顾散列与可读性 |

**设计示例：**

```java
// 场景：统计用户行为日志（user_id, timestamp, action）
// 错误设计（热点）：
RowKey = user_id + timestamp  // 导致同一用户数据都在一个 Region

// 优化设计（散列）：
RowKey = MD5(user_id).substring(0, 6) + user_id + timestamp
// 或使用盐值
RowKey = (user_id % N) + "_" + user_id + timestamp
```

**Scan 场景的 RowKey 设计：**
- 如果需要按时间范围 Scan，将时间戳放在 RowKey 末尾（如 `user_id + (Long.MAX - timestamp)`）
- 这样可以充分利用 HBase 的 RowKey 有序特性，定位到起始 RowKey 后顺序 Scan

**来源参考：**
- Cloudera HBase Documentation: https://docs.cloudera.com/runtime/7.2.2/hbase-data-management/
- 《HBase 权威指南》第 6 章 - RowKey Design

---

## 题目四：HBase 协处理器（Coprocessor）的类型与使用场景

### 题目
HBase 协处理器是什么？它有哪两种类型？各自的职责和使用场景是什么？

### 答案

**协处理器概述：**

协处理器是 HBase 提供的扩展机制，允许用户将自定义代码部署到 RegionServer 上，在数据写入或读取时自动执行，实现了"让计算去数据所在的地方执行"的思想。

**两种类型：**

### 1. Observer 协处理器（观察者）

类似于数据库的触发器（Trigger），在特定事件前后自动执行回调方法。

**类型：**
- `PrePutCoprocessor`：在 Put 操作写入 WAL 和 MemStore **之前**执行，可用于数据验证、过滤
- `PostPutCoprocessor`：在 Put 写入 MemStore **之后**执行，可用于二级索引更新
- `PreGetCoprocessor`：在 Get 查询 **之前**执行，可用于列权限控制
- `PostGetCoprocessor`：在 Get 查询 **之后**执行
- `PreDeleteCoprocessor`：在 Delete 删除操作 **之前**执行
- `PostDeleteCoprocessor`：在 Delete **之后**执行
- `PreSplitCoprocessor`：在 Region 分裂 **之前**执行
- `PostSplitCoprocessor`：在 Region 分裂 **之后**执行

**典型应用场景：**
- 实现列级访问控制（如某些列需要特定权限才能查看）
- 写入时自动构建二级索引（将索引数据写入另一张表）
- 数据合法性校验（如拒绝不合规的数据格式）
- 操作审计日志

### 2. Endpoint 协处理器（端点）

类似于数据库的存储过程，允许在 RegionServer 上执行自定义的聚合计算，将计算逻辑下推到数据层。

**典型方法：**
- `get`：获取某个值
- `getLong`、`getMaxQuorum` 等聚合方法

**典型应用场景：**
- 在服务端执行 count、sum、avg 等聚合操作，减少网络传输
- 实现自定义的过滤和聚合逻辑
- 复杂计算下推，避免大量数据传输到客户端

**部署方式：**
1. 继承 `Coprocessor` 接口实现具体的 Observer 或 Endpoint
2. 打包成 JAR，上传到 HDFS
3. 通过 HBase Shell 或 Java API 关联到表或列族：

```hbase
disable 'my_table'
alter 'my_table', METHOD => 'table_att', 'coprocessor' => 'hdfs:///path/to/coprocessor.jar|com.example.MyObserverCoprocessor|1001|arg1=val1'
enable 'my_table'
```

**注意事项：**
- 协处理器在 RegionServer JVM 中执行，代码异常可能导致 RegionServer 不稳定
- 生产环境建议做好充分测试和异常捕获
- Endpoint 协处理器需要与客户端配合调用 `coprocessorService()`

**来源参考：**
- Apache HBase Coprocessor Documentation: https://hbase.apache.org/book.html#cp
- Hortonworks HBase Coprocessor Guide: https://docs.cloudera.com/

---

## 题目五：HBase 如何保证数据一致性和高可用？

### 题目
HBase 如何保证数据一致性？当 RegionServer 宕机时，HBase 如何保证数据不丢失并快速恢复？

### 答案

**数据一致性保证：**

**1. 写入一致性（Write Consistency）**
- HBase 默认提供「最终一致性」保证
- 数据写入分两步：先写 WAL，再写 MemStore
- WAL 保证了即使 MemStore 数据丢失（宕机），也能从 WAL 恢复
- 可以通过 `setAutoFlush(false)` 批量写入，通过 `flushCommits()` 手动刷写

**2. 强一致性读（Read Consistency）**
- HBase 通过 MVCC（Multi-Version Concurrency Control）实现读写隔离
- 每次 Region 分裂或 Master 选举后，会分配新的 MVCC 编号
- 读操作只能看到已提交（committed）的数据版本

**3. HDFS 底层保证**
- HBase 数据最终存储在 HDFS 上，HDFS 默认 3 副本机制保证数据高可靠
- HFile 写入后不再修改（Write-once, Read-many），保证数据安全

**RegionServer 宕机恢复机制：**

```
宕机检测 → ZooKeeper 通知 HMaster → 重新分配 Region → 数据恢复
```

**详细流程：**

1. **宕机检测**
   - RegionServer 与 Zookeeper 保持心跳连接（默认每 3 秒一次）
   - 如果 Zookeeper 检测到 RegionServer 宕机（9 秒无心跳），标记该节点为宕机状态
   - Zookeeper 通知 HMaster

2. **HMaster 接管**
   - HMaster 检测到 RegionServer 宕机，立即开始恢复流程
   - 将该 RegionServer 上所有的 Region 标记为待迁移状态

3. **WAL 重放（Log Splitting）**
   - 该 RegionServer 的 MemStore 数据还未刷写到 HFile，这部分数据必须恢复
   - HMaster 在另一个活着的 RegionServer 上重新创建对应 Region
   - 将宕机 RegionServer 的 WAL 日志按 Region 分裂，每个 Region 获得自己的 WAL 文件
   - 按序重放 WAL 中的操作，恢复 MemStore 数据

4. **Region 重新分配**
   - HMaster 将宕机的 Region 重新调度到其他 RegionServer
   - 新 RegionServer 加载对应数据后恢复服务

5. **恢复时间（RTO）**
   - 通常在秒级到分钟级，取决于：
     - WAL 文件大小（MemStore 数据量）
     - 集群规模和负载
     - HDFS 副本放置策略

**高可用架构：**
- HMaster 高可用：多个 HMaster 通过 Zookeeper 选举主节点，主节点宕机后自动切换
- Zookeeper 高可用：Zookeeper 本身也是分布式部署（至少 3 节点）
- 数据高可用：HDFS 副本机制保证数据不丢失

**CAP 定理视角：**
- HBase 选择了 **CP（一致性+分区容错性）**，牺牲了部分可用性
- 当网络分区发生时，HBase 优先保证一致性，可能导致部分 Region 不可用

**来源参考：**
- Apache HBase Book - Resilience: https://hbase.apache.org/book.html#wal
- 阿里巴巴 HBase 实战手册: https://www.alibabacloud.com/blog/hbase_

---

> 📌 本面试题库由 GitHub 仓库 [qq286158530/interview-questions](https://github.com/qq286158530/interview-questions) 统一管理
> 
> 最后更新：2026-07-25
