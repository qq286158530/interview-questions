# Redis 面试题精选 (2026-08-22)

> 本文精选5道高质量Redis面试题，涵盖数据类型、持久化、缓存、集群等核心知识点。

---

## 题目一：Redis 有哪几种数据类型？它们的底层实现分别是什么？

### 参考答案

Redis 支持五种基本数据类型，每种都有多种底层编码实现：

| 数据类型 | 底层编码 | 适用场景 |
|---------|---------|---------|
| **String** | `int`、`embstr`、`raw` | 缓存、计数器、分布式锁 |
| **List** | `ziplist`、`linkedlist`、`quicklist` | 消息队列、粉丝列表、最新动态 |
| **Hash** | `ziplist`、`hashtable` | 对象存储、购物车、商品属性 |
| **Set** | `intset`、`hashtable` | 标签、好友关系、去重 |
| **Zset** | `ziplist`、`skiplist` | 排行榜、延时队列、有序集合 |

### 底层实现详解

**String 类型**：
- `int`：保存64位有符号整数，用于整数值
- `embstr`：小于44字节的短字符串，embdedding形式存储在RedisObject中
- `raw`：大于44字节的长字符串，独立分配内存

**List 类型**：
- Redis 7.0 后统一使用 `quicklist`（压缩列表 + 双向链表的混合体）
- 兼顾查询效率和内存占用

**Hash 类型**：
- `ziplist`：field数量少(<512)且值都小于64字节时使用
- `hashtable`：数据量大时自动切换

**Zset 类型**：
- `skiplist`：结合跳表和字典，O(logN) 查找 + 范围操作

---

**来源参考**：
- Redis 官方文档：https://redis.io/docs/latest/develop/data-types/
- 《Redis设计与实现》- 黄健宏

---

## 题目二：Redis 的持久化机制有哪些？各有何优缺点？

### 参考答案

Redis 提供两种持久化机制：

### 1. RDB（Redis Database）

**原理**：在指定时间间隔内生成数据集的快照（point-in-time snapshot）。

```bash
# 配置示例
save 900 1      # 900秒内至少1个key变化
save 300 10     # 300秒内至少10个key变化
save 60 10000    # 60秒内至少10000个key变化
```

**优点**：
- 紧凑的二进制文件，适合备份和灾难恢复
- 恢复大数据集时速度比 AOF 快
- 主进程fork子进程完成，不阻塞客户端

**缺点**：
- 可能丢失最后一次快照后的数据
- fork时内存页表翻倍，可能造成请求卡顿

### 2. AOF（Append Only File）

**原理**：记录所有写操作命令，以日志形式追加保存。

```bash
# 配置示例
appendonly yes
appendfsync everysec  # 每秒同步，平衡性能与安全性
```

**优点**：
- 数据安全性更高，最多丢失1秒数据
- 日志格式清晰，便于灾难恢复

**缺点**：
- 文件体积通常比 RDB 大
- 持续写入时可能比 RDB 慢

### 3. 混合持久化（Redis 4.0+）

```bash
aof-use-rdb-preamble yes
```
同时使用 RDB + AOF，取长补短。

---

**来源参考**：
- Redis 官方文档：https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/

---

## 题目三：什么是 Redis 缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 参考答案

### 1. 缓存穿透

**问题**：查询一个不存在的数据，缓存和数据库都不存在，导致请求直达数据库。

**解决方案**：
- 布隆过滤器（Bloom Filter）拦截不存在的数据
- 缓存空值（set null）
- 增强参数校验

```python
# 布隆过滤器示例
if not bloom_filter.exists(key):
    return "请求不合法"
```

### 2. 缓存击穿

**问题**：某个热点 key 过期瞬间，大量并发请求直达数据库。

**解决方案**：
- 互斥锁（SETNX）+ 双检锁
- 热点数据永不过期
- 逻辑过期 + 后台异步更新

```python
# 双检锁示例
lock = redis.setnx("lock", "1", ex=10)
if lock:
    data = db.query()
    redis.setex("key", ttl, data)
    redis.delete("lock")
else:
    time.sleep(0.1)
    return redis.get("key")
```

### 3. 缓存雪崩

**问题**：大量缓存 key 同时过期，或 Redis 宕机，导致数据库压力骤增。

**解决方案**：
- 过期时间添加随机偏移量
- 高可用架构（Redis Cluster/Sentinel）
- 熔断/限流机制
- 多级缓存架构

---

**来源参考**：
- 美团技术博客：https://tech.meituan.com/2017/03/17/cache.html

---

## 题目四：Redis 主从复制原理是什么？有什么缺点？

### 参考答案

### 主从复制流程

```
1. 从服务器发送 PSYNC 命令
2. 主服务器执行 BGSAVE，生成 RDB 文件
3. 主服务器发送 RDB 文件给从服务器
4. 从服务器加载 RDB 数据
5. 主服务器发送缓冲区积压的命令
6. 从服务器执行增量命令同步
```

### 完整同步（Full Resync）

- 主服务器 fork 子进程生成 RDB 文件
- 发送 RDB 期间的新命令存入复制缓冲区
- RDB 发送完毕后发送缓冲区命令

### 部分同步（Partial Resync，Redis 2.8+）

- 依赖 ** Replication Backlog** 环形缓冲区
- 从服务器偏移量在缓冲区范围内则增量同步
- 否则执行完整同步

### 缺点

1. **主从切换时可能丢数据**：异步复制导致的数据丢失
2. **主从延迟**：从库同步延迟影响业务读取一致性
3. **木桶效应**：从库性能受限于最弱节点
4. **无法自动故障恢复**：需要借助 Sentinel 实现自动切换

```bash
# 推荐架构：Sentinel + 主从
redis-sentinel /path/to/sentinel.conf
```

---

**来源参考**：
- Redis 官方文档：https://redis.io/docs/latest/operate/oss_and_stack/replication/

---

## 题目五：Redis Cluster 是如何实现数据分片的？它如何保证高可用？

### 参考答案

### 数据分片机制

Redis Cluster 采用 **16384 个槽位（slot）** 进行数据分片：

```
CRC16(key) % 16384 = slot
```

**特点**：
- 每个节点负责一部分槽位（如 3 节点集群：节点A负责 0-5460，节点B负责 5461-10922，节点C负责 10923-16383）
- 客户端可连接任意节点，节点自动转发请求到正确节点
- 支持在线迁移槽位（`redis-trib reshard`）

### 高可用保证

1. **主从自动切换**：
   - 每个主节点可有多个从节点
   - 主节点宕机后，从节点自动提升为主
   - 半数以上主节点投票认为主节点不可达

2. **故障检测**：
   - 节点间定期发送 PING/PONG 心跳
   - 超过 `cluster-node-timeout` 未响应则判定为下线

3. **迁移（Failover）**：
   - 从节点发现自己主节点下线后，向集群所有节点广播
   - 集群半数以上主节点投票通过
   - 从节点晋升为新的主节点

### 架构图示

```
        ┌─────────┐
        │ Client  │
        └────┬────┘
             │
      ┌──────┴──────┐
      │             │
 ┌────▼───┐   ┌────▼───┐
 │Node A  │   │Node B  │
 │Master  │   │Master  │
 └───┬────┘   └───┬────┘
     │            │
 ┌───▼───┐   ┌───▼───┐
 │Node A'│   │Node B'│
 │Slave  │   │Slave  │
 └───────┘   └───────┘
```

---

**来源参考**：
- Redis 官方文档：https://redis.io/docs/latest/operate/oss_and_stack/cluster/

---

> 📌 更多面试题持续更新中，欢迎 Star ⭐：https://github.com/qq286158530/interview-questions
