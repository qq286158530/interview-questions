# Redis 面试题（2026-07-30）

> 以下题目由 AI 助手整理，涵盖 Redis 核心知识点，适合面试准备。

---

## 题目一：Redis 的过期键删除策略有哪几种？分别在什么场景下使用？

**参考答案：**

Redis 提供了三种过期键删除策略：

### 1. 定时删除（volatile-xxx + 定时器）
- **原理**：在设置键的过期时间时，同时创建一个定时器（Timer），让定时器在键过期时立即删除它。
- **优点**：对内存最友好，能保证过期键及时被删除，释放内存。
- **缺点**：对 CPU 不友好，在过期键较多时会占用较多 CPU 时间，影响 Redis 处理正常请求的性能。
- **使用场景**：适用于内存紧张、对数据一致性要求极高的场景，但实际生产中很少单独使用。

### 2. 惰性删除（lazy free）
- **原理**：放任过期键不管，只有在访问某个键时，才判断该键是否已过期，如果过期则删除。
- **优点**：对 CPU 非常友好，只在必须删除时才执行删除操作。
- **缺点**：对内存不友好，如果存在大量过期键但长期不被访问，它们会一直占用内存，导致内存泄漏。
- **使用场景**：适合对 CPU 敏感、对内存相对宽裕的场景，是 Redis 的默认策略。

### 3. 定期删除（volatile-random/volatile-ttl/volatile-lru）
- **原理**：每隔一段时间执行一次删除操作，扫描设置了过期时间的键，并删除其中的过期键。
- **优点**：是前两种策略的折中，通过限制删除操作执行的时长和频率，可以减少 CPU 和内存的影响。
- **缺点**：删除执行得太频繁会影响性能，执行得太少又会导致内存堆积。
- **配置**：`CONFIG SET hz 10` 控制每秒执行多少次定期删除（默认 10，即每秒 10 次）。

**实际生产环境**：通常将 `惰性删除 + 定期删除` 组合使用，通过 `maxmemory-policy` 配置具体策略（如 `volatile-lru`、`allkeys-lru` 等）。

**来源参考：**
- https://redis.io/topics/latency（Redis 延迟问题排查）
- https://www.cnblogs.com/javastack/p/13270204.html

---

## 题目二：Redis 主从复制原理是什么？如何处理复制风暴？

**参考答案：**

### 主从复制原理

Redis 主从复制分为全量同步和增量同步两种模式：

#### 全量同步（Sync）
1. **从服务器**向**主服务器**发送 `SYNC` 命令。
2. **主服务器**收到命令后，执行 `BGSAVE` 生成 RDB 文件，同时将此后收到的写命令缓冲区记录到 `replication buffer`。
3. **主服务器**将 RDB 文件发送给从服务器，从服务器加载 RDB 文件。
4. **主服务器**将步骤 2 中记录的缓冲区命令发送给从服务器执行。

#### 增量同步（PSync / Command Propagation）
Redis 2.8+ 引入 `PSYNC`，支持部分重同步：
- **主服务器**维护一个 `replication backlog` 环形缓冲区（默认 1MB）。
- 从服务器记录自己的 `master_repl_offset`（已复制位置）。
- 如果从服务器断开后重连，对比 `replication_run_id` 和偏移量：
  - 如果差距在 backlog 范围内，只发送增量部分。
  - 否则执行全量同步。

#### 复制风暴处理策略
复制风暴是指多个从服务器同时重连或扩容时，对主服务器造成大量资源消耗的现象：

1. **架构层面**：
   - 使用「树形复制」结构，让从服务器作为其他从服务器的主节点（如 A → B → C）。
   - 引入哨兵（Sentinel）或集群（Cluster）分散压力。

2. **运维层面**：
   - 避免多个从服务器同时重启或扩容。
   - 使用 `redis-cli --slave` 错峰执行 replication。
   - 配置 `repl-diskless-sync yes`（无盘复制）减少磁盘 IO。

3. **参数调优**：
   - 增大 `replication backlog` 大小。
   - 合理配置 `repl-backlog-ttl`。

**来源参考：**
- https://redis.io/topics/replication
- https://www.cnblogs.com/knowledge-base/p/redis-master-slave-replication.html

---

## 题目三：Redis 缓存穿透、缓存击穿、缓存雪崩有什么区别？如何解决？

**参考答案：**

### 1. 缓存穿透
- **定义**：查询一个根本不存在的数据，缓存和数据库都不命中，请求直接打到数据库。
- **危害**：恶意攻击者可以大量请求不存在的数据，最终拖垮数据库。
- **解决方案**：
  - **布隆过滤器（Bloom Filter）**：将所有存在的 key 存入布隆过滤器，请求先经过过滤器拦截。
  - **缓存空值**：如果数据库查询结果为空，也缓存一个空值（如 `"NULL"`），并设置较短的 TTL。
  - **参数校验**：对非法参数进行基本校验，直接拦截。

### 2. 缓存击穿
- **定义**：某个热点 key 过期或失效的瞬间，大量并发请求同时查询该 key，直接打到数据库。
- **危害**：数据库瞬时压力剧增，可能导致数据库宕机。
- **解决方案**：
  - **互斥锁（Mutex）**：使用 Redis 的 `SETNX` 实现互斥锁，只允许一个请求去查询数据库并回填缓存。
  - **逻辑过期**：不给 key 设置过期时间，而是存储一个"逻辑过期时间"，在代码层面判断是否过期，过期时使用互斥锁异步重建缓存。
  - **热点数据永不过期**：通过后台异步更新缓存，保证缓存可用性。

### 3. 缓存雪崩
- **定义**：大量缓存 key 在同一时间过期，或者 Redis 本身宕机，导致大量请求直接打到数据库。
- **危害**：数据库压力瞬间飙升，可能导致数据库崩溃。
- **解决方案**：
  - **过期时间随机化**：给缓存过期时间加上随机值（如 `baseTTL + rand(0, TTL)`），避免同时过期。
  - **多级缓存**：引入本地缓存（如 Caffeine、Guava Cache）作为 L1，多级缓存配合。
  - **Redis 高可用**：使用 Redis Sentinel 或 Redis Cluster 保证 Redis 本身的高可用。
  - **服务降级/限流**：在数据库压力大时，主动降级或限流，保护数据库。
  - **预热**：系统启动时主动预热热点数据到缓存。

| 问题 | 核心区别 | 关键词 |
|------|---------|--------|
| 缓存穿透 | 数据根本不存在 | 布隆过滤器、缓存空值 |
| 缓存击穿 | 热点 key 过期 | 互斥锁、逻辑过期 |
| 缓存雪崩 | 大量 key 同时过期 | 随机过期、多级缓存、高可用 |

**来源参考：**
- https://blog.csdn.net/zeb_perfect/article/details/136107374
- https://www.jianshu.com/p/625c0e263ebe

---

## 题目四：Redis 的 RDB 和 AOF 持久化机制有什么区别？如何选择？

**参考答案：**

### RDB（Redis Database Backup）

- **原理**：在指定的时间间隔内，Redis 执行 `BGSAVE` 命令，fork 一个子进程生成内存数据的快照（dump.rdb）文件。
- **配置参数**：
  ```conf
  save 900 1      # 900秒内至少1个key变化则触发BGSAVE
  save 300 10     # 300秒内至少10个key变化
  save 60 10000   # 60秒内至少10000个key变化
  ```
- **优点**：
  - 生成的备份文件紧凑，适合全量备份、数据迁移、冷备份。
  - 恢复大数据集时速度比 AOF 快。
  - fork 子进程执行，不影响父进程处理请求。
- **缺点**：
  - 容易丢失最近一次快照之后的数据（最多丢失一个快照周期的数据）。
  - fork 子进程时，如果数据量大会阻塞主进程（通常很短暂）。

### AOF（Append Only File）

- **原理**：Redis 将所有写操作追加到 AOF 文件的末尾，默认文件名 `appendonly.aof`。
- **配置参数**：
  ```conf
  appendonly yes
  appendfsync everysec   # 每秒同步一次（默认，推荐）
  # appendfsync always  # 每次写操作都同步（性能差，数据最安全）
  # appendfsync no       # 由操作系统决定何时同步（性能最好，数据最不安全）
  ```
- **优点**：
  - 数据安全性更高（特别是 `everysec` 或 `always` 模式）。
  - AOF 文件是追加写入的日志，即使故障也只会丢失很少的数据。
  - 支持rewrite压缩（`BGREWRITEAOF`），可以精简 AOF 文件。
- **缺点**：
  - AOF 文件通常比 RDB 大，恢复速度慢。
  - 高并发场景下，`everysec` 模式可能丢失 1 秒数据。

### 混合持久化（Redis 4.0+）
- **原理**：AOF rewrite 时，先以 RDB 格式写入全量数据，再以 AOF 格式追加增量修改。
- **配置**：`aof-use-rdb-preamble yes`
- **优点**：兼顾 RDB 的快速恢复和 AOF 的数据完整性，是目前生产环境的推荐方式。

### 如何选择？

| 场景 | 推荐策略 |
|------|---------|
| 对数据完整性要求极高（如金融） | AOF + always + 混合持久化 |
| 一般业务系统 | RDB + AOF（everysec）混合持久化 |
| 数据量大的缓存系统 | RDB 单独使用，或纯缓存不持久化 |
| 需要快速灾备恢复 | RDB 定期备份 |

**来源参考：**
- https://redis.io/topics/persistence
- https://www.runoob.com/redis/redis-persistence.html

---

## 题目五：Redis 集群模式有哪些？Redis Cluster 的槽迁移过程是怎样的？

**参考答案：**

### Redis 集群模式

#### 1. 主从复制模式（Master-Slave）
- 一主多从结构，从节点只读，主节点负责写。
- 通过 Sentinel（哨兵）实现自动故障转移。
- **缺点**：没有内置分片，数据都在主节点，无法水平扩展写入能力。

#### 2. Sentinel（哨兵）模式
- 监控主从节点的运行状态。
- 自动完成故障检测和切换（failover）。
- 提供统一的访问入口。
- 适用于数据可靠性要求较高的场景，但单节点写入瓶颈仍在。

#### 3. Cluster 模式（Redis 3.0+）
- 数据自动分片（16384 个槽），分布在多个节点上。
- 每个节点主从复制，保证高可用。
- 客户端直接连接任意节点，通过 MOVED 重定向访问正确的槽。
- 支持在线槽迁移（resharding）。

#### 4. Twemproxy（又叫 Nutcracker）
- Twitter 开源的 Redis 代理中间件。
- 支持分片和后端节点池化，但不支持动态扩容。
- 目前已被 Redis Cluster 替代。

#### 5. Codis
- 豌豆荚开源的 Redis 集群方案。
- 支持动态扩容、数据分片、自动迁移。
- 通过 ZooKeeper 管理集群状态。
- 适合需要平滑扩容的老项目。

### Redis Cluster 槽迁移过程

Redis Cluster 的槽迁移是实现动态扩容的核心机制，步骤如下：

1. **目标节点准备接收**：
   - 管理员通过 `CLUSTER SETSLOT <slot> IMPORTING <source_node_id>` 通知目标节点准备从源节点迁入槽。

2. **源节点标记迁出**：
   - 管理员通过 `CLUSTER SETSLOT <slot> MIGRATING <target_node_id>` 通知源节点该槽将迁出。

3. **数据迁移**：
   - 客户端请求源节点获取该槽的数据。
   - 源节点对请求的 key 执行 `MIGRATE` 命令，将数据迁移到目标节点。
   - 迁移过程是先迁移 key 对应的值，再删除源节点上的 key。
   - 如果迁移过程中有新的写入，源节点会缓存这些 key（keylist）。

4. **更新集群拓扑**：
   - 所有节点执行 `CLUSTER SETSLOT <slot> NODE <target_node_id>` 更新槽分配信息。
   - 该信息通过 Gossip 协议在集群内传播。

5. **客户端重定向**：
   - 迁移完成后，客户端访问该槽的 key 时，源节点返回 `MOVED <slot> <target_ip:port>`。
   - 客户端收到 MOVED 后，更新本地槽缓存，并向正确的节点重发请求。

**注意事项**：
- 迁移过程中，被迁移的 key 无法同时存在于两个节点，避免数据不一致。
- 迁移期间，建议客户端开启 `ASK` 重定向处理（针对迁移中短暂的 key 不存在情况）。
- 迁移过程对业务有一定影响，应在低峰期或使用 `CLUSTER SETSLOT MIGRATING` + `ASKING` 命令组合尽量减少影响。

**来源参考：**
- https://redis.io/topics/cluster-tutorial
- https://www.cnblogs.com/guardwhy/p/13766226.html
