# Spark 大数据工程师高频面试题

> 适用方向：社招/校招大数据工程师 | 整理日期：2026-07-30  
> 核心考察：Spark 架构原理、RDD、DataFrame、Shuffle、内存管理、广播变量

---

## 面试题 1：Spark DAG 与任务调度机制

### 题目

请描述 Spark DAG（有向无环图）的构建过程，以及 Shuffle MapStage 和 ResultStage 是如何被切分的？为什么窄依赖可以pipeline执行，而宽依赖会引入Shuffle？

### 答案

**DAG 构建流程：**

1. **RDD 依赖链**：用户代码中的 Transform 算子（map/filter/groupBy...）形成 RDD 之间的依赖关系链。
2. **DAG 划分**：DAGScheduler 从 DAG 的**终点（Action）**回溯，遇到 Shuffle 宽依赖（shuffle依赖）则划分为一个新的 Stage。窄依赖（map/filter 等）则归属同一 Stage。
3. **Stage 类型**：
   - **ShuffleMapStage**：产生 Shuffle 数据的中间Stage，输出为 Shuffle 文件，供下游 Stage 读取。
   - **ResultStage**：最终执行 Action 的 Stage。
4. **任务切分**：每个 Stage 被切分为多个 **Task**（并行度 = 分区数），ShuffleMapStage 的 Task 称为 ShuffleMapTask，ResultStage 的 Task 称为 ResultTask。
5. **任务提交**：TaskSet 被提交给 TaskScheduler，TaskScheduler 将 Task 分发到 Executor 上执行。

**窄依赖 vs 宽依赖与 Pipeline：**

- **窄依赖**（OneToOneDependency / RangeDependency）：父 RDD 的每个分区最多被子 RDD 的一个分区使用。可以在同一节点内**连续执行**（Pipeline），无需等待所有父分区完成，像流水线一样边读边算边写。
- **宽依赖**（ShuffleDependency）：父 RDD 的一个分区会被子 RDD 的多个分区使用（groupByKey、reduceByKey等），**必须等所有父分区全部完成后**，将数据按 Key 进行分区重分布，才能执行子 RDD 的计算——这就是 Shuffle，代价高（磁盘 I/O、网络 I/O、序列化）。

> **来源**：[Apache Spark 官方文档 – Spark作业调度](https://spark.apache.org/docs/latest/job-scheduling.html)

---

## 面试题 2：RDD、DataFrame、DataSet 的区别与适用场景

### 题目

RDD、DataFrame、DataSet 是 Spark 的三大数据结构，它们之间有什么区别？什么时候该用哪个？在性能上有哪些差异？

### 答案

| 维度 | RDD | DataFrame | DataSet |
|------|-----|-----------|---------|
| **数据结构** | 分布式的 Java/Scala 对象集合，无schema | 分布式的**二维表格**（类似 Pandas DataFrame），有列名和类型 | 强类型的** Typed JVM 对象** + DataFrame的schema |
| **Schema** | 无，需手动解析 | 自动推断或显式定义 | 显式定义 case class 或 Encoder |
| **优化引擎** | 无优化，每行单独处理 | **Catalyst 优化器 + Tungsten**，支持列式存储、谓词下推、常量替换等 | 同 DataFrame（DataSet = DataFrame + 类型安全） |
| **编译时检查** | 无（运行时才发现错误） | 只做**运行时**类型检查 | **编译时**类型安全（Scala/Java） |
| **数据处理方式** | 低级 API，灵活性高 | 高级 SQL-like API | 兼具灵活性和类型安全 |
| **序列化** | Java/Kryo 序列化 | Tungsten 二进制列式存储 | Tungsten + Encoder |

**性能差异**：
- DataFrame/DataSet 通过 **Catalyst 优化器**生成优化后的 Logical/Physical Plan，减少不必要的 Shuffle 和数据扫描。
- DataFrame 使用**堆外内存（Off-Heap）**和列式存储，内存效率更高。
- RDD 适合做底层控制（如图计算、机器学习自定义算法），但大多数 ETL/SQL 场景用 DataFrame 更优。

**选择建议**：
- **DataFrame**：ETL、SQL、报表分析（结构化/半结构化数据）。
- **DataSet**：需要类型安全且兼顾性能的场景（Java/Scala 业务逻辑）。
- **RDD**：需要细粒度控制、数据格式不规则、或者在对性能要求极高且已有高度定制优化时。

> **来源**：[Apache Spark 官方文档 – DataFrame、DataSet和RDD](https://spark.apache.org/docs/latest/rdd-programming-guide.html)

---

## 面试题 3：Spark Shuffle 的原理与优化策略

### 题目

什么是 Shuffle？Spark 1.x 和 Spark 2.x 的 Shuffle 机制有什么变化？生产环境中如何减少 Shuffle 的性能开销？

### 答案

**Shuffle 定义**：将数据按 Key 重新分区、聚合的过程，常见于 groupBy、join、reduceByKey 等算子。需要跨节点传输数据。

**Spark 1.x（Hash Shuffle）：**
- 每个 Mapper 为每个 Reducer 单独写一个文件（`m * r` 个文件），文件数爆炸。
- 开启 File Consolidation 后，同一 Executor 内的多个 Mapper 会合并文件，但仍数量庞大。
- **问题**：小文件过多 → 文件系统压力大、GC 严重、打开文件句柄耗尽。

**Spark 2.x（Sort-Based Shuffle）：**
- 每个 Mapper 将数据写入**一个文件**（按分区排序），并维护一个索引文件。
- Shuffle 文件数降为 `2 * m`（数据文件 + 索引文件）。
- 当 Shuffle Partition ≤ 200 时使用 **BypassMergeSortShuffleHandle**（类似 Hash Shuffle 但有序）。
- **Tungsten Sort Shuffle**（钨丝计划）：使用堆外内存直接操作二进制数据，避免 JVM 对象开销。

**生产优化策略：**

1. **减少 Shuffle 数据量**：
   - 使用 `map-side pre-aggregation`（如 `reduceByKey` 而非 `groupByKey`）。
   - 在 Shuffle 前做 filter、map 过滤无效数据。
2. **合理设置分区数**：
   - 分区数过少 → 并行度低、OOM；过多 → 小任务开销大。通常 `core * 2~4` 为起始值。
   - `spark.sql.shuffle.partitions` 默认 200，可根据数据量调整为 `分区数 = 数据量 / 单分区目标大小（128~256MB）`。
3. **使用广播变量**：
   - 小表广播（`broadcast join`）可避免 Shuffle，直接在每个节点上做 join。
4. **开启 Shuffle 压缩**：`spark.shuffle.compress = true`（默认开启）。
5. **使用高性能 Shuffle 服务**：`spark.shuffle.service.enabled = true`（External Shuffle Service），避免 Executor 崩溃导致 Shuffle 数据丢失。

> **来源**：[Apache Spark 官方文档 – Shuffle Behavior](https://spark.apache.org/docs/latest/configuration.html#shuffle-behavior)

---

## 面试题 4：Spark 内存管理机制（堆内 vs 堆外，统一内存管理）

### 题目

Spark 的内存分为哪几块？堆内内存（On-Heap）和堆外内存（Off-Heap）有什么区别？Executor 发生 OOM 的常见原因有哪些，如何排查？

### 答案

**内存区域划分（Spark 2.x+ 统一内存管理）：**

```
┌──────────────────────────────────────────────────────┐
│                   Executor Memory                    │
│  ┌────────────────┐    ┌────────────────────────┐   │
│  │   Execution    │    │       Storage         │   │
│  │  运行时内存     │ ↔️  │  广播缓存/RDD缓存/数据│   │
│  │ (Shuffle/排序等)│    │   (默认占比 0.5)       │   │
│  └────────────────┘    └────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │             User Memory（40%）                │   │
│  │   用户数据结构、元数据、UDF等                  │   │
│  └──────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────┘
```

- **Execution Memory**：执行时内存，用于 Shuffle Sort、Hash Join、HashMap 等算子内部数据结构。
- **Storage Memory**：存储内存，用于 RDD Cache、Broadcast、Python 进程等。
- **User Memory**：用户代码的自由区域，Spark 不做管理。
- **Reserved Memory**：约 300MB 固定保留，系统用。

**堆内 vs 堆外：**
- **On-Heap**：JVM 堆上的内存，受 GC 影响；通过 `spark.executor.memory` 设置。
- **Off-Heap**：堆外内存，不受 GC 管理，通过 `spark.memory.offHeap.enabled` 开启，`spark.memory.offHeap.size` 设置大小；Tungsten 使用 Off-Heap 直接操作二进制数据，性能更高更稳定。

**OOM 常见原因与排查：**
1. **数据倾斜（Data Skew）**：某个分区数据量远超其他，导致该 Executor OOM。
   - 排查：`Spark UI` 查看 Stage 的 Task 耗时分布，若某 Task 耗时明显偏高即为倾斜。
   - 解决：加盐（salting）、自定义 Partitioner、增大倾斜分区并行度。
2. **Shuffle 分区数过少**：单分区数据量过大。
   - 解决：`spark.sql.shuffle.partitions` 或 `repartition(n)` 调大分区。
3. **大量 UDF 或匿名函数**：创建过多临时对象加剧 GC。
   - 排查：GC 日志（`GC_LOG_DIR`）或 Spark UI Memory页面。
   - 解决：减少 UDF 调用，改用内置函数或 DataFrame API。
4. **RDD 缓存不当**：缓存了过大的 RDD 且 Storage Memory 不足。
   - 解决：合理使用 `persist(LEVEL)` 选择缓存级别，或 `unpersist()`。

> **来源**：[Apache Spark 官方文档 – Memory Management](https://spark.apache.org/docs/latest/configuration.html#memory-management)

---

## 面试题 5：广播变量（Broadcast Variable）的原理与使用场景

### 题目

什么是广播变量？它是如何实现减少网络开销的？在实际使用中需要注意哪些问题？为什么有时候广播变量反而会导致 OOM？

### 答案

**广播变量的原理：**

广播变量将**只读数据**高效地分发到每个 Executor（节点），而不是为每个 Task 单独传输一份数据。

**实现流程：**
1. Driver 端将数据**序列化成二进制**（默认使用 `TorbernRepository`，即堆外内存）。
2. 每个 Executor 启动时向 **BlockManager** 请求广播数据，Driver 将广播块信息注册到 BlockManager。
3. 数据以**分块（chunk）**形式存储在 Executor 的内存或磁盘中，所有共享同一 Executor 的 Task 从本地 BlockManager 读取，无需跨网络传输。
4. **关键**：广播变量只会被发送到每个节点**一次**，而非每个 Task 一次。

**使用示例（Scala）：**
```scala
// 广播小表
val smallTable = spark.sparkContext.broadcast(broadcastData)

// 在算子内通过 value 访问（只读）
val result = largeTable.rdd.filter(row => {
  val small = smallTable.value
  small.contains(row.getString(0))
})
```

**使用场景：**
- `map-side join`：将小表广播，大表做 filter/map，无 Shuffle。
- 维度表（如城市编码、商品分类）广播。
- 机器学习特征工程中的字典/映射表广播。

**注意事项与 OOM 风险：**

1. **广播的数据不能过大**：
   - 广播数据会完整加载到每个 Executor 的内存中（`spark.executor.memory` 的 Storage 区域）。
   - 单个 Executor 广播数据过大 → OOM。
   - **经验公式**：`广播数据 < (Executor Memory * 0.5 * 0.2) = ~10% Executor Memory`，超过此阈值建议用 Shuffle Join。

2. **广播版本一致性**：
   - 广播变量是只读的，但底层数据改变（如广播后更新 DataFrame）不会同步，需重新广播。

3. **序列化选择**：
   - 默认使用 Java `Serializer`（可切换 Kryo），若广播对象自定义了 `java.io.Serializable`，Kryo 通常效率更高。
   - 配置：`spark.serializer = org.apache.spark.serializer.KryoSerializer`

4. **Task 内访问方式**：
   - 必须在 Task 内通过 `broadcast.value` 访问，不要在 Task 外持有引用，否则无法序列化。

5. **广播阈值**：
   - `spark.sql.autoBroadcastJoinThreshold`（默认 10MB），超过此大小的表不会自动广播，需手动 `broadcast(df)`。

> **来源**：[Apache Spark 官方文档 – Broadcast Variable](https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables)

---

## 📌 复习优先级建议

```
P0（必会）: Shuffle原理 → DAG调度 → 内存管理
P1（高频）: DataFrame vs RDD → 广播变量优化
P2（加分）: SparkSQL Catalyst优化器 → Tungsten引擎
```

> 💡 **面试提醒**：Spark 面试常结合真实生产问题（如数据倾斜调优、OOM排查、任务慢查询分析），建议结合 Spark UI 和日志深入理解调优思路。
