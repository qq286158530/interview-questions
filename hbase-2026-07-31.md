# HBase 面试题精选（2026-07-31）

> 🎯 本题集涵盖 HBase 架构、RowKey 设计、存储结构、MemStore/HFile、协处理器等核心知识点，题目来自互联网经典面试资源整理。

---

## 题目一：HBase 的整体架构是怎样的？请描述 Master 和 RegionServer 的职责。

### 参考答案

HBase 采用 **Master-Slave 架构**，核心组件如下：

| 组件 | 职责 |
|------|------|
| **ZooKeeper** | 负责协调和服务注册，保存 `META` 表位置、Master 地址等元数据 |
| **Master** | 负责表结构定义、Region 分配、负载均衡、Schema 变更。不参与实际数据读写 |
| **RegionServer** | 负责数据读写、Flush/Compact、MemStore 管理，是数据服务的实际节点 |
| **Region** | 表的子集，一个 Region 对应一个 RowKey 范围，存储在某个 RegionServer 上 |

**读写流程简述：**

1. 客户端请求 ZooKeeper 获取 `META` 表位置
2. 客户端访问 `META` 表，获取目标 RowKey 对应的 RegionServer 地址
3. 客户端直连目标 RegionServer 进行读写（无中间代理）
4. 写入时数据先写入 MemStore，达到阈值后 Flush 成 HFile 存储

**特点：** HBase 支持高并发写入、水平扩展，适合海量随机写入场景（如监控、推荐系统）。

> 📎 来源：https://www.cnblogs.com/smartloli/p/16933034.html

---

## 题目二：HBase 中 RowKey 如何设计？有哪几种常见的 RowKey 设计策略？

### 参考答案

RowKey 是 HBase 数据的唯一主键，设计优劣直接影响读写性能和集群负载均衡。

### 常见设计策略

**1. 盐值（Salting）**
在 RowKey 头部添加随机前缀，将数据打散到不同 Region，避免热点。
```
RowKey = (随机字母 + 原始主键)
例如：a12345, b67890, a34567
```
**优点**：打散写压力；**缺点**：无法范围查询。

**2. 哈希（Hash）**
对主键做 MD5/SHA 取模，生成固定前缀。
```
RowKey = MD5(原始主键).substring(0,4) + 原始主键
```
**优点**：均匀分布；**缺点**：打乱了时间顺序。

**3. 字符串反转**
反转手机号、订单号等有规律的数字型 RowKey，避免尾部热点。
```
原始：13812345678 → 反转：87654321831
```

**4. 时间戳反转 + 业务前缀**
常用于消息、日志类场景，保证新数据在前。
```
RowKey = (业务前缀)_(Long.MAX_VALUE - timestamp)
```

### 设计原则
- **长度原则**：不超过 100 字节，推荐 16 字节以内
- **唯一原则**：全局唯一
- **散列原则**：避免连续 RowKey 导致热点
- **可读原则**：在满足散列前提下保持一定可读性

> 📎 来源：https://blog.csdn.net/qq_37518039/article/details/124847912

---

## 题目三：HBase 的存储结构是怎样的？请描述 HFile 和 MemStore 的作用。

### 参考答案

### 存储层次

```
Table
└── Region (按 RowKey 范围切分)
    └── Store (每个列族一个 Store)
        ├── MemStore (写缓存，JVM 内存)
        └── HFile (磁盘文件，SSTable 格式)
```

### MemStore

- 写请求先将数据写入 **MemStore**（按 RowKey/TS 排序的内存结构）
- 当 MemStore 达到阈值（默认 `128MB`），触发 **Flush** 生成新的 HFile
- 优点：写性能高，避免随机磁盘 I/O

### HFile（SSTable）

- HBase 在磁盘上的存储格式，类比 HDFS 的 SequenceFile
- **结构**（从新到旧依次增长）：
  - `Data Blocks`：存储 KeyValue 数据对（可配置大小，默认 64KB）
  - `Meta Blocks`：存储布隆过滤器信息
  - `File Info`：文件元信息
  - `Trailer`：读取起点偏移量
- HFile 内部数据按 RowKey 有序排列，支持二分查找

### Minor/Major Compact

- **Minor Compact**：挑选部分小 HFile 合并成大 HFile（删除过期版本）
- **Major Compact**：合并某个 Store 下所有 HFile，生成一个完整 HFile（删除所有过期数据）

> 📎 来源：https://hbase.apache.org/book.html#arch.store

---

## 题目四：HBase 的 WAL（Write-Ahead Log）机制是什么？它如何保证数据不丢失？

### 参考答案

### WAL 机制

HBase 在数据写入 MemStore **之前**，先将操作记录以追加写的方式写入 **WAL（预写日志）**。

```
客户端写请求
    ↓
① 写入 WAL（HLog）  ← 先保证日志落盘
    ↓
② 写入 MemStore     ← 成功后返回客户端
```

### 为什么需要 WAL？

当 RegionServer 崩溃时，MemStore 中未 Flush 的数据会丢失。此时通过 WAL 重放（Replay），恢复未持久化的写入。

### WAL 持久化等级

| 级别 | 说明 |
|------|------|
| `USE_SKIP_WAL` | 不写 WAL，风险最高 |
| `ASYNC_WAL` | 异步批量写磁盘 |
| `SYNC_WAL` | 同步写磁盘（默认） |
| `FSYNC_WAL` | 每次写入都强制刷盘，最安全 |

### 恢复流程

1. RegionServer 崩溃后，ZooKeeper 检测到其临时节点消失
2. Master 重新分配其上的 Region 给其他节点
3. 新节点启动后读取 WAL，按序列重放其中的操作
4. 完成数据恢复

> 📎 来源：https://www.cnblogs.com/hbase-community/p/16063324.html

---

## 题目五：HBase 协处理器（Coprocessor）是什么？有哪两类协处理器，分别用在什么场景？

### 参考答案

### 协处理器简介

协处理器是 HBase 提供的扩展机制，允许用户将**业务代码部署到 RegionServer 上**，在数据写入/读取时**就近执行**，无需多次网络往返。

### 两类协处理器

**1. Observer（观察者）—— 类似于触发器**

在特定事件前后自动触发，类似数据库触发器。

| 类型 | 触发时机 |
|------|----------|
| `PrePutObserver` | Put 操作写入 MemStore **之前** |
| `PostPutObserver` | Put 操作写入 MemStore **之后** |
| `PreGetObserver` | Get 查询执行**之前** |
| `PostGetObserver` | Get 查询执行**之后** |
| `PreDeleteObserver` | Delete 操作执行之前 |
| `PostDeleteObserver` | Delete 操作执行之后 |

**典型应用场景：**
- 写入时自动构建二级索引
- 删除时级联清理关联数据
- 权限校验（阻止不合法的读写）
- 审计日志（记录每次访问）

**2. Endpoint（端点）—— 类似于存储过程**

允许在 RegionServer 上执行**自定义计算逻辑**，客户端调用后自动在每个相关 Region 并行执行，并将结果聚合返回。

**典型应用场景：**
- 聚合查询（求和、计数、最大值）
- 复杂计算（自定义 UDF）
- 跨列族/跨列的计算

### 使用示例（伪代码）

```java
// 定义一个 Observer，在 Put 后触发
public class MyIndexObserver extends BaseRegionObserver {
    @Override
    public void postPut(ObserverContext<RegionCoprocessorEnvironment> e,
                        Put put, WALKey key, WALEdit edit) {
        // 写入二级索引表
        byte[] row = put.getRow();
        byte[] cf = "index_cf".getBytes();
        Put indexPut = new Put(row);
        indexPut.addColumn(cf, "idx".getBytes(), row);
        table.put(indexPut);
    }
}
```

> 📎 来源：https://blogs.apache.com/hbase/attachment/blogs.apache.com/hbase/2019/08/13/hbase-coprocessor-intro/

---

## 📚 更多学习资源

- 官方文档：https://hbase.apache.org/book.html
- HBase 权威指南：https://www.jd.com/chanpin/14468.html
- 面试题库：https://www.cnblogs.com/smartloli/p/16933034.html

---

*本文件由 AI 自动整理 • 每日定时推送 • 欢迎 Star https://github.com/qq286158530/interview-questions*
