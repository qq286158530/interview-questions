# Spark 面试题（2026-08-12）

> 本文档由 AI 自动整理，来源为业界经典面试题库与知识点汇总。

---

## 题目一：Spark 架构与核心组件

**问题：** 请描述 Apache Spark 的整体架构，Driver、Executor、Master、Worker 的作用是什么？Task 是如何被调度执行的？

**答案：**

Spark 采用 **主从架构**，核心组件如下：

| 组件 | 作用 |
|------|------|
| **Driver（驱动器）** | 运行 `main()` 方法，创建 `SparkContext`，负责将用户代码转换为 DAG，将 DAG 划分为 Stage，为每个 Stage 创建 TaskSet，分发 Task 到 Executor |
| **Executor（执行器）** | 运行在 Worker 节点上的进程，负责执行分配给自己的 Task，将结果返回给 Driver；同时提供内存存储（RDD Cache） |
| **Master** | 集群管理器，负责接收注册申请、分配资源、监控集群状态（Standalone 模式下） |
| **Worker** | 运行在集群节点上的守护进程，负责向 Master 注册自己的资源（CPU、内存），启动 Executor 或 Driver |

**Task 执行流程：**

1. 用户代码 → `SparkContext` → 构建 DAG（有向无环图）
2. DAG Scheduler 将 DAG 划分为 Stage（以 Shuffle 为边界，倒序构建）
3. Task Scheduler 将 Stage 内的 Task 发送到 Executor 执行
4. Executor 以 **多线程** 方式并行执行 Task，每个 Task 处理一个数据分片
5. 结果汇总返回 Driver

**来源：** [Spark 官方文档 - Architecture](https://spark.apache.org/docs/latest/cluster-overview.html)

---

## 题目二：RDD 及其特性

**问题：** 什么是 RDD？RDD 有哪些核心特性？为什么 Spark 选择 RDD 作为核心抽象？

**答案：**

**RDD（Resilient Distributed Dataset）** 是 Spark 最底层的不可变、分区化、可并行计算的数据集合。

**RDD 五大核心特性：**

| 特性 | 说明 |
|------|------|
| **分区列表（Partitions）** | 数据集被划分为多个分区，每个分区是并行计算的单位 |
| **计算函数（Compute Function）** | 每个分区上执行的计算逻辑（接收 Partition，返回 Iterator） |
| **依赖关系（Dependencies）** | RDD 之间的依赖关系（窄依赖 vs 宽依赖），决定 DAG 划分 Stage 的方式 |
| **分区器（Partitioner）** | 可选，决定 KV 类型 RDD 的分区策略（如 HashPartitioner、RangePartitioner） |
| **数据位置偏好（Preferred Locations）** | 可选，计算任务优先调度到数据所在节点，实现数据本地性 |

**窄依赖 vs 宽依赖：**

- **窄依赖（Narrow Dependency）：** 一个父分区最多被子集一个子分区使用（如 `map`、`filter`）。可流水线执行，一个分区失败只需重算对应分区。
- **宽依赖（Wide Dependency）：** 一个父分区可能被多个子分区使用（如 `groupByKey`、`join`）。需要 Shuffle，必须等父分区全部完成才能计算子分区。

**为什么选择 RDD：**
1. **高效容错**：通过血统（Lineage）机制，失败时只需重算丢失的分区，而非全量复制
2. **数据本地性**：调度优先考虑数据所在节点，减少网络传输
3. **简单直观**：不可变语义天然适合分布式并行计算

**来源：** [RDD Paper - Resilient Distributed Datasets: A Fault-Tolerant Abstraction for In-Memory Cluster Computing](https://www.usenix.org/system/files/conference/nsdi12/nsdi12-final138.pdf)

---

## 题目三：DataFrame 与 DataSet 的区别

**问题：** Spark 中 DataFrame 和 DataSet 有什么区别？它们与 RDD 相比有什么优势？

**答案：**

**DataFrame：**

- 类似于关系型数据库的表结构，是**结构化**的数据集合
- 每一列有**名称和类型**（Schema 元信息已知）
- 底层基于 `Row` 对象存储，支持 SQL 查询、列式存储优化
- API 风格：`dataframe.select($"name")`

**DataSet：**

- 是 DataFrame 的**强类型版本**，结合了 RDD（类型安全）和 DataFrame（优化执行）的优点
- 编译时检查类型，比 DataFrame 更安全
- Encoder 机制实现序列化与反序列化
- API 风格：`dataset.filter(_.age > 18).map(_.name)`

**DataFrame vs DataSet 对比：**

| 维度 | DataFrame | DataSet |
|------|-----------|---------|
| 类型安全 | 运行时检查 | 编译时检查 |
| Schema | 已知（列名+类型） | 已知 |
| 序列化 | Row 对象（无类型） | 类型对象（强类型 Encoder） |
| 操作方式 | DSL / SQL | Lambda 表达式 |
| 性能 | Tungsten + 列式存储优化 | 同 DataFrame（执行计划相同） |

**与 RDD 相比的优势：**

1. **Catalyst 优化器**：DataFrame/DataSet 的每次操作都会经过 Catalyst 优化器生成最优执行计划（如谓词下推、列剪裁、常量折叠），RDD 无此类优化
2. ** Tungsten 序列化**：使用堆外内存和高效二进制序列化（类似 Kryo），减少内存占用和 GC 开销
3. **数据本地性与列式存储**：减少网络传输，支持向量化计算
4. **统一的 API**：一套 API 同时支持 Scala/Java/Python/SQL

> **注意：** 在 Spark 2.x 以后，`DataFrame = DataSet[Row]`，二者已统一，DataSet 主要用于强类型 Java/Scala 场景。

**来源：** [Spark 官方文档 - DataFrame Guide](https://spark.apache.org/docs/latest/sql-programming-guide.html)

---

## 题目四：Shuffle 机制与优化

**问题：** 什么是 Shuffle？为什么 Shuffle 是 Spark 性能瓶颈？有哪些优化策略？

**答案：**

**什么是 Shuffle：**

Shuffle 是 Spark 中**重新分区数据**的操作，将数据按照某种规则重新组合。当一个 RDD/DF 的某个分区需要依赖父 RDD/DF 的多个分区数据时，必须经过 Shuffle。

**触发 Shuffle 的算子：**

- **重分区类**：`repartition`、`coalesce`、`partitionBy`
- **聚合类**：`groupByKey`、`reduceByKey`、`aggregateByKey`、`combineByKey`
- **关联类**：`join`、`cogroup`
- **排序类**：`sortByKey`、`orderBy`

**Shuffle 执行过程（以 MapReduce 类比）：**

1. **Shuffle Write（Map 端）：** 将数据写入本地磁盘文件，按 Key 分区，每个分区一个文件
2. **Shuffle Read（Reduce 端）：** 每个 Task 从所有 Map 节点拉取属于自己的分区数据，进行聚合/join 等操作

**为什么是性能瓶颈：**

- **磁盘 I/O**：大量数据必须落盘，无法纯内存操作
- **网络 I/O**：跨节点数据传输，带宽消耗大
- **序列化开销**：数据需要序列化/反序列化
- **内存压力**：Reduce 端聚合时需要缓存数据，容易 OOM
- **磁盘随机读写**：大量小文件合并读写

**优化策略：**

| 策略 | 说明 |
|------|------|
| **增加分区数** | 适当增加 shuffle 并行度（如 `repartition(n)`），减少单分区数据量 |
| **使用 Map-side 聚合** | 用 `reduceByKey`/`aggregateByKey` 替代 `groupByKey`，在 Map 端先聚合再 shuffle |
| **避免不必要 Shuffle** | 避免先 `repartition` 再 `join`，同一数据源的多次 shuffle 可合并 |
| **广播变量（Broadcast Join）** | 小表广播到所有节点，转换为 Map Join，避免 Shuffle |
| **调整 Shuffle 文件合并** | `spark.sql.shuffle.partitions`（默认200）适当调大/调小 |
| **使用 Kryo 或 Tungsten 序列化** | 减少序列化数据量 |
| **开启 Shuffle 压缩** | `spark.shuffle.compress=true`，使用 LZ4/Zstd 压缩 |

**来源：** [Spark 官方文档 - Shuffle Behavior](https://spark.apache.org/docs/latest/configuration.html#shuffle-behavior)

---

## 题目五：广播变量与内存管理

**问题：** 什么是广播变量？Spark 的内存管理模型是怎样的？堆内内存与堆外内存有什么区别？

**答案：**

### 广播变量（Broadcast Variable）

**概念：** 广播变量是将**只读数据**高效分发到集群所有节点的机制，避免每个 Task 都复制一份数据，节省大量网络传输和内存。

**使用方式：**
```scala
val broadcastVar = sc.broadcast(Array(1, 2, 3))
broadcastVar.value  // 在 Executor 端访问
```

**原理：**
1. Driver 端将数据序列化并缓存
2. 每个 Executor 启动时从 Driver 或 HDFS 拉取数据，存储为共享变量
3. 所有 Task 共享该 Executor 上的广播数据，无需重复传输

**使用场景：** 大表 join 小表（Map Join）、大维度表关联、配置参数分发

---

### Spark 内存管理模型（统一内存管理 Unified Memory Management）

Spark 1.6 之后引入统一内存管理，将内存分为**存储内存**和**执行内存**两大区域，二者可以动态共享。

**内存区域划分：**

| 区域 | 用途 | 配置参数 |
|------|------|---------|
| **系统预留（Reserved Memory）** | 300MB，保留给系统和其他用途 | `spark.memory.reserved` |
| **用户内存（User Memory）** | 用户代码使用的内存（UDF、Spark 内部元数据） | 约 25% 的剩余内存 |
| **Spark 内存（Spark Memory）** | 执行与存储共享的内存池，默认占 60% | `spark.memory.fraction=0.6` |
| └ 存储内存（Storage Memory） | RDD Cache、Broadcast 数据、Unroll | `spark.memory.storageFraction=0.5` |
| └ 执行内存（Execution Memory） | Shuffle 排序、Hash 聚合、Join 算子、缓存 | |

**堆内 vs 堆外内存：**

| 类型 | 说明 | 配置 |
|------|------|------|
| **堆内内存（On-heap）** | JVM 堆内的内存，受 JVM GC 管理 | `spark.executor.memory` 控制总量 |
| **堆外内存（Off-heap）** | 直接向 OS 申请的堆外内存，避免 GC 暂停，支持精确控制 | `spark.memory.offheap.enabled=true`，`spark.executor.memoryOverhead` 限制 |

**堆外内存优势：**
1. **避免 Full GC**：不受 JVM GC 影响，适合长时间运行任务
2. **精确控制**：内存生命周期管理更精细
3. **Tungsten 优化**：配合 Tungsten 序列化实现零拷贝序列化

**内存动态分配：** 当执行内存不足时，可抢占存储内存（反之亦然），但存储内存被占用后不会主动回收（需等待缓存清理策略）。

**来源：** [Spark 官方文档 - Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management)

---

*整理于 2026-08-12 | 共5道题*
