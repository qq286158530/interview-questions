# Redis 面试题整理

> 📅 整理日期：2026-08-18  
> 📚 来源：[小林coding - 图解 Redis 面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目一：Redis 和 Memcached 有什么区别？为什么选择 Redis 作为 MySQL 的缓存？

### 参考答案

**Redis 与 Memcached 共同点：**
- 都是基于内存的数据库，一般都用来当做缓存使用
- 都有过期策略
- 两者的性能都非常高

**Redis 与 Memcached 区别：**

| 区别 | Redis | Memcached |
|------|-------|-----------|
| 数据类型 | 支持 String、Hash、List、Set、ZSet 等丰富数据类型 | 只支持最简单的 key-value 数据类型 |
| 持久化 | 支持 RDB 和 AOF 两种持久化方式，数据可保存到磁盘 | 无持久化功能，数据全部存在内存中 |
| 集群模式 | 原生支持主从复制、哨兵模式、Cluster 切片集群 | 没有原生集群模式，需客户端实现分片 |
| 高级特性 | 支持发布订阅、Lua 脚本、事务等 | 不支持 |

**为什么用 Redis 作为 MySQL 的缓存？**

1. **高性能**：用户第一次访问 MySQL 数据较慢（从硬盘读取），将数据缓存到 Redis 后，下一次访问直接从内存读取，速度极快。

2. **高并发**：单台 Redis 的 QPS 是 MySQL 的 10 倍。Redis 单机 QPS 能轻松破 10w，而 MySQL 单机 QPS 很难破 1w。把数据库部分数据转移到缓存，可以承受更大并发请求。

**来源：** https://xiaolincoding.com/redis/base/redis_interview.html

---

## 题目二：Redis 过期键的删除策略有哪些？持久化时过期键如何处理？

### 参考答案

**过期删除策略：惰性删除 + 定期删除**

**1. 惰性删除**
- 不主动删除过期键，每次访问 key 时才检查是否过期
- 优点：对 CPU 友好，几乎不占用系统资源
- 缺点：过期键一直没被访问就不会释放，造成内存浪费

**2. 定期删除**
- 每隔一段时间随机从数据库中抽取 20 个 key 检查
- 删除已过期的 key，若过期比例超过 25% 则继续抽取
- 循环执行，但单次执行时间上限 25ms，避免卡死
- 优点：平衡了 CPU 和内存
- 缺点：难以精确控制删除频率

**持久化时对过期键的处理：**

- **RDB 生成阶段**：过期键不会被写入 RDB 文件
- **RDB 加载阶段**：主服务器模式过期键不会载入；从服务器模式所有键都载入，但主从同步时从服务器数据会被清空
- **AOF 写入阶段**：过期键保留，删除时追加 DEL 命令
- **AOF 重写阶段**：已过期键不会被写入新的 AOF 文件

**主从模式**：从库不主动扫描过期键，过期处理依靠主库控制——主库 key 到期时在 AOF 中追加 DEL 指令，同步到所有从库执行删除。

**来源：** https://xiaolincoding.com/redis/base/redis_interview.html

---

## 题目三：Redis 持久化有哪几种方式？RDB 和 AOF 各有什么优缺点？混合持久化是什么？

### 参考答案

**Redis 三种持久化方式：**

**1. RDB 快照（Redis Database）**
- 记录某一时刻的内存数据，以二进制格式写入磁盘
- 命令：`save`（主线程执行，会阻塞）和 `bgsave`（子进程执行，不阻塞）
- 通过 `fork()` 创建子进程，采用写时复制（COW）技术保证数据一致性

**2. AOF 日志（Append Only File）**
- 每执行一条写操作命令，就以追加方式写入日志
- 先执行命令再写日志，好处是：避免语法检查开销、不阻塞当前命令
- 三种写回策略：

| 策略 | 说明 | 安全性 | 性能 |
|------|------|--------|------|
| Always | 每次执行完同步写回 | 最高 | 最差 |
| Everysec | 每秒写回一次 | 中等 | 较好 |
| No | 交给操作系统控制 | 最低 | 最好 |

**3. 混合持久化（Redis 4.0+）**
- RDB 头 + AOF 尾：前半部分是 RDB 格式全量数据，后半部分是 AOF 格式增量命令
- 加载时先快速恢复 RDB 部分，再加载增量 AOF
- 兼顾启动速度和数据丢失风险

**RDB 优缺点：**
- ✅ 恢复速度快
- ✅ 适合备份和灾难恢复
- ❌ 快照频率难把握，频率太低丢数据多，频率太高影响性能
- ❌ 全量快照，数据量大时开销大

**AOF 优缺点：**
- ✅ 丢失数据少（可配置每秒或实时）
- ✅ 文件易读，可手动处理
- ❌ 文件体积大，恢复速度慢
- ❌ 开启后对写性能有一定影响

**来源：** https://xiaolincoding.com/redis/base/redis_interview.html

---

## 题目四：Redis 主从复制、哨兵模式和 Cluster 集群模式分别是什么？集群脑裂问题如何解决？

### 参考答案

**1. 主从复制（一主多从）**
- 主服务器可读写，从服务器只读
- 写操作在主服务器执行，自动同步到所有从服务器
- 命令复制是异步的，主服务器不等从服务器执行完就返回结果
- 无法保证强一致性，数据不一致难以避免
- 是 Redis 高可用的基础

**2. 哨兵模式**
- 解决主从模式下故障需手动恢复的问题
- 哨兵（Sentinel）可以监控主从服务器状态
- 当主节点故障时，自动从从节点中选举新主节点，完成故障转移
- 提供主从节点监控、故障转移、通知等功能

**3. Cluster 切片集群**
- 当数据量大到单台服务器无法缓存时使用
- 采用 16384 个哈希槽（Hash Slot）管理数据分布
- 键值对根据 key 的 CRC16 值对 16384 取模，决定所属槽位
- 槽位可手动分配或自动平均分配
- 每个节点负责一部分槽位，实现数据分片

**集群脑裂问题：**

**问题原因：**
- 主节点网络故障，与所有从节点失联，但与客户端连接正常
- 哨兵未检测到心跳误判主节点下线，选举出新主节点
- 网络恢复后，旧主节点降为从节点，全量同步清空本地数据
- 导致客户端在脑裂期间写入的数据丢失

**解决方案：**
配置两个参数限制主节点写入：

```conf
min-slaves-to-write 1   # 主节点至少有1个从节点连接
min-slaves-max-lag 10   # 主从复制延迟不能超过10秒
```

要求：主库至少要有 1 个从库，且主从数据复制 ACK 延迟不超过 10 秒，否则主库拒绝接收客户端写请求。

即使原主库是假故障，无法响应心跳和 ACK 确认，就会被限制接收写请求，避免脑裂期间写入的数据丢失。

**来源：** https://xiaolincoding.com/redis/base/redis_interview.html

---

## 题目五：Redis 数据类型有哪些？String 类型的底层实现是什么？为什么 Redis 用跳表而不是红黑树实现 Zset？

### 参考答案

**Redis 五种基本数据类型：**

| 类型 | 使用场景 |
|------|----------|
| String | 缓存对象、计数器、分布式锁、共享 session |
| Hash | 缓存对象、购物车 |
| List | 消息队列（需自行实现全局唯一 ID） |
| Set | 聚合计算（点赞、共同关注、抽奖） |
| Zset | 排序场景（排行榜、电话姓名排序） |

**Redis 四种扩展数据类型：**

| 类型 | 使用场景 |
|------|----------|
| BitMap | 二值状态统计（签到、用户是否在线） |
| HyperLogLog | 海量数据基数统计（百万级 UV） |
| GEO | 地理位置（滴滴叫车） |
| Stream | 消息队列（支持消费组） |

**String 类型的底层实现：**
- 底层采用 SDS（Simple Dynamic String，简单动态字符串）
- SDS 相比 C 字符串优势：
  - 支持保存二进制数据（使用 len 判断结束，不依赖空字符）
  - 获取长度 O(1)（C 字符串需遍历 O(n)）
  - 拼接安全，不会缓冲区溢出（自动扩容）

**Zset 为什么用跳表而不是红黑树？**

| 对比 | 跳表 | 红黑树 |
|------|------|--------|
| 查找复杂度 | O(log n) | O(log n) |
| 范围查询 | 只需找到起点，顺序遍历 | 需要中序遍历 |
| 实现难度 | 简单 | 复杂 |
| 插入/删除 | 只需修改相邻节点指针 | 需要多次旋转和变色 |
| 占用内存 | 每个节点多个指针（层级） | 每个节点2个指针 + 颜色位 |
| 算法可控性 | 插入节奏可控 | 插入节奏依赖树平衡 |

Redis 选择跳表的核心原因：
1. **范围查询高效**：Zset 需要按分数排序支持 ZRANGE 等范围查询，跳表只需找到起点后顺序遍历，比红黑树的中序遍历更高效
2. **实现简单**：跳表比红黑树容易实现，降低了代码维护成本
3. **插入性能稳定**：跳表插入无需旋转操作，性能更稳定

**来源：** https://xiaolincoding.com/redis/base/redis_interview.html

---

> 🚀 以上内容由自动化脚本整理推送至 [GitHub 仓库](https://github.com/qq286158530/interview-questions)
