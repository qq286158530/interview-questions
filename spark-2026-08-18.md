# Spark 核心面试题

> 📅 发布日期：2026-08-18  
> 🏷️ 标签：Spark、Big Data、Apache Spark、面试题  
> 📌 适用岗位：大数据开发工程师、Spark 开发工程师

---

## 题目一：Spark 任务调度流程详解 —— 从 Job 到 Task 的完整过程

### 📖 问题

请描述 Spark 的任务调度流程，Job、Stage、Task 是如何划分的？

### ✅ 答案

Spark 的任务调度遵循 **Job → Stage → Task** 的层级结构，整个流程如下：

#### 1. 触发 Action，生成 Job

当代码执行到 Action 算子（如 `collect()`、`count()`、`saveAsTextFile()`）时，触发一个 Job。Spark DAGScheduler 会根据 **RDD 的依赖关系（DAG）** 将 Job 划分为多个 Stage。

#### 2. Stage 划分规则

Spark 根据 **宽依赖（ShuffleDependency）** 来划分 Stage：

- **窄依赖**（Narrow Dependency）：父 RDD 的分区最多被子 RDD 一个分区使用（如 `map`、`filter`）。窄依赖会被**流水线执行**，多个窄依赖操作可以融合在一个 Stage 中。
- **宽依赖**（Wide Dependency）：父 RDD 一个分区被多个子 RDD 分区依赖（如 `groupByKey`、`reduceByKey`）。**宽依赖是 Stage 划分的唯一边界**，遇到宽依赖即划分新 Stage。

> 划分算法：**逆向递归** —— 从最终的 RDD 出发，逆向遍历依赖链，遇到宽依赖就断开，生成新的 Stage。

#### 3. Task 生成与分发

每个 Stage 包含一组 **ShuffleMapTask**（中间结果）或 **ResultTask**（最终结果），Task 个数 = Stage 末端 RDD 的分区数。

调度流程：
```
Driver
  └─ DAGScheduler        （划分 Stage，生成 DAG）
       └─ TaskScheduler   （将 Stage 拆分为 TaskSet）
            └─ SchedulerBackend （与 Cluster Manager 交互，分配资源）
                 └─ Executor     （接收 Task，反序列化并执行）
```

#### 4. 数据本地性调度

TaskScheduler 优先将 Task 调度到数据所在节点，减少网络传输：
- `PROCESS_LOCAL`：数据在同一进程 ✅ 最优
- `NODE_LOCAL`：数据在同一节点
- `RACK_LOCAL`：数据在同一机架
- `ANY`：任意位置

### 📚 来源

- [Spark相关面试题 - 业余砖家 - 博客园](https://www.cnblogs.com/yeyuzhuanjia/p/18790203)
- [Spark面试题 - Golden Blog](https://gujincheng.github.io/2021/11/18/Spark%E9%9D%A2%E8%AF%95%E9%A2%98)

---

## 题目二：RDD 的五大特性与惰性计算机制

### 📖 问题

RDD 的五大特性是什么？如何理解"弹性"与"分布式"？Spark 的惰性计算（Lazy Evaluation）如何优化执行计划？

### ✅ 答案

#### RDD 的五大特性

| # | 特性 | 说明 |
|---|------|------|
| 1 | **分区列表（Partitions）** | RDD 逻辑上划分为多个分区，每个分区分布在不同节点，支持并行计算。分区数决定并行度。 |
| 2 | **计算函数（Compute）** | 每个分区通过 `compute` 函数定义数据生成逻辑，本质是运算逻辑的迭代器。 |
| 3 | **依赖关系（Dependencies）** | RDD 通过 Lineage（血缘）记录父子依赖，分为窄依赖和宽依赖，用于容错恢复。 |
| 4 | **分区器（Partitioner）** | 仅适用于 Key-Value 型 RDD，决定 Shuffle 后的数据分布方式。内置 `HashPartitioner` 和 `RangePartitioner`。 |
| 5 | **优先位置（Preferred Locations）** | 每个分区的首选计算位置（数据所在节点），Task 调度优先考虑数据本地性。 |

#### 如何理解"弹性"与"分布式"

- **弹性**：数据丢失时可通过 Lineage 重新计算，无需全量复制；自动在内存和磁盘间切换存储。
- **分布式**：数据跨节点存储，每个分区独立计算，通过依赖关系协调。

#### 惰性计算（Lazy Evaluation）

- **Transformation**（如 `map`、`filter`）：不立即执行，只记录操作逻辑，构建 RDD 依赖链（Lineage）。
- **Action**（如 `collect`、`count`）：触发实际计算。

惰性计算的优化效果：
1. **全局优化**：Spark 在 Action 触发时分析完整 DAG，合并连续操作（如多个 `map` 融合为一个）。
2. **减少不必要计算**：仅在需要时执行，避免中间结果落盘。
3. **谓词下推 & 列剪裁**：在 Spark SQL 中，Catalyst 优化器将过滤条件下推到数据源，减少读取量。

### 📚 来源

- [Spark相关面试题 - 业余砖家 - 博客园](https://www.cnblogs.com/yeyuzhuanjia/p/18790203)

---

## 题目三：Spark Shuffle 过程详解与性能优化

### 📖 问题

Spark 的 Shuffle 过程是怎样的？Hash Shuffle 和 Sort Shuffle 有何区别？如何优化 Shuffle 性能？

### ✅ 答案

#### Shuffle 过程

Shuffle 是 Spark 中将数据重新分区、跨节点传输的过程。当执行 `groupByKey`、`reduceByKey`、`join` 等需要重新分区操作的算子时触发。

**Write 阶段**：Map 任务将数据按分区器写入本地磁盘，生成临时文件。  
**Read 阶段**：Reduce 任务从多个 Map 节点拉取数据，进行聚合或 join。

#### Hash Shuffle vs Sort Shuffle

| 特性 | Hash Shuffle | Sort Shuffle |
|------|-------------|--------------|
| 文件生成 | 每个 Mapper 为每个 Reducer 生成独立文件 | 所有数据写入一个排序文件 |
| 文件数量 | Mapper数 × Reducer数（小文件爆炸） | Mapper数 × 2（数据文件 + 索引文件） |
| 内存压力 | 低，但磁盘文件过多 | 需排序，但文件数少 |
| 适用场景 | **已弃用** | **默认模式** |

Sort Shuffle 中有一个 **bypass 机制**：当 shuffle 算子没有指定排序且分区数小于 `spark.shuffle.sort.bypassMergeThreshold`（默认 200）时，不进行排序直接合并，绕过排序开销。

#### Shuffle 优化方法

**1. 减少 Shuffle 数据量**
```scala
// 使用 map-side 预聚合
val rdd = sc.textFile("data")
  .map(_.split(","))
  .reduceByKey(_ + _)  // 先局部聚合，减少传输量
```

**2. 使用广播变量**
```scala
val broadcastData = sc.broadcast(largeLookupTable)
val result = rdd.map { case (k, v) => (k, v + broadcastData.value(k)) }
```

**3. 调整 Shuffle 参数**

| 参数 | 默认值 | 优化建议 |
|------|--------|---------|
| `spark.shuffle.file.buffer` | 32KB | 增大到 64KB~1MB（减少磁盘 I/O） |
| `spark.reducer.maxSizeInFlight` | 48MB | 增大到 128MB（提升网络传输效率） |
| `spark.sql.shuffle.partitions` | 200 | 根据数据量调整，避免过多小分区 |
| `spark.shuffle.sort.bypassMergeThreshold` | 200 | 增大到 400（减少排序开销） |

**4. 启用 Kryo 序列化**
```scala
conf.set("spark.serializer", "org.apache.spark.serializer.KryoSerializer")
conf.registerKryoClasses(Array(classOf[MyClass]))
```

**5. 处理数据倾斜**
- 加盐（Salting）：为倾斜 Key 添加随机前缀分散数据。
- 两阶段聚合：局部聚合后再全局聚合。

### 📚 来源

- [Spark相关面试题 - 业余砖家 - 博客园](https://www.cnblogs.com/yeyuzhuanjia/p/18790203)
- [Spark面试题 - Golden Blog](https://gujincheng.github.io/2021/11/18/Spark%E9%9D%A2%E8%AF%95%E9%A2%98)

---

## 题目四：DataFrame、Dataset、RDD 三者的区别与联系

### 📖 问题

请描述 Spark 中 DataFrame、Dataset 和 RDD 的区别与联系，它们各自适合什么场景？

### ✅ 答案

#### 基本定义

- **RDD**（Resilient Distributed Dataset）：Spark Core 的基本数据结构，是数据的分布式集合，提供底层 API。
- **DataFrame**：`Dataset[Row]`，即**按列组织的有 schema 的 Dataset**。类似于关系型数据库的表，或 Python pandas 的 DataFrame。
- **Dataset**：强类型的分布式数据集，使用 Encoder 进行序列化，支持编译时类型检查。

```scala
// Scala 源码定义
type DataFrame = Dataset[Row]
```

#### 三者区别

| 特性 | RDD | DataFrame | Dataset |
|------|-----|-----------|---------|
| **类型安全** | 强类型（编译时检查） | 弱类型（运行时检查） | 强类型（编译时检查） |
| **Schema** | 无 | 有（自动或自定义） | 有 |
| **序列化方式** | Java/Kryo 序列化 | Kryo/Java 序列化 | Encoder（动态代码生成，更高效） |
| **优化** | 无自动优化 | Catalyst 优化器 | Catalyst 优化器 |
| **API 风格** | 函数式算子 | 链式 DSL + SQL | DSL + SQL |
| **数据处理方式** | 逐行处理 | 列式处理（成本更低） | 列式处理 |
| **适用场景** | 非结构化数据、底层控制 | 结构化/半结构化数据 | 结构化数据 + 类型安全需求 |

#### 核心联系

1. **DataFrame 是 Dataset 的特例**（`DataFrame = Dataset[Row]`）。
2. 三者都是**惰性求值**的。
3. **可以相互转换**：
   ```scala
   // RDD -> DataFrame
   val df = spark.createDataFrame(rdd)
   
   // DataFrame -> RDD
   val rdd = df.rdd
   
   // Dataset -> RDD
   val rdd = ds.rdd
   ```
4. DataFrame/Dataset 的底层最终都通过 **Tungsten 引擎** 转换为 RDD 执行，但Dataset 使用自定义 Encoder 而非 Java 序列化，性能更高。

#### 选型建议

- **用 RDD**：需要底层控制、处理非结构化数据、进行低级优化。
- **用 DataFrame**：处理结构化数据，需要 SQL 能力或自动优化。
- **用 Dataset**：需要编译时类型安全，同时享受 DataFrame 的优化红利。

### 📚 来源

- [Dataset和DataFrame的区别 - CSDN](https://blog.csdn.net/m0_56181660/article/details/135637332)
- [Spark面试题 - Golden Blog](https://gujincheng.github.io/2021/11/18/Spark%E9%9D%A2%E8%AF%95%E9%A2%98)

---

## 题目五：Spark 内存管理模型与广播变量原理

### 📖 问题

Spark 的内存是如何管理的？Execution Memory 和 Storage Memory 有何区别？广播变量和累加器的作用是什么？使用时应注意什么？

### ✅ 答案

#### Spark 内存管理模型（统一内存管理）

Spark 1.6 引入了**统一内存管理**机制，将堆内内存分为两部分：

```
┌──────────────────────────────────────────────┐
│          spark.executor.memory                │
│  ┌──────────────────┬───────────────────┐    │
│  │   Execution       │    Storage         │    │
│  │   Memory          │    Memory          │    │
│  │ (Shuffle/Join/   │  (RDD缓存/广播变量) │    │
│  │  Sort等计算)      │                   │    │
│  ├──────────────────┴───────────────────┤    │
│  │         User Memory                  │    │
│  │    (用户数据结构、元数据)             │    │
│  └────────────────────────────────────┘    │
│  Reserved Memory (900MB，固定)             │
└──────────────────────────────────────────────┘
```

| 区域 | 用途 | 特性 |
|------|------|------|
| **Execution Memory** | Shuffle、Join、Sort 等计算 | **不可被 Storage 抢占** |
| **Storage Memory** | 缓存 RDDD、广播变量 | 可被 Execution 动态抢占 |
| **User Memory** | 用户数据结构（HashMap 等） | 无自动管理 |
| **Reserved Memory** | 系统预留（900MB） | 固定不变 |

关键参数：
- `spark.memory.fraction = 0.6`：Execution + Storage 占堆内内存的比例
- `spark.memory.storageFraction = 0.5`：Storage 占 Execution+Storage 的比例

#### 广播变量（Broadcast Variables）

**作用**：高效分发**只读变量**到所有 Executor，避免每个 Task 重复传输。

**原理**：
1. Driver 端调用 `broadcast()` 将变量序列化。
2. 变量只发送**一次**到各节点，存储在 Executor 的内存中。
3. 所有 Task 共享同一份副本，减少网络传输。

```scala
val broadcastVar = sc.broadcast(Array(1, 2, 3))
rdd.map(x => broadcastVar.value.sum)  // Task 从本地读取
```

**使用注意事项**：
- ⚠️ **只读**：广播变量只能读不能修改。
- ⚠️ **不能广播 RDD**：应广播 RDD 的结果（如 `.collect()` 后的数组）。
- ⚠️ **大小适中**：广播变量过大（>20MB）会增加 Executor 内存压力，建议 <10MB。
- ⚠️ **Driver 端定义**：只能在 Driver 端定义，Executor 端无法定义。

#### 累加器（Accumulators）

**作用**：**分布式聚合**，如全局计数、求和。

**原理**：每个 Task 更新本地副本，Driver 端最终合并结果。Task 只可追加，Driver 只可读取。

```scala
val accum = sc.longAccumulator("errorCount")
rdd.foreach(x => if (x.contains("error")) accum.add(1))
```

#### 避免 OOM 的方法

1. 调大内存比例：`spark.memory.fraction=0.8`
2. 减少数据缓存：避免缓存过大的 RDD，使用 `MEMORY_AND_DISK`
3. 增大分区数：减少每个 Task 的数据量
4. 启用堆外内存：`spark.memory.offHeap.enabled=true`
5. 广播小表替代大表 Join

### 📚 来源

- [Spark相关面试题 - 业余砖家 - 博客园](https://www.cnblogs.com/yeyuzhuanjia/p/18790203)

---

> 📌 **面试小贴士**：Spark 面试的核心在于 **原理深度**，尤其是 Shuffle 过程、内存模型、任务调度的细节，结合实际调优经验能大幅加分。
