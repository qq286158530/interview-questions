# Redis 面试题（2026-08-06）

> 本文精选 5 道高频 Redis 面试题，涵盖数据类型、持久化、缓存、集群等核心知识点，助你面试一臂之力。
>
> 来源：[小林 coding - Redis 常见面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目一：Redis 和 Memcached 有什么区别？为什么要用 Redis 作为 MySQL 的缓存？

### 参考答案

**Redis 与 Memcached 的共同点：**
- 都是基于内存的数据库，常用来做缓存
-都有过期策略
- 两者性能都非常高

**主要区别：**

| 区别 | Redis | Memcached |
|------|-------|-----------|
| 数据类型 | 支持 String、Hash、List、Set、ZSet 等丰富数据类型 | 只支持最简单的 key-value 类型 |
| 持久化 | 支持 RDB 和 AOF 两种持久化方式，数据可恢复 | 无持久化功能，数据全存内存，断电即失 |
| 集群模式 | 原生支持主从复制、哨兵、Cluster 分片集群 | 无原生集群，需客户端自行实现分片 |
| 高级特性 | 支持发布订阅、Lua 脚本、事务等 | 不支持 |

**为什么用 Redis 作 MySQL 缓存？**

1. **高性能**：数据在内存中操作，速度极快。首次访问 MySQL 数据后缓存到 Redis，后续访问直接读缓存，延迟从毫秒级降至微秒级。

2. **高并发**：单台 Redis 的 QPS 能轻松破 10w，是 MySQL 的 10 倍以上。可以承接大量并发读请求，减少数据库压力。

---

## 题目二：Redis 的过期删除策略和内存淘汰策略有什么区别？

### 参考答案

**过期删除策略（针对已过期的 key）：**

Redis 采用「惰性删除 + 定期删除」组合策略：

- **惰性删除**：每次访问 key 时才检查是否过期，过期则删除。优点是对 CPU 友好，缺点是过期 key 长期占用内存。
- **定期删除**：每隔一段时间（如 100ms）随机抽取 20 个 key 检查，删除已过期的 key。如果过期 key 比例超过 25%，则继续抽取检查。为避免阻塞，每次定期删除循环不超过 25ms。

**内存淘汰策略（针对内存不足时的 key）：**

当 Redis 运行内存达到 `maxmemory` 上限时，触发淘汰机制。常见策略有：

- `noeviction`：不淘汰，返回错误（默认策略）
- `volatile-lru` / `allkeys-lru`：对设置了过期时间的 key / 所有 key 使用 LRU 算法淘汰
- `volatile-ttl`：淘汰 TTL 最短的 key
- `volatile-random`：随机淘汰设置了过期时间的 key
- `allkeys-random`：随机淘汰所有 key
- `volatile-lfu` / `allkeys-lfu`：使用 LFU（最不经常使用）算法淘汰

> 生产环境中，通常使用 `allkeys-lru` 或 `volatile-lru` 策略来保证热点数据不被淘汰。

---

## 题目三：Redis 的持久化机制有哪些？RDB 和 AOF 各自优缺点是什么？

### 参考答案

Redis 有三种持久化方式：**RDB 快照**、**AOF 日志**、**混合持久化**。

**RDB（Redis Database）：**

记录某一时刻的内存数据，以二进制格式写入磁盘。执行 `bgsave` 时，子进程遍历内存数据写入 RDB 文件，主线程可继续处理请求（利用 COW 写时复制技术）。

优点：恢复速度快（直接加载二进制文件），适合做全量备份。
缺点：可能丢失最后一次快照后的数据，频率太低会丢数据，太高会影响性能。

**AOF（Append Only File）：**

每执行一条写操作命令，就以文本协议追加写入日志文件。重启时逐条执行命令恢复数据。

写回策略（`appendfsync`）：

| 策略 | 含义 | 优点 | 缺点 |
|------|------|------|------|
| Always | 每次写命令执行完后同步写盘 | 数据不丢失 | I/O 开销大 |
| Everysec | 每秒将缓冲区写回硬盘 | 性能适中，最多丢 1 秒数据 | 可能丢 1 秒数据 |
| No | 由操作系统决定何时刷盘 | 性能最好 | 数据丢失不可控 |

**混合持久化（Redis 4.0+）：**

开启后，AOF 重写时先将内存数据以 RDB 格式写入 AOF 文件，再记录增量 AOF 命令。兼得快速启动和低数据丢失，缺点是可读性差且不兼容旧版本。

---

## 题目四：什么是缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 参考答案

**缓存穿透：** 指查询一个数据库中不存在的数据，由于缓存中也没有，每次请求都打到数据库。

**解决方案：**
- 布隆过滤器（Bloom Filter）：将所有合法 key 存入布隆过滤器，查询前先过滤
- 缓存空值：将空结果也缓存起来（TTL 设置短一些，如 60 秒）
- 参数校验：做好请求参数的合法性校验

**缓存击穿：** 指某个热点 key 过期瞬间，大量并发请求同时访问该 key，全部打到数据库。

**解决方案：**
- 互斥锁（Mutex）：只允许一个线程从数据库加载数据，其他线程等待
- 热点数据永不过期：物理不过期 + 逻辑过期（后台异步更新）
- 提前重建：访问时异步检测即将过期的 key 并提前更新

**缓存雪崩：** 指大量 key 同时过期，或 Redis 宕机，导致大量请求直接打到数据库。

**解决方案：**
- 过期时间随机化：将 key 的 TTL 加上随机数，避免同时过期
- 构建高可用集群：使用 Redis Sentinel 或 Cluster 避免单点故障
- 服务降级 / 限流：数据库压力过大时拒绝部分请求
- 预热：系统启动时提前加载热点数据到缓存

---

## 题目五：Redis 主从复制、哨兵模式和 Cluster 集群模式有什么区别？

### 参考答案

**主从复制（Master-Slave）：**

一主多从架构，主节点负责写操作，从节点只读并同步主节点数据。实现读写分离，提高读性能和数据冗余。缺点是主节点故障时需要手动切换。

**哨兵模式（Sentinel）：**

在主从复制基础上增加哨兵进程，监控主从节点的存活状态。当主节点宕机时，哨兵自动完成故障转移（failover），从从节点中选举出新主节点，并通知客户端更新连接。实现**高可用**但不支持数据分片。

**Cluster 模式（切片集群）：**

数据分布在多个节点上，每个节点存储一部分数据（16384 个哈希槽分片）。支持**数据分片 + 高可用**，每个主节点可配置从节点（自动故障转移）。适合数据量巨大的场景。

| 特性 | 主从复制 | 哨兵模式 | Cluster 模式 |
|------|---------|---------|-------------|
| 数据分片 | ❌ | ❌ | ✅ |
| 高可用 | ❌（需手动切换） | ✅ | ✅ |
| 写能力 | 单主 | 单主 | 多主 |
| 适用场景 | 读写分离备份 | 高可用小规模 | 大规模分布式存储 |

> **脑裂问题**：当网络分区导致出现两个主节点时，客户端写入旧主节点的数据可能在恢复后丢失。解决方案是配置 `min-slaves-to-write` 和 `min-slaves-max-lag`，确保主节点在从节点过少或复制延迟过大时拒绝写入。

---

*整理自网络公开资料，供学习交流使用。*
