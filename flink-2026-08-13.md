# Flink 面试题 — 2026-08-13

> 本文档收录 5 道高质量 Flink 面试题，涵盖架构、Watermark、Checkpoint、窗口函数、状态管理等核心知识点。

---

## 题目 1：Flink 的架构是怎样的？为什么说 Flink 是「流优先」的计算框架？

### 参考答案

Flink 架构采用 **Master-Slave 分布式设计**，核心组件包括：

| 组件 | 职责 |
|------|------|
| **JobManager** | 作业调度、Checkpoint 协调、Flink 集群 Master |
| **TaskManager** | 实际执行 Task Slot，托管数据流算子 |
| **ResourceManager** | 管理系统资源（YARN/K8s/Mesos），管理 Task Slot |
| **Dispatcher** | 接收作业提交，启动 JobManager，提供 REST 接口 |

**作业执行流程：**

1. 用户代码 → `StreamGraph`（API 层面）
2. `StreamGraph` → `JobGraph`（添加算子链等优化）
3. `JobGraph` 提交给 JobManager
4. JobManager 向 ResourceManager 申请 Slot
5. TaskManager 启动并注册 Slot，JobManager 将 JobGraph 中的算子分发到各 TaskManager 执行

**为什么说 Flink 是「流优先」？**

- Flink 底层以 **DataStream API** 为核心，批处理（DataSet）是流处理的一种特例（bounded stream）
- 流处理天然支持 **低延迟 + 高吞吐**，批处理只是在无限数据流上加边界（window）
- 对比 Spark Streaming 的「微批」（mini-batch）设计，Flink 是真正的**原生流处理**，事件时间、Watermark、水位线等概念深度融合

---

## 题目 2：Flink 的 Watermark 机制是什么？如何处理乱序数据和迟到事件？

### 参考答案

### Watermark 是什么

Watermark（水位线）是 Flink 用于处理**乱序事件**的核心机制，本质是一个**时间戳**，表示「所有时间戳 ≤ Watermark 的数据都已到达」。

```
Watermark(t) = MAX(event_time) - max_out_of_orderness
```

### 为什么需要 Watermark

在事件时间（Event Time）语义下，数据可能因网络延迟等原因**乱序到达**。如果窗口仅依赖事件时间关闭窗口，必须等到所有数据都到达才能触发计算，Watermark 正是用来**判断数据是否「足够完整」**的信号。

### 处理乱序数据

Flink 提供三种策略处理迟到事件：

| 策略 | 配置方法 | 效果 |
|------|----------|------|
| **默认丢弃** | `WatermarkStrategy.forMonotonousTimestamps()` | 事件 < Watermark 时直接丢弃 |
| **允许延迟** | `.withAllowedLateness(Duration.ofMinutes(5))` | 在窗口触发后，额外等待一段延迟 |
| **侧输出迟到数据** | `OutputTag<Long> lateTag = new OutputTag<>("late", TypeInformation.of(Long.class))` | 将迟到数据重定向到侧输出流 |

### 代码示例

```java
WatermarkStrategy<Event> wms = WatermarkStrategy
    .<Event>forBoundedOutOfOrderness(Duration.ofSeconds(10)) // 最大乱序时间 10s
    .withTimestampAssigner((event, timestamp) -> event.getTimestamp())
    .withIdleness(Duration.ofMinutes(1)); // 处理空闲数据源
```

### 注意事项

- Watermark 生成一般在 **Source 算子**完成，使用 `WatermarkStrategy` 配置
- 多流 JOIN 时，需注意不同流 Watermark 速度不同，Flink 会取 **最小 Watermark** 推进事件时间
- 如果数据源完全无序，建议使用 `forMonotonousTimestamps`（单调递增）

---

## 题目 3：Flink 的 Checkpoint 和 Savepoint 有什么区别？如何实现 EXACTLY_ONCE 语义？

### 参考答案

### Checkpoint vs Savepoint

| 维度 | Checkpoint | Savepoint |
|------|------------|-----------|
| **触发方式** | Flink 自动周期性触发 | 手动触发（用户命令） |
| **目的** | 故障恢复（自动容错） | 计划性备份/升级 |
| **状态后端** | 可选（HashMap / RocksDB） | 相同 |
| **状态清理** | Job 取消/超时自动删除 | 需手动删除 |
| **兼容性问题** | 可能不兼容（状态格式可能变化） | 设计为兼容（通过唯一 ID） |

### Checkpoint 实现原理

Flink 使用 **Chandy-Lamport 分布式快照算法**的变体（Asynchronous Barrier Snapshots）：

1. **JobManager** 定期向所有 Source 算子注入 `CheckpointBarrier`
2. Source 收到 Barrier 后，**快照自身状态**（偏移量等），然后将 Barrier 广播到下游
3. 算子收到所有输入的 Barrier 后，**阻塞输入**，完成快照后向下游转发
4. Checkpoint 完成时，状态写入配置的 **State Backend**（RocksDB/Heap）

```
[Source] --barrier--> [Map] --barrier--> [Window] --barrier--> [Sink]
   |              |              |              |
snapshot      snapshot       snapshot       snapshot
```

### EXACTLY_ONCE 实现条件

要实现端到端 EXACTLY_ONCE，需要满足：

1. **Source 端**：可重置读取偏移量（如 Kafka Consumer 的事务机制）
2. **Flink 内部**：Checkpoint 开启（`env.enableCheckpointing(1000)`）
3. **Sink 端**：支持幂等写入或两阶段提交（2PC）

**Kafka Sink 的 EXACTLY_ONCE**（2PC 提交）：

```java
KafkaSink<String> sink = KafkaSink.<String>builder()
    .setBootstrapServers("localhost:9092")
    .setRecordSerializer(new SimpleStringSerializer())
    .setDeliveryGuarantee(DeliveryGuarantee.EXACTLY_ONCE)
    .setTransactionalIdPrefix("my-flink-job")
    .build();
```

### 常见问题

- **RocksDB State Backend**：增量 Checkpoint 减少写入量，适合大状态场景
- **Checkpoint 超时**：`checkpointing.timeout()` 配置超时时间
- **对齐 Checkpoint vs 非对齐 Checkpoint**：Flink 1.11+ 支持非对齐 Checkpoint，减少阻塞

---

## 题目 4：Flink 的窗口函数有哪几种？滚动窗口、滑动窗口、会话窗口的区别是什么？

### 参考答案

### 窗口类型总览

```
┌─────────────────────────────────────────────────────┐
│                    窗口类型                          │
├─────────────────┬─────────────────┬─────────────────┤
│   Time Window   │  Count Window   │  Session Window │
├─────────────────┼─────────────────┼─────────────────┤
│ 滚动 Tumbling   │  滚动 Tumbling  │   Session       │
│ 滑动 Sliding    │  滑动 Sliding   │                 │
│ 计数 Count      │  计数 Count     │                 │
└─────────────────┴─────────────────┴─────────────────┘
```

### 滚动窗口（Tumbling Window）

- **特点**：窗口大小固定，窗口之间**不重叠、不留缝隙**
- 适用于：按固定周期聚合，如「每分钟 pv」

```java
// 每 5 分钟统计一次
data.stream()
    .keyBy(r -> r.key)
    .window(TumblingEventTimeWindows.of(Time.minutes(5)))
    .sum("count");
```

### 滑动窗口（Sliding Window）

- **特点**：窗口大小固定，但可以**重叠**（由 slide 参数控制）
- 适用于：平滑聚合，如「最近 5 分钟的平均值，每 1 分钟更新一次」

```java
// 窗口大小 10 分钟，滑动步长 5 分钟
.window(SlidingEventTimeWindows.of(Time.minutes(10), Time.minutes(5)))
```

### 会话窗口（Session Window）

- **特点**：由**事件之间的间隔**动态决定窗口边界，而非固定时间
- 适用于：用户行为分析，如「用户 30 秒内无操作则结束会话」

```java
// 会话间隔 10 秒
.window(EventTimeSessionWindows.withGap(Time.seconds(10)))
```

### 增量聚合 vs 全量聚合

| 类型 | 代表函数 | 特点 |
|------|----------|------|
| **增量聚合** | `reduce()`、`aggregate()`、`sum()` | 来一条处理一条，保存中间结果，内存友好 |
| **全量聚合** | `apply()`、`process()` | 等窗口内数据全到再处理，适合复杂逻辑 |

```java
// 增量聚合
.window(TumblingEventTimeWindows.of(Time.minutes(5)))
.reduce(
    (v1, v2) -> new Tuple2<>(v1.f0, v1.f1 + v2.f1),  // 预聚合
    new MyWindowFunction()                              // 全量计算
);
```

---

## 题目 5：Flink 的状态管理机制是什么？如何优化大状态场景下的性能？

### 参考答案

### Flink 状态分类

| 分类维度 | 类型 | 说明 |
|----------|------|------|
| **按数据结构** | ValueState、ListState、MapState、ReducingState、AggregatingState | 不同数据结构适用不同场景 |
| **按访问方式** | Keyed State、Operator State | Keyed State 按 key 分区，Operator State 全局共享 |
| **按一致性** | 原始状态（Raw State）、托管状态（Managed State） | Managed State 由 Flink 管理，Raw State 用户自行管理 |

### 托管状态示例

```java
public class CountWindowFunction extends KeyedProcessFunction<String, Event, String> {
    
    // 定义状态
    private ValueState<Long> countState;
    private ListState<Event> eventList;
    
    @Override
    public void open(Configuration parameters) {
        countState = getRuntimeContext().getState(
            new ValueStateDescriptor<>("count", Long.class));
        eventList = getRuntimeContext().getListState(
            new ListStateDescriptor<>("events", Event.class));
    }
    
    @Override
    public void processElement(Event event, Context ctx, Collector<String> out) {
        // 使用状态
        countState.update(countState.value() + 1);
        eventList.add(event);
    }
}
```

### 大状态优化策略

#### 1. 选择合适的 State Backend

| Backend | 适用场景 | 特点 |
|---------|----------|------|
| **HashMapStateBackend** | 小状态、低延迟 | 内存存储，性能最高 |
| **EmbeddedRocksDBStateBackend** | 大状态、热数据 | 内存+本地磁盘，支持增量 Checkpoint |

```java
// 推荐：大状态使用 RocksDB
env.setStateBackend(new EmbeddedRocksDBStateBackend());
env.enableIncrementalCheckpointing(true); // 增量 Checkpoint
```

#### 2. 状态过期清理（TTL）

```java
StateTtlConfig ttlConfig = StateTtlConfig.newBuilder(Time.days(1))
    .setUpdateType(StateTtlConfig.UpdateType.OnReadAndWrite)
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired)
    .build();

ValueStateDescriptor<String> desc = new ValueStateDescriptor<>("keyed-state", String.class);
desc.enableTimeToLive(ttlConfig);
```

#### 3. 合理设计 KeyBy

- KeyBy 将数据分配到不同 **Slot**，过少的 Key 会导致数据倾斜
- **Broadcast State**：适合需要广播到所有分区的场景（如规则配置）

#### 4. 增量 Checkpoint + RocksDB

```java
env.enableCheckpointing(30_000, CheckpointingMode.EXACTLY_ONCE);
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(10_000); // 间隔至少 10s
env.getCheckpointConfig().setCheckpointTimeout(60_000);
env.getCheckpointConfig().setMaxConcurrentCheckpoints(1);
```

#### 5. 避免大字段作为 State

- State 中避免存储大对象（如完整 JSON 或列表）
- 将 State 拆分到多个 KeyedProcessFunction 链中处理

---

## 参考来源

1. [Apache Flink 官方文档](https://nightlies.apache.org/flink/flink-docs-stable/)
2. [Flink Forward 会议资料](https://flink-forward.org/)
3. [Ververica Blog - Flink 核心原理](https://www.ververica.com/blog)
4. [Flink GitHub 官方仓库 examples](https://github.com/apache/flink)
5. [Apache Flink 1.18 官方文档 - 状态与Checkpoint](https://nightlies.apache.org/flink/flink-docs-stable/docs/concepts/stateful-stream-processing/)
