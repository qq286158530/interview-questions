# 程序员面试题 - 2026-07-26

## 🔥 HBase 核心知识点

---

### 题目1：HBase 的架构原理是什么？一个 RegionServer 由哪些组件构成？

【答案】

**整体架构**：
HBase 采用 Master-Slave 架构，主要包含以下角色：
- **HMaster**：负责表的管理（创建、删除）、Region 的分配和调度、负载均衡，以及元数据管理（协调所有 RegionServer）
- **HRegionServer**：负责处理客户端的读写请求，是真正存储数据的节点
- **ZooKeeper**：负责选举 Master、存储 meta 表位置、服务注册与发现
- **HDFS**：底层的分布式文件系统，负责存储 HFile 和 WAL

**RegionServer 内部组件**：
1. **WAL（Write-Ahead Log）**：预写日志，用于宕机后恢复 MemStore 未刷写的数据
2. **MemStore**：写缓存，按 RowKey + ColumnFamily + Key 排序（LSM 树结构），达到阈值后刷写成 HFile
3. **HFile**：HBase 的实际存储文件，存储在 HDFS 上，包含 Data Block、Index Block、Bloom Filter 等
4. **BlockCache**：读缓存，使用 LRU 策略，热点数据放在堆内缓存
5. **Region**：表的数据分片，每个 Region 负责一段 RowKey 范围

**读写流程**：
- **写**：Client → ZooKeeper → RegionServer → WAL → MemStore → 响应（异步刷写 HFile）
- **读**：先查 BlockCache（缓存），未命中则查 MemStore，最后查 HFile（利用 Bloom Filter 快速定位）

📖 来源：https://hbase.apache.org/book.html#architecture

---分割线---

### 题目2：HBase 的 RowKey 设计原则有哪些？如何避免热点问题？

【答案】

**RowKey 设计核心原则**：

1. **长度原则**：RowKey 不要过长，建议控制在 16~100 字节内，越短越好
   - 过长的 RowKey 会占用大量 HFile 空间，影响索引和 Bloom Filter 效果
   - HBase 支持的 RowKey 最大 64KB，但实际建议控制在可接受范围

2. **散列原则**：RowKey 必须具备散列性，避免数据集中到少数 Region（热点问题）
   - 错误示例：时间戳作为 RowKey 前缀，所有新数据写到同一个 Region
   - 正确做法：使用 MD5、Hash、雪花算法等打散 RowKey

3. **唯一原则**：RowKey 必须唯一标识一行数据

4. **有序原则**：HBase 中的数据按 RowKey 字典序排序，可利用此特性设计区间查询

**避免热点策略**：
- **加盐（Salting）**：在 RowKey 头部添加随机字节，使数据分布到不同 Region
- **哈希打散**：对 RowKey 做 MD5 或其他 hash 取模，分散数据
- **反转策略**：如果查询场景中固定后缀（如手机号后几位），可反转后作为 RowKey 头部
- **时间戳反转**：将时间戳用 Long.MAX_VALUE - timestamp，减少连续时间数据集中

**示例**：
```bash
# 热点：所有订单按时间戳写
RowKey = 20260726100000_12345  ❌

# 散列：MD5 取模分桶
RowKey = 01_md5(20260726100000_12345)  ✅

# 业务+时间组合，加盐
RowKey = userId + "_" + (System.currentTimeMillis() % 100)  ✅
```

📖 来源：https://hbase.apache.org/book.html#rowkey.design

---分割线---

### 题目3：MemStore 刷写的触发条件是什么？HFile 的合并（Compaction）策略是怎样的？

【答案】

**MemStore 刷写（Flush）触发条件**：
1. **内存阈值**：当 MemStore 大小达到 `hbase.hregion.memstore.flush.size`（默认 128MB）时触发刷写
2. **全局内存压力**：当 RegionServer 上所有 MemStore 总大小超过 `hbase.regionserver.global.memstore.size.lower.limit`（默认堆内存 40%）时，会按Region逐个刷写，优先刷写最大的 MemStore
3. **WAL 日志数量**：当 WAL 日志数量超过 `hbase.regionserver.maxlogs`（默认 32）时强制刷写，防止 WAL 过多占用内存
4. **定期刷写**：`hbase.regionserver.optionalcacheflushinterval`（默认 3600000 毫秒 = 1小时），定期将 MemStore 刷写成 HFile
5. **手动触发**：客户端可主动调用 flush 命令

**HFile 合并（Compaction）策略**：
1. **Minor Compaction（小合并）**：
   - 合并少量的相邻 HFile（通常 3~10 个）
   - 只合并时间较近的 HFile，不删除已标记删除的数据
   - 触发条件：`hbase.hstore.compaction.min`（默认 3 个 HFile）

2. **Major Compaction（大合并）**：
   - 合并一个 Store 下所有的 HFile 为一个
   - 会删除已标记为删除的数据（TTL、版本过期的数据也会被清理）
   - 默认每 7 天自动执行一次（`hbase.hregion.majorcompaction`）
   - **生产环境建议关闭自动 Major Compaction**，改为手动在业务低峰期触发，防止 IO 风暴

**刷写与合并的关系**：
- 刷写产生 HFile，HFile 过多触发 Compaction
- Compaction 减少 HFile 数量，降低读放大问题
- 但合并过程会产生大量 IO，需合理控制合并规模和时机

📖 来源：https://hbase.apache.org/book.html#regionserver_splitting_and_compaction

---分割线---

### 题目4：HBase 的数据写入流程是怎样的？为什么需要先写 WAL 再写 MemStore？

【答案】

**完整写入流程**：
1. **定位 Region**：Client 通过 ZooKeeper 获取 `.META.` 表位置，查询目标 RowKey 属于哪个 Region
2. **写入 WAL**：先将数据写入 RegionServer 的 WAL（预写日志），WAL 保证数据持久化
3. **写入 MemStore**：将数据写入对应 Region 的 MemStore（内存中，按 LSM 树结构排序）
4. **返回 ACK**：写入成功，立即向客户端返回响应（此时数据已在内存中）
5. **异步刷写**：MemStore 达到阈值后，异步刷写成 HFile

**为什么先写 WAL 再写 MemStore？**

这是 HBase 实现 **高可靠** 的核心机制：

| 步骤 | WAL 作用 | MemStore 作用 |
|------|---------|--------------|
| 写入位置 | WAL 是 HDFS 上的日志文件，三副本保证可靠 | MemStore 是内存写缓存，断电即丢失 |
| 恢复能力 | 宕机重启后，通过回放 WAL 恢复未刷写的数据 | 数据未刷写前，MemStore 数据丢失 |
| 顺序写 | WAL 是顺序写，性能高 | MemStore 是内存写，性能极高 |
| 刷写时机 | WAL 积累到一定程度才触发刷写 | MemStore 达到阈值立即刷写 |

**异常恢复过程**：
- RegionServer 宕机 → ZooKeeper 检测到心跳丢失 → Master 重新分配 Region
- 新 RegionServer 启动时，读取 WAL 日志并回放，将未持久化的数据重新写入 MemStore
- 这样保证了"数据不丢失"，即使服务器完全断电也能恢复

**性能优化点**：
- WAL 写完即返回，不等待刷盘（借助 HDFS 的三副本机制保证可靠性）
- 异步刷写使写入速度极快（纯内存操作），适合高并发写入场景

📖 来源：https://hbase.apache.org/book.html#wal

---分割线---

### 题目5：HBase 协处理器（Coprocessor）是什么？Observer 和 Endpoint 有什么区别？

【答案】

**协处理器是什么？**
协处理器是 HBase 提供的扩展机制，允许用户在 RegionServer 上运行自定义代码，将业务逻辑下沉到数据所在节点执行，避免大量数据传输。

**两种协处理器类型**：

| 类型 | Observer | Endpoint |
|------|----------|---------|
| **作用** | 类似数据库触发器，在特定事件前后拦截并执行自定义逻辑 | 类似存储过程，允许在 RegionServer 上执行自定义计算 |
| **调用方式** | HBase 自动调用（钩子回调） | 客户端主动调用（RPC 协议） |
| **数据修改** | 可修改数据 | 不直接修改数据，可返回计算结果 |
| **典型场景** | 二级索引、审计日志、数据权限控制 | 聚合计算（如求和、统计）、批量数据处理 |

**Observer 协处理器**：
在指定操作前后触发，类似数据库触发器：

- **RegionObserver**：拦截 GET、PUT、DELETE、SCAN 等数据操作
- **MasterObserver**：拦截 CREATE TABLE、DELETE TABLE、ALTER TABLE 等 DDL 操作
- **WALObserver**：拦截 WAL 相关操作

```java
// Observer 示例：数据写入时自动创建二级索引
public class IndexObserver extends RegionObserver {
    @Override
    public void postPut(ObserverContext e, Put put, WALEdit edit, Durability durability) {
        // 在主表写入后，自动向索引表写入一条记录
        // 实现二级索引功能
    }
}
```

**Endpoint 协处理器**：
在 RegionServer 上执行自定义计算，返回聚合结果：

- **使用 Protocol Buffer 定义接口**
- 客户端调用 `coprocessorService()` 执行
- 常用于：COUNT、SUM、AVG 等聚合操作，或复杂的数据转换

```java
// Endpoint 示例：求列的 SUM
// 定义接口 → 实现类 → 注册到表 → 客户端调用
// 类似于在数据库端运行存储过程，结果直接返回，无需全表传输
```

**注意事项**：
- 协处理器代码运行在 RegionServer 上，异常会导致 RegionServer 崩溃
- 不要在协处理器中执行耗时很长的操作
- 生产环境使用协处理器需充分测试，避免影响集群稳定性

📖 来源：https://hbase.apache.org/book.html#cp

---分割线---

> 📌 本题库每日更新，欢迎 Star：https://github.com/qq286158530/interview-questions
