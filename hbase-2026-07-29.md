# HBase 面试题集锦（2026-07-29）

> 本文件收录 5 道高质量 HBase 面试题，涵盖架构设计、RowKey 策略、存储结构、MemStore/Compaction、协处理器等核心知识点。  
> 适合大数据开发工程师、HBase 运维工程师面试准备使用。

---

## 题目一：HBase 的整体架构是怎样的？请描述核心组件及其职责。

### 参考答案

HBase 是一个基于 Google BigTable 思想设计的分布式列族存储系统，其架构采用 **Master-Slave** 模式，核心组件包括以下几部分：

### 1. Zookeeper（协调服务）
- 负责 HBase 的分布式协调，管理 Regionserver 的活跃状态。
- 实现 Master 高可用：当 Master 宕机时，通过 ZK 选举新的 Master。
- 存放 `META` 表位置信息、集群配置等元数据。

### 2. HMaster（主节点）
- 管理所有 Region 的分配与调度。
- 负责表（Table）的创建、删除、修改（DDL 操作）。
- 监控 Regionserver 健康状态，自动进行负载均衡和故障恢复。
- 在 HBase 2.x 中，HMaster 角色已弱化，很多管理功能下放到 Regionserver。

### 3. Regionserver（数据节点）
- 负责数据的读写操作，是实际存储数据的地方。
- 一个 Regionserver 包含多个 Region，每个 Region 对应一张表的一段行键范围。
- 承担数据 flush、compact、split 等存储策略。

### 4. Region（数据分区）
- 是 HBase 中数据存储和负载均衡的基本单元。
- 每个 Region 由起始行键（StartKey）和结束行键（EndKey）标识。
- 当 Region 过大时，会自动拆分为两个子 Region。

### 5. Store（列族存储）
- 每个 Region 由一个或多个 Store 组成，每个 Store 对应一个列族（Column Family）。
- Store 由 MemStore 和 StoreFile（HFile）组成。

### 6. Write-Ahead Log（WAL）
- 所有写操作先写入 WAL，再写入 MemStore，确保数据持久化和故障恢复。

### 数据读写流程简述
- **写**：Client → ZK 获取 META 位置 → 找到目标 Regionserver → 写入 WAL → 写入 MemStore → 返回成功。
- **读**：Client → ZK → META → 定位 Region → 先查 MemStore，未命中则查 StoreFile。

> 📎 来源：[Apache HBase Official Documentation - Architecture Overview](https://hbase.apache.org/book.html#arch.overview)

---

## 题目二：HBase 的 RowKey 设计原则有哪些？如何设计一个高效的 RowKey？

### 参考答案

RowKey 是 HBase 中用于唯一标识一行记录的主键，其设计直接影响查询性能和读写吞吐量。核心原则如下：

### 1. 长度原则
- RowKey 长度建议控制在 **8~16 字节**（最大 64KB）。
- 原因：HBase 持久化到 HDFS 时，数据按 HFile 组织，过长的 RowKey 会浪费存储空间、增加 MemStore 内存压力。

### 2. 散列原则
- 避免单调递增或递减的 RowKey（如时间戳 `20260729000000`），否则会导致所有数据集中写入同一个 Region（热点 Region）。
- **解决方案**：对单调部分做 MD5 散列、反转时间戳、加盐（Salt）等。

### 3. 唯一性原则
- RowKey 必须唯一定位一行数据，否则同键数据会相互覆盖。

### 4. 查询模式匹配原则
- RowKey 设计应匹配业务常见查询场景：
  - **等值查询**：将目标字段放在 RowKey 前缀（如 `userId_orderId`）。
  - **范围查询**：将时间维度放在 RowKey 尾部，支持字典序范围扫描（如 `deviceId_reverseTimestamp`）。
  - **多条件组合**：可使用组合键，并善用 Scan 的 startRow/stopRow。

### 5. 避免热点策略

| 策略 | 说明 | 示例 |
|------|------|------|
| 加盐（Salt） | 在 RowKey 前拼接 Hash 前缀，打散数据 | `MD5(userId).substring(0,1)_userId` |
| 随机散列 | 对时间戳取反，让新数据分布在不同 Region | `deviceId_reverseTimestamp` |
| 哈希 | 对业务键做 Hash，兼顾均匀分布 | `SHA(orderId).substring(0,6)_orderId` |

### 6. 典型场景示例

**场景：电商订单系统，按用户查询订单**
```
# 差的设计（热点问题）
RowKey: orderId（单调递增）

# 好的设计
RowKey: MD5(userId).substring(0,2) + "_" + userId + "_" + orderId
```
这样不同用户的数据自然散列到不同 Region，避免写入热点。

> 📎 来源：[Apache HBase Book - RowKey Design](https://hbase.apache.org/book.html#rowkey.design)、[GitHub - God-Of-BigData/面试系列](https://github.com/wangzhiwubigdata/God-Of-BigData)

---

## 题目三：HBase 的存储结构是怎样的？请解释 MemStore 和 HFile 的工作原理。

### 参考答案

### 1. HBase 存储层次结构

```
Table
 └── Region
      └── Store（每个 Column Family 一个）
           ├── MemStore（写缓存）
           └── StoreFile（HFile） × N
                └── HFile（HDFS 上的数据文件）
```

### 2. MemStore（写缓存）
- **作用**：接收所有写请求，先写入内存中的 MemStore（按 RowKey 排序的跳表结构），而不是直接落盘。
- **优势**：利用内存排序避免随机写，大幅提升写入性能。
- **Flush 触发条件**（满足任一即触发）：
  - MemStore 大小超过 `hbase.hregion.memstore.flush.size`（默认 128MB）。
  - Region 中所有 MemStore 总大小超过 `hbase.regionserver.global.memstore.size`（默认堆内存 40%）。
  - WAL 文件数量超过 `hbase.regionserver.max.logs`（默认 32）。
- **Flush 过程**：满足条件时，将 MemStore 内存数据按列族写出为 HFile，然后清空 MemStore。

### 3. HFile（HBase 数据文件）
- HBase 底层数据文件格式，构建在 HDFS 之上。
- **数据结构**：HFile 是有序键值对存储，包含以下主要区域：
  - **Data Block**：存储实际的 KeyValue 数据，按 B+ Tree 索引加速范围扫描。
  - **Meta Block**：存储元信息，如布隆过滤器（Bloom Filter）数据。
  - **File Info**：文件级别的元数据（最后修改时间、键值对数量等）。
  - **Index Block**：数据块索引，支持快速定位 KeyValue。

### 4. HFile 的索引机制
- **B+ Tree 树索引**：每个 HFile 有一棵 B+ Tree 索引，索引节点指向数据块位置。
- **布隆过滤器（Bloom Filter）**：快速判断一个 Key 是否存在于 HFile 中，避免无效读取。
  - 每个 HFile 存储一个 BloomFilter 位数组。
  - 写入时将 Key 加入 BloomFilter，读取时先检查，O(1) 判断是否存在。

> 📎 来源：[Apache HBase Book - Storage](https://hbase.apache.org/book.html#perf.compactions)、[GitHub - God-Of-BigData](https://github.com/wangzhiwubigdata/God-Of-BigData)

---

## 题目四：描述 HBase 的 Compaction 机制（用途、分类、区别），以及何时触发 Flush？

### 参考答案

**用途：**
在 HBase 中，每次 MemStore flush 会生成一个 StoreFile。当 StoreFile 数量达到一定程度后，需要执行 Compaction 操作：
1. **合并文件**：将多个小 HFile 合并成一个大 HFile，减少文件数量，提升读性能。
2. **清理过期数据**：清除已标记删除的单元格和多版本数据。
3. **提高读写效率**：减少需扫描的 HFile 数量。

**分类：**

| 类型 | Minor Compaction（小合并） | Major Compaction（大合并） |
|---|---|---|
| 合并范围 | 部分 StoreFile（通常 3~10 个） | 一个 Region 下的**所有** StoreFile |
| 删除处理 | 不清理已标记删除的数据 | 清理所有过期、删除数据 |
| 版本合并 | 不做多版本合并 | 合并多版本，只保留最新版本 |
| 执行频率 | 较频繁（自动触发） | 较少（通常手动或定时） |
| IO 开销 | 较小 | 较大，可能造成 IO 风暴 |

**相关配置参数：**

| 参数 | 默认值 | 说明 |
|---|---|---|
| `hbase.hstore.compactionThreshold` | 3 | StoreFile 数量达到此值触发 Minor Compaction |
| `hbase.hstore.compaction.max` | 10 | Minor Compaction 最多合并的文件数 |
| `hbase.hregion.majorcompaction` | 7 天 | Major Compaction 触发间隔 |

> ⚠️ 生产环境中常关闭自动 Major Compaction，改为在业务低峰期手动执行，避免 IO 风暴影响线上服务。

**Flush 触发条件（MemStore → HFile）：**

| 触发条件 | 说明 |
|---|---|
| MemStore 大小达到阈值 | `hbase.regionserver.global.memstore.size`（默认堆大小的 40%） |
| 单个 MemStore 大小超限 | `hbase.hregion.memstore.flush.size`（默认 128MB） |
| WAL 文件数量过多 | `hbase.regionserver.max.logs`（默认 32 个 WAL） |
| 定期刷新 | `hbase.regionserver.optionalcacheflushinterval`（默认 3600000ms） |
| 手动执行 flush | HBase Shell `flush 'table_name'` |

**为什么需要 WAL？**
由于 MemStore 存放在内存中，若 RegionServer 宕机会导致内存数据丢失。因此在写入 MemStore 之前，数据会先写入 WAL（Write-Ahead Log）。WAL 顺序写入、同一个 RegionServer 上所有 Region 共享一个 WAL 文件。宕机恢复时，通过重放 WAL 恢复 MemStore 中未持久化的数据。

> 📎 来源：[GitHub - God-Of-BigData/面试系列/HBase面试题整理](https://github.com/wangzhiwubigdata/God-Of-BigData/blob/master/%E9%9D%A2%E8%AF%95%E7%B3%BB%E5%88%97/HBase%E9%9D%A2%E8%AF%95%E9%A2%98%E6%95%B4%E7%90%86/HBase.md)、[阿里云开发者社区](https://developer.aliyun.com/article/903136)

---

## 题目五：HBase 协处理器（Coprocessor）是什么？有哪几类？各自用途？

### 参考答案

**协处理器简介：**

HBase 协处理器类似数据库的触发器和存储过程，允许用户将计算逻辑下推到 RegionServer 端直接执行，减少网络传输开销，提升性能。HBase 在 0.92 版本后引入协处理器。

**两大类：**

**1. Observer（观察者）协处理器**

类似于触发器，在特定事件前后自动执行回调：

| 类型 | 触发时机 | 典型场景 |
|---|---|---|
| `RegionObserver` | Get/Put/Delete/Scan 等数据操作前后 | 二级索引、数据验证、权限控制 |
| `WALObserver` | WAL 写入前后 | WAL 切分、自定义备份 |
| `MasterObserver` | 表创建/删除、Region 合并/分裂等管理操作前后 | 审计日志、表结构变更通知 |
| `RegionServerObserver` | RegionServer 启动/关闭、Split/Flush 前后 | 资源管理 |

**2. Endpoint（端点）协处理器**

类似于存储过程，提供自定义 RPC 服务，在 RegionServer 端执行计算并将结果返回：

- 客户端调用 `CoprocessorRpcChannel`，向 RegionServer 发起 RPC 请求。
- RegionServer 执行自定义逻辑（如 SUM、AVG、COUNT 等聚合计算），只返回结果而非原始数据。
- 典型场景：**在服务端直接完成聚合计算**，避免大量数据在网络上传输。

**协处理器注意事项：**
- 资源消耗：协处理器运行在 RegionServer 上，过度使用会拖慢节点性能。
- 线程安全：Observer 回调中避免执行耗时操作，防止阻塞请求处理。
- 部署方式：可通过 HBase 配置（`hbase.coprocessor.*`）或 Shell `alter` 表的方式加载。

> 📎 来源：[Apache HBase Book - Coprocessors](https://hbase.apache.org/book.html#cp)、[博客园 - Hbase协处理器详解](https://www.cnblogs.com/shun7man/p/13179645.html)、[阿里云开发者社区 - HBase Coprocessor实现与应用](https://developer.aliyun.com/article/686346)

---

*整理 by HBase Interview Bot | 题目来源：Apache HBase Official Documentation & 经典面试题库*
