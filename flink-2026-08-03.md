# 程序员面试题 - 2026-08-03

## 🔥 Flink 核心知识

---

### 题目1：Flink 的架构是怎样的？JobManager 和 TaskManager 的职责是什么？

【答案】

**Flink 架构采用主从架构**，由 JobManager、TaskManager 和 Client 组成。

**核心组件**：

| 组件 | 职责 |
|------|------|
| JobManager | 整个集群的协调者，负责接收 Job、调度任务、协调 Checkpoint、协调故障恢复 |
| TaskManager | 真正执行任务的 Worker，每个 TM 包含多个 TaskSlot，用于执行算子子任务 |
| Client | 用户代码的客户端，负责将 JobGraph 提交给 JobManager |

**JobManager 内部组件**：
- **Dispatcher**：接收客户端提交的 Job，为每个 Job 启动一个新的 JobMaster
- **ResourceManager**：管理 TM 的 Slot，与 YARN/K8s 等资源管理器交互
- **JobMaster**：负责执行单个 Job，包括将 JobGraph 转为 ExecutionGraph、调度 Task 到 TM

**TaskManager 的 Slot**：
- 每个 TM 有若干 Slot，Slot 是资源分配的基本单位
- 每个 Slot 拥有固定的内存资源（不隔离 CPU）
- 默认情况下，Flink 允许子任务共享 Slot（适合同一 Job 的不同算子）

**执行流程**：
1. Client 将用户代码转为 StreamGraph → JobGraph
2. Client 提交 JobGraph 到 JobManager
3. JobManager 申请资源，调度 ExecutionGraph
4. TaskManager 接收 Task 并执行

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/overview/

---分割线---

### 题目2：Flink 的 Watermark 机制是什么？解决什么问题？如何使用？

【答案】

**Watermark（水位线）是 Flink 处理乱序事件的核心机制**。

**解决的问题**：
- 在事件时间（Event Time）语义下，数据可能乱序到达
- 窗口需要等到所有数据到齐才触发，但乱序数据可能永远等不到
- Watermark 定义了"到此时间为止，所有数据都已到达"的置信线

**Watermark 本质**：
- Watermark 是一个时间戳 `T`，表示"事件时间 ≤ T 的数据都已到达（或被认为不会再来）"
- 携带 Watermark 的特殊事件，随数据流注入

**两条重要规则**：
1. **窗口触发条件**：`watermark_time >= window_end_time`
2. **迟到数据处理**：
   - `watermark_time < window_end_time`：数据迟到，被丢弃或侧输出
   - `watermark_time >= window_end_time && < window_end_time + allowedLateness`：触发后仍在 allowedLateness 范围内，窗口重新触发

**生成方式**：
```java
// 方式1：固定时间间隔生成（周期性）
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofSeconds(5))
    .withTimestampAssigner(...)
    .withIdleness(Duration.ofMinutes(1));

// 方式2：Punctuated（每条数据后检查是否需要发射）
WatermarkStrategy.forMonotonousTimestamps()...
```

**注意**：
- Watermark 生成得太早 → 窗口可能丢失有效数据
- Watermark 生成得太晚 → 窗口触发延迟增大
- 使用 `withIdleness` 处理空闲数据源，避免 Watermark 停滞

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/timely-stream-processing/

---分割线---

### 题目3：Flink Checkpoint 的原理是什么？Exactly-Once 语义如何实现？

【答案】

**Checkpoint 是 Flink 实现分布式快照的核心机制**，基于 Chandy-Lamport 算法变体实现。

**核心概念**：
- **Barrier（屏障）**：Checkpoint 的核心，分为 Checkpoint Barrier（对齐点）和 Recovery Barrier
- **Barrier 对齐**：在 FLIP-27 之前，多输入算子（如 Join）需要等待所有输入 Channel 的 Barrier 到达才快照（对齐等待），可能导致反压
- **Unaligned Checkpoint**（Flink 1.11+）：Barrier 跳过已处理的记录，直接快照未处理的数据，显著提升大状态下的 Checkpoint 速度

**Exactly-Once 实现步骤**：
1. **Checkpoint Coordinator**（JM 发起）周期发送 Barrier 到所有 Source
2. Source 算子将自身状态写入状态后端，并将 Barrier 发送到下游
3. 中间算子收到 Barrier N：
   - **对齐模式**：暂停处理其他输入 Channel，缓存其数据，直到所有 Channel 的 Barrier N 都到达
   - **对齐完成后**：做快照（状态后端写入），发送 Barrier 到下游
4. Sink 算子完成快照后，向 Checkpoint Coordinator 确认
5. 所有算子都确认后，Checkpoint 完成

**状态后端选择**：
| 后端 | 适用场景 | 特点 |
|------|----------|------|
| HashMapStateBackend | 小状态、低延迟 | JVM Heap，存储在 TM 内存 |
| EmbeddedRocksDBStateBackend | 大状态、超大窗口 | RocksDB，RocksDB 在 TM 本地磁盘 |

**Savepoint vs Checkpoint**：
- Checkpoint：自动触发，基于 TM 本地状态恢复，可能丢失
- Savepoint：手动触发，存储在外部存储，必须指定 Job ID，可用于跨版本升级/迁移

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/learn-flink/fault_tolerance/

---分割线---

### 题目4：Flink 窗口有哪些类型？Session Window 如何实现？滚动窗口和滑动窗口的区别？

【答案】

**窗口类型**：

| 类型 | 说明 | 场景 |
|------|------|------|
| Tumbling Window（滚动窗口） | 大小固定，不重叠 | 每分钟统计、每天统计 |
| Sliding Window（滑动窗口） | 大小固定，可重叠 | 移动平均、持续监控 |
| Session Window（会话窗口） | 间隔超过 Gap 时触发 | 用户行为分析、会话统计 |
| Global Window（全局窗口） | 所有数据进入同一窗口 | 需要自定义触发器 |

**滚动窗口 vs 滑动窗口**：
```
滚动窗口 size=10s:
|----w1----|     |----w2----|     |----w3----|

滑动窗口 size=10s, slide=5s:
|----w1----|     |          |     |
      |----w2----|          |
            |----w3----|          |
```
- 滚动窗口：窗口首尾相接，不重叠，适合周期性统计
- 滑动窗口：可重叠，适合需要"平滑"数据的场景（如 10s 窗口每 5s 滑动一次）

**会话窗口实现**：
```java
DataStream<String> input = ...

// 动态 Gap（根据数据本身决定间隔）
input.keyBy(...)
    .window(EventTimeSessionWindow.withDynamicGap(new SessionWindowTimeGapExtractor<String>() {
        @Override
        public double extract(String element, long previousElementTimestamp) {
            // 从数据中提取会话间隔，如用户最后一次交互时间差
            return extractUserGap(element);
        }
    }))
    .sum(1);

// 固定 Gap
.keyBy(...)
    .window(EventTimeSessionWindow.withGap(Time.seconds(30)))
    .sum(1);
```

**会话窗口原理**：
- Flink 按 key 维护每个会话的状态
- 新数据到达时，如果与现有会话差距超过 Gap，则开启新会话窗口
- 会话窗口本身没有固定的开始结束时间，是动态的

**窗口函数**：
- `reduce()`、`aggregate()`、`sum()`：增量聚合，更高效
- `apply()`/`process()`：全量窗口函数，需要缓存所有数据

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/dev/datastream/operators/windows/

---分割线---

### 题目5：Flink 的状态管理机制是什么？Keyed State 和 Operator State 有何区别？如何避免状态过期或数据丢失？

【答案】

**Flink 状态分类**：

| 分类维度 | 类型 | 说明 |
|----------|------|------|
| 按数据范围 | Keyed State | 按 key 隔离，每个 key 独立状态（仅 KeyedStream 可用） |
| 按数据范围 | Operator State | 算子级别，所有算子实例共享状态（可使用 ListState / UnionListState / BroadcastState） |
| 按数据结构 | ValueState、ListState、MapState、ReducingState、AggregatingState | 不同的数据结构 |

**Keyed State vs Operator State**：

| 特性 | Keyed State | Operator State |
|------|-------------|----------------|
| 作用域 | 按 key 分区，每个 key 独立 | 算子实例级别 |
| 适用算子 | KeyedStream 上的算子 | 所有算子（Source、Sink、普通算子） |
| 重分布策略 | 不需要（按 key 自然分区） | ListState（均匀分配）、UnionListState（全量广播）、BroadCastState（全量广播） |
| 示例 | 窗口状态、key 对应的计数 | Kafka Source 分区 offset、Kafka Connector 的分区状态 |

**状态后端（State Backend）**：
```java
// HashMapStateBackend（内存）
env.setStateBackend(new HashMapStateBackend());

// RocksDBStateBackend（支持大状态）
env.setStateBackend(new EmbeddedRocksDBStateBackend());
```

**如何避免状态过期和数据丢失**：

1. **合理设置 TTL**：
```java
StateTtlConfig ttlConfig = StateTtlConfig.newBuilder(Time.seconds(10))
    .setUpdateType(StateTtlConfig.UpdateType.OnReadAndWrite) // 或 OnCreateAndWrite
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired) // 不返回过期数据
    .cleanupInRocksdbCompactFilter(1000) // RocksDB compact 时清理
    .build();
ValueStateDescriptor<String> desc = new ValueStateDescriptor<>("cached", String.class);
desc.enableTimeToLive(ttlConfig);
```

2. **开启 Checkpoint 并设置合理间隔**：
```java
env.enableCheckpointing(60_000); // 每 60s checkpoint 一次
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(30_000); // 两次 CK 至少间隔 30s
env.getCheckpointConfig().setCheckpointTimeout(120_000);
env.getCheckpointConfig().setTolerableCheckpointFailureNumber(3);
```

3. **使用合适的重启策略**：
```java
env.setRestartStrategy(RestartStrategies.failureRateRestart(
    3,                          // 失败次数上限
    Time.of(5, TimeUnit.MINUTES), // 时间窗口
    Time.of(10, TimeUnit.SECONDS) // 每次重启间隔
));
```

4. **使用 BroadcastState 广播重要配置**：
- 配置变更实时广播到所有算子实例，无需重启

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/learn-flink/overview/

---分割线---

> 📚 更多面试题每日推送中，欢迎 Star：https://github.com/qq286158530/interview-questions
