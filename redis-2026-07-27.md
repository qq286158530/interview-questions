# Redis 面试题精选 (2026-07-27)

> 本文精选 5 道高质量 Redis 面试题，涵盖数据类型、持久化、缓存、集群等核心知识点，适合面试复习巩固使用。

---

## 题目一：Redis 有哪些数据类型？请分别说明它们的典型使用场景。

### 参考答案

Redis 支持丰富的数据类型，主要分为以下几类：

| 数据类型 | 典型使用场景 |
|---------|-------------|
| **String（字符串）** | 缓存、计数器、分布式锁、简单的键值存储 |
| **Hash（哈希）** | 存储对象（如用户信息）、购物车数据、表结构数据 |
| **List（列表）** | 消息队列、任务队列、文章评论列表、最新消息列表 |
| **Set（集合）** | 标签系统、好友关系、抽奖、去重 |
| **Sorted Set（有序集合）** | 排行榜、热搜榜单、延迟队列、优先级队列 |
| **Bitmap（位图）** | 用户签到、活跃用户统计、布隆过滤器基础 |
| **HyperLogLog** | UV 统计、活跃用户数估算 |
| **Geospatial（地理空间）** | 附近的人、地理围栏、配送范围 |
| **Stream（流）** | 消息队列、事件流处理、日志收集 |
| **JSON** | 存储 JSON 文档、半结构化数据 |

**String 类型**是最基础的类型，本质上是字节序列，最大 512MB，常用命令包括 `SET`、`GET`、`INCR`、`INCRBY`。

**Hash 类型**适合存储对象，类似于 Python 的 dict 或 Java 的 HashMap，使用 `HSET`、`HGET`、`HGETALL` 等命令。

**List 类型**是插入有序的字符串列表，支持 LPUSH/LPOP 实现栈，LPUSH/RPOP 实现队列，常用于消息队列场景。

**Set 类型**是无序唯一字符串集合，支持交集(`SINTER`)、并集(`SUNION`)、差集(`SDIFF`)运算，适合标签和关系处理。

**Sorted Set** 每个元素关联一个分数来维持顺序，适合排行榜场景，可以用 `ZADD`、`ZREVRANGE` 命令实现。

**来源：** [Redis 官方文档 - Data Types](https://redis.io/docs/latest/develop/data-types/)

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？如何选择？

### 参考答案

Redis 提供了两种持久化机制：

### RDB（Redis Database Backup）

- **原理**：定时将内存中的数据快照（Snapshot）写入磁盘，生成 `dump.rdb` 文件
- **触发方式**：
  - 定时触发（`save` 配置，如 `save 900 1` 表示 900 秒内至少 1 次写操作则触发）
  - `bgsave` 命令异步执行
  - 主从复制时自动触发
- **优点**：恢复速度快，适合大规模数据备份
- **缺点**：可能丢失最后一次快照后的数据

### AOF（Append Only File）

- **原理**：记录所有写操作命令到日志文件，重启时重放命令恢复数据
- **同步策略**：
  - `always`：每个命令同步写入，安全性最高但性能最差
  - `everysec`（默认）：每秒同步一次，性能和安全性平衡
  - `no`：由操作系统决定何时同步，性能最好但可能丢失较多数据
- **优点**：数据安全性更高，支持重写（rewrite）压缩文件
- **缺点**：文件体积通常比 RDB 大，恢复速度较慢

### 如何选择

| 场景 | 推荐策略 |
|-----|---------|
| 能接受数分钟数据丢失 | 仅用 RDB |
| 对数据安全性要求很高 | 仅用 AOF |
| 最高可靠性 | 同时启用 RDB + AOF |

**生产环境建议**：使用 RDB 做定期备份，AOF 开启 `everysec` 同步策略。

**来源：** [Redis 官方文档 - Persistence](https://redis.io/docs/latest/operate/oss_and_stack/k耐久/)

---

## 题目三：什么是 Redis 缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 参考答案

这是 Redis 缓存最具代表性的三个问题：

### 1. 缓存穿透

**问题描述**：查询一个不存在的数据，缓存没有命中，数据库也没有数据，每次请求都会打到数据库。

**解决方案**：
- **布隆过滤器（Bloom Filter）**：将所有存在的数据哈希到一个 bitmap 中，请求先经过布隆过滤器过滤
- **缓存空值**：即使数据库查不到，也将空值缓存起来（设置较短的过期时间）
- **参数校验**：对请求参数进行合法性校验，拦截非法请求

### 2. 缓存击穿

**问题描述**：某个热点 key 过期瞬间，大量请求同时涌入，直接打在数据库上，造成数据库压力激增。

**解决方案**：
- **互斥锁（Mutex）**：使用 Redis 的 `SETNX` 实现分布式锁，只允许一个线程去查数据库并重建缓存
- **逻辑过期**：不给缓存设置真正过期时间，而是设置一个逻辑过期字段，读到时用单独线程重建缓存
- **热点数据永不过期**：对访问特别频繁的热点数据，设置为永不过期

### 3. 缓存雪崩

**问题描述**：大量缓存 key 在同一时间过期，或者 Redis 宕机，导致大量请求直接打到数据库。

**解决方案**：
- **过期时间随机化**：给缓存过期时间加上随机值，避免同时过期
- **Redis 高可用**：使用 Redis Cluster 或 Sentinel 哨兵机制，保证 Redis 的高可用性
- **限流降级**：使用 Hystrix 或 Sentinel 对数据库访问进行限流和降级
- **多级缓存**：引入本地缓存（如 Caffeine）作为多级缓存，避免完全依赖 Redis

**来源：** [Redis 官方文档 - Redis Architecture](https://redis.io/docs/latest/)

---

## 题目四：Redis 的过期 key 删除策略是什么？内存淘汰机制有哪些？

### 参考答案

### 过期 key 删除策略

Redis 有两种删除策略：

| 策略 | 说明 |
|-----|------|
| **惰性删除（Lazy Expire）** | 当访问某个 key 时，才检查是否过期并删除。优点是节省 CPU，缺点是过期 key 长时间占用内存 |
| **定期删除（Active Expire）** |  Redis 每隔一段时间随机检查一批 key，删除其中过期的 key。折中方案，平衡了 CPU 和内存 |

Redis 默认同时使用两种策略：惰性删除确保过期的 key 被访问时一定会被删除，定期删除负责清理未被访问的过期 key。

### 内存淘汰机制（Maxmemory Policy）

当 Redis 内存达到 `maxmemory` 限制时，会根据配置的淘汰策略来处理：

| 淘汰策略 | 说明 |
|---------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 从已设置过期时间的 key 中，使用 LRU 算法淘汰 |
| `allkeys-lru` | 从所有 key 中，使用 LRU 算法淘汰 |
| `volatile-lfu` | 从已设置过期时间的 key 中，使用 LFU 算法淘汰 |
| `allkeys-lfu` | 从所有 key 中，使用 LFU 算法淘汰 |
| `volatile-random` | 从已设置过期时间的 key 中随机淘汰 |
| `allkeys-random` | 从所有 key 中随机淘汰 |
| `volatile-ttl` | 淘汰 TTL（剩余存活时间）最短的 key |

**实际建议**：
- 缓存场景推荐 `volatile-lru` 或 `allkeys-lru`
- 需要精确控制内存时，应设置合理的 `maxmemory` 值

**来源：** [Redis 官方文档 - Memory Management](https://redis.io/docs/latest/operate/oss_and_stack/k内存/)

---

## 题目五：Redis 主从复制原理是什么？什么是哨兵（Sentinel）模式？什么是 Cluster 集群模式？

### 参考答案

### 主从复制（Replication）

**原理**：主节点（master）负责写操作，从节点（slave）通过复制主节点的 RDB 或 AOF 文件来同步数据，实现读写分离。

**复制过程**：
1. 从节点发送 `PSYNC` 命令给主节点
2. 主节点执行 `bgsave` 生成 RDB 文件，同时记录后续的写命令到缓冲区
3. 主节点将 RDB 文件发送给从节点
4. 从节点加载 RDB 文件
5. 主节点将缓冲区中的增量命令发送给从节点
6. 从节点执行这些命令，完成同步

**支持拓扑**：一主一从、一主多从、树状主从

### 哨兵模式（Sentinel）

**作用**：监控主从节点的运行状态，实现自动故障转移（failover），提供自动化的 Redis 高可用解决方案。

**核心功能**：
- **监控**：Sentinel 持续检查主从节点是否正常运行
- **通知**：故障发生时通知应用程序
- **自动故障转移：当主节点不可用时，Sentinel 会从从节点中选举一个新主节点，并让其他从节点去连接新主节点

**最小配置**：通常使用 3 个或更多 Sentinel 节点来保证可靠性

### Cluster 集群模式

**作用**：数据分片（sharding），支持横向扩展，解决单节点内存限制问题。

**特点**：
- **数据分片**：Redis Cluster 将整个数据空间划分为 16384 个槽位（slot），每个节点负责一部分槽位
- **高可用**：每个主节点可以配置一个或多个从节点（replica），主节点故障时从节点自动升级
- **去中心化**：没有中心协调节点，客户端直接连接任意节点
- **自动路由**：客户端可以请求任意节点，节点会自动将请求路由到正确的节点

**槽位分配**：例如 3 个主节点，节点 A 负责 0-5460 槽位，节点 B 负责 5461-10922 槽位，节点 C 负责 10923-16383 槽位

**适用场景**：
- 数据量巨大，单机无法容纳
- 需要高可用和自动故障转移
- 读多写多的场景

**来源：** [Redis 官方文档 - Replication](https://redis.io/docs/latest/operate/oss_and_stack/k内存/)
[Redis Cluster 文档](https://redis.io/docs/latest/operate/oss_and_stack/cluster/)

---

> 📌 **面试建议**：Redis 面试常围绕原理和实战展开，除了上述知识点，还需要熟悉 Redis 事务（Lua 脚本）、Redis 分布式锁实现、Redis 与 Memcached 的区别等。建议结合项目经验来回答。
