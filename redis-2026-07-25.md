# Redis 面试题集锦

> 本文档整理了 5 道高质量 Redis 面试题，涵盖数据类型、持久化、集群、缓存等核心知识点。  
> 来源：[小林 coding - Redis 常见面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目一：Redis 和 Memcached 有什么区别？为什么 Redis 比 Memcached 更适合做缓存？

### 参考答案

**共同点：**
- 都基于内存数据库，常用于缓存场景
- 都有过期策略
- 性能都非常高

**核心区别：**

| 区别 | Redis | Memcached |
|------|-------|-----------|
| 数据类型 | 支持 String、Hash、List、Set、ZSet 五种丰富数据类型 | 只支持简单的 key-value 类型 |
| 持久化 | 支持 RDB、AOF、混合持久化，重启后可恢复数据 | 无持久化，数据全部存内存，挂掉后数据丢失 |
| 集群 | 原生支持主从复制、哨兵、切片集群 | 无原生集群，需依赖客户端实现分片 |
| 高级特性 | 支持发布订阅、Lua 脚本、事务 | 不支持 |

**为什么 Redis 更适合做缓存：**
- **高性能**：Redis 单机 QPS 可达 10W+，是 MySQL 的 10 倍，直接访问缓存减少数据库压力
- **高并发**：缓存层承接大量请求，保护后端数据库
- **丰富的数据结构**：能应对各种业务场景（如排行榜用 ZSet、点赞用 Set）
- **数据可持久化**：缓存数据可恢复，安全性更高

**来源：** [小林 coding - Redis 和 Memcached 有什么区别](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目二：Redis 的 AOF 持久化是如何工作的？三种写回策略各自的优缺点是什么？

### 参考答案

**AOF 日志工作原理：**

Redis 在执行完每条写操作命令后，将该命令以追加（append）的方式写入一个 AOF 文件（appendonly.aof）。Redis 重启时，读取 AOF 文件中的命令并逐一执行来完成数据恢复。

AOF 日志内容示例（以 `set name xiaolin` 为例）：
```
*3
$3
set
$4
name
$6
xiaolin
```
其中 `*3` 表示命令有 3 部分，`$数字` 表示该部分的字节数。

**先执行命令再写日志的好处：**
1. 避免额外的命令检查开销（语法错误的命令不会写入日志）
2. 不会阻塞当前写操作命令

**风险：**
- 数据可能丢失（命令执行完但还未写入硬盘时宕机）
- AOF 写回硬盘时会阻塞后续操作

**三种写回策略（appendfsync 配置项）：**

| 策略 | 含义 | 优点 | 缺点 |
|------|------|------|------|
| **Always** | 每次执行完命令后同步写回硬盘 | 数据完全不丢失 | 开销最大，每次都要写盘，拖慢 Redis 性能 |
| **Everysec**（默认） | 每次写命令到内核缓冲区，每秒一次刷盘 | 性能适中，最多丢失 1 秒数据 | 宕机可能丢失 1 秒数据 |
| **No** | 由操作系统控制何时刷盘 | 性能最好 | 数据丢失不可控，依赖操作系统 |

**AOF 重写机制：**

随着写命令增多，AOF 文件会越来越大。Redis 通过 bgrewriteaof 后台子进程实现 AOF 重写——读取当前数据库中所有键值对，用一条命令记录到新的 AOF 文件，从而压缩文件体积。重写期间主进程继续处理命令，增量命令写入 AOF 重写缓冲区，重写完成后合并到新 AOF 文件。

**来源：** [小林 coding - AOF 日志是如何实现的](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目三：Redis 的 RDB 快照是怎么实现的？bgsave 时数据能被修改吗？

### 参考答案

**RDB 快照原理：**

RDB（Redis Database）快照将某一时刻的内存数据以二进制格式写入磁盘，是全量快照。相比 AOF 记录操作命令，RDB 直接记录数据，因此恢复速度更快。

Redis 提供两个生成 RDB 文件的命令：
- `save`：在主线程执行，会阻塞主线程，不推荐使用
- `bgsave`：创建子进程生成 RDB 文件，主线程不受影响（推荐）

Redis 也支持通过配置文件自动触发 bgsave：
```
save 900 1      # 900秒内至少1次修改
save 300 10     # 300秒内至少10次修改
save 60 10000   # 60秒内至少10000次修改
```

**bgsave 时数据能被修改吗？可以——依赖写时复制（COW）技术：**

执行 bgsave 时，通过 `fork()` 创建子进程，此时父子进程共享同一片物理内存（只读）。主线程执行读操作时，父子进程互不影响。

当主线程执行写操作时，被修改的数据会复制一份副本（写时复制），bgsave 子进程将副本数据写入 RDB 文件，而主线程继续修改原数据。这就保证了快照期间 Redis 仍能正常处理请求。

**RDB 的优缺点：**
- 优点：恢复数据速度快，全量备份简单
- 缺点：快照频率难把握（太频繁影响性能，太低丢失数据多）；是全量快照，重量级操作

**来源：** [小林 coding - RDB 快照是如何实现的](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目四：Redis 采用了单线程为什么还这么快？6.0 之后为什么又引入了多线程？

### 参考答案

**Redis 单线程为什么快？**

Redis 单线程指的是"接收客户端请求→解析请求→读写操作→发送响应"这一核心过程由单线程完成。其高性能的原因如下：

1. **瓶颈不在 CPU**：Redis 的大部分操作都在内存中完成，采用高效数据结构（SDS、跳表、哈希表等），性能瓶颈通常是内存或网络带宽，而非 CPU，因此单线程反而避免了多线程切换开销。

2. **无锁竞争**：单线程模型不存在多线程竞争问题，不会有加锁解锁、死锁等性能损耗。

3. **I/O 多路复用**：Redis 使用 epoll/select/kqueue 等 I/O 多路复用机制，单线程能同时处理大量 Socket 请求。一个线程监听多个 Socket，内核发现有请求到达时才交给 Redis 线程处理，实现了一个线程处理多个 IO 流的效果。

**Redis 6.0 之前为什么坚持用单线程？**

官方 FAQ 指出：CPU 不是 Redis 性能瓶颈，更多受限于内存大小和网络 I/O。单线程模型简单易维护，如需利用多核 CPU，可在多台服务器上启动多个 Redis 节点或使用分片集群。

**Redis 6.0 之后为什么引入多线程？**

随着网络硬件性能提升，Redis 的瓶颈有时出现在**网络 I/O 处理**上。Redis 6.0 对网络 I/O 引入多线程（默认只针对写响应，可配置开启读请求多线程），命令执行仍为单线程。

配置建议：4 核 CPU 建议 2~3 个 I/O 线程；8 核建议 6 个线程，且线程数必须小于机器核数。官方称该特性对性能提升至少 1 倍。

**来源：** [小林 coding - Redis 采用单线程为什么还这么快](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目五：Redis 的内存淘汰机制和过期 key 删除策略是如何工作的？

### 参考答案

**过期 key 删除策略（两种主动删除 + 惰性删除）：**

1. **惰性删除**：访问 key 时才检查是否过期，过期则删除。优点是对 CPU 友好，缺点是过期 key 堆积可能造成内存浪费。

2. **定时删除**：每个 key 设置一个定时器，到期立即删除。优点是内存友好，缺点是对 CPU 不友好（定时器多影响性能）。

Redis 采用**惰性删除 + 周期删除**结合的策略。

**周期删除**：Redis 通过 `timeout` 定时任务定期抽样检查一批 key，删除其中过期的 key。检查策略有两种：
- `SLOW` 模式：默认每 100ms 执行一次，扫描上限 20 个库，每个库检查少量 key
- `FAST` 模式：每个循环事件周期执行一次，扫描上限当前库的 key 数量

**内存淘汰策略（当 Redis 内存达到上限时）：**

Redis 支持 8 种内存淘汰策略（`maxmemory-policy` 配置）：

| 策略 | 含义 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 在设置了过期时间的 key 中，淘汰最近最少使用的 |
| `volatile-ttl` | 在设置了过期时间的 key 中，淘汰最早过期的 |
| `volatile-random` | 在设置了过期时间的 key 中，随机淘汰 |
| `volatile-lfu` | 在设置了过期时间的 key 中，淘汰使用频率最低的 |
| `allkeys-lru` | 所有 key 中，淘汰最近最少使用的 |
| `allkeys-random` | 所有 key 中，随机淘汰 |
| `allkeys-lfu` | 所有 key 中，淘汰使用频率最低的 |

**LRU 和 LFU 的区别：**
- LRU（Least Recently Used）：最近最少使用，按访问时间淘汰
- LFU（Least Frequently Used）：最不经常使用，按访问频率淘汰，热点数据不会被误淘汰

业务中使用建议：
- 缓存场景优先选 `volatile-lru` 或 `allkeys-lru`
- 有明显冷热数据之分选 LRU
- 访问频率比访问时间更重要选 LFU

**来源：** [小林 coding - Redis 常见面试题](https://xiaolincoding.com/redis/base/redis_interview.html)

---

> 📌 以上 5 道题目涵盖 Redis 核心知识点，建议配合《Redis 设计与实现》和小林 coding 图解系列深入理解。
