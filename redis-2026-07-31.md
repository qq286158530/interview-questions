# Redis 面试题（2026-07-31）

> 涵盖数据类型、持久化、集群、缓存等核心知识点

---

## 题目一：Redis 有哪些数据类型？分别适用于什么场景？

**参考答案：**

Redis 支持五种基本数据类型：

| 数据类型 | 可存储值 | 典型场景 |
|---------|---------|---------|
| **String** | 字符串、整数、浮点数 | 缓存、计数器、分布式锁、Session 存储 |
| **Hash** | 键值对集合（field-value） | 存储对象、购物车、商品信息 |
| **List** | 有序可重复元素列表（双向链表） | 消息队列、最新评论、粉丝列表 |
| **Set** | 无序不重复元素集合 | 标签系统、去重、好友关系、共同关注 |
| **Zset**（Sorted Set） | 有序不重复元素集合（带分数） | 排行榜、延迟队列、带权重的任务队列 |

此外 Redis 6.x+ 还支持 **Stream**（消息流）、**Bitmap**（位图）、**HyperLogLog**（基数统计）、**Geospatial**（地理位置）等扩展类型。

**追问：** String 类型的底层实现是什么？
> Redis 的 String 采用 **SDS（Simple Dynamic String）** 实现，相比 C 字符串，支持 O(1) 长度获取、自动扩容、避免缓冲区溢出、二进制安全。

**来源：** Redis 官方文档 —— Data Types；阿里云开发者社区《Redis 数据类型详解》

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？如何选择？

**参考答案：**

### RDB（Redis Database）

- **原理：** 定时将内存中的全量数据快照（Snapshot）写入二进制文件 `dump.rdb`
- **触发方式：**
  - 手动：`BGSAVE`（后台异步）、`SAVE`（同步，阻塞）
  - 自动：根据 `save` 配置规则（如 `save 900 1` 表示 900 秒内 ≥1 次写操作则触发）
- **优点：** 恢复速度快，适合做灾备、冷备
- **缺点：** 最后一次快照到故障之间可能有数据丢失；fork 子进程时内存消耗较大

### AOF（Append Only File）

- **原理：** 记录所有写操作命令，以追加方式写入 `appendonly.aof`
- **同步策略：**
  - `always` —— 每个写命令同步落盘（最安全，性能最差）
  - `everysec`（默认）—— 每秒同步一次
  - `no` —— 由 OS 决定何时同步
- **优点：** 数据安全性更高；AOF 重写（BGREWRITEAOF）可压缩文件
- **缺点：** 文件体积通常比 RDB 大；恢复速度比 RDB 慢

### 混合持久化（Redis 4.0+）

开启混合持久化后，AOF 重写时会先用 RDB 格式写入数据，再以 AOF 格式追加增量命令，兼顾两者优点。

### 如何选择？

- **仅做缓存：** 可以不开持久化
- **能容忍少量数据丢失 + 追求性能：** RDB
- **数据可靠性要求高：** AOF（或混合持久化）
- **最佳实践：** 两者都开，结合 `appendfsync everysec` + 合理配置 RDB 快照策略

**来源：** Redis 官方文档 —— Persistence；美团技术团队《Redis 持久化机制》

---

## 题目三：Redis 的过期 key 删除策略有哪些？内存淘汰策略有哪些？

**参考答案：**

### 过期 key 删除策略

| 策略 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **定时删除** | 设置定时器主动删除过期 key | 内存友好（过期即释放） | CPU 不友好（key 多时占用大量 CPU） |
| **惰性删除** | 访问 key 时才判断是否过期并删除 | CPU 友好 | 内存不友好（大量过期 key 占用内存） |
| **定期删除** | 每隔一段时间随机检查并删除过期 key | 平衡方案 | 实现复杂 |

**Redis 采用的是「惰性删除 + 定期删除」组合策略。**

### 内存淘汰策略（maxmemory-policy）

当 Redis 内存达到 `maxmemory` 上限时，通过以下策略腾出空间：

| 策略 | 行为 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 从已设置过期时间的 key 中淘汰最近最少使用的 |
| `allkeys-lru` | 从所有 key 中淘汰最近最少使用的 |
| `volatile-lfu` | 从已设置过期时间的 key 中淘汰使用频率最低的 |
| `allkeys-lfu` | 从所有 key 中淘汰使用频率最低的 |
| `volatile-random` | 从已设置过期时间的 key 中随机淘汰 |
| `allkeys-random` | 从所有 key 中随机淘汰 |
| `volatile-ttl` | 淘汰 TTL 最短的 key |

**实际建议：** 缓存场景常用 `allkeys-lru`；需要精确控制内存时结合监控动态调整。

**来源：** Redis 官方文档 —— expire & eviction；腾讯云开发者社区《Redis 内存管理》

---

## 题目四：Redis 主从复制原理是什么？哨兵（Sentinel）和集群（Cluster）有什么区别？

**参考答案：**

### 主从复制原理

1. **全量同步：** 从节点向主节点发送 `PSYNC ? -1`，主节点生成 RDB 快照发送给从节点，从节点加载
2. **增量同步（命令传播）：** 主节点将后续写命令实时以 **replication buffer** + **replication backlog** 环形缓冲区形式发给从节点
3. 从节点默认 **只读**，所有写操作只能在主节点执行
4. 支持 **链式主从**（从节点也可有自己的从节点）

### 哨兵（Sentinel）

- **作用：** 监控主从节点健康、自动故障转移（failover）、通知客户端新主节点地址
- **架构：** 哨兵本身是高可用集群（通常 3 节点以上），通过 Gossip 协议通信
- **适用场景：** 数据量不大（单分片 < 10G）、对一致性要求较高的主备场景
- **局限：** 每个分片只有一个主节点，哨兵不具备数据分片能力

### 集群（Cluster）

- **作用：** 数据分片（Slot 槽）+ 高可用
- **分片机制：** 16384 个 Slot，分布在多个主节点上；客户端请求自动路由到正确节点
- **高可用：** 每个主节点可配置从节点（replica），主节点故障时从节点自动升级
- **适用场景：** 数据量大、需要水平扩展的生产环境
- **局限：** 不支持跨 Slot 的事务/MULTI/EXEC；Slot 迁移期间部分请求可能失败

### 关键区别

| 对比项 | Sentinel | Cluster |
|-------|---------|---------|
| 数据分片 | 无 | 有（16384 Slot） |
| 写能力 | 仅主节点 | 每个分片主节点 |
| 水平扩展 | 需改代码/代理 | 官方支持 |
| 故障转移 | 哨兵投票决定 | 从节点自动晋升 |

**来源：** Redis 官方文档 —— Replication / Sentinel / Cluster；网易云课堂《Redis 高可用方案对比》

---

## 题目五：如何解决 Redis 缓存穿透、缓存击穿、缓存雪崩问题？

**参考答案：**

### 缓存穿透（查询不存在的数据）

**问题：** 查询一个不存在的数据，缓存未命中后直接打到数据库，若大量恶意请求则击垮数据库。

**解决方案：**
1. **布隆过滤器（BloomFilter）：** 在缓存层前加一个布隆过滤器，将所有存在的 key 存入，O(k) 判断是否可能存在，直接拦截不存在的 key
2. **缓存空值（null value）：** 即使查询结果为空，也缓存一个短 TTL 的空值（如 `"null"`），避免重复查库
3. **参数合法性校验：** 对请求参数做基础校验，过滤明显恶意的 id 格式

### 缓存击穿（热点 key 过期）

**问题：** 某个热点 key 过期瞬间，大量并发请求直接打穿到数据库。

**解决方案：**
1. **互斥锁（Mutex）：** 只允许一个线程从数据库加载数据并写回缓存，其他线程等待并从缓存获取（用 `SETNX` 实现）
2. **逻辑过期（不设置 TTL）：** 数据永不过期，但用额外字段记录"逻辑过期时间"，发现过期时用单独线程重建缓存
3. **热点数据永不过期：** 对极热点数据不设过期时间，后台定时更新

### 缓存雪崩（大量 key 同时过期）

**问题：** 大量 key 在同一时间过期，导致瞬间大量请求打到数据库。

**解决方案：**
1. **过期时间随机化：** 给 key 的 TTL 加上随机偏移量（如 `baseTTL + rand(0, 60s)`），避免集中过期
2. **多级缓存：** 本地缓存（Guava/Caffeine）+ Redis + 数据库，多层拦截
3. **服务降级 / 限流：** 缓存不可用时返回默认值或友好提示；用 Sentinel/Cluster 保证缓存高可用
4. **预热：** 系统启动或流量低峰时主动加载热点数据到缓存

### 总结对比

| 问题 | 核心原因 | 核心解决思路 |
|------|---------|------------|
| 穿透 | 查询不存在的数据 | 布隆过滤器 / 缓存空值 |
| 击穿 | 热点 key 过期瞬间并发 | 互斥锁 / 逻辑过期 |
| 雪崩 | 大量 key 同时过期 | 随机 TTL / 多级缓存 / 预热 |

**来源：** 掘金《Redis 缓存三大问题详解》；阿里云开发者社区《Redis 缓存设计与优化》

---

*本面试题由「面试题助手 Bot」整理生成，每日自动更新*
