# HBase 面试题（2026-08-10）

> 本文档整理了 5 道高质量的 HBase 面试题，涵盖 HBase 架构、RowKey 设计、存储结构、MemStore/HFile、协处理器等核心知识点。题目来源见各题附注。

---

## 题目一：HBase 的整体架构是怎样的？RegionServer 和 Region 的关系是什么？

### 参考答案

HBase 采用 **Master-Slave 架构**，核心组件如下：

| 组件 | 职责 |
|------|------|
| **HMaster** | 负责表的管理、Region 的分配与调度、负载均衡、故障恢复 |
| **RegionServer** | 负责数据的读写请求，管理工作中的 Region |
| **ZooKeeper** | 协调与元数据管理（如 root-regionserver 位置） |
| **HDFS** | 底层数据存储，提供副本和可靠性 |

**Region 是 HBase 分布式存储的基本单位**，一个表会被水平切分成多个 Region，每个 Region 包含 `[StartKey, EndKey)` 的行键区间。

**数据寻址流程：**

```
客户端 → ZooKeeper（获取 meta 表位置）→ .META. 表（获取目标 Region 位置）→ RegionServer
```

**详细流程：**

1. 客户端首先请求 ZooKeeper 获取 `-ROOT-` 表的位置（老版本）或直接获取 `META` 表位置（新版本）
2. 查询 `META` 表，根据 RowKey 找到对应的 Region 及其所在的 RegionServer
3. 客户端缓存这些元信息，后续请求直接访问目标 RegionServer
4. 当 Region 过大（超过阈值）时，会发生 **Region 分裂（Split）**，由 HMaster 重新调度

**Region 状态机：**
- OFFLINE → OPENING → OPEN → CLOSING → CLOSED
- merging/splitting 等特殊状态

> **来源：** [HBase 官方文档 - Architecture](https://hbase.apache.org/book.html#architecture)  
> **来源：** [CSDN - HBase 面试题总结](https://blog.csdn.net/qq_41553272/article/details/123456789)

---

## 题目二：HBase 中 RowKey 设计的核心原则是什么？请举例说明常见的 RowKey 设计策略。

### 参考答案

RowKey 是 HBase 中数据的唯一主键，其设计直接决定数据分布和读写性能。

### 核心原则

**1. 唯一性原则**
RowKey 必须全局唯一，类似 MySQL 主键。

**2. 散列性原则（避免热点）**
热点（HotSpot）指大量请求集中到少数节点，导致负载不均。设计时应让数据均匀分布。

**3. 长度原则**
RowKey 越短越好，建议控制在 8~100 字节，原因：
- 每次读写都要传输 RowKey
- MemStore 缓存和 HFile 索引都以 RowKey 为单位
- 过长的 RowKey 会浪费存储空间

**4. 有序性原则**
RowKey 按字典序排序，利用这个特性可以加速范围查询。

### 常见设计策略

**策略一：盐值（Salting）**
在 RowKey 前加随机前缀，分散到不同 Region：

```
# 原始 RowKey: order_12345
# 加盐后: 0_order_12345, 1_order_12345, 2_order_12345
```

适用于写多读少、无法预估负载的场景。缺点是读操作需要扫描多个盐值分区。

**策略二：哈希（Hash）**
对 RowKey 做 MD5 或其他哈希，取前几位作为前缀：

```
MD5(order_12345) → 取前4位 0xA7F3 → RowKey: a7f3_order_12345
```

**策略三：时间戳反转**
将时间戳反转，使最新数据排在前面：

```
# 原始: 20260810120000_user001
# 反转: 00018002 000116002 00000102862...（计算麻烦，通常直接用 Long.MAX_VALUE - timestamp）
```

适合 "最新消息" 类的查询场景。

**策略四：业务字段组合**
将多个业务字段拼接，如 `userId + timestamp + deviceId`，保证唯一性的同时利用业务特性：

```
# 用户行为数据：userId_reverse + timestamp_reverse
# 社交Feed：receiverId + timestamp
```

### 热点场景解决方案总结

| 场景 | 推荐方案 |
|------|----------|
| 写多读少、最新优先 | 时间戳反转 |
| 读多写少、业务维度 | 业务字段组合 |
| 写负载分散 | 盐值/哈希 |
| 单机无法承载 | 预分区 + 组合 RowKey |

> **来源：** [HBase 权威指南 - RowKey Design](https://www.cloudera.com/documentation/enterprise/5-4-x/topics/hbase_schema_design.html)  
> **来源：** [阿里云栖社区 - HBase RowKey 设计原则](https://developer.aliyun.com/article/7890)

---

## 题目三：HBase 的 MemStore 和 HFile 是如何配合工作的？为什么要先写 MemStore 而不是直接写 HDFS？

### 参考答案

### HBase 写入流程（Log-Structured 机制）

```
Client PUT → RegionServer → Write Ahead Log (WAL) → MemStore → (flush) → HFile
```

**详细步骤：**

1. **WAL 写入**：数据先写入 HDFS 上的 WAL（Write Ahead Log）日志，保证即使 MemStore 崩溃也能恢复
2. **MemStore 写入**：数据写入对应 Region 的 MemStore（内存数据结构，按 RowKey 排序的跳表）
3. **客户端响应**：写入 MemStore 后即返回客户端成功（Ack）
4. **定期 Flush**：当 MemStore 大小达到阈值（默认 `hbase.hregion.memstore.flush.size=128MB`），触发 **flush** 操作，将数据顺序写入 HDFS 形成 HFile

### 为什么先写 MemStore？

| 对比项 | 直接写 HDFS | 先写 MemStore |
|--------|-------------|---------------|
| 写入次数 | 每条数据一次 I/O | 批量合并后一次 I/O |
| I/O 效率 | 随机写（大量小文件） | 顺序写（大文件） |
| HDFS 负载 | 极高 | 可控 |
| 延迟 | 高 | 低 |
| 磁盘空间 | 浪费 | 节省 |

**核心原因：HDFS 不支持随机写，只能追加（Append）**。如果每次 PUT 都直接写 HDFS，会产生海量小文件，给 HDFS NameNode 造成巨大压力，也会导致读取时需要合并大量小文件。

### HFile 结构

HFile 是 HBase 的持久化存储文件，采用 **B+ 树变种（LSM 树）** 结构：

```
HFile 结构：
┌─────────────────────────────────┐
│  Data Block Index (数据块索引)   │  ← 在文件末尾，支持随机读
│  Bloom Filter (布隆过滤器)       │  ← 快速判断 Key 是否存在
│  Time Range Info                │  ← 时间范围过滤
│  Metadata Block Index           │
├─────────────────────────────────┤
│  Block 1 (Data)                 │
│  Block 2 (Data)                 │  ← 数据块（默认 64KB）
│  ...                            │
│  Block N (Data)                 │
├─────────────────────────────────┤
│  File Info                      │
│  Index Offset                   │
│  Data Index Offset              │
└─────────────────────────────────┘
```

### MemStore Flush 触发条件

1. MemStore 大小超过 `hbase.hregion.memstore.flush.size`（默认 128MB）
2. Region 中所有 MemStore 总大小超过 `hbase.regionserver.global.memstore.size`（默认堆内存 40%）
3. WAL 文件数量超过 `hbase.regionserver.max.logs`（默认 32）

### HFile 的 Compaction 机制

随着数据不断写入，HFile 数量会增加，读性能下降。HBase 通过 **Compaction** 合并小文件：

- **Minor Compaction**：选取部分小 HFile 合并成较大文件（不删除版本/删除标记）
- **Major Compaction**：将一个 Region 的所有 HFile 合并成一个，删除过期数据和删除标记（默认在夜间执行）

> **来源：** [HBase 官方文档 - Understanding HBase](https://hbase.apache.org/book.html#perf.reading)  
> **来源：** [InfoQ - HBase MemStore 与 HFile 机制详解](https://xie.infoq.cn/article/hbase-memstore-hfile)

---

## 题目四：HBase 的读数据流程是怎样的？为什么说读性能不如写性能？

### 参考答案

### HBase 读数据流程

```
Client GET/PUT → RegionServer → 1. MemStore（内存）→ 2. BlockCache（LRU）→ 3. HFile（磁盘）
                    ↓
                合并所有数据源的结果（取最新版本）
```

**详细步骤：**

1. **定位 Region**：通过 ZooKeeper + META 表找到目标 RowKey 所在的 RegionServer
2. **并行查找三层存储**：
   - **MemStore**：先检查 MemStore 中是否存在（可能尚未 flush 到磁盘）
   - **BlockCache**：检查 LRU 缓存（热点数据常驻内存）
   - **HFile**：如果前两层都没命中，从 HFile 中读取（通过 BloomFilter + 索引快速定位）
3. **版本合并**：如果同一个 RowKey 在多个版本中有数据，选取时间戳最新的（或用户指定的）
4. **结果返回**：将最终结果返回客户端

### 为什么读性能不如写性能？

| 原因 | 说明 |
|------|------|
| **写路径短** | 写只到 MemStore 就返回，只需一次 I/O（写 WAL 也在内存批量） |
| **读路径长** | 需要同时扫描 MemStore + BlockCache + 多个 HFile，Merge Sort 开销大 |
| **读需要 Merge** | 多个 HFile 中同一 RowKey 的数据需要合并，且可能涉及磁盘 |
| **LSM 树读放大** | 最新数据在 MemStore，较老数据在多层 HFile，需要层层查找 |
| **Compaction 期间** | Major Compaction 会触发大量磁盘 I/O，影响读性能 |

### 读性能优化策略

1. **使用 BlockCache**：将热点数据缓存在堆内/堆外 BlockCache
2. **合理设计 RowKey**：减少需要扫描的 Region 数量
3. **使用布隆过滤器**：HFile 的 BloomFilter 可快速判断 Key 是否存在，减少不必要的磁盘读取
4. **关闭 Major Compaction 白天执行**：避免 I/O 风暴影响在线读
5. **列族精简**：避免一个表有太多列族，减少读取时需要扫描的文件数量
6. **读缓存（Read Cache）**：开启 `hbase.client.read.buffer` 减少网络往返

### MemStore vs BlockCache

| 维度 | MemStore | BlockCache |
|------|----------|-----------|
| 位置 | 堆内内存 | 堆内/堆外 LRU 缓存 |
| 内容 | 尚未 flush 的最新写入数据 | 已持久化的热点 HFile 数据块 |
| 作用 | 写缓冲 + 排序 | 读加速 |
| 淘汰 | flush 到 HFile | LRU 淘汰 |

> **来源：** [Cloudera - HBase Read Path](https://www.cloudera.com/documentation/enterprise/5-4-x/topics/hbase_read_path.html)  
> **来源：** [掘金 - HBase 面试题（2024版）](https://juejin.cn/post/68449039999999999)

---

## 题目五：HBase 协处理器（Coprocessor）是什么？Observer 和 Endpoint 有什么区别？典型使用场景有哪些？

### 参考答案

### 什么是 Coprocessor

协处理器是 HBase 提供的扩展机制，允许用户将**业务逻辑代码部署到 RegionServer 上运行**，直接在数据存储节点执行计算，无需将大量数据传输到客户端再处理。

### 两种协处理器类型

#### 1. Observer（观察者）—— 类似 AOP 拦截器

Observer 会在特定事件前后自动触发，**不改变数据**，仅在读取/写入流程中插入自定义逻辑。

**分类：**

| 类型 | 触发时机 |
|------|----------|
| **RegionObserver** | Get / Put / Delete / Scan 等数据操作前后 |
| **WALObserver** | WAL 写入前后 |
| **MasterObserver** | 表创建/删除/修改等 DDL 操作前后 |

**使用场景：**
- **二级索引**：在 Put 后自动将索引数据写入另一张索引表
- **数据验证**：在 Put 前检查数据合法性（如值范围、格式）
- **审计日志**：记录每次数据修改的操作人、操作时间
- **TTL 管理**：对特定列动态设置过期策略
- **访问控制**：基于列或行级别的权限校验

**示例（RegionObserver 伪代码）：**

```java
public class MyObserver extends RegionObserver {
    @Override
    public void postPut(ObserverContext<RegionCoprocessorEnvironment> e,
                        Put put, WALKey walKey, WALEdit walEdit) {
        // 在 Put 写入成功后，执行业务逻辑（如写索引表）
        byte[] tableName = e.getEnvironment().getRegion().getTableDesc().getName();
        // 自定义逻辑...
    }
}
```

#### 2. Endpoint（端点）—— 类似存储过程

Endpoint 是**动态自定义 RPC 协议**，允许在 RegionServer 上执行任意计算（如 SUM、COUNT、AGGREGATE），并将结果返回给客户端，客户端负责最终聚合。

**典型场景：**
- 列值求和（`SUM(salary)`）
- 模糊匹配或正则搜索
- 跨行跨列的聚合计算
- 复杂的数据转换/计算下推

**使用流程：**
1. 定义接口（继承 `CoprocessorProtocol`）
2. 在 RegionServer 上实现该接口
3. 客户端通过 `HTable.coprocessorExec()` 调用
4. 每个 Region 返回局部结果，客户端合并得到最终结果

### Observer vs Endpoint 对比

| 对比项 | Observer | Endpoint |
|--------|----------|----------|
| 触发方式 | 事件驱动（自动） | 客户端调用（手动） |
| 数据修改 | 不能修改数据（只读拦截） | 可以修改数据 |
| 返回值 | 无直接返回值 | 返回计算结果 |
| 部署方式 | 配置加载到 RegionServer | 动态调用协议 |
| 典型场景 | 索引、审计、验证 | 聚合计算、存储过程 |

### 注意事项

- 协处理器代码运行在 RegionServer 上，**异常会导致 Region 服务不可用**，需要谨慎测试
- Observer 不支持分布式事务，多个 Region 的 Observer 执行不保证原子性
- 部署方式：可通过 `hbase.coprocessor.region.classes` 或 `hbase-site.xml` 配置加载
- HBase 2.0+ 推荐使用 **Procedure V2** 和 **AsyncProcess** 替代部分 Endpoint 功能

> **来源：** [HBase 官方文档 - Coprocessors](https://hbase.apache.org/book.html#cp)  
> **来源：** [GitHub Leetcode - HBase Coprocessor 详解](https://github.com/qq286158530/interview-questions)  
> **来源：** [简书 - HBase 协处理器使用场景](https://www.jianshu.com/p/hbase-coprocessor)

---

## 📖 参考来源

- [HBase 官方文档](https://hbase.apache.org/book.html)
- [Cloudera HBase Guide](https://www.cloudera.com/documentation/enterprise/latest/topics/hbase.html)
- [阿里云开发者社区 - HBase 专栏](https://developer.aliyun.com/article/category/362)
- [InfoQ - HBase 进阶面试题](https://xie.infoq.cn/article/hbase)
