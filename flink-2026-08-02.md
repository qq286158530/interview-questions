# Flink 面试题 · 2026-08-02

> 本系列面试题由 AI 助手整理，持续更新中  
> 仓库：https://github.com/qq286158530/interview-questions

---

## 题目一：Flink 的架构是怎样的？请描述 JobManager 与 TaskManager 的协作机制。

### 参考答案

Flink 采用**主从架构**，由 **JobManager**（主节点）和 **TaskManager**（从节点）组成。

**核心组件：**

| 组件 | 职责 |
|------|------|
| **JobManager** | 负责接收 Flink 作业（Job），将 Job 拆分成 Task，调度到 TaskManager 上执行，协调 Checkpoint、故障恢复等 |
| **TaskManager** | 实际执行 Task 的工作节点，每个 TaskManager 是一个 JVM 进程，拥有若干个**Slot**，每个 Slot 可执行一个 Task（或一个子任务） |
| **ResourceManager** | 管理 TaskManager 的 Slot、申请和释放资源，与 YARN/K8s/Mesos 等资源调度框架交互 |
| **Dispatcher** | 接收用户提交的 Job，为每个 Job 启动一个新的 JobMaster，并提供 REST 接口 |

**协作流程：**

1. 客户端（Client）将用户编写的 Flink 作业（StreamGraph → JobGraph）提交给 **Dispatcher** 或 **JobManager**。
2. **JobManager** 根据 JobGraph 创建 **ExecutionGraph**（执行图），并将 Task 调度到拥有足够 Slot 的 **TaskManager** 上。
3. **TaskManager** 启动多个 **Slot**，每个 Slot 执行一个或多个子任务（SubTask）。
4. Task 之间通过**流数据传递**（Streams）进行数据交换，下游 Task 从上游 Task 所在的 TaskManager 拉取数据。
5. JobManager 持续监控 Task 的执行状态，出现故障时触发重启或故障转移（Failover）。

**Slot 与并行度：**
- 每个 TaskManager 的 Slot 数量表示该节点的并发执行能力。
- 作业的**并行度（Parallelism）**决定了一个算子（Operator）有多少个并行实例（SubTask）。
- Slot 是资源分配的最小单位，一个 Slot 可以执行一个完整的 Pipeline（从某个输入流到所有下游算子）。

---

## 题目二：Flink 的 Watermark 机制是什么？它是如何处理乱序数据的？

### 参考答案

**Watermark（水位线）是 Flink 处理**事件时间（Event Time）**乱序问题的核心机制。**

### 为什么需要 Watermark？

在基于事件时间的流处理中，数据到达的顺序可能与事件发生的顺序不一致（网络延迟、乱序等）。如果单纯靠数据到达的顺序来决定窗口触发时机，就会丢失那些"迟到"但本应属于该窗口的数据。

### Watermark 的本质

> **Watermark(T) 表示：所有时间戳 ≤ T 的数据都已经到达（或者说，不会再有早于 T 的数据了）。**

Watermark 是一个带有时间戳的特殊记录，本质上是一个**单调递增的时间戳断言**。

### 窗口与 Watermark 的关系

以 **TumblingEventTimeWindow** 为例：

```
窗口 [10:00, 10:10) 的关闭条件：
-  Watermark >= 10:10  （所有事件时间 < 10:10 的数据都已到达）
-  或窗口超时被强制触发
```

### 乱序处理流程

1. **Watermark 生成**：通常由数据源（Source）算子或自定义 Watermark 生成器产生。常见策略：
   - **固定延迟（FixedWatermarkStrategy）**：周期性地生成 `currentMaxTimestamp - allowedLateness`
   - **自定义 WatermarkGenerator**：根据业务逻辑（如遇到某类特殊数据）动态生成

2. **Watermark 传播**：Watermark 沿着算子链**向下游广播**，算子收到所有输入流的 Watermark 后，取**最小值**作为自己的 Watermark（因为下游算子依赖所有上游输入）。

3. **窗口触发**：当 `Watermark >= 窗口结束时间` 时，窗口被触发计算。

### allowedLateness（允许迟到）

```java
WatermarkStrategy
    .<T>forBoundedOutOfOrderness(Duration.ofSeconds(10))
    .withTimestampAssigner(...)
```

- 设置 `allowedLateness > 0` 后，窗口即使触发后也不会立即删除，会**多等一段时间**（默认也是通过 Watermark 推动）。
- 迟到数据（时间戳在窗口内但晚于 Watermark）会被**重新发送到下游**，触发窗口的增量计算或使用 `ProcessWindowFunction` 的 `context.global()` 输出到侧输出流。

### 典型问题

- **Watermark 过于激进**：导致大量有效数据被当作迟到数据丢弃 → 应合理估算最大乱序程度。
- **多输入流 Watermark 对齐**：当一个算子有多个输入时，需要等待所有输入流的 Watermark 都推进才能推进自己的 Watermark，可能造成**饥饿**问题（Flink 1.15+ 引入 `WatermarkAlignment` 解决这个问题）。

---

## 题目三：Flink 的 Checkpoint 与 Savepoint 有什么区别？如何实现 Exactly-Once 语义？

### 参考答案

### Checkpoint vs Savepoint 对比

| 维度 | Checkpoint | Savepoint |
|------|-----------|-----------|
| **触发方式** | Flink 自动周期性触发（由 JobManager 驱动） | 用户手动触发（通过 CLI 或 API） |
| **用途** | 故障恢复，保证作业持续运行时的状态安全 | 计划性备份（如版本升级、集群迁移） |
| **依赖** | 依赖 CheckpointCoordinator，定期做快照 | 独立于作业运行，可跨作业恢复 |
| **格式** | 特定于作业的二进制格式 | 标准化的外部存储格式（较通用） |
| **删除** | 作业取消后自动清理 | 需要手动删除 |
| **性能影响** | 低（有异步快照+增量 Checkpoint 优化） | 无（手动触发，一次性） |

### Checkpoint 的工作原理

Flink 采用 **Chandy-Lamport 分布式快照算法**的变体（**Asynchronous Barrier Snapshotting, ABS**）实现 Checkpoint：

1. **CheckpointCoordinator**（JobManager 中）定期向所有 Source 算子发送 `CheckpointCoordinatorMessages`（检查点触发消息）。
2. Source 算子收到消息后：
   - 发出 **Barrier（屏障）** 插入到数据流中
   - 快照自己的状态（偏移量等），写入状态后端（如 RocksDB）
   - 将 Barrier 向下游广播
3. 中间算子收到所有输入的 Barrier 后：
   - 对齐输入（等待所有输入流的 Barrier 都到达）
   - 快照自己的状态
   - 将 Barrier 向下游传递
4. Sink 算子完成快照后，向 CheckpointCoordinator 确认完成。
5. 当所有算子都确认后，Checkpoint 完成。

> **为什么需要 Barrier 对齐？**  
> 为了保证"一致性"——确保以同一个数据时间点为分界线来划分状态快照，避免不同算子快照的时间点不一致导致数据重复或丢失。

### Exactly-Once 语义如何实现？

Exactly-Once = **每条数据"恰好一次"被处理**（包括输出）。

Flink 实现 Exactly-Once 需要三个条件：

1. **Source 端**：能够重放（Replay）数据
   - 如 Kafka Source，可以从指定的 Offset 重新消费
   - 快照中保存了当前读取的 Offset，故障恢复时从快照点重新消费

2. **Flink 内部**：通过 Checkpoint 保证算子状态的 Exactly-Once
   - 算子状态在 Barrier 对齐后统一快照，所有算子的状态对应同一时刻

3. **Sink 端**：支持幂等写入或事务写入
   - **幂等 Sink**：如 Redis / 关系型数据库（重复写入结果相同）
   - **事务 Sink**：
     - **预写日志（Write-Ahead-Log, WAL）**：先写日志，再处理，输出时再确认
     - **两阶段提交（2PC）**：Flink 1.4+ 的 `TwoPhaseCommitSinkFunction`，与 Checkpoint 绑定，Checkpoint 完成时真正提交事务。典型实现：`FlinkKafkaProducer`、`IcebergSink`

### 实际配置示例

```java
StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
// 开启 Checkpoint，间隔 10 秒
env.enableCheckpointing(10_000, CheckpointingMode.EXACTLY_ONCE);
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(5000);
env.getCheckpointConfig().setCheckpointTimeout(60000);
env.getCheckpointConfig().setTolerableCheckpointFailureNumber(3);
// 状态后端：RocksDB（增量 Checkpoint）
env.setStateBackend(new EmbeddedRocksDBStateBackend());
```

---

## 题目四：Flink 的窗口函数有哪几种？它们各自的用法和区别是什么？

### 参考答案

Flink 支持两大类窗口：**按键分组窗口（Keyed Windows）** 和 **非按键分组窗口（Non-Keyed Windows）**。在非按键分组下，整个算子只有一个并发，窗口计算是全局的。

### 按窗口机制分类

#### 1. 时间窗口（Time Windows）

| 类型 | 说明 | 场景 |
|------|------|------|
| **TumblingEventTimeWindow** | 固定大小、不重叠、无间隙 | 固定周期的聚合，如"每分钟UV" |
| **SlidingEventTimeWindow** | 固定大小、可重叠，由 `size` 和 `slide` 两个参数控制 | 移动平均、持续监控（如"最近5分钟的平均值，每1分钟更新"） |
| **SessionEventTimeWindow** | 会话窗口，间隙超过 `gap` 时开启新窗口 | 用户行为分析（用户活跃session） |
| **GlobalWindow** | 全局窗口，所有相同 key 的数据进入同一个窗口，需要自定义触发器 | 特殊业务逻辑，不常用 |

#### 2. 计数窗口（Count Windows）

- **TumblingCountWindow**：每 N 条数据触发一次
- **SlidingCountWindow**：每 M 条数据滑动一次，每次对最近 N 条计算

> ⚠️ **计数窗口的缺点**：在分布式环境下，不同算子实例的计数是独立的，可能出现"数据倾斜"导致窗口触发时机不一致，**生产环境建议优先使用时间窗口**。

### 按函数类型分类

| 函数类型 | 特点 | 适用场景 |
|---------|------|---------|
| **ReduceFunction** | 增量聚合，状态最小，性能最优 | 简单的 SUM、MAX、MIN 等 |
| **AggregateFunction** | 增量聚合，支持返回不同类型的结果 | 需要对窗口内数据做复杂聚合（求平均、去重计数） |
| **ProcessWindowFunction** | 全量窗口函数，窗口触发前收集所有元素到迭代器 | 需要遍历窗口内所有数据、访问窗口元信息（window start/end） |
| **WindowFunction** | 类似 ProcessWindowFunction，但功能更弱 | 兼容旧版 API |

### 组合使用（推荐）

```java
stream
    .keyBy(...)
    .window(SlidingEventTimeWindow.of(Time.minutes(5), Time.minutes(1)))
    .aggregate(
        new MyAggregateFunction(),   // 增量聚合，状态小
        new MyProcessWindowFunction() // 全量处理，可访问窗口元信息
    );
```

> **最佳实践**：用 `AggregateFunction + ProcessWindowFunction` 组合，前者负责增量聚合减少状态存储，后者负责在窗口触发时输出完整的上下文信息。

### 代码示例

```java
// 会话窗口，间隙超过 10 秒开启新会话
DataStream<Tuple2<String, Long>> windowed = events
    .keyBy(e -> e.f0)
    .window(EventTimeSessionWindow.withGap(Time.seconds(10)))
    .sum(1);

// 滑动窗口，每 5 分钟计算一次最近 15 分钟的数据
DataStream<MyResult> result = stream
    .keyBy(r -> r.key)
    .window(SlidingEventTimeWindow.of(Time.minutes(15), Time.minutes(5)))
    .process(new MyProcessWindowFunction());
```

---

## 题目五：Flink 的状态管理机制是怎样的？状态后端（State Backend）有哪些？各自适用场景是什么？

### 参考答案

### 什么是 Flink 状态？

**有状态流处理（Stateful Stream Processing）** 是 Flink 区别于无状态计算的核心能力。状态是算子在处理过程中需要"记住"的信息，比如：

- 聚合计算中的中间结果（SUM、COUNT 的当前值）
- 窗口内缓存的数据
- 数据处理中需要跨事件记忆的信息（如上一条数据的关联字段）

Flink 将状态分为两类：

| 类型 | 说明 | 示例 |
|------|------|------|
| **Keyed State** | 按 Key 分组，作用域是当前处理的 Key | `ValueState`、`MapState` |
| **Operator State**（非 Keyed） | 作用域是整个算子实例，所有 Key 共享 | `ListState`、`UnionListState`、`BroadcastState` |

### Keyed State 的主要类型

```
┌─────────────────────────────────────────────┐
│              Keyed State                     │
├──────────┬──────────────────────────────────┤
│ValueState<T>      存一个值                  │
│ListState<T>       存列表                     │
│MapState<K, V>     存 Map                     │
│ReducingState<T>   自动做 reduce              │
│AggregatingState<I, O>  自动做 aggregate      │
└─────────────────────────────────────────────┘
```

### 状态后端（State Backend）

Flink 有三种状态后端，决定了状态的存储位置和序列化方式：

| 状态后端 | 存储位置 | 状态大小 | 性能 | 适用场景 |
|---------|---------|---------|------|---------|
| **HashMapStateBackend** | JVM Heap（内存） | 受限于 JobManager 内存 | 最快（纯内存读写） | 小状态、低延迟场景（状态很小的 Job） |
| **EmbeddedRocksDBStateBackend** | RocksDB（本地 RocksDB 数据库） | 可利用 TaskManager 磁盘，理论上无限 | 较慢（序列化/反序列化开销）但支持增量 Checkpoint | 大状态场景（如大窗口、复杂聚合、JOIN） |
| **BasicRocksDBStateBackend** | 同 EmbeddedRocksDBStateBackend（已废弃，合并到 Embedded） | 同上 | 同上 | 历史版本兼容 |

### RocksDB 的工作原理

RocksDB 是一个嵌入式的 KV 数据库（基于 LSM-Tree），Flink 将状态序列化后写入 RocksDB：

- **读写**：从 RocksDB 读取时需要反序列化，写入会序列化后追加到 MemTable，刷盘后形成 SSTable。
- **增量 Checkpoint**：RocksDB 支持增量快照，只上传新产生的 SSTable 文件，大幅减少 Checkpoint 数据量。
- **TTL**：支持设置状态过期时间（`StateTtlConfig`），自动清理过期状态。

### 状态过期（TTL）

```java
StateTtlConfig ttlConfig = StateTtlConfig
    .newBuilder(Time.days(7))
    .setUpdateType(StateTtlConfig.UpdateType.OnReadAndWrite) // 每次读写都更新TTL
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired) // 过期数据不可见
    .build();

ValueStateDescriptor<String> descriptor = new ValueStateDescriptor<>("myState", String.class);
descriptor.enableTimeToLive(ttlConfig);
```

### 最佳实践

1. **状态大的作业（GB 级）** → 选 **RocksDB**，配合增量 Checkpoint
2. **状态小的作业（MB 级）** → 选 **HashMapStateBackend**，延迟更低
3. **尽量使用值状态**，避免使用 List/Map 状态（序列化开销大）
4. **合理设置 TTL**，避免状态无限膨胀
5. **监控状态大小**，通过 Flink Web UI 或 Metrics（Gauge: `currentXXXStateSize`）观察

---

> 📌 **面试题整理 by AI 助手**  
> 📅 2026-08-02  
> 🌐 https://github.com/qq286158530/interview-questions
