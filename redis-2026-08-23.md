# Redis 常见面试题 · 2026-08-23

> 本文档整理了 5 道高质量 Redis 面试题，涵盖数据类型、持久化、线程模型、集群与缓存等核心知识点。  
> 来源：[小林coding - 图解 Redis](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目一：Redis 有哪些数据类型？各自的适用场景是什么？

### 答案

Redis 提供了丰富的数据类型，常见的有 **5 种基础数据类型**和**4 种扩展数据类型**。

#### 5 种基础数据类型

| 数据类型 | 典型应用场景 |
|---------|------------|
| **String** | 缓存对象、常规计数、分布式锁、共享 session 信息 |
| **Hash** | 缓存对象、购物车 |
| **List** | 消息队列（生产者需自行实现全局唯一 ID，不支持消费组） |
| **Set** | 聚合计算（并集/交集/差集），如点赞、共同关注、抽奖活动 |
| **Zset** | 排序场景，如排行榜、电话姓名排序 |

#### 4 种扩展数据类型（Redis 2.2+）

| 数据类型 | 版本 | 典型应用场景 |
|---------|------|------------|
| **BitMap** | 2.2 | 二值状态统计：签到、判断用户登录状态、连续签到用户总数 |
| **HyperLogLog** | 2.8 | 海量数据基数统计，如百万级网页 UV 计数 |
| **GEO** | 3.2 | 存储地理位置信息，如滴滴叫车 |
| **Stream** | 5.0 | 消息队列，支持自动生成全局唯一消息 ID 和消费组形式消费 |

#### 底层数据结构

- **String → SDS（简单动态字符串）**：二进制安全，O(1) 获取长度，防缓冲区溢出
- **List → quicklist**（Redis 3.2+）：双向链表 + 压缩列表的组合，替代了单独的压缩列表/双向链表
- **Hash → 哈希表 / 压缩列表**（元素 < 512 且每个 < 64 字节时用压缩列表）
- **Set → 哈希表 / 整数集合**（全是整数且 < 512 个时用整数集合）
- **ZSet → 跳表 / 压缩列表**（元素 < 128 且每个 < 64 字节时用压缩列表）

> 📎 来源：<https://xiaolincoding.com/redis/base/redis_interview.html>

---

## 题目二：Redis 如何实现数据持久化？RDB 和 AOF 有什么区别？

### 答案

Redis 共有 **三种** 数据持久化方式：

#### 1. AOF 日志（Append Only File）

每执行一条写操作命令，就把该命令以追加方式写入一个文件。Redis 重启时通过逐一执行日志中的命令来恢复数据。

**写回策略**（`appendfsync` 配置项）：

| 策略 | 含义 | 优点 | 缺点 |
|------|------|------|------|
| `Always` | 每次写命令执行完后同步写回硬盘 | 数据不丢失 | I/O 开销大 |
| `Everysec`（默认） | 每次写命令后先写入内核缓冲区，每秒刷盘一次 | 性能与安全性平衡 | 可能丢失约 1 秒数据 |
| `No` | 由操作系统决定何时刷盘 | 性能最好 | 数据丢失不可控 |

**AOF 重写机制**：当 AOF 文件过大时，后台子进程 `bgrewriteaof` 读取当前数据库中所有键值对，用一条命令记录到新的 AOF 文件，从而压缩文件体积。

#### 2. RDB 快照（Redis Database）

将某一时刻的内存数据以**二进制格式**写入磁盘。恢复速度比 AOF 快，因为直接读入内存即可。

- `save` 命令：在主线程执行，会阻塞
- `bgsave` 命令：创建子进程生成 RDB，避免阻塞
- Redis 也支持按配置自动触发（`save 900 1` 等条件）

RDB 采用 **写时复制（COW）** 技术：fork 出子进程后，父子进程共享物理内存，子进程以只读方式扫描数据并写入 RDB，主线程修改数据时复制副本，不影响快照生成。

#### 3. 混合持久化（Redis 4.0+）

结合 RDB 和 AOF 的优点：
- AOF 重写时，先以 RDB 格式写入全量数据
- 随后增量操作命令以 AOF 格式追加
- 重启恢复时，先加载 RDB 部分（快速），再加载后续 AOF 增量（减少数据丢失）

| 对比项 | RDB | AOF | 混合持久化 |
|--------|-----|-----|-----------|
| 文件体积 | 小（二进制） | 大（文本命令） | 中 |
| 恢复速度 | 快 | 慢 | 快 |
| 数据安全性 | 丢数据多 | 可配置（always/每秒） | 丢数据少 |
| 性能影响 | fork 消耗 | 持续写开销 | 中 |

> 📎 来源：<https://xiaolincoding.com/redis/base/redis_interview.html>

---

## 题目三：Redis 是单线程吗？为什么单线程的 Redis 还能这么快？

### 答案

#### Redis 单线程指的是什么

Redis 单线程指的是 **「接收客户端请求 → 解析请求 → 执行数据读写 → 发送响应」** 这一核心过程由主线程完成。Redis 程序本身并非完全单线程：

- **Redis 2.6+** 启动 2 个后台线程（BIO）：处理关闭文件、AOF 刷盘
- **Redis 4.0+** 新增第 3 个后台线程 `lazyfree`：异步释放内存（如 `unlink` 命令）

#### Redis 为什么这么快

1. **内存操作**：大部分操作在内存中完成，高效数据结构，瓶颈在内存/网络而非 CPU
2. **单线程无竞争**：避免了多线程切换开销和死锁问题
3. **I/O 多路复用**：基于 `epoll` 机制，一个线程处理多个 Socket 连接，主线程通过 `epoll_wait` 监听事件，收到请求才处理，实现了高性能并发

#### Redis 6.0 多线程引入

Redis 6.0 引入了 **I/O 多线程**，用于分担网络 I/O 压力（仅针对 write client socket，不改变命令执行的单线程本质）。通过配置 `io-threads-do-reads=yes` 可开启读请求多线程。

> 📎 来源：<https://xiaolincoding.com/redis/base/redis_interview.html>

---

## 题目四：Redis 如何实现服务高可用？什么是集群脑裂，如何处理？

### 答案

#### 高可用架构

Redis 高可用有三种常见模式：

**① 主从复制（一主多从）**
- 主节点可读写，从节点只读并同步主节点写操作
- 同步是**异步**的，主节点不等从节点执行完就返回客户端，无法保证强一致性

**② 哨兵模式（Sentinel）**
- 监控主从节点状态，自动完成主从故障转移
- 解决了"主节点挂了需要手动恢复"的问题

**③ 切片集群（Redis Cluster）**
- 使用 **16384 个哈希槽** 分布数据，通过 `CRC16(key) % 16384` 决定槽位
- 每个节点存储部分槽位数据，支持水平扩展

#### 集群脑裂（Split-Brain）问题

**什么是脑裂**：主节点网络故障（但与客户端连接正常），哨兵误判主节点宕机并在从节点中选出新主节点，集群出现两个主节点。网络恢复后，旧主节点降为从节点并执行全量同步，之前客户端写入的数据会丢失。

**解决方案**：配置 `min-slaves-to-write` 和 `min-slaves-max-lag`

```conf
min-slaves-to-write 1      # 主节点至少要有 1 个从节点
min-slaves-max-lag 12      # 复制延迟不能超过 12 秒
```

原理：若从库数量不足或延迟过高，主节点拒绝接收写请求，即使旧主节点假故障也无法写入，避免了脑裂期间的数据写入，从而防止数据丢失。

> 📎 来源：<https://xiaolincoding.com/redis/base/redis_interview.html>

---

## 题目五：如何避免缓存雪崩、击穿、穿透？Redis 内存淘汰策略有哪些？

### 答案

#### 三个缓存经典问题

**① 缓存雪崩**：大量缓存数据同一时间过期，导致大量请求直接打到数据库。  
应对：
- 将过期时间随机打散（如基础时间 + 1~10 分钟随机值）
- 设置缓存不过期，由后台异步更新

**② 缓存击穿**：某个热点 key 过期瞬间，大量并发请求直接打到数据库。  
应对：
- 互斥锁（`setNX`）保证只有一个线程重建缓存
- 热点数据不过期，由后台提前更新

**③ 缓存穿透**：请求的数据既不在缓存也不在数据库（如恶意攻击或业务误删）。  
应对：
- 入口校验（参数合法性判断）
- 设置空值或默认值（短期缓存）
- **布隆过滤器**：写入数据库时在布隆过滤器标记，查询时先检查布隆过滤器

#### 内存淘汰策略（8 种，`maxmemory-policy` 配置）

| 类别 | 策略 | 含义 |
|------|------|------|
| 不淘汰 | `noeviction`（默认） | 内存超限直接返回错误 |
| 在过期键中淘汰 | `volatile-random` | 随机淘汰有过期时间的 key |
| | `volatile-ttl` | 优先淘汰更早过期的 key |
| | `volatile-lru` | 淘汰有过期时间的 key 中最久未使用的 |
| | `volatile-lfu`（4.0+） | 淘汰有过期时间的 key 中使用最少的 |
| 在所有键中淘汰 | `allkeys-random` | 随机淘汰任意 key |
| | `allkeys-lru` | 淘汰整个库中最久未使用的 key |
| | `allkeys-lfu`（4.0+） | 淘汰整个库中使用最少的 key |

#### LRU vs LFU

- **LRU（Least Recently Used）**：淘汰最近最少使用。Redis 使用近似 LRU（随机采样 5 个，取最久未用的），避免维护完整链表的性能开销。无法解决缓存污染问题。
- **LFU（Least Frequently Used）**：淘汰使用频率最低的。Redis 对象头的 `lru` 字段分为高 16bit（访问时间戳）和低 8bit（访问计数器），解决了偶尔访问一次就长期驻留缓存的问题。

> 📎 来源：<https://xiaolincoding.com/redis/base/redis_interview.html>

---

*本文档由自动化脚本生成，整理自网络优质技术资源。*
