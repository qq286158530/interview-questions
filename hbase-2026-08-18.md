# 程序员面试题 - 2026-08-18

## 🔥 HBase 大数据面试题

---

### 题目1：HBase 的整体架构是怎样的？RegionServer 和 HMaster 的职责是什么？

【答案】

**HBase 架构组件**：

```
┌─────────────────────────────────────────────────────────┐
│                      Client                              │
│                  (读写请求)                               │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│                   Zookeeper                              │
│            (协调服务 / 元数据管理)                         │
└─────────────────────┬───────────────────────────────────┘
                      │
        ┌─────────────┴─────────────┐
        ▼                           ▼
┌───────────────┐          ┌───────────────┐
│  HMaster      │          │ RegionServer   │
│ (主节点)       │          │ (从节点)        │
│ - 元数据管理   │          │ - 数据读写     │
│ - Region分配  │          │ - MemStore    │
│ - 负载均衡    │          │ - HFile存储    │
│ - 故障转移    │          │ - compaction   │
└───────────────┘          └───────────────┘
                                  │
                                  ▼
                         ┌───────────────┐
                         │   HDFS        │
                         │ (底层存储)     │
                         └───────────────┘
```

**HMaster 职责**：
1. **元数据管理**：管理 Table 的创建、删除、修改等元数据操作
2. **Region 分配**：将 Region 分配给 RegionServer，负责负载均衡
3. **故障转移**：监控 RegionServer 状态，宕机时重新分配其上的 Region
4. **DDL 操作**：处理 namespace、table 的创建与修改

**RegionServer 职责**：
1. **数据读写**：处理客户端的读写请求，直接与 HDFS 交互
2. **MemStore 管理**：写入数据先到 MemStore，满了再刷写成 HFile
3. **StoreFile 管理**：管理 Store 下的 HFile，进行 Compaction
4. **Region 拆分**：当 Region 过大时，执行自动拆分

**工作流程**：
1. Client 请求 Zookeeper 获取元数据（-ROOT- → .META.）
2. 从 .META. 表获取 Region 位置
3. 直接与目标 RegionServer 通信读写数据
4. HMaster 负责集群管理，但不参与实际数据读写

📖 来源：https://hbase.apache.org/book.html

---分割线---

### 题目2：HBase 的 RowKey 设计原则有哪些？如何避免热点问题？

【答案】

**RowKey 设计原则**：

**1. 长度原则**
- RowKey 最大支持 64KB，建议越短越好（不超过 100 字节）
- 原因：RowKey 会冗余存储在 MemStore、HFile、缓存等各处
- 经验：使用定长的业务 ID 或 hash 值，避免超长字符串

**2. 唯一性原则**
- RowKey 必须唯一标识一行数据
- 类似 MySQL 主键，是数据的唯一标识

**3. 散列原则（避免热点）**
- 热点问题：大量请求集中在少数几个 RegionServer 上
- 原因：相邻 RowKey 的数据会写入同一个 Region

**避免热点的策略**：

| 策略 | 方法 | 适用场景 |
|------|------|----------|
| **加盐（Salting）** | RowKey 前加随机字节 | 写入分散，但读取困难 |
| **Hash 打散** | `MD5(rowkey).substring(0, N) + original_key` | 读写均衡 |
| **反转（Reversing）** | 反转手机号、邮箱等固定前缀 | 前缀规律但后缀散列 |
| **时间戳反转** | `Long.MAX_VALUE - timestamp + key` | 时序数据，最近的先被读到 |

**示例**：
```java
// 原始：user_1234567890 (热点)
// 加盐：0_user_1234567890, 1_user_1234567890...
// Hash：hash(user_1234567890).substring(0,4) + user_1234567890

// 业务建议：用户表用 userId hash，订单表用 userId + 时间戳组合
```

**4. 业务查询驱动原则**
- RowKey 设计应服务于主要查询场景
- 常见模式：
  - `userId`：按用户查询
  - `userId + orderId`：用户+订单联合查询
  - `orderId + timestamp`：订单时序查询

📖 来源：https://cloud.google.com/bigtable/docs/schema-design

---分割线---

### 题目3：HBase 的存储结构是怎样的？MemStore 和 HFile 的作用是什么？

【答案】

**HBase 存储层次**：

```
Table
  └── Region (按 RowKey 范围划分)
        └── Store (每个 Column Family 一个 Store)
              ├── MemStore (写缓存，按 RowKey+Column 排序)
              └── StoreFile (HFile) (磁盘文件)
                    └── HFile (多层索引：BloomFilter + BlockIndex)
```

**MemStore（写缓存）**：

1. **作用**：先将数据写入 MemStore，按 Column Family 分开存储
2. **排序**：数据在 MemStore 中按 RowKey + Column 排序（LSM 树结构）
3. **刷盘时机**：
   - MemStore 达到 `hbase.hregion.memstore.flush.size`（默认 128MB）
   - RegionServer 的 MemStore 总量超过限制
   - 定时刷新（`hbase.regionserver.optionalcacheflushinterval`，默认 3600000ms）
   - WAL 日志切换时

4. **为什么先写 MemStore**：
   - 内存写入速度远快于磁盘随机写入
   - 批量排序后顺序写入 HFile，效率高

**HFile（StoreFile）结构**：

```
┌────────────────────────────────────┐
│  HFile                              │
│  ┌──────────────────────────────┐  │
│  │  Trailer (固定长度元数据)      │  │
│  │  - 平均键值大小                │  │
│  │  - 键值对数量                  │  │
│  │  - 数据块偏移量等              │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │  Data Block Index (块索引)    │  │
│  │  - 每个数据块的起始键值         │  │
│  │  - 块位置偏移量                │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │  Bloom Filter (布隆过滤器)      │  │
│  │  - 判断键值是否可能在某个块     │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │  Data Blocks (数据块)          │  │
│  │  - 每个块默认 64KB             │  │
│  │  - 块内键值有序                │  │
│  └──────────────────────────────┘  │
└────────────────────────────────────┘
```

**HFile 特点**：
- 键值对按字典序存储，支持范围扫描
- 多层索引结构，快速定位数据块
- Bloom Filter 快速判断键值是否存在，减少不必要的磁盘 IO
- 数据块大小可配置（`hfile.block.cache.size`）

**读写流程**：

**写流程**：
```
Client → WAL → MemStore → (刷盘) → HFile → (Compaction) → 大HFile
```

**读流程**：
```
Client → BlockCache → MemStore → StoreFile(HFile) → 合并返回
```

📖 来源：https://hbase.apache.org/book.html#arch.regionserver.memstore

---分割线---

### 题目4：HBase 的 Compaction 机制是什么？Minor 和 Major Compaction 有何区别？

【答案】

**Compaction 机制**：

HBase 为了提升读性能，会定期将多个小 HFile 合并成一个大 HFile，减少文件数量，降低读放大问题。

**Minor Compaction（小型 Compaction）**：

| 特性 | 说明 |
|------|------|
| **触发时机** | 后台线程周期性检查（默认每 10s） |
| **合并范围** | 选取部分小文件合并（不含被删除/过期的数据） |
| **文件选择** | 通常选择满足条件的最小几个文件 |
| **结果** | 少量 HFile → 稍大的 HFile |
| **执行速度** | 快，只读少量文件 |

**Major Compaction（大型 Compaction）**：

| 特性 | 说明 |
|------|------|
| **触发时机** | 后台线程或手动触发（默认每 7 天） |
| **合并范围** | 一个 Store 下的所有 HFile 合并 |
| **数据清理** | 清理被删除的数据（墓碑标记）、TTL 过期数据 |
| **结果** | 多个 HFile → 一个大的 HFile |
| **执行速度** | 慢，需要读取重写所有数据 |
| **注意** | 会占用大量 IO，建议在业务低峰期执行 |

**合并流程对比**：

```
Minor Compaction:
  HFile1(1MB) + HFile2(2MB) + HFile3(3MB) → HFile4(6MB)

Major Compaction:
  HFile1 + HFile2 + HFile3 + ... + HFileN → NEW_HFile(包含所有数据)
  (期间会清理: 删除标记的数据、过期数据、版本数超过设定的数据)
```

**删除数据的处理**：
- HBase 删除不是立即删除，而是写入 **墓碑标记（Tombstone Marker）**
- Major Compaction 时才真正删除：墓碑标记 + 过期数据 → 被真正移除
- 这就是为什么 Snapshot 恢复能看到已"删除"的数据

**调优参数**：
```xml
<!-- StoreFile 数量阈值，超过则触发 compaction -->
<property>
  <name>hbase.hstore.compaction.min</name>
  <value>3</value>
</property>

<!-- Major Compaction 周期，0 表示禁用自动 major compaction -->
<property>
  <name>hbase.hregion.majorcompaction</name>
  <value>604800000</value>  <!-- 7天 -->
</property>
```

📖 来源：https://hbase.apache.org/book.html#compaction

---分割线---

### 题目5：HBase 协处理器（Coprocessor）是什么？Observer 和 Endpoint 有何区别？

【答案】

**协处理器是什么**：

协处理器是 HBase 提供的扩展机制，允许用户将自定义代码部署到 RegionServer 上，在数据写入/读取时自动执行，实现类似数据库触发器/存储过程的功能。

**两种协处理器类型**：

| 类型 | 作用 | 类似概念 | 使用场景 |
|------|------|----------|----------|
| **Observer** | 事件钩子，在特定事件触发时执行 | 数据库触发器 | 权限校验、二级索引、数据验证 |
| **Endpoint** | 动态 RPC 端点，可在服务端执行计算 | 存储过程 | 聚合计算、自定义 filter |

**Observer 协处理器**：

三种 Observer，覆盖不同生命周期：

```java
// 1. RegionObserver - 数据级别钩子（最常用）
public class MyRegionObserver extends BaseRegionObserver {
    // 读取前拦截（可修改读请求参数）
    @Override
    public void preGet(final ObserverContext<RegionCoprocessorEnvironment> e,
                       final Get get, final List<Cell> result) {
        // 例如：添加权限校验
        if (!hasPermission(get.getRow())) {
            throw new AccessDeniedException("无权限读取");
        }
    }
    
    // 写入前拦截
    @Override
    public void prePut(final ObserverContext<RegionCoprocessorEnvironment> e,
                      final Put put, final WALEdit edit, final Durability durability) {
        // 例如：自动补充字段、数据格式校验
        validatePut(put);
    }
    
    // 删除前拦截
    @Override
    public void preDelete(final ObserverContext<RegionCoprocessorEnvironment> e,
                         final Delete delete, final WALEdit edit, final Durability durability) {
        // 记录删除日志或实现软删除
    }
    
    // 写入后触发（可用于异步构建二级索引）
    @Override
    public void postPut(final ObserverContext<RegionCoprocessorEnvironment> e,
                      final Put put, final WALEdit edit, final Durability durability) {
        // 异步更新索引表
        updateSecondaryIndex(put);
    }
}

// 2. MasterObserver - DDL 操作钩子
// 3. WALObserver - WAL 相关钩子
```

**Endpoint 协处理器**：

Endpoint 是自定义的 RPC 服务端点，客户端可以调用并在 RegionServer 上执行：

```java
// 1. 定义 Endpoint 接口
public interface ColumnFamilyAggregationProtocol extends CoprocessorProtocol {
    // 定义服务端方法
    int getRowCount(byte[] family) throws IOException;
    int getRowCount(byte[] family, byte[] qualifier) throws IOException;
}

// 2. 实现接口
public class RowCountEndpoint extends BaseEndpointCoprocessor 
    implements ColumnFamilyAggregationProtocol {
    
    @Override
    public int getRowCount(byte[] family) throws IOException {
        Scan scan = new Scan();
        scan.addFamily(family);
        int count = 0;
        try (RegionScanner scanner = getEnvironment().getRegion().getScanner(scan)) {
            List<Cell> results = new ArrayList<>();
            while (scanner.next(results)) {
                count += results.size();
                results.clear();
            }
        }
        return count;
    }
}

// 3. 客户端调用
HTableInterface table = ...;
CoprocessorRpcChannel channel = table.coprocessorService(rowKey);
ColumnFamilyAggregationProtocol protocol = 
    (ColumnFamilyAggregationProtocol) channel.callMethod("getRowCount");
int count = protocol.getRowCount(Bytes.toBytes("cf1"));
```

**加载方式**：

| 方式 | 配置位置 | 作用域 | 适用场景 |
|------|----------|--------|----------|
| **静态加载** | hbase-site.xml | 整个集群 | 系统级协处理器 |
| **动态加载** | Shell/API | 单表 | 业务级协处理器 |

```bash
# 动态加载示例
hbase> disable 'myTable'
hbase> alter 'myTable', METHOD => 'table_att', 'coprocessor' => 
      'hdfs://path/to/coprocessor.jar|com.example.MyObserver|1001|arg1=1,arg2=2'
hbase> enable 'myTable'
```

**典型应用场景**：
- **二级索引**：写入主表时同步更新索引表
- **数据验证**：写入前校验数据格式和完整性
- **权限控制**：基于行级别的访问控制
- **聚合计算**：在服务端执行 COUNT、SUM、AVG 等聚合，避免数据传输

📖 来源：https://hbase.apache.org/book.html#cp
