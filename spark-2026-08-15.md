# Spark 面试题集锦

> 📅 日期：2026-08-15  
> 🏷️ 标签：Spark / 大数据 / 面试题  
> 💡 来源：综合整理自 MindMajix、DataFlair 等技术社区

---

## 题目一：Spark 架构与核心组件

### 📖 问题
请描述 Apache Spark 的整体架构，并说明 Driver、Executor、Cluster Manager、Worker Node 等核心组件的作用及它们之间的关系。

### ✅ 答案

Apache Spark 采用 **主从（Master-Slave）架构**，整体由以下核心组件构成：

| 组件 | 角色 | 说明 |
|------|------|------|
| **Driver（驱动程序）** | 主节点 | 运行 `main()` 方法，创建 `SparkContext`，负责将任务拆分为 Stage 和 Task，分发到 Executor |
| **Executor（执行器）** | 从节点 | 运行在 Worker Node 上，负责执行分发的 Task，向 Driver 汇报结果 |
| **Cluster Manager（集群管理器）** | 资源调度 | 管理集群资源，负责在 Worker Node 上启动 Executor。常见实现：Standalone、YARN、Mesos、Kubernetes |
| **Worker Node** | 实际工作节点 | 集群中运行 Executor 的节点，负责存储 RDD partitions |

**工作流程：**
1. 用户提交 Spark 应用，Driver 创建一个 `SparkContext`
2. Driver 向 Cluster Manager 申请资源（Executor）
3. Cluster Manager 在 Worker Node 上启动 Executor 进程
4. Driver 将应用代码（Jar/PySpark 脚本）发送给 Executor
5. Driver 将 Job 拆分为多个 Stage，每个 Stage 包含多个 Task，分发到各 Executor 并行执行
6. Executor 执行完 Task 后将结果返回给 Driver

**数据本地性（Data Locality）：** Spark 尽可能将 Task 调度到数据所在的节点，减少网络传输。级别依次为：`PROCESS_LOCAL` → `NODE_LOCAL` → `RACK_LOCAL`

> 📚 来源：[MindMajix - Apache Spark Interview Questions](https://mindmajix.com/apache-spark-interview-questions)

---

## 题目二：RDD 与 DataFrame / Dataset 的区别

### 📖 问题
RDD、DataFrame 和 Dataset 是 Spark 中的三种核心数据抽象，它们之间有什么区别？各自适用于什么场景？

### ✅ 答案

| 特性 | RDD | DataFrame | Dataset |
|------|-----|----------|---------|
| **数据结构** | 底层 API，无结构化 | 类 SQL 表结构，有列名和类型 | 强类型 JVM 对象，兼具 RDD 和 DataFrame 优点 |
| **API 风格** | 函数式编程 | 声明式 SQL / 链式调用 | 混合（DataFrame API + Lambda） |
| **编译时类型安全** | ✅ 支持 | ❌ 仅运行时校验 | ✅ 支持（强类型） |
| **Catalyst 优化** | ❌ 不经过优化器 | ✅ 自动优化（谓词下推、列裁剪） | ✅ 支持 |
| **内存管理** | 自行管理 | Tungsten 优化，统一内存管理 | Tungsten 优化 |
| **序列化** | Java/Kryo | Tungsten 二进制编码 | Encoders |
| **适用场景** | 低级操作、非结构化数据 | 结构化/半结构化数据，SQL 查询 | 需要类型安全且希望享受优化的高级开发 |

**核心区别详解：**

1. **DataFrame = RDD[Row] + Schema**：DataFrame 在逻辑上等同于一张关系型数据库表，拥有明确的列名和列类型。Spark 的 **Catalyst 优化器** 会自动分析 Logical Plan 和 Physical Plan，进行谓词下推（Predicate Pushdown）、列裁剪（Column Pruning）、常量替换等优化。

2. **Dataset = DataFrame + Type Safety**：Dataset 是 DataFrame 的扩展，提供 compile-time 类型安全。在 Scala/Java 中，Dataset[T] 的每一行是一个强类型对象 T。

3. **编码器（Encoders）**：Dataset 使用 Encoders 将 JVM 对象序列化为二进制 Tungsten 格式存储，比传统 Java 序列化高效得多。

```scala
// RDD 示例
val rdd = sc.textFile("data.txt").map(_.split(",")).filter(_.length > 3)

// DataFrame 示例
val df = spark.read.csv("data.csv")
df.filter(df("age") > 25).groupBy("dept").count()

// Dataset 示例（ Scala）
case class Person(name: String, age: Int)
val ds = spark.read.json("people.json").as[Person]
ds.filter(_.age > 25).groupBy("dept").count()
```

**选择建议：**
- 多数 ETL 和 BI 场景优先选 **DataFrame**（性能最佳）
- 需要类型安全的复杂业务逻辑选 **Dataset**
- 底层数据处理、无结构化数据选 **RDD**

> 📚 来源：[DataFlair - Top 100 Apache Spark Interview Questions](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目三：Spark Shuffle 机制详解

### 📖 问题
什么是 Shuffle？为什么 Shuffle 是 Spark 作业中最昂贵的操作？如何优化 Shuffle 性能？

### ✅ 答案

**什么是 Shuffle？**

Shuffle 是 Spark 中将数据**按照 Key 进行重新分区**的操作。当一个 RDD  transformation 需要跨分区聚合数据时（如 `groupByKey`、`reduceByKey`、`join`），必须将具有相同 Key 的数据拉到同一个分区，这就产生了 Shuffle。

**Shuffle 的执行过程：**

```
[Map Stage] → [Shuffle Write (写入本地磁盘文件)] → [Shuffle Read (从其他节点拉取)] → [Reduce Stage]
```

1. **Shuffle Write**：Map 任务结束后，将数据按目标分区写入本地磁盘，生成 Shuffle 文件
2. **Shuffle Read**：Reduce 任务启动后，从所有 Map 节点拉取属于自己的 Shuffle 文件数据
3. **合并与聚合**：拉取的数据按 Key 排序（可选）后交给 Reduce 函数处理

**Shuffle 瓶颈原因：**

- **磁盘 I/O**：Shuffle 数据必须落盘（不能纯内存），涉及大量序列化/反序列化
- **网络 I/O**：数据跨节点传输，带宽消耗大
- **内存压力**：Map 端缓冲区 + Reduce 端缓冲区 + 排序内存，容易 OOM
- **磁盘随机写入**：小文件合并写入（Hadoop 中的 merge sort 过程）

**优化策略：**

| 策略 | 说明 |
|------|------|
| 减少 Shuffle 数据量 | 尽量用 `reduceByKey` 代替 `groupByKey`（局部聚合 vs 全量拉取） |
| 增加并行度 | 提高 `spark.sql.shuffle.partitions`（默认200）或 RDD 分区数 |
| 使用广播变量 | 小表 `join` 时用广播避免 Shuffle（`broadcast(spark.table)`） |
| 避免不必要的数据排序 | `sortByKey` 仅在需要时使用 |
| 使用 Kryo 序列化 | `spark.serializer=org.apache.spark.serializer.KryoSerializer` |
| 合理设置缓冲区 | `spark.shuffle.file.buffer`（默认 32KB），增大可减少磁盘读写次数 |
| 开启钨丝计划 | `spark.sql.tungsten.enabled=true`（默认开启）优化二进制编码 |

**经典面试点：`reduceByKey` vs `groupByKey`**
```scala
// ❌ 性能差：groupByKey 全量拉取，所有数据经过网络
rdd.groupByKey()

// ✅ 性能好：reduceByKey 先局部聚合，再 shuffle 少量结果
rdd.reduceByKey(_ + _)
```

> 📚 来源：[Scaler Topics - Shuffle in Spark](https://www.scaler.com/topics/spark/shuffle-in-spark/)

---

## 题目四：Spark 内存管理机制

### 📖 问题
Spark 的内存管理模型是怎样的？请说明 Unified Memory（统一内存）和 Static Memory（静态内存）的区别，以及如何调优 Spark 内存。

### ✅ 答案

**Spark 内存模型（基于堆内内存）：**

Spark 1.6 之前采用静态内存管理，之后引入**统一内存管理（Unified Memory）**，将内存划分为两大区域：

```
┌─────────────────────────────────────────────┐
│              Execution Memory                │
│    (Shuffle、Sort、Hash Join 等计算操作)      │
├──────────────────────┬──────────────────────┤
│                      │                      │
│    Storage Memory    │    User Memory        │
│  (RDD缓存、广播变量)  │  (用户数据结构、元数据) │
│                      │                      │
└──────────────────────┴──────────────────────┘
```

**三大内存区域详解：**

| 区域 | 用途 | 配置参数 | 说明 |
|------|------|----------|------|
| **Storage Memory** | 缓存 RDD（`persist`/`cache`）、广播变量 | `spark.memory.storageFraction`（默认 0.5） | 用于在内存中存储数据，避免重复计算 |
| **Execution Memory** | Shuffle 排序、Hash Join、Array 缓冲区 | 与 Storage 共用统一池 | 执行时临时占用，可抢占 Storage 空间 |
| **User Memory** | 用户自定义数据结构、元信息、Spark 内部元数据 | 固定 = `heap * (1 - spark.memory.fraction)` ≈ 40% | 不可被 Spark 抢占 |

**Unified Memory vs Static Memory：**

- **静态内存**：Storage 和 Execution 内存严格划分，互不借用，容易造成资源浪费
- **统一内存**：Storage 和 Execution 共享一个内存池，**可相互借用**：
  - Execution 空间不足时，可占用 Storage（Storage 需时会重新申请）
  - Storage 空间不足时，**不会被 Execution 抢占**（保证已缓存数据不被驱逐）

**内存管理关键参数调优：**

```bash
# 统一内存管理（默认开启，1.6+）
--conf spark.memory.fraction=0.6        # 执行内存占堆的比例（含 User Memory）
--conf spark.memory.storageFraction=0.5 # Storage 占统一内存池的比例

# 旧版静态内存管理（1.5及以前）
--conf spark.shuffle.memoryFraction=0.2
--conf spark.storage.memoryFraction=0.5

# 堆外内存（off-heap，Tungsten 使用）
--conf spark.memory.offHeap.enabled=true
--conf spark.memory.offHeap.size=4g

# 序列化
--conf spark.serializer=org.apache.spark.serializer.KryoSerializer
```

**内存溢出（OOM）排查思路：**
1. 检查是否存在 `groupByKey` 或 `join` 导致数据倾斜
2. 增大分区数，减少每分区数据量
3. 使用 `persist(LEVEL=MEMORY_AND_DISK)` 将溢出的数据写入磁盘
4. 广播小表，减少 Shuffle 数据量
5. 增加 `spark.executor.memory`

> 📚 来源：[DataFlair - Spark Interview Questions #30-34](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目五：广播变量与累加器

### 📖 问题
什么是广播变量（Broadcast Variable）和累加器（Accumulator）？它们分别在什么场景下使用？有哪些注意事项？

### ✅ 答案

**广播变量（Broadcast Variable）：**

广播变量是**只读的、缓存在每个 Executor 节点内存中的变量**。当 Task 需要使用同一份只读数据时（如配置表、维度表），广播变量可以避免该数据在每个 Task 中重复传输。

```python
# PySpark 示例
broadcast_var = sc.broadcast({"banned_words": ["xxx", "yyy"]})

def filter_words(line):
    words = line.split()
    # 广播变量 .value 获取值，无需序列化传输
    return [w for w in words if w not in broadcast_var.value["banned_words"]]

result = rdd.map(filter_words)
```

**广播变量的优势：**
- 减少网络传输：每个 Executor 只传输一次，而非每个 Task 传输一次
- 节省内存：Executor 内存中只有一份副本
- 适用场景：小表 join（广播小表 vs 大表）、大维度表查找、配置参数分发

**累加器（Accumulator）：**

累加器是**可写的、分布式共享计数器**，用于在 Task 运行时安全地聚合数据（类似 Hadoop Counter）。

```python
# PySpark 示例：统计无效行数
invalid_counter = sc.accumulator(0)

def check_row(line):
    global invalid_counter
    if not is_valid(line):
        invalid_counter.add(1)  # 只允许累加，不允许读取
    return line

rdd.map(check_row).collect()
print("Invalid rows:", invalid_counter.value)  # 仅 driver 可读
```

**累加器的特性：**
- **Task 只写不读**：Task 只能 `add()`，不能读取值（避免不一致）
- **Driver 只读**：只有 Driver 可以读取最终值
- **支持自定义类型**：可定义 `AccumulatorV2` 实现任意类型的累加器
- **容错性**：任务失败重试时，累加器会重新累加

**广播变量 vs 累加器对比：**

| 特性 | 广播变量 | 累加器 |
|------|----------|--------|
| **读写权限** | Executor 只读 | Task 只写，Driver 只读 |
| **用途** | 分发只读配置/维度表 | 聚合计数/求和 |
| **容错** | 无需容错（只读） | 任务失败重试时自动补偿 |
| **性能影响** | 减少网络传输，提升性能 | 几乎无性能影响 |
| **注意** | 过大广播变量会撑爆 Executor 内存 | 不要在 action 内部多次引用（重复累加） |

**常见踩坑点：**
- ❌ 在 transformation（如 `map`）内部调用 `accumulator.value`（会读到旧值）
- ❌ 广播过大的变量（超过 Executor 内存会 OOM）
- ❌ 在 `foreachPartition` 外部修改外部变量（不是线程安全）

> 📚 来源：[MindMajix - Apache Spark Interview Q&A](https://mindmajix.com/apache-spark-interview-questions)  
> 📚 来源：[DataFlair - Shared Variables](https://data-flair.training/blogs/spark-interview-questions/)

---

## 📝 学习建议

1. **动手实践**：每个知识点最好有对应的代码实验（PySpark / Scala）
2. **理解源码**：有余力可阅读 Spark 核心源码（DAGScheduler、TaskScheduler、ShuffleManager）
3. **对比学习**：Hadoop MapReduce vs Spark、DAG vs Lineage、Static vs Unified Memory
4. **关注版本差异**：Spark 2.x → 3.x 有较大变化（如 Adaptive Query Execution）

---

*本面试题集由自动脚本整理生成，每日更新*  
*GitHub 仓库：https://github.com/qq286158530/interview-questions*
