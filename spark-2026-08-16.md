# Spark 面试题 — 2026-08-16

> 涵盖 Spark 架构、RDD、DataFrame/DataSet、Shuffle、内存管理、广播变量等核心知识点。

---

## 题目一：Spark 任务调度流程是怎样的？请详细描述从提交 Spark 作业到任务执行完成的完整流程。

### 参考答案

Spark 任务的完整调度流程分为以下几个层次：

**1. 作业（Job）层面**
- 用户通过 `spark-submit` 提交应用，Driver 启动并创建 `SparkContext`
- 用户的 main() 方法中每遇到一个 `Action`（如 `collect()`、`count()`），会生成一个 Job
- 每个 Job 会构建一个 **DAG（Directed Acyclic Graph）**，描述 RDD 之间的依赖关系

**2. Stage 划分**
- DAGScheduler 根据 RDD 之间的依赖关系（宽依赖/窄依赖）将 Job 划分为多个 Stage
- **窄依赖**（one-to-one）：可以流水线执行，包含在一个 Stage 内
- **宽依赖**（shuffle）：需要 Shuffle，必须划分为不同的 Stage
- 最后一个 Stage 产生 Result，从后往前回溯划分

**3. TaskSet 与 Task 提交**
- 每个 Stage 生成一个 TaskSet（一组 Task），提交给 TaskScheduler
- Task 分为 **ShuffleMapTask**（需要 Shuffle）和 **ResultTask**（最终结果）
- TaskScheduler 将 Task 调度到 Executor 上执行

**4. Task 执行**
- Executor 接收 Task，启动线程池执行
- Task 执行完成后，ShuffleMapTask 会将数据写入本地磁盘的 Shuffle 文件
- ResultTask 返回结果给 Driver

**5. 失败重试与数据本地性**
- 如果 Task 失败，TaskScheduler 会重新提交该 Task（最多重试 4 次）
- 调度时优先考虑 **数据本地性**（PROCESS_LOCAL > NODE_LOCAL > RACK_LOCAL）

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  User Code  │ ──▶ │   Driver    │ ──▶ │  DAGScheduler│
│ spark-submit│     │(SparkContext)│     │ 划分Stage   │
└─────────────┘     └─────────────┘     └─────────────┘
                                              │
                                              ▼
                    ┌─────────────┐     ┌─────────────┐
                    │ TaskScheduler│ ◀── │  TaskSet    │
                    │ 调度Task    │     │ (一组Task) │
                    └─────────────┘     └─────────────┘
                           │
            ┌──────────────┼──────────────┐
            ▼              ▼              ▼
      ┌──────────┐  ┌──────────┐  ┌──────────┐
      │Executor 1│  │Executor 2│  │Executor 3│
      │ Task     │  │ Task     │  │ Task     │
      └──────────┘  └──────────┘  └──────────┘
```

---

## 题目二：RDD 的血缘机制（Lineage）是什么？为什么 Spark 要采用惰性计算？它在容错上是如何实现的？

### 参考答案

**血缘机制（Lineage）**

RDD 在创建时并不真正存储数据，而是记录了它的**血统（Lineage）**——即从源头数据集到当前 RDD 的完整转换链。每个 RDD 包含以下信息：
- 指向父 RDD 的指针
- 父 RDD 到当前 RDD 的转换函数（Transformation）

例如：`rdd3 = rdd1.map(...).filter(...).reduceByKey(...)`
其 Lineage 为：`rdd3 <- filter <- map <- rdd1`

**惰性计算（Lazy Evaluation）**

- Spark 采用**惰性求值**，只有遇到 **Action**（如 `collect()`、`count()`、`saveAsTextFile()`）时才会真正触发计算
- 在此之前，所有 Transformation 只是记录在 DAG 中，形成执行计划
- 惰性计算的好处：
  1. **减少中间结果传输**：通过整体优化后再执行
  2. **节省内存**：不需要存储中间结果
  3. **简化编程模型**：用户只需描述数据转换逻辑

**容错机制**

RDD 的容错基于 **Lineage 重算**：
- 每个 RDD 知道如何从父 RDD 重新计算得到自己
- 当某个 Partition 丢失时，Spark 根据 Lineage 从头重算该 Partition，而不是复制整个数据集
- 窄依赖的 RDD 重算代价小（只需重算父 Partition）；宽依赖重算代价大（需要重算所有父 Partition，这就是为什么 Spark 尽量把窄依赖放在一个 Stage 内）

**与传统分布式框架的区别**：MapReduce 的中间结果落盘，而 Spark 的 RDD 除非 Shuffle 落盘否则都在内存中，并通过 Lineage 实现高效容错。

---

## 题目三：Spark Shuffle 的原理是什么？为什么 Shuffle 是性能瓶颈？如何优化 Shuffle 性能？

### 参考答案

**Shuffle 的原理**

Shuffle 是将数据按照 Key 进行重新分区的过程，介于多个 Stage 之间。举例：`groupByKey()`、`reduceByKey()`、`join()` 等操作都会触发 Shuffle。

**写端（Shuffle Write）**
- 当 ShuffleMapTask 执行完时，需要将数据写入本地磁盘
- Spark 根据分区数（如 `spark.sql.shuffle.partitions = 200`）将数据写入不同的文件
- 每个文件对应一个 Reduce Task，数据按 Key 的 Hash 值路由到不同文件
- 写端会先将数据写入内存缓冲区（`bufferSize = 32KB`），超过阈值时溢写磁盘

**读端（Shuffle Fetch）**
- 每个 Reduce Task 需要从所有 Map Task 所在节点拉取属于它的分区数据
- 通过 HTTP/Netty 获取远程数据，拉取的数据先放入内存缓冲区
- 如果内存不足，数据会溢写到磁盘

```
  Shuffle Write                     Shuffle Fetch
  ┌──────────────┐                ┌──────────────┐
  │ Map Task     │ ── 按Hash路由 ──▶ │ Reduce Task  │
  │ (写入分区文件) │                │ (拉取数据)    │
  └──────────────┘                └──────────────┘
        │                               ▲
        │        Shuffle Files          │
        └───────────────────────────────┘
```

**为什么是性能瓶颈**

1. **大量磁盘 I/O**：数据需要写磁盘再读磁盘，磁盘读写速度远低于内存
2. **网络 I/O**：数据需要在节点间传输，涉及跨机架甚至跨机房
3. **内存压力**：Shuffle 数据可能撑爆内存，导致溢写，进一步降低性能
4. **小文件问题**：大量分区产生大量小文件，元数据管理压力大

**优化方案**

| 优化项 | 说明 |
|--------|------|
| `spark.sql.shuffle.partitions` | 适当增大分区数（避免数据倾斜，默认 200） |
| `spark.shuffle.file.buffer` | 增大写缓冲区（默认 32KB，可调至 1MB） |
| `spark.reducer.maxSizeInFlight` | 增大读缓冲区，减少网络往返次数 |
| 使用 `reduceByKey` 代替 `groupByKey` | `reduceByKey` 有 map-side 预聚合，减少数据量 |
| 广播小表 | `join` 时小表广播可避免 Shuffle |
| 开启 `spark.shuffle.compress` | 压缩 Shuffle 数据（`spark.io.compression.codec`） |
| 使用 Kryo 序列化 | `spark.serializer = org.apache.spark.serializer.KryoSerializer` |

---

## 题目四：Spark 的内存管理模型是怎样的？请解释堆内内存（On-Heap）和堆外内存（Off-Heap）的区别，以及如何调优内存使用。

### 参考答案

**统一内存管理（Spark 1.6+）**

Spark 2.x 起采用统一内存管理，内存被划分为两大区域：**执行内存（Execution Memory）** 和 **存储内存（Storage Memory）**，二者可以动态借用。

```
┌─────────────────────────────────────────────────────┐
│                   堆内内存 (On-Heap)                 │
│  ┌──────────────────┬──────────────────────────┐    │
│  │   Execution      │      Storage             │    │
│  │   (Shuffle/Join  │      (RDD Cache/         │    │
│  │   排序等)        │      Broadcast)          │    │
│  │   ─────────────── │◀── 动态借用 ──▶───────────│    │
│  └──────────────────┴──────────────────────────┘    │
│  ┌────────────────────────────────────────────┐     │
│  │            用户代码内存 (User Memory)        │     │
│  └────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────┘
```

**内存区域详解**

| 区域 | 用途 | 配置参数 | 默认占比 |
|------|------|---------|---------|
| **Storage Memory** | 缓存 RDD、Broadcast 变量 | `spark.memory.fraction` | 0.45 |
| **Execution Memory** | Shuffle 排序、HashJoin、HashMap | `spark.memory.fraction` | 0.45 |
| **User Memory** | 用户数据结构、元数据 | 无（= 1 - spark.memory.fraction） | 0.55 |
| **系统保留** | JVM 内部开销 | 固定 | 300MB |

**堆外内存（Off-Heap）**

- 通过 `spark.memory.offHeap.enabled = true` 开启
- 使用 `spark.memory.offHeap.size` 指定大小（默认 0）
- 主要用于：
  - Tachyon（堆外存储）
  - Netty 零拷贝网络传输
  - Kryo 序列化（可选堆外）
- 优点：避免 GC 开销，适合大内存场景

**调优建议**

1. **数据倾斜场景**：增大分区数，减少单个 Partition 数据量
2. **大量 Shuffle**：增大 `spark.shuffle.file.buffer`（如 256KB）和 `spark.reducer.maxSizeInFlight`（如 96MB）
3. **缓存大量数据**：增大 `spark.memory.storageFraction`（默认 0.5，即 Storage 至少保留 50%）
4. **避免 OOM**：
   - `spark.driver.memory` / `spark.executor.memory` 不要设太大，留足 OS 内存
   - 监控 `spark.memory.fraction`（默认 0.6，即 60% 用于 Execution + Storage）
5. **广播大变量**：小表 Broadcast 避免 Shuffle，广播变量存储在 Storage Memory

**常用内存配置参数**

```bash
spark-submit \
  --conf spark.memory.fraction=0.6 \
  --conf spark.memory.storageFraction=0.5 \
  --conf spark.executor.memory=4g \
  --conf spark.shuffle.file.buffer=256kb \
  --conf spark.reducer.maxSizeInFlight=96m
```

---

## 题目五：广播变量（Broadcast）的原理是什么？它解决了什么问题？使用广播变量有哪些注意事项？

### 参考答案

**为什么需要广播变量**

在分布式计算中，如果每个 Task 都持有一份相同的数据副本，会造成极大的内存浪费和网络开销。例如：在 `join` 操作中广播小表，或在 Filter 中广播过滤规则列表。

**广播变量的原理**

```
Driver                                    Executors
  │                                          │
  │  1. driver.collect() 收集数据             │
  │  ◀─────────────────────────────────────  │
  │                                          │
  │  2. 创建 Broadcast 对象，生成 broadcastId  │
  │  ──────────────────────────────────────▶ │
  │  (不立即发送数据)                          │
  │                                          │
  │  3. 首次 Task 访问时，BlockManager         │
  │     从 Driver 或其他 Executor 获取数据     │
  │  ◀─────────────────────────────────────  │
  │                                          │
  │  4. 数据缓存在 Executor 本地，后续 Task    │
  │     直接从内存读取，不再重复传输            │
  └──────────────────────────────────────────┘
```

**核心机制**

1. **只读语义**：广播变量一旦创建，Executor 上的所有 Task 只能读取，不能修改
2. **高效传输**：通过 `TorrentBroadcast`（默认策略），数据分块传输，Executor 之间可以互相下载，减少 Driver 压力
3. **存储位置**：广播数据优先存在 Executor 的 MemoryStore 中（Storage Memory），如果太大则落盘
4. **容错**：某个 Executor 丢失广播数据时，可从其他 Executor 重新获取，不需要 Driver 重新发送

**代码示例**

```scala
// 定义广播变量（在 Driver 端）
val broadcastList = sc.broadcast(List("apple", "banana", "orange"))

// 在 Executor 端（所有 Task 共享同一份数据）
val filtered = rdd.filter(x => broadcastList.value.contains(x))
```

```python
# PySpark
broadcast_list = sc.broadcast(["apple", "banana", "orange"])
filtered = rdd.filter(lambda x: x in broadcast_list.value)
```

**注意事项**

| 注意点 | 说明 |
|--------|------|
| **只读不可修改** | 广播变量是只读的，在 Task 中修改不会影响其他 Task |
| **大小要适中** | 太大（>1GB）的广播变量会撑爆 Storage Memory，导致频繁溢写 |
| **Driver 端不可修改** | Driver 端调用 `unpersist()` 删除广播变量，但已运行的 Task 不受影响 |
| **广播小表 vs 大表** | 小表广播可加速 Join；大表建议还是走 Shuffle |
| **使用场景** | 过滤规则列表、维度表映射、配置参数、机器学习特征表 |
| **手动清理** | `broadcast.unpersist()` 可删除 Executor 上的缓存，下次再使用时重新广播 |
| **序列化方式** | 广播变量使用 Kryo 序列化，需确保数据类可序列化（Serializable） |

**典型使用场景**

```scala
// 场景1：广播过滤列表
val validIds = sc.broadcast(Set(1001, 1002, 1003))
val filteredRDD = rdd.filter(r => validIds.value.contains(r.id))

// 场景2：广播维度表
val dimTable = sc.broadcast(dimensionData.collectAsMap())
val joined = factRDD.mapPartitions(iter => {
  iter.map(row => (row.id, dimTable.value.getOrElse(row.id, defaultRow)))
})

// 场景3：广播配置
val conf = sc.broadcast(Map("env" -> "prod", "version" -> "2.4"))
```

---

> 📚 以上题目均基于 Spark 核心原理整理，适用于大数据工程师 / Spark 开发工程师岗位面试准备。祝面试顺利！
