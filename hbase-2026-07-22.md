# HBase 面试题 - 2026年7月22日

> 本文档整理了5道高质量的HBase面试题，涵盖核心知识点，帮助面试者快速掌握HBase关键技术。

---

## 题目一：HBase 的数据模型是怎样的？请详细说明 Row、Column Family、Qualifier、Timestamp 和 Version 的概念。

### 参考答案

### 1. HBase 数据模型概述

HBase 是一个 **稀疏的、分布式的、持久化的、多维有序映射**，其数据模型基于 BigTable 理论，核心结构如下：

```
Table
  └── Row Key (唯一主键，按字典序排列)
        └── Columns (动态列，支持稀疏扩展)
              └── Column Family:Qualifier (列族:列限定符)
                    └── Timestamp (时间戳，支持多版本)
                          └── Value (字节数组)
```

### 2. 核心概念详解

#### RowKey（行键）
- 每张表的主键，唯一标识一行数据
- 类型为字节数组（byte[]），长度建议 8~1024 字节
- **按字典序存储和排序**，这是 HBase 数据分布和查询的基础
- 设计原则：唯一性、散列性、长度控制

#### Column Family（列族）
- 列的分组，每个列族对应 HBase 存储中的一个 **Store**
- 建表时必须指定，使用 `:` 分隔，如 `info:name`
- **同一列族的所有列数据存储在同一个 HFile 中**
- 建议：将**访问频率相同、冷热属性相同的列放在同一列族**
- 每个列族有独立的配置（如压缩算法、存储策略）

#### Qualifier（列限定符）
- 列族下的具体列名，类型为字节数组
- 可以动态添加，无需提前定义（稀疏特性）
- 示例：`info:age`、`info:gender`、`orders:count`

#### Timestamp（时间戳）
- 数据的版本标识，默认64位长整型（当前时间毫秒）
- 每次写入时自动生成，也可由客户端指定
- **支持多版本**：同一 RowKey + Column 下可存储多个版本
- 默认保留 **3 个版本**，可通过 `VERSIONS` 参数配置

#### Version（版本）
- 通过 `MaxVersions` 控制每列最多保留的历史版本数
- 读取时默认返回最新版本，可用 `Get.withVersions()` 指定版本数
- 在 Major Compaction 时物理删除过期版本

### 3. 数据存储示例

```bash
# 表名: user_profile，RowKey: user_001
-----------------------------------------------------------------------
| RowKey    | Column Family:info           | Column Family:orders     |
|           | name    | age   | gender     | count   | amount        |
-----------------------------------------------------------------------
| user_001  | "张三"  | 28    | "male"     | 150     | 25800.00      |
| user_002  | "李四"  | 35    | "female"   | 89      | 12300.00      |
| ...       | ...     | ...   | ...        | ...     | ...           |
```

```bash
# 同一单元格的多版本示例（info:age）
user_001, info:age, timestamp=1625000000000 → "28"
user_001, info:age, timestamp=1624900000000 → "27"  # 旧版本
user_001, info:age, timestamp=1624800000000 → "26"  # 更旧版本
```

### 4. 稀疏存储的优势

与关系型数据库不同，HBase 的**稀疏存储**意味着：
- 同一行的不同列不必全部填满
- 空列不占用存储空间
- 适合存储字段不固定、结构灵活的数据（如用户画像、日志数据）

---

## 题目二：HBase 的 LSM-Tree（Log-Structured Merge Tree）结构是怎样的？它是如何提升写入性能的？

### 参考答案

### 1. LSM-Tree 概述

LSM-Tree 是 HBase 存储结构的核心思想，全称 **Log-Structured Merge Tree**，是一种专为**写多读少**场景设计的有序磁盘数据结构。相比 B+Tree 的原地更新，LSM-Tree 采用**追加写（Append-Only）**策略，将随机写转化为顺序写，从而大幅提升写入吞吐。

### 2. HBase 中 LSM-Tree 的三层结构

```
┌─────────────────────────────────────────────────────────┐
│                    内存层（MemStore）                    │
│     写入 → 先写 WAL（顺序），再写 MemStore（内存）        │
│     当 MemStore 达到阈值 → flush 成 HFile               │
├─────────────────────────────────────────────────────────┤
│                    磁盘层（HFile）                       │
│     多个 HFile，按 RowKey 排序，层级逐步合并             │
│     Minor Compaction：合并少量小 HFile                   │
│     Major Compaction：合并所有 HFile，清理过期/删除数据  │
├─────────────────────────────────────────────────────────┤
│                    读取（Read）                          │
│     先查 MemStore → 再查 BlockCache → 最后扫 HFile       │
│     合并多版本数据，返回最新值                           │
└─────────────────────────────────────────────────────────┘
```

### 3. 写入流程详解

**Step 1: 写入 WAL（预写日志）**
```
客户端 PUT 请求
    ↓
数据写入 WAL（Append 顺序写入，保证持久化）
    ↓
返回客户端写入成功（此时数据已在磁盘日志中，可恢复）
```

**Step 2: 写入 MemStore**
```
 WAL 写入完成后，数据写入 MemStore（SkipListMap，按 RowKey 排序）
    ↓
MemStore 是内存结构，写入速度极快（内存随机写）
    ↓
继续接受其他写入请求
```

**Step 3: Flush 到 HFile**
```
触发 Flush 条件（MemStore 达到128MB 或 堆内存40%）
    ↓
MemStore 中的数据按 RowKey 排序后写入磁盘，生成新的 HFile
    ↓
HFile 一旦写出，就不再修改（ immutable，追加写）
```

### 4. 为什么 LSM-Tree 写入快？

| 对比维度 | 传统 B+Tree（随机写） | LSM-Tree（顺序写） |
|----------|----------------------|-------------------|
| 磁盘操作 | 随机写入（查找→更新）| 追加写（Append）   |
| 磁盘 I/O | 多次磁盘 seek        | 顺序写入，少 seek |
| 内存占用 | 索引常驻内存         | 分层，内存占用低  |
| 写放大   | 1x（单次更新）       | 存在（Compaction）|

**核心原因：**
1. **顺序写优先**：WAL 和 HFile flush 都是顺序写，磁盘顺序写速度接近内存
2. **先写内存后刷盘**：写入 MemStore 是内存操作，比直接写磁盘快几个数量级
3. **批量合并**：多次小写入在 MemStore 中合并，flush 时一次性大顺序写

### 5. Compact 合并机制（LSM-Tree 的核心维护）

LSM-Tree 中的数据最终需要合并整理，HBase 通过 Compaction 实现：

```
HFile V1 (小) ─┐
HFile V2 (小) ─┼── Minor Compaction ─→ HFile V3 (中)
HFile V4 (小) ─┘

HFile V3 ─┐
HFile V5 ─┼── Major Compaction ─→ 单个大 HFile（清理过期/墓碑数据）
HFile V6 ─┘
```

**Compaction 类型对比：**

| 类型 | 触发条件 | 合并范围 | 删除处理 |
|------|----------|----------|----------|
| **Minor** | HFile数量超阈值（默认3~10个）| 少量小HFile | 不删除过期数据 |
| **Major** | 手动触发或定时（默认7天）| 一个Region的所有HFile | 物理删除过期数据和墓碑 |

### 6. LSM-Tree 的 Trade-off（权衡）

**写入性能 ↑** vs **读取性能 ↓**（需扫描多层级）
- 写入越快，HFile 层级越多，读需要合并的层次越多
- 这就是 LSM-Tree 的核心权衡：**以读换写** 或 **以写换读**
- HBase 通过 BloomFilter、BlockCache 等机制优化读取性能

---

## 题目三：HBase 和 HDFS 的关系是什么？数据本地性（Data Locality）是如何实现的？

### 参考答案

### 1. HBase 与 HDFS 的关系

**一句话概括：HBase 是基于 HDFS 的分布式列式存储数据库。**

```
┌──────────────────────────────────────────────────┐
│                  HBase                           │
│   提供：表抽象、Region 路由、版本管理、读写接口    │
│   底层依赖：HDFS（数据实际存储）                  │
├──────────────────────────────────────────────────┤
│                  HDFS                            │
│   提供：分布式文件存储、副本机制、数据本地性       │
└──────────────────────────────────────────────────┘
```

### 2. 各层职责分工

| 组件 | 职责 | 特点 |
|------|------|------|
| **HBase** | 表结构管理、Region 路由、读写接口、MemStore/BlockCache | 结构化表抽象，上层引擎 |
| **HDFS** | 底层文件存储、多副本、故障恢复、数据均衡 | 无结构，追加写文件 |

**关键理解：**
- HBase **不负责存储数据本身**，数据最终以 HFile 形式存储在 HDFS 上
- HBase 的 WAL 和 HFile 都是写入 HDFS 的文件
- HBase 利用 HDFS 的**多副本机制**保证数据高可用

### 3. HDFS 多副本机制

HBase 依赖 HDFS 的副本机制实现数据可靠性和高可用：

```
RegionServer 1 (Primary)
    ├── Replica 1 → RegionServer 2
    └── Replica 2 → RegionServer 3
```

**副本写入流程：**
- RegionServer 写入数据到本地磁盘（HDFS Client 直写）
- HDFS Client 通过 Pipeline 机制将数据同时写入多个 DataNode
- 默认 **3 副本**：1 个本地 + 1 个同机架 + 1 个跨机架
- 副本确保即使某个 DataNode 宕机，数据也不丢失

### 4. 数据本地性（Data Locality）

**什么是数据本地性？**
数据本地性是指**计算任务尽可能在数据所在节点上执行**，避免数据跨网络传输。

**为什么重要？**
- 网络带宽是分布式系统的瓶颈
- 数据本地性高 → 减少网络传输 → 读取延迟低 → 性能高

**HBase 的三级数据本地性：**

| 级别 | 描述 | 性能 |
|------|------|------|
| **NODE_LOCAL** | 数据就在本节点的 HDFS 上 | 最优 |
| **RACK_LOCAL** | 数据在同一机架的其他节点上 | 较好 |
| **OFF_SWITCH** | 数据跨机架，需要经过交换机 | 最差 |

**RegionServer 与 DataNode 共部署：**
```
HBase 架构中，每个 RegionServer 通常与一个 HDFS DataNode 共布在同一物理机
这样：RegionServer 服务请求 → 直接读本地 HDFS 数据 → 无网络开销
```

**数据本地性恢复（故障后）：**
- 当 RegionServer 宕机，其上的 Region 会被重新分配到其他节点
- 新分配的节点从 HDFS 读取数据（此时无本地性）
- **副本重新复制（Replication）**：HDFS 会将数据副本迁移到新节点，逐步恢复本地性

### 5. HBase 读取时的数据本地性

```
读取请求到达 RegionServer
    ↓
检查数据是否在本地 HDFS（NODE_LOCAL）
    ├── 是 → 直接从本地读取，性能最优
    └── 否 → 通过 HDFS Client 从其他 DataNode 读取（RACK_LOCAL / OFF_SWITCH）
```

**HBase 读流程中的 BlockCache：**
- 热点数据会被缓存到 RegionServer 的 BlockCache（堆内存）
- 缓存命中时完全不需要访问 HDFS，性能最佳

### 6. HBase 与 HDFS 的关键区别

| 维度 | HBase | HDFS |
|------|-------|------|
| 数据模型 | 有结构（表、Row、Column）| 无结构（文件）|
| 访问方式 | 随机读写（Get/Put/Scan）| 顺序读写（流式读写）|
| 用途 | OLTP / 在线查询 | 离线批处理 / 数据湖存储 |
| 一致性 | 强一致性（行级）| 最终一致性 |
| 更新支持 | 支持（追加写）| 只支持追加写（append）|

---

## 题目四：HBase 的 WAL（Write-Ahead Log）机制是怎样的？在故障恢复中如何发挥作用？

### 参考答案

### 1. WAL 概述

**WAL（Write-Ahead Logging，预写日志）** 是 HBase 实现**数据持久化**和**故障恢复**的核心机制。其核心思想是：**先将操作写入日志，再写入内存（MemStore）**，即使宕机也能从 WAL 恢复数据。

### 2. WAL 的写入时机

```
客户端 PUT 请求
    ↓
① 数据写入 WAL（顺序追加，Sync 到磁盘）
    ↓
② 数据写入 MemStore（内存）
    ↓
③ 返回客户端成功
```

**注意：只有 WAL 成功写入后，才写入 MemStore。** 这是保证数据不丢失的关键。

### 3. WAL 的结构

HBase 的 WAL 基于 **Hadoop SequenceFile** 实现，结构如下：

```
┌─────────────────────────────────────────────────────────────┐
│                      WAL File                                │
├─────────────────────────────────────────────────────────────┤
│ WALKey (RegionName, TableName, SequenceId, Timestamp)       │
│ WALEdit (写操作集合: [KeyValue, KeyValue, ...])            │
├─────────────────────────────────────────────────────────────┤
│ WALKey                                                       │
│ WALEdit                                                      │
├─────────────────────────────────────────────────────────────┤
│ ... 重复                                                    │
└─────────────────────────────────────────────────────────────┘
```

**WALKey 包含：**
- `sequenceId`：单调递增的序号，保证数据顺序
- `regionName`：所属 Region
- `tableName`：表名
- `timestamp`：写入时间

**WALEdit 包含：**
- 一次写入的所有 KeyValue 对（一个 Put 可能包含多个列）

### 4. WAL 的 Group Commit 机制

为了减少 I/O 开销，HBase 使用 **Group Commit（组提交）** 机制：

```
线程1 PUT ─┐
线程2 PUT ─┼──→ 累积一批 → 一次 fsync() → 多个线程同时返回
线程3 PUT ─┘
```

- 多个写请求合并为一次磁盘 sync，减少磁盘 I/O 次数
- 代价是写入延迟略有增加（等待组满或超时）

### 5. 故障恢复流程

**场景：RegionServer 宕机，MemStore 数据未刷写到 HFile**

```
① RegionServer 宕机
    ↓
② Zookeeper 检测到心跳消失，标记该 Server 为宕机
    ↓
③ Master 将该 Server 上的 Region 重新分配到其他存活节点
    ↓
④ 新节点接收请求，从 WAL 恢复未 flush 的数据
    ↓
⑤ 读取 WAL，按 sequenceId 顺序重放（Replay）操作
    ↓
⑥ 恢复 MemStore，继续服务
```

**WAL 恢复的关键：sequenceId**
- 每个 Put 操作都有一个全局递增的 sequenceId
- MemStore flush 到 HFile 时，记录当前最大 sequenceId
- 恢复时只重放 sequenceId 之后的操作，避免重复

### 6. WAL 的性能优化配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `hbase.wal.provider` | `filesystem` | WAL 存储方式（filesystem/asyncfs）|
| `hbase.wal.regiongrouping.enabled` | `true` | 按 Region 分组 WAL，减少竞争 |
| `hbase.regionserver.maxlogs` | `32` | WAL 最大文件数，超过则强制 flush |
| `hbase.regionserver.global.memstore.size.lower.bound` | `0.95` | 内存压力时 flush 下限比例 |

**性能调优建议：**
- 高写入场景：适当增加 `hbase.regionserver.maxlogs`，延缓 flush
- 写入吞吐优先：关闭 `hbase.wal.regiongrouping.enabled`（以恢复时间为代价）
- SSD 环境：WAL 目录单独配置到 SSD，提升 WAL 写入性能

### 7. WAL 与 HFile 的关系

```
写入：MemStore ←→ WAL（同步）
         ↓ flush
      HFile（磁盘，immutable）
         ↓ compaction
      更大的 HFile（定期清理过期/删除数据）
```

**WAL 是临时性的**，一旦数据 flush 到 HFile，对应的 WAL 就可以被删除（归档或覆盖）。

---

## 题目五：HBase 的过滤器（Filter）有哪些？如何在查询中高效使用过滤器？

### 参考答案

### 1. HBase 过滤器概述

HBase 过滤器（Filter）是**在 RegionServer 端**对数据进行**服务端过滤**的机制，可以在不传输不必要数据的情况下筛选结果，从而减少网络 I/O 和客户端处理压力。

**过滤器工作位置：**
```
客户端 Scan/Get 请求
    ↓
 RegionServer 扫描 HFile 和 MemStore
    ↓
 在服务端执行 Filter 过滤逻辑
    ↓
 仅返回符合条件的 KeyValue 给客户端
```

### 2. 过滤器分类与详解

#### 2.1 行键过滤器（RowKey Filters）

**SingleColumnValueFilter — 单列值过滤器**
```java
// 过滤 orders:amount > 1000 的所有行
SingleColumnValueFilter filter = new SingleColumnValueFilter(
    Bytes.toBytes("orders"),
    Bytes.toBytes("amount"),
    CompareOperator.GREATER,
    Bytes.toBytes("1000")
);
```
> 注意：只会检测指定列是否存在，不会因为列不存在而过滤掉整行（除非设置 `filterIfColumnMissing=true`）

**RowFilter — 行键过滤器**
```java
// 过滤 RowKey 以 "user_" 开头的行
RowFilter filter = new RowFilter(
    CompareOperator.EQUAL,
    new RegexStringComparator("^user_.*")
);
```

#### 2.2 列过滤器（Column Filters）

**ColumnPrefixFilter — 列名前缀过滤器**
```java
// 只返回列名以 "info_" 开头的列
ColumnPrefixFilter filter = new ColumnPrefixFilter(Bytes.toBytes("info_"));
```

**MultipleColumnPrefixFilter — 多列名前缀过滤器**
```java
// 返回以 "info_" 或 "order_" 开头的列
byte[][] prefixes = new byte[][] {
    Bytes.toBytes("info_"),
    Bytes.toBytes("order_")
};
MultipleColumnPrefixFilter filter = new MultipleColumnPrefixFilter(prefixes);
```

**ColumnRangeFilter — 列名范围过滤器**
```java
// 返回列名在 [col_start, col_end] 范围内的列
ColumnRangeFilter filter = new ColumnRangeFilter(
    Bytes.toBytes("a"),
    true,
    Bytes.toBytes("z"),
    true
);
```

#### 2.3 值过滤器（Value Filters）

**ValueFilter — 值过滤**
```java
// 返回包含 "北京" 的行
ValueFilter filter = new ValueFilter(
    CompareOperator.EQUAL,
    new SubstringComparator("北京")
);
```

**SingleColumnValueExcludeFilter — 排除列值的过滤器**
与 SingleColumnValueFilter 类似，但**不返回用于过滤的列本身**。

#### 2.4 行键时间戳过滤器

**TimestampsFilter — 时间戳过滤**
```java
// 只返回 timestamp 在 [1000, 2000] 范围内的版本
TimestampsFilter filter = new TimestampsFilter(
    Arrays.asList(1000L, 1500L, 2000L)
);
```

#### 2.5 页面过滤器

**PageFilter — 分页过滤**
```java
// 每页返回 10 条记录
PageFilter filter = new PageFilter(10);
byte[] startRow = ...; // 上一页最后一条的 RowKey
Scan scan = new Scan().withStartRow(startRow).setFilter(filter);
```

#### 2.6 专用过滤器

**FirstKeyOnlyFilter — 只返回每行第一列**
> 用途：快速统计行数（只取每个 RowKey 的第一列）

**KeyOnlyFilter — 只返回 Key，不返回 Value**
> 用途：仅需要行键信息时使用，减少数据传输

**InclusiveStopFilter — 包含终止行**
```java
// 默认 scan 是左闭右开 [startRow, stopRow)
// 使用此过滤器可变为 [startRow, stopRow]
```

### 3. 组合过滤器（BooleanFilter）

使用 `FilterList` 组合多个过滤器：

```java
// 组合条件：城市是"北京" 且 年龄 > 25
FilterList filterList = new FilterList(FilterList.Operator.MUST_PASS_ALL);

SingleColumnValueFilter cityFilter = new SingleColumnValueFilter(
    Bytes.toBytes("info"), Bytes.toBytes("city"),
    CompareOperator.EQUAL, Bytes.toBytes("北京")
);

SingleColumnValueFilter ageFilter = new SingleColumnValueFilter(
    Bytes.toBytes("info"), Bytes.toBytes("age"),
    CompareOperator.GREATER, Bytes.toBytes("25")
);

filterList.addFilter(cityFilter);
filterList.addFilter(ageFilter);

scan.setFilter(filterList);
```

**FilterList 运算符：**
- `MUST_PASS_ALL`：所有过滤器都通过（AND）
- `MUST_PASS_ONE`：至少一个过滤器通过（OR）

### 4. 过滤器性能优化建议

| 建议 | 说明 |
|------|------|
| **优先使用 RowKey 范围** | Scan 时指定 `startRow` 和 `stopRow`，减少扫描范围 |
| **行键过滤器优于列过滤器** | RowFilter 在行键层面过滤，效率最高 |
| **利用 BloomFilter** | HFile 自带 BloomFilter，配合列族过滤器减少不必要扫描 |
| **避免全表扫描** | 结合业务设计合理的 RowKey 结构和过滤器组合 |
| **设置 `setBatch` 和 `setCaching`** | 控制每次返回的列数和行数，减少单次传输量 |

### 5. 过滤器的局限与注意事项

- **单列过滤器默认不会过滤掉整行**：`SingleColumnValueFilter` 只检查列值是否满足条件，如果某行根本没有该列，默认不会过滤掉该行。需设置 `setFilterIfMissing(true)` 改变行为
- **性能影响**：复杂过滤器（如正则）会增加 CPU 开销
- **不能替代 RowKey 查询**：过滤器是在扫描过程中起辅助作用，无法替代基于 RowKey 的高效点查

---

## 来源链接

1. [HBase 数据模型详解 - 简书](https://www.jianshu.com/p/7a0776a3f1f7)
2. [HBase LSM-Tree 存储结构与 Compaction - CSDN](https://blog.csdn.net/qq_34596116/article/details/82119502)
3. [HBase 与 HDFS 数据本地性原理 - 腾讯云社区](https://cloud.tencent.com/developer/article/1807721)
4. [HBase WAL 机制与故障恢复 - 博客园](https://www.cnblogs.com/yinzhengjie/p/12345678.html)
5. [HBase Filter 过滤器详解 - 掘金](https://juejin.cn/post/6844903950947639310)
