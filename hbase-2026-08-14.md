# HBase 面试题（2026-08-14）

> 整理自：
> - [CSDN - HBase深入浅出](https://blog.csdn.net/tianhouquan/article/details/105354181)
> - [博客园 - HBase 面试题总结](https://www.cnblogs.com/bigdataer/p/14592701.html)
> - [阿里云开发者社区 - HBase 面试题整理](https://developer.aliyun.com/article/765432)

---

## 题目一：HBase 的架构组成及各组件作用

### 参考答案

HBase 采用 **ZooKeeper + HMaster + HRegionServer** 的分布式架构：

| 组件 | 作用 |
|------|------|
| **ZooKeeper** | 负责 HMaster 的选举和协调，保证集群高可用；存储元数据路径（如 `/hbase/master`）；监控 RegionServer 的上下线 |
| **HMaster** | 负责表的 CRUD 操作（创建、删除、修改表结构）；管理 Region 的负载均衡与分裂；调度 Region 到合适的 RegionServer；处理元数据变更 |
| **HRegionServer** | 负责数据的读写请求；管理 MemStore 和 HFile 的刷新与合并；与 ZooKeeper 保持心跳汇报状态 |
| **Region** | HBase 表的水平分片单位，每个 Region 包含 `[StartKey, EndKey)` 区间的一行数据 |
| **Store** | 对应列族，每个列族对应一个 Store，包含 MemStore 和多个 HFile |
| **ZooKeeper** | 协调组件，负责选主、分布式锁、服务健康检查 |

**整体工作流程：** Client 先连接 ZooKeeper 获取 Meta 表位置，再请求对应的 RegionServer 进行读写；HMaster 负责元数据管理，不参与实际数据读写。

---

## 题目二：HBase 的 MemStore 与 HFile 工作机制是什么？

### 参考答案

**MemStore（写缓存）：**

- 每次 PUT 请求先将数据写入 MemStore（基于 LSM-Tree 的内存有序结构）
- 按 RowKey 字典序排列，相同 RowKey 的不同版本按时间戳倒序排列
- 当 MemStore 达到阈值（默认 128MB），触发 **Flush** 操作将数据刷写成 HFile
- Flush 是按 Region 级别的，同一 Region 的多个列族会各自独立 Flush

**HFile（存储文件）：**

- HBase 在 HDFS 上的实际存储格式，基于 Hadoop 的 SSTable
- 包含 Data Block、Meta Block、FileInfo、Index Block 等结构
- HFile 一旦写入不再修改，数据的更新和删除通过新增版本来实现
- Minor Compaction：多个小 HFile 合并成一个大 HFile（不删除过期版本）
- Major Compaction：整个 Store 下所有 HFile 合并成一个大文件，**删除过期版本和标记删除的数据**

**为什么先写 MemStore 而不是直接写 HFile？**

1. HFile 写入需要完成 I/O，频繁写入会严重降低性能
2. MemStore 在内存中排序后批量刷写，顺序写入 HDFS 效率更高
3. 减少 HDFS 小文件数量，提升读取性能

---

## 题目三：HBase 的 RowKey 设计原则有哪些？

### 参考答案

RowKey 是 HBase 数据的唯一标识，设计好坏直接影响读写性能：

**1. 避免热点（HotSpot）原则**
- 热点的本质是大量请求集中在少数节点上，导致部分 RegionServer 负载过高
- 不要使用单调递增或递减的 RowKey（如时间戳、设备ID自增）
- 常用方案：RowKey 拼接哈希前缀、随机数前缀、语义化前缀

**2. 长度适中原则**
- RowKey 最大支持 64KB，建议控制在 16~100 字节
- 过长的 RowKey 会增加 MemStore 内存开销和 HFile 索引大小
- 短而有语义的 RowKey 最佳（如 `deviceId_eventType_timestamp`）

**3. 唯一性原则**
- RowKey 必须唯一标识一行数据，类似关系型数据库的主键
- 组合 RowKey 时确保能区分不同业务实体

**4. 可排序原则**
- HBase 按字典序对 RowKey 排序，相同前缀的数据会相邻存储
- 利用这一特性可以设计"范围查询"场景（如分页、时间区间查询）
- 设计示例：`{userId}_{timestamp}` 逆序，可快速查询某用户最新N条记录

**5. 查询场景驱动原则**
- 根据实际查询模式设计 RowKey，优先满足最频繁的查询条件
- 读多写少场景：可考虑预聚合或预切割热点数据
- 典型 RowKey 模式：`{查询字段1}_{查询字段2}_{时间戳}`

---

## 题目四：HBase 的协处理器（Coprocessor）有哪两类？各有什么用途？

### 参考答案

HBase 协处理器类似 HDFS 的 DataNode 插件机制，允许用户在 RegionServer 上运行自定义代码。

**1. Observer Coprocessor（观察者）**
- 类似于关系型数据库的触发器（Trigger）
- 在特定事件前后自动执行回调，不修改处理逻辑
- 常见 Hook 点：
  - `preGet` / `postGet`：读取操作前后
  - `prePut` / `postPut`：写入操作前后
  - `preDelete` / `postDelete`：删除操作前后
  - `preOpen` / `postOpen`：Region 打开时
  - `preFlush` / `postFlush`：Flush 操作前后
- **典型应用**：二级索引实现（如 Phoenix HBase 索引）、数据访问审计、权限控制

**2. Endpoint Coprocessor（端点）**
- 类似于关系型数据库的存储过程（Stored Procedure）
- 在 RegionServer 上执行自定义计算，将计算逻辑下推到数据所在节点
- 主动触发调用，常用于聚合、统计、复杂计算等场景
- **典型应用**：
  - 自定义聚合（Sum、Avg 等在 RegionServer 上并行计算后汇总）
  - 批量数据转换和导出
  - 自定义过滤器

**协处理器加载方式：**
- 静态加载：通过 `hbase-site.xml` 配置，重启集群生效（适合全集群级别）
- 动态加载：通过 HBase Shell 或 API 指定表级别加载（适合开发测试）

---

## 题目五：HBase 数据写入流程及 Flush 触发条件是什么？

### 参考答案

**数据写入完整流程（PUT 请求）：**

1. **ZooKeeper 定位 Meta 表**：Client 先从 ZooKeeper 获取 `-ROOT-` 表位置（老版本）或直接获取 Meta 表位置
2. **定位目标 Region**：从 Meta 表中找到目标 RowKey 所属的 Region 及其 RegionServer 地址
3. **写入 MemStore**：将数据写入对应 RegionServer 的 MemStore，同时追加到 WAL（Write-Ahead Log）
4. **返回客户端**：写入 MemStore 成功后即返回写入成功（异步刷写）
5. **后台 Flush**：MemStore 达到阈值后触发 Flush，生成 HFile
6. **后台 Compaction**：小 HFile 合并成大 HFile，减少文件数量提升读性能

**WAL（预写日志）作用：**
- 保证数据不丢失：MemStore 数据未刷盘前，先写入 WAL 持久化
- RegionServer 宕机恢复时，通过重放 WAL 恢复未刷盘数据

**Flush 触发条件（满足任一即触发）：**

| 触发条件 | 默认值 | 说明 |
|----------|--------|------|
| MemStore 大小达到 `hbase.hregion.memstore.flush.size` | 128MB | 单个 MemStore 阈值 |
| Region 所有 MemStore 总大小超过 `hbase.regionserver.global.memstore.size` | 堆内存×40% | 全局阈值，触发所有 Region 异步 Flush |
| WAL 文件数量超过 `hbase.regionserver.max.logs` | 32 | WAL 堆积过多时强制 Flush 释放日志 |
| RegionServer 即将关闭 | - | 服务端关闭前执行 |
| 手动执行 flush 命令 | - | 用户主动触发 |

**注意事项：**
- Flush 按 Region 独立执行，多个列族各自独立 Flush
- 全局 MemStore 超限时会按时间戳由老到新依次 Flush，优先刷旧数据释放 WAL

---

> 📚 更多面试题持续更新中，欢迎 Star！
> GitHub: https://github.com/qq286158530/interview-questions
