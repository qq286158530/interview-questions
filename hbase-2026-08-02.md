# HBase 面试题 — 2026-08-02

> 本题集整理自网络公开技术资料，涵盖 HBase 架构、RowKey 设计、MemStore/HFile 存储机制、Compaction 策略、协处理器等核心知识点，适合大数据工程师面试复习与能力巩固。

---

## 题目一：HBase 的数据存储结构是怎样的？请详细说明 HFile 的内部结构和索引机制。

### 参考答案

**HBase 存储层次结构：**

```
Table → Region → Column Family → MemStore (内存)
                           → HFile  (磁盘)
```

- **Table**：按 RowKey 水平切分成多个 Region，分布到不同 RegionServer。
- **Region**：每个 Region 管理一段 RowKey 区间 `[startKey, endKey)`。
- **Column Family**：同列族的数据存储在一起，形成独立的 Store（MemStore + HFiles）。
- **MemStore**：写缓存，按 RowKey 有序存储在内存中。
- **HFile**：磁盘上的最终存储文件。

**HFile 内部结构（Google BigTable 格式）：**

HFile 是一个 B-Tree 变体（LSM-Tree 的磁盘层），分为以下块：

1. **Data Block（数据块）**：存储 KeyValue 对，默认 64KB。遍历时加载到 BlockCache。
2. **Meta Block（元数据块，可选）**：存储布隆过滤器（Bloom Filter）信息。
3. **File Info（文件信息块）**：存储文件元信息（最后更新时间、压缩算法、最小时间戳等）。
4. **Index Block（索引块）**：存储每个 Data Block 的起始 RowKey 和文件偏移量，实现快速定位。
5. **Trailer（ trailer 块）**：定长块，指向各块的起始偏移量，是 HFile 读取的入口。

**关键索引机制：**

- **Bloom Filter**：每个 HFile 在 Meta Block 中存储 RowKey 布隆过滤器，可快速判断某个 RowKey 是否**不存在**于该文件，避免无效 IO。查询时先查 Bloom Filter，再决定是否读取该 HFile。
- **Block Index**：存储每个 Data Block 的一级索引，通过二分查找定位目标 Block。
- **两层索引**：Block 索引（块级）+ Bloom Filter（行级/列级），共同实现快速随机读。

**为什么 HFile 设计成这样？**

- **顺序写**：HFile 只追加写，不原地更新，靠 Compaction 合并来重写。
- **追加写保证高写入吞吐**：写入是顺序 IO，不随机寻址。
- **分层索引保证随机读效率**：Index Block 放内存，Data Block 按需加载到 BlockCache。

> 📎 来源：HBase 官方文档 — "HBase Architecture: StoreFile"  
> https://hbase.apache.org/book.html#arch.store

---

## 题目二：HBase 的 Compaction 有哪几种类型？Minor Compaction 和 Major Compaction 的区别是什么？Compaction 带来哪些问题？

### 参考答案

**Compaction 本质：**

HBase 写数据时，先写 MemStore，MemStore 刷盘后生成 HFile。随着时间推移，同一个 Store 下会有多个 HFile，读取时需要合并多个文件，效率低下。Compaction 就是**将多个小 HFile 合并成少量大 HFile**的过程。

**两种 Compaction 类型：**

1. **Minor Compaction（小合并）**
   - 自动触发：HBase 定期（默认每 10 分钟检查一次）将**较小的 HFile 合并成较大的 HFile**。
   - 只合并满足条件的 HFile（排除正在写入的文件），不处理被删除/过期的数据。
   - 速度较快，对系统压力小，但不会清理 tombstone（墓碑标记）。

2. **Major Compaction（大合并）**
   - 默认策略：每天自动执行一次（`hbase.hregion.majorcompaction`），将该 Store 下的**所有 HFile 合并成一个**。
   - **执行期间会清理**：所有被删除的单元格（tombstone 标记的数据）、超过版本数限制的旧版本、TTL 到期的数据。
   - 合并后删除旧 HFile，释放磁盘空间。

**Major Compaction 的问题（重点）：**

1. **IO 风暴（Compaction Storm）**
   - Major Compaction 期间产生大量顺序写和读，可能与正常读写 IO 竞争，导致读写延迟突增。
   - 解决方案：关闭自动 Major Compaction，改为手动在业务低峰期触发。

2. **网络流量峰值**
   - 合并过程中 HFile 数据通过 HDFS 复制，网络带宽占用显著。
   - HDFS 默认 3 副本，合并后重新写入 3 份数据。

3. **HBase-2.0 之前的 write 阻塞问题**
   - HBase 老版本中，Major Compaction 期间会将所有 HFile 读入内存再重写，大 Region 可能导致 Full GC。
   - HBase-2.0 后使用 Compaction 并行化和无锁读取缓解了此问题。

4. **读放大（Read Amplification）**
   - Compaction 进行中时，同一行数据可能同时存在于多个 HFile 中，读取时需要合并多个文件。

**最佳实践：**

- 关闭自动 Major Compaction，在业务低峰期手动触发。
- 控制单个 Region 的 HFile 数量（通过 `hbase.hstore.compaction.max` 限制）。
- 使用 Storage API/BulkLoad 绕过 MemStore 直接生成 HFile，减少 Compaction 压力。

> 📎 来源：HBase 官方文档 — "Compaction"  
> https://hbase.apache.org/book.html#compaction

---

## 题目三：HBase 的 RowKey 设计的三大原则是什么？请结合实际场景说明如何避免热点（HotSpot）问题。

### 参考答案

**RowKey 设计三大原则：**

1. **长度短小**：建议 16~100 字节，越短越好。HBase 的 KeyValue 结构中 RowKey 被反复存储，过长的 RowKey 会浪费内存（BlockCache）和磁盘空间。
2. **唯一性**：RowKey 必须是该行数据的唯一标识，类似主键。
3. **散列（避免热点）**：RowKey 的设计要保证数据均匀分布到所有 Region，避免热点。

**热点（HotSpot）的本质：**

HBase 按 RowKey 的字典序排列数据，并按区间将 Region 分给不同 RegionServer。如果大量 RowKey 前缀相同，数据会集中写入某个 RegionServer，导致：
- 单台 RegionServer 压力过大（CPU/IO/网络）
- 读写延迟增加
- GC 压力增大，稳定性下降

**常见热点场景及解决方案：**

| 场景 | 问题 | 解决方案 |
|------|------|----------|
| 时间戳作为 RowKey 前缀 | `20260802xxx` 导致当天数据集中到同一 Region | 盐值（Salting）：`MD5(ts)%N + original_key` |
| 用户 ID 递增 | 所有新用户数据写入最新 Region | 哈希打散：`hash(userId).substring(0,4) + userId` |
| 固定枚举值做前缀 | 如 `type=A` 的数据全在一个 Region | 随机数打散或换查询维度 |

**最佳实践示例：**

```
# 场景：按用户 ID 查询，最近 7 天订单
# 错误设计（热点）：
RowKey = orderId  (递增，新增订单集中在最新 Region)

# 正确设计（散列+业务导向）：
RowKey = MD5(userId).substring(0,2) + "#" + userId + "#" + Long.MAX_VALUE - timestamp

# 查询时：
- 先根据 userId 计算 MD5 前缀
- 再按时间范围倒序 Scan
```

**RowKey 设计的业务导向原则：**

- 如果查询主要按**用户 ID 查**：RowKey 以用户 ID 开头，支持 `Get` 高效单行查询。
- 如果查询主要按**时间范围**：RowKey 末尾放时间戳，支持范围 Scan。
- 如果两者都要兼顾：考虑**双 RowKey 设计**（主表 + 索引表），或使用协处理器建立二级索引。

> 📎 来源：HBase 官方文档 — "RowKey Design"  
> https://hbase.apache.org/book.html#rowkey.design

---

## 题目四：HBase 读取数据时，如何从 BlockCache、MemStore 和 HFile 三层中定位数据？Bloom Filter 在其中起到什么作用？

### 参考答案

**HBase 读数据的三层查找路径（从上到下，命中即返回）：**

```
① BlockCache (LRU Cache，热点数据)
        ↓ 未命中
② MemStore (写缓存，最新写入但未刷盘的数据)
        ↓ 未命中
③ HFile (磁盘文件)
```

**详细读取流程：**

1. **BlockCache（块缓存）**
   - LRU（Least Recently Used）缓存，存储最近读取的 HFile 数据块。
   - 读取时先在 BlockCache 中查找，数据块按 KeyValue 格式存储。
   - 命中则直接返回，减少磁盘 IO。

2. **MemStore**
   - 如果 BlockCache 未命中，查找 MemStore。
   - MemStore 包含已写入但尚未刷盘的最新数据（最完整的数据源）。
   - 如果目标数据在 MemStore 中找到，直接返回。

3. **HFile**
   - 如果上述两层都未命中，从 HFile 加载数据。
   - 使用 **Bloom Filter** 快速判断该 HFile 是否可能包含目标 RowKey：
     - Bloom Filter 说「不存在」→ 跳过该 HFile（避免无效 IO）
     - Bloom Filter 说「可能存在」→ 加载 HFile，扫描定位
   - 定位到数据后，将该 Block 加入 BlockCache，供后续查询使用。

**Bloom Filter 的作用：**

Bloom Filter 是一个空间高效的概率数据结构，用于判断「一个元素是否在一个集合中」：

- **不存在一定准确**：如果 Bloom Filter 返回 false，该 RowKey 一定不在该 HFile 中。
- **存在可能误判**：如果 Bloom Filter 返回 true，该 RowKey **可能**存在（存在假阳性概率，但可控）。

```
Bloom Filter 大小配置：hbase-default.xml 中 hbase.defaults.for.version.skip 是 false 时默认开启
实际配置项：hfile.block.bloom.enabled = true
```

**没有 Bloom Filter 的问题：**

- 每个 HFile 都需要被打开并扫描，即使目标 RowKey 明显不在其中。
- 读放大严重，大量无效磁盘 IO。
- HBase 官方建议：**开启 Bloom Filter，节省 ~50% 的读取 IO**。

**BlockCache 的淘汰策略：**

- `LRUBlockCache`：默认策略，内存达到阈值后淘汰最久未访问的 Block。
- `BucketCache`：可配置为堆外内存，减少 GC 压力。
- `SlabCache`（已废弃）：先分配固定大小内存块，易产生内部碎片。

> 📎 来源：HBase 官方文档 — "BlockCache and Read Path"  
> https://hbase.apache.org/book.html#blockcache

---

## 题目五：HBase 的协处理器（Coprocessor）有哪两类？它们分别解决什么问题？请详细说明使用场景和注意事项。

### 参考答案

**协处理器的定位：**

协处理器是 HBase 提供的**在 RegionServer 端运行自定义代码**的扩展机制，允许用户在数据读、写、查询的关键路径上注入业务逻辑，无需修改 HBase 核心代码。

**两大类型对比：**

| 特性 | Observer（观察者型） | Endpoint（端点型） |
|------|---------------------|-------------------|
| 类似机制 | 数据库触发器 | 数据库存储过程 |
| 执行时机 | 在特定操作前后自动触发 | 客户端主动调用 |
| 数据可见性 | 可见原数据，可修改 | 不可见原始数据 |
| 返回结果 | 无直接返回值（通过副作用生效） | 返回计算结果 |
| 典型场景 | 权限校验、二级索引、审计日志 | 聚合计算、复杂过滤 |

---

**Observer Coprocessor（观察者型）**

**核心机制**：类似触发器，在指定事件前后自动执行自定义逻辑。

**常用钩子（HOOK）**：

```
PreGet / PostGet        — 读取操作前后
PrePut / PostPut        — 写入操作前后
PreDelete / PostDelete  — 删除操作前后  
PreOpen / PostOpen      — Region 打开前后
PreFlush / PostFlush    — MemStore 刷盘前后
```

**典型使用场景：**

1. **列级权限控制（PostPut 校验）**
   ```java
   // 用户写入数据后，检查用户是否有该列的写权限
   // 无权限则抛出 AccessDeniedException，触发 Rollback
   @Override
   public void postPut(ObserverContext<RegionCoprocessorEnvironment> c,
                      Put put, WALEdit edit) {
       // 校验逻辑
   }
   ```

2. **二级索引同步（PostPut 写索引表）**
   ```java
   // 主表写入后，自动写入索引表
   // 例如：用户表 RowKey = userId，写入后同步更新"手机号→userId"索引表
   ```

3. **审计日志（PostPut 记录操作）**
   ```java
   // 所有写操作记录到审计表
   // 记录：操作人、时间、表名、RowKey、操作类型
   ```

---

**Endpoint Coprocessor（端点型）**

**核心机制**：在每个 RegionServer 上执行自定义计算，客户端收集各 Region 的结果后汇总，类似 MapReduce 的 map → reduce 过程。

**使用流程**：
```
1. 定义 Protobuf 服务描述（.proto 文件）
2. 实现 CoprocessorService 接口
3. 客户端调用：table.coprocessorService()
4. 每个 Region 执行服务端逻辑，返回结果
5. 客户端汇总所有 Region 的返回值
```

**典型使用场景**：

1. **服务端聚合计算**
   ```java
   // 计算某列的总和/平均值，在 RegionServer 端执行，避免传输原始数据
   // 返回聚合结果，而非全部原始行
   ```

2. **位图计算（Bitmap Intersection）**
   ```java
   // 计算多个标签的用户交集，在服务端用位图运算，客户端只取结果
   ```

3. **复杂 JSON/Protobuf 过滤**
   ```java
   // 将非结构化数据解析和过滤下推到 RegionServer，减少网络传输
   ```

---

**协处理器的注意事项（面试重点）：**

1. **异常处理不当会炸掉 RegionServer**
   - 协处理器中未捕获的异常会上传 RegionServer，可能导致该 Region 不可用。
   - 解决：使用 `ObserverContext.bypass()` 或在代码中充分 try-catch。

2. **不要在 Observer 中做耗时操作**
   - Pre/Put 等钩子在写路径上，耗时操作会**阻塞写吞吐**。
   - 解决：将耗时操作放入异步队列，协处理器中只做轻量判断。

3. **版本兼容性问题**
   - 协处理器与 HBase 版本强绑定，升级 HBase 可能导致协处理器失效。
   - 解决：在测试环境充分验证后再上线。

4. **加载方式**
   - **静态加载**（hbase-site.xml）：重启 HBase 生效，全局可用。
   - **动态加载**（HBase Shell）：`alter 'table', METHOD => 'table_att'` 动态挂载，无需重启。

> 📎 来源：HBase 官方文档 — "Coprocessors"  
> https://hbase.apache.org/book.html#cp

---

*整理：小憨宝 | 适合大数据/HBase 工程师面试复习*
