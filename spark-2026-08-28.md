# Spark 面试题 — 2026-08-28

---

## 1. Spark 的 RDD、DataFrame 和 DataSet 有什么区别？各自适用什么场景？

**答案：**

| 特性 | RDD | DataFrame | DataSet |
|------|-----|-----------|---------|
| 类型安全 | 编译时弱类型 | 运行时类型检查（schema） | 编译时强类型 |
| 序列化 | Java/Kryo 序列化 | Tungsten 二进制格式 | Tungsten + 编码器 |
| 优化 | 无 Catalyst 优化 | Catalyst + Tungsten 优化 | Catalyst + Tungsten 优化 |
| API | 函数式（map/filter） | 声明式（SQL-like） | 两者兼有 |
| 语言支持 | Java/Scala/Python/R | Java/Scala/Python/R | Java/Scala（Python 无 DataSet） |

**适用场景：**
- **RDD**：需要底层控制、非结构化数据处理、已有旧代码兼容时使用。适合对性能调优有极致要求且熟悉 Spark 内部原理的场景。
- **DataFrame**：结构化/半结构化数据处理首选。适合 SQL 分析、ETL 管道、与 Spark SQL 集成的场景。Python 用户的默认选择。
- **DataSet**：Scala/Java 用户追求类型安全 + 优化的最佳选择。适合复杂领域模型、需要编译时错误检查的场景。

> ⚡ 实际开发中，DataFrame/DataSet 是主流推荐，RDD 仅在特殊场景下使用。

---

## 2. Spark 中 Shuffle 是什么？为什么它是性能瓶颈？如何优化？

**答案：**

**什么是 Shuffle：**
Shuffle 是 Spark 在执行宽依赖算子（如 `groupByKey`、`reduceByKey`、`join`、`repartition`）时，需要将数据重新分区并跨节点传输的过程。每个 Map 任务的输出需要按 Key 重新分区，发送到对应的 Reduce 任务。

**为什么是瓶颈：**
1. **磁盘 I/O**：Shuffle 数据需要写入磁盘再读取
2. **网络 I/O**：大量数据跨节点传输
3. **内存压力**：需要缓冲大量中间数据，可能触发 GC 甚至 OOM
4. **序列化/反序列化开销**：数据需要编码解码

**优化策略：**
1. **减少 Shuffle 数据量**：使用 `reduceByKey` 替代 `groupByKey`（先在 Map 端聚合）
2. **使用 Broadcast Join**：小表广播避免 Shuffle
3. **调整分区数**：合理设置 `spark.sql.shuffle.partitions`（默认 200），避免分区过多或过少
4. **启用 Map-Side 聚合**：利用 CombineByKey 系列算子
5. **使用 Kryo 序列化**：比 Java 序列化体积小、速度快
6. **AQE（Adaptive Query Execution）**：Spark 3.x 自动优化 Shuffle 分区数和 Join 策略

---

## 3. Spark 的内存管理机制是怎样的？堆内内存和堆外内存如何分配？

**答案：**

Spark 1.6+ 采用**统一内存管理**（Unified Memory Management），将 Executor 内存分为以下区域：

```
Executor Memory
├── Reserved Memory (300MB，固定)
├── User Memory (1 - spark.memory.fraction)
│   └── 用户数据结构、Spark 内部元数据
└── Spark Memory (spark.memory.fraction, 默认 0.6)
    ├── Storage Memory (spark.memory.storageFraction, 默认 0.5)
    │   └── 缓存数据（cache/persist）、广播变量、Unroll
    └── Execution Memory (剩余 0.5)
        └── Shuffle、Join、Sort、Aggregation 中间数据
```

**核心设计：**
- Storage 和 Execution 内存**可以互相借用**
- Execution 优先级高于 Storage（Execution 可驱逐 Storage，反之不行）
- 借用的 Storage 内存，数据不会被丢弃，只是停止访问

**堆外内存（Off-Heap）：**
- 通过 `spark.memory.offHeap.enabled=true` 启用
- 由 `spark.memory.offHeap.size` 控制大小
- 避免 GC 暂停，Tungsten 引擎大量使用
- 数据以二进制形式存储，序列化/反序列化开销更低

**调优建议：**
- 频繁 GC → 增大 Executor 内存或启用 Off-Heap
- 频繁 Cache 淘汰 → 增大 `spark.memory.storageFraction`
- Shuffle Spill 严重 → 增大 `spark.memory.fraction` 或 Executor 内存

---

## 4. 广播变量（Broadcast Variable）和累加器（Accumulator）分别是什么？使用时需要注意什么？

**答案：**

### 广播变量（Broadcast Variable）
- **作用**：将只读变量高效地分发到每个 Executor，而非每个 Task 复制一份
- **原理**：使用高效的广播算法（如 BitTorrent 风格），数据只传输一次并缓存在 Executor 级别
- **典型场景**：广播小表做 Map-side Join、广播配置字典/查找表

```scala
val lookupTable = sc.broadcast(Map("a" -> 1, "b" -> 2))
rdd.map(x => lookupTable.value(x))
```

**注意事项：**
1. 广播变量是**不可变的**，修改不会同步到其他节点
2. 广播后不应再修改原始变量
3. 广播大变量会导致 Executor 内存压力，建议配合 Kryo 序列化
4. 使用 `unpersist()` 释放不再需要的广播变量

### 累加器（Accumulator）
- **作用**：实现分布式环境下的"只增"聚合操作，Driver 端读取结果
- **原理**：各 Task 累加后，结果汇总到 Driver
- **典型场景**：计数器、错误统计、数据质量校验

```scala
val errorCount = sc.longAccumulator("ErrorCounter")
rdd.foreach { x =>
  if (isError(x)) errorCount.add(1)
}
println(errorCount.value)
```

**注意事项：**
1. 在 Transformation 中使用累加器时，若 Task 失败重试，累加器可能被**重复计数**
2. 在 Action 中使用是安全的（只执行一次）
3. 自定义累加器需实现 `AccumulatorV2` 接口
4. 累加器的值只能在 Driver 端读取，Worker 端看不到

---

## 5. Spark 的 DAG 和 Stage 是如何划分的？Task 调度流程是怎样的？

**答案：**

### DAG（有向无环图）
当用户提交 Spark 应用时，Driver 中的 DAGScheduler 会根据 RDD 的依赖关系构建 DAG：
- **窄依赖（Narrow Dependency）**：父 RDD 的每个分区最多被子 RDD 的一个分区使用（如 map、filter、union）
- **宽依赖（Wide Dependency）**：父 RDD 的分区可能被子 RDD 的多个分区使用（如 groupByKey、reduceByKey、join）

### Stage 划分
- DAGScheduler 以**宽依赖为边界**将 DAG 切分为多个 Stage
- 同一个 Stage 内的算子可以**流水线化（pipelined）**执行，无需 Shuffle
- 下游 Stage 必须等待上游 Stage 完成（需要 Shuffle 数据）

```
Stage 0 (map, filter)  →  Shuffle Write
         ↓
Stage 1 (reduceByKey)  →  Shuffle Write
         ↓
Stage 2 (sort, saveAsTextFile)
```

### Task 调度流程
1. **Application 提交** → Driver 启动，创建 SparkContext
2. **Job 触发** → 遇到 Action 算子（如 collect、save）时触发 Job
3. **DAG 构建** → DAGScheduler 根据 RDD 依赖构建 DAG
4. **Stage 切分** → 按宽依赖切分为多个 Stage（ShuffleMapStage / ResultStage）
5. **TaskSet 提交** → TaskScheduler 为每个 Stage 创建 TaskSet
6. **Task 分配** → SchedulerBackend 将 Task 分配到 Executor
7. **Task 执行** → Executor 执行 Task，结果返回 Driver 或写入 Shuffle 文件
8. **结果收集** → ResultStage 的结果返回 Driver

**关键参数：**
- `spark.scheduler.mode`：FAIR（公平调度）或 FIFO（先进先出，默认）
- `spark.task.cpus`：每个 Task 使用的 CPU 核数（默认 1）
- `spark.locality.wait`：数据本地性等待时间（默认 3s）

---

> 📅 生成日期：2026-08-28
> 🔗 推荐阅读：
> - [Apache Spark 官方文档](https://spark.apache.org/docs/latest/)
> - [Spark Internals - GitBook](https://jaceklaskowski.gitbooks.io/mastering-apache-spark/)
> - [Databricks Blog](https://www.databricks.com/blog)
