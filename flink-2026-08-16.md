# 程序员面试题 - 2026-08-16

## 🔥 Flink 核心知识

---

### 题目1：Flink 的架构是怎样的？JobManager 和 TaskManager 的职责是什么？

【答案】

**Flink 架构采用主从模式**，核心组件包括 JobManager、TaskManager 和 ResourceManager。

**JobManager（主节点）**：
- 负责整个作业的调度协调
- 接收用户提交的 Flink 作业（JobGraph）
- 将 JobGraph 转换为 ExecutionGraph，决定并行执行计划
- 协调 Checkpoint 机制
- 故障恢复时重新调度任务
- 包含三个核心组件：
  - **Dispatcher**：接收用户提交的作业，启动 JobMaster
  - **ResourceManager**：管理 TaskManager 资源槽位（Slot）
  - **JobMaster**：负责 ExecutionGraph 的执行，协调单个作业

**TaskManager（从节点）**：
- 负责实际的数据处理任务执行
- 以 Slot 为单位管理资源，每个 TaskManager 有一到多个 Slot
- 每个 Slot 可以运行一个或多个任务（子任务）
- 与 JobManager 保持心跳，汇报任务状态
- 数据在 TaskManager 之间进行 Shuffle 传输

**工作流程**：
```
用户代码 → StreamGraph → JobGraph → ExecutionGraph
                                    ↓
              JobManager 调度到 TaskManager 执行
                                    ↓
              TaskManager 通过 Slot 执行 Task
```

**Slot 资源管理**：
- Slot 数量 = TaskManager CPU 核心数（默认）
- Slot 之间内存是隔离的，但 CPU 是共享的
- Flink 支持 Slot 共享，同一作业的子任务可以共享一个 Slot

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/zh/docs/concepts/overview/

---分割线---

### 题目2：Flink 的 Watermark 是什么？如何解决乱序和迟到数据问题？

【答案】

**Watermark（水位线）是 Flink 处理乱序事件的核心机制**，本质上是一个时间戳，用于告诉算子"此时间之前的数据都已经到齐了"。

**为什么需要 Watermark**：
流处理中，由于网络延迟、分布式系统等因素，数据的到达顺序往往和事件时间（Event Time）不一致，这就是乱序问题。Watermark 机制让算子能够安全地处理迟到数据。

**Watermark 生成方式**：

1. **周期性生成**（常用）：
```java
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofSeconds(10))
    .withTimestampAssigner(...)
```
设置最大乱序容忍时间为 10 秒。

2. **标点式生成**：
在遇到特定事件时生成 Watermark，比如收到特定的订单完成事件才推进水位线。

**Watermark 传播机制**：
- Watermark 从 Source 算子向下游传递
- 算子收到多个输入流时，取各流 Watermark 的最小值（因为要保证所有流都到齐）
- 当 Watermark 超过窗口结束时间时，触发窗口计算

**窗口与 Watermark 的关系**：
```
假设窗口 [10:00, 10:05)，Watermark = 10:05
当 Watermark >= 10:05 时，窗口闭合，触发计算
```

**迟到数据处理策略**：

1. **Allowed Lateness（允许迟到）**：
```java
.window(TumblingEventTimeWindows.of(Time.minutes(5)))
.allowedLateness(Time.minutes(2))
```
窗口触发后，仍然保留 2 分钟，迟到数据会触发窗口增量计算。

2. **侧输出流（Side Output）**：
```java
OutputTag<String> lateTag = new OutputTag<>("late-data"){};
...
.lateDataOutputTag(lateTag)
```
超过允许迟到时间的数据被发送到侧输出流，避免丢失。

**Watermark 对齐的影响**：
- 多流输入时，快流需要等待慢流，影响处理速度
- 可以使用 WatermarkStrategy.withIdleness() 处理空闲数据源

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/zh/docs/concepts/time_model/

---分割线---

### 题目3：Flink 的 Checkpoint 和 Savepoint 有什么区别？如何保证 Exactly-Once 语义？

【答案】

**Checkpoint 和 Savepoint 的区别**：

| 特性 | Checkpoint | Savepoint |
|------|------------|-----------|
| 触发方式 | Flink 自动周期性执行 | 手动触发 |
| 用途 | 故障恢复 | 计划性停机、版本升级 |
| 删除机制 | 作业删除时自动清理 | 手动删除 |
| 存储位置 | 由 Flink 管理 | 用户指定 |
| 格式 | 作业相关 | 标准格式，可跨作业使用 |

**Checkpoint 原理**：
1. JobManager 定期向所有算子发送 Checkpoint 触发消息
2. 算子收到消息后，先做 Barrier 对齐（Barrier Alignment）
3. 将状态快照（State）写入配置的存储后端（如 HDFS、RocksDB）
4. 快照完成后通知 JobManager

**Barrier 对齐机制（Exactly-Once 保证的关键）**：
```
TaskManager 输入流: [barrier-1, data, barrier-2, data, ...]
                         ↓
              收到 barrier-1 后，阻塞后续数据
              将 barrier-1 之前的数据处理完
              快照 barrier-1 位置的状态
              然后处理 barrier-2 之后的数据
```

**两种一致性级别**：
- **Exactly-Once**：所有数据都被处理且仅处理一次（Flink 通过两阶段提交实现）
- **At-Least-Once**：数据可能被处理多次，不会丢失

**两阶段提交（TwoPhaseCommitSink）实现 Exactly-Once**：
1. **预提交阶段**：数据处理完成后，先写入状态但不确定提交
2. **提交阶段**：Checkpointer 确认所有算子快照完成，触发正式提交
3. **故障恢复**：从最近成功的 Checkpoint 恢复，重新处理未提交的数据

**常见 Connector 的语义支持**：
| Connector | 语义 | 原因 |
|-----------|------|------|
| Kafka Source | Exactly-Once | 支持 offset 提交和 Checkpoint 协调 |
| Kafka Sink | Exactly-Once | 两阶段提交 |
| File Sink | Exactly-Once | 幂等写入 |
| MySQL Sink | At-Least-Once | 需要业务侧幂等配合 |
| Elasticsearch | At-Least-Once | 不支持事务 |

**Savepoint 手动触发**：
```bash
bin/flink savepoint <jobId> [targetDirectory]
```
用于：集群升级、作业迁移、A/B 测试

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/zh/docs/ops/state_checkpointing/

---分割线---

### 题目4：Flink 的窗口函数有哪些？Tumbling、Sliding、Session Window 的区别和使用场景？

【答案】

**Flink 支持三种窗口类型**：

**1. 时间窗口（Time Windows）**
- Tumbling Window（滚动窗口）：无重叠、固定大小
- Sliding Window（滑动窗口）：有重叠
- Session Window（会话窗口）：动态大小，间隙触发

**2. 计数窗口（Count Windows）**
- Tumbling Count Window
- Sliding Count Window

**滚动窗口（TumblingWindow）**：
```java
// 每5分钟一个窗口，无重叠
stream.keyBy(...).window(TumblingEventTimeWindows.of(Time.minutes(5)))
```
- 特点：窗口首尾相接，不重叠
- 场景：固定周期的聚合统计，如"每小时销售额"

**滑动窗口（SlidingWindow）**：
```java
// 窗口大小10分钟，滑动步长5分钟
stream.keyBy(...).window(SlidingEventTimeWindows.of(Time.minutes(10), Time.minutes(5)))
```
- 特点：窗口有重叠，可以更平滑地输出结果
- 场景：移动平均、趋势分析，如"最近10分钟平均CPU使用率，每5秒更新一次"

**会话窗口（SessionWindow）**：
```java
// 会话间隔超过3分钟则开启新窗口
stream.keyBy(...).window(EventTimeSessionWindows.withGap(Time.minutes(3)))
```
- 特点：窗口大小不固定，由数据之间的间隔决定
- 场景：用户行为分析，如"用户活跃会话期间的行为统计"

**全局窗口（GlobalWindow）**：
```java
stream.keyBy(...).window(GlobalWindows.create())
```
将所有相同 key 的数据分配到同一个窗口，需要配合 Trigger 使用。

**增量聚合 vs 全量聚合**：
```java
// 增量聚合：每条数据都更新状态，窗口关闭时输出
.reduce((a, b) -> a + b)

// 全量聚合：收集所有数据到内存，窗口关闭时计算
.apply(new WindowFunction<...>() { ... })
```

**窗口实战建议**：
- EventTime 窗口必须设置 Watermark，否则窗口永远不会触发
- 优先使用增量聚合，减少内存占用
- 迟到数据过多时，考虑使用侧输出流分离处理
- Session Window 的 gap 设置要合理，过小会产生过多碎片窗口

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/zh/docs/dev/datastream/operators/windows/

---分割线---

### 题目5：Flink 的状态管理机制是怎样的？StateBackend 如何选择？状态过期如何处理？

【答案】

**Flink 状态管理分为两大类**：

**1. Keyed State（键控状态）**
- 作用域绑定到当前处理的 key
- 只有 KeyedStream 才可以使用
- 类型：
  - ValueState<T>：单一值
  - ListState<T>：列表
  - MapState<K, V>：键值对
  - ReducingState<T>：聚合后结果
  - AggregatingState<I, O>：聚合中间状态

**2. Operator State（算子状态）**
- 作用域是整个算子实例，所有 key 共享
- 支持 ListState（列表）、UnionListState（广播）、BroadcastState（广播状态）
- 常见于 Source 算子（如 Kafka offset）

**状态使用示例**：
```java
public class CountWindowAverage extends RichFlatMapFunction<Long, Double> {
    private ValueState<Long> count;
    private ValueState<Long> sum;

    @Override
    public void open(Configuration parameters) {
        count = getRuntimeContext().getState(
            new ValueStateDescriptor<>("count", Long.class));
        sum = getRuntimeContext().getState(
            new ValueStateDescriptor<>("sum", Long.class));
    }

    @Override
    public void flatMap(Long input, Collector<Double> out) {
        Long currentCount = count.value() == null ? 0L : count.value();
        Long currentSum = sum.value() == null ? 0L : sum.value();
        
        count.update(currentCount + 1);
        sum.update(currentSum + input);
        
        if (count.value() >= 2) {
            out.collect((double) sum.value() / count.value());
            count.clear();
            sum.clear();
        }
    }
}
```

**StateBackend 选择**：

| StateBackend | 存储介质 | 适用场景 |
|-------------|---------|---------|
| HashMapStateBackend | JVM Heap | 小状态、低延迟，数据量 < 10GB |
| EmbeddedRocksDBStateBackend | RocksDB（本地磁盘） | 大状态（TB级）、增量 Checkpoint |

**RocksDB 是大状态场景的首选**：
- 将状态序列化后存储在 RocksDB 中
- 支持增量 Checkpoint，减少 checkpoint 时间
- 异步快照，不阻塞数据处理
- 适合 Flink SQL 大状态 join 场景

**状态过期（TTL）处理**：
```java
StateTtlConfig ttlConfig = StateTtlConfig.newBuilder(Time.days(7))
    .setUpdateType(StateTtlConfig.UpdateType.OnReadAndWrite)
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired)
    .build();

ValueStateDescriptor<String> stateDescriptor =
    new ValueStateDescriptor<>("my-state", String.class);
stateDescriptor.enableTimeToLive(ttlConfig);
```
- **OnReadAndWrite**：读写时都更新 TTL
- **OnCreateAndWrite**：仅创建时更新 TTL
- **NeverReturnExpired**：不返回过期数据（默认），会在后台清理

**状态大小估算**：
- 状态大小影响 Checkpoint 时间和恢复时间
- Key 越多，状态越大
- 建议监控 state.size metrics
- 大状态场景优先选择 RocksDB

**最佳实践**：
1. 合理设计 key，避免 key 过多导致状态爆炸
2. 使用 RocksDB 处理大状态
3. 定期清理不需要的状态
4. 避免在状态中存储大对象
5. 使用 ListState 而非 ValueState<List> 以获得更好性能

📖 来源：https://nightlies.apache.org/flink/flink-docs-release-1.18/zh/docs/dev/datastream/fault_tolerance/state/

---分割线---

## 📚 更多资源

- Apache Flink 官方文档：https://flink.apache.org/zh/
- Flink 中文社区：https://flink-cn.org/
- 面试题仓库：https://github.com/qq286158530/interview-questions
