# Spark 面试题（2026-08-19）

> 本文档整理 5 道高频 Spark 面试题，涵盖核心知识点，适合大数据工程师面试准备。

---

## 题目一：Spark 的架构设计与核心组件

### 问题
请描述 Apache Spark 的整体架构，核心组件有哪些？Driver、Executor、Worker 之间是什么关系？

### 答案

Spark 采用 **主从架构**，核心组件如下：

| 组件 | 角色 | 说明 |
|------|------|------|
| **Driver** | 主节点 | 运行 `main()` 方法，创建 `SparkContext`，负责任务调度（将 Job 划分为 Stage 和 Task） |
| **Executor** | 从节点 | 每个 Worker 节点上的进程，负责执行分发的 Task，以线程池方式并行处理 |
| **Worker** | 节点管理 | 管理本节点上的 Executor 进程，向 Master 汇报资源状态 |
| **Master** | 集群调度 | 协调资源分配，决定在哪些 Worker 上启动 Executor |

**工作流程：**
1. 用户提交 Spark 作业，Driver 启动并创建 `SparkContext`。
2. `SparkContext` 向 Cluster Manager（如 YARN/Kubernetes/Standalone）申请 Executor 资源。
3. Master 将资源分配给 Worker，Worker 启动 Executor 进程。
4. Driver 将应用代码发送给 Executor，并发送 Task 给 Executor 并行执行。
5. Executor 执行完 Task 后，将结果返回给 Driver。
6. Driver 根据 Task 结果决定是否触发进一步调度（如 Shuffle）。

**与 MapReduce 的区别：** MapReduce 是进程级并行，而 Spark 是线程级并行（同一 Executor 内的多 Task 共享内存），因此 Spark 调度开销更小。

---

## 题目二：RDD 的特性与算子分类

### 问题
什么是 RDD？RDD 有哪些核心特性？Transformation 和 Action 算子的区别是什么？

### 答案

**RDD（Resilient Distributed Dataset）** 是 Spark 最底层的抽象，表示一个弹性的、分区的、可并行计算的数据集合。

### RDD 的五大核心特性

| 特性 | 说明 |
|------|------|
| **分区列表（Partitions）** | 数据被切分成多个分区，每个分区是并行计算的最小单位 |
| **计算函数（Compute Function）** | 每个分区可以执行相同的计算逻辑 |
| **依赖关系（Dependencies）** | RDD 之间存在依赖关系（窄依赖 / 宽依赖），决定 DAG 的切分方式 |
| **分区器（Partitioner）** | 可选，决定 Shuffle 时数据的分区策略（如 HashPartitioner、RangePartitioner） |
| **位置偏好（Preferred Locations）** | 可选，调度时优先将计算移到数据所在节点（数据本地性） |

### Transformation vs Action

| | Transformation | Action |
|---|---|---|
| **返回值** | 新的 RDD（懒执行） | 非 RDD 类型（触发实际计算） |
| **执行时机** | 不会立即执行，只是构建 DAG | 遇到 Action 才会触发 Job 执行 |
| **示例** | `map`、`filter`、`flatMap`、`groupByKey`、`join` | `collect`、`count`、`reduce`、`saveAsTextFile`、`take` |

**重要特性——懒执行（Lazy Evaluation）：** Transformation 不会立即执行，只有遇到 Action 时，Driver 才会生成 DAG 并提交任务。这种设计避免了重复计算，也给 Spark 提供了全局优化的空间。

---

## 题目三：Spark Shuffle 的原理与优化

### 问题
什么是 Shuffle？为什么 Spark 需要 Shuffle？Shuffle 过程中数据是如何存储和传输的？

### 答案

**Shuffle** 是将数据按照 Key 进行重新分区、聚合的操作，常见于 `groupByKey`、`reduceByKey`、`join`、`sortByKey` 等算子。它打破了数据本地性，需要跨节点传输数据。

### Shuffle 的执行流程（Spark 2.x 之后基于 Sort-based Shuffle）

1. **Map 端：**
   - 每个 Task 将数据写入本地内存缓冲区（Buffer），按 Key 进行分区。
   - 缓冲区满时，将数据溢写（Spill）到磁盘，排序后形成多个 Segment 文件。
   - 所有数据写完后，将这些文件合并为一个索引文件 + 数据文件（`*.index` + `*.data`）。

2. **Reduce 端：**
   - Reduce Task 根据分区信息（由 MapTask 输出文件的 Index 文件描述），通过网络抓取（Fetch）对应分区的数据。
   - 抓取来的数据先放在内存缓冲区，满了则溢写到磁盘，最终合并排序后进行聚合计算。

### Shuffle 的主要开销

- **磁盘 I/O**：溢写和合并过程产生大量磁盘读写。
- **网络 I/O**：跨节点数据传输。
- **内存压力**：缓冲区、合并操作都会消耗内存，可能引发 OOM 或频繁 GC。

### Shuffle 优化建议

- **减少 Shuffle 数据量**：业务层面尽量过滤、提前聚合。
- **合理设置分区数**：分区过多会增加小文件开销，分区过少会导致数据倾斜；推荐 `200~2000` 之间。
- **开启 Shuffle 压缩**：`spark.shuffle.compress=true`（默认开启）。
- **使用 HashShuffle Manager**（小规模场景）：`spark.shuffle.manager = hash`（已被 Spark 2.x 废弃）。
- **避免 Shuffle 倾斜**：检查 Key 分布，使用加盐、随机前缀等方法打散热点 Key。

---

## 题目四：Spark 广播变量与内存管理机制

### 问题
什么是广播变量？Spark 的内存模型是怎样的？Executor 内存是如何分配的？

### 答案

### 广播变量（Broadcast Variable）

广播变量用于将 **只读的、较大的共享数据** 发送到每个 Executor，只发送一次，避免每个 Task 都复制一份，从而节省大量网络传输和内存。

```python
# 创建广播变量
broadcast_var = sc.broadcast({"status": "OK", "code": 200})

# 在算子中使用（无需 driver 再次传递）
def process(row):
    return row.id in broadcast_var.value
```

- `broadcast.value` 在 Executor 端访问，只读。
- 广播变量存储在 **堆内内存**（Executor JVM Heap），大小受 `spark.driver.memory` 和 `spark.executor.memory` 限制。
- 广播后 Executor 端各 Task 共享同一份数据副本。

### Spark 内存模型（Executor 端）

Executor JVM 堆内存分为两大区域：

```
+----------------------------+
|      堆内内存 (on-heap)      |
|  +----------------------+  |
|  |   Memory Manager     |  |
|  | +---------+---------+|  |
|  | | Execution| Storage |  |
|  | |  内存    |  内存   |  |
|  | +---------+---------+|  |
|  +----------------------+  |
+----------------------------+
```

| 区域 | 用途 | 特点 |
|------|------|------|
| **Execution 内存** | Shuffle 排序、Hash 聚合、缓存中间结果 | 借用机制：空间不足时可临时借用 Storage 内存 |
| **Storage 内存** | 缓存 RDD/Broadcast 数据、序列化数据 | 可借用回 Execution，但有上限 `spark.storage.memoryFraction` |

**相关参数：**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `spark.executor.memory` | 1g | Executor 总堆内存 |
| `spark.memory.fraction` | 0.6 | 通用内存占 Executor 内存的比例 |
| `spark.memory.storageFraction` | 0.5 | Storage 内存占通用内存的比例 |
| `spark.executor.memoryOverhead` | executorMemory × 0.1 | 堆外内存（用于 JVM 元数据、代码缓存等） |

**堆外内存（Off-heap）：** 通过 `spark.memory.offHeap.enabled=true` 开启，使用 `spark.memory.offHeap.size` 指定大小，可避免 JVM GC 开销。

---

## 题目五：DataFrame 与 DataSet 的区别及优势

### 问题
Spark SQL 中的 DataFrame 和 DataSet 有什么区别？它们相比 RDD 有什么优势？

### 答案

### DataFrame vs DataSet

| 特性 | DataFrame | DataSet |
|------|-----------|---------|
| **语言支持** | 跨语言（Scala/Java/Python/SQL/R） | 仅 Scala/Java（强类型语言） |
| **类型安全** | 无编译时类型检查（Row 型，弱类型） | 有编译时类型检查（强类型） |
| **底层表示** | `Row[]` + Schema（元数据） | `T[]`（JVM 对象数组）+ Schema |
| **API 风格** | 类似 pandas DataFrame，DSL 风格 | 类似 RDD，需要 case class 定义schema |
| **序列化** | Tungsten（Binary Encoder） | Tungsten（自动生成 Encoder） |

> **注意：** 在 Spark 2.x 中，DataFrame 和 DataSet 已统一——DataFrame 是 `DataSet[Row]` 的别名。

### DataFrame/DataSet 相比 RDD 的优势

1. **自动生成 Schema，无需手动定义**
   ```python
   # RDD 需要手动指定 schema
   rdd = sc.textFile("people.csv").map(lambda x: (x.split(",")[0], int(x.split(",")[1])))
   
   # DataFrame 自动推断
   df = spark.read.csv("people.csv", header=True, inferSchema=True)
   df.printSchema()
   ```

2. **Tungsten 序列化 + Off-Heap 内存管理**
   - RDD 使用 Java/Kryo 序列化，DataFrame 使用高性能的 Tungsten Binary Encoder，序列化速度更快、体积更小。
   - DataFrame 可使用堆外内存，减少 GC 开销。

3. **Catalyst 优化器——逻辑计划与物理计划**
   - DataFrame/SQL 的操作经过 **Catalyst 优化器** 生成最优执行计划：
     ```
     SQL / DSL 表达式
          ↓
     Unresolved Logical Plan（未解析的逻辑计划）
          ↓
     Logical Plan（解析后的逻辑计划）
          ↓
     Optimized Logical Plan（优化后的逻辑计划）
          ↓
     Physical Plans（多个物理计划）
          ↓
     Cost Model → 选择最优物理计划
          ↓
     RDD 操作（执行）
     ```
   - 常见优化：列剪裁（Column Pruning）、谓词下推（Predicate Pushdown）、常量替换、子查询重写等。

4. **代码可读性与协作效率**
   - DataFrame API 更简洁，DSL 风格让非程序员（分析师）也能上手。
   - SQL 接口让 BI 和临时查询更方便。

5. **性能优势明显**
   - 在大多数场景下，DataFrame 比手写 RDD 更快（得益于优化器），且无需人工调优。

---

## 参考来源

- [Apache Spark 官方文档](https://spark.apache.org/docs/latest/)
- 《Spark The Definitive Guide》— Bill Chambers & Matei Zaharia
- [尚硅谷 Spark 教程](https://www.bilibili.com/video/BV1aZ4y1r7pG)
- [CSDN 大数据频道](https://blog.csdn.net/chongbin007/category_10254624.html)
- [GitHub: interview-questions](https://github.com/qq286158530/interview-questions)

---

*整理 by AI 助手 | 2026-08-19*
