# HBase 面试题 — 2026-08-01

> 本题集整理自网络公开技术资料，涵盖 HBase 架构、RowKey 设计、存储结构、协处理器等核心知识点，适合大数据工程师面试复习与能力巩固。

---

## 题目一：HBase 的架构是怎样的？为什么说它是一个「假排序」的数据库？

### 参考答案

**HBase 架构核心角色：**

1. **ZooKeeper**：负责协调服务，管理 -ROOT- 表位置、Master 选举、RegionServer 状态心跳。
2. **Master**：管理表结构（DDL 操作）、Region 分配、负载均衡、垃圾回收（合并 HFile、清理 MemStore）。**不是数据节点，不参与实际读写**。
3. **RegionServer**：实际数据读写的 Worker 进程，负责：
   - 接收客户端读写请求
   - 管理 MemStore（写缓存）和 HFile（磁盘存储）
   - Minor Compaction 和 Major Compaction

**三层结构（ZooKeeper → -ROOT- → .META.）：**

```
Client → ZooKeeper (-ROOT- 位置)
       → -ROOT- 表（只有一条记录）→ .META. 表（记录各 Region 位置）
       → .META. 表 → RegionServer
```

**「假排序」的核心含义：**

- HBase 按 **RowKey 的字典序**排序存储，查询时利用这个有序性做范围扫描（Scan）。
- 但这种「排序」是基于 LSM-Tree（Log-Structured Merge-Tree）结构的，**数据先写内存（MemStore）**，达到阈值后才刷盘成 HFile，**磁盘上的 HFile 之间的数据是无序合并的**。
- 换句话说：查询时 HBase 能利用 RowKey 有序性做快速定位，但底层数据文件是顺序追加写的，这正是 LSM-Tree 的设计精髓——**写有序，存储无序**。

> 📎 来源：HBase 官方文档 — "Architecture"  
> https://hbase.apache.org/book.html#architecture

---

## 题目二：HBase 的 RowKey 设计有哪些原则？为什么 RowKey 设计是 HBase 性能优化的关键？

### 参考答案

**RowKey 设计四大原则：**

1. **长度适中**：建议 16~100 字节，越短越好。HBase 的 KeyValue 中 RowKey 被反复存储，冗余很大，太长浪费内存和磁盘。
2. **散列化（避免热点）**：热点 = 大量请求集中到少量 RegionServer 上。措施：
   - 将高基数字段（如用户 ID）放在 RowKey 前面
   - 使用盐（Salting）：在 RowKey 前面加随机前缀
   - 使用哈希（如 MD5）打散：`MD5(userId).substring(0,4) + userId`
3. **唯一性**：RowKey 必须唯一定位一行数据。
4. **业务导向**：结合查询模式设计。
   - 如果经常按时间范围查：时间戳放后面，如 `userId#20260801`
   - 如果经常按用户查：`userId#eventId`

**为什么 RowKey 是性能关键：**

- HBase 按 RowKey 分 Region，如果 RowKey 设计不当导致热点，所有读写集中在单台 RegionServer，会造成**读写瓶颈和 GC 压力**。
- RowKey 决定数据分布（预分区）和查询效率（Scan 范围）。
- 一旦 RowKey 设计确定，改动代价极大（需要数据迁移）。

> 📎 来源：HBase 官方文档 — "RowKey Design"  
> https://hbase.apache.org/book.html#rowkey.design

---

## 题目三：描述 HBase 的写流程（写入 MemStore 的过程），以及 MemStore 和 HFile 的关系。

### 参考答案

**HBase 写流程：**

```
1. Client 先访问 ZooKeeper，获取 -ROOT- → .META. → 目标 Region 位置
2. Client 直接向目标 RegionServer 发送写请求（缓存元数据，后续请求无需再查 ZooKeeper）
3. RegionServer 将数据写入 WAL（Write-Ahead Log，先写日志再写内存）
4. 数据写入 MemStore（按 RowKey 排序的内存结构）
5. 返回客户端写入成功
```

**MemStore 的作用：**

- MemStore 是 **RegionServer 内存中的有序结构**（跳表实现），按 RowKey + ColumnFamily + Qualifier + Timestamp 排序。
- 写操作先落 WAL 保证持久性，数据在 MemStore 中有序排列，等待刷盘。

**MemStore → HFile 的刷盘机制（Flush）：**

- 当 MemStore 达到阈值（默认 128MB，单个 MemStore 达到 `hbase.hregion.memstore.flush.size`），触发 flush 线程将数据刷成磁盘上的 HFile。
- 刷盘时数据按列族分开存储，每个 HFile 内数据按键值有序排列。
- **Major Compaction** 会合并多个 HFile 为一个，清理过期版本和删除标记。

**LSM-Tree 的核心思想：**

```
写操作 → WAL（顺序写）→ MemStore（内存有序）
              ↓ 刷盘
          HFile（磁盘有序文件）
              ↓ Major Compaction 合并
          更大的有序 HFile
```

这就是 HBase 写性能高的原因：**写操作是顺序写 WAL + 内存写，比随机磁盘 IO 快几个数量级**。

> 📎 来源：HBase 官方文档 — "Write Path"  
> https://hbase.apache.org/book.html#writepath

---

## 题目四：HBase 的协处理器（Coprocessor）是什么？有哪些类型？各自的使用场景是什么？

### 参考答案

**协处理器的本质：**

协处理器是运行在 RegionServer 上的自定义代码，允许用户在**数据写入/读取/查询**的关键路径上注入自定义逻辑，类似数据库的触发器/存储过程。

**两大类型：**

1. **Observer Coprocessor（观察者型）**
   - 类似触发器，在特定事件前后执行
   - 三种子类：
     - **PreGet / PostGet**：在 GET 读取前/后执行
     - **PrePut / PostPut**：在 PUT 写入前/后执行（可修改数据或做权限校验）
     - **PreDelete / PostDelete**：在 DELETE 前/后执行
   - 场景举例：
     - 实现列级权限控制（PostPut 校验用户权限）
     - 实现二级索引（写数据时同步更新索引表）
     - 审计日志（记录谁在什么时候改了什么数据）

2. **Endpoint Coprocessor（端点型）**
   - 类似存储过程，在 RegionServer 上执行自定义计算逻辑
   - 客户端调用 `coprocessorService()` 在每个 Region 上并行执行，最后汇总结果
   - 场景举例：
     - 聚合查询（SUM、COUNT、AVG）在 RegionServer 端执行，减少网络传输
     - 复杂计算（位图运算、Filter 下推）

**协处理器的限制/注意事项：**

- 协处理器代码异常会导致 RegionServer 崩溃（可配置白名单和异常处理）
- 不要在 Observer 中做耗时操作，会阻塞读写链路
- 加载方式：指定 table、table + column family、或全局加载

> 📎 来源：HBase 官方文档 — "Coprocessors"  
> https://hbase.apache.org/book.html#cp

---

## 题目五：HBase 如何保证数据的最终一致性？它与 CAP 定理的关系是什么？

### 参考答案

**HBase 的一致性模型：**

HBase 本身是 **强一致性** 的单行操作保证：
- 对单行数据的读写（Get/Put/Delete）在同一个 RegionServer 内是原子操作，MVCC 机制保证读取的是某个一致快照。
- 但 **Scan 操作是最终一致的**：不同 RegionServer 返回的数据时间点可能略有差异。

**与 CAP 定理的关系：**

CAP 定理指出：分布式系统无法同时满足 Consistency（一致性）、Availability（可用性）、Partition Tolerance（分区容错性）。

HBase 的选择是 **CP（一致性 + 分区容错性）**：
- 当网络分区发生时，HBase 优先保证一致性——受影响的 Region 会不可用，直到分区恢复。
- ZooKeeper 负责监控 RegionServer 状态，分区后 Master 会将故障 Region 迁移到其他节点。
- 在正常情况下（无网络分区），HBase 提供高可用性。

**BR（商业可靠）一致性保证的实现：**

1. **WAL（Write-Ahead Log）**：写操作先落日志，RegionServer 崩溃后可通过 WAL 恢复未刷盘的数据。
2. **MemStore 刷盘机制**：定期将内存数据刷成 HFile，保证数据持久化。
3. **Minor / Major Compaction**：合并 HFile，清理过期数据和墓碑标记（逻辑删除）。
4. **MVCC**：多版本并发控制，写操作生成新版本，读操作根据时间戳或版本号读取对应快照。
5. **Region 恢复**：Master 监控到 RegionServer 宕机后，重新分配 Region 并恢复数据。

**什么场景下 HBase 的一致性可能「变弱」？**

- 大规模 Compaction 时短暂的数据不可见
-  Region split/merge 过程中
-  BulkLoad 导入数据时

> 📎 来源：HBase 官方文档 — "Consistency"  
> https://hbase.apache.org/book.html#consistency

---

*整理：小憨宝 | 适合大数据/HBase 工程师面试复习*
