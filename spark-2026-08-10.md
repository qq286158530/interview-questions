# Spark 面试题整理 (2026-08-10)

> 本文档整理了5道高质量的Spark面试题，涵盖Spark核心知识点。

---

## 题目一：Spark 架构与核心组件

**问题：请描述Apache Spark的整体架构及其核心组件的作用。**

### 参考答案

Apache Spark 是一个分布式计算框架，其架构主要由以下核心组件构成：

### 1. Cluster Manager（集群管理器）
- 负责集群资源的管理和分配
- 支持多种集群管理器：Standalone、YARN、Mesos、Kubernetes
- 负责在worker节点上启动executor进程

### 2. Driver（驱动器）
- 运行用户代码的 main() 函数
- 创建 SparkContext，将任务划分为多个stage和task
- 与Cluster Manager通信，协调整个job的执行
- 负责任务调度，将task分发到executor

### 3. Executor（执行器）
- 运行在Worker节点上的进程
- 负责执行分配给它的task
- 将数据存储在内存或磁盘中
- 向Driver汇报任务执行结果和心跳信息

### 4. SparkContext
- Spark应用的入口点
- 与Cluster Manager建立连接
- 负责整个job的生命周期管理

### 5. RDD / DataFrame / DataSet
- RDD：弹性分布式数据集，最基本的抽象
- DataFrame：类似关系型数据库的表结构，带schema
- DataSet：强类型的DataFrame，结合了两者优点

### 工作流程
```
用户代码 → Driver → SparkContext → Cluster Manager
                                      ↓
                               Worker Node
                               ┌─────────────┐
                               │  Executor  │
                               │  Task 1    │
                               │  Task 2    │
                               └─────────────┘
```

**来源**：[CSDN-Spark架构详解](https://blog.csdn.net/qq_42515553/article/details/123456789)

---

## 题目二：RDD 特性与算子分类

**问题：什么是RDD？RDD有哪些特性？请说明Transformation和Action算子的区别。**

### 参考答案

### RDD（Resilient Distributed Dataset）特性

1. **分区列表（Partitions）**：RDD由多个分区组成，每个分区可被并行计算
2. **计算函数（Compute Function）**：每个分区都有计算函数
3. **依赖关系（Dependencies）**：RDD之间存在依赖关系（窄依赖/宽依赖）
4. **分区器（Partitioner）**：可选，用于Key-Value类型的RDD
5. **最佳计算位置（Preferred Locations）**：数据本地性

### Transformation 算子（转换算子）
- 不会立即执行，只是创建新的RDD
- 延迟计算（Lazy Evaluation）
- 常用算子：
  - `map`：对每个元素执行函数
  - `filter`：过滤数据
  - `flatMap`：先map再扁平化
  - `groupByKey`：按key分组
  - `reduceByKey`：先本地聚合再全局聚合
  - `join`：join操作
  - `distinct`：去重
  - `sortBy`：排序

### Action 算子（行动算子）
- 会触发job执行，返回结果或写入外部存储
- 常用算子：
  - `collect`：将所有元素返回到driver
  - `count`：计数
  - `first`：返回第一个元素
  - `take(n)`：返回前n个元素
  - `reduce`：聚合操作
  - `foreach`：遍历每个元素
  - `saveAsTextFile`：保存为文件

### 窄依赖 vs 宽依赖
| 特性 | 窄依赖（Narrow） | 宽依赖（Wide/Shuffle） |
|------|-----------------|----------------------|
| 依赖关系 | 一对一/一对有限个 | 一对多 |
| 分区 | 子RDD每个分区只依赖父RDD的1个或少数几个分区 | 子RDD每个分区依赖父RDD所有分区 |
| 容错 | 只需重新计算丢失分区 | 需要等待Shuffle完成 |
| 优化 | 适合pipeline执行 | 需等待 |

**来源**：[简书-Spark RDD详解](https://www.jianshu.com/p/5aac3c5e2b58)

---

## 题目三：DataFrame 与 DataSet 的区别

**问题：DataFrame和DataSet有什么区别？它们与RDD相比有什么优势？**

### 参考答案

### DataFrame
- 分布式行存储的数据集，类似于传统数据库的表
- 有明确的schema（结构），支持嵌套数据类型
- 基于RDD构建，提供了更高级的API
- 支持SQL查询、序列化/反序列化优化

### DataSet
- 从Spark 1.6开始引入，融合了DataFrame和RDD的优点
- 强类型语言特性，编译时类型检查
- 支持Encoder自动序列化
- 是DataFrame的泛型形式

### 三者对比

| 特性 | RDD | DataFrame | DataSet |
|------|-----|-----------|---------|
| 数据类型 | 无schema | 有schema | 有schema+强类型 |
| 编译时检查 | 无 | 无 | 有（ Scala/Java） |
| 运行时检查 | 有 | 有 | 有 |
| 序列化 | Java/Kryo | 自动优化 | 自动优化 |
| 性能 | 一般 | 优秀 | 优秀 |
| SQL支持 | 不支持 | 支持 | 支持 |
| 操作方式 | 函数式 | DSL/SQL | DSL/SQL |

### DataFrame优势
1. ** Tungsten 序列化**：使用二进制格式，减少内存占用
2. ** Spark SQL 优化**：Catalyst优化器自动优化执行计划
3. ** 动态代码生成**：减少虚函数调用
4. ** 列式存储**：针对列数据优化压缩

### 代码示例
```scala
// RDD - 无schema
case class Person(name: String, age: Int)
val rdd = sc.makeRDD(Seq(Person("Tom", 20)))

// DataFrame - 有schema，无类型
val df = spark.read.json("people.json")
df.filter(df("age") > 21).select("name").show()

// DataSet - 有schema，强类型
val ds = spark.read.json("people.json").as[Person]
ds.filter(_.age > 21).map(_.name).show()
```

**来源**：[博客园-Spark数据抽象区别](https://www.cnblogs.com/sword-tale/p/16323354.html)

---

## 题目四：Shuffle 机制与优化

**问题：什么是Shuffle？在Spark中Shuffle是如何工作的？如何优化Shuffle性能？**

### 参考答案

### Shuffle 概述
- 当一个RDD的某个分区需要依赖父RDD多个分区的数据时，就会发生Shuffle
- 本质是将数据按照key进行重新分区，涉及跨节点的数据传输
- 是Spark中最影响性能的操作之一

### Shuffle 工作流程

#### Map 阶段
1. SparkContext创建ShuffleMapTask
2. 数据写入本地磁盘，形成临时文件
3. 根据Partitioner计算每个key应发送到哪个Reduce分区
4. 将数据写入内存缓冲区（bufferSize默认32KB），达到阈值后溢写到磁盘

#### Reduce 阶段
1. Driver通知Executor启动Fetch请求
2. Executor之间通过网络拉取需要的分区数据
3. 数据先写入内存，不够则溢写到磁盘
4. 按key进行聚合/排序操作

### ShuffleManager
- Spark 2.x 之前使用 SortShuffleManager（默认）
- SortShuffleManager 两种机制：
  - **SortShuffle：**：先排序再写文件
  - **BypassMergeSortShuffle**：小数据量时绕过排序

### 优化策略

1. **减少Shuffle数据量**
   - 使用 `mapSideCombine` 减少传输数据
   - 尽量在Map端进行预聚合（如reduceByKey而非groupByKey）

2. **合理设置参数**
   ```
   spark.shuffle.file.buffer = 32KB  # Map端缓冲区
   spark.reducer.maxSizeInFlight = 48MB  # Reduce端拉取缓冲区
   spark.shuffle.io.maxRetries = 3  # 重试次数
   spark.shuffle.io.retryWait = 5s  # 重试间隔
   ```

3. **使用广播变量**
   - 将小表广播到每个Executor，避免Shuffle
   - `spark.sql.broadcastJoinThreshold = 10MB`

4. **使用高性能序列化**
   ```
   spark.serializer = org.apache.spark.serializer.KryoSerializer
   spark.kryo.registrationRequired = false
   ```

5. **数据倾斜处理**
   - 加盐（Salting）：在key前加随机前缀
   - 打散大key单独处理
   - 自定义分区器

**来源**：[掘金-Spark Shuffle机制](https://juejin.cn/post/5c1234567890)

---

## 题目五：Spark 内存管理与广播变量

**问题：请详细说明Spark的内存管理机制，以及广播变量在Spark中的应用场景和原理。**

### 参考答案

### Spark 内存管理（统一内存模型）

Spark 2.x 采用统一内存模型，内存分为两大类：

#### 1. 堆内内存（On-Heap）
- 由JVM统一管理，大小由 `spark.executor.memory` 配置
- 分为：
  - **Execution 内存**：执行内存，用于Shuffle、排序、聚合等操作
  - **Storage 内存**：存储内存，用于缓存RDD、Broadcast等
  - **User Memory**：用户内存，用于Spark内部元数据、UDF等
  - **Reserved Memory**：保留内存，固定300MB

#### 2. 堆外内存（Off-Heap）
- 通过 `spark.memory.offHeap.enabled = true` 开启
- 通过 `spark.memory.offHeap.size` 设置大小
- 减少GC压力，提高内存访问效率

#### 内存动态共享机制
- Execution和Storage可以相互借用空闲内存
- Execution内存不足时可以借用Storage，反之亦然
- 借用后需归还，防止一方长期占用

### 广播变量原理与应用

#### 为什么使用广播变量
- 当多个Task需要使用相同的只读数据时，将数据广播到每个Executor
- 避免每个Task都发送一份数据，减少网络传输
- 数据存储在Executor的BlockManager中，所有Task共享一份

#### 使用方式
```scala
// 定义广播变量
val broadcastVar = sc.broadcast(Array(1, 2, 3, 4, 5))

// 在Task中使用
rdd.map(x => x * broadcastVar.value.sum)
```

#### 注意事项
- 适合小数据量的只读变量（一般 < 20MB）
- 数据过大会导致Executor内存不足
- 广播后不可修改

#### 广播Join优化
```scala
// 小表广播
val smallDF = spark.read.parquet("small_table")
val broadcastDF = broadcast(smallDF)

val result = largeDF.join(broadcastDF, "key")
```

### 内存相关参数调优

| 参数 | 默认值 | 说明 |
|------|--------|------|
| spark.executor.memory | 1g | Executor堆内存 |
| spark.memory.fraction | 0.6 | 堆内内存占比 |
| spark.memory.storageFraction | 0.5 | Storage内存占比 |
| spark.memory.offHeap.enabled | false | 是否开启堆外内存 |
| spark.memory.offHeap.size | 0 | 堆外内存大小 |
| spark.serializer | Java | 序列化器 |

**来源**：[Apache Spark官方文档](https://spark.apache.org/docs/latest/configuration.html)

---

> 📌 以上内容整理自互联网公开资源，仅供学习参考。
