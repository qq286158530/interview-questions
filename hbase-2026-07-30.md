# HBase 面试题

> 📅 日期：2026-07-30
> 🔖 标签：#HBase #大数据 #面试题

---

## 题目一：HBase 的整体架构是怎样的？请详细说明各组件的作用。

### 参考答案

HBase 采用 **Master-Slave** 架构，主要包含以下组件：

| 组件 | 作用 |
|------|------|
| **ZooKeeper** | 负责选举 Master、存储元数据（如 .META. 表位置）、监控 RegionServer 上下线 |
| **HMaster** | 负责表的管理（创建/删除/修改）、Region 的分配与调度、负载均衡、垃圾回收 |
| **RegionServer** | 负责数据的读写、MemStore 与 HFile 的管理、Region 的合并与分裂 |
| **Region** | HBase 表的数据分片，每个 Region 包含 `[StartKey, EndKey)` 范围内的数据 |
| **WAL (Write-Ahead Log)** | 写操作先写入 WAL，用于宕机后的数据恢复 |

**读写流程：**
1. **写流程**：Client → ZooKeeper → 获取 .META. 位置 → 找到目标 RegionServer → 写入 WAL → 写入 MemStore → 返回成功
2. **读流程**：Client → ZooKeeper → 获取 .META. 位置 → 找到目标 RegionServer → 合并 MemStore 和 HFile 返回结果

---

## 题目二：HBase 中 RowKey 设计的原则有哪些？如何优化 RowKey 设计？

### 参考答案

### RowKey 设计原则

1. **长度原则**：RowKey 长度建议控制在 16 字节以内（最长 64KB），过长的 RowKey 会占用内存和 HBase 存储空间
2. **散列原则**：RowKey 应尽量打散，避免热点（Hot Spotting），可采用 MD5 哈希、加盐（Salting）等方法
3. **唯一性原则**：RowKey 必须唯一，类似于主键
4. **排序原则**：HBase 中数据按 RowKey 字典序排序，设计时需考虑 Scan 范围查询的效率

### 常见优化策略

| 策略 | 说明 | 示例 |
|------|------|------|
| **加盐（Salting）** | 在 RowKey 头部添加随机前缀，使数据分散到不同 Region | `saltaaa`、`saltbbb` |
| **哈希（Hashing）** | 对原始 RowKey 做 MD5 或其他哈希打散 | `MD5(originalKey).substring(0, 16)` |
| **反转（Reversing）** | 反转固定前缀或后缀，避免热点 | 手机号 `13800138000` → `0000383100` |
| **时间戳反转** | 查询最新数据时，可用 `Long.MAX_VALUE - timestamp` | - |

---

## 题目三：HBase 的存储结构是什么？请说明 MemStore 和 HFile 的工作原理。

### 参考答案

### HBase 存储结构层次

```
Table
 └── Region (按 RowKey 范围切分)
      └── Store (按 Column Family 划分)
           ├── MemStore (写缓存)
           └── HFiles (磁盘文件)
```

### MemStore

- **作用**：写操作先将数据写入 MemStore，类似于 HBase 的写缓存
- **大小**：默认 `128MB`（可配置 `hbase.hregion.memstore.flush.size`）
- **刷写时机**：
  1. MemStore 大小达到阈值
  2. WAL 大小超过阈值（`hbase.regionserver.global.memstore.size.lower.limit`）
  3. 定期刷写（`hbase.regionserver.optionalcacheflushinterval`，默认 1 小时）
  4. RegionServer 关闭时
- **特性**：数据按 RowKey 排序后写入 HFile，保证数据有序

### HFile

- **作用**：HBase 中实际存储数据的文件格式，基于 Hadoop 的 TSTable
- **结构**：
  ```
  HFile
   ├── Scanneable Block Index (数据块索引)
   ├── Data Blocks (数据块，默认 64KB)
   ├── Meta Blocks (可选)
   └── File Info (文件信息)
  ```
- **Compaction**：Minor Compaction 合并小文件，Major Compaction 合并所有 HFile 并清理删除数据
- **BlockCache**：读缓存，默认占堆内存 25%，使用 LRU 策略

---

## 题目四：HBase 的协处理器（Coprocessor）是什么？有哪两类协处理器？各自的使用场景是什么？

### 参考答案

### 协处理器概述

协处理器是运行在 RegionServer 上的用户代码，允许用户在数据写入和读取时执行自定义逻辑，无需修改 HBase 核心代码。

### 两类协处理器

| 类型 | 继承类 | 触发时机 |
|------|--------|----------|
| **Observer** | `BaseRegionObserver` | 钩子方法，类似 AOP 拦截，在特定事件前后执行 |
| **Endpoint** | `BaseEndpointCoprocessor` | 动态添加 RPC 端点，类似存储过程，可在服务端执行计算 |

### Observer 常见场景

| 钩子方法 | 场景 |
|----------|------|
| `preGet` / `postGet` | 权限控制、列级访问控制、数据过滤 |
| `prePut` / `postPut` | 数据验证、审计日志、记录修改历史 |
| `preDelete` / `postDelete` | 删除操作拦截、软删除实现 |
| `preOpen` / `postOpen` | Region 打开时的初始化逻辑 |

### Endpoint 常见场景

- **聚合计算**：在服务端执行 `sum`、`count`、`avg` 等聚合，减少网络传输
- **自定义过滤器**：服务端过滤数据，只返回符合条件的结果
- **批量数据处理**：服务端批量处理数据后返回结果集

### 配置方式

1. **加载到表**：`alter 't1', 'coprocessor' => 'hdfs:///path/to/coprocessor.jar|com.example.Observer|arg1|arg2'`
2. **加载到全局**：`hbase-site.xml` 中配置 `hbase.coprocessor.*` 相关的 RegionServer 类路径

---

## 题目五：HBase 如何保证数据一致性和高可用性？

### 参考答案

### 数据一致性

HBase 提供 **强一致性** 的读写：
- **写一致性**：数据写入 WAL + MemStore 后返回客户端成功，写入操作在所有副本间保证原子性
- **读一致性**：HBase 读取时通过 MVCC（多版本并发控制）保证读取到已提交的数据
- **集群间复制**：可通过 `replication` 特性实现跨集群数据同步（最终一致性）

### 高可用性

| 机制 | 说明 |
|------|------|
| **Master 高可用** | 启动多个 Master，通过 ZooKeeper 选举主 Master |
| **RegionServer 高可用** | ZooKeeper 监控心跳，宕机后 Master 将其上的 Region 迁移到其他节点 |
| **WAL 持久化** | 写操作先写 WAL，宕机后通过 WAL 恢复未刷盘的数据 |
| **HDFS 副本** | HBase 数据存储在 HDFS 上，默认 3 副本，保证数据高可靠 |
| **META 表高可用** | .META. 表的 Region 也会被 Master 监控和迁移 |

### 宕机恢复流程

```
1. ZooKeeper 检测到 RegionServer 失联（心跳超时）
2. Master 获取失联 RegionServer 上的所有 Region 列表
3. Master 将这些 Region 分配到其他存活的 RegionServer
4. 目标 RegionServer 打开 Region，加载对应的 HFile 和 WAL
5. 通过 WAL 重放（Replay）恢复未刷盘的数据
```

---

> 📚 **参考来源**
> - Apache HBase Official Documentation: https://hbase.apache.org/book.html
> - HBase 权威指南 (HBase: The Definitive Guide)
> - 腾讯云开发者社区：https://cloud.tencent.com/developer
> - 简书 HBase 技术专题：https://www.jianshu.com

---

*Generated by AI Assistant - 专注于高质量面试题整理*
