# HBase 面试题 - 2026-08-27

> 今日 5 道 HBase 高频面试题，涵盖架构设计、存储原理、读写流程等核心知识点。

---

## 1. HBase 的整体架构包含哪些核心组件？各自的作用是什么？

**参考来源：** [HBase数据库：分布式列式存储的王者之路](https://blog.csdn.net/m0_74385041/article/details/152283190)

**答：**

HBase 采用 Master-Slave 架构，核心组件包括：

1. **HMaster（主节点）**
   - 负责 Region 的分配与负载均衡
   - 处理 DDL 操作（建表、删表等）
   - 监控 RegionServer 的状态，在故障时进行 Region 迁移
   - 不参与数据的读写路径（这是面试常考点）

2. **RegionServer（从节点）**
   - 负责实际的数据读写请求
   - 管理多个 Region，每个 Region 包含多个 Store（对应列族）
   - 内部包含 MemStore（写缓存）、BlockCache（读缓存）、WAL（预写日志）

3. **ZooKeeper**
   - 存储 HBase 的元数据（如 meta 表的位置）
   - 负责 HMaster 的选举（HA 场景）
   - 监控 RegionServer 的存活状态
   - 客户端通过 ZooKeeper 获取 meta 表地址，进而定位数据

4. **HDFS**
   - HBase 的底层存储，StoreFile（HFile）存储在 HDFS 上
   - 提供高可靠性和高吞吐量的数据存储

**关键理解：** 客户端读写数据时，HMaster **不参与**数据路径。客户端通过 ZooKeeper → meta 表 → RegionServer 的两级定位直接访问数据。

---

## 2. HBase 的 RowKey 设计有哪些原则？如何避免热点问题？

**参考来源：** [HBase读流程深度解析](https://blog.csdn.net/qq_41840843/article/details/158501136)

**答：**

### RowKey 设计三大原则

1. **长度原则**：RowKey 建议越短越好（通常 10-100 字节），因为 RowKey 会存储在每个 KeyValue 中，过长会占用大量存储空间。
2. **散列原则**：避免连续递增的 RowKey（如时间戳），防止数据集中写入单个 Region 造成热点。
3. **唯一原则**：RowKey 必须唯一标识一行数据。

### 避免热点的常见方案

| 方案 | 原理 | 适用场景 |
|------|------|----------|
| **盐值散列（Salting）** | 在 RowKey 前加随机前缀（如 `00_`、`01_`...`99_`），打散到不同 Region | 写入量大、可预知热点 |
| **Hash 散列** | 对原始 RowKey 取 MD5/Hash，用前 N 位作为前缀 | 需要均匀分布 |
| **反转（Reversing）** | 将固定格式的字段反转（如手机号 `138xxxx1234` → `4321xxxx831`） | 尾部变化大的字段 |
| **预分区（Pre-splitting）** | 建表时预先创建多个 Region，并定义 SplitPoint | 已知数据分布范围 |

### 示例

```
# 原始 RowKey（热点风险）
20260827_user_10086

# 盐值散列后
03_20260827_user_10086   → Region-3
57_20260827_user_10086   → Region-57
```

---

## 3. HBase 读数据的完整流程是怎样的？涉及哪些缓存机制？

**参考来源：** [HBase读流程深度解析：从客户端请求到数据返回](https://blog.csdn.net/qq_41840843/article/details/158501136)

**答：**

### 读流程三阶段

**第一阶段：元数据定位**
1. 客户端先查本地缓存的 meta 表位置
2. 缓存未命中则访问 ZooKeeper 获取 meta 表所在的 RegionServer

**第二阶段：Region 定位**
3. 查询 meta 表，找到目标 RowKey 所在的 Region 及其 RegionServer 地址
4. 客户端缓存该位置信息，后续请求直接访问

**第三阶段：数据读取（RegionServer 内部）**
5. **BlockCache**（读缓存）：先检查是否缓存了该数据块，命中则直接返回
6. **MemStore**（写缓存）：检查是否有未刷盘的最新写入数据
7. **StoreFile（HFile）**：
   - 使用 **BloomFilter** 快速过滤不包含目标 RowKey 的文件
   - 通过 HFile 的索引定位数据块
   - 从 HDFS 读取数据块并缓存到 BlockCache
8. **合并结果**：将 BlockCache、MemStore、StoreFile 的结果按时间戳排序，处理删除标记，返回最新版本

### 三种缓存对比

| 缓存 | 位置 | 内容 | 作用 |
|------|------|------|------|
| 客户端缓存 | Client 侧 | Region 位置信息 | 减少元数据查询开销 |
| BlockCache | RegionServer 堆内存 | 已读取的数据块 | 加速热点数据读取 |
| MemStore | RegionServer 堆内存 | 未刷盘的写入数据 | 读取最新写入的数据 |

---

## 4. HBase 的 MemStore 和 HFile（StoreFile）有什么关系？什么是 MemStore Flush？

**参考来源：** [HBase数据库：分布式列式存储的王者之路](https://blog.csdn.net/m0_74385041/article/details/152283190)

**答：**

### MemStore 与 HFile 的关系

- **MemStore**：每个 Store（列族）有一个 MemStore，是一个有序的内存数据结构（ConcurrentSkipListMap）。写入数据时先写 MemStore。
- **HFile（StoreFile）**：MemStore 达到阈值后，数据会被刷写（Flush）到磁盘，形成 HFile 文件，存储在 HDFS 上。
- **关系**：MemStore 是 HFile 的内存缓冲区，一次 Flush 生成一个 HFile。

### MemStore Flush 触发条件

1. **MemStore 大小达到阈值**：单个 Region 的 MemStore 超过 `hbase.hregion.memstore.flush.size`（默认 128MB）
2. **RegionServer 全局 MemStore 占比过高**：所有 MemStore 总大小超过堆内存的 `hbase.regionserver.global.memstore.size`（默认 40%），触发阻塞写入
3. **WAL 文件数量过多**：WAL 文件数超过阈值，触发最老 MemStore 的 Flush
4. **定期自动 Flush**：超过 `hbase.regionserver.optionalcacheflushinterval`（默认 1 小时）
5. **手动 Flush**：通过 shell 命令 `flush 'table_name'` 触发

### HFile 结构

HFile 采用 **LSM-Tree**（Log-Structured Merge Tree）结构：
- 数据按 RowKey 有序存储
- 包含 Data Block、Index Block、BloomFilter Block、Trailer 等
- 读取时先查索引定位 Data Block，再用 BloomFilter 过滤

### Compaction（合并）

随着 HFile 增多，读取需要合并多个文件，影响性能。因此 HBase 会定期执行 Compaction：
- **Minor Compaction**：合并相邻的小 HFile
- **Major Compaction**：合并所有 HFile，清理过期数据和删除标记（慎用，IO 开销大）

---

## 5. HBase 协处理器（Coprocessor）是什么？有哪些类型？应用场景有哪些？

**参考来源：** [Sqoop导入数据到HBase完全指南](https://blog.csdn.net/qq_41840843/article/details/158579499)

**答：**

### 协处理器概述

协处理器是 HBase 提供的一种在 RegionServer 端执行自定义代码的机制，类似于数据库的触发器/存储过程。它允许将计算逻辑下推到数据所在节点执行，减少网络传输和客户端负担。

### 两种类型

| 类型 | 类比 | 执行时机 | 常见用途 |
|------|------|----------|----------|
| **Observer** | 触发器/钩子 | 特定事件发生前后 | 权限校验、审计日志、二级索引维护 |
| **Endpoint** | 存储过程 | 客户端显式调用 | 聚合计算（SUM/COUNT/AVG）、分布式统计 |

### Observer 协处理器

- **RegionObserver**：监听 Region 级别的事件（Get/Put/Delete/Scan 前后）
- **MasterObserver**：监听 Master 级别的事件（建表/删表前后）
- **WALObserver**：监听 WAL 写入事件

### 实际应用场景

1. **二级索引**：在数据写入时自动维护索引表（Observer 的 prePut/postPut）
2. **权限控制**：在 preGet/prePut 中校验访问权限
3. **数据审计**：记录所有数据修改操作
4. **聚合查询**：通过 Endpoint 实现服务端 COUNT、SUM 等计算，避免全量数据拉取到客户端
5. **数据校验**：在写入前校验数据格式和业务规则

### 部署方式

```bash
# 方式一：静态部署（需要 disable 表）
alter 'table_name', METHOD => 'table_att', 
  'coprocessor' => 'hdfs:///path/to/coprocessor.jar|com.example.MyObserver|1001'

# 方式二：动态部署（通过 Shell）
# 在 hbase-site.xml 中配置
```

---

*本文档由 AI 自动生成，每日更新。*
