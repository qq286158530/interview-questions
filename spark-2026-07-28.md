# Spark 核心面试题（2026-07-28）

> 📚 每日Spark面试题推送 | 覆盖Spark架构、RDD、DataFrame、Shuffle、内存管理等核心知识点

---

## 题目一：Spark DAG 与工作流程

**问题：请描述Spark任务的完整执行流程，DAG是如何生成的？**

### 答案

Spark任务的完整执行流程如下：

1. **RDD构建阶段**：用户代码创建RDD，形成DAG（Directed Acyclic Graph，有向无环图）
2. **DAGScheduler划分阶段**：DAGScheduler将DAG划分为多个Stage，划分的依据是**是否存在Shuffle操作**。宽依赖（Shuffle依赖）会触发Stage的划分，窄依赖则在同一个Stage内
3. **TaskSet生成**：每个Stage内部，Spark将操作切分成多个Task（一个Partition对应一个Task），形成TaskSet
4. **TaskScheduler调度**：TaskScheduler将Task发送到各个Executor上执行
5. **Executor执行**：Executor接收Task，反序列化后执行具体的计算逻辑

**关键点：**
- **窄依赖**：map、filter等，一对一或一对多，**可以在同一Stage内流水线执行**
- **宽依赖**：groupByKey、join等，会触发Shuffle，**必须等父Stage全部完成**
- DAGScheduler从DAG的末端（Action算子）向前回溯，遇到Shuffle边界就划分Stage

> 📎 来源：[Apache Spark官方文档 - DAGScheduler](https://spark.apache.org/docs/latest/rdd-programming-guide.html)

---

## 题目二：RDD vs DataFrame vs DataSet

**问题：RDD、DataFrame和DataSet三者有什么区别？各自适用场景是什么？**

### 答案

| 特性 | RDD | DataFrame | DataSet |
|------|-----|-----------|---------|
| **数据结构** | 原始JVM对象，无schema | Row对象 + 固定Schema | 强类型JVM对象 + Schema |
| **优化** | 无Catalyst优化 | 有Catalyst优化 | 有Catalyst优化 |
| **类型安全** | ✅ 完全类型安全 | ❌ 运行时检查 | ✅ 编译时检查 |
| **操作方式** | 函数式算子 | SQL / 链式API | 链式API |
| **序列化** | Java/Kryo | Row格式 | Encoder |
| **GC开销** | 高 | 中 | 低（堆外内存） |

**适用场景：**

- **RDD**：需要精细控制数据分区、追求最大灵活性的底层开发；与第三方库交互；数据无结构化场景
- **DataFrame**：需要做数据分析、处理结构化/半结构化数据；习惯SQL或Pandas风格；需要自动优化执行计划
- **DataSet**：既需要类型安全又需要Catalyst优化；处理强类型业务数据；喜欢OOP风格的链式调用

**核心原理：**
DataFrame和DataSet共享同一个Tungsten执行引擎，享有**Catalyst优化器**的优化（谓词下推、列式裁剪、常量折叠等），而RDD没有这一层优化。

> 📎 来源：[Databricks - RDD vs DataFrame vs DataSet](https://www.databricks.com/blog/2016/07/14/a-tale-of-three-apache-spark-apis-rdds-dataframes-and-datasets.html)

---

## 题目三：Spark Shuffle 机制与优化

**问题：什么是Shuffle？Spark Shuffle的实现机制是什么？如何优化Shuffle性能？**

### 答案

**什么是Shuffle：**
Shuffle是Spark中**重新分区数据**的操作，会导致数据在不同的Executor/Partition之间物理传输。典型的Shuffle操作包括：repartition、groupByKey、join、distinct等。

**Shuffle实现机制（Sort-based Shuffle）：**

1. **Map端**：每个Map Task将数据写入**本地磁盘文件**，同时维护一个索引文件记录每个Reduce Partition的数据偏移量
2. **Shuffle Write**：数据按Key排序后分Partition写入，生成Shuffle文件
3. **Shuffle Read**：Reduce Task通过HTTP从各Map节点拉取属于自己分区的数据
4. **Merge**：拉取到的数据在Reduce端进行排序合并

**优化策略：**

| 优化方向 | 具体做法 |
|---------|---------|
| **减少Shuffle数据量** | 使用map-side预聚合（如reduceByKey替代groupByKey） |
| **增加并行度** | 合理设置shuffle partitions（默认200，建议根据数据量调整） |
| **使用广播变量** | 小表broadcast，避免大表Shuffle |
| **开启序列化** | spark.serializer=org.apache.spark.serializer.KryoSerializer |
| **开启consolidateFiles** | spark.shuffle.service.enabled=true（开启External Shuffle Service） |
| **本地性等待** | spark.locality.wait调优，减少跨节点网络传输 |

> 📎 来源：[Apache Spark - Shuffle Behavior](https://spark.apache.org/docs/latest/tuning.html#shuffle-behavior)

---

## 题目四：Spark 内存管理模型

**问题：Spark的内存管理机制是怎样的？Executor内存如何划分？堆内与堆外内存的区别是什么？**

### 答案

**统一内存管理（Spark 1.6+）模型：**

Spark将内存划分为**存储内存**和**执行内存**两部分，二者可以动态共享和借用：

```
┌─────────────────────────────────────┐
│           JVM Heap (堆内内存)         │
│  ┌──────────────┬──────────────────┐ │
│  │   Execution  │    Storage       │ │
│  │   Memory     │    Memory        │ │
│  │  (执行内存)    │  (存储内存)       │ │
│  │  动态共享     │   动态共享        │ │
│  └──────────────┴──────────────────┘ │
│  ┌──────────────────────────────────┐ │
│  │       User Memory (用户内存)       │ │
│  │   RDD弥留对象、内部元数据等         │ │
│  └──────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**各区域说明：**

| 区域 | 占比 | 用途 |
|------|------|------|
| **Storage Memory** | 可动态调整（默认约46%） | RDD Cache、Broadcast、Unroll |
| **Execution Memory** | 可动态调整（默认约46%） | Shuffle排序、Hash表、Task结果缓存 |
| **User Memory** | 约 8% | 用户数据结构、元数据、Internal Metadata |
| **Reserved Memory** | 约 300MB | 系统预留 |

**堆外内存（Off-Heap）：**
- 通过`spark.memory.offHeap.enabled=true`开启
- 使用`spark.memory.offHeap.size`设置大小（默认关闭）
- 优点：避免GC开销，适合长期缓存的数据
- 通过Netty的PooledByteBufAllocator分配堆外内存

**常见问题：**
- `Executor OOM`：通常是Storage和Execution争抢内存，或Shuffle数据量超出内存
- `GC overhead`：Execution内存被大量占用导致频繁GC

> 📎 来源：[Apache Spark - Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management)

---

## 题目五：广播变量（Broadcast Variable）

**问题：什么是广播变量？它的工作原理是什么？什么场景下应该使用广播变量？**

### 答案

**广播变量的定义：**
广播变量是Spark中**将数据分发到每个Executor节点的机制**。当一个只读变量被广播后，Spark会在每个Executor上保留一份副本，所有Task在该Executor上运行时共享这份数据，避免了每个Task都持有一份副本造成的重复内存消耗。

**工作原理：**

1. **广播创建**：`spark.sparkContext.broadcast(value)`创建广播变量
2. **广播阶段**：Driver将数据序列化（默认使用Torrent广播策略）发送到各Executor
3. **Task使用**：Task通过`.value`访问广播变量（首次访问时会反序列化）
4. **清理**：Task执行完毕后，广播变量不会自动清理，Executor生命周期结束时释放

**广播策略：**
- **Torrent广播（默认）**：类似BitTorrent，数据分块传输，Driver作为种子节点
- **HTTP广播**：简单的HTTP服务器分发，适合小数据量
- **P2P广播**：通过外部Shuffle Service协调

**使用场景：**

| 场景 | 示例 |
|------|------|
| **小表广播Join** | 大表join小表时，将小表广播，避免Shuffle |
| **配置字典** | 静态配置表、维度表 |
| **特征映射** | ML特征编码映射 |
| **过滤条件** | 较大的广播列表作为过滤条件 |

**示例代码：**
```scala
// 广播小表
val smallTable = sc.broadcast(broadcastData)

// 在Map端使用
val result = largeRDD.map { record =>
  val lookupTable = smallTable.value
  // 使用lookupTable进行join或转换
}
```

**注意事项：**
- 广播变量只读，修改无效
- 广播的数据不宜过大（建议<20MB），过大会导致Driver OOM或序列化开销大
- 广播后通过`.unpersist()`手动清理释放内存

> 📎 来源：[Apache Spark - Broadcast Variable](https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables)

---

## 📌 面试要点速记

| 知识点 | 核心一句话 |
|--------|-----------|
| DAG划分 | 宽依赖划Stage，窄依赖流水线执行 |
| DataFrame优化 | Catalyst优化器 + Tungsten堆外内存 |
| Shuffle | 数据跨节点重分区，瓶颈在磁盘和网络 |
| 内存管理 | 存储/执行内存动态共享，User Memory固定 |
| 广播变量 | 只读副本，节省Task重复内存开销 |

---

*📅 推送日期：2026-07-28 | 由自动化脚本整理生成*
