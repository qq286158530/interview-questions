# Spark 面试题 — 2026-08-27

> 每日精选5道Spark面试题，涵盖架构、RDD、DataFrame、Shuffle、内存管理等核心知识点。

---

## 1. Spark 的架构由哪些核心组件组成？Driver 和 Executor 的职责分别是什么？

**答案：**

Spark 采用主从（Master-Slave）架构，核心组件包括：

- **Driver Program（驱动程序）**：运行用户编写的 `main()` 函数，创建 `SparkContext`，负责：
  - 将用户程序划分为多个 Stage（根据 Shuffle 边界划分）
  - 为每个 Stage 创建一组 Task，组成 TaskSet
  - 将 Task 分发给 Executor 执行，并跟踪任务状态
  - 协调整个作业的执行流程

- **Cluster Manager（集群管理器）**：负责资源的分配和管理。支持 Standalone、YARN、Mesos、Kubernetes 等。

- **Executor（执行器）**：运行在 Worker 节点上的进程，负责：
  - 执行 Driver 分配的 Task
  - 将数据缓存在内存或磁盘中
  - 向 Driver 汇报任务执行状态和心跳

- **Application**：一个 Spark 应用，包含一个 Driver 和多个 Executor。

**执行流程**：用户提交应用 → Driver 向 Cluster Manager 申请资源 → Cluster Manager 在 Worker 节点上启动 Executor → Driver 将 Task 分发给 Executor 执行 → Executor 将结果返回给 Driver。

---

## 2. RDD 有哪些特性？RDD 的 Transformation 和 Action 有什么区别？请举例说明。

**答案：**

**RDD（Resilient Distributed Dataset）** 是 Spark 的核心抽象数据结构，具有以下特性：

- **Resilient（弹性）**：支持容错，通过血缘关系（Lineage）可以重新计算丢失的分区
- **Distributed（分布式）**：数据分布在集群的不同节点上
- **Dataset（数据集）**：是数据的抽象表示
- **不可变性**：一旦创建不能修改，只能通过 Transformation 生成新的 RDD
- **惰性计算（Lazy Evaluation）**：Transformation 不会立即执行，只有遇到 Action 才会触发计算
- **可分区**：数据被划分为多个 Partition，每个 Partition 可在不同节点上并行处理
- **可持久化**：支持 persist/cache，将数据缓存到内存或磁盘

**Transformation vs Action：**

| 特性 | Transformation | Action |
|------|---------------|--------|
| 返回值 | 新的 RDD | 非 RDD（数值、集合等） |
| 计算方式 | 惰性，不立即执行 | 立即触发 Job 执行 |
| 示例 | `map()`, `filter()`, `flatMap()`, `groupByKey()`, `reduceByKey()`, `join()` | `count()`, `collect()`, `take()`, `saveAsTextFile()`, `reduce()`, `foreach()` |

---

## 3. Spark 中 Shuffle 是什么？为什么 Shuffle 是性能瓶颈？如何优化 Shuffle？

**答案：**

**Shuffle 的定义：**
Shuffle 是 Spark 在宽依赖（Wide Dependency）下进行数据重分布的过程。当一个操作需要将数据按照某个 Key 重新分组或分区时（如 `groupByKey`、`reduceByKey`、`join`），就会触发 Shuffle。

**为什么 Shuffle 是性能瓶颈：**

1. **磁盘 I/O**：Shuffle 过程中需要将中间结果写入磁盘
2. **网络 I/O**：数据需要在不同节点之间传输
3. **数据序列化/反序列化**：产生额外的 CPU 开销
4. **内存压力**：Shuffle Write 阶段需要大量内存缓冲区来排序和聚合数据

**优化策略：**

- **减少 Shuffle 数据量**：优先使用 `reduceByKey` 替代 `groupByKey`，因为 `reduceByKey` 会在 Map 端进行预聚合（Combine）
- **使用 Map 端聚合**：利用 Map 端的 Combine 操作减少落盘数据量
- **调整分区数**：通过 `spark.sql.shuffle.partitions`（默认200）或 `repartition()` 调整分区数量，避免数据倾斜
- **广播小表**：对于大小表 Join，使用广播变量（Broadcast Join）避免 Shuffle
- **使用 AQE（Adaptive Query Execution）**：Spark 3.x 的自适应查询执行可以动态合并小分区、优化 Join 策略
- **数据倾斜处理**：对倾斜的 Key 加盐（Salting）或拆分处理

---

## 4. Spark 的内存管理机制是怎样的？堆内内存和堆外内存有什么区别？

**答案：**

**Spark 内存管理分为两大区域：**

**堆内内存（On-Heap Memory）：**
- 在 JVM 堆内分配，受 GC（垃圾回收）管理
- 分为三个区域：
  - **Execution Memory（执行内存）**：用于 Shuffle、Join、Sort、Aggregation 等操作中的数据缓冲
  - **Storage Memory（存储内存）**：用于缓存 RDD（cache/persist）、广播变量（Broadcast）、Unroll 等
  - **User Memory（用户内存）**：用户代码中创建的对象、数据结构等
- Spark 统一内存管理模型中，Execution 和 Storage 可以互相借用空间（Unified Memory Management）

**堆外内存（Off-Heap Memory）：**
- 在 JVM 堆外通过 `sun.misc.Unsafe` 直接分配，不受 GC 管理
- 用于减少 GC 压力，适合大规模数据处理
- 通过 `spark.memory.offHeap.enabled` 和 `spark.memory.offHeap.size` 配置
- 堆外内存中也分为 Execution 和 Storage 两个区域，但空间隔离，不能互相借用

**关键配置参数：**
- `spark.memory.fraction`（默认0.6）：Execution + Storage 占总堆内存的比例
- `spark.memory.storageFraction`（默认0.5）：Storage 占统一内存的初始比例
- `spark.executor.memory`：Executor 堆内存大小
- `spark.executor.memoryOverhead`：堆外额外内存（用于 JVM 开销、NIO Direct Memory 等）

---

## 5. 什么是广播变量（Broadcast Variable）？它在什么场景下使用？与累加器（Accumulator）有什么区别？

**答案：**

**广播变量（Broadcast Variable）：**

广播变量是一种**只读**的共享变量，由 Driver 创建后分发到每个 Executor，每个 Executor 只保存一份副本，其中的 Task 共享使用。

**核心优势：**
- 避免每个 Task 都拷贝一份数据，减少网络传输和内存开销
- 以高效的广播算法（如 TorrentBroadcast）分发大数据集

**典型使用场景：**
- **大小表 Join**：将小表广播到所有 Executor，避免大表 Shuffle
- **查找表/字典**：将配置信息、映射表等广播到各节点
- **共享模型参数**：机器学习中广播模型权重

```scala
val lookupTable = Map("a" -> 1, "b" -> 2)
val broadcastLookup = sc.broadcast(lookupTable)
rdd.map(x => broadcastLookup.value.getOrElse(x, 0))
```

**与累加器（Accumulator）的区别：**

| 特性 | 广播变量 | 累加器 |
|------|---------|--------|
| 数据流向 | Driver → Executor | Executor → Driver |
| 读写性 | 只读（Executor 端） | 只能累加（Executor 端写，Driver 端读） |
| 用途 | 共享只读数据 | 聚合统计（如计数器、求和） |
| 副本数 | 每个 Executor 一份 | 每个 Task 一份，最终汇总 |
| 典型场景 | 广播小表、共享配置 | 统计异常记录数、累加计数 |

```scala
val errorCount = sc.longAccumulator("ErrorCounter")
rdd.foreach { line =>
  if (line.contains("ERROR")) errorCount.add(1)
}
println(s"Error count: ${errorCount.value}")
```

---

> 📚 **参考资料：**
> - [Apache Spark 官方文档 - Cluster Mode Overview](https://spark.apache.org/docs/latest/cluster-overview.html)
> - [Apache Spark 官方文档 - RDD Programming Guide](https://spark.apache.org/docs/latest/rdd-programming-guide.html)
> - [Apache Spark 官方文档 - Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management)
> - [Apache Spark 官方文档 - Broadcast Variables](https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables)
> - [Databricks Blog - Unified Memory Management in Spark](https://www.databricks.com/blog/2015/05/28/tuning-java-garbage-collection-for-spark-applications.html)
