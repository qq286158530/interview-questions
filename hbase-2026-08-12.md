# HBase 面试题 — 2026-08-12

> 本题集涵盖 HBase 架构、RowKey 设计、存储结构、MemStore/HFile、协处理器等核心知识点，适合面试准备与知识巩固。

---

## 1. HBase 的整体架构是怎样的？请说明 Master 和 RegionServer 的职责。

### 参考答案

HBase 采用 **Master-Slave** 架构，主要由以下组件构成：

| 组件 | 职责 |
|------|------|
| **ZooKeeper** | 负责选举 Master、存储元数据（-ROOT-/META表位置）、心跳检测 |
| **HMaster** | 管理表结构（DDL操作）、分配 Region、负载均衡、监控 RegionServer 状态 |
| **RegionServer** | 负责数据读写（CRUD）、合并/拆分 Region、与 HDFS 交互 |

### 读写流程概述

1. **写数据**：Client → ZooKeeper → 获取 META 表位置 → 找到对应 RegionServer → 写入 MemStore → 返回成功
2. **读数据**：Client → ZooKeeper → META表定位 Region → MemStore + HFile 合并读取

> 📎 来源：https://hbase.apache.org/book.html#_architecture

---

## 2. HBase 中 RowKey 设计的核心原则是什么？有哪些常见的 RowKey 设计技巧？

### 参考答案

### 核心原则

1. **长度原则**：RowKey 长度建议控制在 16~100 字节，越短越好（持久化存储和传输效率）
2. **散列原则**：避免热点（hotspot），RowKey 开头不要使用固定值导致数据集中到少量 Region
3. **唯一原则**：每行数据的 RowKey 必须唯一，类似于主键

### 常见设计技巧

| 技巧 | 说明 | 示例 |
|------|------|------|
| **反转盐（Reversal）** | 反转时间戳或固定前缀 | `userid_reverse + timestamp` |
| **加盐（Salting）** | 头部拼接随机字节，使数据打散 | `MD5(userid) % N + userid` |
| **哈希（Hashing）** | 对长字符串取 Hash 缩短长度 | `MD5(orderid).substring(0,8)` |
| **组合字段** | 多字段拼接，常见顺序：用户ID + 时间 + ID | `userid + yyyyMMddHH + sequence` |

### 热点问题解决示例

❌ **热点 RowKey**：`stock_20260101`（大量记录以相同前缀写入同一 Region）

✅ **优化 RowKey**：`MD5(product_id).substring(0,2) + product_id + timestamp`

> 📎 来源：https://cloud.google.com/bigtable/docs/hbase-data-model

---

## 3. HBase 的 MemStore 和 HFile 分别是什么？它们在数据写入和持久化过程中扮演什么角色？

### 参考答案

### MemStore（写缓存）

- 位于 **RegionServer 内存**中，默认大小 `128MB`（可配置）
- 数据写入时先落 MemStore，**按列族（Column Family）分组**，内部按键排序
- 当 MemStore 达到阈值，触发 **Flush** 操作，将数据刷写成 HFile

### HFile（HBase 数据文件）

- 位于 **HDFS** 上，是 HBase 的最终存储格式
- HFile 中数据按 RowKey 排序，每个 HFile 包含多个 Block（读取最小单元）
- 顺序写入，一次写入后**不再修改**（WORM 特性）

### 数据持久化流程

```
写入请求 → WAL（HDFS） → MemStore（内存） 
       ↓
  MemStore 触发 Flush → 生成 HFile（HDFS）
       ↓
  HFile 数量过多 → Minor Compaction（小合并）
       ↓
  合并为更大的 HFile → Major Compaction（major compaction 会删除标记的删除数据）
```

### 关键参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `hbase.hregion.memstore.flush.size` | `128MB` | 单个 MemStore 刷写阈值 |
| `hbase.hstore.compaction.min` | `3` | 触发 Minor Compaction 的最小 HFile 数 |

> 📎 来源：https://hbase.apache.org/book.html#regionserver.arch

---

## 4. 什么是 HBase 的协处理器（Coprocessor）？Observer 和 Endpoint 的区别是什么？

### 参考答案

协处理器是 HBase 提供的**扩展机制**，允许用户在 RegionServer 上运行自定义代码，实现类似数据库触发器和存储过程的功能。

### 两类协处理器

#### 1. Observer（观察者，类似触发器）

- 挂载在特定**事件钩子**上，事件触发时自动执行
- **常见类型**：
  - `PreGetObserver`：读操作前拦截，可用于列级权限控制
  - `PostGetObserver`：读操作后拦截，可用于修改返回结果
  - `PrePutObserver` / `PostPutObserver`：写操作前后拦截
  - `PreDeleteObserver` / `PostDeleteObserver`：删除前后拦截

- **应用场景**：数据验证、审计日志、实时聚合、列级访问控制

```java
public class MyObserver extends RegionObserver {
    @Override
    public void prePut(ObserverContext<RegionCoprocessorEnvironment> c,
                       Put put, WALEdit edit) throws IOException {
        // 在 Put 写入前进行数据校验
        if (isValid(put)) {
            // 放行
        } else {
            throw new DoNotPassIOException("Invalid data");
        }
    }
}
```

#### 2. Endpoint（端点，类似存储过程）

- 允许在 **RegionServer 端**执行自定义计算逻辑
- 客户端调用时，数据**就近计算**，减少网络传输
- **应用场景**：聚合查询（求和、平均）、复杂计算下推

```java
// 定义 Endpoint 接口
public interface AggregateProtocol {
    int getSum(byte[] columnFamily, byte[] column);
}

// 服务端实现
public class AggregateEndpoint extends BaseEndpointCoprocessor 
    implements AggregateProtocol {
    @Override
    public int getSum(byte[] cf, byte[] column) {
        // 在 RegionServer 内部扫描计算
        ...
    }
}
```

### 加载方式

| 方式 | 说明 |
|------|------|
| **静态加载（xml 配置）** | 修改 `hbase-site.xml`，所有表生效，重启生效 |
| **动态加载（shell/API）** | 加载到单张表，无需重启，临时生效 |

> 📎 来源：https://hbase.apache.org/book.html#cp

---

## 5. HBase 如何保证数据一致性？Write-Ahead Log (WAL) 的作用是什么？

### 参考答案

### WAL（预写日志）机制

HBase 在数据写入 **MemStore 前**，会先将操作写入 WAL（存储在 HDFS 上）。如果服务在数据未刷写到 HFile 前宕机，WAL 可以**恢复未持久化的数据**，防止数据丢失。

### 数据一致性保证层次

| 层次 | 保证方式 |
|------|----------|
| **写入一致性** | 默认 `写完 MemStore + WAL 即返回成功`（最终一致） |
| **读取一致性** | 同一 Region 内，读取最新版本数据（MVCC 多版本并发控制） |
| **分布式一致性** | 依赖 ZooKeeper 选举 Master，依赖 HDFS 数据块副本 |
| **宕机恢复** | WAL replay + MemStore 持久化 |

### MVCC（多版本并发控制）

HBase 使用 MVCC 保证**读不加锁**，读写不互相阻塞：

1. 写操作分配 **writeNumber**，写入 MemStore 时标记
2. 读操作只能看到 `writeNumber ≤ 当前活跃读编号` 的数据
3. 读操作完成后才更新活跃读编号，确保**快照语义**

### WAL 配置选项

| 参数 | 说明 |
|------|------|
| `WAL edit` | 每条写操作在 WAL 中的记录单元 |
| `WAL Provider` | 可选 `filesystem`（默认）或 `multiWAL`（并行写多个 WAL） |
| `Append` | WAL 顺序追加写入 HDFS，利用 HDFS 顺序写特性保证高性能 |

### 容灾恢复流程

```
RegionServer 宕机
       ↓
ZooKeeper 检测到心跳丢失，通知 Master
       ↓
Master 将该 RS 上的 Region 分配给其他 RS
       ↓
新 RS 重放 WAL，恢复 MemStore 数据
       ↓
继续对外提供服务
```

> 📎 来源：https://hbase.apache.org/book.html#wal

---

*整理自 HBase 官方文档及业界经典面试题库 | 2026-08-12*
