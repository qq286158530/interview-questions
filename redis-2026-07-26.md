# Redis 面试题 2026-07-26

> 每道题均附详细答案，覆盖 Redis 数据类型、持久化、集群、缓存等核心知识点。

---

## 题目 1：Redis 有哪些主要的数据类型？它们各自的使用场景是什么？

### 答案

Redis 支持以下主要数据类型：

| 类型 | 数据结构 | 典型使用场景 |
|------|---------|-------------|
| **String** | 字符串（SDS） | 缓存简单值、计数器、分布式锁（SETNX）、限流（SETEX） |
| **Hash** | 压缩哈希表 | 存储对象（如用户信息、商品详情），比 String 节省空间且支持字段级操作 |
| **List** | 双向链表 | 消息队列（LPUSH+BRPOP）、最新列表（朋友圈动态）、任务队列 |
| **Set** | 整数集合/哈希表 | 标签系统、去重（UV 统计）、好友关系（交集/并集） |
| **Sorted Set** | 跳表+字典 | 排行榜（ZINCRBY）、延迟队列、有序通知 |
| **Bitmap** | 位图 | 签到统计、用户在线状态、布隆过滤器基础 |
| **HyperLogLog** | 概率数据结构 | UV 统计（独立访客数量），内存极小但有误差 |
| **Geo** | zset（geohash） | 附近的人、地理位置服务 |
| **Stream** | 日志流 | 消息队列、事件溯源 |

**核心特点**：所有操作都是原子性的，支持过期时间（EXPIRE），数据存在内存中但可通过配置持久化到磁盘。

**来源**：综合整理（Redis 官方文档 + 常见面试题库）

---

## 题目 2：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别，如何选择？

### 答案

Redis 有两种持久化机制：

### RDB（Redis Database）

- **原理**：定时生成内存数据的快照，保存为 `dump.rdb` 文件
- **触发方式**：
  - 定时任务（`save` 配置，如 `save 900 1` 表示 900 秒内 ≥1 次写操作则触发）
  - `BGSAVE` 命令手动触发（fork 子进程执行，不阻塞主线程）
  - 主从复制时从节点自动触发
- **优点**：恢复速度快（直接加载 rdb 文件），适合备份/灾难恢复
- **缺点**：可能丢失最近一次快照之后的数据

### AOF（Append Only File）

- **原理**：将每次写操作追加到 `appendonly.aof` 文件
- **配置**（`appendfsync`）：
  - `always`：每条写命令都 fsync，最安全但最慢
  - `everysec`（默认）：每秒执行一次 fsync，推荐使用
  - `no`：由操作系统决定何时 fsync，最快但最不安全
- **Rewrite**：AOF 文件过大会自动压缩（BGREWRITEAOF），去除冗余命令
- **优点**：数据安全性更高（最多丢失 1 秒数据），可读性好
- **缺点**：文件体积通常比 RDB 大，恢复速度慢

### 如何选择

| 场景 | 推荐策略 |
|------|---------|
| 允许分钟级数据丢失，追求高性能 | 仅 RDB |
| 对数据安全性要求高 | RDB + AOF（同时开启） |
| 缓存场景，不需要持久化 | 关闭持久化 |

**最佳实践**：同时开启 RDB 和 AOF，使用 AOF 恢复数据（RDB 作为备份）。

**来源**：综合整理（Redis 官方文档 https://redis.io/docs/management/persistence/）

---

## 题目 3：Redis 的过期 key 删除策略有哪几种？它们有什么区别？

### 答案

Redis 有两种过期 key 删除策略：

### 1. 惰性删除（Lazy Expiration）

- **机制**：当访问某个 key 时，才检查它是否过期，如果过期则删除
- **优点**：对 CPU 友好，只在需要时才检查，不用额外资源
- **缺点**：如果过期 key 长期不被访问，会一直占用内存（内存泄漏风险）

### 2. 定期删除（Active Expiration）

- **机制**：Redis 每隔一段时间（默认每 100ms）随机抽取一批设置了过期时间的 key，检查并删除过期的 key
- **配置**：`hz` 参数控制检查频率（默认 10，即每秒执行 10 次）
- **优点**：主动释放内存，避免过期 key 堆积
- **缺点**：会对 CPU 产生一定压力

### 内存淘汰策略（当内存达到上限时）

当 Redis 内存达到 `maxmemory` 上限时，会触发**内存淘汰策略**（不是删除过期 key）：

| 策略 | 说明 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `allkeys-lru` | 在所有 key 中淘汰最近最少使用的（**最常用**） |
| `allkeys-random` | 随机淘汰所有 key |
| `volatile-lru` | 在设置了过期时间的 key 中淘汰 LRU |
| `volatile-random` | 在设置了过期时间的 key 中随机淘汰 |
| `volatile-ttl` | 淘汰剩余 TTL 最短的 key |

**实际建议**：如果作为缓存使用，推荐 `allkeys-lru` 或 `volatile-lru`。

**来源**：综合整理（Redis 官方文档 https://redis.io/docs/management/optimization/memory-optimization/）

---

## 题目 4：Redis 主从复制原理是什么？如何应对主节点故障？

### 答案

### 主从复制原理

Redis 主从复制采用 **PSYNC** 协议（Redis 2.8+），分为全量同步和增量同步：

#### 全量同步（第一次加入集群 or 断线重连）

1. **从节点**发送 `PSYNC ? -1`（请求全量同步）
2. **主节点**执行 `BGSAVE` 生成 RDB 快照，同时记录后续的写命令到 **replication buffer**
3. 主节点将 RDB 文件发送给从节点（通常通过 socket 传输）
4. 从节点清空本地数据，加载 RDB 文件
5. 主节点将 replication buffer 中的增量命令发送给从节点
6. 从节点执行这些命令，与主节点保持同步

#### 增量同步（短暂网络中断后重连）

- 从节点发送 `PSYNC <runid> <offset>`
- 主节点根据 `runid` 和 `offset` 发送积压的写命令
- 如果从节点的数据太旧（offset 不在积压 buffer 中），则触发全量同步

#### 关键配置

```bash
replicaof <masterip> <masterport>   # 从节点配置主节点
masterauth <password>               # 主节点密码（如果开启了 requirepass）
```

### 哨兵模式（Sentinel）——应对主节点故障

哨兵是 Redis 的高可用解决方案：

1. **监控**：哨兵每秒向主/从/其他哨兵发送心跳检测存活
2. **自动故障转移**：主节点不可达时，哨兵集群投票确认后，从从节点中选举新主节点，其他从节点指向新主节点
3. **通知**：故障转移后，通过 Pub/Sub 通知客户端（如修改了配置的主节点地址）

```
主观下线（SDown）：单个哨兵认为主节点不可达
客观下线（ODown）：多数哨兵认为主节点不可达，触发故障转移
```

### Cluster 模式（数据分片）

- 数据自动分片到 16384 个槽位
- 每个主节点负责一部分槽位
- 每个槽位可有 1 个主节点 + N 个从节点（自动故障转移）
- 客户端可连接任意节点，自动路由到正确节点

**来源**：综合整理（Redis 官方文档 https://redis.io/docs/management/replication/ + https://redis.io/docs/management/sentinel/）

---

## 题目 5：什么是缓存穿透、缓存击穿、缓存雪崩？如何应对？

### 答案

这是 Redis 缓存最经典的三类问题：

### 1. 缓存穿透

**定义**：查询一个**根本不存在**的数据，缓存没有命中，每次都去查数据库。

**危害**：恶意请求或频繁查询不存在的数据，会导致数据库压力剧增。

**解决方案**：
- **布隆过滤器（Bloom Filter）**：将所有存在的 key 存入布隆过滤器，查询前先过滤
- **缓存空值**：将查询结果为空也缓存一个短时间的值（如 `NULL`，TTL 设置短一些）
- **参数校验**：对请求参数做基本校验（如 id > 0），拦截非法参数

### 2. 缓存击穿

**定义**：某个**热点 key 过期**的瞬间，大量请求同时涌入，直接打到数据库。

**解决方案**：
- **互斥锁（SETNX）**：第一个请求去查数据库并重建缓存，其他请求等待缓存重建完成
- **逻辑过期**：不给缓存设置真正的过期时间，而是存一个"逻辑过期时间"字段，后台异步更新
- **热点数据永不过期**：对极热点数据不设置过期时间，由后台任务主动更新

### 3. 缓存雪崩

**定义**：大量缓存**同一时间过期**，或者 Redis **突然宕机**，导致大量请求直接打到数据库。

**解决方案**：
- **过期时间随机化**：给 key 的 TTL 加上随机值（如 `TTL = base + random(0, 3600)`）
- **多级缓存**：本地缓存 + Redis 缓存，减少对 Redis 的直接压力
- **Redis 高可用**：使用 Redis Cluster 或哨兵模式，保证 Redis 不轻易宕机
- **限流降级**：使用 Hystrix / Sentinel 等工具对数据库做限流和降级
- **预热**：系统启动时提前加载热点数据到缓存

### 对比总结

| 问题 | 原因 | 关键词 | 核心解法 |
|------|------|--------|---------|
| 穿透 | 查询不存在的数据 | "不存在" | 布隆过滤器、缓存空值 |
| 击穿 | 热点 key 过期 | "一个 key" | 互斥锁、逻辑过期 |
| 雪崩 | 大量 key 同时过期 or Redis 宕机 | "集体过期/宕机" | 随机 TTL、高可用架构 |

**来源**：综合整理（常见 Redis 面试题库 + 系统设计实践）

---

> 📚 本题库由 AI 自动整理，每日更新。  
> 仓库地址：https://github.com/qq286158530/interview-questions
