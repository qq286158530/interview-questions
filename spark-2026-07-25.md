# Spark 面试题精选 (2026-07-25)

> 本文档收录5道高质量Spark面试题，涵盖Spark架构、RDD、DataFrame/DataSet、Shuffle、广播变量等核心知识点。

---

## 题目一：Spark 架构核心原理

**问题：请描述Spark的整体架构与核心组件，以及Driver和Executor的作用。**

### 参考答案

Spark 采用 **主从架构（Master-Slave）**，核心组件包括：

| 组件 | 角色 | 职责 |
|------|------|------|
| **Driver** | 主节点 | 解析用户代码，构建DAG图，划分Stage，调度Task到Executor |
| **Executor** | 从节点 | 负责执行Task，缓存数据，向Driver汇报结果 |
| **Cluster Manager** | 资源管理 | 申请/分配/回收Executor资源（Standalone/YARN/K8s） |
| **Worker** | 节点代理 | 管理本节点Executor进程 |

### 核心工作流程

```
用户代码 → Driver构建DAG → DAGScheduler划分Stage → TaskScheduler分发Task → Executor执行
```

**关键点：**
- **DAG（Directed Acyclic Graph）**：DAGScheduler将DAG划分为多个Stage，Stage内为窄依赖（无需Shuffle），Stage间为宽依赖（需要Shuffle）
- **窄依赖**：map/filter等，一对一或一对多，无需跨分区数据移动
- **宽依赖**：groupByKey/reduceByKey等，触发Shuffle，数据需要重新分区

**Driver的核心作用：**
1. 承载用户代码的`main()`方法
2. 创建SparkContext（Spark程序的入口）
3. 将代码转换为RDD/DataFrame/Dataset操作
4. 协调整个作业的执行

**Executor的核心作用：**
1. 启动时向Driver注册自己
2. 接收Driver分配的Task，在工作线程中执行
3. 将结果返回给Driver或存储到磁盘/内存
4. Executor崩溃时，其上的Task会在其他Executor重新执行

**来源：** [CSDN-Spark架构原理](https://blog.csdn.net/qq_42500847/article/details/125893485)

---

## 题目二：RDD 五大特性与算子分类

**问题：RDD有哪些核心特性？Transformation和Action算子的区别是什么？请举例说明。**

### 参考答案

### RDD 五大核心特性

| 序号 | 特性 | 说明 |
|------|------|------|
| 1 | **分区列表** | RDD由多个分区组成，每个分区可并行计算 |
| 2 | **计算函数** | 每个分区都有计算函数（分区上的Iterator[T] => Iterator[U]） |
| 3 | **依赖关系** | RDD之间存在依赖关系（窄依赖/宽依赖） |
| 4 | **分区器（可选）** | Key-Value RDD可选的分区策略（HashPartitioner/RangePartitioner） |
| 5 | **位置偏好（可选）** | 每个分区最佳计算位置的元数据（如HDFS块位置） |

### Transformation vs Action

| 特征 | Transformation | Action |
|------|----------------|--------|
| 返回类型 | 新的RDD | 非RDD（如Unit/Seq/基本类型） |
| 懒加载 | ✅ 延迟执行，构建DAG | ✅ 触发DAG执行 |
| 示例 | map, filter, groupByKey, join | collect, count, saveAsTextFile |
| 目的 | 定义计算逻辑 | 收集结果/触发计算 |

### 常用算子举例

**Transformation（懒执行）：**
```scala
val rdd1 = sc.textFile("hdfs://path")       // HdfsIO
val rdd2 = rdd1.map(_.toUpperCase)          // Map
val rdd3 = rdd2.filter(_.contains("SPARK")) // Filter
val rdd4 = rdd3.distinct()                   // Distinct
```

**Action（触发执行）：**
```scala
rdd.collect()        // 返回Array[String]
rdd.count()          // 返回Long
rdd.take(10)         // 返回Array[String]
rdd.saveAsTextFile() // 写出到HDFS
rdd.foreach(println)  // 遍历打印
```

**注意：** Spark的懒加载机制使得Transformation不立即执行，只有遇到Action时才会真正触发计算，这是Spark性能优化的关键。

**来源：** [简书-Spark RDD核心特性](https://www.jianshu.com/p/186f0e15b80a)

---

## 题目三：DataFrame 与 DataSet 的区别

**问题：Spark中DataFrame和DataSet有什么区别？它们与RDD的关系是什么？**

### 参考答案

### 三者对比

| 特性 | RDD | DataFrame | DataSet |
|------|-----|-----------|---------|
| **数据类型** | 无类型（原始JVM对象） | Row类型（半结构化） | 强类型（Case Class/Typed） |
| **Schema** | 无 | 有（列名+类型） | 有（编译时检查） |
| **编译时检查** | 无 | 无（运行时） | 有（编译时） |
| **优化** | 无（手写优化） | Catalyst优化器 | Catalyst优化器 |
| **序列化** | Java/Kryo | Off-Heap Tungsten | Encoder |
| **使用场景** | 低级API/非结构化 | 结构化/SQL查询 | 类型安全业务逻辑 |

### DataFrame = DataSet[Row]

```scala
// DataFrame本质是DataSet[Row]
type DataFrame = DataSet[Row]
```

### 互相转换

```scala
// RDD -> DataFrame
val df = rdd.toDF("name", "age")

// DataFrame -> RDD
val rdd = df.rdd

// DataSet -> DataFrame
val df = ds.toDF

// DataFrame -> DataSet
val ds = df.as[Person]
```

### 统一API：DataFrame和DataSet统一成Dataset

Spark 2.x后，`DataFrame`和`DataSet`统一为`Dataset` API，区别在于：
- `DataFrame` = `Dataset[Row]`（无类型，动态语言特性）
- `DataSet[T]` = 强类型T（编译时类型安全）

### 性能优化原理

**Catalyst优化器**对DataFrame/DataSet自动进行：
1. **谓词下推**：将Filter条件下推到数据源
2. **列剪裁**：只读取需要的列
3. **常量替换**：将可优化的表达式提前计算
4. **广播 Join**：小表广播到各节点避免Shuffle

```sql
-- 实际执行时会被优化
SELECT a.id, b.name 
FROM a JOIN b ON a.id = b.id 
WHERE a.age > 18

-- 优化后：先Filter再Join，减少数据传输量
```

**来源：** [知乎-Spark DataFrame与DataSet](https://zhuanlan.zhihu.com/p/618242735)

---

## 题目四：Shuffle 机制详解

**问题：什么是Shuffle？为什么Spark需要Shuffle？ShuffleManager有哪些？各有何优缺点？**

### 参考答案

### 什么是Shuffle

Shuffle是**重新分区数据**的过程，当RDD的某个分区需要依赖另一个RDD的多个分区数据时（如groupByKey），必须跨Executor传输数据。

```
Stage 1: [P0, P1, P2] → Shuffle Write → Shuffle Read → Stage 2: [P0', P1', P2']
         (Map端写磁盘)                              (Reduce端拉取)
```

### 为什么需要Shuffle

- **宽依赖必然触发**：如`groupByKey`、`reduceByKey`、`join`、`sortByKey`
- 相同Key的数据必须汇聚到同一分区才能进行聚合/排序

### ShuffleManager演进

| 版本 | ShuffleManager | 特点 |
|------|-----------------|------|
| Spark 1.x | **Hash Shuffle** | 每个Mapper为每个Reducer创建文件，文件数 = M×R，磁盘IO大 |
| Spark 2.x | **Sort Shuffle** | 排序后写入同一文件，通过索引访问，文件数 = 2M |
| Spark 3.x | **Sort Shuffle（默认）** + **钨丝计划优化** | Unsafe Shuffle，直接操作二进制内存 |

### Hash Shuffle（已淘汰）

```
[Mapper 0] → [R0 file, R1 file, R2 file]  
[Mapper 1] → [R0 file, R1 file, R2 file]
[Mapper 2] → [R0 file, R1 file, R2 file]

问题：文件数过多（1000 mappers × 1000 reducers = 100万文件）
```

### Sort Shuffle（推荐）

```
[Mapper 0] → [sorted data file + index file]
[Mapper 1] → [sorted data file + index file]

机制：
1. 数据写入内存缓冲区
2. 达到阈值后溢写到磁盘（排序）
3. 合并所有溢写文件，生成最终排序文件+索引
```

**Sort Shuffle优势：**
- 文件数大幅减少（2M vs M×R）
- 支持有序聚合（sortByKey无需额外排序）
- 内存压力小（基于磁盘排序）

### Shuffle调优参数

```scala
// spark-defaults.conf
spark.shuffle.file.buffer  32kb    // Map端缓冲区
spark.shuffle.manager      sort    // 选择Shuffle方式
spark.shuffle.sort.bypassMergeThreshold  200  // Sort Shuffle回退阈值
spark.reducer.maxSizeInFlight  48mb    // Reduce端拉取缓冲区
```

**来源：** [博客园-Spark Shuffle机制](https://www.cnblogs.com/sq16884461/p/10469908.html)

---

## 题目五：广播变量与内存管理

**问题：什么是广播变量？Spark的内存模型是怎样的？如何调优内存使用？**

### 参考答案

### 广播变量（Broadcast Variable）

**定义：** 将只读的大变量（如配置表、维度表）广播到各Executor节点，避免每个Task都复制一份。

**原理：**
```
未广播：Task 1 → 复制大表(100MB)
        Task 2 → 复制大表(100MB)  
        Task 3 → 复制大表(100MB)
        总传输：300MB × N个Task

已广播：Executor 1 → 大表(100MB) × 共享
        Executor 2 → 大表(100MB) × 共享
        总传输：100MB × Executor数
```

**使用方式：**
```scala
// 定义广播变量
val broadcastVar = sc.broadcast(Array(1, 2, 3, 4, 5))

// 在算子内使用
rdd.map(x => x * broadcastVar.value.sum)

// 销毁广播变量（不再使用时）
broadcastVar.unpersist()
```

**注意事项：**
- 只适合小表/中等规模数据（几百MB以内）
- 大表建议用`join`代替广播
- Spark会自动选择是否广播（spark.sql.autoBroadcastJoinThreshold，默认10MB）

### Spark 内存模型（统一内存管理）

Spark 2.x采用**统一内存管理**，内存分为：

```
┌─────────────────────────────────────────────────────────────�┐
│                    Heap Memory (堆内内存)                    │
├─────────────┬───────────────┬──────────────────────────────┤
│   Execution │    Storage    │       User Memory            │
│   (执行内存) │  (存储内存)    │      (用户内存)               │
│   30%       │   30%         │      20%                      │
│   可弹性扩展│  可弹性扩展    │      预留空间                   │
│             │  (缓存+RDD)   │                               │
├─────────────┴───────────────┴──────────────────────────────┤
│                    Reserved Memory (reserved 300MB)         │
│                    系统预留                                  │
└─────────────────────────────────────────────────────────────┘
```

**内存区域说明：**

| 区域 | 用途 | 弹性 | 参数 |
|------|------|------|------|
| **Execution Memory** | Shuffle排序/聚合/Hash表 | 可借用Storage | spark.memory.fraction (0.6) |
| **Storage Memory** | RDD缓存/Broadcast缓存 | 可借用Execution | spark.memory.storageFraction (0.5) |
| **User Memory** | 用户数据结构/Spark内部元数据 | 固定 | 1 - spark.memory.fraction |
| **Reserved** | 系统预留 | 固定 | 300MB |

**动态占用机制：**
- 当Execution内存不足时，可借用Storage内存
- 当Storage内存不足时，只能使用未占用的空闲内存
- 借用后若原持有者需要，可强制驱逐

### 内存调优建议

```scala
// 数据倾斜场景：增大Shuffle缓冲区
spark.sql.shuffle.partitions = 200

// 缓存策略选择
rdd.persist(StorageLevel.MEMORY_AND_DISK) // 超过内存时溢写磁盘

// 广播大表调优
spark.sql.autoBroadcastJoinThreshold = 100MB  // 适度增大
```

**来源：** [Apache Spark官方文档](https://spark.apache.org/docs/latest/tuning.html)

---

## 📚 更多资源

- [Spark官方文档](https://spark.apache.org/docs/latest/)
- [0voice/interview_internal_preset - GitHub面试题库](https://github.com/0voice/interview_internal_preset)

---

*本文档由自动化脚本生成，每日更新*
