# HBase 面试题 — 2026-08-21

> 📚 涵盖 HBase 架构、RowKey 设计、存储结构、MemStore/HFile、协处理器等核心知识点

---

## 题目一：HBase 的整体架构是怎样的？请描述 Master、RegionServer、ZooKeeper 的职责

### 参考答案

HBase 采用 **Master-Slave 架构**，核心组件如下：

| 组件 | 职责 |
|------|------|
| **Master** | 负责表的 DDL 操作（创建/删除表、列族修改）、负载均衡（Region 切分与调度）、RegionServer 宕机后的失效恢复。不参与实际数据读写 |
| **RegionServer** | 负责管理 Master 分配给自己的 Region，真正处理读写请求；与 HDFS DataNode 交互完成数据持久化 |
| **ZooKeeper** | 选举集群 active Master；存储 `.META.` 表位置信息；监测 RegionServer 存活状态（通过临时节点） |

**读写流程简述：**
1. 客户端先访问 ZooKeeper，获取 `.META.` 表所在的 RegionServer
2. 从 `.META.` 表中查到目标 RowKey 所在的 RegionServer
3. 直接连接目标 RegionServer 进行读写（客户端会缓存 meta 位置，重复查询 ZooKeeper）

> 📎 来源：https://hbase.apache.org/book.html#arch.overview

---

## 题目二：HBase 的存储结构（StoreFile/HFile、MemStore）是如何工作的？

### 参考答案

**核心存储机制：写放大 + LSM-Tree**

1. **MemStore（写缓存）**
   - 写入数据先写入 MemStore（内存有序结构，按 RowKey + Column + Version 排序）
   - 每个 Store（列族）对应一个 MemStore
   - 默认大小超过 `hbase.hregion.memstore.flush.size`（128MB）时，触发 flush 生成 HFile

2. **HFile（StoreFile）**
   - HBase 在 HDFS 上的实际存储格式，基于 Hadoop Bloom File 实现
   - 结构：Data Block → Meta Block → File Info → Index Block → Bloom Filter → Trailer
   - 每个 Block 默认 64KB，支持 Bloom Filter 快速判断某 RowKey 是否存在

3. **LSM-Tree 合并**
   - Minor Compaction：选取多个较小的 HFile 合并成更大的（不清理过期/删除数据）
   - Major Compaction：将一个 Store 下所有 HFile 合并成一个，**物理删除**过期版本和被标记删除的数据

> 📎 来源：https://hbase.apache.org/book.html#perf.schema

---

## 题目三：HBase 的 RowKey 设计有哪些原则？如何避免热点（Hotspotting）问题？

### 参考答案

### RowKey 设计四大原则

| 原则 | 说明 |
|------|------|
| **长度原则** | 越短越好，推荐 ≤ 16 字节（最大 64KB）。过长的 RowKey 会影响索引大小和 MemStore 内存开销 |
| **散列原则** | RowKey 必须足够随机，避免单调递增/递减（如时间戳开头），否则所有请求打到同一个 RegionServer 形成热点 |
| **唯一原则** | 相当于主键，同一 RowKey 的不同列族/列视为同一行 |
| **业务导向** | 将查询最频繁的字段放在 RowKey 前面（前缀原则） |

### 解决热点问题的常用技巧

1. **加盐（Salting）**：在 RowKey 头部加随机前缀（如 `hash(rowKey) % N + originalKey`），让数据分散到不同 Region
2. **翻转（Reversing）**：将单调递增部分（如时间戳）反转，使分布更随机
3. **哈希（Hashing）**：对完整 RowKey 取 MD5/SHA，取前几位做前缀
4. **时间戳反转**：查询"最近消息"场景下用 `Long.MAX_VALUE - timestamp` 作为 RowKey 尾部

> 📎 来源：https://hbase.apache.org/book.html#rowkey.design

---

## 题目四：HBase 的协处理器（Coprocessor）有哪两类？分别适用于什么场景？

### 参考答案

HBase 提供了两类协处理器，均运行在 RegionServer 上：

### 1. Observer（观察者，类比触发器）
- **机制**：在特定事件前后插入回调逻辑，类似 AOP
- **使用场景**：
  - `PrePut` / `PostPut`：写入前/后执行自定义逻辑（如数据校验、衍生字段计算）
  - `PreGet` / `PostGet`：读取前/后修改返回值或记录访问日志
  - `PreDelete` / `PostDelete`：删除前/后做关联清理
  - `PreOpen`：Region 启动前初始化

- **典型应用**：实现二级索引（写入主表时同步更新索引表）、数据审计

### 2. Endpoint（端点，类比存储过程）
- **机制**：在 RegionServer 上执行用户自定义的 RPC 回调方法，客户端调用后会在每个相关 Region 上执行并聚合结果
- **使用场景**：
  - 复杂聚合查询（求和、统计）直接下推到 RegionServer 层，减少网络传输
  - 自定义批处理逻辑

### 两类对比

| 维度 | Observer | Endpoint |
|------|----------|----------|
| 触发方式 | 事件驱动（自动） | 客户端主动调用 |
| 返回值 | 无（或修改原有操作） | 有（自定义聚合结果） |
| 执行范围 | 单 Region | 所有相关 Region 并行执行后聚合 |

> 📎 来源：https://hbase.apache.org/book.html#cp

---

## 题目五：HBase 如何保证数据一致性？它与 Cassandra 的最终一致性有何不同？

### 参考答案

### HBase 的一致性模型

HBase 默认为 **强一致性**（Strong Consistency）：
- 写入：写入 WAL（Write-Ahead Log）+ MemStore，成功后返回客户端 ACK
- 读取：先读 MemStore，再读 HFile（Merge 合并多版本），返回最新版本
- 同一行读写由同一个 RegionServer 处理，通过 MVCC（Multi-Version Concurrency Control）保证读写互不阻塞

### 与 Cassandra 最终一致性的核心区别

| 维度 | HBase | Cassandra |
|------|-------|-----------|
| **一致性** | 强一致（单副本读写等价） | 最终一致（可配置 CAP 三角） |
| **写入路径** | 先 WAL → MemStore → flush HFile | 先 CommitLog → MemTable → SSTable |
| **读取路径** | 读 MemStore + HFile merge | 读 MemTable + SSTable merge |
| **冲突处理** | 基于时间戳（最新版本 wins） | 基于向量时钟（vector clock）或数据版本 |
| **副本同步** | RegionServer 与 HDFS DataNode 同分布，写入由 HDFS 副本机制保证 | Gossip 协议在节点间异步复制 |
| **CAP 取舍** | 偏向 CP（可用性略弱，分区时 Region 不可用） | 可配置 CP 或 AP |

### 实际理解
HBase 的强一致并不意味着"全球实时同步"，而是说**对同一个 RowKey 的读写是线性一致的**，写入后后续读取一定能读到最新值（除非网络分区导致整个 Region 不可用）。这与 Cassandra 可以返回旧值的最终一致模型有本质区别。

> 📎 来源：https://hbase.apache.org/book.html#acls

---

*📌 本题库由面试题助手自动生成，每日更新 | GitHub: https://github.com/qq286158530/interview-questions*
