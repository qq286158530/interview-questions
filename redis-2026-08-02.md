# Redis 面试题整理

> 日期：2026-08-02  
> 来源：综合整理自 Redis 官方文档及常见面试题库

---

## 题目一：Redis 有哪些数据类型？分别适用于什么场景？

### 参考答案

Redis 支持五种基础数据类型：

| 数据类型 | 底层实现 | 适用场景 |
|---------|---------|---------|
| **String** | SDS（简单动态字符串） | 缓存、计数器、分布式锁、session共享 |
| **List** | QuickList（3.2后） | 消息队列、排行榜、最新消息列表 |
| **Hash** | hashtable + ziplist（小数据量） | 对象存储、购物车、用户信息 |
| **Set** | intset（小数据量）+ hashtable | 标签系统、好友关系、去重 |
| **Zset** | skiplist + hashtable | 有序集合、排行榜、延迟队列 |

此外还有三种高级数据类型：
- **Bitmap**：位图，适合签到、统计活跃用户
- **HyperLogLog**：基数统计，适合UV统计
- **Geospatial**：地理位置，适合附近的人功能

**实战Tips**：
- String类型值最大512MB，但建议单个值不超过100KB
- Hash遍历时用HSCAN而非KEYS命令，避免阻塞
- Zset的score可存储时间戳实现延迟队列

---

## 题目二：Redis 的持久化机制是什么？RDB和AOF有什么区别？如何选择？

### 参考答案

Redis有两种持久化方式：

### RDB（Redis Database）

**原理**：在指定时间点生成数据集的快照（Snapshot）

```bash
# 触发方式
save 900 1      # 900秒内至少1个key变化
save 300 10     # 300秒内至少10个key变化
save 60 10000   # 60秒内至少10000个key变化
bgsave          # 异步后台保存
```

**优点**：恢复速度快、文件紧凑、适合备份  
**缺点**：可能丢失最后一次快照后的数据

### AOF（Append Only File）

**原理**：记录每个写操作命令，以日志形式追加保存

```bash
appendonly yes
appendfsync everysec  # 推荐：每秒同步，最多丢1秒数据
# appendfsync always   # 每次写入都同步，最安全但最慢
# appendfsync no       # 由OS决定同步时机，最快但最危险
```

**优点**：数据安全性更高、只追加写入（适合大文件）  
**缺点**：文件体积大、恢复速度慢

### 混合持久化（Redis 4.0+）

```bash
aof-use-rdb-preamble yes
```
先以RDB格式写入AOF文件开头，后续追加AOF操作日志。

### 如何选择？

| 场景 | 推荐策略 |
|-----|---------|
| 数据安全性要求极高 | AOF + everysec |
| 追求最佳性能、可容忍少量数据丢失 | RDB |
| 两者都要 | 混合持久化 |
| 做灾难恢复 | RDB定期备份 + AOF |

> **常见误区**：AOF关闭appendonly也可用rdb持久化，但AOF开启且不记录bgsave命令时两者可共存。

---

## 题目三：Redis 的过期 key 删除策略是什么？

### 参考答案

Redis采用**惰性删除 + 定期删除**组合策略：

### 1. 惰性删除（Lazy Free）

```c
// 访问key时检查是否过期
if (isExpired(key)) {
    deleteKey(key);
}
```
**优点**：对CPU友好，只删除必须删除的  
**缺点**：过期key堆积可能占用内存

### 2. 定期删除

```
每100ms随机抽取设置了过期时间的key：
1. 抽取所有expires字典中的key
2. 删除已过期的key
3. 若过期key比例 > 25%，继续抽取
```

### 3. 内存淘汰策略（当内存满时）

| 策略 | 说明 |
|-----|-----|
| `noeviction` | 不淘汰，返回错误（默认）|
| `volatile-lru` | 只从设置过期时间的key中淘汰最久未用的 |
| `volatile-ttl` | 只从设置过期时间的key中淘汰TTL最短的 |
| `volatile-random` | 随机淘汰 |
| `allkeys-lru` | 所有key中淘汰最久未用的 |
| `allkeys-random` | 随机淘汰所有key |
| `volatile-lfu` | 用频率最少的（Redis 4.0+）|
| `allkeys-lfu` | 所有key用频率最少的（Redis 4.0+）|

**实战建议**：
- 缓存场景推荐 `allkeys-lru` 或 `volatile-lru`
- 验证码等临时数据用 `volatile-ttl`
- 敏感数据不要依赖过期策略删除，应主动DEL

---

## 题目四：Redis 主从复制原理是什么？哨兵和集群有什么区别？

### 参考答案

### 主从复制流程

```
1. 从库发送 PSYNC <runid> <offset> 到主库
2. 主库判断是全量还是增量同步：
   - 首次：FULLRESYNC，返回RDB文件
   - 续传：+CONTINUE，发送积压缓冲区命令
3. 从库接收RDB，清空旧数据，加载新数据
4. 主库此后将新写命令异步发送给从库
```

### 关键参数

```bash
repl-backlog-size 1mb      # 积压缓冲区大小（决定增量复制能力）
repl-timeout 60            # 复制超时时间
min-slaves-to-write 2      # 主库写需至少2个从库确认（5.0前）
```

### 哨兵（Sentinel）vs 集群（Cluster）

| 特性 | 哨兵模式 | 集群模式 |
|-----|---------|---------|
| **定位** | 高可用（HA）| 数据分片（分布式）|
| **数据存储** | 主从复制，每节点完整数据 | 16384个slot分片，每个节点存部分数据 |
| **故障转移** | 哨兵投票选举新主 | 从节点自动接管所属slot |
| **写操作** | 仅主库可写 | 只有master可写（每个slot有独立master）|
| **扩缩容** | 加从库即可 | 需迁移slot（手动或reshard）|
| **客户端** | 支持smart客户端 | 需支持MOVED重定向的smart客户端 |

### 选型建议

- **数据量 < 10GB**：哨兵模式足够，架构简单
- **数据量 > 10GB 或需高并发**：集群模式，支持水平扩展
- **写敏感场景**：选哨兵（主库写更稳），或集群+读写分离

---

## 题目五：Redis 如何实现分布式锁？有什么安全隐患？

### 参考答案

### 基础实现（SETNX）

```lua
-- 加锁
if redis.call('SET', key, value, 'NX', 'PX', expireMs) == 1 then
    return 1
else
    return 0
end

-- 解锁（需判断value是自己的）
if redis.call('GET', key) == requestId then
    redis.call('DEL', key)
    return 1
else
    return 0
end
```

### Redisson 封装（生产推荐）

```java
RLock lock = redisson.getLock("myLock");
try {
    // 等待30秒，锁定10秒后自动释放
    lock.tryLock(30, 10, TimeUnit.SECONDS);
    // 业务逻辑
} finally {
    if (lock.isHeldByCurrentThread()) {
        lock.unlock();
    }
}
```

### 安全隐患

| 问题 | 原因 | 解决方案 |
|-----|-----|---------|
| **锁过期，业务未完成** | 锁TTL设短了 | 看门狗（watchdog）自动续期 |
| **误删他人锁** | 解锁时没判断value | 用Lua脚本原子判断+删除 |
| **主从切换丢锁** | 主库加锁成功但未同步给从库 | RedLock算法（多主节点投票）|
| **时钟漂移** | 多节点时间不同步 | 用单调递增token而非时间戳 |

### RedLock 原理

```
1. 尝试在 N 个Redis节点上加锁（大部分成功则成功）
2. 计算获取锁耗时，锁自动释放时间需 > 耗时的2倍
3. 失败时向所有节点发送解锁命令
```

**注意**：RedLock 本身有争议，被质疑在分布式环境中未必安全，生产中也可考虑 etcd / ZooKeeper 实现分布式锁。

---

## 题目来源

1. Redis 官方文档：https://redis.io/docs/
2. Redis命令参考：http://doc.redisfans.com/
3. 《Redis设计与实现》- 黄健宏
4. 牛客网Redis面试题库
5. GitHub: rolrbend/redis-interview-questions

---

*整理不易，如果对你有帮助，欢迎 Star ⭐ 本仓库！*
