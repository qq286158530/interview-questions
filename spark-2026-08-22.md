# Spark 面试题汇总（2026-08-22）

> 本系列面试题来源于 GitHub 开源项目 [qq286158530/interview-questions](https://github.com/qq286158530/interview-questions)，持续更新，欢迎 Star。

---

## 题目一：Spark 的架构是怎样的？Driver 和 Executor 之间是什么关系？

### 参考答案

Spark 采用 **Master-Slave** 架构，核心组件包括：

| 组件 | 角色 | 职责 |
|------|------|------|
| **Driver** | Master | 负责任务调度（DAGScheduler、TaskScheduler），将 Job 划分为 Stage 和 Task |
| **Executor** | Slave | 真正执行 Task，存储计算结果，每个 Executor 是一个 JVM 进程 |
| **Master/ResourceManager** | 集群管理器 | 管理集群资源（Standalone / YARN / Kubernetes） |
| **Worker/NodeManager** | 节点代理 | 向 Master 注册节点，启动 Executor |

**Driver 与 Executor 的关系：**

1. **Client 模式**：Driver 在提交任务的客户端机器上运行，和 Executor 通信
2. **Cluster 模式**：Driver 被 Master 调度到某个 Worker 节点上运行
3. **通信方式**：Driver 与 Executor 之间通过 **Netty** 进行 RPC 通信，Executor 向 Driver 反向注册
4. **生命周期**：Executor 由 Worker 启动，为多个 Task 提供计算服务；Driver 掌控整个作业生命周期

**常见追问：**
- Spark Streaming 的 Receiver 运行在 Executor 内
- Executor 失败时，TaskScheduler 会重新调度到其他 Executor（Task 级别容错）

📎 **来源**：[Spark 官方文档 - Cluster Mode Overview](https://spark.apache.org/docs/latest/cluster-overview.html)

---

## 题目二：RDD 的血统（Lineage）机制是什么？如何实现容错？

### 参考答案

**Lineage（血统）** 是 Spark RDD 核心的容错机制，核心思想是 **"记录数据 transformations，不记录实际数据"**。

```
RDD A (读取HDFS) 
  → RDD B (map) 
    → RDD C (filter) 
      → RDD D (reduceByKey)
```

**容错原理：**

1. **宽依赖 vs 窄依赖**：
   - **窄依赖**：父 RDD 至多被一个子 RDD 使用（如 map、filter），可流水线执行，失败时只需重算对应分区
   - **宽依赖**：父 RDD 被多个子 RDD 使用（如 groupByKey、join），形成 Shuffle，失败需重新计算整个 Stage

2. **重算机制**：当某个 Partition 丢失时，Spark 根据 Lineage 从源头重新计算，而不是复制数据

3. **Checkpoint（检查点）**：
   - 对于长依赖链（如多次宽依赖串联），重算代价极高
   - `rdd.checkpoint()` 将 RDD 物化到可靠存储（HDFS），截断 Lineage

**与传统容错对比：**

| 机制 | Spark RDD | Hadoop MR |
|------|-----------|-----------|
| 容错粒度 | Partition（细粒度） | TaskAttempt（细粒度） |
| 恢复方式 | Lineage 重算 | Checkpoint / 数据复制 |
| 数据一致性 | 最终一致 | 强一致 |

📎 **来源**：[RDD Paper - Resilient Distributed Datasets](https://www.usenix.org/system/files/conference/nsdi12/nsdi12-final138.pdf)

---

## 题目三：DataFrame 和 DataSet 的区别是什么？什么时候该用哪个？

### 参考答案

**三者的关系：**

```
RDD[Row]  ←  untyped  ←  DataFrame = Dataset[Row]
Dataset[T]  ←  typed   ←  DataFrame（当 T = Row 时）
```

**DataFrame 特性：**
- 本质是 `Dataset[Row]`，是一种 **untyped**（编译时无类型检查）的分布式数据集
- 有 Schema，支持 SQL 查询，底层基于 **Tungsten Binary Format**
- 操作符如 `select`、`filter` 返回新的 DataFrame

**DataSet 特性：**
- **typed** API，编译时进行类型检查，IDE 可提示错误
- 支持 `Encoder` 进行序列化，序列化效率高于 Java/Kryo
- 可在 lambda 和 SQL 风格之间自由切换

**对比表：**

| 特性 | DataFrame | DataSet |
|------|-----------|---------|
| 类型安全 | 编译时无检查 | 编译时检查 |
| 编译提示 | 无 | 有 |
| 操作方式 | DSL / SQL | Lambda / SQL |
| 序列化 | Tungsten | Encoders |
| join 操作 | 容易运行时错误 | 编译时检查 |
| Catalyst 优化 | 完全支持 | 完全支持 |

**选择建议：**
- **用 DataFrame**：做 ETL、报表、SQL 查询，字段动态未知
- **用 DataSet**：业务代码中需要强类型，领域对象结构固定
- 在 **Spark SQL** 内部，三者最终都转化为 Spark Plan，无性能差异

📎 **来源**：[Spark 官方文档 - DataFrame API Guide](https://spark.apache.org/docs/latest/sql-programming-guide.html)

---

## 题目四：Spark Shuffle 的原理是什么？为什么 Shuffle 是性能瓶颈？

### 参考答案

**Shuffle 触发条件：** 当一个 RDD/DataFrame 操作需要将数据按 Key 重新分区聚合时（如 `groupByKey`、`join`、`reduceByKey`），会产生 Shuffle。

**执行过程（以 Shuffle Write 为例）：**

```
Stage 1 (Task 0, 1, 2)          Stage 2 (Task 0, 1, 2)
┌─────────────────┐             ┌─────────────────┐
│ map() 输出数据  │  ── Shuffle ──→  reduce() 读取数据  │
│ 按 partitioner  │             │ 按 key 聚合      │
│ 写入本地磁盘    │             │                  │
└─────────────────┘             └─────────────────┘
     Shuffle Write                   Shuffle Read
```

**Shuffle Write 细节：**
1. 每个 Map Task 将数据写入**本地磁盘**，生成多个文件（对应下游 Reduce 分区数）
2. 文件经过 **FileConsolidation** 合并（多个 Map Task 的同分区文件合成一个，减少文件数）
3. 每个分区对应一个 **bucket** 或 **spill 文件**

**Shuffle Read 细节：**
1. Reduce Task 通过 **Netty** 从多个 Map Task 所在节点拉取数据
2. 拉取的数据先放入 **HashMap** 缓存，聚合后执行计算

**成为瓶颈的原因：**

| 原因 | 说明 |
|------|------|
| **磁盘 I/O** | Shuffle 数据先落盘，再从网络拉取，双重磁盘读写 |
| **网络 I/O** | 跨节点数据传输，数据量大时网络带宽成为瓶颈 |
| **序列化开销** | 数据需要序列化/反序列化（Kryo/Java） |
| **内存压力** | Shuffle Read 端 HashMap 缓存数据，内存不足会溢写到磁盘 |
| **文件句柄** | 文件数过多（`spark.shuffle.file.buffer` 可调） |

**优化手段：**
- `spark.shuffle.file.buffer`（默认 32KB）调大
- 使用 **SortShuffle** 替代 **HashShuffle**（Spark 2.x+ 默认 SortShuffle）
- `spark.sql.shuffle.partitions` 调整分区数（默认 200）
- 避免 `groupByKey`，用 `reduceByKey`/`aggregateByKey` 替代

📎 **来源**：[Spark 官方文档 - Shuffle Behavior](https://spark.apache.org/docs/latest/rdd-programming-guide.html#shuffle-operations)

---

## 题目五：Spark 的内存管理模型是怎样的？堆内内存和堆外内存的区别是什么？

### 参考答案

Spark 2.x 采用 ** Unified Memory Management**（统一内存管理），将存储内存和执行内存打通，动态协调。

**内存划分（Heap Memory）：**

```
┌────────────────────────────────────────────┐
│                   Heap                     │
│  ┌──────────┐ ┌──────────┐ ┌────────────┐  │
│  │  存储    │ │  执行    │ │   其他     │  │
│  │ Storage  │ │ Execution│ │  Reserved  │  │
│  │ (缓存等) │ │(Shuffle  │ │ (300MB)    │  │
│  │          │ │ 排序等)  │ │            │  │
│  └──────────┘ └──────────┘ └────────────┘  │
│       ↑          ↑                          │
│   动态共享   ←──  →   （可相互借用）          │
└────────────────────────────────────────────┘
```

**执行内存（Execution Memory）：**
- Shuffle 排序、Hash 聚合、Join 的 Hash 表等
- 可以借用存储内存，反过来不行

**存储内存（Storage Memory）：**
- RDD Cache、Broadcast 变量、Task 结果缓存
- 可以借用执行内存（但被驱逐后需重新计算）

**堆外内存（Off-Heap Memory）：**
- 通过 `spark.memory.offHeap.enabled=true` 开启
- 使用 `sun.misc.Unsafe` 直接操作操作系统内存，绕过 JVM 堆
- 优点：减少 GC 开销，适合大内存场景（> 64GB）
- 缺点：需要手动管理，容易出现 native memory leak

**内存参数调优：**

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `spark.memory.fraction` | 0.6 | 堆内用于 Spark 的比例 |
| `spark.memory.storageFraction` | 0.5 | 存储内存占用的初始比例 |
| `spark.memory.offHeap.enabled` | false | 是否启用堆外内存 |
| `spark.memory.offHeap.size` | 0 | 堆外内存大小 |

📎 **来源**：[Spark 官方文档 - Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management)

---

> 📌 **往期回顾**：[Spark 面试题（8月21日）](./spark-2026-08-21.md)
