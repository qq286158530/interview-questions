# Redis 面试题精选（2026-08-13）

> 本文档整理自网络收集，包含 5 道高质量 Redis 面试题，涵盖数据类型、持久化、集群、缓存等核心知识点。

---

## 题目一：Redis 有哪几种数据类型？它们的典型应用场景是什么？

### 参考答案

Redis 支持五种基础数据类型：

| 数据类型 | 底层实现 | 典型应用场景 |
|---------|---------|-------------|
| **String** | SDS（简单动态字符串） | 缓存、计数器、分布式锁、session 共享 |
| **List** | QuickList（压缩列表+双向链表） | 消息队列、关注列表、时间轴、排行榜 |
| **Hash** | 哈希表或压缩列表（元素少时） | 对象存储、购物车、商品规格 |
| **Set** | 哈希表或整数集合（纯整数时） | 标签系统、去重、好友关系、共同关注 |
| **Zset** | 跳表（SkipList）+ 压缩列表 | 有序排行榜、延时队列、热度排名 |

**扩展类型（由底层类型组合实现）：**
- **Bitmap**：位图，适合签到、在线状态统计
- **HyperLogLog**：基数统计，适合 UV 统计
- **Geospatial**：地理位置，适合附近的人功能
- **Stream**：流式数据，适合消息队列

**答案要点：**
- String 类型最大能存储 512MB，数据量过大时应避免使用（用 Hash 拆分）
- List 是链表而非数组，LPUSH/LPOP 是栈结构，LPUSH/RPOP 是队列结构
- Zset 的 score 可用于权重排序，兼顾唯一性和有序性
- Hash 适合字段不固定的对象，避免滥用导致 bigkey 问题

> 📎 来源：[Redis 官方文档 - Data Types](https://redis.io/docs/data-types/)、[CSDN-Redis数据类型详解](https://blog.csdn.net/ThinkWon/article/details/103522397)

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？如何选择？

### 参考答案

Redis 提供两种持久化机制：

### RDB（Redis Database）

**原理：** 在指定时间点生成数据集的快照（Snapshot）， fork 一个子进程来完成写入，父进程继续处理命令。

**配置示例：**
```
save 900 1      # 900秒内至少1个key变化则触发bgsave
save 300 10     # 300秒内至少10个key变化则触发bgsave
save 60 10000   # 60秒内至少10000个key变化则触发bgsave
```

**优点：** 恢复速度快，适合大规模数据备份
**缺点：** 可能丢失最后一次快照之后的数据

### AOF（Append Only File）

**原理：** 记录每个写操作到文件，以 Redis 协议文本保存。重写可压缩文件体积。

**配置示例：**
```
appendonly yes
appendfsync everysec   # 每秒同步（折中方案）
# appendfsync always   # 每命令同步（最安全，性能差）
# appendfsync no       # 依赖OS同步（性能最好，风险高）
```

**优点：** 数据安全性更高，everysec 模式最多丢失1秒数据
**缺点：** 文件体积较大，恢复速度较慢

### 对比总结

| 特性 | RDB | AOF |
|-----|-----|-----|
| 文件体积 | 小 | 大 |
| 恢复速度 | 快 | 慢 |
| 数据完整性 | 可能丢失数据 | 较高 |
| fork 性能开销 | 有（fork 阻塞） | 无 |
| 适用场景 | 缓存、容灾备份 | 数据库 |

### 生产环境建议

- **同时开启** RDB + AOF，结合使用
- AOF 重写（bgrewriteaof）可控制文件大小
- 高写入场景建议 `appendfsync everysec`

> 📎 来源：[Redis 官方文档 - Persistence](https://redis.io/docs/management/persistence/)、[阿里云-Redis持久化机制](https://help.aliyun.com/document_detail/149968)

---

## 题目三：什么是 Redis 缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 参考答案

### 缓存穿透

**定义：** 查询一个不存在的数据，缓存和数据库都不存在，导致请求直接打到数据库。

**危害：** 恶意攻击可导致数据库崩溃。

**解决方案：**
1. **布隆过滤器（BloomFilter）**：将所有合法 key 存入过滤器，请求先经过过滤器过滤
2. **缓存空值**：即使 key 不存在，也缓存一个空值（如 `NULL`），并设置较短过期时间
3. **参数校验**：对请求参数进行合法性校验

### 缓存击穿

**定义：** 某个热点 key 过期瞬间，大量并发请求直接打到数据库。

**解决方案：**
1. **互斥锁（Mutex）**：只允许一个线程去查询数据库并回填缓存，其他线程等待
2. **逻辑过期**：不给缓存设置真正的过期时间，而是在 value 中包含过期时间字段，读到时用单独线程更新缓存
3. **热点数据永不过期**：评估热点数据，设置为更长的过期时间或永不过期

### 缓存雪崩

**定义：** 大量缓存 key 在同一时间过期，或 Redis 宕机，导致数据库压力骤增。

**解决方案：**
1. **过期时间分散**：给过期时间加上随机偏移量，避免集中过期
   ```java
   setRedis(key, value, time + Random.nextInt(300));
   ```
2. **多级缓存**：引入本地缓存（如 Caffeine）作为一级，Redis 作为二级
3. **高可用架构**：使用 Redis Cluster 或 Sentinel 保证 Redis 可用性
4. **熔级降级**：数据库压力大时返回默认值，避免数据库崩溃
5. **提前预热**：系统启动时主动加载热点数据到缓存

> 📎 来源：[美团技术团队-Redis缓存问题](https://tech.meituan.com/2017/03/02/cache-optimization.html)、[掘金-Redis三缓存问题](https://juejin.cn/post/684490412592702976)

---

## 题目四：Redis 主从复制原理是什么？为什么需要哨兵（Sentinel）？

### 参考答案

### 主从复制原理

Redis 主从复制采用 **PSYNC** 机制，支持全量同步和增量同步：

**全量同步流程：**
1. 从服务器向主服务器发送 `SYNC` 命令
2. 主服务器执行 `BGSAVE`，在后台生成 RDB 文件
3. 主服务器将 RDB 文件发送给从服务器
4. 主服务器将缓冲区的写命令发送给从服务器
5. 从服务器执行，完成同步

**增量同步（命令传播）：**
- 主服务器维护一个 **replication buffer** 和 **replication backlog**
- 每次写命令同时发送给所有从服务器
- 从服务器记录已处理的 offset，断线重连后根据 offset 补发缺失命令

**配置方式：**
```bash
# 从节点配置
replicaof 192.168.1.100 6379
replica-read-only yes  # 从节点只读
```

### 为什么需要哨兵（Sentinel）

**问题：** 主从模式下，主节点宕机后需要手动切换，运维成本高。

**哨兵职责：**
1. **监控（Monitoring）：** 监控主从节点运行状态
2. **自动故障转移（Automatic Failover）：** 主节点宕机时，自动选举从节点为新主节点
3. **通知（Notification）：** 故障转移后，通过 API 通知应用方
4. **配置提供（Configuration Provider）：** 客户端向哨兵询问当前主节点地址

**哨兵工作流程：**
```
主观下线 → 哨兵投票（SDOWN） → 客观下线（ODOWN）
       → 选举领头的哨兵 → 从节点投票选出新主
       → 旧主重置为新主的从节点
```

**配置示例：**
```bash
sentinel monitor mymaster 192.168.1.100 6379 2
sentinel down-after-milliseconds mymaster 30000
sentinel failover-timeout mymaster 180000
```

> 📎 来源：[Redis 官方文档 - Replication](https://redis.io/docs/management/replication/)、[Redis 官方文档 - Sentinel](https://redis.io/docs/management/sentinel/)

---

## 题目五：Redis Cluster 是如何工作的？它与 Codis/代理模式有什么区别？

### 参考答案

### Redis Cluster 工作原理

Redis Cluster 采用 **哈希槽（Hash Slot）** 方式进行数据分片：

**槽分配：**
- 共有 16384（2^14）个槽
- 每个 key 通过 `CRC16(key) % 16384` 计算所属槽位
- 槽分配给集群中的不同主节点

**集群结构：**
```
集群包含 N 个主节点，每个主节点可配 1~N 个从节点
示例：3主3从 -> 每主1从，共6个节点
```

**故障转移：**
- 半数以上主节点认为某个主节点不可达时，触发故障转移
- 从节点通过 Raft 协议选举，票数过半者成为新主节点

**客户端路由：**
- 客户端直连任意集群节点
- MOVED 重定向：节点返回正确节点地址，客户端刷新路由缓存
- ASK 重定向：仅针对正在迁移的槽

**配置示例：**
```bash
# 初始化集群（6个节点）
redis-cli --cluster create 192.168.1.1:6379 192.168.1.2:6379 \
  192.168.1.3:6379 192.168.1.4:6379 192.168.1.5:6379 192.168.1.6:6379 \
  --cluster-replicas 1
```

### Redis Cluster vs Codis vs 代理模式

| 特性 | Redis Cluster | Codis | 代理模式（如 Twemproxy） |
|-----|--------------|-------|------------------------|
| **架构** | 无中心化，客户端直连 | Proxy 代理层 | Proxy 代理层 |
| **数据迁移** | 支持（在线迁移槽） | 支持 | 不支持 |
| **客户端支持** | 需要智能客户端 | 任意 Redis 客户端 | 任意 Redis 客户端 |
| **性能开销** | 无中间层，最优 | Proxy 有开销 | Proxy 有开销 |
| **集群规模** | 最大 16384 节点（实际建议<1000） | 可管理大规模集群 | 受 Proxy 能力限制 |
| **跨槽操作** | 不支持（需客户端合并） | 支持（Proxy 聚合） | 支持 |
| **运维复杂度** | 较高 | 中等 | 较低 |

### 生产环境选择建议

- **小规模（< 10 节点）：** 直接使用 Redis Cluster
- **大规模、多租户：** Codis 更适合，提供更好的迁移和运维体验
- **简单场景、想要统一管理：** 代理模式（如 Redis-COPYs）

> 📎 来源：[Redis 官方文档 - Redis Cluster](https://redis.io/docs/management/scaling/)、[Codis 官方文档](https://github.com/CodisLabs/codis)、[美团技术团队-Codis集群](https://tech.meituan.com/2016/03/17/codis.html)

---

## 📚 更多资源

- [Redis 官方文档](https://redis.io/docs/)
- [Redis 命令参考](http://redisdoc.com/)
- [Redis 面试题汇总 - GitHub](https://github.com/qq286158530/interview-questions)

---

*整理于 2026-08-13 by 小憨宝 🐼*
