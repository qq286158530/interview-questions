# Flink 面试题精选（2026-08-08）

> 本文档整理自网络资源，涵盖 Flink 架构、Watermark、Checkpoint、窗口函数、状态管理等核心知识点。

---

## 题目一：Flink 的架构是怎样的？为什么说 Flink 是流计算引擎？

### 参考答案

Flink 采用**流优先**的架构，核心是 **Stream Execution Engine**，将批处理视为流处理的一种特例。

**核心组件：**

1. **JobManager（Master）**
   - 负责调度 Task、协调 Checkpoint、协调故障恢复
   - 包含 `ResourceManager`、`Dispatcher`、`JobMaster` 三个组件

2. **TaskManager（Worker）**
   - 负责执行具体的 Task（Subtask）
   - 每个 TaskManager 是一个 JVM 进程，槽位数（Slot）决定并发能力

3. **Slot 与资源**
   - Flink 的基本资源单位是 TaskSlot
   - 每个 Slot 可以执行一个 Subtask（包含完整的算子链）

**为什么说 Flink 是流计算引擎？**

- Flink 从设计之初就是**原生流处理**，批处理只是流处理的一个有界数据子集
- DataStream API 用于无界流，DataSet API 用于有界批处理
- 相比 Spark Streaming 的微批处理（Mini-Batch），Flink 是**真正的事件驱动逐条处理**，延迟更低

**数据流图（Stream Graph）：**
```
Source → Transformation → Sink
   ↓           ↓             ↓
Subtask    Subtask       Subtask
```

---

## 题目二：Flink 的 Watermark 是什么？如何处理乱序数据？

### 参考答案

**Watermark（水位线）** 是 Flink 用于处理**乱序事件**和**事件时间（Event Time）**的核心机制。

### 基本概念

- Watermark 是一个**时间戳 T**，表示"所有时间戳 ≤ T 的事件已经全部到达"
- 本质上是一种**启发式断言（Heuristic Assertion）**，告诉算子"不会再有时间戳更小的事件了"

### 解决乱序的原理

在事件时间模式下，数据到达可能乱序（比如网络延迟），Watermark 的作用：

```
Watermark T 的含义：假设已到达所有时间戳 ≤ T 的数据
```

**窗口触发时机：** 当 `watermark > window_end_time` 时，窗口闭合，不再等待迟到的数据。

### 三种策略

| 策略 | 说明 | 场景 |
|------|------|------|
| **固定延迟（Fixed）** | 固定周期插入 Watermark | 简单场景 |
| **单调递增（Monotonous）** | 严格递增的 Watermark | 有序数据 |
| **周期性（Periodic）** | 每隔 N 条或每隔时间插入 | 通用场景 |

### 代码示例

```java
// 方式1：固定延迟 Watermark
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofSeconds(10))
    .withTimestampAssigner(..., "timestamp");

// 方式2：单并行度单调递增 Watermark
WatermarkStrategy
    .forMonotonousTimestamps()
    .withTimestampAssigner(..., "timestamp");
```

### 迟到数据处理

- **allowedLateness**：窗口保留迟到数据的时间窗口，超时后清除状态
- **sideOutputLateData**：将迟到数据输出到侧输出流

```java
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofSeconds(10))
    .withTimestampAssigner(..., "timestamp")
    .withAllowedLateness(Duration.ofMinutes(5));  // 允许5分钟迟到
```

---

## 题目三：Flink 的 Checkpoint 和 Savepoint 有什么区别？如何保证 Exactly-Once 语义？

### 参考答案

### Checkpoint vs Savepoint

| 特性 | Checkpoint | Savepoint |
|------|------------|-----------|
| **触发方式** | Flink 自动周期性触发 | 手动触发（用户命令） |
| **存储位置** | JobManager 内存（可配置） | 外部文件系统（HDFS/S3） |
| **用途** | 故障自动恢复 | 计划性停止/重启/升级 |
| **清理策略** | Job 删除时自动清理 | 手动删除 |
| **格式** | 轻量，专为快速恢复设计 | 重量级，兼容版本升级 |

### Checkpoint 原理（Chandy-Lamport 算法变体）

Flink 采用 **分布式快照（Barrier Alignment）** 算法：

1. **Checkpoint Coordinator** 定期向 Source 算子发送 Barrier
2. Source 收到 Barrier 后，**保存自身状态**，然后**广播 Barrier 到下游**
3. 中间算子收到所有输入的 Barrier 后（Barrier Alignment），保存状态并向下游传递
4. 所有算子完成 Checkpoint 后，向 Coordinator 确认

### Exactly-Once 语义保证

Exactly-Once = **每条数据被处理且仅处理一次**（端到端）

需要三个条件同时满足：

1. **Source 端**：可重置数据读取位置（如 Kafka Consumer 的 Offset 提交）
2. **Flink 内部**：Checkpoint 保存完整算子状态，故障后从最近快照恢复
3. **Sink 端**：支持幂等写入或事务写入（如 Redis 幂等、Kafka 事务）

### Sink 的 Exactly-Once 实现

| Sink 类型 | 语义 | 原理 |
|-----------|------|------|
| **Kafka Sink（两阶段提交）** | Exactly-Once | 预提交 + 正式提交 |
| **Redis Sink** | At-Least-Once | 建议结合去重 |
| **JDBC Sink** | At-Least-Once | 建议使用幂等键或事务 |
| **File Sink** | Exactly-Once | 原子性文件移动 |

### 两阶段提交（2PC）实现

```java
// Kafka Transactions 实现
1. preCommit: 预提交 producer transaction
2. commit:   确认 Checkpoint 完成，提交 transaction
3. abort:    Checkpoint 失败，中止 transaction
```

---

## 题目四：Flink 的窗口函数有哪些？如何选择？滚动窗口、滑动窗口、会话窗口的区别？

### 参考答案

### 窗口分类

```
┌─────────────────────────────────────────────────────────────┐
│                        窗口类型                              │
├───────────────┬──────────────────┬──────────────────────────┤
│   Time Window │  Count Window    │    Session Window        │
├───────────────┼──────────────────┼──────────────────────────┤
│  Tumbling     │  Tumbling        │    Session Gap           │
│  Sliding      │  Sliding         │                          │
│  Count        │  Count           │                          │
└───────────────┴──────────────────┴──────────────────────────┘
```

### 1. 滚动窗口（Tumbling Window）

- **特点**：窗口大小固定，**不重叠**，首尾相接
- 适用场景：固定周期统计，如"每分钟订单数"

```java
// 每60秒一个窗口
timeWindow(Time.seconds(60))

// 每100条一个窗口
countWindow(100)
```

### 2. 滑动窗口（Sliding Window）

- **特点**：窗口大小固定，**可重叠**，由窗口大小和滑动步长决定
- 适用场景：移动平均、持续监控

```java
// 窗口大小60秒，滑动步长10秒
timeWindow(Time.seconds(60), Time.seconds(10))

// 窗口100条，滑动50条
countWindow(100, 50)
```

### 3. 会话窗口（Session Window）

- **特点**：由**事件之间的间隔**决定窗口边界，而非固定时间
- 适用场景：用户行为分析、异常检测

```java
// 会话间隔超过30秒则开启新窗口
window(SessionWindows.withGap(Time.seconds(30)))
```

### 全窗口函数（AllWindowFunction）

对于需要访问窗口内**所有数据**的场景：

```java
// 窗口化之后调用 process
.windowAll(TumblingEventTimeWindows.of(Time.seconds(60)))
.process(new ProcessAllWindowFunction<...>() {
    @Override
    public void process(Context ctx, Iterable<...> elements) {
        // 可以遍历窗口内所有元素
    }
});
```

### 增量聚合 vs 全量聚合

| 模式 | 适用函数 | 特点 | 内存占用 |
|------|----------|------|----------|
| **增量聚合** | `reduceFunction`, `aggregateFunction` | 来一条处理一条，增量聚合 | 低 |
| **全量聚合** | `processWindowFunction` | 窗口关闭后遍历全部数据 | 高 |

```java
// 增量聚合：性能好，适合 sum/avg/max/min
.reduce((v1, v2) -> v1 + v2)

// 全量聚合：可以获得窗口元信息（时间、触发次数等）
.process(new ProcessWindowFunction<...>())
```

---

## 题目五：Flink 的状态管理机制是怎样的？Keyed State 和 Operator State 的区别？

### 参考答案

### Flink 状态分类

```
┌──────────────────────────────────────────────────────────┐
│                      Flink State                         │
├────────────────────────┬─────────────────────────────────┤
│     Keyed State        │         Operator State          │
├────────────────────────┼─────────────────────────────────┤
│ 与 Key 绑定的状态       │ 与算子实例绑定的状态              │
│ 必须先 keyBy           │ 不需要 keyBy                     │
│ 支持多种数据结构        │ 通常是 List 或 Union             │
└────────────────────────┴─────────────────────────────────┘
```

### Keyed State（键控状态）

必须在 `keyBy` 之后使用，状态按 Key 分区，每个 Key 独立维护状态。

**支持的数据结构：**

| 类型 | 说明 |
|------|------|
| `ValueState<T>` | 单值状态，存储单个值 |
| `ListState<T>` | 列表状态 |
| `MapState<K, V>` | 映射状态 |
| `ReducingState<T>` | 自动聚合的状态 |
| `AggregatingState<I, O>` | 聚合后输出的状态 |

**代码示例：**

```java
DataStream<String> stream = ...

stream.keyBy(r -> r.getKey())
    .process(new KeyedProcessFunction<String, String, String>() {
        
        // 声明状态
        private ValueState<Integer> countState;
        private ListState<String> historyState;
        
        @Override
        public void open(Configuration parameters) {
            // 初始化状态
            countState = getRuntimeContext().getState(
                new ValueStateDescriptor<>("count", Integer.class));
            historyState = getRuntimeContext().getListState(
                new ListStateDescriptor<>("history", String.class));
        }
        
        @Override
        public void processElement(String value, Context ctx, Collector<String> out) {
            // 使用状态
            Integer count = countState.value() == null ? 0 : countState.value() + 1;
            countState.update(count);
            historyState.add(value);
        }
    });
```

### Operator State（算子状态）

与 Key 无关，所有并行实例共享或独立维护状态。

**两种模式：**

1. **ListState**：每个算子实例持有一个列表状态，并行度变化时均匀分配
2. **UnionListState**：并行度变化时，每个算子实例获取完整状态列表，自己选取需要部分
3. **BroadcastState**：广播状态， 所有并行实例持有一份相同副本

**代码示例：**

```java
// Kafka Connector 的 Operator State
public class CustomSource implements SourceFunction<String>, CheckpointedFunction {
    
    private ListState<String> checkpointedState;
    
    @Override
    public void initializeState(FunctionInitializationContext context) {
        // 从 Checkpoint 恢复状态
        checkpointedState = context.getOperatorStateStore()
            .getListState(new ListStateDescriptor<>("source-state", String.class));
    }
    
    @Override
    public void snapshotState(FunctionSnapshotContext context) {
        // Checkpoint 时保存状态
        checkpointedState.clear();
        checkpointedState.addAll(bufferedElements);
    }
}
```

### 状态后端（State Backend）

| 后端 | 存储位置 | 适用场景 |
|------|----------|----------|
| **HashMapStateBackend** | TaskManager JVM 堆内存 | 小状态、低延迟 |
| **EmbeddedRocksDBStateBackend** | RocksDB（本地磁盘） | 大状态、增量 Checkpoint |

```java
// 配置状态后端
env.setStateBackend(new EmbeddedRocksDBStateBackend());
// 或
env.setStateBackend(new HashMapStateBackend());
```

### 状态过期与清理

```java
// 状态 TTL（Time-To-Live）
StateTtlConfig ttlConfig = StateTtlConfig.newBuilder(Time.hours(1))
    .setUpdateType(StateTtlConfig.UpdateType.OnReadAndWrite)
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired)
    .build();

ValueStateDescriptor<String> stateDesc = 
    new ValueStateDescriptor<>("my-state", String.class);
stateDesc.enableTimeToLive(ttlConfig);
```

---

## 参考来源

1. Flink 官方文档 - DataStream API：https://nightlies.apache.org/flink/flink-docs-stable/docs/concepts/overview/
2. Flink 官方文档 - Checkpointing：https://nightlies.apache.org/flink/flink-docs-stable/docs/ops/state/checkpoints/
3. Flink 官方文档 - State Backends：https://nightlies.apache.org/flink/flink-docs-stable/docs/ops/state/state_backends/
4. Flink 官方文档 - Windows：https://nightlies.apache.org/flink/flink-docs-stable/docs/dev/datastream/operators/windows/
5. Flink 官方文档 - Event Time & Watermarks：https://nightlies.apache.org/flink/flink-docs-stable/docs/concepts/time/

---

*整理by AI助手 | 公众号：xxx（侵删）*
