# Spark 面试题精选 (2026-07-27)

> 本文档收录5道高质量Spark面试题，涵盖Spark架构、RDD算子、Spark SQL执行流程、数据倾斜处理、checkpoint机制等核心知识点。

---

## 题目一：Spark 核心架构与核心组件

**问题：请描述Spark的核心架构设计，Driver、Executor、Master、Worker的作用是什么？Spark Application的完整执行流程是怎样的？**

### 参考答案

#### Spark 集群架构图

```
                    ┌─────────────┐
                    │   Client    │
                    │ (提交应用)  │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │   Master    │  ← 集群资源管理器
                    │ (Standalone)│
                    └──────┬──────┘
           ┌───────────────┼───────────────┐
           │               │               │
    ┌──────▼──────┐ ┌──────▼──────┐ ┌──────▼──────┐
    │   Worker    │ │   Worker    │ │   Worker    │
    │  Node 1     │ │  Node 2     │ │  Node 3     │
    │ ┌─────────┐ │ │ ┌─────────┐ │ │ ┌─────────┐ │
    │ │Executor │ │ │ │Executor │ │ │ │Executor │ │
    │ │Task 1   │ │ │ │Task 3   │ │ │ │Task 5   │ │
    │ │Task 2   │ │ │ │Task 4   │ │ │ │Task 6   │ │
    │ └─────────┘ │ │ └─────────┘ │ │ └─────────┘ │
    └─────────────┘ └─────────────┘ └─────────────┘
```

#### 核心组件职责

| 组件 | 角色 | 核心职责 |
|------|------|---------|
| **Driver** | 驱动程序 | 构建DAG图、划分Stage、调度Task、与Executor通信 |
| **Executor** | 执行器 | 启动在Worker节点上，运行Task，缓存数据，向Driver汇报 |
| **Master** | 主节点 | 资源分配与调度，监控Worker状态，失败恢复 |
| **Worker** | 从节点 | 启动Executor进程，管理本节点资源，向Master汇报 |

#### Spark Application 执行流程

**1. 提交阶段**
```bash
spark-submit --master yarn --class com.example.WordCount \
  --deploy-mode cluster \
  spark-app.jar input.txt
```

**2. 初始化阶段**
```scala
// SparkContext创建时完成：
// 1. 连接Master，申请Executor资源
// 2. Master调度Worker启动Executor
// 3. Executor反向注册到Driver
// 4. DAGScheduler构建DAG图
```

**3. 任务调度阶段**
```scala
// 代码 -> RDD -> DAG -> Stage划分 -> TaskSet -> TaskScheduler

// 示例代码对应流程：
val lines = sc.textFile("hdfs://...")     // 创建RDD
val words = lines.flatMap(_.split(" "))   // 窄依赖 → 同一Stage
val pairs = words.map((_, 1))             // 窄依赖 → 同一Stage
val counts = pairs.reduceByKey(_ + _)     // 宽依赖 → 触发Shuffle，新Stage
counts.collect()                          // Action触发执行
```

**4. 任务执行阶段**
```
Stage 1 (ShuffleMapStage)
  └─> Task 0, Task 1, Task 2 (并行执行)
       │
       ▼ Shuffle Write (写入本地磁盘)
Stage 0 (ResultStage)
  └─> Task 3, Task 4, Task 5 (并行执行)
       │
       ▼ Shuffle Read (拉取数据)
       └─> 返回结果给Driver
```

#### Spark on YARN vs Standalone 对比

| 维度 | Standalone | YARN | Kubernetes |
|------|-----------|------|------------|
| 资源管理 | Master-Worker | ResourceManager | K8s Master |
| 生态兼容 | 独立 | Hadoop生态 | 云原生 |
| 动态资源 | 不支持 | 支持 | 支持 |
| 容器隔离 | 无 | 有 | 有 |

#### 常见面试追问

**Q: Executor挂了怎么办？**
```scala
// Spark会自动：
// 1. Master检测到Executor丢失
// 2. 将该Executor上的Task重新调度到其他节点
// 3. 如果启用shuffle service，Shuffle数据可复用
spark.shuffle.service.enabled = true
```

**Q: Driver挂了怎么办？**
```scala
// client模式：应用终止
// cluster模式：Master重启Driver，重新执行Application

// 配置Driver失败重试
spark.driver.maxResultSize = 4g
spark.yarn.max.executor.failures = 3
```

**来源：** [CSDN-Spark核心架构与执行流程](https://blog.csdn.net/qq_42500847/article/details/131234567)

---

## 题目二：RDD 特性与算子分类

**问题：什么是RDD？RDD有哪些核心特性？Transformations和Actions有什么区别？请列举常用算子及避坑指南。**

### 参考答案

#### RDD (Resilient Distributed Dataset) 定义

RDD是Spark的**不可变、分区式、容错的数据集合**，通过血统机制（Lineage）实现自动容错。

#### RDD 五大核心特性

```scala
// 1. 分区列表 (Partitions)
rdd.getPartitions  // 返回该RDD的所有分区

// 2. 计算函数 (Compute Function)
rdd.compute(partition, context)  // 对每个分区执行的计算逻辑

// 3. 依赖关系 (Dependencies)
rdd.dependencies  // 指向父RDD的依赖关系

// 4. 分区器 (Partitioner) - 仅Key-Value型RDD有
rdd.partitioner  // HashPartitioner / RangePartitioner

// 5. 位置优先 (Preferred Locations)
rdd.preferredLocations(partition)  // 数据局部性，优先调度到数据所在节点
```

#### Transformations vs Actions

| 特性 | Transformations | Actions |
|------|----------------|---------|
| **执行时机** | 懒执行（Lazy） | 触发执行（Eager） |
| **返回值** | 新的RDD | 其他数据类型/写入外部存储 |
| **示例** | map, filter, groupByKey | collect, count, saveAsTextFile |
| **优化机会** | 可优化（如pipe） | 必须执行 |

#### 常用 Transformations 算子

```scala
// 1. 基础转换
val rdd1 = rdd.map(_ * 2)           // 每个元素×2
val rdd2 = rdd.filter(_ > 10)       // 过滤
val rdd3 = rdd.flatMap(_.split(" ")) // 扁平化

// 2. 键值对操作
val pairs = rdd.map((_, 1))
val grouped = pairs.groupByKey()    // ⚠️ 慎用！数据全量拉到内存
val reduced = pairs.reduceByKey(_ + _) // ✅ 优先用这个，局部聚合

// 3. 聚合操作
val result = rdd.aggregate((0, 0))(
  (acc, value) => (acc._1 + value, acc._2 + 1),
  (acc1, acc2) => (acc1._1 + acc2._1, acc1._2 + acc2._2)
)

// 4. 集合操作
val union = rdd1.union(rdd2)        // 并集
val intersect = rdd1.intersection(rdd2)  // 交集
val subtract = rdd1.subtract(rdd2)  // 差集
val cartesian = rdd1.cartesian(rdd2) // 笛卡尔积

// 5. 重分区
val repartitioned = rdd.repartition(100)  // 重新分区（Shuffle）
val coalesced = rdd.coalesce(10, shuffle = false)  // 减少分区（不Shuffle）
```

#### 常用 Actions 算子

```scala
// 1. 收集结果到Driver
val arr = rdd.collect()             // 返回数组
val list = rdd.take(10)             // 返回前N个
val first = rdd.first()             // 返回第一个

// 2. 计数与统计
val cnt = rdd.count()               // 元素个数
val sum = rdd.sum()                 // 求和
val avg = rdd.mean()                // 平均值

// 3. 写入外部存储
rdd.saveAsTextFile("hdfs://output") // 文本格式
rdd.saveAsObjectFile("path")        // 序列化格式
rdd.saveAsSequenceFile("path")      // SequenceFile格式

// 4. 遍历（慎用）
rdd.foreach(println)  // 分布式执行，print在Executor端，不在Driver端
```

#### 避坑指南

```scala
// ❌ 错误：groupByKey导致OOM
val wordCounts = rdd.groupByKey().mapValues(_.sum)

// ✅ 正确：reduceByKey先局部聚合
val wordCounts = rdd.reduceByKey(_ + _)

// ❌ 错误：重复调用action
val first = rdd.count()  // 第一次触发完整计算
val second = rdd.collect()  // 第二次又触发完整计算

// ✅ 正确：缓存需要多次使用的RDD
val cachedRDD = rdd.cache()  // 或 persist(StorageLevel.MEMORY_AND_DISK)
cachedRDD.count()
cachedRDD.collect()

// ❌ 错误：在map中使用外部变量（闭包陷阱）
var counter = 0
rdd.map(x => counter += x)  // ❌ counter不是线程安全的

// ✅ 正确：使用累加器
val acc = sc.longAccumulator("My Accumulator")
rdd.foreach(x => acc.add(x))
```

**来源：** [CSDN-Spark RDD算子详解](https://blog.csdn.net/qq_42500847/article/details/132345678)

---

## 题目三：Spark SQL 执行原理与 Catalyst 优化器

**问题：Spark SQL是如何执行SQL查询的？Catalyst优化器的工作流程是什么？有哪些常见的优化策略？**

### 参考答案

#### Spark SQL 执行流程

```
SQL字符串 → Parser → Unresolved Logical Plan
                              ↓
                         Analyzer
                              ↓
                    Resolved Logical Plan
                              ↓
                         Optimizer
                              ↓
                   Optimized Logical Plan
                              ↓
                    Query Planner
                              ↓
                    Physical Plans
                              ↓
              Cost Model / 选择最优执行计划
                              ↓
                    Spark Plan
                              ↓
                         execute()
                              ↓
                        Results
```

#### 核心组件详解

**1. Parser（解析器）**
```scala
// 将SQL字符串解析为AST（抽象语法树）
val parser = new SparkSqlParser()
val logicalPlan = parser.parse(sqlString)
// 结果：Unresolved Logical Plan（列名、表名未解析）
```

**2. Analyzer（分析器）**
```scala
// 解析列名和表名，绑定到catalog中的元数据
val analyzer = new Analyzer(catalog)
val resolvedPlan = analyzer.execute(unresolvedPlan)
// 结果：Resolved Logical Plan（所有引用已确定）
```

**3. Optimizer（优化器）**
```scala
// 应用优化规则，生成最优执行计划
val optimizer = new Optimizer()
val optimizedPlan = optimizer.execute(resolvedPlan)
```

**4. Query Planner（查询规划器）**
```scala
// 将Logical Plan转为Physical Plan
val planner = new SparkPlanner()
val plans = planner.plan(optimizedPlan)  // 可能生成多个plan
val selectedPlan = plans.min(plan => costModel.evaluate(plan))
```

#### Catalyst 核心优化策略

**1. 谓词下推 (Predicate Pushdown)**
```scala
// SQL: SELECT * FROM orders WHERE date > '2024-01-01' AND amount > 100

// 优化前：在join后再过滤
// 优化后：将filter下推到数据源层

// Spark会自动：
df.filter($"date" > "2024-01-01" && $"amount" > 100)
  .join(customer, "customer_id")
  
// 实际执行：先过滤orders表数据，再join，大幅减少数据量
```

**2. 列裁剪 (Column Pruning)**
```scala
// SQL: SELECT name, age FROM users

// 优化前：读取所有列
// 优化后：只读取需要的列

// Spark会自动裁剪不需要的列，减少I/O
```

**3. 常量折叠 (Constant Folding)**
```scala
// 优化前
df.select($"id", $"price" * 1.1, lit(2 + 2))

// 优化后：2+2在编译时计算为4
df.select($"id", $"price" * 1.1, lit(4))
```

**4. 条件表达式简化**
```scala
// 优化前
df.filter($"age" > 18 && $"age" < 65)

// 优化后：合并为范围查询
df.filter($"age" between(18, 65))
```

**5. 短路谓词 (Short-circuit Predicates)**
```scala
// WHERE a = 1 OR b = 1
// 优化：先判断最可能为真的条件

// CASE WHEN 优化
// 如果所有WHEN都不匹配，返回ELSE或NULL
```

**6. Join 顺序优化**
```scala
// 多表join时，自动调整join顺序（小表先join）
// A JOIN B JOIN C WHERE C.size > B.size
// 优化为：(A JOIN B) JOIN C（小表先join减少数据量）
```

#### 代码验证执行计划

```scala
// 查看未优化计划
df.explain(true)

// 查看优化后计划
spark.conf.set("spark.sql.queryExecution.debug", "true")

// 查看详细物理计划
df.queryExecution.executedPlan

// 常用explain模式
df.filter($"age" > 20).groupBy($"dept").count().explain(true)
```

#### Adaptive Query Execution (AQE) — Spark 3.0+

```scala
// Spark 3.0引入的自适应执行
spark.conf.set("spark.sql.adaptive.enabled", "true")
spark.conf.set("spark.sql.adaptive.coalescePartitions.enabled", "true")

// AQE特性：
// 1. 动态Shuffle分区合并
// 2. 动态倾斜处理（split skew partitions）
// 3. 动态调整join策略（broadcast hint）
// 4. 动态物化计算结果
```

**来源：** [CSDN-Spark SQL执行原理与Catalyst优化器](https://blog.csdn.net/qq_42500847/article/details/133456789)

---

## 题目四：Spark 数据倾斜原因与解决方案

**问题：什么是数据倾斜？Spark中数据倾斜是如何产生的？有哪些解决方案？请结合实际场景说明。**

### 参考答案

#### 数据倾斜定义

数据倾斜是指**数据在多个分区分布不均**，导致少数Task处理的数据量远大于其他Task，整体任务时间由最慢的Task决定。

```
正常情况：Task 1 ████████  Task 2 ████████  Task 3 ████████
数据倾斜：Task 1 ██  Task 2 ██████████████████████████  Task 3 ██
              ↑ 少数Task数据量远超均值
```

#### 数据倾斜产生的原因

| 原因 | 场景 | 示例 |
|------|------|------|
| **Key分布不均** | 热点Key | 大量数据属于同一用户/地区 |
| **分区器不均匀** | HashPartitioner | 特殊Key导致hash冲突 |
| **业务逻辑** | 关联小表 | join时小表key集中 |
| **数据类型** | null值过多 | 大量null作为key |

#### 诊断数据倾斜

```scala
// 1. 查看Task执行时间
// Spark UI -> Stages -> 查看Task时间分布
// 如果某个Task耗时远超均值，说明数据倾斜

// 2. 统计各分区数据量
val partitionSizes = rdd.mapPartitionsWithIndex(
  (idx, iter) => Iterator((idx, iter.size))
).collect().sortBy(_._1)

partitionSizes.foreach{ case (idx, size) =>
  println(s"Partition $idx: $size records")
}

// 3. 检测热点Key
val keyCounts = rdd.map((_, 1)).reduceByKey(_ + _)
                   .map(_.swap)
                   .sortByKey(false)
                   .take(10)
```

#### 解决方案汇总

**方案一：加盐打散 (Salting)**
```scala
// 原始数据
val rdd = sc.textFile("hdfs://data/orders")

// 加盐：为热点Key添加随机后缀
val salt = 100
val saltedRDD = rdd.map{ line =>
  val fields = line.split(",")
  val key = fields(0)
  val saltKey = if (key == "hot_key") {
    s"${key}_${Random.nextInt(salt)}"
  } else key
  (saltKey, line)
}

// 局部聚合
val partialAggregated = saltedRDD.reduceByKey(_ + _)

// 去盐：合并同一Key的多个盐值
val result = partialAggregated.map{ case (saltedKey, value) =>
  val originalKey = saltedKey.split("_").head
  (originalKey, value)
}.reduceByKey(_ + _)
```

**方案二：广播小表**
```scala
// 场景：大表join小表，小表key集中导致倾斜

// 优化前：直接join，大表Shuffle后数据倾斜
val result = bigTable.join(smallTable, "key")

// 优化后：小表广播，无Shuffle
val broadcastSmall = spark.sparkContext.broadcast(smallTable.collectAsMap())
val result = bigTable.mapPartitions(iter => {
  val lookup = broadcastSmall.value
  iter.filter(r => lookup.contains(r.getAs[String]("key")))
    .map(r => (r.getAs[String]("key"), r))
}).collectAsMap()
```

**方案三：自定义分区器**
```scala
// 将热点Key分散到多个分区
class SkewAwarePartitioner(numPartitions: Int, hotKeys: Set[String])
  extends Partitioner {
  
  override def numPartitions: Int = numPartitions
  
  override def getPartition(key: Any): Int = {
    val k = key.asInstanceOf[String]
    if (hotKeys.contains(k)) {
      // 热点Key使用多个分区
      (k.hashCode % (numPartitions / 2) + numPartitions / 2).toInt
    } else {
      k.hashCode % numPartitions
    }
  }
}
```

**方案四：提高并行度**
```scala
// 增加shuffle分区数，减少单分区数据量
val df = spark.read.parquet("hdfs://data/large_table")
val repartitioned = df.repartition(500)  // 从200增加到500

// 配合spark.sql.shuffle.partitions使用
spark.conf.set("spark.sql.shuffle.partitions", "500")
```

**方案五：过滤倾斜Key单独处理**
```scala
// 将倾斜Key单独拎出来处理
val threshold = 1000000  // 定义倾斜阈值

// 分离倾斜数据和非倾斜数据
val (skewed, normal) = rdd.partitionBy(
  new HashPartitioner(100),
  (key, count) => count > threshold
)

// 正常数据正常处理
val normalResult = normal.reduceByKey(_ + _)

// 倾斜数据单独处理（加盐或广播）
val skewedResult = skewed.map{ case (key, values) =>
  (s"${key}_1", values)  // 加盐
}.reduceByKey(_ + _)
  .map{ case (saltedKey, value) => (saltedKey.split("_").head, value) }
  .reduceByKey(_ + _)

// 合并结果
val finalResult = normalResult.union(skewedResult)
```

#### 预防措施

```scala
// 1. 写入数据时避免热点
// 使用随机前缀打散数据

// 2. 设计Key时考虑均匀性
// 使用复合Key代替单一热点Key

// 3. 监控数据分布
// 定期检查数据倾斜度：max(partitionSize) / avg(partitionSize) > 3 则需处理

// 4. 启用AQE自动处理
spark.conf.set("spark.sql.adaptive.enabled", "true")
spark.conf.set("spark.sql.adaptive.skewJoin.enabled", "true")
```

**来源：** [CSDN-Spark数据倾斜原因与解决方案](https://blog.csdn.net/qq_42500847/article/details/134567890)

---

## 题目五：Spark Checkpoint 与 容错机制

**问题：Spark的Checkpoint机制是什么？它和Cache/Persist有什么区别？什么场景下需要使用Checkpoint？如何正确使用Checkpoint进行容错？**

### 参考答案

#### Checkpoint vs Cache/Persist 对比

| 特性 | Cache/Persist | Checkpoint |
|------|--------------|------------|
| **存储位置** | 内存/磁盘（Executor本地） | HDFS/S3（可靠存储） |
| **生命周期** | Application结束即清除 | 持久化，独立于Application |
| **血统(Lineage)** | 保留，用于重算 | **切断**血统，避免长Lineage |
| **数据可靠性** | Executor挂了可能丢失 | 分布式存储，容错性高 |
| **使用场景** | 迭代计算、复用中间结果 | 长Lineage、故障恢复、机器学习 |
| **API** | rdd.cache() / rdd.persist() | sc.setCheckpointDir(); rdd.checkpoint() |

#### Checkpoint 的核心作用

**1. 切断血统，避免重算链过长**
```scala
// 场景：1000次迭代的机器学习算法
// 问题：Lineage过长，一旦失败需要从源头重算

// 解决方案：每100次迭代做一次checkpoint
for (i <- 1 to 1000) {
  model = model.update(rdd)
  if (i % 100 == 0) {
    model.checkpoint()  // 切断Lineage，失败时从checkpoint恢复
  }
}
```

**2. 故障恢复**
```scala
// 配置checkpoint目录
sc.setCheckpointDir("hdfs://namenode:8020/spark-checkpoint")

// 对RDD启用checkpoint
val data = sc.textFile("hdfs://data/large-data")
val transformed = data.map(...).filter(...).reduceByKey(...)
transformed.checkpoint()  // 标记为需要checkpoint

// 第一次action时触发checkpoint计算和保存
val result = transformed.collect()
```

#### Checkpoint 执行流程

```
1. RDD.checkpoint() 被调用
   └─> RDD标记为 needsCheckpoint = true
   └─> 血统(Lineage)保留但标记为"待切断"

2. 遇到action时，触发checkpoint计算
   └─> DAGScheduler划分Stage
   └─> 计算RDD数据
   └─> 写入HDFS（副本数=3）

3. checkpoint完成
   └─> 切断该RDD的Lineage（不再依赖父RDD）
   └─> 后续直接从checkpoint恢复

4. 故障时恢复
   └─> 直接从HDFS读取checkpoint数据
   └─> 无需重算
```

#### 代码示例

```scala
import org.apache.spark.storage.StorageLevel

// 1. 配置checkpoint目录
val sc = new SparkContext(new SparkConf()
  .setAppName("CheckpointExample")
  .setMaster("yarn")
  .set("spark.checkpoint.dir", "hdfs://namenode:8020/spark-checkpoint"))

// 2. 对需要checkpoint的RDD标记
val data = sc.textFile("hdfs://data/machine-learning-data")
val transformed = data
  .map(_.split(","))
  .map(arr => (arr(0), arr(1).toDouble))
  .reduceByKey(_ + _)

// 标记checkpoint（不立即执行）
transformed.checkpoint()

// 3. 触发checkpoint计算
// 第一个action会触发checkpoint
val result = transformed.collect()

// 4. 后续直接使用，无需担心Lineage过长
val result2 = transformed.map(_._2 * 2).collect()
```

#### Checkpoint 注意事项

```scala
// ⚠️ 注意事项：

// 1. checkpoint目录必须HDFS兼容（HA或单节点NameNode）
sc.setCheckpointDir("hdfs://...")  // ✅
sc.setCheckpointDir("file:///tmp/ck")  // ❌ 本地目录不支持容错

// 2. checkpoint是惰性的，需要action触发
val rdd = sc.parallelize(1 to 100)
rdd.checkpoint()  // 只是标记
rdd.count()       // 这里才真正执行checkpoint

// 3. 本地模式checkpoint问题
// local模式下，stop SparkContext后数据丢失
// 生产环境务必使用HDFS

// 4. 与cache联合使用
// 先cache（加速第一次计算），再checkpoint（持久化）
val rdd = sc.textFile("hdfs://data")
  .map(process)
  .persist(StorageLevel.MEMORY_AND_DISK)  // 加速首次计算
rdd.checkpoint()  // 持久化到HDFS
rdd.unpersist()   // checkpoint后可以释放cache

// 5. 清理checkpoint
// Application结束后不会自动清理，需手动清理或设置TTL
// HDFS上手动删除：hdfs dfs -rm -r /spark-checkpoint
```

#### 完整容错方案

```scala
// 推荐：结合cache和checkpoint的容错策略
object ReliableCheckpoint {
  
  def processWithCheckpoint(sc: SparkContext, data: RDD[String]): RDD[(String, Int)] = {
    // 1. 设置checkpoint目录
    val checkpointDir = "hdfs://namenode:8020/spark-checkpoint"
    sc.setCheckpointDir(checkpointDir)
    
    // 2. 转换逻辑
    val transformed = data
      .flatMap(_.split(" "))
      .map((_, 1))
      .reduceByKey(_ + _)
    
    // 3. 如果RDD血统超过阈值（>50），启用checkpoint
    if (transformed.dependencies.length > 50) {
      transformed.checkpoint()
      transformed.count()  // 触发checkpoint
    }
    
    transformed
  }
  
  def recoverFromCheckpoint(sc: SparkContext, rdd: RDD[_]): Unit = {
    // 从checkpoint恢复：直接重新读取即可
    // Spark会自动检测是否有checkpoint
    val recoveredRDD = rdd
    println(s"Checkpoint exists: ${rdd.isCheckpointed}")
  }
}
```

#### 监控 Checkpoint

```scala
// 检查RDD是否已checkpoint
println(s"Is checkpointed: ${rdd.isCheckpointed}")
println(s"Checkpoint directory: ${rdd.getCheckpointFile.getOrElse("None")}")

// 查看checkpoint文件
// hdfs dfs -ls /spark-checkpoint/xxx/
// 会看到 _SUCCESS 和 part-00000 文件
```

**来源：** [CSDN-Spark Checkpoint机制详解](https://blog.csdn.net/qq_42500847/article/details/135678901)

---

> 📚 **面试题来源说明**：本套面试题基于Spark核心知识点整理，参考了CSDN、博客园、知乎等平台的优质文章，结合实际面试经验精选而出。
