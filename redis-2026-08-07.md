# Redis 面试题精选 — 2026年8月7日

> 本文档精选 5 道高质量 Redis 面试题，涵盖数据结构、持久化、缓存、集群等核心知识点。

---

## 题目一：Redis 和 Memcached 有什么区别？为什么 Redis 比 Memcached 更适合作为 MySQL 的缓存？

### 参考答案

**Redis 与 Memcached 的共同点：**
- 都是基于内存的数据库，一般都用来充当缓存使用
- 都有过期策略
- 两者性能都非常高

**Redis 与 Memcached 的区别：**

| 区别 | Redis | Memcached |
|------|-------|-----------|
| 数据类型 | 支持 String、Hash、List、Set、ZSet 等丰富数据类型 | 只支持最简单的 key-value 数据类型 |
| 持久化 | 支持 RDB 和 AOF 两种持久化方式，数据可保存到磁盘 | 无持久化功能，数据全部存在内存，重启后数据丢失 |
| 集群 | 原生支持主从复制、哨兵、Cluster 切片集群模式 | 无原生集群支持，需要依赖客户端实现分片 |
| 高级特性 | 支持发布/订阅、Lua 脚本、事务等 | 不支持 |

**为什么 Redis 更适合作为 MySQL 的缓存？**

主要因为 Redis 具备「高性能」和「高并发」两大特性：

1. **高性能**：用户第一次访问 MySQL 数据时速度较慢（从硬盘读取）。将数据缓存到 Redis 后，后续访问直接从内存读取，速度极快。当 MySQL 数据变更时，只需同步更新 Redis 缓存即可。

2. **高并发**：单台 Redis 的 QPS（每秒处理请求数）可达 10W+，而 MySQL 单机 QPS 通常难以超过 1W。直接访问 Redis 可以承受远大于 MySQL 的并发量，将大量请求拦截在数据库层之前。

📎 **来源**：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目二：Redis 是单线程的吗？为什么单线程的 Redis 还能保持这么高的性能？

### 参考答案

**Redis 确实是单线程的吗？**

Redis 的单线程指的是：**接收客户端请求 → 解析请求 → 执行数据读写等操作 → 发送响应** 这一核心过程由主线程完成。但 Redis 本身并非完全单线程，它在启动时会启动多个后台线程（BIO）：

- **Redis 2.6 版本**：启动 2 个后台线程，分别处理关闭文件、AOF 刷盘任务
- **Redis 4.0 版本之后**：新增 lazy free 线程，用于异步释放内存（如 `unlink key` / `flushdb async`）

三个任务队列：`BIO_CLOSE_FILE`（关闭文件）、`BIO_AOF_FSYNC`（AOF 刷盘）、`BIO_LAZY_FREE`（释放内存）。

**Redis 6.0 之后引入了 I/O 多线程**：针对网络 I/O 采用多线程处理（默认 3 个 I/O 线程），以分担网络压力，但命令执行仍然是单线程的。

**Redis 单线程为什么这么快？**

1. **内存为瓶颈**：Redis 大部分操作在内存中完成，采用高效数据结构，瓶颈在于内存或网络带宽，而非 CPU，所以单线程反而避免了切换开销

2. **无竞争开销**：单线程模型避免了多线程竞争和锁操作，没有线程切换的性能损耗，也不存在死锁问题

3. **I/O 多路复用**：采用 `epoll` 机制，一个线程处理多个 Socket 请求，内核持续监听所有连接请求和数据请求，有请求到达时才交给 Redis 线程处理

📎 **来源**：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目三：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？混合持久化是怎么工作的？

### 参考答案

Redis 共有三种持久化方式：**RDB（快照）**、**AOF（日志）**、**混合持久化**。

### RDB 快照
将某一时刻的内存数据以二进制格式写入磁盘。Redis 提供两个命令：
- `save`：在主线程执行，会阻塞主线程
- `bgsave`：fork 子进程生成 RDB 文件，不阻塞主线程

bgsave 通过**写时复制（COW）**技术，在快照期间主线程仍可处理写操作，被修改的数据会复制副本给子进程写入 RDB。

### AOF 日志
每执行一条写操作命令，就将该命令以追加方式写入文件。Redis 重启时逐一执行命令来恢复数据。

AOF 有三种写回策略（`appendfsync`）：

| 策略 | 说明 | 安全性 | 性能 |
|------|------|--------|------|
| Always | 每次写命令执行完后同步写回硬盘 | 最安全，数据几乎不丢失 | 低 |
| Everysec | 每秒将缓冲区写回硬盘 | 折中，最多丢失 1 秒数据 | 较高 |
| No | 由操作系统决定何时写回 | 性能最好，但可能丢失大量数据 | 最高 |

AOF 文件过大会触发**重写机制**（bgrewriteaof），由子进程读取当前所有键值对，用一条命令记录到新 AOF 文件，实现压缩。

### 混合持久化（Redis 4.0+）
结合 RDB 和 AOF 的优点：
- AOF 重写时，先将以 RDB 格式写入 AOF 文件前半部分
- 之后主线程的增量操作以 AOF 格式写入后半部分
- 恢复时先加载 RDB 部分（快速），再加载 AOF 增量部分（数据更完整）

**优点**：兼顾重启速度和低数据丢失风险
**缺点**：AOF 可读性差，不兼容 Redis 4.0 之前版本

📎 **来源**：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目四：如何解决 Redis 集群脑裂导致的数据丢失问题？

### 参考答案

### 什么是脑裂？

脑裂（Split-Brain）是指 Redis 主从架构中，主节点与从节点之间网络突然断开，但主节点与客户端网络仍然正常。此时：
1. 客户端继续向主节点写入数据（数据暂存在缓冲区）
2. 哨兵检测到主节点失联，选举从节点为新主节点
3. 网络恢复后，旧主节点降级为从节点
4. 从节点执行全量同步，清空本地数据后复制新主节点数据

结果：客户端写入旧主节点缓冲区的数据全部丢失。

### 解决方案

在 Redis 配置文件中设置两个参数：

```
min-slaves-to-write 1
min-slaves-max-lag 10
```

- `min-slaves-to-write x`：主节点必须至少有 x 个从节点连接，否则拒绝写入
- `min-slaves-max-lag x`：主从复制延迟不能超过 x 秒，否则拒绝写入

**工作原理**：即使原主节点是假故障（网络问题），它无法在规定时间内收到从节点的 ACK 确认，就会被限制接收客户端写请求，从而避免在脑裂期间积累大量写数据。

即使发生网络分区，脑裂期间旧主节点因无法满足从节点数量/延迟要求而停止接收写请求，新主节点可以正常服务，网络恢复后旧主节点被降为从节点时也不会丢失大量数据。

📎 **来源**：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目五：Redis 的过期删除策略和内存淘汰策略有什么区别？请分别说明。

### 参考答案

### 一、过期删除策略

Redis 对 key 设置过期时间时，会将该 key 和过期时间存入**过期字典**。Redis 采用**惰性删除 + 定期删除**的组合策略：

**惰性删除**：每次访问 key 时才检查是否过期，过期则删除
- ✅ 优点：对 CPU 友好，只占用很少资源
- ❌ 缺点：如果 key 过期但一直未被访问，内存无法释放

**定期删除**：每隔一段时间随机从过期字典中抽取 20 个 key 检查，删除已过期的 key
- 如果过期 key 超过 25%（5/20），继续抽取删除
- 为避免阻塞，每次定期删除循环不超过 25ms
- ✅ 优点：定期释放一定过期内存，对 CPU 和内存都友好
- ❌ 缺点：删除频率难以精确控制

### 二、内存淘汰策略

当 Redis 运行内存达到 `maxmemory` 阈值时，触发内存淘汰策略，共 8 种：

**不淘汰（1种）：**
- `noeviction`：内存满时拒绝写入，直接返回错误（Redis 3.0+ 默认策略）

**在设置了过期时间的数据中淘汰（4种）：**
- `volatile-random`：随机淘汰任意过期 key
- `volatile-ttl`：优先淘汰最早过期的 key
- `volatile-lru`：淘汰最久未使用的过期 key（Redis 3.0 前默认）
- `volatile-lfu`：淘汰访问频率最低的过期 key（Redis 4.0+ 新增）

**在所有数据中淘汰（3种）：**
- `allkeys-random`：随机淘汰任意 key
- `allkeys-lru`：淘汰最久未使用的任意 key
- `allkeys-lfu`：淘汰访问频率最低的任意 key（Redis 4.0+ 新增）

### LRU vs LFU 算法

- **LRU（Least Recently Used）**：淘汰最近最少使用的数据。Redis 通过在对象头中记录最后一次访问时间戳，随机采样 5 个 key 淘汰最久未使用的。无法解决一次性大量数据的缓存污染问题。
- **LFU（Least Frequently Used）**：淘汰访问频率最低的数据。对象头的 24bit lru 字段分为高 16bit（访问时间戳）和低 8bit（访问计数器），解决了 LRU 无法处理缓存污染的问题。

📎 **来源**：[图解 Redis — 小林coding](https://xiaolincoding.com/redis/base/redis_interview.html)

---

*本面试题整理自网络公开资料，仅供学习参考。如有侵权请联系删除。*
