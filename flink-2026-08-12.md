# Flink 面试题 — 2026-08-12

> 整理自网络精选，包含 Flink 核心知识点：架构、Watermark、Checkpoint、窗口函数、状态管理。

---

## 1. Flink 的架构是怎样的？JobManager 和 TaskManager 的职责是什么？

### 参考答案

Flink 采用**主从架构**，由 JobManager（主节点）和 TaskManager（工作节点）组成。

**JobManager 职责：**
- 负责接收 Flink 作业（Job），将 Job 转换为 **ExecutionGraph**（执行图）
- 协调 Checkpoint 和故障恢复
- 调度 Task 到 TaskManager 上执行
- 收集 Task 的执行状态和结果

**TaskManager 职责：**
- 启动后向 ResourceManager 注册自己的资源（内存、CPU slot）
- 实际执行 Operator 的算子逻辑（subtask）
- 管理本地状态（State Backend）
- 与 JobManager 通信汇报任务状态

**作业提交流程：**
1. 客户端（Client）将用户编写的 StreamGraph 提交给 JobManager
2. JobManager 将 StreamGraph 转换为 ExecutionGraph
3. JobManager 向 ResourceManager 申请执行 slots
4. ResourceManager 向 TaskManager 发起 Slot 提供请求
5. TaskManager 提供 slot，JobManager 将 Task 调度到对应 TaskManager 执行

> **来源：** [Flink 官方文档 - Architecture](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/flink-architecture/)

---

## 2. Flink 的 Watermark（水位线）机制是什么？如何处理乱序数据？

### 参考答案

**Watermark 本质：**
Watermark 是 Flink 用于处理**事件时间（Event Time）**乱序问题的一种机制，本质上是一个时间戳，代表着"所有时间戳小于该 Watermark 的数据都已经到达"。

**核心思想：**
- 如果 Watermark 的值为 `T`，则意味着不再接受时间戳 `< T` 的数据（虽然理论上可能还有迟到数据）
- 当某个窗口的**关闭时间**（window max timestamp）小于当前 Watermark 时，该窗口触发计算

**乱序处理策略：**

| 策略 | 说明 |
|------|------|
| **Watermark 机制** | 使用 Watermark 等待一定时间的乱序数据，设置 `allowedLateness` 延迟关闭窗口 |
| **窗口延迟关闭** | 通过 `allowedLateness(millis)` 设置窗口关闭后的额外等待时间，迟到数据仍可触发窗口计算 |
| **侧输出流（Side Output）** | 使用 `OutputTag` 将迟到严重的数据单独输出到侧流，不丢失重要数据 |

**生成方式：**
```java
// 在 Source 层面自定义 Watermark 策略
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofSeconds(10))
    .withTimestampAssigner((event, timestamp) -> event.getTimestamp());
```

> **来源：** [Flink 官方文档 - Event Time & Watermarks](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/time-management/)

---

## 3. Flink 的 Checkpoint（检查点）和 Savepoint 的区别是什么？如何保证 exactly-once 语义？

### 参考答案

**Checkpoint vs Savepoint：**

| 特性 | Checkpoint | Savepoint |
|------|-----------|-----------|
| **触发方式** | Flink 自动周期性地触发 | 手动触发（用户命令） |
| **目的** | 故障恢复，保证容错 | 计划性停机维护、版本迁移 |
| **存储格式** | 状态轻量，与检查点数据合并 | 独立存储，包含 JobGraph 等完整元数据 |
| **清理策略** | 自动清理旧检查点 | 需手动清理或设置 TTL |
| **位置** | 由 `state.checkpoints.dir` 指定 | 由 `savepoint_directory` 指定 |

**Exactly-Once 语义保证原理：**

Flink 通过 **Chandy-Lamport 分布式快照算法**的变体实现 exactly-once：

1. **Barrier 对齐：** JobManager 向所有 Source 注入 Barrier（屏障），Barrier 随数据流向下游传递
2. **状态快照：** 每个算子在收到所有输入的 Barrier 后，对自身状态做快照（CheckpointBarrier 对齐点）
3. **异步写入：** 快照数据异步写入远程存储（如 HDFS/S3）
4. **两阶段提交：** Sink 算子使用两阶段提交协议，确保事务性输出

**关键配置：**
```java
env.enableCheckpointing(10000); // 每10秒做一次检查点
env.getCheckpointConfig().setCheckpointingMode(CheckpointingMode.EXACTLY_ONCE);
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(500);
env.getCheckpointConfig().setCheckpointTimeout(60000);
```

> **来源：** [Flink 官方文档 - Checkpointing](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/ops/state/checkpoints/)

---

## 4. Flink 的状态管理：State Backend 有哪些类型？如何选择？

### 参考答案

**Flink 支持三种 State Backend：**

| 类型 | 状态存储位置 | 适用场景 | 特点 |
|------|------------|---------|------|
| **HashMapStateBackend** | JVM Heap（TaskManager 内存） | 小状态、低延迟场景 | 性能最高，但受 JVM 堆内存限制 |
| **EmbeddedRocksDBStateBackend** | RocksDB（本地磁盘 + 内存） | 大状态、增量 Checkpoint | 支持增量快照，状态可超过内存大小 |
| **AdaptiveRocksDBStateBackend** | RocksDB | Flink 1.18+ 推荐 | 自动调优，根据状态大小自适应 |

**选择建议：**
- **小状态、低延迟**（如实时 ETL）：选 `HashMapStateBackend`
- **大状态、超大窗口**（如会话窗口、状态后置）：选 `RocksDBStateBackend`
- Flink 1.18+ 推荐使用 `AdaptiveRocksDBStateBackend`，自动选择最优配置

**配置方式：**
```java
// HashMapStateBackend（默认）
env.setStateBackend(new HashMapStateBackend());

// RocksDBStateBackend（需引入依赖）
EmbeddedRocksDBStateBackend rocksDBStateBackend = new EmbeddedRocksDBStateBackend();
env.setStateBackend(rocksDBStateBackend);
env.getCheckpointConfig().setCheckpointStorage("hdfs:///checkpoints/");
```

**Keyed State vs Operator State：**

- **Keyed State：** 作用于 KeyedStream，按 Key 隔离，每个 Key 独立一份（如 `ValueState`, `ListState`, `MapState`）
- **Operator State：** 作用于算子实例，所有 Key 共享（如 Kafka Connector 的分区 offset）

> **来源：** [Flink 官方文档 - State Backend](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/ops/state/state_backends/)

---

## 5. Flink 窗口函数有哪些类型？Session Window 是如何工作的？

### 参考答案

**窗口函数分类：**

**按时间划分：**
- **Tumbling Window（滚动窗口）**：固定大小、不重叠。如 `window(TumblingEventTimeWindows.of(Time.seconds(10)))`
- **Sliding Window（滑动窗口）**：固定大小、可重叠。如 `window(SlidingEventTimeWindows.of(Time.seconds(10), Time.seconds(5)))`
- **Session Window（会话窗口）**：按活动间隙划分，无固定大小
- **Global Window（全局窗口）**：所有数据划入同一窗口，需配合 Trigger 使用

**按类型划分：**
- **Count Window（计数窗口）**：按元素数量触发，如每 100 条数据计算一次
- **Time Window（时间窗口）**：按时间间隔触发

**Session Window 工作原理：**

Session Window 的核心是**会话间隙（Gap）**：当两条数据的时间间隔超过设定的 Gap 时，视为一个新会话开始。

```
数据:  [1]----[2]----[3]        [4]----[5]
               ↑ Gap = 3s        ↑ Gap = 3s
窗口1:  [1, 2, 3]              窗口2: [4, 5]
```

**特点：**
- 窗口大小**不固定**，取决于数据到达间隔
- 支持设置**动态 Gap**（通过 `SessionWindowTimeGapExtractor`）
- 支持 `allowedLateness`，在窗口关闭后额外等待迟到数据

**代码示例：**
```java
// 固定 Gap
DataStream<Word> result = stream
    .keyBy(v -> v.f0)
    .window(SessionWindows.withGap(Time.seconds(10)))
    .sum(1);

// 动态 Gap
DataStream<Word> result = stream
    .keyBy(v -> v.f0)
    .window(SessionWindows.withDynamicGap(new SessionWindowTimeGapExtractor<Word>() {
        @Override
        public long extract(Word element) {
            return element.getGapMillis(); // 根据每条数据动态计算 Gap
        }
    }))
    .sum(1);
```

> **来源：** [Flink 官方文档 - Windows](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/dev/datastream_api/overview/#windows)

---

*本文由 Flink 面试题助手整理 | 每日自动更新*
