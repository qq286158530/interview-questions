# 程序员面试题 - 2026-08-09

## 🔥 Spark 大数据

---

### 题目1：Spark 的架构是怎样的？为什么比 Hadoop MapReduce 快？

【答案】

**Spark 架构核心组件**：

1. **Driver（驱动器节点）**
   - 运行 `main()` 方法，创建 `SparkContext`
   - 负责任务调度、DAG 划分、Task 分发
   - 与 Cluster Manager 通信申请资源

2. **Executor（执行器进程）**
   - 驻留在 Worker 节点上
   - 负责执行 Task、返回结果
   - 每个 Application 拥有独立的 Executor 进程

3. **Cluster Manager（集群管理器）**
   - 负责集群资源管理（Standalone / YARN / Mesos）
   - 为 Application 分配容器资源

4. **Worker Node**
   - 集群中的计算节点
   - 启动 Executor 或 Driver

**为什么比 Hadoop MapReduce 快？**

| 特性 | Hadoop MapReduce | Spark |
|------|------------------|-------|
| 数据存储 | 磁盘 I/O，每次迭代读写磁盘 | 基于内存（RDD）迭代计算在内存完成 |
| 任务启动 | 每次 Task 启动新进程 | Executor 进程常驻，Task 线程复用 |
| 中间结果 | 写磁盘（Sort-Based Shuffle） | 内存存储，Shuffle 也可落盘 |
| 计算模型 | 线性 DAG（Map → Shuffle → Reduce） | DAG 更丰富，支持窄依赖/宽依赖优化 |
| 网络传输 | 低效序列化 | Kryo 高效序列化 |

**核心优势**：
- **内存计算**：避免频繁磁盘 I/O
- **DAG 优化**：Stage 划分后流水线执行
- ** Tungsten 项目**：手写内存管理、Cache-aware 计算
- **Executor 复用**：避免进程启动开销

📖 来源：https://github.com/CyC2018/CS-Notes

---分割线---

### 题目2：RDD 是什么？有哪些特性？Transformation 和 Action 的区别？

【答案】

**RDD（Resilient Distributed Dataset）弹性分布式数据集**：

是 Spark 最核心的抽象，表示不可变、可分区、能在集群并行计算的数据集合。

**RDD 五大特性**：

1. **`分区列表`**：数据被切分成多个分区，每个分区是数据集的子集
   ```scala
   rdd.partitions  // 返回 Array[Partition]
   ```

2. **`计算函数`**：每个分区都有计算函数（_split(index) => Iterator[T]）
   ```scala
   // 示例：map 算子的 compute 函数
   (split, context) => split.iterator
   ```

3. **`依赖关系`**：RDD 与父 RDD 之间的依赖关系（窄依赖 / 宽依赖）
   ```scala
   rdd.dependencies  // 返回 Seq[Dependency]
   ```

4. **`分区器`**（可选）：对于 KV 型 RDD，指定分区策略（HashPartitioner / RangePartitioner）
   ```scala
   rdd.partitionner  // Some(Partitioner)
   ```

5. **`最佳位置`**：计算任务优先分配到数据所在节点（数据本地性）
   ```scala
   rdd.preferredLocations(rdd.partitions(0))
   ```

**Transformation vs Action**：

| 特性 | Transformation（转换） | Action（动作） |
|------|------------------------|----------------|
| 执行方式 | 懒执行（Lazy），记录 DAG 不立即执行 | 触发执行（Eager），真正计算 |
| 返回值 | 新的 RDD | 其他类型（Unit / 数值 / 文件） |
| 链式调用 | 可以链式调用 | 终止链式调用 |
| 示例 | `map` `filter` `flatMap` `groupByKey` `join` | `collect` `count` `saveAsTextFile` `reduce` |

**经典面试点**：
- Transformation 是惰性的，只有遇到 Action 时才会真正触发计算
- 这允许 Spark 进行 Pipeline 优化（如 filter 后再 map 合并）
- `collect` 会将所有分区数据拉取到 Driver 端，注意数据量

📖 来源：https://github.com/CyC2018/CS-Notes

---分割线---

### 题目3：Spark Shuffle 是什么？Hash Shuffle 和 Sort Shuffle 的区别？

【答案】

**Shuffle（洗牌）**：

是 Spark 中将数据按 Key 重新分区的过程，俗称"洗牌"。发生在 `groupByKey`、`join`、`reduceByKey` 等需要跨分区聚合的操作时。

**经典 Shuffle 流程**：
```
Map Stage（分区内聚合）
    ↓ 写本地磁盘，按 reduceId 分文件
Shuffle Write（每个 Map Task 写多个文件）
    ↓
Reduce Stage（跨分区读取）
Shuffle Read（每个 Reduce Task 拉取需要的文件）
    ↓ 聚合
结果
```

**Hash Shuffle vs Sort Shuffle**：

| 特性 | Hash Shuffle | Sort Shuffle |
|------|-------------|--------------|
| 文件数量 | `M * R`（M=Map Task数，R=Reduce Task数） | `2 * M`（每个 Map Task 写一个数据文件 + 索引文件） |
| 文件数问题 | 文件数过多，小文件磁盘 I/O 压力大 | 优化为固定 2 个文件 |
| 是否排序 | 否 | 是（按 Key 排序） |
| 内存压力 | 低 | 需排序，内存压力大（可溢写磁盘） |
| 引入版本 | 早期版本 | Spark 1.1+ 引入，1.2 成为默认 |
| 优化版 | —— | Tungsten Sort Shuffle（钨丝计划，堆外内存） |

**Sort Shuffle 执行流程**：
1. Map 端将数据写入内存缓冲区
2. 缓冲区满则溢写磁盘（ spill）
3. 全部数据写完后，对所有小文件归并排序
4. 生成 `data.out`（数据文件）和 `index`（索引文件）
5. Reduce 端根据 index 拉取对应数据

**为什么 Sort Shuffle 取代了 Hash Shuffle？**
- Hash Shuffle 每个 Map Task 为每个 Reduce Task 创建一个文件，`M * R` 个文件会压垮文件系统
- Sort Shuffle 每个 Map Task 只产生 2 个文件，大幅减少文件数

**优化建议**：
- 减少 Shuffle 次数：`coalesce`/`repartition` 合并分区
- 避免 Shuffle：能用 map-side 聚合就不用 groupByKey
- `spark.shuffle.service.enabled = true` 开启 External Shuffle Service

📖 来源：https://github.com/CyC2018/CS-Notes

---分割线---

### 题目4：Spark 内存管理是怎样的？堆内内存和堆外内存有什么区别？

【答案】

**Spark 内存管理模型（统一内存管理，Spark 1.6+）**：

Spark 采用统一内存管理，将内存划分为 **存储内存** 和 **执行内存** 两部分，二者可以动态借用。

**内存区域划分**：

```
┌─────────────────────────────────────┐
│           JVM 堆内存 (Heap)           │
├────────────────┬────────────────────┤
│   存储内存       │     执行内存         │
│ (Storage)      │   (Execution)      │
│  存放：         │  存放：             │
│  - RDD Cache   │  - Shuffle 缓冲    │
│  - 广播变量     │  - 序列化数据       │
│  - 内部元数据   │  - 随机访问数据      │
├────────────────┼────────────────────┤
│              用户内存 (User)          │
│              存储：用户数据结构         │
├────────────────────────────────────┤
│             预留内存 (Reserved)       │
│              300MB 固定              │
└─────────────────────────────────────┘
```

**堆内内存（On-Heap）**：
- JVM 堆上的内存，由 JVM 管理
- 默认 `spark.memory.fraction = 0.6`（60% 用于存储+执行）
- `spark.memory.storageFraction = 0.5`（存储/执行各占一半）
- 缺点：受 JVM GC 影响，大内存下 GC 频繁

**堆外内存（Off-Heap，Tungsten 计划引入）**：
- 直接向操作系统申请内存，不经过 JVM
- 开启：`spark.memory.offHeap.enabled = true`
- `spark.memory.offHeap.size` 设置大小
- 优点：
  - 避免 GC 停顿
  - 序列化数据直接操作堆外内存，更高效
  - 可以使用 `sun.misc.unsafe` 直接操作内存地址

**存储内存 vs 执行内存动态占用规则**：
- 当一方不足时，可借用另一方空闲空间
- 借出方内存不足时，需将数据驱逐（Eviction）到磁盘
- 执行内存被存储内存借用时，无法强制收回（可能导致 OOM）

**内存分配参数**：
```scala
// 统一内存比例（存储+执行占 heap 的比例）
spark.memory.fraction = 0.6

// 存储内存初始比例
spark.memory.storageFraction = 0.5

// 堆外内存
spark.memory.offHeap.enabled = true
spark.memory.offHeap.size = 4g
```

**常见 OOM 原因**：
- `Storage` 空间不足且无法驱逐 → 任务失败
- `Execution` 借用 `Storage` 后无法归还
- 广播变量过大占用 `Storage`

📖 来源：https://github.com/CyC2018/CS-Notes

---分割线---

### 题目5：广播变量（Broadcast）是什么？它解决了什么问题？如何使用？

【答案】

**广播变量（Broadcast Variable）**：

是 Spark 提供的只读变量，只在每个 Executor 中保存一份副本，所有 Task 共享，而非每个 Task 一份。从而避免变量随 Task 重复传输，节省网络 I/O 和内存。

**解决的问题**：

| 场景 | 不使用广播 | 使用广播 |
|------|-----------|----------|
| 大表 Join 小表 | 小表数据每个 Task 传输一次（N 个 Task = N 次传输） | 广播到每个 Executor 只传输一次 |
| 公共配置 | 每个 Task 包含配置副本 | Executor 共享一份 |
| 字典映射 | 字典随数据反复传输 | 广播后 Executor 内共享 |

**经典场景：大小表 Join**
```scala
// 不使用广播（数据量小但每次传输）
val result = bigTable.join(smallTable, "key")  // smallTable 每次 shuffle 都传输

// 使用广播（高效）
val smallBroadcast = spark.sparkContext.broadcast(smallTable.collectAsMap())
val result = bigTable.mapPartitions { iter =>
    val map = smallBroadcast.value
    iter.filter { r => map.contains(r.key) }
}
```

**使用方式**：

```scala
// 1. 创建广播变量
val broadcastVar = spark.sparkContext.broadcast(Array(1, 2, 3, 4, 5))

// 2. 在 Task 中访问（使用 .value）
rdd.map { x => broadcastVar.value.sum * x }

// 3. 注销广播变量（释放资源）
broadcastVar.unpersist()
```

**广播变量的特性**：
- **只读**：一旦创建不可修改，修改需重新广播
- **Executor 级共享**：同一 Executor 内的 Task 共享同一副本
- **自动优化**：Spark SQL 中小表自动广播（`spark.sql.autoBroadcastJoinThreshold`，默认 10MB）
- **序列化**：广播变量会被序列化发送到 Executor，使用 Kryo 序列化

**手动广播 vs 自动广播**：
```scala
// Spark SQL 自动广播（阈值 10MB）
spark.conf.set("spark.sql.autoBroadcastJoinThreshold", "100MB")

// 强制广播（broadcast hint）
import org.apache.spark.sql.functions.broadcast
val result = bigTable.join(broadcast(smallTable), "key")
```

**注意事项**：
- 广播变量不宜过大（超过 `spark.sql.autoBroadcastJoinThreshold` 可能导致 OOM）
- 不需要广播时及时 `unpersist()` 释放
- 广播变量更新需要重新创建

📖 来源：https://github.com/CyC2018/CS-Notes

---分割线---
