# Spark 面试题 — 2026-08-23

> 本次收录 5 道高质量 Spark 面试题，涵盖 Spark 架构、RDD、DataFrame、Shuffle、内存管理等核心知识点。  
> 题目来源综合整理自网络公开技术文章与面试经验。

---

## 题目一：Spark 的架构是怎样的？Driver 和 Executor 分别承担什么角色？

### 参考答案

Spark 采用 **主从架构（Master-Slave）**，核心组件如下：

| 组件 | 角色说明 |
|------|---------|
| **Driver（驱动器）** | 负责执行用户代码，构建 `SparkContext`，将作业拆分为多个 Task，分发到 Executor 执行；管理整个作业的生命周期 |
| **Executor（执行器）** | 运行在 Worker 节点上的进程，负责执行分配到的 Task，计算并存储结果；每个应用拥有独立的 Executor 进程 |
| **Cluster Manager** | 集群资源管理器（Standalone / YARN / Mesos），负责分配 Worker 节点上的资源 |
| **Worker Node** | 集群中运行 Executor 的节点，负责提供计算资源 |

**作业执行流程简述：**

1. 用户提交代码 → Driver 创建 `SparkContext`
2. `SparkContext` 向 Cluster Manager 申请资源，启动 Executor
3. DAGScheduler 将 Job 划分为 Stage，Stage 划分为 Task
4. TaskScheduler 将 Task 分发到各 Executor 的线程池中并行执行
5. 结果汇总回 Driver，Driver 发出终止指令

> 📎 参考来源：  
> https://spark.apache.org/docs/latest/cluster-overview.html  
> https://www.cnblogs.com/jingwhale/p/12839803.html

---

## 题目二：RDD 是什么？为什么 Spark 选择 RDD 作为核心抽象？

### 参考答案

**RDD（Resilient Distributed Dataset）** 是 Spark 最底层的不可变、分区式、可并行计算的数据集合。

### RDD 的五大属性

| 属性 | 说明 |
|------|------|
| ` partitions` | 数据分区的列表，每个分区对应一个计算任务 |
| ` computeFn` | 计算函数，用于处理每个分区 |
| ` dependencies` | 依赖关系（窄依赖 / 宽依赖），决定 Stage 划分 |
| ` partitioner` | 可选的分区器（HashPartitioner / RangePartitioner） |
| ` preferredLocations` | 每个分区的优先计算位置（数据本地性） |

### 为什么选择 RDD？

1. **自动容错**：通过血缘（Lineage）机制，某个分区丢失时可根据依赖链重新计算，无需数据副本
2. **数据本地性**：调度器了解数据位置，优先将任务派发到数据所在节点，减少网络传输
3. **延迟计算（Lazy Evaluation）**：所有 transformation 不会立即执行，直到遇到 Action 才会触发计算
4. **高效的并行计算**：天然支持分布式并行，且相比 HDFS 更高效（内存计算、中间结果不落盘）

### 窄依赖 vs 宽依赖

| 类型 | 含义 | 代表操作 | 影响 |
|------|------|---------|------|
| **窄依赖（Narrow）** | 每个父分区至多被一个子分区使用 | `map`、`filter`、`union` | 可以流水线执行，一个节点内完成 |
| **宽依赖（Wide/Shuffle）** | 一个父分区可能被多个子分区使用 | `groupByKey`、`join`、`sortBy` | 需要 Shuffle，形成 Stage 边界 |

> 📎 参考来源：  
> https://spark.apache.org/docs/latest/rdd-programming-guide.html  
> https://time.geekbang.org/column/article/138556

---

## 题目三：DataFrame 和 DataSet 有什么区别？与 RDD 相比有什么优势？

### 参考答案

### 三者的关系

```
RDD[Person]          // 底层：无 schema，纯 JVM 对象
DataFrame = Dataset[Row]  // 中文行：自带 schema（列名+类型），以 Row 对象存储
DataSet[T]           // 泛型：带类型 + schema，是 DataFrame 的类型安全版本
```

### DataFrame vs DataSet 对比

| 维度 | DataFrame | DataSet |
|------|-----------|---------|
| **数据结构** | 无类型，`Dataset[Row]` | 有类型，`Dataset[T]` |
| **编译时检查** | 无（运行才发现类型错误） | 有（编译时即可发现） |
| **序列化方式** | 内部使用 Tungsten 二进制编码 | Encoder 序列化，效率高 |
| **操作方式** | DSL（SQL-like）| Lambda 表达式 + DSL |
| **易用性** | 高，可用 SQL 操作 | 中，兼具类型安全和 DSL |

### 相比 RDD 的优势

1. **性能优化（Catalyst 优化器）**  
   - DataFrame 经过 Catalyst 优化器分析后生成最优执行计划（包括谓词下推、列剪裁、常量折叠等）
   - Tungsten 序列化：使用堆外内存和二进制编码，比 Java 序列化高效 2-10 倍

2. **统一的数据抽象**  
   - DataFrame = Dataset[Row]，DataSet = Dataset[T]，三者可以相互转换
   - 方便融合 SQL 查询和函数式编程

3. **外部数据源集成**  
   - 支持直接读取 JSON、Parquet、ORC、JDBC、Hive 等数据源，无需手动定义解析逻辑

4. **Spark SQL 统一**  
   - DataFrame 可以直接注册为临时视图执行 SQL，RDD 则不行

> 📎 参考来源：  
> https://spark.apache.org/docs/latest/sql-programming-guide.html  
> https://www.cnblogs.com/swordfall/p/12240728.html

---

## 题目四：Spark Shuffle 的原理是什么？为什么 Shuffle 是性能瓶颈？

### 参考答案

### Shuffle 的触发条件

当一个 RDD/DataFrame 操作需要**将数据按某种规则重新分区**时触发，典型操作包括：
`groupByKey`、`reduceByKey`、`join`、`distinct`、`repartition`、`sortBy` 等。

### Shuffle 执行流程（以 Shuffle Write → Shuffle Read 为例）

**Shuffle Write（Map 阶段）：**
1. 每个 Map Task 将结果先写入**本地磁盘**（Executor 所在节点）
2. 根据分区器（如 HashPartitioner）计算出每个 Key 对应的目标分区
3. 将数据写入 `spark.shuffle.dir` 指定的本地目录，生成多个小文件（每个分区对应一个文件）

**Shuffle Read（Reduce 阶段）：**
1. Reduce Task 根据分区信息，去**多个不同的 Executor 节点**拉取属于自己的数据
2. 拉取回来的数据先放在 Executor 内存（内存不足时溢写到磁盘）
3. 按 Key 进行聚合（sort-merge-base Shuffle，Spark 2.x+）

### 为什么是性能瓶颈？

| 问题 | 原因 |
|------|------|
| **磁盘 I/O** | Shuffle 数据必须落盘（不能纯内存），涉及大量顺序/随机读写 |
| **网络 I/O** | Reduce 阶段跨节点拉取数据，网络传输量大 |
| **内存压力** | 聚合时数据全部加载到内存，容易 OOM |
| **磁盘小文件爆炸** | 每个 Map Task 产生 N 个小文件（分区数），文件数 = M × N，文件系统压力大 |
| **Checksum 校验** | 写入时计算校验值，读出时校验，增加 CPU 消耗 |

### Spark 3.x 的优化

- **Sort-based Shuffle 默认化**（Spark 2.x 已引入，替代 Hash-based Shuffle）
- **Shuffle File Streaming**（避免大量小文件）
- **External Shuffle Service**（Executor 失败时不丢失 Shuffle 数据）

> 📎 参考来源：  
> https://spark.apache.org/docs/latest/rdd-programming-guide.html#shuffle-operations  
> https://zhuanlan.zhihu.com/p/84332434

---

## 题目五：Spark 的内存管理模型是怎样的？堆内内存和堆外内存有什么区别？

### 参考答案

### Unified Memory（统一内存管理）— Spark 1.6+

Spark 将内存分为**存储内存**和**执行内存**两部分，二者可以相互借用（动态共享）。

```
┌──────────────────────────────────────────┐
│              统一内存（Unified Memory）      │
│  ┌──────────────┬──────────────────────┐  │
│  │  存储内存      │     执行内存          │  │
│  │ (Storage)    │    (Execution)       │  │
│  │ 缓存RDD/广播  │ Shuffle/排序/哈希/Join │  │
│  └──────────────┴──────────────────────┘  │
└──────────────────────────────────────────┘
```

### 堆内内存（On-Heap Memory）

- JVM 堆内的内存，由 JVM 管理
- `spark.memory.fraction`（默认 0.6）控制统一内存占总堆内存的比例
- `spark.memory.storageFraction`（默认 0.5）控制 Storage 预留下限
- **缺点**：受 JVM GC 影响，内存碎片化；JVM 对象头开销大

### 堆外内存（Off-Heap Memory）

- 通过 `spark.memory.offHeap.enabled=true` 开启
- 使用 `sun.misc.Unsafe` 直接操作操作系统内存，不经过 JVM
- `spark.memory.offHeap.size` 设置大小（需手动指定）
- **优点**：避免 GC 停顿；对象序列化后紧凑存储，节省空间；适合大内存场景
- **缺点**：不透明，调试困难；需要序列化（Tungsten Binary Format）

### 内存区域详解

| 区域 | 说明 | 配置参数 |
|------|------|---------|
| **Reserved Memory** | 系统预留（默认 300MB） | 固定 |
| **User Memory** | 用户代码使用（UDF 临时对象等） | `1 - spark.memory.fraction` |
| **Unified Memory** | Storage + Execution 共享 | `spark.memory.fraction` |
| **Execution Memory** | Shuffle 排序、Hash Join、UDAF 等 | 动态借用 / 被借出 |
| **Storage Memory** | RDD 缓存、广播变量、临时数据 | 动态借用 / 被借出 |

### 常见 OOM 原因

1. `Execution` 内存不足时无法向 `Storage` 借用（`Storage` 内存有下限）
2. RDD 缓存数据量超过 `Storage Memory` 上限，部分分区会被淘汰（LRU）
3. 广播变量过大，占用大量 `Storage` 内存

> 📎 参考来源：  
> https://spark.apache.org/docs/latest/tuning.html#memory-management-overview  
> https://www.cnblogs.com/importbigdata/p/12094200.html

---

*📅 本文件由自动化脚本生成，每日定时推送至 GitHub 仓库*  
*仓库：https://github.com/qq286158530/interview-questions*
