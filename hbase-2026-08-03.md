# HBase 面试题精选（2026-08-03）

> 本文档整理自网络公开技术文章，涵盖 HBase 架构、存储结构、RowKey 设计、读写流程、协处理器等核心知识点。

---

## 题目一：HBase 的整体架构是怎样的？ZooKeeper在其中扮演什么角色？

### 参考答案

HBase 采用典型的 Master-Slave 架构，主要包含以下组件：

**1. ZooKeeper（协调服务）**
- 选举 Master 主节点，保证集群中只有一个 Active Master
- 存储元数据路径（如 `/hbase/meta-region-server`）和 `-ROOT-` 表位置
- 监控 RegionServer 上下线，通过 Watch 机制通知 Master
- 保存集群入口地址（hbase-site.xml 中的 `hbase.zookeeper.quorum`）

**2. Master（管理节点）**
- 分配 Region 给 RegionServer，实现负载均衡
- 负责建表、修改列族、删除表等 DDL 操作
- 监控 RegionServer 状态，处理故障转移
- 管理 Namespace、Region 合并与分裂

**3. RegionServer（数据节点）**
- 负责数据读写，直接与 Client 交互
- 管理 Region：处理分裂、合并、 compaction
- 每个 RegionServer 包含多个 Region 和一个 WAL（预写日志）
- 包含 BlockCache（读缓存）、MemStore（写缓存）

**4. Client（客户端）**
- 先查询 ZooKeeper 获取元数据位置，再直连 RegionServer
- 持有 `hbase:meta` 表的缓存，减少元数据查询次数

**架构特点**：HBase 依赖 ZooKeeper 实现分布式协调，但不存储实际数据；数据分布在 RegionServer 上，Master 仅负责调度，真正的数据读写不经过 Master。

---

## 题目二：HBase 的 RowKey 设计有哪些原则？如何避免热点问题？

### 参考答案

### RowKey 设计核心原则

**1. 唯一性原则**
RowKey 必须唯一标识一行数据，类似于主键。如果不唯一，写入时会产生覆盖，读取结果不确定。

**2. 长度原则**
RowKey 应尽量短小，原因：
- MemStore 缓存和 HFile 中均以 RowKey 为索引
- 过长的 RowKey 会占用大量存储空间（成百上千字节的索引）
- 推荐长度：16~100 字节，典型设计 8~16 字节（如 Long 转 byte[]）

**3. 散列原则（避免热点）**
热点（Hotspot）指大量请求集中在少数 RegionServer 上，导致负载不均。解决方式：

| 策略 | 说明 | 适用场景 |
|------|------|----------|
| 盐值（Salting） | RowKey 前拼接随机字节前缀 | 写多读少，随机分布 |
| 哈希（Hashing） | 对原 RowKey 做 MD5/Long.hashCode 截取 | 需要保证同一业务数据相邻 |
| 反转（Reversing） | 反转时间戳等固定前缀 | 适合时间开头、ID 结尾的 RowKey |
| 随机 RowKey | 完全随机字符串 | 不关心数据顺序的场景 |

**4. 排序原则**
RowKey 按字典顺序（byte 比较）排列，决定了数据的物理存储顺序。利用这一特性：
- 将经常一起访问的数据设计为相近的 RowKey（前缀相同）
- 利用 Scan 时的时间范围过滤

### 典型热点场景及解决方案

**场景：时间戳开头的 RowKey（如 `2026080312000001`）**
- 问题：大量写请求集中在当前时间对应的 Region，形成写热点
- 解决：使用盐值或反转时间戳（如 `reverse_timestamp + user_id`）

**场景：用户 ID 开头（如 `user001_20260803`）**
- 问题：热门用户的读请求集中在同一 Region
- 解决：加盐或哈希打散

---

## 题目三：HBase 的 MemStore 和 HFile 是什么？它们之间如何配合工作？

### 参考答案

### MemStore（写缓存）

- **位置**：每个 Region 的每个列族对应一个 MemStore
- **作用**：先将数据写入 MemStore（内存），而不是直接落盘，大幅提升写入性能
- **Flush 触发条件**（满足任一即触发）：
  -  MemStore 大小超过 `hbase.hregion.memstore.flush.size`（默认 128MB）
  -  RegionServer 的 MemStore 总大小超过 `java.heapsize * 0.4`
  -  WAL 日志数量超过 `hbase.regionserver.max.logs`（默认 32）
  -  定期 flush（`hbase.regionserver.flush.policy`）
- **数据格式**：按 RowKey 排序的 KeyValue 链表（跳表结构 ConcurrentSkipListMap）

### HFile（数据文件）

- **位置**：`/hbase/data/default/{TableName}/{RegionId}/{ColumnFamily}/`
- **来源**：MemStore 达到阈值后 flush 成 HFile
- **结构**（四层 B+Tree 变体）：
  - **Data Block**：存储实际的 KeyValue 数据，配置 `blocksize`（默认 64KB）
  - **Meta Block**：用户自定义的元信息（如布隆过滤器数据）
  - **File Info**：文件元信息（最后更新时间、bloom filter 类型等）
  - **Index Block**：Data Block 和 Meta Block 的索引

### 两者配合流程

```
写入请求 → WAL（先写日志） → MemStore（内存排序） 
         ↓
    满足flush条件 → 生成 HFile（磁盘有序文件）
         ↓
    定期 compaction → 合并小 HFile 为大 HFile
```

**关键特性**：
- **写入流程**：先写 WAL 保证持久化，再写 MemStore，MemStore 满后 flush 成 HFile
- **LSM-Tree 结构**：MemStore + HFile 的组合即 LSM-Tree（Log-Structured Merge Tree），牺牲部分读性能换取极快写入速度
- **读流程**：先查 BlockCache → 查 MemStore → 查 HFile（多版本合并）

---

## 题目四：HBase 的读写流程是怎样的？如何保证数据一致性？

### 参考答案

### 写流程（Put / Delete）

```
1. Client 请求 ZooKeeper，获取 -ROOT- 表位置（早期版本）或 meta 表位置
2. Client 连接 meta 表所在的 RegionServer，获取目标 RowKey 所在的 RegionServer
3. Client 将数据写入目标 RegionServer 的 WAL（预写日志，顺序写）
4. 数据写入 MemStore（按 RowKey 排序），同时记录到 WAL
5. 若启用了 `setAutoFlush(false)`，数据先在客户端 buffer 中积累
6. 返回写入成功响应（如果写 WAL 成功即认为成功）

异常情况：
- WAL 写入失败则整个写入失败
- MemStore 写成功后若机器故障，可通过 WAL 恢复数据
```

### 读流程（Get / Scan）

```
1. Client 先查 ZooKeeper → 获取 meta 表位置 → 获取目标 RegionServer
2. Scan 操作流程：
   a. 创建 Scanner，扫描 meta 表定位目标 Region
   b. 按 BloomFilter 过滤不可能包含数据的 HFile
   c. 在 BlockCache（读缓存）中查找
   d. 在 MemStore 中查找
   e. 读取所有相关 HFile，进行多路归并排序
   f. 将热点数据放入 BlockCache 供后续读加速
3. Get 操作：定位到具体 Region 后执行单行读取
4. 返回合并后的结果（同一 RowKey 的多版本数据按时间倒序）
```

### 数据一致性保证

| 机制 | 说明 |
|------|------|
| WAL（预写日志） | 写操作先写日志，宕机后可从 WAL 恢复 MemStore 未 flush 的数据 |
| MemStore 原子写入 | 单行数据的写入在 MemStore 内部是原子的 |
| MVCC（多版本并发控制） | 读操作不受正在进行的 compaction / flush 影响，读取一致性快照 |
| HFile 只读 | HFile 一旦生成就不再修改，数据的更新通过生成新 HFile 实现 |
| Region 分裂一致性 | 分裂时 father region 会进行 flush，分裂后 father 数据完整迁移到两个 son regions |

---

## 题目五：HBase 协处理器（Coprocessor）是什么？有哪些使用场景？

### 参考答案

### 概念

协处理器是 HBase 提供的扩展机制，允许用户代码在 RegionServer 端直接运行，将计算逻辑下推到数据节点，减少网络传输。与 HDFS DataNode 的「计算向存储移动」理念一致。

### 两类协处理器

**1. Observer（观察者）—— 类比触发器**
在特定事件发生时执行回调，类似于数据库触发器，**不能修改数据**。

| 类型 | 触发时机 | 典型场景 |
|------|----------|----------|
| RegionObserver | Get / Put / Delete / Scan 等操作前后 | 过滤列、列级权限控制、审计日志 |
| MasterObserver | 创建表、删除表、修改表等 DDL 操作前后 | 表创建审批、DDL 审计 |
| WALObserver | WAL 写入前后 | WAL 复制到第三方存储 |

**2. Endpoint（端点）—— 类比存储过程**
动态添加 RPC 接口，允许在 RegionServer 上执行任意计算，Client 调用后返回结果。

- 类比：数据库的存储过程 / 函数
- 使用 ProtoBuf 定义接口协议
- 支持在服务端执行聚合（如 Sum、Count）、复杂过滤等操作

### 使用场景示例

**场景1：二级索引**
- 业务：按 email 查 user_id（RowKey 为 user_id，反向建 email 索引）
- 实现：Observer 在 Put 后自动写入索引表

**场景2：权限控制**
- 实现：RegionObserver 在 Get/Put 前检查用户权限，不符合则抛 AccessDeniedException

**场景3：聚合查询**
- 实现：Endpoint 在 RegionServer 上做 Sum/Count 聚合，减少网络传输
- 典型用例：按列族统计行数、计算列值之和

**场景4：数据自动压缩/归档**
- 实现：Observer 在 flush 或 compaction 时，将冷数据归档到外部存储（如 OSS）

### 加载方式

| 方式 | 配置位置 | 生效范围 | 注意事项 |
|------|----------|----------|----------|
| 表级配置 | `hbase table` 命令 | 单表 | 通过 `coprocessor$` 列族参数指定 |
| 全局配置 | `hbase-site.xml` | 所有表 | 配置在 `hbase.coprocessor.*` 路径下 |
| 代码加载 | HBase shell / Java API | 运行时 | 重启后失效 |

---

> 📚 **参考来源**
> - [HBase 官方文档 - Coprocessors](https://hbase.apache.org/book.html#cp)
> - [HBase 官方文档 - Architecture](https://hbase.apache.org/book.html#architecture)
> - [HBase 官方文档 - Writing Data](https://hbase.apache.org/book.html#writing)
> - [HBase 官方文档 - Reading Data](https://hbase.apache.org/book.html#reading)
> - [IBM Developer - HBase Architecture Explained](https://developer.ibm.com/articles/hbase-architecture-explained/)
