# Flink 面试题集 — 2026-08-15

> 涵盖 Flink 架构、Watermark、Checkpoint、窗口函数、状态管理等核心知识点，题目经典实用，答案详细准确。

---

## 题目一：Flink 的架构是怎样的？请描述 JobManager 和 TaskManager 的作用

### 参考答案

Apache Flink 采用 **主从架构**，核心组件包括 **JobManager**（主节点）和 **TaskManager**（从节点）。

### JobManager（主节点）

- **作用**：负责整个集群的协调和任务调度，是 Flink 作业的"大脑"
- **核心组件**：
  - **Dispatcher**：接收用户提交的 Job，为每个 Job 创建一个 JobMaster，并提供 REST 接口
  - **ResourceManager**：管理 TaskManager 的 Slot（槽位），负责 Slot 的申请与分配，与 YARN/K8s 等资源管理器交互
  - **JobMaster**：负责单个 JobGraph 的执行，包括将 JobGraph 转换为 ExecutionGraph、调度 Task 到 TaskManager 执行、协调 Checkpoint、处理 Task 失败等
- **高可用**：通过 ZooKeeper 或 K8s 实现 JobManager 的 HA 部署，避免单点故障

### TaskManager（从节点）

- **作用**：执行具体的 Task（算子实例），是 Flink 作业的实际执行者
- **核心机制**：
  - **Slot**：TaskManager 的资源分配单位，每个 Slot 拥有固定的内存（和可选的 CPU）
  - **Task 调度**：JobMaster 将算子链（Operator Chain）调度到 Slot 中执行，同一 Slot 可执行多个子任务
  - **数据传输**：Task 之间通过基于 Netty 的网络栈进行数据交换，支持流水线式数据传输（低延迟）
- **并行度**：一个 TaskManager 可运行多个 Slot，通过设置 `taskmanager.numberOfTaskSlots` 配置

### 作业执行流程

```
用户代码 → JobGraph → ExecutionGraph → 调度到 TaskManager → Slot 执行
```

1. 用户编写的 DataStream/DataSet API 代码经过编译生成 JobGraph
2. JobMaster 将 JobGraph 优化并转换为 ExecutionGraph（包含并行度信息）
3. ResourceManager 向 TaskManager 申请 Slot 资源
4. 获得 Slot 后，JobMaster 将 Task 调度到对应 TaskManager 执行
5. Task 之间通过数据流进行连接，形成完整的作业 pipeline

### 知识点速记

| 组件 | 角色 | 核心职责 |
|------|------|----------|
| JobManager | 主节点 | 协调、调度、Checkpoint 协调 |
| TaskManager | 从节点 | 执行 Task、管理 Slot |
| ResourceManager | JM 子组件 | Slot 管理、资源分配 |
| JobMaster | JM 子组件 | 单作业执行管理 |

---

## 题目二：Flink 的 Watermark 机制是什么？如何处理乱序数据？

### 参考答案

### 为什么需要 Watermark

在事件时间（Event Time）处理场景中，数据从 source 到算子的过程中，由于网络延迟、分布式系统等因素，数据到达的顺序往往和事件发生的顺序不一致（**乱序**）。Flink 需要一种机制来判断：**某窗口的所有数据是否已经到齐**，这时候 Watermark 就派上用场了。

### Watermark 的定义

**Watermark（水位线）** 是一个时间戳（Timestamp），本质上是一种 **承诺/断言**：

> **当 Watermark 到达某个时间 T 时，意味着所有事件时间 < T 的数据都已经到达（或不会再到达）。**

### Watermark 的两种策略

#### 1. 固定延迟策略（Fixed Watermark）

```java
WatermarkStrategy
    .forMonotonousTimestamps()  // 单调递增（水印不倒退）
    .withTimestampAssigner(...)
    .withIdleness(Duration.ofMinutes(1));  // 空闲源处理
```

#### 2. 可容忍延迟策略（Allowed Lateness）

```java
WatermarkStrategy
    .forBoundedOutOfOrderness(Duration.ofSeconds(10))  // 允许最多10秒乱序
    .withTimestampAssigner(...)
```

表示：Watermark 允许最多 `maxOutOfOrderness` 的乱序，如果窗口关闭后又来了延迟数据，可以配合 `sideOutputLateData` 将迟到数据输出到侧输出流。

### 窗口与 Watermark 的关系

```
窗口 [10:00, 10:10)
Watermark T=10:00 触发 → 窗口关闭，执行计算
Watermark T=10:05 触发 → 此时窗口已关闭，无影响
迟到数据 T<10:00，allowedLateness>0 → 仍可触发窗口重新计算
迟到数据 T<10:00，allowedLateness=0 → 直接进入 sideOutput
```

### 简单图解

```
数据流（事件时间）:
Event A(ts=9:58) → Event B(ts=10:02) → Watermark(10:00) → Event C(ts=9:55) → Watermark(10:05)
                    ↑乱序                                      ↑严重迟到
                    
窗口 [9:00, 10:00) 触发时机：
1. Watermark >= 10:00 时触发（第一次）
2. 如果设置了 allowedLateness(5min)，则迟到数据 ts<10:00 仍可触发窗口重新计算
```

### 多流合并时的 Watermark

当多个输入流合并时（比如 union、connect、join），Flink 取 **所有输入流 Watermark 的最小值** 作为当前 Watermark，因为必须等所有流都到齐才能保证完整性。

### 关键点总结

| 概念 | 说明 |
|------|------|
| Watermark 本质 | 时间戳，承诺所有 < T 的数据已到齐 |
| 处理乱序 | 配合 `BoundedOutOfOrderness` 允许一定延迟 |
| 窗口触发 | Watermark >= 窗口_end_time 触发窗口计算 |
| 迟到数据 | `allowedLateness` 内仍可触发窗口；之外进入 sideOutput |
| 多流合并 | 取所有流 Watermark 的最小值 |

---

## 题目三：Flink 的 Checkpoint 和 Savepoint 有什么区别？

### 参考答案

Flink 的容错机制核心是 **分布式快照**（Checkpoint/Savepoint），但两者在用途、触发方式、存储等方面有明显区别。

### Checkpoint — 自动容错

#### 什么是 Checkpoint

Checkpoint 是 Flink **自动周期性地** 对分布式状态进行快照的机制，用于在作业失败时自动恢复。当作业崩溃重启时，Flink 会从最近成功的 Checkpoint 恢复状态，无需人工干预。

#### 核心原理：Chandy-Lamport 算法变体

Flink 采用 **异步屏障快照（Asynchronous Barrier Snapshotting, ABS）** 算法：

1. **JobManager 的 CheckpointCoordinator** 定期向所有 Source 算子发送 `CheckpointBarrier`
2. Source 算子收到 Barrier 后，**保存自己的状态**（偏移量等），然后将 Barrier 广播到下游
3. 当多个输入流的算子收到 **所有流的 Barrier**（对齐）时，触发状态快照
4. 快照完成后，向 CheckpointCoordinator 确认完成

#### 配置示例

```java
StreamExecutionEnvironment env = StreamExecutionEnvironment.getExecutionEnvironment();
// 每10秒做一次 Checkpoint
env.enableCheckpointing(10_000);
// 两次 Checkpoint 之间至少间隔3秒
env.getCheckpointConfig().setMinPauseBetweenCheckpoints(3000);
// Checkpoint 超时时间
env.getCheckpointConfig().setCheckpointTimeout(60000);
// 只保留最近1个 Checkpoint
env.getCheckpointConfig().setTolerableCheckpointFailureNumber(0);
env.getCheckpointConfig().setMaxConcurrentCheckpoints(1);
```

#### 状态后端

| 状态后端 | 适用场景 | 特点 |
|----------|----------|------|
| HashMapStateBackend | 小状态、快速恢复 | 内存存储，重启丢失 |
| EmbeddedRocksDBStateBackend | 大状态、超大 Checkpoint | RocksDB+本地磁盘，支持增量 Checkpoint |

### Savepoint — 手动备份

#### 什么是 Savepoint

Savepoint 是由用户**手动触发**的快照，用于计划内的作业停止/重启/升级。

```bash
# 触发 Savepoint
flink savepoint <jobId> [targetDirectory] [-d]  # -d 表示触发后停止作业

# 从 Savepoint 恢复
flink run -d -s savepointPath <jar> ...
```

#### 与 Checkpoint 的核心区别

| 特性 | Checkpoint | Savepoint |
|------|------------|-----------|
| 触发方式 | 自动（周期性） | 手动（按需） |
| 用途 | 故障恢复 | 计划维护、版本升级 |
| 删除 | 自动清理旧版本 | 需手动删除 |
| 作业拓扑变更 | 通常兼容 | 可手动调整 |
| 存储位置 | 可配置 | 固定目录 |
| 失败容忍 | Checkpoint 失败可容忍 | 必须成功才停止作业 |

### 增量 Checkpoint（Incremental Checkpoint）

RocksDB 状态后端支持增量 Checkpoint，只保存相对于上一次 Checkpoint 的增量变化，避免每次全量快照，显著减少存储和写入压力：

```java
EmbeddedRocksDBStateBackend rocksDB = new EmbeddedRocksDBStateBackend(true); // true = 增量
env.setStateBackend(rocksDB);
```

### 常见问题与解决方案

**Q: Checkpoint 对性能的影响？**
- ABS 机制在 Barrier 对齐期间，上游数据会缓存（可能造成短暂反压）
- 可通过 `setCheckpointMode(CheckpointingMode.AT_LEAST_ONCE)` 减少对齐开销（但精度降低）

**Q: 作业拓扑变更后如何恢复？**
- Savepoint 支持手动映射状态到新算子（通过算子 ID）
- 建议为关键算子显式设置 `uid(String)` 以保持 ID 稳定

---

## 题目四：Flink 窗口函数有哪几种？滚动窗口、滑动窗口、会话窗口有什么区别？

### 参考答案

Flink 支持两大类窗口：**滚动窗口（Tumbling Window）** 和 **滑动窗口（Sliding Window）**，以及更高级的 **会话窗口（Session Window）** 和 **全局窗口（Global Window）**。

### 窗口的分类

#### 1. 滚动窗口（Tumbling Window）

- **特点**：窗口大小固定，不重叠，首尾相接
- **适用场景**：按固定时间段统计，如"每小时的订单量"
- **示例**：`TumblingEventTimeWindows.of(Time.minutes(5))`

```
|<- 5min ->|<- 5min ->|<- 5min ->|
  [0-5)      [5-10)     [10-15)
```

#### 2. 滑动窗口（Sliding Window）

- **特点**：窗口大小固定，但可以重叠，由**窗口大小**和**滑动步长**两个参数决定
- **适用场景**：需要保留历史上下文，如"最近5分钟的平均值，每1分钟更新一次"
- **示例**：`SlidingEventTimeWindows.of(Time.minutes(5), Time.minutes(1))`

```
|<-   5min   ->|
     |<-   5min   ->|
          |<-   5min   ->|
[0-5)       [1-6)        [2-7)
滑动步长=1min，窗口大小=5min
```

#### 3. 会话窗口（Session Window）

- **特点**：由数据之间的**间隔时间**动态决定窗口边界，没有固定大小
- **适用场景**：用户行为分析，如"用户超过30秒不操作则会话结束"
- **实现**：需要指定**会话间隙（Session Gap）**
- **示例**：`EventTimeSessionWindows.withGap(Time.seconds(30))`

```
Data A ── Data B ────── Data C ── Data D
      gap=10s        gap=10s
    窗口1              窗口2
```

#### 4. 计数窗口（Count Window）

- **特点**：按照元素数量触发，而非时间触发
- **示例**：每 100 条数据计算一次
- **注意**：计数窗口是 TumblingCountWindow 或 SlidingCountWindow

### 窗口函数的类型

窗口内的计算分为两类：

#### 1. 全量窗口函数（AggregateFunction / ProcessWindowFunction）

- **增量聚合**：数据来一条处理一条，保存中间结果，窗口关闭时输出最终结果
  - `reduceFunction` / `aggregateFunction`
  - 内存占用低，适合大数据量场景
- **全量窗口函数**：先缓存窗口内所有数据，窗口关闭时一起处理
  - `processWindowFunction`
  - 可以获取窗口的完整上下文信息（窗口开始/结束时间、窗口内所有数据）
  - 可以与增量聚合结合：`aggregate(AggregateFunction, ProcessWindowFunction)`

#### 2. 实际使用示例

```java
// 增量聚合 + 全量上下文（最常用）
stream
    .keyBy(...)
    .window(TumblingEventTimeWindows.of(Time.minutes(5)))
    .aggregate(
        new AverageAggregate(),   // 增量聚合：来一条算一条
        new ProcessWindowFunction<...>() {  // 提供窗口元信息
            @Override
            public void process(Context context, Iterable<T> input, 
                                Collector<O> out) {
                // 可以访问窗口开始/结束时间、key、当前 watermark 等
                long windowStart = context.window().getStart();
                long windowEnd = context.window().getEnd();
                // ...
            }
        }
    );
```

### 关键区别速查

| 窗口类型 | 大小 | 重叠 | 边界 | 典型场景 |
|----------|------|------|------|----------|
| 滚动窗口 | 固定 | 否 | 固定对齐 | 每小时统计 |
| 滑动窗口 | 固定 | 是 | 固定对齐 | 移动平均 |
| 会话窗口 | 不固定 | 否 | 动态 gap | 用户行为 |
| 全局窗口 | 无界 | — | 永不触发 | 需配合触发器 |

---

## 题目五：Flink 的状态管理和状态后端有哪几种？如何选择合适的状态后端？

### 参考答案

### 为什么需要状态

Flink 是**有状态的流处理框架**。状态（State）是算子在处理过程中需要"记住"的信息，比如：

- 聚合计算中的中间结果（sum、count）
- 窗口中缓存的数据
- Join 算子中维护的左右表数据
- CEP 中间匹配结果

### 状态的分类

#### 1. 按算子划分

| 状态类型 | 作用域 | 示例 |
|----------|--------|------|
| **Keyed State** | 绑定到当前处理的 Key | `valueState.update("newValue")` |
| **Operator State**（非 Keyed） | 整个算子实例共享 | Kafka Connector 的分区偏移量 |

#### 2. 按数据结构划分

| 状态类型 | 数据结构 | 适用场景 |
|----------|----------|----------|
| **ValueState\<T\>** | 单值 | 存储简单值 |
| **ListState\<T\>** | 列表 | 存储多个值 |
| **MapState\<K, V\>** | 映射表 | 键值对存储 |
| **ReducingState\<T\>** | 自动聚合 | 持续聚合 |
| **AggregatingState\<I, O\>** | 自定义聚合 | 复杂聚合 |

#### 3. 状态访问示例

```java
public class CountWindowAverage 
    extends KeyedProcessFunction<String, Long, Double> {
    
    // 声明状态
    private ValueState<Long> countState;
    private ValueState<Long> sumState;
    
    @Override
    public void open(Configuration parameters) {
        // 初始化状态描述器
        countState = getRuntimeContext().getValueState(
            new ValueStateDescriptor<>("count", Long.class));
        sumState = getRuntimeContext().getValueState(
            new ValueStateDescriptor<>("sum", Long.class));
    }
    
    @Override
    public void processElement(Long value, Context ctx, 
                               Collector<Double> out) throws Exception {
        Long currentCount = countState.value();
        Long currentSum = sumState.value();
        
        currentCount = (currentCount == null) ? 1L : currentCount + 1;
        currentSum = (currentSum == null) ? value : currentSum + value;
        
        countState.update(currentCount);
        sumState.update(currentSum);
        
        out.collect((double) currentSum / currentCount);
    }
}
```

### 状态后端（State Backend）

状态后端决定了 **状态的存储方式** 和 **Checkpoint 的存储位置**。

| 状态后端 | 存储介质 | 性能 | 状态大小 | 适用场景 |
|----------|----------|------|----------|----------|
| **HashMapStateBackend** | JVM Heap（内存） | 最快 | 受限于 JVM 堆大小 | 小状态、低延迟场景 |
| **EmbeddedRocksDBStateBackend** | RocksDB（本地磁盘 + 内存缓存） | 稍慢（SSD 可弥补） | 支持超大状态（TB级） | 大状态、增量 Checkpoint |

### HashMapStateBackend

```java
env.setStateBackend(new HashMapStateBackend());
// 状态存储在 TaskManager 的 JVM 堆内存中
// 重启后状态丢失（除非开启 Checkpoint）
```

**优点**：读写性能高（纯内存）
**缺点**：状态大小受限于 JVM 堆，通常用于 GB 级别状态

### EmbeddedRocksDBStateBackend

```java
EmbeddedRocksDBStateBackend rocksDB = new EmbeddedRocksDBStateBackend(true); // 增量
env.setStateBackend(rocksDB);
```

**特点**：
- 状态存储在 **RocksDB**（嵌入式的 KV 数据库）中，数据先写内存后刷盘
- 支持 **增量 Checkpoint**（只保存增量 SST 文件），大幅减少存储
- 支持 **大状态**（数十 GB 甚至 TB 级）
- **读写比 HashMap 慢**，但通过 SSD 可显著提升

### 如何选择状态后端

```
状态大小 < 1GB？→ HashMapStateBackend（简单、低延迟）
状态大小 > 1GB？→ EmbeddedRocksDBStateBackend（大状态场景）
需要增量 Checkpoint？→ EmbeddedRocksDBStateBackend（必须配合 RocksDB）
追求最低延迟？→ HashMapStateBackend
状态在 TM 重启后需要恢复？→ 任何后端 + Checkpoint
```

### 状态过期与清理

```java
// 状态 TTL（过期自动清理）
ValueStateDescriptor<T> desc = new ValueStateDescriptor<>(
    "myState", TypeInformation.of(String.class));
desc.enableTimeToLive(StateTtlConfig.newBuilder(Time.minutes(10))
    .setUpdateType(StateTtlConfig.UpdateType.OnCreateAndWrite)  // 创建/写入时刷新 TTL
    .setStateVisibility(StateTtlConfig.StateVisibility.NeverReturnExpired)  // 永不返回过期数据
    .build());
```

### 总结

| 维度 | 说明 |
|------|------|
| 状态本质 | 算子处理过程中的"记忆" |
| Keyed State | 按 Key 隔离，context 获得 |
| Operator State | 算子级别，需实现 CheckpointedFunction |
| HashMapStateBackend | 内存存储，最快，小状态 |
| RocksDBStateBackend | 磁盘存储，支持大状态+增量 Checkpoint |
| 状态 TTL | 防止状态无限膨胀，自动过期清理 |

---

## 📚 参考来源

1. [Flink 官方文档 — DataStream API](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/overview/)
2. [Flink 官方文档 — State & Checkpointing](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/stateful-stream-processing/)
3. [Flink 官方文档 — Windows](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/dev/datastream/operators/windows/)
4. [Flink 官方文档 — Event Time & Watermarks](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/concepts/time/)
5. [Flink 官方文档 — RocksDB State Backend](https://nightlies.apache.org/flink/flink-docs-release-1.18/docs/ops/state/state_backends/)

> 本题集整理于 2026-08-15，供面试准备参考使用。
