# HBase 面试题精选 — 2026-08-28

> 5 道高频 HBase 面试题，涵盖架构设计、RowKey 设计、存储结构、读写流程、协处理器等核心知识点。

---

## 1. 请简述 HBase 的整体架构，各核心组件的职责是什么？

**参考来源：** [Apache HBase Architecture — Official Documentation](https://hbase.apache.org/book.html#arch.overview)

**答案：**

HBase 采用经典的 Master-Slave 架构，核心组件包括：

- **HMaster（Master 节点）：** 负责 Region 的分配与调度、DDL 操作（建表/删表）、负载均衡、故障 Region 的重新分配。HMaster 可以部署多个（Active-Standby 模式），通过 ZooKeeper 选主保证高可用。
- **HRegionServer（RegionServer 节点）：** 负责处理数据的读写请求，管理多个 Region。每个 RegionServer 包含：
  - **多个 Region：** 一张表按 RowKey 范围水平切分后的数据分片。
  - **HLog（WAL）：** 预写日志，保证数据写入的持久性和故障恢复能力。
  - **MemStore：** 内存写缓存，每个 Store（列族）一个，数据先写入 MemStore。
  - **BlockCache：** 读缓存，缓存频繁读取的数据块。
- **ZooKeeper：** 集群协调者。存储 HBase 元数据表（hbase:meta）的位置、RegionServer 的存活状态、Master 选举等。HBase 强依赖 ZooKeeper，是集群的"神经系统"。

**核心流程：** Client → ZooKeeper（查 meta 表位置） → RegionServer（读写数据）；HMaster 负责管理和调度，不直接参与数据读写。

---

## 2. HBase 的 RowKey 设计有哪些原则？如何避免热点问题？

**参考来源：** [Apache HBase — RowKey Design](https://hbase.apache.org/book.html#rowkey.design)

**答案：**

RowKey 是 HBase 表设计中最关键的部分，因为它决定了数据的物理存储分布和访问模式。核心设计原则：

**（1）长度原则：** 建议 10~100 字节，越短越好。RowKey 会存储在每个 KeyValue 中，过长会占用大量存储空间。

**（2）唯一性：** RowKey 必须唯一标识一行数据，设计时需确保不重复。

**（3）散列原则（避免热点）：** 这是最核心的设计要点。如果 RowKey 是单调递增的（如时间戳开头），所有新数据会集中写入同一个 Region，造成写热点。

**常见的散列策略：**

- **加盐（Salting）：** 在 RowKey 前加随机前缀（如 `hash(RowKey) % regionNum`），打散到不同 Region。缺点：范围查询变困难。
- **反转（Reversing）：** 将固定格式的数据反转，如手机号 `13800138000` → `00083100831`，使尾部差异（高频变化部分）出现在前缀。
- **哈希（Hashing）：** 对原始 RowKey 取 MD5/SHA 哈希的前 N 位作为前缀，既保证散列性，又保持确定性（同一 RowKey 总映射到同一位置）。

**（4）业务相关性：** RowKey 应设计为最常用的查询模式服务。例如，如果查询主要按 `userId + timestamp` 进行，RowKey 应设计为 `userId_timestamp`，而非 `timestamp_userId`（会导致同一用户数据分散）。

---

## 3. HBase 的 MemStore 和 HFile 分别是什么？数据刷写（Flush）和合并（Compaction）机制如何工作？

**参考来源：** [Apache HBase — Storage Architecture](https://hbase.apache.org/book.html#hbase.storage)

**答案：**

**MemStore：**
- 每个 Store（列族）对应一个 MemStore，是内存中的有序写缓冲区（SortedMap 结构）。
- 写入数据时先写 WAL（HLog），再写 MemStore。数据在 MemStore 中按 RowKey 排序。
- 当 MemStore 大小达到阈值（默认 128MB，由 `hbase.hregion.memstore.flush.size` 控制），触发 **Flush**，将内存数据持久化为磁盘上的 HFile。

**HFile（StoreFile）：**
- MemStore Flush 生成的底层存储文件，基于 Hadoop 的 HFile 格式（实际是 HDFS 上的文件）。
- HFile 内部采用 **LSM-Tree**（Log-Structured Merge-Tree）结构，数据按 RowKey 排序，支持高效的顺序读和二分查找。
- 包含 Data Block、Index Block、Bloom Filter Block、Trailer 等结构。

**Flush 机制：**
- 单个 MemStore 达到阈值，触发该 Region 的所有 MemStore Flush（整个 Region 级别，不是单列族）。
- 全局 MemStore 总量达到堆内存一定比例时，也会触发 Flush。
- RegionServer 关闭时，会强制 Flush 所有 MemStore。

**Compaction 机制：**
- **Minor Compaction：** 选取相邻的几个小 HFile 合并为一个较大的 HFile，减少文件数量，不清理已删除/TTL 过期数据。自动触发。
- **Major Compaction：** 将一个 Store 下的所有 HFile 合并为一个 HFile，同时清理标记删除和过期数据。默认 7 天自动触发一次（`hbase.hregion.majorcompaction`），但生产环境常设为 0（禁用自动），改为手动或低峰期调度。

**为什么需要 Compaction？** HFile 数量过多会导致读放大（需要合并多个 HFile 的结果），Compaction 通过合并减少文件数，提升读性能。

---

## 4. HBase 读写数据的完整流程是怎样的？

**参考来源：** [Apache HBase — Client](https://hbase.apache.org/book.html#client)

**答案：**

**写入流程：**

1. Client 发起 Put 请求，先通过 ZooKeeper 查询 `hbase:meta` 表，找到目标 RowKey 所在的 RegionServer。
2. Client 缓存 Region 位置信息（后续请求直接访问，meta 变更时更新缓存）。
3. 请求发送到目标 RegionServer，数据先写入 **WAL（HLog）**，保证持久化。
4. 数据写入对应列族的 **MemStore**（内存缓冲区）。
5. Client 收到写入成功响应。
6. 后台异步：MemStore 达到阈值 → Flush → 生成 HFile 写入 HDFS；HFile 数量达到阈值 → Compaction。

**读取流程：**

1. Client 同样先定位目标 RegionServer（通过 meta 表 + 缓存）。
2. RegionServer 收到 Get/Scan 请求后，需要合并多个数据源：
   - **BlockCache（读缓存）：** 检查热点数据是否已缓存。
   - **MemStore：** 内存中尚未 Flush 的最新数据。
   - **HFile：** 磁盘上已持久化的数据。
3. 使用 **Bloom Filter** 快速判断某个 RowKey 是否存在于某个 HFile 中，跳过不相关的 HFile。
4. 通过 HFile 的 **Index Block** 定位具体的数据 Block。
5. 合并（Merge）以上所有来源的结果，返回最新版本的数据给 Client。

**关键优化点：**
- Bloom Filter 显著减少无效磁盘 IO。
- BlockCache 缓存热点数据，减少磁盘读取。
- Region 预切分避免建表时的热点。
- 客户端缓存 Region 位置，减少 meta 表查询。

---

## 5. HBase 协处理器（Coprocessor）是什么？有哪些类型？典型使用场景有哪些？

**参考来源：** [Apache HBase — Coprocessors](https://hbase.apache.org/book.html#cp)

**答案：**

协处理器是 HBase 提供的一种将计算逻辑下推到 RegionServer 端执行的机制，类似于数据库中的触发器/存储过程，可以显著减少网络传输和客户端开销。

**两种类型：**

**（1）Observer（观察者）：**
- 类似于数据库的触发器，在特定事件发生时回调用户代码。
- 主要接口：
  - `RegionObserver`：Region 级别的回调，如 prePut / postPut（写入前后）、preGet / postGet（读取前后）、preDelete / postDelete。
  - `MasterObserver`：Master 级别回调，如建表前后、Region 分配前后。
  - `WALObserver`：WAL 相关回调。
- **典型场景：** 数据校验（写入前校验字段合法性）、自动填充（写入时自动添加时间戳/版本号）、权限控制（读取前做鉴权）、二级索引维护（写入时同步更新索引表）。

**（2）Endpoint（终端）：**
- 类似于数据库的存储过程，提供自定义 RPC 接口，Client 可直接调用。
- 数据在 RegionServer 端本地处理，只返回聚合结果，大幅减少网络传输。
- **典型场景：** Server 端聚合计算（Sum、Count、Avg）、分布式数据统计、复杂查询下推。
- 经典案例：`hbase-server` 内置的 `RowCounter` 和 `SumEndPoint` 就是 Endpoint 协处理器的实现。

**部署方式：**
- **静态部署：** 在 `hbase-site.xml` 中配置，需要重启 RegionServer。
- **动态部署：** 通过 HBase Shell 或 API 热加载（`alter 'table', METHOD => 'table_att', 'coprocessor' => '...'`），无需重启。

**注意事项：**
- 协处理器运行在 RegionServer 进程中，代码 bug 可能导致 RegionServer 崩溃。
- 生产环境需要充分测试，建议做好异常隔离和日志记录。

---

> 📖 更多 HBase 学习资料：
> - [Apache HBase Official Reference Guide](https://hbase.apache.org/book.html)
> - [HBase: The Definitive Guide (O'Reilly)](https://www.oreilly.com/library/view/hbase-the-definitive/9781492024255/)
> - [HBase Wiki](https://wiki.apache.org/hadoop/HBase)
