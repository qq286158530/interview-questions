# Spark 核心面试题（2026-08-03）

## 题目一：Spark 架构与核心组件

**问题：** 请描述 Apache Spark 的整体架构，以及 Driver、Executor、Worker、Cluster Manager 各自的作用是什么？

**答案：**

Apache Spark 采用 Master-Slave 架构，主要包含以下核心组件：

### 1. Driver（驱动器）
- **作用**：是 Spark 应用程序的入口点，负责运行用户的 `main()` 方法
- **核心职责**：
  - 创建 `SparkContext`，与 Cluster Manager 通信申请资源
  - 将用户代码转换为 DAG（有向无环图）
  - 将 DAG 划分为 Stage，进而转换为 Task
  - 调度 Task 到 Executor 执行
  - 收集 Executor 的执行结果
- **运行位置**：通常在客户端，但在 YARN Cluster 模式 下也运行在集群内部

### 2. Executor（执行器）
- **作用**：运行在 Worker 节点上的工作进程，负责执行具体的 Task
- **核心职责**：
  - 启动时向 Driver 注册，并保持心跳
  - 接收 Driver 分发的 Task，在 JVM 中执行
  - 将数据缓存在内存或磁盘中（通过 `RDD.persist()`）
  - 将结果返回给 Driver

### 3. Worker（工作节点）
- **作用**：集群中的从节点，负责管理本节点上的资源
- **核心职责**：
  - 向 Master 注册自己的 CPU 和内存资源
  - 启动和管理 Executor 进程
  - 监控 Executor 的运行状态

### 4. Cluster Manager（集群管理器）
- **作用**：管理集群资源，负责为 Spark 应用分配资源
- **支持的类型**：
  - **Standalone**：Spark 自带的简单集群管理器
  - **YARN**：Hadoop 生态的通用资源管理器
  - **Mesos**：Apache 的通用集群管理器
  - **Kubernetes**：容器化编排平台

### 架构流程：
```
Client → Driver → Cluster Manager → Worker → Executor
                      ↑
              资源分配请求
```

### 部署模式：
| 模式 | Driver运行位置 | Executor运行位置 | 适用场景 |
|------|---------------|-----------------|---------|
| Local | 客户端 | 客户端 | 开发/测试 |
| Standalone Cluster | Master节点 | Worker节点 | 小规模集群 |
| YARN Cluster | NM节点 | NM节点 | 生产环境 |
| YARN Client | 客户端 | NM节点 | 交互式分析 |

---

## 题目二：RDD 特性与算子分类

**问题：** 什么是 RDD？RDD 有哪些核心特性？请列举常用的 Transformation 和 Action 算子，并说明它们的区别。

**答案：**

### RDD 概述
RDD（Resilient Distributed Dataset）是 Spark 的核心抽象，表示一个弹性的分布式数据集。它是只读的、分区记录的集合，可以在集群上进行并行操作。

### RDD 的五大核心特性

| 特性 | 说明 |
|------|------|
| **分区列表（Partitions）** | RDD 被划分为多个分区，每个分区是数据集的一个逻辑片段，可并行处理 |
| **计算函数（Compute Function）** | 每个分区上执行的计算函数，如 `map`、`filter` |
| **依赖关系（Dependencies）** | RDD 与父 RDD 之间的依赖关系，分为窄依赖和宽依赖 |
| **分区器（Partitioner）** | 对于 Key-Value RDD，指定分区策略（如 HashPartitioner、RangePartitioner） |
| **优先位置（Preferred Locations）** | 计算每个分区时希望优先读取的位置（如 HDFS 块位置），实现数据本地性 |

### Transformation（转换算子）
返回一个新的 RDD，是**懒加载（Lazy）**的，不会立即执行。

**常见 Transformation 算子：**

| 算子 | 说明 | 依赖类型 |
|------|------|---------|
| `map(func)` | 对每个元素执行函数 | 窄依赖 |
| `filter(func)` | 过滤满足条件的元素 | 窄依赖 |
| `flatMap(func)` | 先 map 再展平 | 窄依赖 |
| `mapPartitions(func)` | 以分区为单位执行函数，效率更高 | 窄依赖 |
| `groupByKey([numTasks])` | 按 key 分组 | 宽依赖 |
| `reduceByKey(func, [numTasks])` | 按 key 聚合，更高效 | 宽依赖（但有 map 端聚合） |
| `join(rdd, [numTasks])` | 两个 RDD 按 key join | 宽依赖 |
| `cogroup(other)` | 对多个 RDD 按 key 协同分组 | 宽依赖 |
| `distinct([numTasks])` | 去重 | 宽依赖 |
| `repartition(numPartitions)` | 重新分区，可能触发 shuffle | 宽依赖 |
| `coalesce(numPartitions, shuffle=false)` | 缩小分区，避免 shuffle | 窄依赖（shuffle=false） |

### Action（行动算子）
触发 Job 执行，将结果返回到 Driver 或写入存储。

**常见 Action 算子：**

| 算子 | 说明 |
|------|------|
| `collect()` | 返回所有元素到 Driver |
| `count()` | 返回元素个数 |
| `take(n)` | 返回前 n 个元素 |
| `first()` | 返回第一个元素 |
| `reduce(func)` | 按函数聚合所有元素 |
| `fold(zeroValue)(func)` | 带初始值的 reduce |
| `aggregate(zeroValue)(seqOp, combOp)` | 更通用的聚合 |
| `foreach(func)` | 对每个元素执行函数（副作用） |
| `saveAsTextFile(path)` | 保存为文本文件 |
| `countByKey()` | 按 key 计数，返回 Map |
| `collectAsMap()` | 以 Map 形式返回 Key-Value 对 |

### 核心区别

| 特性 | Transformation | Action |
|------|---------------|--------|
| 返回类型 | 新的 RDD | 具体结果或写入存储 |
| 执行时机 | 懒加载（不立即执行） | 立即触发计算 |
| 链式调用 | 可链式调用 | 通常是终止操作 |
| 示例 | `rdd.map(x => x * 2)` | `rdd.collect()` |

### 经典面试题：窄依赖 vs 宽依赖

- **窄依赖（Narrow Dependency）**：父 RDD 的每个分区最多被子 RDD 的一个分区使用，如 `map`、`filter`。可以**流水线（Pipeline）**执行，一个分区出错只需重新计算对应分区。
- **宽依赖（Wide Dependency）**：父 RDD 的分区可能被多个子 RDD 分区使用，如 `groupByKey`、`join`。涉及 **Shuffle**，需要等待父分区全部完成。

---

## 题目三：Spark Shuffle 机制详解

**问题：** 什么是 Shuffle？为什么 Spark 需要 Shuffle？Hash Shuffle 和 Sort Shuffle 有什么区别？如何优化 Shuffle 性能？

**答案：**

### 什么是 Shuffle

Shuffle 是 Spark 中**重新分区数据**的操作，会导致数据跨节点流动。当一个 RDD 的某个分区需要依赖上一个 RDD 的多个分区时，就需要 Shuffle。

### 需要 Shuffle 的算子

**引发 Shuffle 的典型算子：**
- `groupByKey`、`reduceByKey`、`aggregateByKey`
- `join`、`cogroup`
- `repartition`、`coalesce(shuffle=true)`
- `distinct`
- 任何需要重新分区以实现数据全局操作的算子

### Hash Shuffle（Spark 1.x 时代）

**工作原理：**
```
Mapper:
  for each record (K, V):
    partitionId = K.hashCode % numPartitions
    write to file (partitionId)
    
Reducer:
  for each file:
    read all records with same key
    aggregate
```

**问题：**
- 每个 Mapper 为每个 Reducer 生成一个文件 → **文件数 = M × R**（M=Mapper数，R=Reducer数）
- 文件数过多，造成 **文件系统压力** 和 **GC 压力大**
- 小文件问题严重

### Sort Shuffle（Spark 2.x 之后）

**工作原理（以 SortShuffleWriter 为例）：**
```
1. Map Side:
   - 将数据写入内存缓冲区
   - 按 partitionId 和 key 排序
   - 分区内的数据按 key 排序
   - 每个分区写一个有序文件（索引文件 + 数据文件）

2. Reduce Side:
   - 根据索引文件读取对应分区的数据
```

**两种模式：**

| 模式 | 触发条件 | 特点 |
|------|---------|------|
| **BypassMergeSortShuffleWriter** | 分区数 ≤ `spark.shuffle.sort.bypassMergeThreshold`（默认200）且没有 map 端聚合 | 类似 Hash Shuffle，但不合并文件，适用于不需要排序的场景 |
| **SortShuffleWriter** | 一般情况 | 强制排序，生成一个有序的数据文件 + 索引文件 |

### Tungsten Sort Shuffle（Unsafe Shuffle）

- 基于 `sun.misc.Unsafe` 直接操作堆外内存
- 使用 `ByteBuffer` 序列化数据
- 避免 JVM GC 开销
- 要求记录序列化后小于 128MB，且支持序列化器是 Kryo 或 Spark SQL 的 Tungsten 内部编码

### 优化 Shuffle 性能的建议

1. **减少 Shuffle 数据量**
   - 在 map 端进行预聚合（`reduceByKey` 优于 `groupByKey`）
   - 使用 filter 提前过滤不必要的数据

2. **增加分区数**
   - 适中增加分区数（如 2-3 倍 CPU 核数），避免单分区数据过大
   - `spark.sql.shuffle.partitions`（默认200）可调整

3. **使用广播变量**
   - 小表广播到各个 Executor，避免 Shuffle（`broadcast join`）

4. **调整 Buffer 大小**
   - `spark.shuffle.file.buffer.size`（默认 32KB）：Mapper 端写入缓冲区
   - 增大可减少磁盘 IO 次数

5. **使用高性能序列化器**
   - `spark.serializer = org.apache.spark.serializer.KryoSerializer`
   - Kryo 比 Java 序列化快 10 倍，体积更小

6. **启用 shuffle compress**
   - `spark.shuffle.compress = true`（默认）
   - `spark.io.compression.codec = snappy`（默认）

7. **本地化等待**
   - `spark.locality.wait` 调整本地化任务等待时间

---

## 题目四：广播变量与累加器

**问题：** 什么是广播变量（Broadcast Variable）和累加器（Accumulator）？它们各自的使用场景是什么？有哪些注意事项？

**答案：**

### 广播变量（Broadcast Variable）

#### 概念
广播变量是只读的、缓存在每个 Executor 内存中的共享变量。通过广播，Driver 将数据分发到每个 Executor一次，所有 Task 共享这份数据，避免了每个 Task 都拷贝一份造成的冗余。

#### 创建与使用

**Scala：**
```scala
// 创建广播变量
val broadcastVar = sc.broadcast(Array(1, 2, 3, 4, 5))

// 在 Task 中读取（只读）
val result = broadcastVar.value.map(x => x * 2)

// 销毁（释放资源）
broadcastVar.unpersist()
```

**Python：**
```python
broadcast_var = sc.broadcast([1, 2, 3, 4, 5])
result = broadcast_var.value
```

#### 核心特点

| 特性 | 说明 |
|------|------|
| **只读** | 一旦创建不可修改，保证数据一致性 |
| **高效传播** | 通过 BitTorrent 式的广播机制，大数据量时分发效率高 |
| **Executor 缓存** | 每个 Executor 内存中只保存一份副本 |
| **自动更新** | Driver 端更新后，下一个 Job 会自动使用新值 |

#### 适用场景

1. **大表广播 Join**：小表广播到所有节点，避免 Shuffle
   ```scala
   val smallTable = spark.read.parquet("path/to/small").collect()
   val broadcastSmall = sc.broadcast(smallTable)
   
   largeTable.join(broadcastSmall.value, "key")
   ```

2. **配置参数分发**：如维度表、配置字典

3. **共享只读数据**：鸢尾花数据集、特征映射等

4. **避免重复大对象传输**：如 ML 模型的特征编码

#### 注意事项
- **数据量不宜过大**：广播变量存储在 Executor 堆内存中，过大可能导致 OOM
- **只读约束**：修改广播变量不会生效（实际 Task 读取的是副本）
- **及时销毁**：长时间运行时调用 `unpersist()` 释放缓存

---

### 累加器（Accumulator）

#### 概念
累加器是支持**只加（Add-only）**操作的共享变量。Task 可以向累加器增加值，但只能从 Driver 读取最终结果。常用于**全局计数**和**全局求和**。

#### 创建与使用

**Scala：**
```scala
// 创建累加器，初始值为0
val accum = sc.longAccumulator("My Accumulator")

// 在 Task 中添加
rdd.foreach(x => accum.add(x))

// 在 Driver 中获取结果
println(accum.value)
```

**Python：**
```python
accum = sc.accumulator(0)
def count_handler(x):
    global accum
    accum += x

rdd.foreach(count_handler)
print(accum.value)
```

#### 核心特点

| 特性 | 说明 |
|------|------|
| **只加操作** | 支持 `add`、`+=` 等累加操作 |
| **Task 安全** | 每个 Task 对累加器的修改会被 Executor 合并，最终结果正确 |
| **只读结果** | Task 只能累加，不能读取中间值 |
| **容错性** | 如果 Task 重新执行，累加器的值会正确累加（可能重复累加） |

#### 适用场景

1. **全局计数**：统计满足条件的记录数、空值数等
   ```scala
   val nullCount = sc.longAccumulator("nullCount")
   rdd.foreach { record =>
     if (record.contains("null")) nullCount.add(1)
   }
   ```

2. **全局求和**：统计总和、最大值标识等

3. **性能指标**：计数异常数据、调试信息汇总

#### 注意事项

1. **只加语义**：累加器只在 Action 中保证正确，Transformation 中可能重复计数
   ```scala
   // ❌ 错误：在 Transformation 中使用（可能多次执行）
   rdd.map(x => { accum.add(1); x })
   
   // ✅ 正确：在 Action 后使用
   rdd.foreach(x => accum.add(1))
   ```

2. **只读结果**：Task 内不能读取 `accum.value`，只能 `add()`

3. **自定义累加器**：可以实现 `AccumulatorV2` 抽象类实现更复杂的聚合逻辑

4. **空值安全**：`spark.sql.legacy.allowParamlessCount` 控制是否允许 COUNT(DISTINCT) 使用累加器

#### 广播变量 vs 累加器对比

| 维度 | 广播变量 | 累加器 |
|------|---------|--------|
| **用途** | 分发只读数据 | 全局累加计数 |
| **可变性** | 只读 | 只加 |
| **Task 读取** | ✅ 可以 | ❌ 不可以 |
| **Task 写入** | ❌ 不可以 | ✅ 可以 |
| **Driver 读取** | ✅ 可以 | ✅ 可以 |
| **典型应用** | 大表广播 Join | 全局计数/求和 |

---

## 题目五：Spark 内存管理机制

**问题：** 请详细描述 Spark 的内存管理机制，包括堆内内存（Heap Memory）和堆外内存（Off-Heap Memory）的划分，Execution 和 Storage 内存的动态分配，以及常见的 OOM 原因和优化方法。

**答案：**

### Spark 内存模型概述

Spark 2.x 之后采用 **Unified Memory Management**（统一内存管理），Execution 和 Storage 内存可以动态借用。

### 内存划分

#### 1. 堆内内存（Heap Memory）

由 JVM 管理的统一堆内存，配置项：`spark.memory.fraction`（默认 0.6，即 60% 用于 Spark）

```
┌─────────────────────────────────────────────────────┐
│                  JVM Heap Memory                      │
├─────────────┬───────────────────────────────────────┤
│  System     │         Spark Memory (60%)             │
│  (40%)      │  ┌─────────────────┬─────────────────┐ │
│             │  │   Execution     │    Storage      │ │
│             │  │   Memory        │    Memory       │ │
│             │  │   (可动态借用)   │    (可动态借用)   │ │
│             │  └─────────────────┴─────────────────┘ │
└─────────────┴───────────────────────────────────────┘
```

#### 2. 堆外内存（Off-Heap Memory）

通过 `spark.memory.offHeap.enabled=true` 启用，使用 JDK `DirectByteBuffer` 或 `sun.misc.Unsafe` 操作堆外内存。

配置项：`spark.memory.offHeap.size`（默认 0，表示禁用）

```
┌──────────────────────────────────────────┐
│           Off-Heap Memory                │
│  ┌──────────────┬──────────────────────┐  │
│  │  Execution  │      Storage        │  │
│  │  Memory     │      Memory         │  │
│  └──────────────┴──────────────────────┘  │
└──────────────────────────────────────────┘
```

### Execution vs Storage Memory

| 类型 | 用途 | 特性 |
|------|------|------|
| **Execution Memory** | Shuffle 排序、Hash 表、缓存中间结果 | 需要连续内存，紧迫时可 evict Storage |
| **Storage Memory** | RDD Cache、Broadcast 缓存、Unroll | 可借给 Execution，但需要协商 |

#### 动态借用规则

```
1. Execution 内存不足时：
   - 可以 evicted Storage 内存
   - 最多借到 (Execution + Storage) * spark.memory.storageFraction

2. Storage 内存不足时：
   - 只能借用 Execution 空闲内存
   - 借不到则溢写到磁盘
```

配置参数：
- `spark.memory.fraction = 0.6`：Spark 内存占堆内比例
- `spark.memory.storageFraction = 0.5`：Storage 占 Spark 内存的比例（可动态借用上限）

### 用户内存（User Memory）

`spark.memory.fraction` 之外的 40% 堆内存，用于：
- 用户数据结构（如 Scala 对象）
- Spark 内部元数据
- UDF/Spark SQL 生成的临时对象
- **OOM 高发区！**

### 常见 OOM 原因与优化

#### 1. Executor 堆内 OOM

**原因：**
- `spark.executor.memory` 设置过小
- 单个分区数据量过大（数据倾斜、Key 集中）
- 创建了过多的大对象（如广播大表）
- `collect()` 将数据拉回 Driver

**优化：**
```scala
// 避免大表 collect
val result = rdd.take(100)  // 而非 rdd.collect()

// 增加分区
rdd.repartition(1000)

// 增加内存
--conf spark.executor.memory=4g
```

#### 2. Metadata OOM（Driver OOM）

**原因：**
- 大量 Partition 元数据占用 Driver 内存
- `spark.sql.shuffle.partitions` 过大

**优化：**
```scala
// 减少 shuffle 分区数
spark.conf.set("spark.sql.shuffle.partitions", "200")

// 调整 maxResultSize
--conf spark.driver.maxResultSize=2g
```

#### 3. Shuffle OOM

**原因：**
- `spark.shuffle.memoryFraction` 过低（Spark 1.x）
- 大量数据需要排序但内存不足
- `groupByKey` 导致数据倾斜

**优化：**
```scala
// Spark 2.x 调整内存比例
--conf spark.memory.fraction=0.7
--conf spark.memory.storageFraction=0.3

// 使用 reduceByKey 替代 groupByKey（map 端预聚合）
rdd.reduceByKey(_ + _)  // 比 groupByKey 更省内存
```

#### 4. 堆外 OOM

**原因：**
- `spark.memory.offHeap.size` 设置过小
- Kryo 序列化数据过大

**优化：**
```scala
--conf spark.memory.offHeap.enabled=true
--conf spark.memory.offHeap.size=2g
```

### 内存监控与调优

```scala
// 查看内存使用
sc.getConf.getAll.foreach(println)

// 在代码中打印内存状态
val storageInfo = sc.getPersistentStorage
storageInfo.conf.getAll.foreach(println)
```

**常用配置参数汇总：**

| 参数 | 默认值 | 说明 |
|------|-------|------|
| `spark.executor.memory` | 1g | Executor 堆内存 |
| `spark.memory.fraction` | 0.6 | Spark 内存占比 |
| `spark.memory.storageFraction` | 0.5 | Storage 内存占比 |
| `spark.memory.offHeap.enabled` | false | 启用堆外内存 |
| `spark.memory.offHeap.size` | 0 | 堆外内存大小 |
| `spark.executor.memoryOverhead` | executorMemory * 0.1 | 堆外预留内存 |
| `spark.driver.maxResultSize` | 1g | Driver collect 最大结果 |

### 最佳实践

1. **预估数据量**：根据数据规模和分区数计算单分区大小
   ```
   单分区大小 ≈ 总数据量 / 分区数
   ```

2. **合理设置内存**：
   - 经验公式：`executor.memory ≈ 单Task峰值内存 × 2`
   - Storage 和 Execution 比例 4:6 或 5:5

3. **使用 Kryo 序列化**：
   ```scala
   --conf spark.serializer=org.apache.spark.serializer.KryoSerializer
   --conf spark.kryo.classesToRegister=...
   ```

4. **及时释放缓存**：
   ```scala
   rdd.unpersist()  // 释放 RDD 缓存
   broadcastVar.unpersist()  // 释放广播变量
   ```

5. **避免大 SQL**：将大 SQL 拆分为多个小 Job

---

## 参考来源

1. **Apache Spark 官方文档** - RDD Programming Guide
   - https://spark.apache.org/docs/latest/rdd-programming-guide.html

2. **Apache Spark 官方文档** - Shuffle Operations
   - https://spark.apache.org/docs/latest/rdd-programming-guide.html#shuffle-operations

3. **Apache Spark 官方文档** - Cluster Mode Overview
   - https://spark.apache.org/docs/latest/cluster-overview.html

4. **Apache Spark 官方文档** - Memory Management
   - https://spark.apache.org/docs/latest/tuning.html#memory-management

5. **Apache Spark 官方文档** - Broadcast Variables
   - https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables

---

*整理日期：2026-08-03 | 来源：Apache Spark 官方文档及业界实践*
