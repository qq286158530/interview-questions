# Spark 核心面试题（2026-08-14）

> 本文精选5道高质量Spark面试题，涵盖Spark架构、RDD、DataFrame、Shuffle、内存管理等核心知识点。

---

## 题目一：Spark 与 Hadoop MapReduce 的核心区别是什么？为什么Spark更高效？

### 参考答案

| 对比维度 | Apache Spark | Hadoop MapReduce |
|---------|-------------|-----------------|
| **数据处理方式** | 批处理 + 实时流处理 | 仅批处理 |
| **数据存储位置** | 内存（RAM） | HDFS 磁盘 |
| **计算速度** | 内存中运行快 100 倍，磁盘快 10 倍 | 相对较慢 |
| **缓存机制** | 支持 cache/persist 持久化到内存 | 高度依赖磁盘，无原生内存缓存 |
| **计算模型** | DAG 计算，支持迭代计算 | 线性 Map→Shuffle→Reduce |
| **交互模式** | 支持交互式查询（Spark Shell） | 无原生交互模式 |

**Spark 更高效的原因：**

1. **内存计算**：Spark 将数据保留在内存中，避免了 MapReduce 频繁的磁盘 I/O
2. **DAG 优化**：Spark 构建 DAG 有向无环图进行整体优化，减少 shuffle 次数
3. **Lazy Evaluation**： transformations 懒执行，直到 action 时才真正触发计算
4. **多语言支持**：提供 Scala、Python、Java、R 四种 API
5. **统一引擎**：一次开发可用于批处理、流处理、机器学习、SQL 查询

> 📎 来源：[TPointTech - Apache Spark Interview Questions](https://www.tpointtech.com/apache-spark-interview-questions)
> 📎 来源：[DataFlair - Apache Spark Interview Questions](https://data-flair.training/blogs/spark-interview-questions/)

---

## 题目二：RDD 的特性是什么？RDD 的 Transformation 和 Action 有哪些区别？

### 参考答案

**RDD（Resilient Distributed Datasets）核心特性：**

1. **分区列表（Partitions）**：数据被划分为多个分区，并行计算的基础
2. **计算函数（Compute Function）**：每个分区都有计算函数
3. **依赖关系（Dependencies）**：RDD 之间存在依赖关系，形成 lineage（血统）
4. **分区器（Partitioner）**：Key-Value RDD 可指定分区器（Hash / Range）
5. **最优位置（Preferred Locations）**：数据本地性，计算向数据移动

**Transformation vs Action 区别：**

| 特性 | Transformation | Action |
|------|---------------|--------|
| **执行方式** | 懒执行（Lazy），构建 DAG | 立即执行触发计算 |
| **返回值** | 新的 RDD | 其他数据类型 |
| **示例** | map, filter, groupByKey, join | collect, count, reduce, save |

**常用 Transformation：**
- `map(func)`：对每个元素执行函数
- `filter(func)`：过滤数据
- `flatMap(func)`：flatMap = map + flatten
- `groupByKey()`：按键分组
- `reduceByKey(func)`：按键聚合（比 groupByKey 高效）

**常用 Action：**
- `collect()`：返回所有元素到 driver
- `count()`：计数
- `reduce(func)`：聚合
- `take(n)`：返回前 n 个
- `saveAsTextFile()`：保存为文件

> 📎 来源：[DataFlair - RDD Transformation and Action](https://data-flair.training/blogs/spark-interview-questions/)
> 📎 来源：[TPointTech - Apache Spark Interview Questions](https://www.tpointtech.com/apache-spark-interview-questions)

---

## 题目三：Spark Shuffle 机制是什么？为什么 Shuffle 是性能瓶颈？如何优化？

### 参考答案

**Shuffle 机制：**

Shuffle 是 Spark 将数据重新分布的过程，类似于 MapReduce 的 Shuffle 阶段。当执行 `groupByKey`、`join`、`reduceByKey` 等需要跨分区聚合的操作时，Spark 需要将具有相同 key 的数据拉到同一个分区，这个过程就是 Shuffle。

**Shuffle 执行流程：**
1. Map 阶段：每个 Task 处理数据并输出 (key, value)
2. Shuffle Write：数据写入本地磁盘，按 key 进行分区
3. Shuffle Read：每个 Reduce Task 从各个 Mapper 拉取属于自己的分区数据
4. Reduce 阶段：聚合相同 key 的数据

**Shuffle 是性能瓶颈的原因：**

1. **磁盘 I/O**：大量数据写入磁盘再读取
2. **网络 I/O**：数据跨节点传输
3. **序列化/反序列化**：数据需要序列化传输
4. **内存压力**：Shuffle 数据可能撑爆内存

**优化策略：**

1. **减少 Shuffle 数据量**
   - 使用 `reduceByKey` 替代 `groupByKey`（预聚合）
   - 尽量在 map 端完成聚合

2. **增加并行度**
   - 合理设置 `spark.sql.shuffle.partitions`（默认 200）
   - 避免数据倾斜

3. **使用广播变量**
   - 小表广播：`broadcast(smallRDD)`
   - 避免大表 join 时的 Shuffle

4. **选择合适的输出格式**
   - 使用 `ConsolidateFiles` 合并小文件
   - 考虑使用 `sort-based shuffle` 替代 `hash-based shuffle`

```scala
// 示例：reduceByKey 优于 groupByKey
val rdd1 = sc.parallelize(List(("a",1),("b",1),("a",1)))
// 高效：先局部聚合再全局聚合
rdd1.reduceByKey(_+_).collect()
// 低效：所有相同 key 拉到同一分区
rdd1.groupByKey().mapValues(_.sum).collect()
```

> 📎 来源：[DataFlair - Apache Spark Interview Questions](https://data-flair.training/blogs/spark-interview-questions/)
> 📎 来源：[TPointTech - Spark Shuffle Optimization](https://www.tpointtech.com/apache-spark-interview-questions)

---

## 题目四：Spark 广播变量和累加器（Accumulator）的作用是什么？如何使用？

### 参考答案

**广播变量（Broadcast Variables）：**

广播变量是只读的共享变量，会被缓存到每个节点的内存中，而不是每个 Task 都复制一份。主要用于优化 join 操作，避免将小表数据随 Task 发送到各个节点。

**使用场景：**
- 大表 join 小表时，将小表广播
- 配置参数、维度表等只读数据

```scala
// 定义广播变量
val broadcastVar = sc.broadcast(Array(1, 2, 3, 4, 5))

// 在算子内使用
val result = rdd.map(x => x * broadcastVar.value.sum)
```

**累加器（Accumulators）：**

累加器是只能进行关联操作（associative）的变量，用于在 Spark 程序中对数据做全局计数或求和。

**使用场景：**
- 统计处理记录数
- 全局计数器
- 自定义聚合逻辑

```scala
// 定义累加器
val accum = sc.longAccumulator("My Accumulator")

// 在算子内累加
rdd.foreach(x => accum.add(x))

// 在 driver 端获取结果
println(accum.value)
```

**注意事项：**

1. 广播变量在 Task 中只读，不会导致数据竞争
2. 累加器只有在 Task 完全执行成功后才会被汇总
3. 在 transformation 中使用累加器可能计数不准确（因为 Task 可能重试）
4. 建议在 action 中使用累加器

> 📎 来源：[DataFlair - Shared Variables in Spark](https://data-flair.training/blogs/spark-interview-questions/)
> 📎 来源：[TPointTech - Apache Spark Interview Questions](https://www.tpointtech.com/apache-spark-interview-questions)

---

## 题目五：Spark 内存管理机制是怎样的？堆内内存（On-Heap）和堆外内存（Off-Heap）有什么区别？

### 参考答案

**Spark 内存模型（统一内存管理，Spark 1.6+）：**

Spark 采用统一内存管理，将内存划分为**存储内存**和**执行内存**两部分，二者可以动态占用。

```
┌─────────────────────────────────────┐
│           堆内内存 (On-Heap)          │
├──────────────────┬──────────────────┤
│    存储内存       │    执行内存       │
│  (Storage)       │  (Execution)     │
│                  │                  │
├──────────────────┴──────────────────┤
│         用户内存 (User Memory)        │
│    (RDD metadata, UDF等)             │
├─────────────────────────────────────┤
│       预留内存 (Reserved Memory)     │
│           300MB                       │
└─────────────────────────────────────┘
```

**内存区域说明：**

| 内存区域 | 作用 | 默认比例 |
|---------|------|---------|
| **存储内存** | 缓存 RDD、广播变量、thin shuffle | ~45% |
| **执行内存** | Shuffle、Sort、Hash join | ~45% |
| **用户内存** | 用户数据结构、元数据 | ~27% |
| **预留内存** | 系统预留 | 300MB |

**堆内 vs 堆外内存：**

| 特性 | 堆内内存 (On-Heap) | 堆外内存 (Off-Heap) |
|------|------------------|---------------------|
| **存储位置** | JVM 堆内存 | JVM 堆外直接内存 |
| **管理方式** | JVM GC 管理 | 手动管理（Unsafe） |
| **序列化** | Java 对象 | 字节缓冲区 |
| **优点** | 简单，GC 回收 | 减少 GC 压力，避免 OOM |
| **缺点** | GC 频繁影响性能 | 管理复杂 |

**配置参数：**
```scala
// 堆内内存配置
spark.memory.fraction = 0.6        // 执行+存储占总内存比例
spark.memory.storageFraction = 0.5 // 存储内存占总内存比例

// 堆外内存配置
spark.memory.offHeap.enabled = true
spark.memory.offHeap.size = 2g

// 执行内存
spark.executor.memory = 4g
```

**内存优化建议：**
1. 对于迭代算法，开启 `spark.memory.fraction` 提高内存利用率
2. 大数据量场景启用堆外内存减少 GC
3. 使用 `spark.rdd.compress` 压缩 RDD 存储
4. 合理设置 `spark.sql.shuffle.partitions` 避免 OOM

> 📎 来源：[TPointTech - Apache Spark Memory Management](https://www.tpointtech.com/apache-spark-interview-questions)
> 📎 来源：[DataFlair - Spark Architecture](https://data-flair.training/blogs/spark-interview-questions/)

---

*💡 提示：以上答案仅供参考，实际面试中还需结合项目经验深入回答。*
