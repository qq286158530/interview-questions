# HBase 面试题精选（2026-08-06）

> 涵盖 HBase 架构、RowKey 设计、存储结构、MemStore/HFile、协处理器等核心知识点

---

## 题目一：HBase 的整体架构是怎样的？RegionServer 和 HMaster 的职责是什么？

### 参考答案

HBase 采用 **Master-Slave（主从）架构**，核心组件如下：

| 组件 | 职责 |
|------|------|
| **HMaster** | 负责表的 CRUD 操作（创建表、删除表等），管理 RegionServer 的负载均衡，监控 RegionServer 上下线，处理元数据变更。HMaster 无单点瓶颈问题（通过备份 Master 实现高可用）。 |
| **RegionServer** | 负责处理数据的读写请求，管理一个或多个 Region。每个 Region 对应表中一段连续的行键范围。 |
| **ZooKeeper** | 负责选举主 Master、存储元数据（如 .META. 表位置），检测 RegionServer 存活状态。 |
| **HDFS** | 提供底层数据存储，HBase 的所有数据（HFile、WAL）都存储在 HDFS 上，具备副本容灾能力。 |

**读写流程简要说明：**

1. **写流程**：Client 先写 WAL（Write-Ahead Log）到 HDFS，然后写入 MemStore。MemStore 写满后flush 成 HFile 落盘。
2. **读流程**：Client 先从 ZooKeeper 获取 .META. 表位置，定位到目标 RegionServer，然后在 MemStore 和 HFile 中合并查找（BlockCache 加速）。

---

## 题目二：HBase 的 RowKey 设计有哪些原则？如何避免热点（Hotspotting）问题？

### 参考答案

### RowKey 设计原则

1. **长度原则**：RowKey 不宜过长，建议控制在 10~100 字节，HBase 会将数据加载到内存，过长会浪费内存。
2. **唯一性原则**：RowKey 必须唯一标识一行数据，类似关系型数据库的主键。
3. **散列（Hash）原则**：避免单调递增或递减的 RowKey（如时间戳顺序），否则所有写请求会集中到同一个 RegionServer，造成热点。

### 热点场景及解决方案

| 热点场景 | 解决方案 |
|----------|----------|
| 使用时间戳作前缀（如 `202608061200`） | 对 RowKey 取 MD5 或 hash 后截取前几位作前缀 |
| 使用用户 ID 作前缀，用户 ID 连续 | 加盐（Salting）：在 RowKey 前加随机前缀，分散到不同 Region |
| 查询经常是范围查询 | 设计Reversible RowKey（如将时间戳反转），兼顾范围查询和散列 |
| 唯一 ID 查询为主 | 直接用唯一 ID 作 RowKey，或加盐 + 反转组合 |

**经典加盐示例：**
假设原 RowKey = `userId_time`，加盐后 = `盐值_userId_time`（盐值 = hash(userId) % N），写请求被分散到 N 个 Region。

---

## 题目三：MemStore 和 HFile 的工作机制是什么？为什么 HBase 写入比读取快？

### 参考答案

### MemStore（写缓存）

- Client 写入数据时，先 Append 到 **WAL（Write-Ahead Log）**，再写入 **MemStore**。
- MemStore 是内存中的有序数据结构（类似跳表），保证数据按 RowKey 排序。
- 当 MemStore 大小达到阈值（默认 `hbase.hregion.memstore.flush.size=128MB`）时，触发 **flush** 操作，将内存数据顺序写入 HDFS 生成 HFile。

### HFile（HBase 的存储文件格式）

- HFile 是只读的、持久化到 HDFS 的有序文件，内部按 Block 存储（默认 64KB/64KB/64KB 三个值：`BLOCKSIZE`, `BLOCKCACHE_SIZE`, `COMPRESS_BLOCKSIZE`）。
- HFile 中包含 **Bloom Filter**（布隆过滤器）和 **Block Index**（块索引），用于快速定位数据。

### 为什么写入快？

1. **顺序写 WAL**：WAL 顺序追加到 HDFS，开销极低。
2. **写 MemStore 是内存操作**：MemStore 在内存中完成，延迟极低。
3. **批量刷盘**：多个 MemStore 达到阈值后才 flush 成 HFile，平摊了 I/O 开销。
4. **读取需要合并**：读数据时需要合并 MemStore + 多个 HFile（多层归并排序），开销远大于写入。

### HBase 读数据时的"写前复制（Read-Estimation）"现象

读请求会在 MemStore 和所有相关 HFile 中查找，然后通过 **Multi.Version Concurrency Control（MVCC）** 保证只返回最新版本数据。

---

## 题目四：HBase 的存储结构是怎样的？LSM-Tree（Log-Structured Merge Tree）在 HBase 中是如何体现的？

### 参考答案

### HBase 存储结构总览

```
Table (逻辑表)
  └── Region（按 RowKey 范围划分）
        └── Store（每个 Column Family 对应一个 Store）
              ├── MemStore（内存有序缓存）
              └── HFile（持久化有序文件，按 RowKey 排序）
```

- **Table** 按 RowKey 范围水平拆分成多个 **Region**，分布到不同 RegionServer。
- 每个 Column Family 对应一个独立的 **Store**，包含一个 MemStore 和多个 HFile。

### LSM-Tree 在 HBase 中的体现

HBase 的存储引擎采用 **LSM-Tree**（日志结构合并树）模型：

| 层级 | 组件 | 说明 |
|------|------|------|
| L0 | MemStore | 内存有序缓存，写入先到此处 |
| L1+ | HFile | 持久化的有序文件，定期 compaction 合并 |

**Compaction 机制：**

- **Minor Compaction**：将多个小的 HFile 合并成一个大的 HFile（只合并相邻层级）。
- **Major Compaction**：将一个 Store 下所有 HFile 合并成一个，清理已删除和过期数据（默认每 7 天执行一次）。

**LSM-Tree 的核心优势：**
- 写入完全顺序，性能极高。
- 通过后台 Compaction 不断合并文件，保证读取时只需合并少量文件。
- 利用 SSD/HDD 顺序写特性，吞吐量远超随机写入。

---

## 题目五：HBase 协处理器（Coprocessor）是什么？Observer 和 Endpoint 分别用在什么场景？

### 参考答案

### 协处理器概述

HBase 协处理器是一种**扩展机制**，允许用户在 RegionServer 上运行自定义代码，实现类似数据库触发器和存储过程的功能，无需修改 HBase 核心源码。

### 两类协处理器

#### 1. Observer（观察者）—— 类似触发器

Observer 在特定事件前后自动执行，用于**拦截和修改**操作。

| 类型 | 触发时机 | 常见用途 |
|------|----------|----------|
| **RegionObserver** | Get/Put/Delete/Scan 等数据操作前后 | 数据验证、动态列、访问控制、审计日志 |
| **MasterObserver** | CreateTable/DeleteTable/AlterTable 等DDL操作前后 | 表创建/删除的权限校验、自动创建关联索引表 |
| **WALObserver** | WAL 写入前后 | WAL 复制到其他存储（如 Kafka）、实时流处理 |

**代码示例（RegionObserver 伪代码）：**
```java
// 在 Put 之前拦截，添加数据校验
@Override
public void prePut(ObserverContext<RegionCoprocessorEnvironment> e,
                   Put put, WALEdit edit, Durability durability) {
    // 如果某列值 < 0，拒绝写入
    if (invalidValue(put)) {
        throw new DoNotRetryIOException("Invalid value!");
    }
}
```

#### 2. Endpoint（端点）—— 类似存储过程

Endpoint 允许在 RegionServer 上**执行用户自定义的分布式计算**，结果汇总到客户端。

**典型场景：**
- **自定义聚合**：在 RegionServer 上执行 `SUM/COUNT/AVG` 聚合，而不是把数据全拉到客户端再计算，极大减少网络传输。
- **二级索引**：表变更时自动更新索引表（结合 Observer 实现）。
- **自定义过滤器**：在 RegionServer 上执行复杂过滤逻辑。

### 加载方式

| 方式 | 配置位置 | 适用场景 |
|------|----------|----------|
| **静态加载** | `hbase-site.xml` → `hbase.coprocessor.region.classes` | 系统级，所有表生效 |
| **动态加载**（Shell） | `alter 'table', METHOD => 'table_att_unset', NAME => 'coprocessor$1'` | 单表生效，需重启表 |
| **HTableDescriptor** | Java 代码在创建表时指定 | 代码层面控制 |

### 注意事项

- **Observer 死循环风险**：如果 prePut 中又触发一次 Put，可能造成死循环，需在代码中避免。
- **性能影响**：协处理器执行在 RegionServer 主流程中，复杂逻辑会影响吞吐，需要谨慎评估。
- **版本兼容性**：协处理器代码需与 HBase 版本匹配，升级 HBase 时可能需要重新编译。

---

> 📚 以上题目整理自 HBase 官方文档、阿里巴巴 HBase 技术团队博客、牛客网/掘金等社区优质内容。
> 如有疏漏，欢迎提交 PR 修正。
