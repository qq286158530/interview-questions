# 程序员面试题 - 2026-08-15

## 🔥 HBase

---

### 题目1：HBase 的架构是怎样的？各组件的作用是什么？

【答案】

HBase 是一个基于 HDFS 的列式存储 NoSQL 数据库，采用 Master-Slave 架构：

**核心组件**：

| 组件 | 作用 |
|------|------|
| **ZooKeeper** | 协调服务，管理 RegionServer 状态，实现分布式锁 |
| **HMaster** | 主节点，负责表的 CRUD 操作、Region 分配、负载均衡 |
| **RegionServer** | 数据节点，负责数据的读写、MemStore 和 HFile 管理 |
| **Region** | 表的数据分片，每个 Region 存储一定范围的 RowKey |
| **WAL（Write-Ahead Log）** | 预写日志，用于宕机恢复，保证数据不丢失 |
| **MemStore** | 写缓存，先写入内存，达到阈值后 flush 为 HFile |
| **HFile** | HBase 的实际存储文件，基于 HDFS，存储在 DataNode 上 |
| **BlockCache** | 读缓存，热点数据缓存于此，减少磁盘 I/O |

**写入流程**：
```
Client → ZooKeeper 获取 Meta 位置 → 写入 WAL → 写入 MemStore → 返回成功
```

**读取流程**：
```
Client → ZooKeeper → MemStore（写缓存）→ BlockCache（读缓存）→ HFile → 合并返回
```

**特点**：
- 强一致性：写操作原子性保证
- 高并发读写：基于 LSM-Tree 架构
- 水平扩展：新增 RegionServer 自动负载均衡
- 容错性：数据多副本存储在 HDFS 上

📖 来源：https://hbase.apache.org/book.html

---分割线---

### 题目2：HBase 的 RowKey 设计原则有哪些？如何避免热点问题？

【答案】

**RowKey 设计核心原则**：

1. **长度原则**
   - RowKey 最大 64KB，建议越短越好（10-100 字节）
   - 过长会占用过多 MemStore 内存，影响存储效率

2. **唯一性原则**
   - RowKey 必须唯一标识一行数据
   - 类似主键，是 HBase 数据访问的主键

3. **散列原则**（避免热点）
   - 避免使用单调递增的 RowKey（如时间戳、自增 ID）
   - 热点问题：大量请求集中在少数 Region 上，导致负载不均

**避免热点的常用方法**：

| 方法 | 说明 | 示例 |
|------|------|------|
| **加盐（Salting）** | RowKey 前加随机前缀，分散到不同 Region | `MD5(rowKey) % N + originalKey` |
| **哈希（Hash）** | 对原 RowKey 做 MD5/SHA 取模打散 | `MD5(orderId).substring(0,6) + orderId` |
| **反转（Reverse）** | 反转时间戳或手机号，避免单调递增 | `reverse(timestamp)` |
| **随机数** | 直接加随机数后缀 | `userId + random(1000)` |

**设计示例**：
```java
// 场景：订单表，按用户查询订单
// 避免：rowKey = orderId（单增，热 点）
// 推荐：rowKey = userId + orderId（查询友好 + 散列）
// 或：rowKey = MD5(userId).substring(0,4) + userId + timestamp
```

**最佳实践**：
- 根据业务查询模式设计 RowKey
- 多条件查询配合 Filter 使用
- 利用 Phoenix 建立二级索引优化查询

📖 来源：https://hbase.apache.org/book.html#rowkey.design

---分割线---

### 题目3：HBase 的存储结构是怎样的？MemStore 和 HFile 的作用是什么？

【答案】

**HBase 存储层次结构**：

```
Table
├── Region（按 RowKey 范围分片）
│   ├── Store（每个 Column Family 一个 Store）
│   │   ├── MemStore（写缓存，JVM 堆内存）
│   │   └── HFile（磁盘文件，StoreFile 的实际格式）
│   └── WAL（预写日志）
└── HDFS（底层存储，多副本）
```

**MemStore**：
- 作用：写缓存，数据先写入 MemStore，内存排序后 flush 为 HFile
- 特点：
  - 按 RowKey + Column Family + Column Qualifier + Timestamp 排序
  - 达到阈值（默认 128MB）时触发 flush
  - 每个 Region 的每个 Column Family 有一个 MemStore
- 优化：合并写，提高顺序写磁盘的效率

**HFile**（StoreFile）：
- 作用：HBase 的实际存储文件格式，基于 Google BigTable 的 SSTable
- 结构：
  ```
  HFile
  ├── Data Block（数据块，默认 64KB，可配置）
  │   └── KeyValue: RowKey + Column Family + Qualifier + Timestamp + Value
  ├── Meta Block（元数据块，可选）
  ├── File Info（文件信息：起止 RowKey、时间戳等）
  ├── Index Block（索引块：Block 索引 + Root 索引）
  └── Trailer（文件尾部，指向各部分偏移量）
  ```
- 特点：
  - 稀疏列式存储：同一列族的数据存储在一起
  - 支持数据版本管理（默认保留3个版本）
  - 支持 Compaction 合并小文件

**Compaction 机制**：
| 类型 | 触发条件 | 合并范围 |
|------|----------|----------|
| **Minor Compaction** | HFile 数量达到阈值（默认3个） | 合并部分小 HFile，不删除过期版本 |
| **Major Compaction** | 自动触发（默认7天）/手动 | 合并所有 HFile，删除过期版本和删除标记 |

**LSM-Tree 架构优势**：
- 写操作：先写内存，后顺序写磁盘（高吞吐）
- 读操作：合并内存和磁盘数据（布隆过滤器加速）

📖 来源：https://hbase.apache.org/book.html#perf.storage

---分割线---

### 题目4：HBase 的读写流程是怎样的？如何保证数据一致性？

【答案】

**写流程（Put）**：
```
1. Client 请求 ZK，获取 Meta 表所在的 RegionServer
2. Client 根据 RowKey 访问 Meta 表，确定数据所属的 RegionServer
3. Client 将数据写入目标 RegionServer：
   a. 先写入 WAL（Write-Ahead Log），保证宕机可恢复
   b. 再写入 MemStore（按序排列）
4. MemStore 达到阈值后，flush 成 HFile
5. HFile 异步执行 Compaction 合并
```

**读流程（Get/Scan）**：
```
1. Client 请求 ZK，获取 Meta 表位置
2. 定位目标 RegionServer
3. 在该 RegionServer 上执行读操作：
   a. 先查 MemStore（内存，最新数据）
   b. 再查 BlockCache（读缓存，热点数据）
   c. 最后查 HFile（磁盘），使用布隆过滤器定位
4. 合并所有数据版本，返回最新值
```

**数据一致性保证**：

| 机制 | 说明 |
|------|------|
| **WAL（预写日志）** | 数据先写 WAL 再写 MemStore，宕机后从 WAL 恢复 |
| **MemStore 原子性** | 单行数据写入 MemStore 是原子操作 |
| **MVCC（多版本并发控制）** | 读写不互斥，通过版本号实现一致性快照读 |
| **HDFS 副本** | 数据自动复制3份，DataNode 挂了不影响读取 |
| **Region 恢复** | Region 迁移时先执行 flush，切换过程对用户透明 |

**强一致性 vs 最终一致性**：
- HBase 是**强一致性**数据库
- 写操作必须等所有副本写入成功才返回
- 读取时能读到所有已提交的数据

**性能优化建议**：
- 批量写入：使用 `put(List<Put>)` 减少 RPC 次数
- 批量读取：使用 `get(List<Get>)` 或 `scan` 设置缓存
- 关闭 WAL：适合高吞吐、允许少量数据丢失的场景（`put.setWriteToWAL(false)`）

📖 来源：https://hbase.apache.org/book.html#arch overview

---分割线---

### 题目5：HBase 协处理器是什么？Observer 和 Endpoint 有什么区别？

【答案】

**协处理器（Coprocessor）**：HBase 提供的扩展机制，允许用户在 RegionServer 上运行自定义代码，实现服务端计算。

**两种协处理器类型**：

| 类型 | 作用 | 使用场景 |
|------|------|----------|
| **Observer** | 钩子函数，拦截读写操作，在特定事件前后执行 | 数据验证、审计日志、二级索引、访问控制 |
| **Endpoint** | 类似存储过程，动态自定义 RPC 协议，在服务端执行计算 | 聚合计算、复杂计算、返回计算结果 |

**Observer 协处理器**：

继承 `BaseRegionObserver`，覆盖钩子方法：

```java
public class MyObserver extends BaseRegionObserver {
    @Override
    public void prePut(ObserverContext<RegionCoprocessorEnvironment> e,
                       Put put, WALEdit edit, Durability durability) throws IOException {
        // 在 put 之前执行：数据校验、过滤、修改数据
        if (isValid(put)) {
            // 合法数据，继续写入
        } else {
            // 非法数据，拒绝写入
            throw new DoNotRetryIOException("Invalid data!");
        }
    }
    
    @Override
    public void postPut(ObserverContext<RegionCoprocessorEnvironment> e,
                        Put put, WALEdit edit, Durability durability) {
        // 在 put 之后执行：异步构建二级索引、发送通知
        buildSecondaryIndex(put);
    }
}
```

**常用钩子方法**：
- `preGet/postGet`：读取操作前后
- `prePut/postPut`：写入操作前后
- `preDelete/postDelete`：删除操作前后
- `preOpen/postOpen`：Region 打开时
- `preCompact/postCompact`：Compaction 前后

**Endpoint 协处理器**：

继承 `BaseEndpointCoprocessor`，定义计算协议：

```java
public class SumEndpoint extends BaseEndpointCoprocessor {
    // 定义协处理器协议
    public interface SumService extends CoprocessorService {
        void sumColumn(RpcController controller, SumRequest request,
                       RpcCallback<SumResponse> done);
    }
    
    @Override
    public void sumColumn(RpcController controller, SumRequest request,
                          RpcCallback<SumResponse> done) {
        // 在 RegionServer 端执行聚合计算
        SumResponse response = SumResponse.newBuilder()
            .setSum(calculateSum(request.getColumn())).build();
        done.run(response);
    }
}

// 客户端调用
String column = "info:age";
SumResponse response = table.coprocessorService(
    SumService.class, startKey, endKey,
    new Batch.Call<SumService, SumResponse>() {
        public SumResponse call(SumService service) throws IOException {
            return service.sumColumn(null, SumRequest.newBuilder()
                .setColumn(column).build());
        }
    });
```

**使用方式**：
```java
// 动态加载（无需重启 HBase）
HTableDescriptor desc = new HTableDescriptor(tableName);
desc.addCoprocessor("com.example.MyObserver",
    org.apache.hadoop.hbase.util.Bytes.toBytes("/hdfs/path/to/coprocessor.jar"),
    100, null);

// 或通过 HBase Shell
disable 'myTable'
alter 'myTable', METHOD => 'table_att', 'coprocessor' => 'hdfs:///path/to/jar|com.example.MyObserver|100|arg1=val1'
enable 'myTable'
```

**典型应用场景**：
- **二级索引**：Phoenix 自动维护，使用 Observer 同步索引表
- **数据验证**：写入前检查数据合法性
- **聚合计算**：服务端聚合减少网络传输（Sum、Count、Avg）
- **审计日志**：记录所有数据变更操作

📖 来源：https://hbase.apache.org/book.html#cp

---分割线---

*本文件由小憨宝自动生成 | 每日程序员面试题推送 v3*
