# Spark 面试题精选 — 2026-08-29

> 每日5道高质量Spark面试题，涵盖核心知识点。

---

## 1. Spark 的整体架构是怎样的？Driver 和 Executor 分别承担什么职责？

**参考来源：** [Apache Spark Official Documentation - Cluster Mode Overview](https://spark.apache.org/docs/latest/cluster-overview.html)

**答案：**

Spark 采用 **主从架构（Master-Slave）**，核心组件包括：

- **Driver Program（驱动程序）：**
  - 运行用户的 `main()` 函数，创建 `SparkContext`（或 `SparkSession`）
  - 负责将用户程序转化为 **Task（任务）**，并调度到 Executor 上执行
  - 维护 DAG（有向无环图），通过 DAGScheduler 划分 Stage，通过 TaskScheduler 分发 Task
  - 收集 Executor 的执行结果

- **Executor（执行器）：**
  - 运行在 Worker 节点上的 JVM 进程
  - 负责执行具体的 Task，并将结果返回给 Driver
  - 每个 Executor 拥有独立的 **Cache** 和 **内存空间**，用于缓存 RDD 数据
  - 一个集群可以有多个 Executor，每个 Executor 可以并行执行多个 Task（由 `spark.executor.cores` / `spark.executor.instances` 控制）

- **Cluster Manager（集群管理器）：**
  - 负责资源的分配和管理，支持 Standalone、YARN、Mesos、Kubernetes 等部署模式

整体流程：Driver 提交应用 → Cluster Manager 分配资源 → Executor 启动 → Driver 调度 Task 到 Executor 执行 → 结果返回。

---

## 2. RDD、DataFrame 和 DataSet 三者有什么区别？各自适用什么场景？

**参考来源：** [Apache Spark RDD Programming Guide](https://spark.apache.org/docs/latest/rdd-programming-guide.html) | [Spark SQL Guide - Datasets and DataFrames](https://spark.apache.org/docs/latest/sql-programming-guide.html)

**答案：**

| 特性 | RDD | DataFrame | DataSet |
|------|-----|-----------|---------|
| **类型安全** | 编译时类型安全 | 无类型（运行时检查） | 编译时类型安全 |
| **API 风格** | 函数式（map/filter/reduce） | 声明式（SQL-like） | 两者兼有 |
| **序列化** | Java/Kryo 序列化 | Tungsten 二进制格式 | Tungsten + Encoders |
| **优化** | 无 Catalyst 优化器 | 有 Catalyst + Tungsten | 有 Catalyst + Tungsten |
| **语言支持** | Java/Scala/Python/R | Java/Scala/Python/R | 仅 Java/Scala |
| **Schema** | 无 Schema | 有 Schema | 有 Schema + 类型 |

**适用场景：**
- **RDD：** 需要细粒度控制底层逻辑时使用，如自定义分区器、非结构化数据处理
- **DataFrame：** 结构化数据分析，SQL 熟悉的开发者首选，性能最优
- **DataSet：** 需要类型安全 + Catalyst 优化的场景，Scala/Java 开发者的首选

> **注意：** 在 Spark 2.x+ 中，`DataFrame` 实际上是 `Dataset[Row]` 的别名。在 Python/R 中只有 DataFrame 概念。

---

## 3. Spark 的 Shuffle 机制是什么？为什么 Shuffle 是性能瓶颈？如何优化？

**参考来源：** [Apache Spark Shuffle Operations](https://spark.apache.org/docs/latest/rdd-programming-guide.html#shuffle-operations) | [Jira SPARK-3099 - Sort-Based Shuffle](https://issues.apache.org/jira/browse/SPARK-3099)

**答案：**

**Shuffle 是什么：**
Shuffle 是 Spark 在执行宽依赖算子（如 `groupByKey`、`reduceByKey`、`join`、`repartition`）时，需要将数据重新分布到不同分区的过程。这涉及跨节点的数据传输。

**为什么是性能瓶颈：**
1. **磁盘 I/O：** Shuffle 过程中数据需要写入磁盘再读取
2. **网络 I/O：** 数据需要在节点之间通过网络传输
3. **序列化/反序列化：** 数据传输前后的编解码开销
4. **内存压力：** 大量中间数据需要缓存，可能导致 OOM

**Spark 的 Shuffle 实现演进：**
- **Hash Shuffle（Spark 1.1）：** 每个 Map Task 为每个 Reduce Task 创建一个文件，文件数 = M × R，产生大量小文件
- **Sort-Based Shuffle（Spark 1.2+，默认）：** 每个 Map Task 只生成一个数据文件 + 索引文件，按 Partition ID 排序后写入，大幅减少文件数量

**优化策略：**
1. **减少 Shuffle 数据量：** 使用 `reduceByKey` 替代 `groupByKey`（先在 Map 端预聚合）
2. **合理设置分区数：** `spark.sql.shuffle.partitions`（默认 200），数据量大时适当增大
3. **使用广播变量替代 Shuffle Join：** 小表广播，避免大表 Shuffle
4. **开启 Shuffle 数据压缩：** `spark.shuffle.compress=true`（默认开启）
5. **调整内存配置：** 增大 `spark.memory.fraction`，优化统一内存管理
6. **避免数据倾斜：** 对倾斜 Key 加盐（Salting）或使用 AQE（Adaptive Query Execution）

---

## 4. Spark 的内存管理模型是怎样的？统一内存管理（Unified Memory Management）如何工作？

**参考来源：** [Apache Spark Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management) | [Jira SPARK-10000 - Unified Memory Management](https://issues.apache.org/jira/browse/SPARK-10000)

**答案：**

**Spark 内存分为三大区域：**

```
Executor Memory
├── Reserved Memory（保留内存）: 300MB（固定）
├── User Memory（用户内存）: (1 - spark.memory.fraction) × (Heap - 300MB)
└── Spark Memory（Spark 管理内存）: spark.memory.fraction × (Heap - 300MB)
    ├── Storage Memory（存储内存）: 缓存 RDD / Broadcast / Unroll
    └── Execution Memory（执行内存）: Shuffle / Join / Sort / Aggregation
```

**统一内存管理（Spark 1.6+）：**

核心思想：**Storage 和 Execution 共享同一块内存池，可以互相借用。**

- **默认比例：** `spark.memory.storageFraction=0.5`（Storage 和 Execution 各占一半）
- **Execution 优先：** 当 Execution 内存不足时，可以驱逐 Storage 占用的内存（但不能驱逐超过 Storage 的基础份额）
- **Storage 不能驱逐 Execution：** Storage 内存不足时只能等待，不能抢占 Execution 的内存
- **空闲时互相借用：** 未使用的 Storage 内存可以被 Execution 使用，反之亦然

**关键配置参数：**
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `spark.memory.fraction` | 0.6 | Spark 管理内存占总堆内存的比例 |
| `spark.memory.storageFraction` | 0.5 | Storage 在 Spark 内存中的基础占比 |
| `spark.memory.offHeap.enabled` | false | 是否启用堆外内存 |
| `spark.memory.offHeap.size` | 0 | 堆外内存大小 |

**优化建议：**
- 缓存密集型任务：适当增大 `spark.memory.storageFraction`
- Shuffle 密集型任务：保持默认或减小 Storage 占比
- 大数据量场景：开启堆外内存避免 GC 停顿

---

## 5. Spark 的广播变量（Broadcast Variable）是什么？工作原理和使用场景是什么？

**参考来源：** [Apache Spark Broadcasting Guide](https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables)

**答案：**

**广播变量是什么：**
广播变量是 Spark 提供的一种**只读共享变量**，它可以将一个只读变量缓存在每个 Executor 的内存中（而非每个 Task 一份副本），所有 Task 共享同一份数据。

**工作原理：**
1. **Driver 创建：** 调用 `sc.broadcast(value)` 创建广播变量
2. **高效分发：** 使用类似 BitTorrent 的 P2P 协议分发到各 Executor
3. **Executor 缓存：** 每个 Executor 只保存一份副本，该 Executor 上的所有 Task 共享
4. **只读性：** 广播变量分发后不可修改，保证一致性

**为什么使用广播变量：**
- **不使用广播：** 每个 Task 持有一份变量副本 → N 个 Task = N 份副本 → 内存浪费 + 序列化开销大
- **使用广播：** 每个 Executor 只有 1 份副本 → 大幅减少内存和网络开销

**典型使用场景：**
1. **广播 Join（Map-Side Join）：** 将小表广播到所有 Executor，避免 Shuffle Join
   ```scala
   val smallDF = spark.table("small_table")
   val largeDF = spark.table("large_table")
   // Spark 自动优化：当小表小于 spark.sql.autoBroadcastJoinThreshold（默认 10MB）时自动广播
   largeDF.join(smallDF, "key")
   // 或手动广播
   val broadcastSmall = spark.sparkContext.broadcast(smallMap)
   largeDF.rdd.map(row => lookup(broadcastSmall.value, row))
   ```
2. **共享查找表/字典：** 如 IP 地址库、配置映射表等
3. **共享大对象：** 如预训练模型、规则引擎等

**注意事项：**
- 广播变量适合**只读、中等大小**的数据（几百 MB 以内）
- 过大的广播变量会占用 Executor 内存，影响执行性能
- 可以调用 `unpersist()` 释放 Executor 上的缓存
- Spark 2.0+ 的 DataFrame API 中，`spark.sql.autoBroadcastJoinThreshold` 会自动触发广播 Join

---

*本文档由 Spark 面试题助手自动生成，内容基于 Apache Spark 官方文档及业界最佳实践。*
