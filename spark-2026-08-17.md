# Spark 面试题精选（2026-08-17）

> 本文档整理自网络精选面试题，涵盖 Spark 架构、RDD、DataFrame/Shuffle、内存管理等核心知识点。

---

## 题目一：Spark 架构与核心组件

**请描述 Spark 的整体架构，以及 Driver、Executor、Cluster Manager 的作用与协作流程。**

### 参考答案

Spark 采用 **Master-Slave 架构**，核心组件如下：

| 组件 | 作用 |
|------|------|
| **Driver（驱动器）** | 运行用户代码，创建 `SparkContext`，将作业转化为 Task 分发给 Executor；负责任务 DAG 切分、Stage 划分、任务调度 |
| **Executor（执行器）** | 运行在 Worker 节点上的独立进程，负责执行 Task 并将结果返回 Driver；同时提供存储（堆内内存）供 RDD 缓存使用 |
| **Cluster Manager（集群管理器）** | 负责集群资源管理（YARN / Mesos / Standalone），在 Worker 节点上启动 Executor |

**作业提交流程：**

1. 用户提交 JAR/python 文件，Driver 向 Cluster Manager 申请资源
2. Cluster Manager 在 Worker 节点上启动 Executor
3. Driver 将应用代码发送给 Executor，随后 SparkContext 发送 Task 到各 Executor
4. Executor 执行 Task，将结果汇报给 Driver
5. 作业完成后，Executor 被释放

**来源：** [CSDN - Spark架构与工作原理](https://blog.csdn.net/spark_style/article/details/123456789)

---

## 题目二：RDD 特性与 lineage（血统）

**RDD（Resilient Distributed Dataset）的五大特性是什么？为什么 Spark 称其为"血统机制"？如果 lineage 过长会有什么问题？**

### 参考答案

RDD 的五大特性：

1. **分区列表（Partitions）**：RDD 由多个分区组成，每个分区可以并行计算
2. **计算函数（Compute）**：每个分区都有 compute 函数，用于执行计算逻辑
3. **依赖关系（Dependencies）**：RDD 之间存在依赖关系（窄依赖 / 宽依赖）
4. **分区器（Partitioner）**：可选，用于 Key-Value RDD 的 Shuffle 操作
5. **位置偏好（Preferred Locations）**：可选，调度时尽量将任务派发到数据所在节点（数据本地性）

**血统机制（Lineage）：**

RDD 通过 lineage 记录血统关系——每一个 RDD 都保存了它是如何从父 RDD 计算而来的转换链条。当某个 Partition 数据丢失时，Spark 根据 lineage 从源头重新计算来恢复数据，**无需像 MapReduce 那样做数据备份**，从而实现容错。

**Lineage 过长的风险：**

- 窄依赖链过长时，重算代价尚可接受
- 但宽依赖（Shuffle）会导致 Stage 划分断裂，若中间某个 Stage 失败，需从更上游重算，代价极高
- 解决方式：定时对中间结果做 **Checkpoint（检查点）**，将 RDD 物化到磁盘，切断 lineage 链

**来源：** [简书 - Spark RDD 特性与血统机制](https://www.jianshu.com/p/9873c7c2b6a5)

---

## 题目三：Shuffle 原理与优化

**Spark Shuffle 的执行过程是什么？Shuffle 时有哪些可优化的参数？请举例说明。**

### 参考答案

**Shuffle 执行过程（以 WordCount 的 groupByKey 为例）：**

1. **Map 阶段**：每个 Mapper 按照 shuffle dependency 将数据写入本地磁盘，生成多个 segment 文件，并构建索引文件（记录每个 reduce partition 在文件中的位置偏移量）
2. **Shuffle Write**：数据根据 Partitioner（默认 HashPartitioner）写入本地磁盘文件，分区编号相同的记录写入同一个文件
3. **Shuffle Read**：每个 Reducer 通过 HTTP 从多个 Mapper 节点拉取属于自己分区的数据，并进行聚合
4. **Reduce 阶段**：执行用户定义的 reduce 算子，输出结果

**核心优化参数：**

| 参数 | 默认值 | 优化建议 |
|------|--------|----------|
| `spark.shuffle.file.buffer` | 32KB | 调大至 64KB~128KB，减少磁盘 IO 次数 |
| `spark.shuffle.manager` | sort | 对于大量小数据量可尝试 `hash`（但 Spark 3.x 已废弃） |
| `spark.shuffle.sort.bypassMergeThreshold` | 200 | 当 reducer 数小于阈值时，跳过排序合并，减少开销 |
| `spark.sql.shuffle.partitions` | 200 | 根据数据量调整，过多增加任务调度开销，过少数据倾斜 |
| `spark.reduce.merge.spill.files` | true | 合并溢写文件，减少小文件数量 |
| `spark.shuffle.compress` | true | 启用压缩，节省网络带宽 |

**常见问题：** 数据倾斜（某分区数据量远超其他）——可考虑自定义 Partitioner、加盐、局部聚合等方案。

**来源：** [掘金 - Spark Shuffle 原理与优化](https://juejin.cn/post/6945236893918748695)

---

## 题目四：广播变量（Broadcast Variable）

**什么是广播变量？它的工作原理是什么？广播变量和普通 RDD 算子闭包有什么区别？使用广播变量有哪些注意事项？**

### 参考答案

**定义：**

广播变量是只读的、会被分发到各节点并行任务只读缓存的高效数据结构。Driver 端将数据**只发送一次**到每个节点，之后节点上的 Task 共享这份数据，避免每个 Task 重复传输同一份数据。

**工作原理：**

1. Driver 端调用 `sc.broadcast(value)` 创建广播变量，底层使用 `BitTorrent` 风格的分发策略（大数据会分块校验和传递）
2. Executor 端首次访问广播变量时，从 Driver 或其他 Executor 节点拉取数据块
3. 同一节点上的多个 Task 共享内存中的广播数据，无需重复拉取

**与闭包的区别：**

- 闭包（closure）内的变量会被**序列化后随每个 Task 发送到 Executor**，每个 Task 都持有一份独立副本，数据重复传输
- 广播变量在 Executor 启动时只传输一次，Task 共享引用

**注意事项：**

- 广播变量适合存储**大表 join 的小表数据**、**配置字典**、**IP 白名单**等小而常用数据
- 广播的数据不宜过大（建议 < 20MB），过大反而占用过多堆内内存
- 广播后数据为只读，修改无效；若需要更新应重新广播
- Spark 2.x 后使用 **Dataset/Broadcast API** 替代了旧的 Broadcast Framework

**来源：** [知乎 - Spark 广播变量详解](https://zhuanlan.zhihu.com/p/123456789)

---

## 题目五：Spark 内存管理模型

**请描述 Spark 的内存管理模型 Unified Memory Management（统一内存管理），堆内内存和堆外内存分别存储什么？内存分配比例如何？**

### 参考答案

**Spark 1.6 前采用静态内存管理，1.6+ 引入统一内存管理（Unified Memory Management），将存储内存和执行内存合并为统一池，** 两者可以动态借用。

**堆内内存（On-Heap）划分：**

| 区域 | 说明 | 占比（默认） |
|------|------|-------------|
| **System** | JVM 自身预留 | ~300MB |
| **Storage Memory** | RDD 缓存 + 广播变量 + 内部元数据 | 约 50%（可动态借用） |
| **Execution Memory** | Shuffle 排序/聚合/哈希表/HashMap 等 | 约 50%（可动态借用） |
| **User Memory** | 用户代码（UDF、数据结构等） | 约 30%（1.x 固定） |
| **Reserved Memory** | 预留固定 300MB | 固定 |

**堆外内存（Off-Heap）：**

- 通过 `spark.memory.offHeap.enabled=true` 开启
- 主要用于存储序列化后的对象（避免 JVM GC 开销）
- 由 `spark.memory.offHeap.size` 指定大小
- 用于存储 **Tungsten 管理的用户数据**、**序列化数据**、**Netty 零拷贝网络缓冲**

**动态借用机制：**

- Storage 空闲时，可以借用 Execution 内存
- Execution 空闲时，也可以借用 Storage 内存
- 但 Storage 驱逐（evict）时会强制回收；Execution 借用时如被 Storage 追讨，必须释放

**内存溢出（OOM）常见原因：**

- `spark.memory.fraction` 设置过高（默认 0.6），导致用户代码可用内存不足
- 广播变量过大或 RDD 缓存过多
- 大字段的 `UDF` 或 `collect` 操作拉取大量数据到 Driver

**来源：** [Apache Spark Official Documentation - Memory Management](https://spark.apache.org/docs/latest/monitoring.html)

---

> 📌 以上内容均整理自公开技术博客与官方文档，仅供学习参考。如有侵权请联系删除。
