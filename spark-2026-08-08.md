# Spark 面试题精选 · 2026-08-08

> 本文档精选 5 道高质量 Spark 面试题，涵盖 Spark 架构、RDD、DataFrame、Shuffle、内存管理等核心知识点。

---

## 题目一：RDD 的五大特性是什么？与 DataFrame、DataSet 有何区别？

### 参考答案

**RDD（弹性分布式数据集）的五大特性：**

1. **分区列表（a list of partitions）**：RDD 是由多个分区组成的，每个分区是数据的一个子集，可并行计算。
2. **算子列表（a function for computing each partition）**：RDD 中封装了作用在每个分区上的计算函数（如 `map`、`filter`）。
3. **依赖关系（a list of dependencies）**：RDD 记录与其他 RDD 之间的依赖关系，分为窄依赖和宽依赖，用于 DAG 调度和容错。
4. **分区器（optional, a Partitioner）**：对于键值对 RDD，可指定分区器（HashPartitioner / RangePartitioner），决定数据如何分区。
5. **位置偏好（optional, preferred locations）**：记录每个分区最佳计算位置（偏好），用于任务调度时实现数据本地性优化。

**RDD vs DataFrame vs DataSet：**

| 维度 | RDD | DataFrame | DataSet |
|------|-----|-----------|---------|
| 数据结构 | 无结构，每行是 Java/Scala 对象 | 类表格，二维表结构（Row） | 强类型，类对象（Encoder） |
| 优化 | 无 Catalyst 优化 | Catalyst 优化 + Tungsten | 同 DataFrame + 强类型检查 |
| 编译时检查 | 无（运行时才发现错） | 弱（SQL 语法错误） | 强（编译期即报错） |
| 数据处理方式 | 底层 API，灵活但繁琐 | 高级 API，简洁 | 兼具灵活与类型安全 |
| 序列化 | Java/Kryo 手动 | 自动 Tungsten 二进制 | 自动 Encoder |
| Spark 1.6+ | ✅ | ✅ | ✅（DataFrame = DataSet[Row]） |

> **面试加分点**：提到 `DataFrame = DataSet[Row]`，DataFrame 是 DataSet 的特例；DataFrame 通过 Catalyst 优化器支持谓词下推、列裁剪等优化。

**来源：**
- [2024年最新Spark面试题精选（附答案） - CSDN](https://blog.csdn.net/2401_84615650/article/details/138842695)
- [Spark面试题 - Golden Blog](https://gujincheng.github.io/2021/11/18/Spark%E9%9D%A2%E8%AF%95%E9%A2%98)

---

## 题目二：Spark 为什么比 MapReduce 快？Shuffle 过程是怎样的？

### 参考答案

**Spark 比 MapReduce 快的根本原因：**

1. **DAG 计算模型 vs 迭代模型**：MapReduce 每一步都需要将中间结果写入磁盘（HDFS），而 Spark 基于 DAG，流水线式执行（pipelining），中间结果在内存中流转，无需频繁落盘。
2. **减少 Shuffle 次数**：一个复杂的 MR 作业可能有多个 Map 和 Reduce 阶段，每次 Shuffle 都伴随磁盘 I/O。Spark 的 DAG 调度可以合并多个计算阶段，减少 Shuffle 次数。
3. **基于内存计算**：Spark 优先将数据缓存在内存（`cache` / `persist`），迭代计算场景下性能提升显著。
4. **任务间数据共享**：通过广播变量、共享变量避免重复数据传输。
5. ** Tungsten 高效编码**：使用 Off-Heap 内存和二进制编码，减少序列化开销和 GC 压力。

**Spark Shuffle 过程（以 Sort Shuffle 为例）：**

```
Shuffle 过程分三个阶段：
  Map 端  → Shuffle 写 → 中间文件落盘
           → 网络传输（Fetch）
  Reduce 端 → Shuffle 读 → 聚合
```

1. **Map 端（ShuffleMapTask）**：
   - 数据写入内存数据结构（Map 或 Array，视算子而定）
   - 达到阈值后溢写磁盘（spill）
   - 最后对所有溢写文件和内存数据做归并排序
   - 写入同一个 data 文件 + 一个索引文件（标识下游各分区起止位置）

2. **Shuffle 文件持久化**：
   - 每个 ShuffleMapTask 产生一个 data 文件和一个 index 文件
   - Bypass 模式（Reducer 数 < `spark.shuffle.sort.bypassMergeThreshold`，默认 200）不排序，直接 hash 分区写临时文件再合并

3. **Reduce 端**：
   - ResultTask 根据 index 文件读取属于自己的分区数据（`BlockStoreShuffleFetcher`）
   - 数据先入内存，不够则溢写磁盘，然后执行聚合逻辑

**Hash Shuffle vs Sort Shuffle：**
- Hash Shuffle（1.6 之前）：每个 Mapper 为每个 Reducer 创建一个文件，小文件爆炸问题严重
- Sort Shuffle：每个 Mapper 只产生 2 个文件（data + index），大幅减少文件数

> **面试加分点**：提到 Spark 3.0 后已废弃 Hash Shuffle，统一使用 Sort Shuffle；Sort Shuffle 的 Bypass 机制适用于 Shuffle 时不需要排序且 Reducer 数量不多的场景。

**来源：**
- [年前突击：2022最全Spark面试体系 - 华为云社区](https://bbs.huaweicloud.com/blogs/326863)
- [Spark广播变量深度解析 - 腾讯云开发者社区](https://cloud.tencent.com/developer/article/2594947)

---

## 题目三：Spark 的内存管理机制是怎样的？如何避免 OOM？

### 参考答案

**Spark 内存分为两大区域：堆内内存（On-heap）和堆外内存（Off-heap）**

**堆内内存** 基于 JVM 管理，Executor 是一个 JVM 进程，堆空间由 Spark 统一划分逻辑区域。

**堆外内存**（1.6+ 引入）通过 JDK Unsafe API 直接操作操作系统内存，减少 GC 开销，适合大内存场景。默认关闭，需设置 `spark.memory.offHeap.enabled=true`。

**内存区域划分（统一内存管理 Unified Memory Manager，Spark 1.6+）：**

```
堆内内存（systemMaxMemory）
├── Reserved Memory：300MB（固定，系统预留）
└── Usable Memory
    ├── Unified Memory（Storage ↔ Execution 动态共享）
    │   ├── Storage Memory（RDD 缓存、广播变量）
    │   └── Execution Memory（Shuffle、Join、Sort 计算）
    └── Other Memory（用户数据结构、Spark 内部元数据）
```

**核心机制——动态内存占用：**
- Storage 和 Execution 内存相互借用：当一方内存不足，可借用另一方的空闲空间
- 但 Storage 无法从 Execution 抢回被占用的内存（`acquireStorageMemory` 不能抢占正在被 Execution 使用的内存）
- 当 Execution 内存不足时，可以将数据溢出（spill）到磁盘

**避免 OOM 的方法：**

1. **合理设置 `spark.executor.memory`**，避免内存分配过小
2. **调整 `spark.memory.fraction`（默认 0.6）和 `spark.memory.storageFraction`（默认 0.5）**，根据作业特性分配存储/执行内存比例
3. **使用广播变量** 替代大表 Join，减少 Shuffle 过程中的内存压力
4. **及时 `unpersist()` 不再使用的 RDD**，释放存储内存
5. **开启堆外内存**：`spark.memory.offHeap.enabled=true`，减少 GC 压力
6. **使用 Kryo 序列化**（`spark.serializer=org.apache.spark.serializer.KryoSerializer`），减少对象头开销，降低内存占用
7. **开启 Shuffle 溢写**：`spark.shuffle.service.enabled=true`（External Shuffle Service），减少 Executor 内内存压力
8. **降低数据倾斜**：对 Key 做加盐处理或扩大分区数，避免个别 Partition 数据量过大

> **面试加分点**：提到静态内存管理（Spark 1.6 之前）和统一内存管理的区别；提到 YARN 模式下 `memoryOverhead`（堆外内存）的计算方式：`max(executorMemory * 0.10, 384MB)`，以及 YARN 资源规整化（ceil 的影响）。

**来源：**
- [Spark面试题——Spark的内存管理机制 - 牛客网](https://m.nowcoder.com/discuss/393188234556092416)
- [Spark相关面试题 - 博客园](https://www.cnblogs.com/yeyuzhuanjia/p/18790203)

---

## 题目四：Spark 广播变量的原理是什么？使用广播变量有哪些常见陷阱？

### 参考答案

**广播变量的作用：**
将一个只读的大数据对象高效分发到集群每个节点，每个节点只存储一份副本，所有 Task 共享访问，避免每个 Task 重复传输同一份数据，从而大幅减少网络 I/O 和内存占用。

**底层实现——TorrentBroadcast 机制：**

1. **序列化 + 分片**：Driver 将广播数据序列化为字节数组后，切分为固定大小的块（默认 4MB / `spark.broadcast.blockSize`），每块存储到 BlockManager 中，分配唯一 `BroadcastBlockId`
2. **P2P 传输**：Executor 从 Driver 获取部分数据块后，也成为数据提供者（Seeder），其他 Executor 可直接从邻居节点拉取缺失块——类似 BitTorrent 的去中心化下载
3. **懒加载**：数据块仅在任务实际访问时才下载，已下载的块缓存在 Executor 内存/磁盘中，供后续 Task 重用
4. **容错**：某个节点数据块丢失，可从其他节点重新拉取，无需 Driver 重发

**广播变量的使用：**

```scala
// 创建
val broadcastVar = sc.broadcast(Map("key1" -> "value1", "key2" -> "value2"))

// Task 中访问
rdd.map { record =>
  val lookupValue = broadcastVar.value.get(record.key)  // 通过 .value 获取
}
```

**常见陷阱：**

1. **数据更新滞后**：广播变量创建后不可变，若源数据变化需重新广播并触发相关 RDD 重建
2. **序列化异常**：广播的数据必须可序列化（Java / Kryo），包含非序列化对象（如 lambda 捕获的外部对象）会抛异常
3. **内存泄漏**：广播变量在应用程序结束或手动 `unpersist()` 前会一直占用内存，需及时释放
4. **大小评估失误**：过大的广播变量可能导致 Executor OOM，建议先用 `collect()` 检查数据规模；生产环境建议不超过 1GB
5. **广播阈值未配置**：`spark.sql.autoBroadcastJoinThreshold`（默认 10MB）控制自动广播小表的阈值，超大表不会自动广播，需手动 `broadcast()`
6. **Driver 端 OOM**：广播数据首先在 Driver 端收集并切分，Driver 内存不足会导致广播创建失败

> **面试加分点**：提到广播变量适用于 Map Side Join 场景（将小表广播替代 Shuffle Join）；提到 PySpark 中同样支持广播变量，但通过 Py4J 桥接 JVM 层实现，性能略低于 Scala/Java 原生实现。

**来源：**
- [Spark广播变量深度解析：高效分发大只读变量的原理与实战 - 腾讯云](https://cloud.tencent.com/developer/article/2594947)
- [Spark相关面试题 - 博客园](https://www.cnblogs.com/yeyuzhuanjia/p/18790203)

---

## 题目五：Spark 任务调优有哪些手段？如何解决数据倾斜问题？

### 参考答案

**Spark 开发调优手段：**

1. **避免创建重复 RDD**：同一份数据不要多次 `textFile`，复用已有 RDD
2. **复用 RDD**：对多次使用的 RDD 执行 `cache()` 或 `persist(StorageLevel.MEMORY_AND_DISK)`
3. **尽量避免 Shuffle 类算子**：`groupByKey`、`join`（大表 join 大表）等会触发 Shuffle，优先用 map-side 预聚合
4. **使用 map-side 预聚合的算子**：`reduceByKey` / `aggregateByKey` 优于 `groupByKey`（在 map 端先做一次 combine，减少网络传输）
5. **使用高性能算子**：
   - `mapPartitions` 替代 `map`（按分区迭代，减少函数调用次数）
   - `foreachPartitions` 替代 `foreach`
   - `filter` 后配合 `coalesce` 减少分区数
6. **合理设置并行度**：`spark.sql.shuffle.partitions`（默认 200）根据数据量调整，避免过多小任务或过少大任务
7. **广播大变量**：小表广播到各节点，减少 Shuffle 数据量
8. **使用 Kryo 序列化**：减少序列化数据体积和 CPU 开销
9. **开启数据本地性等待**：`spark.locality.wait`（默认 3s），让任务等待数据所在节点空闲，避免强制跳转
10. **JVM 堆外内存配置**：`spark.executor.memoryOverhead`（默认 `max(executorMemory * 0.10, 384MB)`）

**数据倾斜的解决方案：**

数据倾斜 = 某些 Partition 数据量远大于其他 Partition，导致少数 Task 执行时间过长。

1. **定位倾斜 Key**：通过 Spark UI 或日志查看 Task 时长，定位是哪个 Key 数据量过大
2. **过滤倾斜 Key**：如果倾斜 Key 不影响业务结果，可直接 `filter` 掉
3. **加盐（Salting）**：给倾斜 Key 加上随机前缀，将一个 Key 分散到多个 Partition，分别处理后再去前缀合并
   ```scala
   // 给倾斜 Key 加随机后缀
   val keyedRDD = rdd.map(x => (x._1 + "_" + Random.nextInt(n), x._2))
                    .reduceByKey(_ + _)
                    .map(x => (x._1.split("_")(0), x._2))
   ```
4. **增大 shuffle 并行度**：将 `spark.sql.shuffle.partitions` 调大，让倾斜 Key 的数据分散到更多 Partition（治标不治本，但能缓解）
5. **两阶段聚合（局部聚合 + 全局聚合）**：先给 Key 加随机前缀做局部聚合，再去掉前缀做全局聚合（适合使用 `reduceByKey` 的场景）
6. **广播 Join 替代 Shuffle Join**：小表广播，避免大表 Shuffle 重新分区
7. **自定义分区器**：使用自定义逻辑将倾斜 Key 分散到指定 Partition
8. **拆分大 Key 单独处理**：将倾斜 Key 单独识别出来单独处理，处理完后合并回结果

> **面试加分点**：提到数据倾斜是面试高频问题，结合具体业务场景（如双十一订单数据、用户 ID 分布不均等）描述思路会加分；提到 Spark 3.0 的 AQE（Adaptive Query Execution）可以动态调整 Shuffle 分区数，在运行时自动缓解数据倾斜。

**来源：**
- [Spark面试题 - Golden Blog](https://gujincheng.github.io/2021/11/18/Spark%E9%9D%A2%E8%AF%95%E9%A2%98)
- [绝密100个Spark面试题 - 博客园](https://www.cnblogs.com/crazymakercircle/p/17539112.html)

---

*本文件由自动化脚本生成，每日更新*
