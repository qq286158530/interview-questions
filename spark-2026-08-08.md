# Spark 核心面试题（2026-08-08）

> 每道题均涵盖 Spark 架构、RDD、DataFrame、Shuffle、内存管理等核心知识点，答案详细准确，供面试复习参考。

---

## 题目一：Spark 架构核心组件有哪些？请描述 Driver、Executor、Cluster Manager 的作用及其协作流程。

### 参考答案

Spark 采用 **主从架构**，核心组件如下：

| 组件 | 角色 | 说明 |
|------|------|------|
| **Driver** | 主进程 | 运行 `main()` 方法，创建 `SparkContext`，负责任务调度（将 Job 划分为 Stage 和 Task） |
| **Executor** | 从进程 | 驻留在 Worker 节点上，为每个 Application 启动，负责执行分发的 Task、缓存数据 |
| **Cluster Manager** | 资源管理器 | 独立于 Spark 的集群资源管理服务（Standalone / YARN / Mesos），负责分配 Worker 节点上的 CPU 和内存 |
| **Worker** | 节点代理 | 运行在集群节点上，监控本节点的资源（CPU/内存），启动 Executor |

**协作流程：**

1. 用户提交 Application，Driver 向 Cluster Manager 申请资源。
2. Cluster Manager 在 Worker 节点上启动 Executor（每个 Executor 是一个 JVM 进程）。
3. SparkContext 将应用代码（Jar/Python 文件）发送给 Executor，同时将 Job 转化为 DAG。
4. DAG Scheduler 将 DAG 划分为多个 Stage，Stage 转化为 TaskSet，由 Task Scheduler 分发到各 Executor 执行。
5. Executor 执行完 Task 后将结果返回给 Driver，Driver 汇总结果后向用户返回。

> **来源：** [Spark 官方文档 - Architecture](https://spark.apache.org/docs/latest/cluster-overview.html)

---

## 题目二：RDD 的血缘机制（Lineage）是什么？为什么说 RDD 是"天然容错"的？

### 参考答案

**血缘机制（Lineage）：**

RDD 在创建时通过"转换操作"（Transformation）记录其依赖关系，形成一条**有向无环图（DAG）**。每个 RDD 都知道自己是从哪个父 RDD 通过什么转换得来的。

```
RDD A → map → RDD B → filter → RDD C
```

**天然容错原理：**

- 传统 MR 的中间结果落盘到 HDFS，容错依赖副本机制，成本高。
- RDD 的容错策略是**重新计算（Recompute）**：当某个 Partition 数据丢失时，RDD 根据 Lineage 从父 RDD 重新计算该 Partition，无需复制数据。
- 如果 Lineage 过长（宽依赖层级过深），可以设置 **Checkpoint** 将中间 RDD 物化到磁盘，切断血缘链，防止无限重算。

**宽依赖 vs 窄依赖：**

- **窄依赖**（Narrow Dependency）：一个父 Partition 至多被子 Partition 中的一个使用——可并行计算，如 `map`、`filter`。
- **宽依赖**（Wide Dependency）：一个父 Partition 被多个子 Partition 使用——触发 Shuffle，是 Stage 划分的边界，如 `groupByKey`、`join`。

> **来源：** [Spark 官方文档 - RDD Programming Guide](https://spark.apache.org/docs/latest/rdd-programming-guide.html#rdd-operations)

---

## 题目三：Spark Shuffle 的工作原理是什么？Hash Shuffle 和 Sort Shuffle 有什么区别？

### 参考答案

**Shuffle 的本质：**

Shuffle 是 Spark 将数据按 Key 进行重新分区的过程，介于前一个 Stage 的写操作和下一个 Stage 的读操作之间。本质是**跨节点的数据传输与重新分区**，会触发磁盘 I/O 和网络 I/O。

**Hash Shuffle（已废除）：**

- 每个 Mapper 为每个 Reducer 在本地磁盘单独写一个文件（文件数 = M × R，M=Mapper 数，R=Reducer 数）。
- 文件数爆炸导致磁盘 I/O 成为瓶颈，且会产生大量小文件。
- **优化版 Hash Shuffle**（Spark 0.8+）引入 **Consolidation**：相同 Worker 上同一批 Mapper 共享输出文件（按 Reducer 分组），减少文件数。

**Sort Shuffle（Spark 1.2+ 默认）：**

- 每个 Mapper 将数据写入一个**排序过的临时文件**（按 Partition ID + Key 排序）。
- 数据写入内存缓冲区，满了就排序后 spill 到磁盘。
- Mapper 结束后，将所有 spill 文件归并排序，生成最终的 `index` 文件和 `data` 文件。
- **Bypass 模式**：当 Reducer 数量 ≤ `spark.shuffle.sort.bypassMergeThreshold`（默认 200）且不是聚合类 Shuffle 时，使用 bypass 模式，性能接近 Hash Shuffle 但更稳定。

**Tungsten Sort Shuffle（Spark 1.5+，基于 Tungsten 项目）：**

- 使用 `sun.misc.unsafe` 直接操作堆外内存，避免 JVM GC 开销。
- 使用自定义序列化器进行排序（而不是 Java 对象排序）。
- 在 Spark 2.0+ 已整合进 Sort Shuffle，不再单独存在。

> **来源：** [Spark Shuffle 机制深度解析](https://spark.apache.org/docs/latest/rdd-programming-guide.html#shuffle-operations)

---

## 题目四：Spark 的内存管理模型是怎样的？请说明 Unified Memory（统一内存管理）和 Static Memory（静态内存管理）的区别。

### 参考答案

Spark 1.6 前使用**静态内存管理**，之后引入**统一内存管理**（Unified Memory），后者是默认方案。

**统一内存管理（Unified Memory）：**

堆内存被划分为三大区域（比例可配置）：

| 区域 | 默认占比 | 用途 |
|------|----------|------|
| **Execution Memory** | 30% | Shuffle 排序、Hash Join 哈希表、缓存 RDD 序列化数据等计算相关 |
| **Storage Memory** | 30% | 缓存 RDD/Broadcast 变量、Unroll 解压缩等存储相关 |
| **User Memory** | 40% | 用户数据结构（如 Scala 对象）、内部元数据 |

**动态占用机制（核心区别）：**

- Execution 和 Storage 区域**可以互相借用**：当 Execution 不足时，可驱逐 Storage 中可释放的缓存块；反之亦然。
- Storage 只能借用 Execution 空闲部分，但 Execution 可强制抢占 Storage（通过 `spark.memory.fraction` 控制）。
- 这解决了静态管理下"计算内存不足但存储内存闲置"的浪费问题。

**存储层级：**

```
Memory → Disk（Level: MEMORY_ONLY / MEMORY_AND_DISK 等）
```

- `MEMORY_ONLY`：优先存内存，不足则重算（默认推荐）。
- `MEMORY_AND_DISK`：优先内存，溢出则 spill 到磁盘。
- `DISK_ONLY`：仅磁盘（适合大文件）。

**常见 OOM 原因：**
- 广播变量过大 → 占用 Storage Memory，导致缓存被驱逐
- 大量 `groupByKey` / `join` → Execution Memory 溢出
- 解决思路：增加 `spark.executor.memory`，调低 `spark.memory.fraction`，或改用 `reduceByKey`（有 combiner，减少 shuffle 数据量）

> **来源：** [Spark 官方文档 - Memory Management](https://spark.apache.org/docs/latest/tuning.html#memory-management-overview)

---

## 题目五：广播变量（Broadcast Variable）的原理是什么？它如何在 Spark 中减少数据传输量？

### 参考答案

**核心概念：**

广播变量是将**只读数据**高效分发到集群所有节点的一种机制。每个 Executor 只会在**首次访问时**将广播变量拉取到本地，之后被该 Executor 上的所有 Task 共享，**无需每个 Task 都持有一份副本**。

**工作原理：**

1. Driver 端调用 `sc.broadcast(value)` 创建广播变量。
2. Spark 将数据序列化为二进制块（默认使用 **BitTorrent** 风格的 P2P 分发策略，大广播变量分块传输）。
3. Executor 从 Driver 或其他 Executor 的 BlockManager 获取数据块，写入本地磁盘并缓存。
4. 各 Task 读取广播变量时，直接从本地 BlockManager 获取，无需跨网络重复拉取。

**减少数据传输量的原理：**

假设一个 100 MB 的 Lookup 表参与 1000 个 Task 的计算：

| 方式 | 数据传输量 | 说明 |
|------|-----------|------|
| 普通变量 | 100 MB × 1000 = **100 GB** | 每个 Task 单独传输一份 |
| 广播变量 | 100 MB × N（Executor 数） ≈ **1～5 GB** | 每个 Executor 只需一份 |

**使用注意事项：**

```scala
// Scala 示例
val broadcastVar = sc.broadcast(Array(1, 2, 3))
val result = rdd.map(x => x * broadcastVar.value(0))
```

- 仅适合**小而常用**的只读数据（配置表、字典等），大文件建议存 HDFS 后用 `sc.textFile`。
- 广播变量修改后**不会同步**，是只读的。
- Spark 自动对 `join` 时的小表进行广播优化（**Broadcast Hash Join**），也可手动 `broadcast()` 显式广播。

> **来源：** [Spark 官方文档 - Broadcast Variables](https://spark.apache.org/docs/latest/rdd-programming-guide.html#broadcast-variables)

---

## 📚 更多推荐

- [Spark 官方文档](https://spark.apache.org/docs/latest/)
- [尚硅谷 Spark 教程](https://www.bilibili.com/video/BV1qK4y1r7XJ)
- 《Spark 权威指南》— Holden Karau, Andy Konwinski
