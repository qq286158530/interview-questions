# Redis 面试题（2026-08-01）

> 🎯 本次整理 5 道 Redis 高频面试题，涵盖数据类型、持久化、集群、缓存等核心知识点。

---

## 题目 1：Redis 有哪些数据类型？它们的底层实现分别是什么？

### 参考答案

Redis 提供了 5 种基础数据类型，随着版本迭代还增加了多种扩展类型：

| 数据类型 |底层实现 | 应用场景 |
|----------|---------|----------|
| **String** | SDS（Simple Dynamic String，简单动态字符串） | 缓存、计数器、分布式锁、session 共享 |
| **List** | QuickList（压缩列表 + 双向链表） | 消息队列、排行榜、关注列表 |
| **Hash** | 压缩列表（ziplist）**或** 哈希表（hashtable） | 对象存储、购物车数据 |
| **Set** | 整数集合（intset）**或** 哈希表 | 去重、标签系统、好友关系 |
| **Sorted Set（ZSet）** | 压缩列表 **或** 跳表（skiplist）+ 哈希表 | 排行榜、有序集合查询 |

**补充说明：**

- 当元素数量少、每个元素体积小时，Redis 优先使用压缩列表以节省内存（Redis 7 统一使用 listpack 替代 ziplist）。
- ZSet 使用**跳表 + 哈希表**双重结构：哈希表存储 member→score 映射，跳表实现有序.score 排序，两者的 pointer 共享同一份数据，不存在冗余存储。
- Redis 5.0 引入了 **Stream**（基于 RadixTree 实现），用于消息队列场景，弥补了 List 作队列的不足。

---

## 题目 2：Redis 的持久化机制有哪些？它们各自的优缺点是什么？

### 参考答案

Redis 有两种主流持久化方式，通常可以结合使用：

### 1. RDB（Redis Database）—— 全量快照

**原理：** 在指定时间间隔内，将 Redis 在某一时刻的内存数据快照（dump.rdb）写入磁盘。

**触发方式：**
- 手动触发：`BGSAVE`（后台异步）或 `SAVE`（同步，会阻塞）
- 自动触发：配置 `save m n`，表示 m 秒内如果有 n 次写操作则触发

**优点：**
- 恢复速度快（直接加载 .rdb 文件）
- 文件紧凑（全量数据），适合备份、灾难恢复
- fork 子进程完成，不影响主进程性能

**缺点：**
- 两次快照之间如果发生故障，会丢失部分数据
- 大数据量时 fork 子进程会阻塞，且生成 RDB 文件重量级

---

### 2. AOF（Append Only File）—— 增量日志

**原理：** 将每一个写操作命令追加到 .aof 文件末尾。Redis 重启时通过重放 AOF 文件中的命令来恢复数据。

**刷盘策略（appendfsync）：**
| 策略 | 说明 | 安全性 | 性能 |
|------|------|--------|------|
| `always` | 每个写命令都同步刷盘 | 最安全 | 最低 |
| `everysec`（默认） | 每秒刷盘一次 | 最多丢 1 秒数据 | 较高 |
| `no` | 由操作系统决定何时刷盘 | 不可控 | 最高 |

**优点：**
- 数据安全性更高（especially with everysec/always）
- AOF 文件可读，易于分析

**缺点：**
- 文件体积通常比 RDB 大（增量 vs 全量）
- 恢复速度比 RDB 慢

---

### 3. 混合持久化（Redis 4.0+）

开启 `aof-use-rdb-preamble yes` 后，AOF 重写时会先以 RDB 格式写入全量数据，再追加增量 AOF 命令。兼顾恢复速度和安全性。

**建议生产环境：** RDB + AOF 混合使用，配合 `everysec` 刷盘策略。

---

## 题目 3：Redis 的过期 key 删除策略是什么？

### 参考答案

Redis 采用**惰性删除 + 定期删除**相结合的策略：

### 1. 惰性删除（Lazy Free）

- **原理：** 当客户端访问某个 key 时，Redis 检查它是否已过期——如果过期则删除，未过期则返回。
- **优点：** 对 CPU 友好，只在必须访问时才处理过期。
- **缺点：** 如果过期 key 长期不被访问，会一直占用内存（内存泄漏风险）。

### 2. 定期删除（Active Expire）

- **原理：** Redis 每隔一段时间（默认 100ms，可配置）随机抽取部分设置了过期时间的 key，检查并删除已过期的 key。
- **实现：** 在 `redis.c/activeExpireCycle()` 函数中，以 `ACTIVE_EXPIRE_CYCLE_SLOW_TIME_PERC`（默认 25ms）为时间上限，遍历各个数据库的过期字典（expires dict）进行扫描。
- **优点：** 主动清理内存，避免大量过期 key 堆积。
- **缺点：** 对 CPU 有一定压力。

### 3. 内存淘汰策略（当内存达到 maxmemory 时）

当 Redis 内存使用达到上限，Redis 会通过**淘汰策略**主动删除 key：

| 策略 | 说明 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 从已设置过期时间的 key 中淘汰最少使用的 |
| `allkeys-lru` | 从所有 key 中淘汰最少使用的 |
| `volatile-lfu` | 从已设置过期时间的 key 中淘汰使用频率最低的 |
| `allkeys-lfu` | 从所有 key 中淘汰使用频率最低的 |
| `volatile-random` | 随机淘汰已设置过期时间的 key |
| `allkeys-random` | 随机淘汰所有 key |
| `volatile-ttl` | 淘汰最接近过期时间的 key |

**实战建议：** 如无特殊需求，推荐使用 `allkeys-lru` 或 `volatile-lru`，并结合业务合理设置 `maxmemory`。

---

## 题目 4：Redis 主从复制原理是什么？如何处理数据不一致？

### 参考答案

### 主从复制流程

Redis 主从复制采用 **PSYNC** 协议（Redis 2.8+），支持全量同步和增量同步：

```
┌─────────┐                ┌─────────┐
│  Master │ ── PSYNC ────► │  Slave  │
└─────────┘                └─────────┘
```

**完整复制流程：**

1. **建立连接**：从节点保存主节点信息，建立 socket 连接。
2. **发送 PING**：主从节点互相 PING，检测网络和对方状态。
3. **身份验证**：如果配置了 `requirepass`，从节点需发送 `AUTH` 验证。
4. **发送端口**：主节点向从节点发送监听端口。
5. **全量同步（PSYNC ? -1）**：
   - 从节点发送 `PSYNC ? -1`
   - 主节点执行 `BGSAVE` 生成 RDB，同时将新命令写入 **replication buffer**
   - RDB 发给从节点，从节点加载
   - 主节点将 replication buffer 中的增量命令发给从节点（**完整重同步**）
6. **增量同步（PSYNC <runid> <offset>）**：
   - 从节点断线重连后，发送 `PSYNC <runid> <offset>`
   - 主节点判断 offset 仍在环形缓冲区中，则只发送增量命令（**部分重同步**）

### 部分重同步（Partial Resync）实现——repl-backlog

主节点维护一个固定大小的环形缓冲区（`repl-backlog-buffer`），默认 1MB。

- 主节点记录每个写命令的 offset
- 从节点记录已接收的 offset
- 断线重连时，如果从节点的 offset 仍在 backlog 范围内 → 增量同步；否则 → 全量同步

### 数据不一致的原因及解决方案

**原因：**
- 网络分区导致主从不一致
- 主节点故障切换时（无 Sentinel/RW Splitting）写入未同步到从节点
- 异步复制本身的特性——主节点确认写入后，从节点可能还未同步

**解决方案：**

1. **开启 AOF + everysec**：降低数据丢失风险
2. **使用 Sentinel 集群**：实现自动故障转移（详见题目 5）
3. **使用 Redis Cluster**：数据分片，副本自动迁移
4. **读一致性要求高的场景**：使用 `WAIT` 命令（`WAIT numslaves timeout`）确保指定数量的从节点确认后再返回
5. **业务层面**：对一致性敏感的数据写入主库，读取可根据场景分散到主/从库

---

## 题目 5：什么是缓存穿透、缓存击穿、缓存雪崩？如何解决？

### 参考答案

这是 Redis 缓存使用中最经典的三类问题，简称"缓存三穿"。

---

### 1. 缓存穿透

**定义：** 查询一个**根本不存在**的数据。由于缓存中找不到，请求直接打到数据库，数据库也没有记录。如果攻击者大量请求这种不存在的数据，会导致数据库压力过大。

**常见场景：**
- 恶意刷接口（如 ID=-1 或不存在的 ID）
- 数据库中被删除的数据，缓存也清掉了，但查询没更新

**解决方案：**

| 方案 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **布隆过滤器（BloomFilter）** | 将所有存在的 key 存入 BloomFilter，查询前先检查是否可能存在 | 内存占用极低，性能好 | 存在误判（假阳性），不能删除 |
| **缓存空值** | 对查询为空的 key 在缓存中存一个空值（`NULL` 或占位符） | 简单有效 | 浪费缓存空间，需设短 TTL |
| **参数合法性校验** | 接口层拦截非法参数（如负数 ID） | 最直接 | 不能覆盖所有场景 |

---

### 2. 缓存击穿

**定义：** 某个**热点 key 过期**的瞬间，大量并发请求同时涌入，全部打到数据库。

**特点：** key 存在，只是过期了，但由于是热点数据，并发量极大。

**解决方案：**

| 方案 | 原理 |
|------|------|
| **互斥锁（Mutex）** | 使用 Redis `SETNX` 加锁，只允许一个请求回源查 DB，其他请求等待锁释放后从缓存取 |
| **逻辑过期（不设 TTL）** | 不设置 TTL，而是在 value 中存入过期时间字段，读取时判断是否过期——如果过期则用互斥锁异步回源 |
| **热点数据永不过期** | 对热点 key 不设 TTL，靠主动更新来刷新缓存 |

**推荐方案：** 逻辑过期 + 互斥锁，是应对热点击穿的经典组合。

---

### 3. 缓存雪崩

**定义：** 大量的缓存 key 在**同一时间集中过期**，导致大量请求直接打到数据库。

**特点：** key 都存在，只是刚好一起过期。

**解决方案：**

| 方案 | 原理 |
|------|------|
| **过期时间随机化** | 设置 TTL 时加上一个随机值（如 `TTL = base + random(0, 3600)`），避免集中过期 |
| **Redis 高可用** | 使用 Sentinel 或 Cluster 避免 Redis 单点故障 |
| **服务降级/限流** | 当 DB 压力过大时，返回兜底数据或拒绝部分请求 |
| **预热** | 系统启动时主动加载热点数据到缓存 |
| **多级缓存** | 本地缓存（如 Caffeine/Guava） + Redis + DB，多层挡截 |

---

### 一句话总结

| 问题 | 原因 | 核心解决思路 |
|------|------|-------------|
| **穿透** | 查询不存在的数据 | 布隆过滤器 / 缓存空值 |
| **击穿** | 热点 key 过期瞬间大量并发 | 互斥锁 / 逻辑过期 |
| **雪崩** | 大量 key 同时过期 | 随机 TTL / 多级缓存 / 高可用 |

---

> 📚 以上题目由 AI 整理自 Redis 核心知识点。建议结合源码（Redis 7.x）和实际项目经验深入理解。
> 
> 觉得有用？请给仓库点个 ⭐！
