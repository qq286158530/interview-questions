# HBase 面试题精选（2026-08-23）

> 本文档整理了 5 道高质量 HBase 面试题，涵盖架构原理、RowKey 设计、存储结构、协处理器等核心知识点。题目参考了业界经典面试题库与官方文档，来源真实可靠。

---

## 题目一：HBase 的整体架构是怎样的？ZooKeeper 在其中扮演什么角色？

### 参考答案

HBase 采用典型的 **Master-Slave 架构**，核心组件如下：

| 组件 | 职责 |
|------|------|
| **HMaster** | 负责表的管理、Region 的分配与调度、负载均衡，以及 RegionServer 的失效检测。不直接参与数据读写。 |
| **RegionServer** | 负责数据的读写服务，每个 RegionServer 管辖多个 Region，是实际数据存储和查询的执行节点。 |
| **ZooKeeper** | 负责 HBase 集群的协调服务。存放 **-ROOT-** 和 **.META.** 表的元数据位置、RegionServer 的活跃状态、以及 Master 的选举信息。ZooKeeper 通过临时节点感知 RegionServer 的上下线。 |
| **Client** | 客户端通过 ZooKeeper 获取 .META. 表位置，再访问目标 RegionServer，数据读写不经过 Master。 |

**读写数据流程简述：**
1. Client 连接 ZooKeeper，获取 -ROOT- 表位置。
2. 从 -ROOT- 表查到 .META. 表中目标 Region 的位置。
3. 直接与目标 RegionServer 通信读写数据。
4. 只有在表结构变更（如 split、merge）时才需要 Master 介入。

**为什么 ZooKeeper 至关重要？**
- HBase 没有自己实现 Master 选举，而是依赖 ZooKeeper 的 Paxos 能力来实现 Master 的高可用切换。
- RegionServer 向 ZooKeeper 注册临时节点，Master 通过监听这些节点感知集群状态。

> 📎 来源参考：[HBase 官方文档 - Architecture](https://hbase.apache.org/book.html#arch.overview)，[腾讯云 - HBase 架构详解](https://cloud.tencent.com/developer/article/1191699)

---

## 题目二：HBase 中 RowKey 设计的核心原则有哪些？为什么要避免热点？

### 参考答案

RowKey 是 HBase 数据的**唯一主键**，设计好坏直接影响读写性能和集群稳定性。核心原则如下：

### 1. 长度原则
- RowKey 建议控制在 **16~100 字节**以内，越短越好。
- HBase 存储时按字典序排序，过长的 RowKey 会占用大量 MemStore/HFile 空间。

### 2. 唯一性原则
- RowKey 必须唯一标识一行数据，否则后写入的数据会覆盖先前的版本（基于 timestamp 区分版本）。

### 3. 散列（Hash）原则
- 避免使用单调递增或递减的 RowKey（如时间戳 `20260823001`），否则所有写入会集中到同一个 RegionServer，造成**热点（Hot Spotting）**。
- 常用方案：在 RowKey 前面加盐（Salting）前缀，或对 RowKey 取 Hash 打散。

### 4. 业务相关原则
- 将查询条件中**最常作为过滤条件**的字段放在 RowKey 前面（如用户 ID、订单 ID）。
- 利用 HBase 字典序扫描的特性，设计支持范围查询的 RowKey。

### 热点问题的危害
- 单个 RegionServer 接收大量读写请求，导致负载过高、GC 频繁。
- 其他 RegionServer 资源空闲，集群整体吞吐量下降。
- 严重时甚至引发 RegionServer OOM。

### 解决热点的方法
| 方法 | 原理 | 适用场景 |
|------|------|---------|
| 加盐（Salting） | RowKey 前面加随机字母前缀，分散到不同 Region | 写入分散，但无法范围查询 |
| Hash 打散 | 对 RowKey 或其部分字段取 Hash | 写入均匀，可结合业务前缀 |
| 反转（Reversal） | 将固定前缀反转（如手机号 138xxx → xxx831） | 手机号等有规律前缀的场景 |

> 📎 来源参考：[HBase 官方文档 - RowKey Design](https://hbase.apache.org/book.html#rowkey.design)，[阿里云 - HBase RowKey 设计指南](https://help.aliyun.com/document_detail/179412.html)

---

## 题目三：HBase 的 MemStore 和 HFile 分别是什么？它们之间的关系是什么？

### 参考答案

### MemStore（写缓存）
- 当数据写入 HBase 时，首先写入 **MemStore**（内存结构），而不是直接落盘。
- MemStore 会对 RowKey 进行**字典序排序**，并按列族（Column Family）组织 KeyValue。
- 写入 MemStore 的操作是**内存操作**，因此 HBase 写入速度极快（接近内存写入速度）。
- 每个 Column Family 对应一个 MemStore 实例。

### HFile（StoreFile，存储文件）
- 当 MemStore 达到阈值（默认 `hbase.hregion.memstore.flush.size = 128MB`）时，会触发 **flush** 操作，将内存数据顺序写入 HDFS，形成一个 HFile。
- HFile 是 HBase 数据在 HDFS 上的实际存储格式，基于 **Hadoop SequenceFile**，内部使用 **Bloom Filter + Root Index + Data Block** 结构。
- HFile 一旦写出，就不再修改（Immutable），后续的更新合并通过 compaction 机制完成。

### 关系与数据合并流程

```
写入流程：Client → MemStore（内存排序）→ 触发flush → HFile
读取流程：Client → MemStore（先查）→ HFile（未命中MemStore时）→ 合并返回
```

**为什么先写 MemStore 再 flush？**
- 避免频繁的小文件写入，减少 HDFS 的 RPC 开销。
- 通过内存排序后再写 HFile，保证数据有序，提升后续扫描效率。

### Compaction 机制
- Minor Compaction：挑选部分 HFile 合并成更大的 HFile（只合并相邻的小文件）。
- Major Compaction：将一个 Store 下所有 HFile 合并成一个，删除标记（Delete）和过期版本（TTL）在此阶段真正删除数据。

> 📎 来源参考：[HBase 官方文档 - Storage Schema](https://hbase.apache.org/book.html#arch.storage)，[Cloudera - HBase MemStore & HFile](https://www.cloudera.com/documentation/enterprise/6/6.3/topics/admin_hbase_compaction.html)

---

## 题目四：HBase 协处理器（Coprocessor）是什么？它有哪几种类型？各自的使用场景是什么？

### 参考答案

### 什么是协处理器？

协处理器是 HBase 提供的**扩展机制**，允许用户将自定义代码**部署到 RegionServer 节点上**，在数据写入/读取时自动触发执行，无需在客户端手动调用。这类似于数据库的触发器或存储过程。

### 两种类型

#### 1. Observer（观察者）协处理器
- **行为类似触发器**：在特定事件前后自动执行回调。
- 常用钩子：
  - `prePut` / `postPut`：在数据写入前/后执行
  - `preGet` / `postGet`：在数据读取前/后执行
  - `preDelete` / `postDelete`：在数据删除前/后执行
  - `preOpen` / `postOpen`：在 Region 打开时执行

- **使用场景**：
  - 数据写入时自动建立二级索引（如 Phoenix 的索引实现）
  - 记录操作审计日志
  - 在写入前进行数据验证或权限校验
  - 实现数据的自动聚合/统计

#### 2. Endpoint（端点）协处理器
- **行为类似存储过程**：将计算逻辑下沉到 RegionServer 执行，减少网络传输。
- 使用时由客户端调用 `CoprocessorService`，在每个 Region 上执行自定义 RPC 服务。
- **使用场景**：
  - 在服务端直接执行 `SUM`、`AVG`、`COUNT` 等聚合计算（避免全表扫描回传）
  - 复杂计算逻辑需要跨行跨列族的数据处理
  - 实现自定义的批量数据转换或导出逻辑

### 部署方式

| 方式 | 描述 |
|------|------|
| **加载到表** | 通过 `alter` 命令将协处理器 JAR 关联到指定表 |
| **加载到 Namespace** | 关联到整个命名空间，该空间下所有表共享 |
| **系统级加载** | 配置在 `hbase-site.xml` 中，全局生效（HBase 内部使用，如 `RegionServerObserver` 实现 split/merge 钩子） |

### 注意事项
- 协处理器运行在 RegionServer 上，**异常会影响该节点稳定性**，需做好异常捕获。
- Observer 协处理器不应执行耗时过长或资源密集型操作。
- Endpoint 协处理器需要注意结果的并行汇总逻辑设计。

> 📎 来源参考：[HBase 官方文档 - Coprocessors](https://hbase.apache.org/book.html#cp)，[Hortonworks - HBase Coprocessor Introduction](https://docs.cloudera.com/HDPDocuments/HDP3/HDP-3.1.0/hbase-overview/content/hbase_coprocessors.html)

---

## 题目五：描述一下 HBase 的数据写入（Put）完整流程，越详细越好。

### 参考答案

HBase 的写入流程高度精简高效，核心链路如下：

### 详细流程图

```
Client
  │
  ▼
① 定位 Region（通过 ZooKeeper → -ROOT- → .META. 获取目标 Region 地址）
  │
  ▼
② 写入 WAL（Write-Ahead Log，先写日志）
  │
  ▼
③ 写入 MemStore（按 RowKey 排序）
  │
  ▼
④ 确认写入成功（返回 Client）
```

### ① Region 定位（Metadata 查找）
- Client 先请求 ZooKeeper，获取 `-ROOT-` 表所在的 RegionServer。
- 查询 `-ROOT-` 表得到 `.META.` 表中对应 RowKey 范围的 Region 信息。
- Client 会**缓存**这些元数据，后续请求直接访问目标 RegionServer，避免重复查询。

### ② 写入 WAL（预写日志）
- 数据首先Append 写入 **HLog（WAL）**，这是 HBase 的容错机制。
- WAL 保存在 HDFS 上，即使 RegionServer 宕机，也能通过 WAL 恢复未 flush 到 HFile 的数据。
- 写入 WAL 失败则整个写入失败。

### ③ 写入 MemStore
- WAL 写成功后，数据进入 **MemStore**，按 RowKey 字典序排列。
- 此时已经可以返回 Client 写入成功（异步刷盘）。

### ④ 异步刷盘（Flush）
- MemStore 达到阈值（默认 128MB）后，触发 flush 线程，将数据顺序写出为 HFile。
- 如果集群写入压力很大，会堆积多个 MemStore，此时要注意 **Region 级别和全局 MemStore 大小** 的限制（`hbase.regionserver.global.memstore.size`，默认堆内存的 40%）。

### 如果 MemStore 满了怎么办？
- 当 RegionServer 的全局 MemStore 使用量超过阈值，RegionServer 会**阻塞写入**并强制 flush 最旧的 MemStore，直到低于安全线。
- 这是 HBase 生产环境中常见的写入阻塞（Write Stall）问题，RowKey 设计不当（热点）会加剧这一问题。

### 关键配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `hbase.hregion.memstore.flush.size` | 128MB | 单个 MemStore flush 阈值 |
| `hbase.regionserver.global.memstore.size` | 堆内存 40% | RegionServer 全局 MemStore 上限 |
| `hbase.hregion.memstore.block.multiplier` | 4 | 超过 `flush.size * multiplier` 时阻止写入 |

> 📎 来源参考：[HBase 官方文档 - Write Path](https://hbase.apache.org/book.html#path.client)，[深入理解 HBase 数据写入机制 - 美团技术团队](https://tech.meituan.com/2016/07/06/hbase-in-meituan.html)

---

## 📚 延伸学习资源

- [HBase Official Documentation](https://hbase.apache.org/book.html)
- [《HBase权威指南》- O'Reilly](https://www.oreilly.com/library/view/hbase-the-definitive/9781449314682/)
- [Cloudera HBase Guide](https://docs.cloudera.com/documentation/enterprise/latest/topics/hbase.html)

---

*整理 by HBase Interview Bot | 2026-08-23*
