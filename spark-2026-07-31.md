# Spark 面试题 - 2026-07-31

## 🔥 Spark 核心知识点

---

### 题目1：Spark 架构是怎样的？Driver、Executor、Master、Worker 的作用是什么？

【答案】

**Spark 架构核心组件**：

| 组件 | 作用 |
|------|------|
| **Driver（驱动器）** | 运行 `main()` 方法，创建 `SparkContext`，负责任务分发和结果汇总 |
| **Executor（执行器）** | 运行在 Worker 节点上，执行具体 Task，向 Driver 汇报结果 |
| **Master** | 集群管理器，负责资源分配和调度（Standalone 模式下） |
| **Worker** | 集群节点，运行 Executor 进程 |

**详细流程**：

```
用户提交 Spark 作业
        ↓
Driver 创建 SparkContext，向 Master 注册申请资源
        ↓
Master 让 Worker 启动 Executor
        ↓
SparkContext 将 Job 划分为多个 Stage，每个 Stage 包含多个 Task
        ↓
Executor 接收 Task，执行计算
        ↓
结果返回给 Driver，Driver 汇总后输出
```

**关键点**：
- **SparkContext** 是 Spark 应用的入口，负责与集群管理器通信
- **Task** 是 Spark 的最小执行单元，运行在 Executor 的线程池中
- **Stage** 是根据 Shuffle 划分的一组 Task 集合
- **DAGScheduler**：将 Job 划分为 Stage，构建 DAG
- **TaskScheduler**：将 Task 提交到 Executor 执行

📖 来源：https://spark.apache.org/docs/latest/architecture.html

---分割线---

### 题目2：RDD 是什么？有哪些特性？RDD 与 DataFrame/DataSet 的区别？

【答案】

**RDD（Resilient Distributed Dataset）**：弹性分布式数据集，是 Spark 最底层的抽象，不可变、分区、可并行计算。

**RDD 五大特性**：

1. **` partitions`**：一组分区列表，数据按分区存储在集群中
2. **` computeFn(partitions: RDD[T] ⇒ Iterator[T])`**：计算函数，用于计算每个分区
3. **` dependencies`**：依赖关系（窄依赖/宽依赖）
4. **` partitioner`**：可选的分区器（HashPartitioner、RangePartitioner）
5. **` preferredLocations`**：数据优先位置（数据本地性）

**RDD vs DataFrame vs DataSet**：

| 特性 | RDD | DataFrame | DataSet |
|------|-----|-----------|---------|
| **数据结构** | 无结构，每行类型独立 | 有 Schema，每列有类型 | 有类型，JVM 对象 |
| **优化** | 无 Catalyst 优化 | 有 Catalyst 优化 | 有 Catalyst 优化 |
| **API** | Lambda 函数 | SQL / 链式 API | 强类型 + Lambda |
| **编译时检查** | 无（运行时） | 弱（运行时） | 有（编译时） |
| **性能** | 较低 | 高 | 高 |
| **序列化** | Java/Kryo | Catalyst 自动 | Encoder |

**为什么 DataFrame/DataSet 更快**：
- **Catalyst 优化器**：谓词下推、列裁剪、常量折叠
- **Tungsten 堆外内存管理**：减少 GC 开销
- **生成 JVM ByteCode**：减少虚函数调用

📖 来源：https://spark.apache.org/docs/latest/rdd-programming-guide.html

---分割线---

### 题目3：Spark Shuffle 是什么？为什么 Shuffle 会影响性能？有哪些优化策略？

【答案】

**Shuffle（洗牌）**：将数据按 Key 进行重新分布，是 MapReduce 模型的标配操作。

**触发 Shuffle 的算子**：
- `groupByKey`、`reduceByKey`、`join`、`distinct`、`repartition`、`sortByKey` 等

**Spark Shuffle 流程**：

```
Map 阶段：
  每个 Mapper 将数据写入本地磁盘分区文件
        ↓
Shuffle Write：
  根据分区器（如 HashPartitioner）将数据写入不同的 bucket（文件）
        ↓
Shuffle Read：
  每个 Reducer 去抓取属于自己分区的数据（通过网络拉取）
        ↓
Reduce 阶段：
  对数据进行聚合、排序等操作
```

**为什么影响性能**：
1. **磁盘 I/O**：大量数据写入磁盘
2. **网络 I/O**：跨节点数据传输
3. **序列化/反序列化**：数据需要序列化后传输
4. **内存压力**：数据需要在内存中缓冲

**优化策略**：

| 策略 | 说明 |
|------|------|
| **减少 Shuffle 数据量** | 过滤无用数据、使用 map-side join |
| **合理设置分区数** | 分区数 ≈ cores × 2~4，避免过多或过少 |
| **`consolidateFiles=true`** | 合并 Shuffle 文件，减少文件数（Spark 2.x+） |
| **广播小表** | `broadcast` 将小表广播，避免 Shuffle |
| **使用 Filter 提前过滤** | 减少参与 Shuffle 的数据量 |
| **调整缓冲区** | `spark.shuffle.file.buffer`（默认 32KB） |
| **选择合适的分区器** | HashPartitioner / RangePartitioner |

📖 来源：https://spark.apache.org/docs/latest/tuning.html

---分割线---

### 题目4：Spark 内存管理是怎样的？堆内内存和堆外内存有什么区别？

【答案】

**Spark 内存分为两大区域**：

**1. 堆内内存（On-heap Memory）**：
- 大小由 `--executor-memory` 或 `spark.executor.memory` 设置
- 由 JVM 管理，有 GC 压力
- 用于：RDD 缓存、用户数据结构、Shuffle 排序缓冲区

**2. 堆外内存（Off-heap Memory）**：
- 通过 `spark.memory.offheap.enabled` 开启
- 通过 Java Unsafe API 直接操作堆外内存
- 用于：Tungsten 序列化数据，减少 GC 开销

**内存区域细分（Spark 2.x+ Unified Memory）**：

```
┌─────────────────────────────────────────────┐
│            Execution Memory                  │
│   (Shuffle 排序、HashJoin、缓存等)            │
├─────────────────────────────────────────────┤
│              Reserved Memory                │
│            (300MB，系统预留)                   │
├─────────────────────────────────────────────┤
│              User Memory                    │
│    (用户代码、Spark 内部元数据等)              │
├─────────────────────────────────────────────┤
│              Storage Memory                 │
│   (RDD 缓存、广播变量、Task 结果缓存)          │
└─────────────────────────────────────────────┘
```

**Execution 与 Storage 内存可以互相借用**（动态占用）：
- Execution 内存不足时可借用 Storage 空间（需 Storage 空闲）
- Storage 内存不足时可借用 Execution 空间（需 Execution 空闲）
- 通过 `spark.memory.fraction`（默认 0.6）控制占总内存比例

**关键参数**：
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `spark.executor.memory` | 1g | 堆内内存大小 |
| `spark.memory.fraction` | 0.6 | 堆内内存中 Spark 内存占比 |
| `spark.memory.storageFraction` | 0.5 | Spark 内存中 Storage 占比 |
| `spark.memory.offheap.enabled` | false | 是否使用堆外内存 |
| `spark.memory.offheap.size` | 0 | 堆外内存大小 |

📖 来源：https://spark.apache.org/docs/latest/tuning.html#memory-management

---分割线---

### 题目5：广播变量（Broadcast）是什么？为什么能提升性能？有哪些注意事项？

【答案】

**广播变量**：将一份只读数据缓存在每个 Executor 上，供该 Executor 上的所有 Task 共享，避免每个 Task 都持有一份副本。

**工作原理**：
```
未使用广播：
  Task1: 复制一份大表 → 占用 N 份内存
  Task2: 复制一份大表 → 占用 N 份内存
  Task3: 复制一份大表 → 占用 N 份内存
  （N = Task 总数）

使用广播：
  Executor 内存中只存 1 份大表，所有 Task 共享
  Task1, Task2, Task3 → 引用同一份数据
```

**使用方式**：
```scala
// 广播小表
val broadcastTable = spark.sparkContext.broadcast(largeTable.collectAsMap())

// 在算子中使用
val result = bigTable.map(row => {
  val lookup = broadcastTable.value.getOrElse(row.key, defaultValue)
  // 使用 lookup 做计算
})
```

**为什么能提升性能**：
1. **减少网络传输**：数据只在 Executor 间传输一次，而非每个 Task 传输一次
2. **减少内存占用**：所有 Task 共享一份数据，而非 N 份
3. **加速 join**：广播小表可避免 Shuffle（map-side join）

**适用场景**：
- 小表 / 维表（< 20MB，通常可广播）
- 静态配置数据
- 字典表、码表

**注意事项**：

| 注意点 | 说明 |
|--------|------|
| **大小控制** | 广播数据不宜过大（一般 < 20MB），过大会导致 Executor OOM |
| **版本一致** | 广播变量在 Task 开始时读取，广播后更新不会生效 |
| **避免在 Driver 端遍历** | 不要在 Driver 端使用 `broadcast.value` 遍历大集合 |
| **`spark.sql.autoBroadcastJoinThreshold`** | DataFrame/Dataset 自动广播阈值（默认 10MB） |
| **`broadcast()` 函数** | SparkSession 直接支持 `df.join(broadcast(smallDF))` |

📖 来源：https://spark.apache.org/docs/latest/tuning.html#broadcasting-large-variables
