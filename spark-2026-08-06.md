# Spark 高频面试题（2026-08-06）

> 本系列面试题来源于 GitHub 开源项目 [qq286158530/interview-questions](https://github.com/qq286158530/interview-questions)，持续更新中，欢迎 star ⭐

---

## 题目一：Spark 架构核心——Driver 与 Executor 的协作机制

**问题：** 描述 Spark Application 的核心架构组件，以及 Driver 和 Executor 是如何协作完成一个 Spark Job 的？

**答案：**

Spark Application 的核心架构包含以下组件：

| 组件 | 角色 | 说明 |
|------|------|------|
| **Driver** | 驱动器 | 负责创建 SparkContext、构建 DAG、划分 Stage、调度 Task 分发到 Executor |
| **Executor** | 执行器 | 驻留在 Worker Node 上，负责执行分配到的 Task，将结果返回给 Driver |
| **Cluster Manager** | 集群管理器 | 负责分配资源（YARN / Mesos / Standalone），启动 Driver 和 Executor 进程 |
| **Worker Node** | 工作节点 | 集群中实际运行 Executor 的机器 |

**完整协作流程：**

1. **提交阶段**：用户通过 `spark-submit` 提交 Application，Driver 在集群中启动（位置由 Cluster Manager 决定）。
2. **注册阶段**：Driver 创建 `SparkContext`，向 Cluster Manager 注册 Application 申请资源。
3. **启动阶段**：Cluster Manager 在 Worker Node 上启动 Executor 进程，Executor 向 Driver 注册。
4. **执行阶段**：
   - 用户代码被翻译为 RDD DAG（Directed Acyclic Graph）。
   - DAG Scheduler 将 DAG 划分为多个 Stage（以 Shuffle 为边界）。
   - Task Scheduler 将每个 Stage 内的 Task 打包，发送到各个 Executor。
5. **完成阶段**：Executor 执行 Task，计算结果回传给 Driver。全部完成后，Executor 进程被释放。

**核心点：** Driver 是整个 Application 的"大脑"，负责规划和调度；Executor 是"肌肉"，负责实际计算。一个 Application 只有一个 Driver，但可以有多个 Executor。

---

## 题目二：RDD 核心特性与五大特性详解

**问题：** RDD（Resilient Distributed Dataset）的五大特性是什么？这些特性如何保证 Spark 的容错性和高性能？

**答案：**

RDD 是 Spark 最核心的数据抽象，表示一个可分区的、只读的、弹性分布式数据集。RDD 具备以下五大特性：

### 1. A list of partitions（RDD 由多个分区组成）
RDD 的数据被逻辑切分为多个分区（Partition），每个分区是数据的一个子集。分区数量决定了并行度（parallelism）。一个 Partition 对应一个 Task 在 Executor 上执行。

```python
# 查看 RDD 分区数
rdd.getNumPartitions()  # 例如返回 8
```

### 2. A function for computing each split（每个分区都有计算函数）
每个分区都可以独立执行相同的计算函数（函数式编程）。Spark 将用户传入的函数（如 `map`、`filter`）序列化后发送到各个 Executor 节点，在本地分区数据上执行。

### 3. A list of dependencies on other RDDs（RDD 之间存在依赖关系）
RDD 与 RDD 之间存在依赖关系（Lineage），形成 DAG。依赖分为两类：
- **窄依赖（Narrow Dependency）**：父 RDD 的每个分区最多被一个子 RDD 分区使用（如 `map`、`filter`）。可以流水线执行（pipeline）。
- **宽依赖（Wide Dependency）**：父 RDD 的每个分区可能被多个子 RDD 分区使用（如 `groupByKey`、`join`）。触发 Shuffle，必须等父分区全部完成才能开始子分区计算。

### 4. Optionally, a Partitioner for key-value RDDs（可选：Key-Value RDD 的分区器）
对于键值对类型的 RDD（PairRDD），可以指定分区器（Partitioner），决定了 Shuffle 后数据的分布方式。Spark 内置两种分区器：
- **HashPartitioner**：基于 key 的 hash 值分配到不同分区。
- **RangePartitioner**：基于 key 的范围均匀分布，适合有序数据。

自定义分区器可以实现业务相关的分区策略。

### 5. Optionally, a list of preferred locations（可选：数据最优位置列表）
RDD 的每个分区会"偏好"某些存储位置（通常是对应 HDFS block 所在节点）。Task Scheduler 会优先将 Task 调度到数据所在节点，实现**数据本地性（Data Locality）**，减少网络传输，提升性能。

**容错机制：** 当某个节点失败时，Spark 根据 RDD 的 Lineage（血统）重新计算丢失的分区，而不需要整个数据集复制——这是 RDD"弹性"的核心。

---

## 题目三：Shuffle 原理——为什么 Shuffle 是 Spark 性能瓶颈？

**问题：** 什么是 Shuffle？Shuffle 在 Spark 中是如何实现的？为什么 Shuffle 被称为性能瓶颈？

**答案：**

### 什么是 Shuffle？

Shuffle 是 Spark 中**重新分区数据**的操作，会导致数据在不同 Executor 或不同节点之间流动。典型触发 Shuffle 的算子包括：

- **聚合类**：`groupByKey`、`reduceByKey`、`aggregateByKey`、`combineByKey`
- **排序类**：`sortByKey`、`orderBy`
- **连接类**：`join`、`cogroup`
- **重分区类**：`repartition`、`coalesce(shuffle=true)`

### Shuffle 的实现原理（HashPartitioner 为例）

以 `reduceByKey(_+_)` 为例，执行流程如下：

```
Stage 1（Map 阶段）
┌─────────────────────────────────────────────┐
│ Executor1: (K1,1), (K1,2), (K2,1)           │
│ Executor2: (K1,3), (K2,2), (K3,1)           │
└─────────────────────────────────────────────┘
           ↓ Shuffle Write（写入本地磁盘文件）
┌─────────────────────────────────────────────┐
│ Executor1: 按 K 写入不同文件                 │
│   file-for-K1: (K1,1), (K1,2), (K1,3)       │
│   file-for-K2: (K2,1), (K2,2)               │
│   file-for-K3: (K3,1)                       │
└─────────────────────────────────────────────┘
           ↓ Shuffle Read（从其他节点拉取数据）
Stage 2（Reduce 阶段）
┌─────────────────────────────────────────────┐
│ Executor1: 聚合 K1 的所有值 → (K1, 6)        │
│ Executor2: 聚合 K2 的所有值 → (K2, 3)        │
└─────────────────────────────────────────────┘
```

**Shuffle Write 阶段：**
- Map 任务完成后，将结果按照 key 的 hash 值写入本地磁盘（临时文件），每个 reduce partition 对应一个文件。
- 数据先写入内存缓冲区（`buffer size = 32KB`），写满后溢写到磁盘。
- 涉及大量磁盘 I/O 和序列化开销。

**Shuffle Read 阶段：**
- Reduce 任务启动后，去各个 Map 节点拉取属于自己的数据文件。
- 数据先拉取到 Executor 内存缓冲区，积累到一定程度后再执行聚合逻辑。
- 涉及大量网络 I/O。

### 为什么 Shuffle 是性能瓶颈？

| 原因 | 说明 |
|------|------|
| **磁盘 I/O** | Shuffle Write 必须将数据落盘，无法纯内存操作 |
| **网络 I/O** | 数据需要在节点间传输，跨机房时延迟显著 |
| **内存压力** | Shuffle 数据会占用堆内存，可能触发 GC |
| **序列化开销** | 数据需要序列化（Kryo/Java）才能网络传输 |
| **磁盘空间** | 大量中间文件占用磁盘，可能导致磁盘空间不足 |
| **启动开销** | 每个 Shuffle 产生新 Stage，需要重新启动 Task |

### 优化建议

1. **减少 Shuffle 次数**：避免过多 `repartition`，尽量在 Shuffle 前过滤数据。
2. **使用 Map 端聚合**：`reduceByKey` 优于 `groupByKey`，因为可以在 Map 端先做一次本地聚合。
3. **合理设置分区数**：分区数过少导致并行度不够，过多则小任务过多。一般设置为 core 数的 2-3 倍。
4. **使用广播变量**：小表广播可以避免 Shuffle。
5. **开启 Shuffle 合并**：`spark.shuffle.service.enabled = true`（External Shuffle Service）。

---

## 题目四：Spark 内存管理——堆内内存与堆外内存

**问题：** Spark 的内存管理模型是怎样的？Executor 的堆内内存和堆外内存是如何划分和使用的？

**答案：**

Spark 的 Executor 内存管理采用统一内存管理（Unified Memory Management，从 Spark 1.6 开始引入），将内存划分为**存储内存**和**执行内存**两大类，二者可以动态共享和借用。

### 堆内内存（On-Heap Memory）

Executor 运行在 JVM 堆上，由 `spark.executor.memory` 统一控制，划分为以下区域：

```
┌──────────────────────────────────────────────────────────┐
│                    Executor Heap                          │
│  ┌──────────────┬───────────────┬──────────────────────┐ │
│  │   User Memory │  Storage Memory │   Execution Memory │ │
│  │   (约 20%)    │    (约 45%)     │      (约 45%)       │ │
│  │              │   ↕ 可借用      │    ↕ 可借用          │ │
│  │  用户代码/RDD │  RDD缓存/Broadcast│  Shuffle/Join/Sort │ │
│  │  算子内部变量 │                │                      │ │
│  └──────────────┴─────────────────┴──────────────────────┘ │
│  ┌─────────────────────────────────────────────────────┐  │
│  │              Reserved Memory (约 300MB)              │  │
│  └─────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

| 区域 | 默认占比 | 用途 |
|------|---------|------|
| **Reserved Memory** | 300MB | 系统预留，存储 Spark 内部元数据 |
| **User Memory** | 20% | 用户代码、RDD 算子内部变量、RDD 依赖信息等 |
| **Storage Memory** | 弹性（默认 50%） | RDD 缓存（`cache()`、`persist()`）、广播变量、Task 结果 |
| **Execution Memory** | 弹性（默认 50%） | Shuffle 排序、Hash 聚合、Join 的 Hash 表等 |

### 堆外内存（Off-Heap Memory）

通过 `spark.memory.offHeap.enabled = true` 开启，堆外内存**不经过 JVM 堆**，直接使用操作系统物理内存。用于：
- `Off-Heap` 模式的 `Tungsten` 编码数据（避免 JVM GC 开销）
- 广播变量的存储（通过 `spark.sql.autoBroadcastJoinThreshold` 控制）
- 减少 GC 暂停时间，适合对延迟敏感的场景

配置方式：
```bash
spark.executor.memoryOverhead=1g    # 堆外内存大小
spark.memory.offHeap.enabled=true
spark.memory.offHeap.size=2g
```

### 存储内存与执行内存的动态借用机制

两大区域的边界是动态浮动的：
- 当 Storage Memory 不足时，可以借用 Execution Memory。
- 当 Execution Memory 不足时，可以借用 Storage Memory。
- 借用后需要归还：当一方内存紧张时，会触发对方的 Spill（溢写磁盘）。

### 内存分配策略

```scala
// RDD 缓存默认使用 Storage Memory
val rdd = sc.textFile("hdfs://...").cache()  // 放入 Storage Memory

// 自定义 StorageLevel 控制缓存位置和方式
rdd.persist(StorageLevel.MEMORY_AND_DISK_SER)
```

### 常见内存问题

1. **OOM（OutOfMemoryError）**：Storage Memory 占用过多导致 Execution Memory 不足，Shuffle 时失败。
2. **GC 频繁**：堆内对象过多导致 Full GC，需要优化数据结构或增加堆外内存。
3. **数据倾斜**：某 Partition 数据量远超其他，导致单节点 OOM。

---

## 题目五：广播变量（Broadcast Variable）——原理、优势与使用场景

**问题：** 什么是广播变量？Spark 为什么引入广播变量？它的工作原理是什么？有哪些使用注意事项？

**答案：**

### 什么是广播变量？

广播变量是 Spark 提供的一种**只读、一次性分发**的共享变量机制。通过广播变量，可以将一份只读数据（lookup 表、配置信息、小表数据）高效地分发到集群的每个 Executor 上，而不是每个 Task 都复制一份。

```python
# 普通变量：每个 Task 都会复制一份
broadcast_factor = 2
rdd.map(lambda x: x * broadcast_factor)  # ❌ 每个 task 都复制

# 广播变量：整个 Executor 共享一份
broadcast_factor = sc.broadcast(2)
rdd.map(lambda x: x * broadcast_factor.value)  # ✅ Executor 级别共享
```

### 工作原理

```
普通变量分发方式：
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Task 1     │    │  Task 2     │    │  Task 3     │
│  copy: 2    │    │  copy: 2    │    │  copy: 2    │
└─────────────┘    └─────────────┘    └─────────────┘
每个 Task 独立复制，网络传输 N 份

广播变量分发方式：
┌────────────────────────────────────────────────────────┐
│                 Executor（共享同一份）                   │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐          │
│  │  Task 1   │  │  Task 2   │  │  Task 3   │          │
│  │ 使用相同  │  │ 使用相同  │  │ 使用相同  │          │
│  │ broadcast │  │ broadcast │  │ broadcast │          │
│  └───────────┘  └───────────┘  └───────────┘          │
│                    broadcast = 2                        │
└────────────────────────────────────────────────────────┘
只传输 1 份，Executor 内所有 Task 共享
```

### 广播变量的优势

| 优势 | 说明 |
|------|------|
| **减少网络传输** | 大数据（如 lookup 表）只传输一次到每个 Executor，而不是每个 Task 传输一次 |
| **降低内存占用** | 每个 Executor 只需存储一份，而非每个 Task 存储一份 |
| **加速 Join** | 小表广播（Broadcast Join）可以避免 Shuffle，直接在 Map 端做 Join |
| **支持大对象** | Spark 默认限制广播变量不超过 8MB（`spark.sql.autoBroadcastJoinThreshold`），超过则不广播 |

### 广播 Join 的原理

当 Spark 检测到小表可以完全放入广播变量时，会采用 **Broadcast Hash Join**：

```sql
-- 小表被广播，大表正常扫描，在 Map 端做 Join
SELECT * FROM big_table JOIN small_table ON big_table.id = small_table.id
-- Spark 自动将 small_table 广播到每个 Executor
```

执行流程：
1. Driver 将小表数据收集（collect）到 Driver 内存。
2. 将小表数据封装为广播变量，分发到各个 Executor。
3. 大表数据在各自的 Executor 上被扫描，与本地的广播变量做 Hash Join。
4. **无 Shuffle、无排序**，性能极高。

### 使用注意事项

```python
# 1. 只读：广播后不能修改
broadcast_var = sc.broadcast([1, 2, 3])
broadcast_var.value.append(4)  # ⚠️ 警告：修改不会生效，且不安全

# 2. 及时释放：不再使用时应销毁
broadcast_var.unpersist()  # 销毁 Executor 上的副本

# 3. 广播大表要评估大小
# 小表 < spark.sql.autoBroadcastJoinThreshold（默认 10MB）自动广播
# 超过则需要手动广播或使用其他 Join 策略

# 4. 广播变量的序列化
# 广播变量必须可序列化（Kryo/Java）
```

### 典型使用场景

1. **小表广播 Join**：维度表 join 事实表，数据量小于广播阈值时优先使用。
2. **配置信息共享**：全局配置、IP 白名单、规则字典等。
3. **机器学习特征**：训练好的模型参数、特征映射表。
4. **跨 Task 共享只读数据**：避免重复传输，提高效率。

---

> 📌 **收藏此文档，持续更新中！**  
> GitHub 仓库：[qq286158530/interview-questions](https://github.com/qq286158530/interview-questions)  
> 贡献面试题：欢迎提交 PR，一起打造最全面的面试题库！
