# HBase 面试题（2026-08-13）

> 整理自：[GitHub - wangzhiwubigdata/God-Of-BigData](https://github.com/wangzhiwubigdata/God-Of-BigData/blob/master/%E9%9D%A2%E8%AF%95%E7%B3%BB%E5%88%97/HBase%E9%9D%A2%E8%AF%95%E9%A2%98%E6%95%B4%E7%90%86/HBase.md)

---

## 题目一：HBase 的架构组成及各组件作用

### 参考答案

HBase 采用 **ZooKeeper + HMaster + HRegionServer** 的分布式架构：

| 组件 | 作用 |
|------|------|
| **ZooKeeper** | 负责监控 HRegionServer 的上下线状态，实现 HBase 的故障转移（Failover） |
| **HMaster** | 负责表的 CRUD 操作（创建表、修改表等），以及 Region 的分配和调度 |
| **HRegionServer** | 负责直接处理客户端的读写请求，一个 RegionServer 可以管理多个 Region |

**Region 内部结构：**
- 每个 Region 按列族划分为多个 **Store**
- 每个 Store 包含一个 **MemStore**（写缓存）和多个 **StoreFile**（HFile）

**数据写入流程：**
1. Client 访问 ZooKeeper，获取 Meta 表位置
2. 向目标 HRegionServer 发起写入请求
3. 先写入 **WAL**（预写日志）防止数据丢失
4. 再写入 **MemStore**（内存缓冲区）
5. MemStore 达到阈值后 flush 成 **HFile**

---

## 题目二：HBase 中 MemStore 和 HFile 的作用是什么？

### 参考答案

**MemStore（写缓存）：**
- HBase 为了保证随机读取性能，HFile 内的 RowKey 必须有序
- 因此数据不能直接写入 HFile，而是先写入 MemStore（内存缓冲区）
- MemStore 支持随机插入，保证所有操作在内存中有序
- MemStore 达到阈值后，会 flush 到磁盘生成 HFile

**HFile（底层存储文件）：**
- HFile 是 HBase 数据存储的底层文件格式，基于 HDFS
- 每个 MemStore flush 时生成一个 HFile
- HFile 过多时触发 **Compaction** 合并操作

**为什么需要 MemStore？**
1. **写入性能**：利用 Hadoop 顺序写入大文件的性能优势
2. **数据持久化前的缓冲**：MemStore 数据在内存中
3. **故障恢复**：通过 WAL（预写日志）机制保证数据不丢失

**WAL 机制：**
- WAL 文件是追加、顺序写入的，每个 RegionServer 只有一个
- 同一 RegionServer 上所有 Region 写入同一个 WAL
- 宕机时，通过重放 WAL 恢复 MemStore 中未持久化的数据

---

## 题目三：描述 HBase 的 RowKey 设计原则

### 参考答案

HBase 查询只提供两种方式：**get（按 RowKey 查询）** 和 **scan（范围查询）**，所以 RowKey 设计至关重要。

### 三大原则

**1. 长度原则（越短越好）**
- RowKey 是二进制码流，建议 **不超过 16 字节**
- 原因：
  - HFile 中按 KeyValue 存储，RowKey 过长会占用大量空间
  - MemStore 缓存效率降低，系统无法缓存更多数据
  - 操作系统 64 位，8 字节对齐，16 字节是最佳倍数

**2. 散列原则（避免热点）**
- 如果 RowKey 是时间戳递增，应将高位作为散列字段
- 目的：将数据均衡分布到不同 RegionServer，实现负载均衡
- 如果按时间正序排列，所有新数据会集中在一个 RegionServer 上，造成热点

**3. 唯一原则**
- RowKey 必须在设计上保证唯一性

### 业务性设计举例

- **多条件查询**：如果查询条件较多，RowKey 应能支持多条件查询
- **最新数据优先**：可采用 `Long.Max - 时间戳` 的方式，使 RowKey 递减排列

---

## 题目四：HBase 的 Compaction 机制是什么？有哪几种类型？

### 参考答案

**Compaction 的作用：**
1. 合并多个 HFile 成一个文件
2. 清除过期、多余版本的数据
3. 提高读写数据的效率

**两种 Compaction 类型：**

| 类型 | 说明 |
|------|------|
| **Minor Compaction** | 部分文件的合并，包含过期版本清理（minVersion=0 且设置 ttl），但**不做删除数据和多版本数据的清理** |
| **Major Compaction** | 对 Region 下的**所有 StoreFile** 执行合并，最终整理成一个文件 |

**触发条件：**
- 当 StoreFile 数量达到 `hbase.hstore.compactionThreshold`（默认 3）时触发
- Major Compaction 会删除数据、合并多版本，推荐在闲时手动执行

---

## 题目五：HBase 读写数据的流程是怎样的？

### 参考答案

**读数据流程：**
1. Client 先访问 **ZooKeeper**，获取 Meta 表所在位置
2. Client 访问 Meta 表所在的 HRegionServer，读取 Meta 元数据
3. Client 根据元数据访问对应的 HRegionServer
4. HRegionServer 扫描 **MemStore** 和 **StoreFile** 查询数据
5. 返回结果给 Client

**写数据流程：**
1. Client 访问 ZooKeeper，找到 Meta 表，获取元数据
2. 确定数据对应的 HRegion 和 HRegionServer
3. Client 向目标 HRegionServer 发起写入请求
4. **先写入 WAL**（预写日志），再写入 **MemStore**
5. WAL 和 MemStore 均写入成功后，数据写入成功
6. MemStore 达到阈值后，flush 到 **StoreFile**
7. StoreFile 过多时触发 **Compact** 合并
8. Region 过大时触发 **Split** 分裂

---

## 参考来源

1. [GitHub - God-Of-BigData HBase面试题整理](https://github.com/wangzhiwubigdata/God-Of-BigData/blob/master/%E9%9D%A2%E8%AF%95%E7%B3%BB%E5%88%97/HBase%E9%9D%A2%E8%AF%95%E9%A2%98%E6%95%B4%E7%90%86/HBase.md)
2. [博客园 - 史上最全HBase面试题](https://www.cnblogs.com/crazymakercircle/p/17638626.html)
3. [CSDN - HBase经典面试常问问题汇总](https://blog.csdn.net/godlovedaniel/article/details/113242751)

---

*本面试题整理由 AI 自动生成，仅供参考学习*
