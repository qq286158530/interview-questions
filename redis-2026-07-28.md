# Redis 面试题整理

> 📅 日期：2026-07-28  
> 🏷️ 来源：[小林coding - 图解Redis](https://xiaolincoding.com/redis/base/redis_interview.html)

---

## 题目一：Redis 和 Memcached 有什么区别？为什么 Redis 比 Memcached 更适合作为 MySQL 的缓存？

### 参考答案

**Redis 与 Memcached 共同点：**

- 都是基于内存的数据库，一般都用来当做缓存使用
- 都有过期策略
- 两者的性能都非常高

**Redis 与 Memcached 区别：**

| 区别 | Redis | Memcached |
|------|-------|-----------|
| 数据类型 | 支持丰富数据类型（String、Hash、List、Set、ZSet 等） | 只支持最简单的 key-value 类型 |
| 持久化 | 支持 RDB 和 AOF 两种持久化方式，数据可恢复 | 无持久化功能，数据全部存在内存 |
| 集群 | 原生支持集群模式 | 无原生集群支持，需依赖客户端实现分片 |
| 高级特性 | 支持发布订阅、Lua 脚本、事务等 | 不支持 |

**为什么用 Redis 作为 MySQL 的缓存？**

1. **高性能**：用户第一次访问 MySQL 数据较慢（从硬盘读取），将数据缓存到 Redis 后，下一次访问直接从内存获取，速度极快。
2. **高并发**：单台 Redis 的 QPS（每秒处理请求数）是 MySQL 的 10 倍，Redis 单机 QPS 能破 10w，而 MySQL 单机很难破 1w。

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 各自有什么优缺点？混合持久化是怎么工作的？

### 参考答案

Redis 共有三种数据持久化方式：

### 1. AOF 日志（Append Only File）

每执行一条写操作命令，就把该命令以追加的方式写入到一个文件里。Redis 重启时读取该文件记录的命令，逐一执行来恢复数据。

**AOF 写回策略（appendfsync 配置项）：**

| 策略 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| Always | 每次写命令执行完后同步写回硬盘 | 数据不丢失 | IO 开销大 |
| Everysec | 每次写命令先写入内核缓冲区，每秒刷盘一次 | 性能适中，最多丢失 1 秒数据 | 默认推荐策略 |
| No | 由操作系统控制何时刷盘 | 性能最好 | 数据丢失不可控 |

**AOF 重写机制**：当 AOF 文件过大时，后台子进程 `bgrewriteaof` 会读取数据库中所有键值对，用一条命令记录到新的 AOF 文件，实现压缩。

### 2. RDB 快照（Redis Database）

将某一时刻的内存数据，以二进制的方式写入磁盘。

- `save` 命令：在主线程执行，会阻塞
- `bgsave` 命令：创建子进程生成 RDB 文件，避免阻塞
- 底层利用 **写时复制（COW）** 技术，fork 子进程时共享父进程内存

**RDB 优点**：恢复数据速度快
**RDB 缺点**：快照频率难把握，频率太低丢失数据多，频率太高影响性能

### 3. 混合持久化（Redis 4.0+）

集成了 RDB 和 AOF 的优点：

- AOF 重写时，先将以 RDB 格式写入 AOF 文件前半部分
- 再将增量命令以 AOF 格式写入后半部分
- 启动时加载 RDB 部分快速恢复，再加载 AOF 部分补全增量数据

**优点**：重启恢复速度快 + 丢失数据少  
**缺点**：AOF 可读性差；兼容性差，4.0 之前版本不支持

---

## 题目三：Redis 是单线程吗？为什么单线程的 Redis 还能达到每秒数万 QPS？

### 参考答案

**Redis 单线程指的是**：「接收客户端请求 → 解析请求 → 进行数据读写等操作 → 发送数据给客户端」这一流程由一个线程（主线程）完成。

**但 Redis 并不完全是单线程**，它还有后台线程：
- `bio_close_file`：异步关闭文件
- `bio_aof_fsync`：异步 AOF 刷盘
- `bio_lazy_free`：异步释放内存（lazyfree）
- `io_thd_1/2/3`（Redis 6.0+）：多线程处理网络 I/O

**Redis 6.0 之前核心工作为什么用单线程？**
- Redis 的大部分操作都在内存中完成，瓶颈是内存大小和网络带宽，而非 CPU
- 单线程避免了多线程切换开销和锁竞争问题
- 使用 I/O 多路复用机制（epoll）处理大量 Socket 请求

**Redis 为什么这么快？**

1. **内存操作**：数据在内存中，读写速度极快
2. **高效数据结构**：SDS（动态字符串）、跳表、压缩列表等内部结构经过优化
3. **单线程模型**：无锁竞争、无线程切换开销
4. **I/O 多路复用**：一个线程处理多个 IO 流（select/epoll），高效监听处理海量并发

Redis 6.0 之后引入了多线程 I/O 用于分担网络读写的压力，但**命令执行仍然是单线程**。

---

## 题目四：Redis 的过期删除策略和内存淘汰策略有什么区别？分别有哪些实现？

### 参考答案

### 一、过期删除策略

Redis 使用「惰性删除 + 定期删除」两种策略配合使用：

**1. 惰性删除（Passive/Lazy Expiration）**
- 每次访问 key 时，才检查是否过期，过期则删除
- **优点**：对 CPU 时间友好，只占用很少资源
- **缺点**：过期 key 长期占用内存，对内存不友好

**2. 定期删除（Active Expiration）**
- 每隔一段时间，随机从数据库中取出 20 个 key 检查
- 删除已过期的 key
- 如果已过期 key 超过 5 个（25%），继续随机抽取；低于 25% 则停止
- 单次执行时间上限默认 25ms，避免阻塞

**优点**：删除一部分过期数据，释放内存  
**缺点**：执行频率难以确定

### 二、内存淘汰策略（当内存达到 maxmemory 上限时）

Redis 8.0 提供了 8 种淘汰策略：

| 策略 | 说明 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 从已设置过期时间的 key 中，淘汰最近最少使用的 |
| `volatile-lfu` | 从已设置过期时间的 key 中，淘汰最不经常使用的 |
| `volatile-ttl` | 从已设置过期时间的 key 中，淘汰即将过期的 |
| `volatile-random` | 从已设置过期时间的 key 中，随机淘汰 |
| `allkeys-lru` | 从所有 key 中，淘汰最近最少使用的 |
| `allkeys-lfu` | 从所有 key 中，淘汰最不经常使用的 |
| `allkeys-random` | 从所有 key 中，随机淘汰 |

**常用场景**：
- 作为缓存时，推荐 `volatile-lru` 或 `allkeys-lru`
- 作为持久化存储时，使用 `noeviction`

---

## 题目五：Redis 如何实现服务高可用？哨兵模式是如何工作的？集群脑裂问题怎么解决？

### 参考答案

### 一、Redis 高可用方案

**1. 主从复制（一主多从）**
- 主服务器进行读写操作，从服务器只读
- 主服务器将写操作同步给从服务器
- 缺点：从服务器命令复制是异步的，无法保证强一致性

**2. 哨兵模式（Sentinel）**
- 监控主从服务器运行状态
- 自动完成主从故障转移
- 核心功能：
  - **监控**：定期向主从服务器发送 PING，检测是否下线
  - **自动故障转移**：主节点客观下线后，选举从节点为新主节点
  - **通知**：向客户端发布故障转移事件

**3. 切片集群（Redis Cluster）**
- 使用 16384 个哈希槽（Hash Slot）分布数据
- 每个 key 通过 CRC16 取模映射到对应槽位
- 支持动态扩容和数据迁移

### 二、集群脑裂问题及解决方案

**什么是脑裂？**
主节点网络突然断开，但与客户端网络正常。客户端继续向旧主节点写入数据，哨兵以为主节点挂了，选举出新主节点，导致集群出现两个主节点。网络恢复后，旧主节点降为从节点，全量同步清空本地数据，造成之前写入的数据丢失。

**解决方案：配置 `min-slaves-to-write` 和 `min-slaves-max-lag`**

```conf
# 主节点必须至少有 1 个从节点连接
min-slaves-to-write 1
# 主从复制延迟不能超过 10 秒
min-slaves-max-lag 10
```

**原理**：当从节点数量不足或复制延迟过长时，主节点拒绝接收客户端写请求，即使原主节点网络假故障也无法写入数据，从根本上避免脑裂产生的数据丢失。

---

> 📚 更多面试题：[小林coding - 图解Redis](https://xiaolincoding.com/redis/base/redis_interview.html)
