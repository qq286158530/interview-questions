# Redis 面试题 — 2026-08-19

> 本文档整理了 5 道高质量 Redis 面试题，涵盖数据类型、持久化、缓存、集群等核心知识点。

---

## 题目一：Redis 有哪几种数据结构？它们的典型应用场景是什么？

### 参考答案

Redis 支持五种核心数据结构：

| 数据结构 | 命令示例 |底层实现 | 典型应用场景 |
|---------|---------|---------|------------|
| **String** | `SET/GET` | SDS（简单动态字符串） | 缓存、计数器、分布式锁、Session |
| **Hash** | `HSET/HGET` | 压缩列表 或 字典 | 对象存储、购物车、商品信息 |
| **List** | `LPUSH/RPOP` | 压缩列表 或 双向链表 | 消息队列（最新消息列表）、任务队列、关注列表 |
| **Set** | `SADD/SMEMBERS` | 整数集合 或 字典 | 标签系统、去重、好友关系、共同关注 |
| **Zset（Sorted Set）** | `ZADD/ZRANGE` | 压缩列表 或 跳表+字典 | 排行榜、延时队列、有序消息 |

**进阶补充：**
- **HyperLogLog**：用于基数统计，如 UV 统计，内存仅 12KB
- **Bitmap**：位图，适合签到、状态监控
- **Geospatial**：GEO 类型，适合附近的人、打车定位
- **Stream**：流式数据结构，替代 List 实现消息队列

> **来源：** [Redis 官方文档 - Data Types](https://redis.io/docs/data-types/)  
> **来源：** [CSDN - Redis五种数据结构的实现原理](https://blog.csdn.net/qq_35190492/article/details/108556989)

---

## 题目二：Redis 的持久化机制是什么？RDB 和 AOF 有什么区别？如何选择？

### 参考答案

Redis 提供了两种持久化机制：

#### RDB（Redis Database Backup）
- **原理**：在指定时间间隔内，生成内存数据的**快照（Snapshot）**，保存为 `dump.rdb` 文件
- **触发方式**：
  - 手动：`BGSAVE`（后台异步）或 `SAVE`（同步，会阻塞）
  - 自动：`save m n`（m秒内n次修改则触发）
- **优点**：恢复快，适合大规模数据备份
- **缺点**：可能丢失最后一次快照后的数据

#### AOF（Append Only File）
- **原理**：记录所有写操作命令，以日志追加方式写入 `appendonly.aof`
- **同步策略**：
  - `appendfsync always`：每次写都同步（最安全，性能最差）
  - `appendfsync everysec`：每秒同步（默认，推荐）
  - `appendfsync no`：由操作系统决定何时同步（性能最好，可能丢数据）
- **优点**：数据安全性更高
- **缺点**：文件体积大，恢复速度慢

#### 混合持久化（Redis 4.0+）
- 开启 `aof-use-rdb-preamble yes`
- AOF 重写时先用 RDB 格式写入，再追加增量 AOF 命令
- 兼顾快速恢复和大数据安全性

#### 如何选择？
| 场景 | 推荐策略 |
|------|---------|
| 能接受数分钟数据丢失 | 仅 RDB |
| 要求数据不丢失 | AOF everysec + RDB 混合 |
| 大数据量高性能 | RDB + AOF everysec |

> **来源：** [Redis 官方文档 - Persistence](https://redis.io/docs/management/persistence/)  
> **来源：** [腾讯云 - Redis 持久化详解 RDB/AOF](https://cloud.tencent.com/developer/article/1592230)

---

## 题目三：Redis 的过期键删除策略有哪几种？内存淘汰机制有哪些？

### 参考答案

#### 一、过期键删除策略

| 策略 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **定时删除** | 设置过期时间时同时创建定时器，到期立即删除 | 内存友好，及时释放 | CPU 消耗高，影响响应时间 |
| **惰性删除** | 访问键时检查是否过期，过期则删除 | CPU 友好 | 内存泄漏（过期键大量堆积） |
| **定期删除** | 每隔一段时间扫描expires字典，删除过期键 | 折中方案 | 实现复杂，需合理配置时间/频率 |

**Redis 采用的是惰性删除 + 定期删除的组合策略。**

#### 二、内存淘汰机制（当内存达到 maxmemory 时）

| 策略 | 说明 |
|------|------|
| `noeviction` | 不淘汰，返回错误（默认） |
| `volatile-lru` | 从已设置过期时间的键中淘汰最近最少使用的 |
| `allkeys-lru` | 从所有键中淘汰最近最少使用的 |
| `volatile-lfu` | 从已设置过期时间的键中淘汰最不经常使用的 |
| `allkeys-lfu` | 从所有键中淘汰最不经常使用的 |
| `volatile-random` | 从已设置过期时间的键中随机淘汰 |
| `allkeys-random` | 从所有键中随机淘汰 |
| `volatile-ttl` | 淘汰即将过期的键（TTL 最小的） |

**实际应用建议：**
- 缓存场景常用 `allkeys-lru` 或 `volatile-lru`
- 使用 `maxmemory` 限制内存，并设置合理的淘汰策略
- 热点数据预加载，结合监控避免频繁淘汰

> **来源：** [Redis 官方文档 - Expires](https://redis.io/docs/data-types/expires/)  
> **来源：** [Redis 官方文档 - Eviction](https://redis.io/docs/reference/eviction/)

---

## 题目四：Redis 主从复制原理是什么？哨兵模式解决了什么问题？

### 参考答案

#### 主从复制原理

```
Master ────────> Slave1
   │                  │
   └───────> Slave2
```

**复制过程：**
1. **建立连接**：从库向主库发送 `SYNC`（Redis 2.8之前）或 `PSYNC`（2.8之后，支持断点续传）
2. **全量同步**：主库执行 `BGSAVE`，生成 RDB 快照，同时用缓冲区记录后续写命令
3. **传输快照**：将 RDB 文件发送给从库
4. **增量同步**：从库加载完 RDB 后，主库再将缓冲区中的命令传给从库
5. **命令传播**：此后主库每执行一个写命令，就异步发送给从库

**关键配置项：**
- `replicaof <masterip> <masterport>`（从库配置）
- `masterauth <password>`（主库密码认证）

#### 哨兵模式（Sentinel）

哨兵是 Redis 的高可用方案，解决以下问题：

| 问题 | 哨兵解决方案 |
|------|------------|
| 主库宕机 | 自动故障转移（Failover），选举新主库 |
| 主观下线 | `sentinel monitor <name> <ip> <port> <quorum>` 多哨兵投票判定 |
| 客观下线 | 超过 quorum 数量的哨兵认为主库不可用，触发故障转移 |
| 从库切换 | 自动重配置所有从库指向新主库 |
| 通知客户端 | 客户端通过哨兵获取当前主库地址 |

**哨兵集群：**
- 至少需要 3 个哨兵节点（奇数），避免脑裂
- `quorum` 设置为 2，保证多数节点认定

> **来源：** [Redis 官方文档 - Replication](https://redis.io/docs/management/replication/)  
> **来源：** [Redis 官方文档 - Sentinel](https://redis.io/docs/management/sentinel/)

---

## 题目五：如何解决 Redis 缓存穿透、缓存击穿、缓存雪崩问题？

### 参考答案

这是 Redis 面试中最经典的高频问题，被称为"缓存三剑客"。

#### 1. 缓存穿透（Cache Penetration）

**问题**：大量请求查询**不存在的数据**（如恶意刷接口），绕过缓存直接打到数据库。

**解决方案：**
- **布隆过滤器（BloomFilter）**：在访问缓存前用布隆过滤器判断 key 是否存在
- **缓存空值**：即使数据不存在，也缓存一个空值（如 `NULL`），设置较短 TTL
- **参数校验**：接口层增加参数合法性校验
- **黑名单拦截**：IP/用户维度的访问频率限制

#### 2. 缓存击穿（Cache Breakdown）

**问题**：某个**热点 key 过期**，大量并发请求同时击穿到数据库。

**解决方案：**
- **互斥锁（分布式锁）**：只允许一个线程重建缓存，其他线程等待
- **热点数据永不过期**：不设 TTL，通过后台异步更新
- **逻辑过期**：不设置真正过期时间，存一个逻辑过期字段，访问时判断并后台更新

```python
# 互斥锁伪代码
if cache过期:
    lock = redis.setnx(lock_key, "1", expire=10)
    if lock:
        data = db.query()
        redis.setex(cache_key, ttl, data)
        redis.delete(lock_key)
    else:
        time.sleep(0.1)
        return redis.get(cache_key)  # 等待并重试
```

#### 3. 缓存雪崩（Cache Avalanche）

**问题**：大量 key **集中过期** 或 **Redis 宕机**，导致数据库压力骤增。

**解决方案：**
- **过期时间分散**：给过期时间加随机偏移量，避免集中过期
- **多级缓存**：本地缓存 + Redis 缓存，减少 Redis 压力
- **Redis 高可用**：部署哨兵/集群，避免单点故障
- **流量降级**：数据库压力过大时，返回兜底数据
- **提前演练**：使用 `flushall` 测试系统韧性

```
# 过期时间随机化示例
ttl = base_ttl + random.randint(0, 3600)  # 基础TTL + 0~1小时随机
```

| 问题 | 核心矛盾 | 核心解决思路 |
|------|---------|------------|
| 穿透 | 请求查不存在的数据 | 布隆过滤器 + 空值缓存 |
| 击穿 | 热点 key 过期瞬间 | 互斥锁 / 逻辑过期 |
| 雪崩 | 大量 key 集中过期 / 宕机 | 过期随机化 + 高可用部署 |

> **来源：** [美团技术团队 - Redis 缓存问题与解决方案](https://tech.meituan.com/2017/03/17/cache.html)  
> **来源：** [阿里云 - Redis 缓存穿透、击穿、雪崩详解](https://help.aliyun.com/document_detail/199500.html)

---

*整理日期：2026-08-19 | 由 AI 自动整理自 Redis 官方文档及优质技术社区*
